# wombat — Architect's Brief

> The altitude **above** the contract. `planning/contract.yaml` is the source of truth (DEC-*, governance, epics, tickets); this file is the one-page spine, the live blockers, and the decision log. Maintained by the `architect` agent. Last synced: 2026-07-30 (contract **v2.158** — **DEC-66 USER-MODELING IS THE PRODUCT SPINE (Jim frame ruling)**: ONE user model, two halves — the existing gate-facing empirical-behavioral half (DEC-11/15) + the new mouth-facing declarative half (DEC-65); the chat wedge is v1, NOT the destination — EP-37 (parent FEAT-3) records observational growth (TK-299 dream_derive minted, DISPATCH GATED by DEF-13 until the DEC-65 arc lands + Jim sees the first dream_facts pass); UserFactsStore `source` column = the provenance spine (dream|derived|behavior|told), builders must not collapse it. Board 208/216; TK-292..TK-298 the only dispatchable batch. Same day, v2.156 — **DEC-65 THE COMPANION ARC MINTED**: Jim feedback ("too stiff... Getting to know me is important too") → ISS-29 → DEC-65 + DEF-12 + TK-292..TK-298 — a FIFTH Mouth.CHAT companion register (assistant_name + new wombat_user_name), item_kind selection at ComposeStage, durable wombat_user_facts (mig 011) + wombat_chat_turns dream-input ledger (mig 012), source-side known_user_context stamp on BOTH chat surfaces (closes DEC-64's typed-chat follow-up), nightly off-path dream_facts extraction (DEC-23 custody), one charter accuracy sentence (never weakened). Batch order TK-292→293→294→295→296→297→298. Previous sync: 2026-07-21, contract **v2.155** — **BOARD CLOSED 208/208: the full DEC-64 walkie-talkie arc LANDED — TK-288 (LastSpokenRegister + on_spoken seam), TK-289 (replying_to payload stamp), TK-290 (deterministic grounding bundle), TK-291 (e2e proof, zero src, byte-untouched neighbors). BATCH-VERIFY PENDING — ONE Opus review audits the whole four-ticket diff; then the operator runtime relaunch + live walkie-talkie ear-check picks up DEC-60/61/62/63/64. Prior v2.150 — JIM RULING RECORDED + THE CONVERSATION ARC MINTED (board reopens 204/208): the Steward converses WALKIE-TALKIE style (ISS-28 → DEC-64; Jim verbatim intent — no VAD, keep the PTT button, respond like a walkie talkie, leave room for VAD later). PTT stays the ONLY mic trigger AND the reply channel (source-confirmed zero app changes — app/src/ptt.ts → drop dir → ASRSource IS the loop); Q-97 UNTOUCHED; auto-listen/VAD = DEF-11 with the new on_spoken seam pinned as the future attachment point. The two research-verified gaps close MOUTH-SIDE on the existing payload wire (Q-50 payload-verbatim UPHELD — context IS payload): TK-288 (EP-30, LastSpokenRegister + on_spoken hooks at SpeakSink post-adapter.speak + BriefDeliverStage on voice_spoken; TTL 120.0s + 600-char cap pinned constants, no knob) → TK-289 (EP-29, ASRSource FOURTH optional ctor hook context_hook stamps replying_to at payload-build, reserved keys unoverridable, ZERO compose diff via format_payload_fields) → TK-290 (EP-29, build_voice_context: bounded deterministic today-gcal + 5-recent-gmail-subjects from ExternalItemStore; DEC-26 bodies structurally absent; store failure = no keys + one WARNING, CON-3) → TK-291 (EP-29, e2e FakeModel-spy proof + DEC-62/57/60 + brief/draft/reflection byte-untouched pins). Rejected on DEC-64: VAD (Jim), T1 free tool loop (mouth-not-brain + taint tier), T2 intent legs (unscheduled follow-up), compose-side injection, multi-turn history, gate re-pathing. Typed-chat grounding = recorded follow-up. NEXT — dispatch TK-288 → 289 → 290 → 291; the DEC-63 pair's Opus batch-verify still owed; operator relaunch picks up DEC-60/61/62/63**; prior v2.149 — **TK-287 DONE, BOARD HAD CLOSED 204/204: the DEC-63 pair complete (FlushDayLatch + SeenLedger) — the repeating-digest class structurally dead at both seams; batch-verify pending**; prior v2.148 — **TK-286 DONE (board 203/204): DEC-63a exactly-once source ingest LANDED — persisted SeenLedger (pg wombat_seen_events, migration 010, sha256 canonical-payload fingerprint) behind a DedupingEnqueuer wrapped ONLY around build_source_registry's queue at the composition root; an unchanged already-ingested payload never re-enqueues after ack-delete (restart-proof, proven on throwaway pg incl. the live-defect repro), a CHANGED fingerprint is the sole legitimate re-entry, any inner raise propagates UNRECORDED (at-least-once preserved); schema_preflight gains the ninth ensure_all_schemas call per the v2.147 ruling so the production table exists at boot; queue/registry/pollers and DEC-57 byte-untouched; batch-verify pending (one Opus review of the DEC-63 pair after TK-287); NEXT — TK-287 (FlushDayLatch), then the operator relaunch picks up DEC-60/61/62/63 together**; prior v2.145 — **TK-284 DONE, BOARD RE-CLOSED 202/202: DEC-62 capability honesty is fully landed — the pinned CAPABILITY_CHARTER (single-source src/wombat/persona/capabilities.py) rides the COMPOSE guard suffix + compose.py's frozen fallback byte-equally, speech_shape inherits it at construction (v2.141 ruling), and the AC1 internals leak-check is re-scoped to the payload-derived user message only (v2.143 ruling); TK-285's live proof already on record (alarm ask → honest can't-do, control turn unaffected); batch-verify pending (one Opus review of the DEC-62 pair); remaining items operator-only (runtime relaunch, hands-free ear-check, GCP publish)**; prior v2.139 — TK-283 DONE, BOARD CLOSED 200/200: DEC-61's mouth_model_timeout_seconds (10.0, wombat_params.yaml v8) injected at all five model-calling mouth sites from the composition root, ctor defaults untouched; batch-verify pending; remaining items are operator-only (runtime relaunch, hands-free ear-check, GCP publish)**; prior v2.137 — DEC-61 mouth-wait tunable ruled, TK-283 minted, board 199/200; prior v2.129 — **DEC-60 VOICE TURNS ARE CONVERSATION (Jim directive): every non-command ASR transcript is an ItemKind.CHAT turn with a voice_turn origin marker (the ISS-23 absorption class closed for the SECOND origin); voice-originated replies SPEAK even when held (supersedes the DEC-57 quiet-held clause IN PART for voice origin only); pane echo via a bounded broker voice-turn ledger + token-guarded GET /chat/voice-turns polled by ChatPane; drop-dir pickup conversation-grade (asr 300s -> 2.0s, transcribe via asyncio.to_thread, never-silent scan logging); ISS-25 records the live defect pair (gate-absorbed transcripts at 15:24:41 + stranded WAVs). Board 194/199 — BUILD ORDER TK-278 -> TK-279 -> TK-282 -> TK-280 -> TK-281.** Prior v2.122 state: **THE CHAT ROUND-TRIP IS PROVEN LIVE END-TO-END** (driven test, boot 19:33:13: gate decision chat/hold/0.5225 → compose → DeepSeek 200 → reply IN THE PANE inside 30s, ZERO TTS — the DEC-57 quiet-held path exactly as ruled; ISS-23 closed operator-visibly; the last open item of the original 2026-07-19 E2E, MANUAL_TEST.md Part B, is complete). TK-273 first evidence: boot-time backlog flush observed (gmail items, hold, urgency=None — watch-note, benign-until-evidence; next composed recurrence names the mystery cluster). **ISS-24** → **TK-274** (P2, sole open ticket): the console guard's unanchored '-m wombat' regex false-positives on settings_app children and bricks app-first launches — end-or-whitespace anchor in both scripts (CIM trailing-space quirk honored). Persona watch-note: reply addressed the operator as 'wombat' — no name-awareness in compose; Jim's to raise (DEC-38 policy surface if so). Prior v2.121: **TK-273 DONE — BOARD CLOSES 190/190: ISS-23 gate-decision observability LANDED (EP-1), closing the DEC-57 v2.119 batch** — one INFO line per gate decision (gate_stage.py, after the entries pairing: item_id/item_kind/event_class/action/urgency/load, score fields from decision.items[0] when carried else None) and one per composed dispatch (compose_dispatch_router.py, after composer_name resolution incl. fallback); event_class via NEW pure export `resolve_event_class_for_item()` from user_model.py (Q-41 ruling-1 logic, `UserModel.resolve_event_class` delegates, zero behavior change); existing module loggers only — lines land in TK-259 per-boot custody, ids+enums only, NEVER payload text (caplog-asserted). Builder bar green (pytest 1680/92 pg-skipped, ruff, mypy 334 files); architect re-ran pytest pre-commit. **BATCH-VERIFY PENDING** — one Opus review audits the whole TK-272+TK-273 batch, then the operator re-drives the live chat round-trip; next boot the recurring compose cluster names its item in the log. Prior v2.120 — **TK-272 DONE: CHAT ALWAYS ANSWERS LANDED (DEC-57, EP-32)** — built exactly on the v2.119 rulings: gate/pipeline.py skips the pending-set add for a HOLD ItemKind.CHAT and returns GateDecision(HOLD, items=(scored,)) carrying the REAL score; additive optional `held_chat` bool (default False) on the surfaced_item/compose_request/composed_output artifact wires + additive accessors (plain-JSON-native, older artifacts unaffected); review_or_speak.py forwards a HOLD CHAT entry as a surfaced Transition with held_chat=True instead of the terminal hold_report ack (hold-silence stands byte-identically for every non-chat kind); speech_shape.py + sinks/speak.py fold held_chat into their EXISTING voice-off pass-through branches — held chat = quiet-by-design text-only (ZERO speech_shape model calls, no TTS, silent Done spoken=False degraded=False, never the Degraded branch); surfaced chat byte-identical to today; chat_reply.py ZERO diff (text delivery sits upstream of shaping). Builder self-ran the full bar green (pytest 1678/92 pg-skipped; throwaway-pg FULL suite 1791/11 live-gated incl. 2 NEW TK-272 e2e tests for AC1 held-chat + AC2 surfaced-chat on a real Gate + real WombatQueue + FakeModel spy + recording adapter); architect re-ran pytest pre-commit. **BATCH-VERIFY PENDING** — one Opus review audits the whole TK-272/TK-273 batch after both land, then the operator re-drives the live chat round-trip. NEXT: TK-273 (gate-decision observability), the sole open ticket. Prior v2.118 — **THE REAL ROOT CAUSE OF THE MISSING CHAT REPLIES: chat turns are GATE-ABSORBED** (source-proven: CHAT → EventClass.GENERIC (user_model.py:49), sub-0.75-threshold urgency → HOLD → Q-96/ISS-4 hold-silence ack into the pending set; re-driven test: item drained, ZERO DeepSeek calls). Premise corrections recorded append-only in ISS-21/ISS-22/Q-117 (every boot has ONE recurring compose cluster — the 18:00:13 cluster was NOT the driven reply; Q-117's route-back fired same-day and re-resolves INTO DEC-57). **DEC-57: chat is a SOLICITED exchange** — every chat turn earns a composed TEXT reply; the gate keeps FULL voice/interruption authority (held chat = quiet text-only, surfaced = full speak path); no pending-set absorption for chat; hold-silence stands verbatim for every non-chat kind; no new EventClass (deferral) → **TK-272** (P1, the routing) → **TK-273** (P2, gate-decision observability; the recurring per-boot item self-identifies next boot — leading suspect: flush over the pending-set backlog, which holds Jim's absorbed turns). TK-269/270/271 stand on their merits — none was the missing-reply cause. Prior v2.116: **BATCH SELECTED (no contract edit, v2.116 stands): TK-271 alone** — the sole open ticket (board 187/188); all broker seams re-verified live pre-build with ZERO drift (resolve() early-return-on-popped-future surface.py:137-141, discard() unconditional (None, ts) write :157, wait_for/discard timeout arm :377-379, LATE_SLOT_MAX_SIZE=128/TTL=900.0 :84-85, discard-path eviction :153-157, poll_late pop-on-read :159-174); no new ruling needed — the ISS-22 v2.116 record binds the fix shape; the operator round-trip re-drive follows the land, outside the loop. Prior: **ISS-22: the driven round-trip FAILED on the delivery seam, root-caused in source** — a two-bug pair in ChatReplyBroker makes timeout/resolve order-DEPENDENT: resolve() returns even when the popped future is done (wait_for cancels at timeout with an await before discard() runs — a resolve in that window drops the text; discard then parks None = late-expected-forever), and discard() clobbers any parked text → **TK-271** (P1, S): order-independence by construction (done-future resolve parks the text; discard never overwrites; the exact live interleaving becomes a deterministic regression test). PROVEN LIVE first: TK-269 wake works (~25-30s reply, composed AND spoken), header truthful, id plumbing + pane poll wiring verified SOUND. **Q-117 RESOLVED-BY-OBSERVATION** per its own disposition (no gate hold on the send; no chat-gate ruling warranted; standing route-back recorded). Re-drive the round-trip after TK-271. Prior v2.115: **TK-270 DONE — BOARD CLOSES 187/187**: DEC-56(b) late-reply delivery landed (EP-32) — ChatReplyBroker bounded late-slot (cap 128 oldest-evicted + 900s TTL, pop-on-read; discard() marks late-expected, resolve() parks iff late-expected else true no-op), held response now carries the item id (pre-computed at submit, never model-visible — CON-1 intact), token-guarded GET /chat/reply/{id} (same 401 shape, CORS preflight covered), pane polls after held with sendChat's abort discipline up to an honest 12-min bound, appends with a visible 'replied late' marker or renders an honest gave-up line. files_in_scope normalized += app/src/chat.test.ts (TK-266 precedent). The TK-269+TK-270 batch is fully landed — BATCH-VERIFY PENDING (one Opus review), then the operator closeout: restart runtime+app onto final code and the driven chat round-trip, which also judges Q-117 (gate-HOLD cadence tripwire — if replies still lag minutes, a new DEC rules chat gate treatment). Prior v2.114 — **TK-269 DONE (186/187)**: DEC-56(a) drain wake landed — DrainWake event in runtime.py (coalescing, cleared-per-wake), pump idle sleep now interruptible (byte-identical when unwired), ChatSource.poll() fires the wake post-enqueue-commit per the v2.113 placement ruling, RuntimeBundle.chat_source pass-through; background pollers/Sweeper deliberately unwoken. Batch-verify pending; TK-270 (late-reply delivery) is the sole open ticket; Q-117 stays open as the gate-HOLD cadence tripwire. Prior v2.112 — **ISS-21/DEC-56: chat reply latency IS the drain-pump beat** (~10min7s, log-proven; replies composed AND SPOKEN on the next beat while the pane's 30s window orphans the text — broker discard-on-timeout). Ruled: (a) interactive enqueue WAKES the pump — edge-triggered coalescing event, idle sleep becomes interruptible wait, ASMP-2/DEC-41 byte-identical, background pollers deliberately don't wake → **TK-269** (P1, EP-1); (b) late replies DELIVERED not orphaned — bounded broker late-slot, held response carries the item id (never model-visible), token-guarded GET /chat/reply/{id}, pane polls + 'replied late' marker + honest give-up → **TK-270** (P1, EP-32; supersedes Q-110(d) discard IN PART); (c) spoken channel stays unconditional, confirmed coherent. Build order TK-269 → TK-270 (the whole open board; TK-268 landed v2.111). Prior v2.110: ISS-20 recorded: a batch-review repro run from the repo root inherited the live .env and stomped the live chat-handshake.json with a dead port (second failure of the TK-237-era verify-outside-repo-root discipline) → **TK-268** structural guard (P2, EP-1): `_write_chat_handshake` refuses to overwrite a handshake whose recorded port still answers — one loud WARNING, boot continues; dead/absent/unparsable → write as today; deliberately NO test-mode env flag. Operationally resolved 16:09:48 — TK-260 watchdog respawned onto TK-267 code (its SECOND live save today), port 57507 LISTENING, chat restored, TK-263 probe live. Whole-day arc LIVE in the 16:09 boot; board 184/184 all batch-verified; open proof = Jim's operator ear-check of the DEC-55 spoken summary. Prior v2.108: **TK-267 DONE, BOARD FULLY CLOSED 184/184; batch-verify pending (single Opus review), then the operator closeout (restart onto final code, driven chat round-trip, spoken-summary heard live).** Prior v2.106 state — **DEC-55 (JIM DIRECTIVE): speech is a SUMMARY, never formatted text** — new `speech_shape` stage between compose and speak makes one cheap DeepSeek call (same provider/profile/budget guards, egress set unchanged); SpeakSink speaks the summary; failure/validator-violation = silence + degrade, NEVER verbatim; deterministic no-markdown/length post-validator = the no-placebo bar; ack path exempt; fifth persona 'speech' mouth = recorded deferral → **TK-267** (P1, EP-30, the sole open ticket after the pending TK-265/266 batch-verify). Jim CONFIRMED LIVE post-TK-264: Fish cloned voice WORKS — ISS-17 closed end-to-end. Prior v2.103-2.105 records: **TK-264 DONE (board 181/183): the ISS-17 sentinel fix LANDED — playback.py's `_normalize_sentinel_sizes()` runs in play() strictly BEFORE the DEC-53a validation, patching RIFF/data size fields to actual byte lengths IFF the value is exactly in the closed set `_SENTINEL_SIZES = {0xFFFFFFFF, 0xFFFFFF00}`; non-sentinel values and unparseable buffers pass through as the same object, so genuine truncation still raises and every TK-262 rejection case stands verbatim — the paid Fish cloned voice plays again while the validate-before-native-call invariant holds by construction. Builder bar green (test_playback 14; full suite 1605/92 pg-skipped; ruff; mypy strict 330 files; diff = exactly the two files_in_scope; build resumed from the interrupted wf_274355ce-bc4 run — half-landed edits audited to spec, verified not re-implemented); architect re-ran pytest pre-commit. BATCH-VERIFY PENDING. Jim retest after restart: Fish should PLAY (no fallback), completing the half-proven chat round-trip. NEXT: TK-265 (P1, DEC-54), then TK-266.** Prior v2.102: ISS-18/ISS-19 routed: the LOCAL TTS fallback is its own boot-killer evading ALL TK-259 instrumentation (hard native-abort in SAPI/comtypes; TK-260 watchdog scored its first live save) → **DEC-54** subprocess-isolated local TTS (TK-265, P1 — supersedes DEC-53(d) IN PART via its own fired revisit clause; cloud winsound path keeps DEC-53a validation); chat send has no fetch timeout (pane stuck 'Sending...' forever) → TK-266 (P2, >30s AbortController + honest timed_out state + short probeChat abort). Chat round-trip HALF-proven live (in + DeepSeek 200; reply delivery killed by ISS-18) — full retest after TK-264. Prior v2.101: ISS-17/TK-264 routed: DEC-53a verified live (crash-loop closed, DEC-28 local-TTS fallback engaged) but EVERY Fish.audio WAV carries the streaming unknown-length sentinel (0xFFFFFF00 class) and is rejected, silently defeating the paid cloned voice — TK-264 sentinel-normalize-before-validation in playback.py is the NEXT READY ticket; genuine-truncation rejection and the validate-before-native-call invariant intact. Prior sync v2.100: **TK-263 DONE (board 180/180): ISS-16 header-liveness fix LANDED — Header 'Running' now derives from a real chat-port round-trip probe (probeChat() in app/src/chat.ts: fresh getInfo() + GET to the bridge loopback port, any HTTP response = alive, no POST /chat, token never in a URL) polled at mount + every 15s with interval cleared on unmount; rendered states/styling byte-identical; DEC-52(c) death-notification deferral stands. Builder app bar green (App.test.tsx 10/10, suite 270/270, lint, build; diff = exactly the three files_in_scope); architect re-ran pytest pre-commit. THE FIVE-LEAF BATCH TK-262→259→261→260→263 IS FULLY LANDED — single Opus BATCH-VERIFY is the next cadence step.** Prior sync (v2.98): **TK-261 DONE (board 178/180): DEC-52(e) runtime-owned single-instance lock LANDED — main() acquires an exclusive 127.0.0.1 TCP bind on new operator-tier config field wombat_singleton_port (default 63218, not app-editable) after per-boot logging is configured and before serve(); the socket is a pure OS-released mutex held for process lifetime (never accepted/listened/closed — released on ANY death incl. hard kill, no stale-lock class); bind failure = exactly one loud ERROR naming the port + field, then exit(1), no traceback spew; serve() stays library-clean, wombat-console.ps1 untouched (belt-and-braces guard stands). Builder bar green (full suite 1586/92 pg-skipped; ruff; mypy strict 329 files; diff = exactly the four files_in_scope); architect re-ran pytest pre-commit. BATCH-VERIFY PENDING (one Opus review after the whole TK-262→259→261→260 + TK-263 batch lands). NEXT: TK-260, then TK-263.** Prior sync (v2.96): **TK-262 DONE (board 176/180): DEC-53(a) validate-before-play LANDED — `_validate_wav_bytes()` in voice/playback.py raises ValueError (empty / malformed RIFF-WAVE via stdlib wave / declared-frames-overrun-buffer, each naming defect class + byte length) strictly BEFORE `winsound.PlaySound`; valid buffers reach PlaySound byte-identical with SND_MEMORY; SpeakSink regression test proves the ValueError degrades to the existing terminal Degraded (spoken=False, degraded=True) with a warning — the ISS-15 crash-loop closes by construction, zero sink/queue changes. Builder bar green (7 playback tests; full suite 1575/92 skipped; ruff; mypy strict 328 files; diff = exactly the two files_in_scope); architect re-ran pytest pre-commit. BATCH-VERIFY PENDING (one Opus review after the whole TK-262→259→261→260 + TK-263 batch lands). Live re-test of chat round-trip + drain of the replaying items follows a real boot. NEXT: TK-259.** Prior sync (v2.95): live-E2E findings routed: ISS-15 segfault crash-loop → DEC-53/TK-262, ISS-16 false Running header → TK-263, TK-259 faulthandler amendment; build order TK-262 → TK-259 → TK-261 → TK-260, TK-263 independent. Earlier: contract **v2.89** — **TK-257 DONE, BOARD CLOSES 174/174. The in-app Google OAuth UI (DEC-50 UI half) landed: GoogleConnections rows in the API Keys view — four-state status chip, Connect/Reconnect, consent-in_progress poll-only-while-in_progress, consent-error via role=alert, restart notice pointing at Settings > System; api.ts getGoogleStatus/connectGoogleService over tokenedFetch (no new bridge). DEC-50 fully discharged on the code side (TK-256 backend + TK-257 UI); operator half (consent click + GCP Testing-vs-Production check) stays with Jim. The whole TK-254→255→256→257 batch has landed; BATCH-VERIFY PENDING (one Opus review audits TK-254..257 + the TK-252/253 debt). UI ownership returns to Jim per DEC-47(b) except where DEC-50 supersedes. Full detail in the decision log below.** Prior sync (v2.83): **TK-252 DONE, board 170/171. The ISS-9 stale test is retired: `test_idle_second_drive_on_empty_queue_parks_wait` rewritten+RENAMED `test_idle_second_drive_on_empty_queue_completes_done_pg` to the as-built TK-230/DEC-41 Done-on-empty contract (COMPLETED + DRAIN_HEARTBEAT artifact; raising-FakeModel mouth-guard kept; all stale WAITING/self-park prose incl. the module docblock corrected), the pg-bearing name closing the `-k 'pg or postgres'` selector escape vector. Landed ahead of declared dep TK-254 (bar-ordering preference, not a code dependency — the v2.82 precedent). ONLY TK-254 remains open (root conftest env-strip + keyring tripwire, the ISS-10 hermeticity guarantee); Jim's operator half (Google re-consent + OAuth publishing status) still outstanding; batch-verify pending for TK-253/252/254 per the v2 cadence. Full detail in the decision log below.** Prior sync (v2.79): **TK-251 DONE — BOARD CLOSES 168/168; the DEC-48 iteration-4 Phase-2 UI pass (TK-248 tokens → TK-249 shell → TK-250 upcoming → TK-251 inbox/notepad) is COMPLETE. TK-251: getGmailMessages() over GET /external/gmail (DEC-45 five-field projection, TK-246 envelope verbatim, no snippet/body), iteration-4 inbox cards with the literal two-band priority chip (needs-reply/fyi = DEC-47(c) deferral), row-click → main-process-validated open-in-Gmail via shell.openExternal (v2.75 r3 — renderer passes only message_id, never a URL), notepad honest-empty (no scratch read route exists, DEC-48(d)). CONSEQUENCE LIVE: per DEC-48/DEC-47(b), UI ownership RETURNS TO JIM — operator-owned, no agent UI work without new direction. STANDING DEBT: the owed batch verifies (TK-244..247 and TK-248..251, plus earlier owed batches) per the v2 cadence. Full detail in the decision log below.** Prior sync (v2.70): **TK-245 DONE (board 162/164): the DEC-45(c)/(d) sync wiring lands (EP-36) — SourceRegistry gains an optional sink seam (one-WARNING loud-degrade, enqueue arm byte-untouched, ASMP-2 held), composition-root whitelist projections (gcal payload verbatim; gmail five-key metadata + band, body key never referenced — DEC-26 allowlist byte-unchanged), RuntimeBundle.external_item_store on the DSN posture, serve() prune-once at boot on the 30-day constant; batch-verify pending; NEXT TK-246 ∥ TK-247. Full detail in the decision log below. Previous sync (v2.69): **TK-244 DONE (board 161/164): the DEC-45 external_store substrate lands wiring-free (EP-36) — wombat_external_items via migration 008 as the SEVENTH preflight entry, ExternalItemStore on the Q-46 lazy pattern, EXTERNAL_ITEMS_PRUNE_DAYS=30 shipped per v2.68 r3; batch-verify pending. Earlier sync (v2.67): TK-241 DONE — the DEC-43/DEC-44 settings-to-Postgres arc is COMPLETE (board 160/164; only the v2.66 Jim-directive arc TK-244..247 remains open — the original 160-ticket board would have closed here). The boot-read half lands: load_config() sources APP_EDITABLE_FIELDS from wombat_settings via TK-240's SettingsStore in the removed JSON source's EXACT precedence slot (init kwargs > env > .env > table > defaults). Per v2.62 ruling B: module-level `_TABLE_SETTINGS_HOLDER` populated by ONE `store.get_all()` inside load_config() (DSN from os.environ then dotenv_values('.env'), mirroring settings_app `__main__` — NEVER from the table; libpq connect_timeout=3 appended; any store failure = exactly one WARNING + treated-as-absent, load_config never raises for store reasons, CON-3; absent DSN = silent no-op) and cleared in a `finally` on EVERY exit path — bare WombatConfig() is zero-DB-I/O by construction (v2.61 r1, AC4 pinned). CR5-2 secret-drop + TypeAdapter guards ported verbatim into `_SettingsTableSource`; the JSON source deleted outright (grep-AC); SettingsStore imported lazily inside load_config() to break the import cycle. RIDERS: R1 (v2.62 A as amended v2.63) WOMBAT_SETTINGS_FILE definition relocated to settings_store.py, live.py verified UNTOUCHED (moot-rider held); R2 (v2.61 r3) `path.rename` FileNotFoundError = already-migrated-by-the-other-process, log-and-continue, pg-gated race test pins it; R3 (v2.64) test_output_effects_live.py's collection-time load_config() replaced by a LAZY string-condition skipif (short-circuits on the unarmed env var before any config/DB I/O; armed semantics preserved) + NEW AC5 hermeticity test (module reimport under a DSN with a call-recording psycopg.connect stub proves ZERO import-time connections; fails against the old eager code) + architect repo-wide grep confirms no test module calls load_config() at import time today — FLAGGED to the batch verify whether a standing suite-wide structural assert should harden AC5 further. Builder self-ran the bar green (all five ACs runnable; suite twice under fresh throwaway pgs 5437/5438: 1617/11/5 — all 5 stash-proven pre-existing pg-armed-only failures, batch-verify debt not TK-241 regressions); architect pytest pre-commit: 1519 passed / 84 skipped / ZERO failed, exit 0 — the v2.51-tracked persona-name env-contamination failure DIED with the file source, exactly as predicted at v2.63. BATCH-VERIFY PENDING per the v2 cadence (TK-240..243, plus the owed v2.42..46 and v2.50..54). NEXT — the owed batch verifies, then dispatch TK-244 → TK-245 → {TK-246 ∥ TK-247}.** Prior sync (v2.66): **JIM SCOPE DIRECTIVE recorded (external-API persistence + scratch surface): DEC-45 + DEC-46 accepted, EP-36 minted (parent FEAT-1), TK-244 → TK-245 → TK-247 (EP-36) + TK-246 (EP-32, parallel after TK-244) — board 160 → 164. INVESTIGATION (source-cited): gcal/gmail is fetch-render-discard today (BriefGatherStage → BriefPayload artifact into the run journal; the registry poll loop enqueues into wombat_queue — transient, consumed on drain) — NO canonical store; runtime tables = exactly the six preflight entries (001/003/004/005/006/007; 002 released); upsert identity exists (gcal event_id under singleEvents=true + 48h window; gmail message_id + internalDate received_at + 24h lookback); NO scratch surface exists (pending_journal = gate custody, EntityKG = behavior claims, kb = static YAML). DEC-45 — ONE wombat_external_items table (source+item_key PK, JSONB payload, occurs_at window key, fetched_at/first_seen_at provenance; MIGRATION 008, SEVENTH preflight entry) synced on the existing source-poll beat via explicit WHITELIST projections (gcal payload verbatim; gmail metadata+triage band; body_text NEVER stored, body-key-guard allowlist unchanged — DEC-26 never laundered), token-guarded settings_app READ-ONLY GET routes with the DEC-43 loud degrade (final surface shape BINDS to the concurrent app design iteration), prune-on-boot 30d, DSN custody unchanged. DEC-46 — wombat_scratchpad scoped working memory (scope_key/entry_key/value JSONB; MIGRATION 009, EIGHTH preflight entry) behind ScratchpadStore on RuntimeBundle, purge-on-boot 14d, named NOT-list (not a second queue, not the memory graph, not a chat log, not gate input). SEQUENCING — all four new tickets sit behind TK-241 (in flight) so no collision with the running batch; TK-245/TK-247 serialize the shared bootstrap.py/runtime.py touches. Zero Jim questions — horizons/shapes/cadence decided as delegated hows, rejected alternatives recorded on both DECs. NEXT — TK-241 lands + owed batch verifies, then dispatch TK-244 → 245 → {246 ∥ 247}.** Prior sync (v2.64): **ORCHESTRATOR RULING unblocking TK-241 (the final open ticket; board 159/160): the builder proved a live-db-touch blocker — with the v2.62 ruling-B DSN resolution (os.environ → dotenv_values('.env') at cwd), tests/persona/test_output_effects_live.py's UNGUARDED module-collection-time load_config() call would make every ordinary repo-root pytest SELECT from the live production wombat_settings table (port 5436). Ruled, builder option (a) strengthened: (1) ruling B's DSN resolution STANDS — the production runtime boots from .env (TK-186 lineage); stripping .env pickup would make the table source a placebo in the shipped deployment; the TEST is the defect. (2) test_output_effects_live.py joins TK-241 files_in_scope — its load_config() call goes LAZY under proper isolation (chdir(tmp_path)/DSN scrub), collection performs zero I/O, the env-gated live-eval semantics preserved when armed. (3) CLASS CLOSED — new TK-241 AC: a runnable structural/hermeticity check (collection-import test, AST/grep no-import-time-load_config assert, and/or conftest guard) proving bare collection + the default unarmed suite performs ZERO load_config()-driven DB connections from repo cwd — the TK-202/Q-103 suite-hermeticity posture applied to the table source. NEXT — TK-241 closes the board 160/160, then the owed batch verifies (TK-240..243, v2.42..46, v2.50..54).** Prior sync (v2.63): **TK-243 DONE (board 159/160; DEC-43 batch 3/4; only TK-241 remains; v2 cadence — BATCH-VERIFY PENDING for TK-240..243): the hot-apply half of DEC-43 lands (EP-32). Built ahead of TK-241 — dependency-legal in substance (consumes only the TK-240 SettingsStore; the v2.62 ruling-A ordering hazard DISSOLVED — live.py file machinery incl. its WOMBAT_SETTINGS_FILE import deleted outright, so TK-241 live.py rider is MOOT). LivePersona takes an optional SettingsStore (None ⇒ fully in-memory, one loud warning, never crashes); construction FULLY LAZY (v2.61 ruling 2, zero store I/O structurally asserted); set() key-level-upserts the five axes + pins only (persist failure ⇒ ONE WARNING, never raises into the drain, CON-3); poll_settings() = one get_all() per Sweeper beat, value-diff detection, FIRST-beat hydration proves restart survival at the first-beat boundary, own-write cursor advance keeps dream nudges (explicit=False) from reading as app edits, read failures warn once per streak and retry next beat; grep-AC proves no file mtime/read path remains. Ten out-of-scope test files = mechanical ctor-signature ripples only (settings_path kwarg deleted), accepted. Architect pytest pre-commit — 1526/79/1, the SAME v2.51-tracked env-contamination failure (now dies at the TK-241 config file source), none new. NEXT — TK-241 closes the board 160/160, then the owed batch verifies.** Prior sync (v2.60): **TK-242 DONE (board 158/160; DEC-43 batch 2/4, built ahead of TK-241 — dependency-legal, depends_on TK-240 only per the v2.56 parallel-eligible note; v2 cadence — BATCH-VERIFY PENDING): the app-write half of DEC-43 lands (EP-32). settings_app/api.py `create_app` takes `SettingsStore | None` where it took `settings_path`; GET/PUT ride `store.get_all()`/`store.put()` with the loud read-only degrade — store None/raising ⇒ GET 200 all-null admitted view + `storage_unavailable: true`, PUT 503 with a FIXED generic detail (module constant, never exception-derived — no secret/DSN echo, no bare-500 path); `_read_settings`/`_write_settings` (incl. TK-235 atomic-write machinery, obsolete with the file) removed. `__main__.py` resolves WOMBAT_PG_DSN from env then cwd `.env` via dotenv_values (never `load_config`), runs `ensure_schema` then `import_legacy_settings_file` once at startup — the SECOND and LAST DEC-44 production call site; absent DSN ⇒ boots degraded, never fails. Token/key-vault routes byte-unchanged. test_api.py rewritten to the store shape (pg-gated round-trip + subprocess legacy-import chdir'd per v2.58(a)); the TK-240 sole-call-site structural test updated to exactly-two-call-sites (its pending-TK-242 clause redeemed). Architect pytest re-run pre-commit: 1528/78/1 — the SAME v2.51-tracked persona-name env-contamination failure ('John' from the operator's real settings file at repo cwd; dies with the file source at TK-241/243), none new. NEXT: TK-241 (WombatConfig DB source swap), then TK-243.** Prior sync (v2.59): **TK-240 DONE (board 157/160; DEC-43 batch 1/4; v2 cadence — BATCH-VERIFY PENDING): the settings substrate lands — src/wombat/settings_store.py owns wombat_settings (key/value JSONB + updated_at) via packaged 007_wombat_settings.sql; ensure_schema is the SIXTH ensure_all_schemas entry and NEVER calls the import (DEC-44 held structurally, grep-tested); SettingsStore get_all/put with loud pre-write SecretFieldRefused on SecretStr keys; opt-in import_legacy_settings_file (empty-table-guarded one-time, admits APP_EDITABLE_FIELDS + wombat_persona_pins, drops secrets loud, renames to .migrated — never deletes) wired into serve() AFTER assemble_runtime / BEFORE _drive_and_serve per the v2.58 ruling (b), the sole in-repo call site pending TK-242. Jim's real wombat.settings.json verified untouched post-suite — the twice-fired hazard is closed; the v2.58 chdir(tmp_path) test-hygiene pin held in every import-adjacent test. Architect pytest re-run pre-commit: 1523 passed / 76 skips / 1 pre-existing v2.51-tracked env failure, none new. NEXT: TK-241 (WombatConfig DB source swap).** Prior sync (v2.58): **DEC-43 BATCH RE-DISPATCHED under the DEC-44 shape: TK-240 → TK-241 → TK-242 → TK-243 (the whole 156/160 remainder). Two pre-build rulings recorded at v2.58: (a) TEST HYGIENE binding all four tickets — any test touching import_legacy_settings_file or the serve() import wiring runs under monkeypatch.chdir(tmp_path); no test ever lets the import see the repo root (Jim's real wombat.settings.json lives there — the DEC-44 hazard file); (b) IMPORT PLACEMENT — the serve() call lands after assemble_runtime returns / before _drive_and_serve; the one-migration-boot non-persona lag is ACCEPTED self-healing (DEC-32 restart-to-apply; persona heals on the first Sweeper beat), builder must not move it ahead of load_config. All seams re-verified live incl. installed cog-worx sweeper.py:76 clock-once-per-beat. Owed batch verifies (v2.42..46, v2.50..54) still outstanding. NEXT: build TK-240 → 241 → 242 → 243.** Prior sync (v2.57): **DEC-44 RECORDED (ORCHESTRATOR-ENDORSED; supersedes-in-part DEC-43's fold-into-ensure_schema migration mechanics): the one-time wombat.settings.json → wombat_settings import becomes an EXPLICIT OPT-IN `import_legacy_settings_file`, called ONLY by the two production entry points — the runtime serve() boot path (runtime.py, TK-240 wires it) and the settings_app `__main__` entry (TK-242 wires it). Bare ensure_schema/ensure_all_schemas NEVER touches the file — pinned by a NEW runnable TK-240 AC (empty db + settings file in cwd → file byte-untouched, zero rows imported). Why: PROVEN-LIVE hazard, not hypothetical — the pg-gated suite calls ensure_all_schemas/assemble_runtime from repo cwd, and the folded-in shape TWICE consumed-and-renamed Jim's real settings file into a throwaway WOMBAT_TEST_PG_DSN database during TK-240 builder verification (batch wf_98c1b7e8-4d7; all four tickets returned blocked). Everything else in DEC-43 stands. TK-240 normalized (title/intent/budget + files_in_scope += runtime.py + test_runtime.py); TK-242 gains the `__main__` second-call-site wiring in intent + AC3; TK-241/TK-243 untouched. Board 156/160; NEXT: re-dispatch TK-240 → 241 → 242 → 243 under the DEC-44 shape; the owed batch verifies (v2.42..46, v2.50..54) still stand.** Prior sync (v2.56): **DEC-43 BATCH SELECTED + BRIEFING-PINNED (board 156/160; the whole remainder): TK-240 → TK-241 → TK-242 → TK-243 — the Jim-directed settings-to-Postgres arc (DEC-43, recorded v2.55). Pre-build ruling at v2.56: TK-243 files_in_scope normalized += src/wombat/runtime.py + tests/unit/test_runtime.py + tests/pathways/test_dream_persona_stage.py (the poll_settings_file → poll_settings rename has call sites in all three; runtime.py:101 is the Sweeper beat). All briefing seams re-verified live against the tree: schema_preflight carries exactly FIVE ensure_schema entries today (TK-240 adds the sixth); config.py `_AppEditableJsonSettingsSource` at :75 / APP_EDITABLE_FIELDS at :54 with the CR5-2 SecretStr-drop + TypeAdapter guards to port verbatim; settings_app `create_app(settings_path, key_store, token)` with `_read_settings`:96/`_write_settings`:111 to swap; LivePersona ctor `(initial_matrix, assistant_name, settings_path=WOMBAT_SETTINGS_FILE)` with the mtime cursor + `_last_warned_mtime` machinery to retire. STILL OWED: the two batch verifies — v2.42..v2.46 and v2.50..v2.54 — plus this batch's own verify when it lands. NEXT: build TK-240 → 241 → 242 → 243, then run all owed batch verifies.** Prior sync (v2.54): **TK-237 DONE — BOARD CLOSED AGAIN 156/156: the full v2.49 batch TK-238 → TK-239 → TK-235 → TK-236 → TK-237 has landed (the DEC-42 lifecycle pair + the CRF-7/9/10 cleanup trio). TK-237 closes CRF-10 — the installed `wombat` console script now boots the REAL runtime: pyproject [project.scripts] repointed to `wombat.__main__:main`, `def main()` wraps `asyncio.run(serve())` with the module guard calling it (python -m byte-identical), Hello-World stub deleted from `__init__.py` (grep-proven absent under src/tests). Independently verified — from a cwd outside the repo, bare env fails loud ConfigurationError naming DEEPSEEK_API_KEY exit 1; dummy creds + pg unset names WOMBAT_PG_DSN exactly (AC literal) before assemble_runtime so no drainer stands up; ruff/mypy-strict clean, pytest 1518 passed / 70 gated skips / 1 PROVEN-pre-existing environmental failure (local wombat.settings.json persona contamination — not a repo defect). STILL OWED: the two batch verifies — v2.42..v2.46 (priority batch) and v2.50..v2.54 (this batch) — per the v2 cadence. NEXT: run both batch verifies, then route any findings.** Prior sync (v2.48): **OPERATOR-DIRECTED SCOPE REOPEN (DEC-42, Jim verbatim 2026-07-11): runtime lifecycle controls — the runtime moves to a VISIBLE persistent console (closing the window kills the process; tee'd timestamped file logs KEPT — the exit-5 post-mortem precedent) and the Electron app gains a Restart-server button. Logic homes IN-REPO under scripts/ (the out-of-repo desktop launcher becomes a thin caller; its re-point = operator step). Restart = kill-all-matching (Stop-Wombat.ps1 CIM shape) → verify-gone bounded wait → start exactly ONE (ASMP-2 structural; works from running/dead/never-started); Electron spawns the script hidden+detached/unref'd so it never parents the runtime (closing the UI never kills it); single-flight + disabled-while-pending + loud success/failure; Windows-only per DEC-6/DEF-10. TICKETS: TK-238 (EP-1, P1, S — console lifecycle scripts, deps TK-53) → TK-239 (EP-32, P1, M — restart button, wombat:* IPC + RuntimeControls under the DEC-39 design system). Board reopens 151/156; the TK-235..TK-237 cleanup batch + the two pending batch verifies stand unchanged. NEXT: a build batch picks up TK-238 → TK-239.** Prior sync (v2.47): **CLEANUP BATCH TK-235 → TK-236 → TK-237 SELECTED + BRIEFING-PINNED (board 151/154; the final three tickets): all seams re-verified live — TK-235 rides the trail/renderer.py `_save_sidecar` atomic-write precedent over the bare `write_text` in `put_settings`; TK-236 AC1 no-drift scope ruled to wombat-core WombatConfig-backed vars only (WOMBAT_TEST_PG_DSN / WOMBAT_MEMORY_* / NEO4J_* are separate documented consumers, untouched); TK-237 entry-point move verified safe (nothing imports the `wombat:main` Hello-World stub). SEPARATELY OWED: the TK-230..TK-234 priority-batch Opus verify remains PENDING per the v2 cadence. NEXT: build TK-235 → TK-236 → TK-237, then the batch verifies close the board 154/154.** Prior sync (v2.46): **TK-234 DONE (board 151/154) — CRF-1 closed, priority batch TK-230..TK-234 fully landed; `_checked_fill` lowercases the live type attribute at BOTH fill sites.** Prior sync (v2.45): **TK-233 DONE (board 150/154; priority batch 4/5; v2 cadence — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole TK-230..TK-234 batch after all land): CRF-6 MAJOR is closed — `_build_local_transcriber` broadens its guard from `except ImportError` to `except Exception` around FasterWhisperTranscriber construction (the `_build_local_tts` sibling shape byte-for-byte), so a whisper model-load failure at construction (uncached model + offline, bad WOMBAT_ASR_MODEL, corrupted HF cache) degrades voice to None instead of crashing boot through _maybe_register_asr → build_source_registry → assemble_runtime — CON-3 voice-additive restored; both TK-217 role-discriminated warnings byte-preserved, no provider/config/fallback-direction change (DEC-28 cloud-to-local held). voice/select.py only per the complexity budget; 3 new tests prove the repro inverted (RuntimeError at construction → None + loud primary warning + boot survives; a healthy cloud STT primary keeps flying wrapped with an absent local fallback + the fallback-role message; the ImportError path byte-preserved both roles). Bar green builder-run (test_select 22 passed; ruff clean; mypy strict 0 errors/318 files; full suite 1507 passed/70 documented skips, none new) + architect pytest re-run pre-commit; scope exactly the two files_in_scope. NEXT: TK-234 (CRF-1 password-guard casing normalization, the batch closer).** Prior sync (v2.44): **TK-232 DONE (board 149/154; priority batch 3/5; v2 cadence — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole TK-230..TK-234 batch after all land): CRF-4 MAJOR is closed — SourceRegistry._poll_loop now guards each enqueue() on the pattern_detector (TK-204/CR3-2) two-arm precedent (specific QueueFullError arm + broad Exception arm; each logs loud naming the source id + the dropped event_key, marks degraded, CONTINUES the loop — a full queue or reaped-pg error can never again permanently kill a source poll task), the degraded mark clears only on an all-enqueues-succeeded iteration (iteration_ok; the TK-171 most-recent-outcome semantics tightened), and stop() awaits every task in a per-task try/except (pass CancelledError, log-and-suppress anything else naming the source) so every source stop() always runs and runtime._drive_and_serve finally-teardown always completes (the shutdown-leak half closed). registry.py only per the complexity budget; no retry/backoff, no dead-letter store, ASMP-2 enqueue-only held. 3 new tests prove all three ACs runnable (the CRF-4 repro inverted — task.done() stays False under QueueFullError every interval). Bar green builder-run (ruff clean, mypy strict 0 errors/318 files, 1504 passed/70 documented skips) + architect pytest re-run pre-commit; scope exactly the two files_in_scope. NEXT: TK-233 (CRF-6 transcriber except broadened, CON-3).** Prior sync (v2.43): **TK-231 DONE (board 148/154; priority batch 2/5; v2 cadence — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole TK-230..TK-234 batch after all land): CRF-3 MAJOR is closed per DEC-41(e) — build_engine now pins `max_steps=_ENGINE_MAX_STEPS` (100_000; explained module constant — the TK-97/TK-52 eternal Wait-self-park runs accrue one committed step per Sweeper re-drive and would have silently FAILED at cog-worx's 1000-step default in ~1000 days, while 100_000 keeps the runaway-loop guard real) and wires `event_sink=_log_engine_event` (module-level sink over cogworx.coordination.events: RUN_FAILED → ERROR naming the run_id, everything else DEBUG — no run ever dies silent into a None sink). bootstrap.py only; no Engine subclass, no cog-worx edits (DEC-12 held), no config fields. ACs proven runnable: engine carries the pinned kwargs; a real BriefTimerStage self-park run survives 2000 fire_timer wakes still WAITING (the CRF-3 repro inverted); a tiny-ceiling harness with the SAME sink trips into FAILED with exactly one caplog ERROR naming the run_id. Bar green builder-run (ruff clean, mypy strict 0 errors/318 files, 1501 passed/70 skipped) + architect pytest re-run pre-commit; scope two of three files_in_scope (test_runtime.py untouched — runtime seam not in play). NEXT: TK-232 (CRF-4 registry enqueue guard).** Prior sync (v2.42): **TK-230 DONE (board 147/154; priority batch 1/5; v2 cadence — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole TK-230..TK-234 batch after all land): the CRF-2 CRITICAL is closed per DEC-41 — DrainQueueStage returns Done (terminal DRAIN_HEARTBEAT) on empty and never self-parks (transitions = the single gate edge); NEW read-only WombatQueue.pending_count() mirrors drain() eligibility exactly (pin (b) held); runtime._run_drain_pump peeks each beat and chains fresh wombat-drain-<uuid> runs until empty, gathered with Sweeper.run_forever in _drive_and_serve replacing the old single boot drive (pin (c) held — beat = bootstrap._DRAIN_POLL_INTERVAL_SECONDS; ASMP-2 one-pump sequential held; brief/dream drives + teardown byte-identical). The corrected test_serve_boot AC5 proves a SECOND post-cycle item drains without restart — the repro inverted; an AwaitHuman-parked draft no longer deafens the queue; idle beats start zero runs. Bar green builder-run (ruff clean, mypy strict 0 errors, 1498 passed/70 skipped, throwaway-pg 17 passed) + architect pytest re-run pre-commit; scope exactly the seven files_in_scope. NEXT: TK-231.** Prior sync (v2.40): **CLOSEOUT REGISTER ROUTED AND RULED (Q-115): the board REOPENS at 146/154. The crux ruled as DEC-41 — the drain becomes BOUNDED-EPISODE runs launched by a wombat-owned drain pump (DrainQueueStage returns Done on empty; serve() peeks a new read-only `pending_count()` and fires a fresh journaled `engine.run` per work cycle, chained until empty, zero runs on idle beats; ASMP-2 one-pump sequential; DEC-8/DEC-12 explicitly HELD — installed cog-worx proves runs are bounded episodes by design: fire_timer no-ops on terminal runs, Done cancels timers, max_steps exists to fail forever-loops; only the TK-53-era Q-71 one-eternal-run HOW is superseded); the brief/dream timer runs keep Sweeper custody with build_engine pinning max_steps=100_000 + a wired logging event_sink so RUN_FAILED is never silent. PRIORITY daily-driver batch (build order): TK-230 (CRF-2 CRITICAL drain pump; corrects the AC5 second-item blind spot) → TK-231 (CRF-3 max_steps+event_sink) → TK-232 (CRF-4 registry enqueue guard, pattern_detector two-arm precedent + stop() suppress-and-log) → TK-233 (CRF-6 transcriber except broadened to the TTS-sibling shape, CON-3) → TK-234 (CRF-1 password-guard casing normalization, both fill sites). CLEANUP batch: TK-235 (CRF-7 atomic settings write) → TK-236 (CRF-9 .env.example completeness) → TK-237 (CRF-10 real console entry point). Deferrals recorded, not dropped: DEF-9 (CRF-5 pending_journal compaction) + DEF-10 (CRF-8 macOS reopen). wombat is NOT daily-reliable until the priority batch lands. NEXT: dispatch TK-230→…→TK-234.** Prior sync (v2.34): **TK-136 DONE — THE BOARD CLOSES at 146/146 (EP-26 closeout batch 2/2; v2 cadence — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole EP-26 batch now that both tickets have landed; EP-25's batch verify remains due too): LoginHandoffStage lands exactly per the v2.32 Q-114 rulings — the shared `_checked_fill` UNCONDITIONAL deny-always password guard covering BOTH capability fill sites (no human-confirm token exists anywhere; a password field among submit_form fields blocks the whole submit with NO submit click) + the standalone login_handoff/login_confirm pair over the ruled injected UNGATED PageStateProvider, pure closed-lexicon aria detection (the courtesy handoff; the guard is the strong guarantee), widened both-name journal walk, fresh-recheck-or-re-park on `login-complete`, refuse-loud otherwise, and the documented never-taken LoginConfirmTerminalStage stub closing the StageGraph (BriefTimerTerminalStage/TK-53 precedent). Bar green builder-run (1496/70/0, real-playwright guard tests ran) + architect pytest re-run pre-commit; scope exactly the four files_in_scope. NEXT: the EP-26 + EP-25 batch-level Opus verifies, then the browser-arc review pass.**)

## What wombat is

A quiet, deterministic personal assistant — "The Steward" — where the **LLM is a mouth, not a brain**. Built as a pure **adopter of the local cog-worx** cognitive framework (Stages + pathways on its Engine; never forks the Engine). The product thesis rides on a deterministic **interruption gate**: a two-axis **Urgency × Cognitive Load** pressure engine with **no LLM in the decision**.

## The locked spine (DEC-1..DEC-19 — do not change without escalation)

- **Gate (DEC-1/13):** two-axis Urgency × Cognitive Load, deterministic code between the queue and the loop; per-event ratings, personalized + adaptive; Cognitive Load cumulative across a durable pending set.
- **cog-worx mapping (DEC-12):** wombat = Stages + pathways on cog-worx's Engine; no loop code, no forked Engine.
- **Loop (DEC-8/14):** one queue-driven drain pathway; pluggable input sources register with a source registry; idles on empty.
- **Memory (DEC-11, supersedes DEC-2; DEC-15):** cog-worx user_model scope + a thin PA empirical-user-model layer; behavior + outcomes only, never motive. The dev agent-memory pipeline is a *separate, out-of-scope* layer.
- **Mouth (DEC-4/18):** DeepSeek (cloud, OpenAI-compat) via the cog-worx S4 seam, behind the budget guard; renders pre-decided output only.
- **Taint invariant (DEC-26, v0.74):** `taint_drops_external` is INVARIANT TRUE across all wombat code — no stage may ever construct/bind a `StageToolPolicy(taint_drops_external=False)` (the TK-148 verifier's 8th probe proved a False re-bind after latch re-exposes external tools). Structural guard lands with TK-151; until then every policy-touching briefing (TK-149/TK-153/brief-cluster) carries it as a named binding constraint. Relaxing it ever = Jim decision.
- **Brief & dreaming (DEC-16/17/23):** morning brief = scheduled pathway = one forced flush through the gate (selects its own items via the gate's threshold-free `select_items` seam, never the live pending set); nightly "dreaming" = off-path batch pathway that re-tunes ratings. **DEC-23:** off-path (S1) consolidation-time LLM inference IS admitted in the dream pathway (the cog-worx reconciler/extractor use a model-backed oracle) — the GATE stays model-free (NG-4 governs the gate, not off-path consolidation; bounded by a BudgetGuard).
- **Side effects (DEC-19):** journal trail + review-before-send via `AwaitHuman` and the cog-worx approval gate.
- **External tier (DEC-20 + DEC-22):** the cog-worx `external` capability tier is admitted ONLY for the approved AwaitHuman dispatch stages (Gmail draft, browser); engine default stays `{read,write}`. The planning **authorization is GRANTED** (DEC-22, settled by architect under Jim's delegation). The only remaining act is the **build-time enablement** of the cog-worx external-tier flag in deploy config — a TK-151 bring-up step, not a planning blocker; TK-151 fails closed until that flag is set (wombat never self-grants at runtime).
- **Wombat-day (DEC-21):** the canonical day is the **civil-local date** in a configured IANA timezone, computed at access time (survives laptop sleep across midnight); one shared `DailyLedger` primitive (TK-152) owns the boundary for spend/ceiling/brief ledgers.
- **Scope (DEC-3/6/7/9/10):** v1 = read-only Google Calendar + Gmail-only → one combined morning brief; voice + browser in v1; runs on a laptop.
- **Voice providers (DEC-28, v1.64):** STT/TTS are pluggable slots behind the EXISTING Transcriber/TTSAdapter seams; defaults local+free (faster-whisper/pyttsx3, zero-config = fully offline). Cloud voice (ElevenLabs/Deepgram/Fish Audio) is **structural per-provider user opt-in** (config selection AND user-supplied key, or no construction); degrade strictly cloud→local, never local→cloud.
- **Front-end direction (DEC-31, Jim-decided):** the Electron + React app (settings surface in v1, DEC-29 zero-analytics / DEC-30 config-surface-only) is intended to grow into wombat's front-end/companion surface (future 3D assistant avatar). The loopback token-guarded Python settings API is its entire backend (DEC-32 config tiers: keyring secrets / settings.json non-secrets / .env operator-owned; restart-to-apply).
- **Persona (DEC-33..36, v1.81, Jim-approved):** the personality matrix is **5 axes as named enum levels** (brevity/warmth/directness/humor/proactivity; DEFAULT = today's steward, byte-identical prompts) rendered by a **pure-function clause-algebra builder** with an immutable guard suffix; **hot-apply via LivePersona** (DEC-34, Jim authority — supersedes-in-part DEC-32's restart-to-apply for IN-PROCESS persona actuation only); voice tuning is a **deterministic CLOSED grammar intercepted pre-queue at ASRSource** (DEC-35 — never enqueued/gate-rated/mouth-rendered); learning is **explicit feedback tokens only**, aggregated nightly by dream_persona under RatingTuner-pattern custody (DEC-36; implicit tone-adaptation = DEF-8). Constitution-excluded axes are NAMED as excluded (interruption-eagerness, motive-empathy, persuasion/sycophancy, action-initiative). Persona shapes only HOW gate-cleared output reads — never whether/when anything surfaces.

- **Persona fidelity restoration (DEC-37, v1.84):** the RECOVERED full Jim-approved proposal supersedes the v1.81 summary-based record where they diverge — proactivity actuates as a **bounded deterministic `urgency_threshold` offset inside a recorded `personality_band` in `wombat_params.yaml`** (TK-215; gate-constant custody, CON-3/NG-3 intact); degrade-path brevity/warmth variants + recorded directness/humor model-path-only asymmetry (TK-216); reflection = the FOURTH persona mouth (its CON-6/NG-1 bars ARE the immutable guard_suffix); humor = none|dry only, never in drafts/reflection; deterministic one-line command ack; settings-app persona edits hot-apply via mtime poll; ≥2-signal nightly threshold + 7-day explicit-set pin.
- **Persona expression redesign (DEC-38, v1.97, Jim directive — "be bold"):** the prompt algorithm becomes a swappable STRATEGY seam `render(mouth, matrix, cues) -> Expression`; humor is UNBARRED structurally — per-mouth persona behavior is POLICY DATA in a versioned operator-editable `persona_policy.yaml` (params custody; default policy = today's placement byte-exactly; supersedes-in-part DEC-37(c)); the cues bag is typed+extensible (growth axes mood/scene/temporal/tone/INTONATION, v1 empty); Expression is a TYPE shaped to grow TTS delivery hints (v1 instruction text only); the TK-210 eval permitted-set derives from the SAME loaded policy (no silent tweaks). Five invariants survive ANY strategy, SEAM-enforced: purity/determinism, DEFAULT byte-identity, guard_suffix unremovable by any strategy or cue, measurable-or-flagged (never placebo). Carried by TK-219 → TK-220 → TK-221 (+ TK-210 re-land).
- **Drain lifecycle (DEC-41, v2.40):** the drain runs as **bounded episodes** — DrainQueueStage returns Done on empty; a wombat-owned **drain pump** in serve() peeks the durable queue (`pending_count()`) and fires a fresh journaled `engine.run` per work cycle (chained until empty; idle beats start zero runs). The Sweeper remains the beat for the brief/dream timers ONLY, with `build_engine` pinning `max_steps=100_000` + a logging `event_sink` (RUN_FAILED never silent). ASMP-2 one-pump-sequential preserved; DEC-8 idles-on-empty and DEC-12 no-cognitive-loop/never-fork held — the pump is run-launch scheduling, not loop code; only the TK-53-era Q-71 one-eternal-run HOW is superseded.
- **External-information persistence (DEC-45, v2.66, Jim directive):** information wombat nabs from external APIs (other than STT/TTS providers) persists to Postgres — ONE upsert-shaped `wombat_external_items` table (source+item_key PK, JSONB payload, `occurs_at` window key, fetched_at/first_seen_at provenance; migration 008, seventh preflight entry) synced on the EXISTING source-poll beat via explicit whitelist projections (gcal `CalendarEvent.to_payload` verbatim; gmail metadata + triage band — `body_text` NEVER stored, the body-key-guard allowlist never grows, DEC-26 taint never laundered; stored rows are UNTRUSTED display/query data only, never trusted instruction). Read path = token-guarded READ-ONLY settings_app GET routes under the DEC-43 loud degrade; final surface shape binds to the concurrent app design iteration. Prune-on-boot 30d; DSN/env custody unchanged. Carried by TK-244 → TK-245 → TK-246.
- **Scratch surface (DEC-46, v2.66, Jim directive):** `wombat_scratchpad` scoped working memory (scope_key/entry_key/value JSONB upserts; migration 009, eighth preflight entry) behind a lazy dsn-injected `ScratchpadStore` on RuntimeBundle so stages can persist intermediate work-state across steps/runs; purge-on-boot 14d by updated_at. Named NOT-list: not a second queue (writing scratch never surfaces anything — CON-3), not the memory graph/user model (CON-6/DEC-11), not a chat log, not gate input; scratch stays data inside the CON-1 boundary and never un-latches taint. Substrate ships ahead of its first consumer BY DIRECTIVE (recorded). Carried by TK-247.
- **App rescope (DEC-39, v1.98, Jim directive):** the Electron app grows beyond settings — basic CHAT riding the full queue/gate/mouth discipline (runtime-owned loopback+token ChatSource surface; NO side-channel to the model; supersedes-in-part DEC-31's entire-backend + DEC-30's config-surface-ONLY clauses), mic/audio controls on the existing drop-dir/ASRSource path (output volume EXCLUDED v1 — the no-placebo bar extends to controls), and BINDING design requirements (bright color-theory tokenized palette, complete Tailwind theme — never MUI, no gradients, icons, minimal animation, scale, not-ai-slop). DEC-29 zero-analytics reaffirmed on every new surface. Carried by TK-222..TK-225 + TK-198/TK-200 amendments.

## Persona design — the full recovered intent (FEAT-14, DEC-33..37)

The v1.81 record was reconstructed from an orchestrator summary after a power cut; the proposing architect's FULL Jim-approved proposal was later recovered and DEC-37 (v1.84) restored the dropped/inverted substance. The complete design, in one place, so it can never be lost again:

- **Matrix (DEC-33 + DEC-37):** five closed named-level axes — brevity terse|balanced|expansive, warmth reserved|neutral|warm, directness gentle|plain|blunt, humor **none|dry** (two levels; the v1.81 "playful" was recorder-added, struck), proactivity minimal|balanced|forward. DEFAULT = (terse, reserved, plain, none, **balanced**) = today's steward prompts byte-for-byte AND today's gate exactly (balanced = zero offset). Excluded axes, NAMED so absence is a decision: interruption-eagerness (CON-2/3), empathy-as-motive-inference (CON-6), persuasion/sycophancy (CON-1), action-initiative (CON-5), coaching/clinical register (NG-2), persistence/reminder-frequency (NG-3).
- **Prompt actuation (TK-206/207):** one pure clause-algebra builder over FOUR mouths (compose, brief, draft, reflection) — base_role + length/register/hedging/humor clauses + an IMMUTABLE guard_suffix present at every matrix value (reflection's CON-6/NG-1 motive/clinical bars ARE its suffix). Humor renders ONLY for compose/brief — never Gmail drafts (the user's outbound voice) or reflection. Exhaustive 162-combination property tests; DEFAULT byte-identity pinned against the live constants.
- **Gate actuation (DEC-37a, TK-215):** proactivity = a bounded deterministic offset on `urgency_threshold` — a `personality_band` block in `wombat_params.yaml` (minimal +0.10 / balanced 0.00 / forward −0.10, PROVISIONAL under DEF-1; hard floor 0.60 / cap 0.95; human-edited, versioned, same custody as every gate constant — no tuner or runtime path writes it; composes with RatingTuner, the band bounds the COMBINED effect). CON-3 intact — no axis creates a surfacing, every surfaced item still clears the deterministic gate; NG-3 intact — no cap/ceiling ever raised (minimal may only lower willingness); zero LLM. Read via LivePersona at scoring time (next-scored-item semantics).
- **Degrade actuation (DEC-37e, TK-216):** brevity wrapper variants in TemplateComposer + the brief's fixed lines (terse = today's exact bytes); warm adds ONE fixed greeting line to the brief degrade; directness/humor have NO degrade variant — recorded honest asymmetry, model-path-only, pinned by test.
- **Hot-apply & custody (DEC-34 + DEC-37g, TK-209):** LivePersona is the ONE runtime authority; mouths + gate read at render/scoring time; set() applies next turn and persists to `wombat.settings.json` (the single persisted home; never .env, never secrets). Voice commands refresh synchronously; settings-APP writes hot-apply too via an mtime poll on the existing timer beat (settings API keeps zero-runtime-imports, TK-197 untouched; last-write-wins acceptable on the single-user file). Providers/keys/name stay restart-to-apply (DEC-32/34). TK-200's persona selects show hot-apply, not the restart notice.
- **Voice commands (DEC-35 + DEC-37f, TK-211/212):** closed versioned utterance grammar, deterministic whole-utterance normalized matching (no LLM/embeddings/fuzzy — CON-1); intercepted at ASRSource AFTER transcription, BEFORE enqueue — never queued/gate-rated/mouth-rendered; unmatched speech flows byte-unchanged; trail line BEFORE apply (CON-4), no AwaitHuman; ends with ONE deterministic one-line ack ("Warmth is now warm.") via the existing speak/degrade path, zero model calls.
- **Learning (DEC-36 + DEC-37h, TK-213/214 + DEF-8):** explicit closed-lexicon feedback tokens only (the TK-51 grammar discipline; motive-free `persona_feedback` events) → nightly `dream_persona` AFTER `dream_tune` under RatingTuner-pattern custody: ≥2 consistent in-window signals, at most ONE clamped step per axis per night, journaled human-readably; an explicitly user-set axis is PINNED against nudges for 7 days (explicit always outranks learning). Implicit tone-adaptation stays deferred (DEF-8).
- **Eval bar (TK-210, EP-33's done-bar):** env-gated live-eval harness measures OUTPUT effects per level (brevity monotonicity, warmth greeting presence/absence, directness hedge-lexicon, humor only-where-permitted + asserted ABSENT from draft/reflection, forbidden-term classifiers at all levels); proactivity is proven deterministically at the gate (TK-215), exempt from sampling; any unmeasurable axis gets FLAGGED in governance, never shipped as placebo.
- **Redesign layer (DEC-38, v1.97) + measurability record (ISS-7, v1.96):** the first armed TK-210 run proved humor(dry) and brevity(balanced) clause texts too WEAK to move the live mouth — adjudicated as clause-strength, not concept; the fix rides the DEC-38 policy-data surface (TK-221 calibration) and TK-210's amended symmetric-fail-loud ACs. **TK-221 landed 2026-07-10 (v2.02): both axes now separate live under the unweakened N=9 rule — the measurability finding is DISCHARGED; the non-separation contingency never triggered.** The clause algebra survives as the v1 DEFAULT strategy behind the new seam; the per-mouth humor walls survive as the DEFAULT POLICY (data), no longer code.
- **Sequencing:** TK-196 → EP-33 (now incl. TK-219/220/221 → TK-210 done-bar) → EP-34 → EP-32 remainder (now incl. chat/audio/design TK-222..225) → EP-35 (P3); browser EP-25/26 stay P3 behind all of it.

## v1 scope boundary

IN: read-only calendar, Gmail-only (incl. drafts via review-before-send), one combined morning brief, voice input source, browser/computer use, laptop deployment, pluggable voice providers (local default, opt-in cloud — DEC-28), the local Electron app (settings + basic chat riding queue/gate/mouth + mic/audio controls + the binding tokenized design system — DEC-29/30/31/32 as rescoped by DEC-39; output-volume honestly excluded, no analytics ever), the tunable personality matrix (FEAT-14, Jim-approved v1.81 + the v1.84 fidelity restoration — DEC-33..37, incl. the gate personality_band TK-215 and degrade deltas TK-216; sequenced EP-33 → EP-34 → EP-32 remainder → EP-35).
OUT / deferred: always-on hardware, non-Gmail email, sending without review, the dev agent-memory RAG pipeline, accounts/multi-tenancy/packaging/installer/auto-update (DEC-30), the 3D avatar/companion rendering (flagged future, DEC-31), cloud-provider live smokes until Jim's keys (DEF-7), automatic tone-adaptation from implicit behavior (DEF-8 — persona learns only from explicit feedback tokens, DEC-36).

## Posture

Local-data scoped to **storage**; **exactly one egress in the DEFAULT configuration — the DeepSeek phrasing call** (ASMP-1 accepted 2026-06-20). **User-opted-in voice providers are additional permitted egress (DEC-28, v1.64)** — opt-in is structural (config selection + user-supplied key), degrade is strictly cloud→local, and TK-195 pins the default-zero-voice-egress invariant. S4 local-model swap stays the deferred escape hatch.

## Current state

- Stage: **`analyze`**. Contract **VALID** (137 nodes, 90 governance items); `gate_check analyze` **PASS** (contract v0.41). De-risk spikes built/run (v0.21/0.22); spike-outcome reconciliation done (v0.23 — Q-39 layout, Q-40 TK-48 constants). **All 6 de-risk spikes now DISPOSITIONED (v0.24):** RISK-4/5 retired (running code); RISK-1/2/3/6 mitigated (architecture/safety proven) with the residual human/live validation homed as deferrals **DEF-1** (scoring calibration), **DEF-2** (slot ratings), **DEF-3** (presence accuracy) — each with a concrete revisit_trigger. **All 6 spike tickets (TK-22/24/26/48/73/4) are now `done`; nothing parked on Jim's desk.** 66 tests green.
- **Production build — 45/83 tickets done; THE v0.39 DEMO MILESTONE IS REACHED (v0.56); FEAT-7 — THE MORNING-BRIEF WEDGE — IS CODE-COMPLETE (TK-97 v1.03, the LAST ticket of the ingestion arc; all EP-19 + EP-20 children done); the arc ran (TK-3 v0.59, TK-12 v0.60, TK-71 v0.62, TK-15 v0.64, TK-72 v0.67, TK-74 v0.70, TK-146 v0.72, TK-148 v0.74, TK-75 v0.76, TK-76 v0.78, TK-16 v0.80, TK-9 v0.82/83, TK-29 v0.85, TK-53 v0.87, TK-17 v0.88, TK-28 v0.90, TK-98 v0.92, TK-99 v0.94, TK-100 v0.96, TK-101 v0.98, TK-96 v1.00, TK-97 v1.03 all done).** **WOMBAT IS NOW ONE STANDING PROCESS (TK-53, v0.87, independently adversarially verified):** `python -m wombat` → serve() (WOMBAT_PG_DSN required loud, NOT in REQUIRED_ENV) → `bootstrap.assemble_runtime()` registers `wombat.drain` + composes the REAL gate over the **TK-29 PgPendingJournal (isinstance-verified — the Q-70/RISK-5 boot obligation MET; RISK-5 resolution updated: durability honestly claimable for the live runtime)** + budgets LIVE (Q-69 AC proven meaningful vs cog-worx's unbounded defaults) → one initial drive → `Sweeper.run_forever` re-drives the Wait-self-park — NO authored loop (DEC-12); SourceRegistry start/stop owned in serve(); AC5 full cycle RAN on throwaway docker pg; 506/4 with DSN (only live-Google smokes skip). **One verifier concern homed: Q-72 → NEW TK-17** (EP-1, P1, S, dep TK-53): `openai` (cog-worx's optional providers extra) is UNDECLARED in pyproject — Engine._build_context imports it eagerly before the degrade path, so a clean uv-sync checkout can't run the DSN-gated boot test and a clean install can't boot serve(); ruled a CORE-dependency TICKET, not a deferral (protects the Q-46 clean-checkout done-bar; ticket count 82→83, TK-17 is a leaf). **TK-17 DONE (v0.88) — Q-72 FULLY DISCHARGED:** core dep is now `cog-worx[providers]` (the path dep DOES expose the extra — the Q-72 ruled-preferred form; no parallel openai pin), uv.lock regenerated in the same change; AC1 proven on a genuinely clean venv (uninstall openai → plain `uv sync`, no extras flag → DSN-gated boot suite 2 passed vs throwaway docker pg, no ImportError) — the Q-46 clean-checkout done-bar RESTORED; AC2 diff = exactly pyproject.toml + uv.lock; no-DSN 468/32 unchanged, ruff clean. **Q-69 (v0.83) — two standing facts:** (1) PROCESS RULE restated on the record: **builders do NOT write contract.yaml** — status/changelog/governance are architect-only acts (the TK-9 builder's v0.82 self-record was verified well-formed and re-owned, not reverted); (2) **the TK-9 budget layers are correct but LIVE IN NO RUNNABLE PATH** — `demo_drain.py` hand-rolls and bypasses both (recorded limitation + sanctioned header-comment rider on the TK-9 commit); **TK-53 now carries the budget-live-at-boot AC** (boot assembles via `bootstrap.build_engine`/`build_compose_stage`, never hand-rolled; test asserts non-None spend ledger + non-default BudgetPolicy). **EP-15 COMPLETE; both Q-61-class unowned wires CLOSED** (AuthorizedSession constructed in exactly two grep-verified sites; no-consent-at-boot proven three ways; independent zero/one/both registration). **THE BRIEF-FACING GMAIL INGESTION IS CODE-COMPLETE** (poller + triage; TK-77 deferred-not-dropped). **PRE-LIVE OBLIGATION LIST — TK-29 CLEARED (v0.85); remaining: gcal live smoke + gmail live smoke** (Jim bring-up: two consent passes + enable Gmail API, then run both smokes). **THE BRIEF CLUSTER IS UNDERWAY — TK-98 ✅ v0.92 (BriefGatherStage built + independently adversarially verified, PASS):** the Q-74 shape landed exactly — raising `fetch_window`/`fetch_recent` extracted on both pollers with `poll()` a proven byte-equivalent wrapper (existing TK-72/TK-75 poll tests untouched — the regression net HELD), frozen JSON-native `BriefPayload` with metadata-only `GmailBriefItem` (the no-body-key property proven at RUNTIME: a SECRET-MARKER body pushed through the real stage+triage never reached the artifact; the Q-65 AST guard covers the new files with no allowlist addition), per-source broad-except degrade flags, Transition(brief_force_flush) + `wombat.brief_payload` artifact, never touches ctx.journal (AC3 seam held via load-bearing spy). Q-56 over-pull #3 stands closed (v0.91). **TK-99 ✅ v0.94 (BriefForceFlushStage built + independently adversarially verified, PASS_WITH_CONCERNS — nothing blocking):** the Q-75 shape landed exactly — DetectConflicts FOLDED IN (ruling (a), no graph node; TK-98's committed transition stays honest), narrow injected select_items callable (pending mutation structurally impossible — the audit-CRITICAL invariant PROVEN on a real Gate + InMemoryPendingJournal-backed PendingSet: bit-identical list(), equal cumulative_load(), ZERO new journal records, spy ceiling never called), three-family adaptation (events→prep w/ replay-stable journaled-generated_at seconds_to_event; conflicts→conflict via conflict_to_payload; gmail→recap via the PriorityBand HIGH→known_human/NORMAL→automated bridge), sealed frozen+slots BriefDecisionArtifact w/ NO scoring keys in the wire form (Q-50), transitions=('brief_compose',) freezing TK-100's name; 519/37, ruff clean, zero new mypy, scope exact. **The one verifier concern HOMED → Q-76 (HOMED-to-frontier, the Q-56 pattern):** conflict_to_payload carries DAY-level granularity only (drops the overlap start/end minutes) — TK-100's briefing RULED it (Q-77/v0.95): **option (b) enrich-from-sealed-prep** — conflict lines cross-reference the sealed prep bucket by event_id (both present → each event's own sealed start–end times; else honest day-level; no overlap re-derivation, NO TK-74 reopen; residual rides DEF-1). **TK-100 BRIEFED + BUILD-READY (Q-77 rulings LOCKED v0.95):** last epic-dep narrowed EP-5→TK-8+TK-9 (both done); BriefComposeStage = SIBLING of the drain ComposeStage, mouth = ctx.model (S4) only, layer-1 per-drive BudgetGuard automatic on the bootstrap Engine, layer 2 wired LIVE by NEW `bootstrap.build_brief_compose_stage` sharing the `spend:tokens` row (ONE daily token cap across drain+brief; TK-96/97 MUST assemble via the factory — the Q-69 lesson made structural); template = `compose/brief_template.py` (layout normalized), ONE pure `render_brief_lines(artifact, tz)` = the prompt's user message = the S8 fallback EXACTLY (cannot drift), Q-50 user-facing-only; degrade = compose.py catch-set parity incl. BudgetExceededError, run() never raises, `transitions=('brief_deliver',)` freezes TK-101's stage name; output wire BRIEF_TEXT `{text,degraded,tokens_spent}` via new artifacts.py helpers, no domain type; files 2→4 (+artifacts.py +bootstrap.py), estimate S→M. **TK-100 ✅ v0.96 (BriefComposeStage built + independently adversarially verified, PASS — nothing blocking):** the Q-77 shape landed exactly — sibling stage (compose.py untouched), exactly-once ctx.model call behind the layer-2 ceiling check, ONE pure `render_brief_lines` feeding BOTH the model user message AND the S8 fallback (single source of truth proven by driving the failure path), CON-1 proven by the verifier's OWN adversarial render (scoring keys/raw ids stuffed in → ZERO matches in the rendered prompt), Q-76 option (b) confirmed both directions (both-in-prep → sealed local start–end times; either missing → honest day-level line; detect_conflicts never re-invoked), S8 catch-set parity with compose.py (diff + independent CancelledError probe), `bootstrap.build_brief_compose_stage` wires the budget LIVE on the shared `spend:tokens` row (the Q-69 lesson structural; TK-96/97 MUST assemble via it), BRIEF_TEXT wire `{text,degraded,tokens_spent}` (tokens honest: real spend on success, 0 on fallback — the compose None-vs-0 divergence documented, deliberate), transitions freeze TK-101's name as `brief_deliver`; 540/37, ruff clean, zero new mypy, factory test correctly placed in tests/unit/test_runtime.py (sibling precedent, not creep). **TK-101 ✅ v0.98 (BriefDeliverStage built + independently adversarially verified, PASS — nothing blocking; THE BRIEF CLUSTER’S FOUR STAGES ARE ALL BUILT):** the Q-78 shape landed — brief_deliver TERMINAL (transitions=()), BRIEF_TEXT via ctx.last_output(brief_compose), tz-local run-keyed header append + stdout echo, injected speak seam (EP-30 NARROWED as ruled — the LAST Q-56 built-path over-pull DISCHARGED; voice stays v1 per DEC-6/7, TK-164 binds real TTS into this seam), terminal Done BRIEF_DELIVERED {delivered_at, voice_spoken, replay}, never touches ctx.journal (load-bearing spy), no model call. AC4 run_id-keyed file-marker idempotency PROVEN by the verifier’s own harness (same run_id → byte-identical file + replay=true; prefix-collision safe — bracket-delimited; malformed-marker re-appends; first-run guarded); speak-once-verbatim / None→text-only+warning / raising-speak→text stands; sink write failure raises LOUD (voice = only swallowed path); optional WOMBAT_BRIEF_PATH + WOMBAT_VOICE_ENABLED (not REQUIRED_ENV); bootstrap.build_brief_deliver_stage (blank path → ConfigurationError; TK-96/97 MUST assemble via it). **ARCHITECT RULING (v0.98, cosmetic HOW):** the as-built simpler bracketed header ([run=<id>] delivered_at=<iso>) ACCEPTED over the Q-78 decorative line — AC1 fully satisfied, cleaner idempotency marker, parity would be churn; recorded as the v1 header shape (builder flagged, Q-39 precedent). 554/37, ruff clean, zero new mypy. **TK-96 ✅ v1.00 (walking-skeleton brief pathway ASSEMBLED + independently adversarially verified, PASS — THE FEAT-7 MORNING-BRIEF CHAIN NOW FIRES END-TO-END):** the Q-79 shape landed exactly — NEW pathways/brief_pathway.py (BRIEF_PATHWAY_ID `wombat.brief`, build_brief_pathway mirroring build_drain_pathway, no bootstrap import/no cycle, PUBLIC brief_trigger_artifact seed); sources/bootstrap.py behavior-preserving poller extraction (TK-16's 6 tests UNCHANGED) + build_brief_fetches (unwired source → lazy-raising placeholder → gather degrades *_unavailable — honest degraded brief on a Google-less boot, proven e2e both-unavailable COMPLETED without crashing); bootstrap.assemble_runtime registers `wombat.brief` off the SAME composed gate (select_items=gate.select_items — ONE gate, ONE pending set, **ASMP-2 HELD**, verifier proved no second Gate) via the MANDATORY compose/deliver factories (shared spend:tokens row + loud sink LIVE — the Q-69 lesson structural); registration CONDITIONAL on WOMBAT_BRIEF_PATH (blank → loud skip, drain unaffected); additive RuntimeBundle.brief_pathway_id = the TK-97 entrypoint. AC1 e2e fire REAL: real cog-worx Engine walked ALL FOUR stages via the registered graph (final.steps contains brief_force_flush — only possible if the Engine walked it); exactly one brief per invocation by exact run-header count; no stage-file edits; 563/37, ruff clean, zero new mypy, scope exact. **Q-56 residue is now EXACTLY TK-77→EP-13 (the last one).** Remaining path to the brief: **TK-97 BRIEFED + BUILD-READY (Q-80 rulings LOCKED v1.01 — the LAST ticket of the arc):** mechanism = Wait-SELF-PARKING scheduler stage (cog-worx has NO standalone cron timer — Timer is run-bound, armed only by a committed Wait), pathway `wombat.brief_schedule` re-driven by the existing TK-53 Sweeper — **v1.02 amendment (builder-flagged, runnable-proven):** StageGraph unconditionally requires a reachable terminal stage, so `BriefTimerStage.transitions=('brief_timer','brief_timer_terminal')` with a never-reached `BriefTimerTerminalStage` stub (transitions=(), raises if entered) in brief_pathway.py — the TK-53 `_WaitForeverStage`/`_TerminalStage` precedent, runtime behavior unchanged; fence-READ (BriefRunLedger over the SAME composed DailyLedger, row `brief:run`) + MARK-after-COMPLETED; brief run_id is wombat-day-keyed (`wombat-brief-<date>`) so TK-101's proven marker closes the crash-after-Deliver double window; **AC4 reworded to the honest v1 exactly-once-DELIVERY guarantee** (cross-restart journaled resume rides the TK-53 cold-boot deferral); morning_brief_time already homed in OperatingParams (no params change); scheduler registered inside the same WOMBAT_BRIEF_PATH conditional (blank → loud skip of both, serve() never crashes) + additive RuntimeBundle.brief_schedule_pathway_id + a second boot drive in serve(); deps EP-9→TK-28 (all done), files 3→5 (+bootstrap.py +runtime.py), estimate M. **TK-97 ✅ v1.03 (durable once-daily brief timer BUILT + independently adversarially verified, PASS — nothing blocking; THE ARC IS DONE):** the Q-80-as-amended shape landed exactly — `domain/brief_schedule.py` BriefRunLedger (row `brief:run` over the SAME composed DailyLedger) + pure tz-aware `next_fire_at`/`is_due` (DST both directions fold=0, midnight straddle, strict >now); Wait-self-parking `BriefTimerStage` (fence-READ gates the fire, mark_ran ONLY after RunStatus.COMPLETED, loud-no-mark-no-raise on crash/non-COMPLETED, DEBUG skip when already ran, never touches ctx.journal, no model call); two-stage `wombat.brief_schedule` graph with the never-reached raising `BriefTimerTerminalStage` stub (StageGraphError genuinely reproduced without it — the v1.02 amendment is REAL); `fire_brief` closure built AFTER build_engine with wombat-day-keyed run_id `wombat-brief-<date>`, registered inside the SAME WOMBAT_BRIEF_PATH conditional (+ additive RuntimeBundle.brief_schedule_pathway_id); serve() fires a second boot drive only when registered (None → skip, no crash — boot arm + crash-miss catch). VERIFIED: fence proven on real pg by the verifier's OWN script (raising fire → ledger 0, no double-mark; same-day double-drive → one fire, skip); AC4 BOTH crash windows genuine on docker pg (crash-before-Deliver → unmarked re-fire, ONE brief; crash-after-append-before-mark → day-keyed run_id + TK-101 marker → replay=true, header count stays 1 — the run_id→marker tie is structural); AC2 sleep-jump + cold-boot via one reused tick()-driven Sweeper; full DST battery; honest exactly-once-DELIVERY framing (no over-claim). 590/38 no-DSN, 639/4 with docker pg, ruff clean, zero new mypy, scope exact. One non-blocking note: `next_fire_at`/`is_due` don't runtime-guard naive datetimes (rides the codebase-wide aware-clock convention; note only). Feature/epic status fields left as-recorded per the EP-15/EP-16 precedent — FEAT-7 flips only after live verification. **THE ONLY GATE TO THE FIRST LIVE MORNING BRIEF IS JIM'S OAUTH BRING-UP (client + 2 consent passes + enable Gmail API) + THE TWO ARMED LIVE SMOKES (gcal + gmail) — an operator step, NOT a build blocker.** **TK-28 DONE (v0.90, independently adversarially verified — EP-9 support work COMPLETE; the DEC-13/RISK-2 quiet-across-midnight posture is CLOSED):** decay over the journaled `added_at` through the JOURNALED `PendingSet.remove()` (decayed items never resurrect on rebuild — RISK-5 reinforced); `DayRollover` exactly-once `LedgerReset` via `DailyLedger.increment("rollover:gate")==1` — the verifier attacked the exactly-once claim on real docker pg with its own 3-boundary + reconnect probe (composite `(ledger_name, wombat_date)` PK gives each day an independent counter; same-day repeats + mid-day restart silent); kill-consistency four genuine sub-proofs (incl. post-restart ≤ ceiling−N and TK-24 kill-mid-decay convergence); wake-burst K=5>ceiling=2 → exactly 2 surfacings + 1 reset + pre-sleep items decayed; pipeline purely additive (TK-27 arms BYTE-IDENTICAL, diff-proven); Gate ctor args REQUIRED (the Q-69 lesson); 476/37 no-DSN, 519/4 with DSN; scope exact. Heartbeat arc-note discharge stands (Q-73 — decay-before-arms, no idle heartbeat). TK-75 landed the Q-65 shape exactly (readonly-only credential probed live; transport-only; the body-key guard proven load-bearing by a planted rogue ref). Guard-allowlist text CORRECTED to the as-built four files (real consumer = stages/ingest_email_body.py; taint.py is docstring-only) — Q-39 precedent, builder flagged not deviated. GMAIL_LIVE smoke armed awaiting Jim's gmail bring-up (consent + enable Gmail API on the Cloud app). TK-148 = the trifecta break, adversarially verified on the real framework (drive-level latch dominates per-stage policy; content-independent; irreversible; refusal trail proven on real pg) — its boundary is now locked by DEC-26. TK-146 landed the Q-63 shape exactly at the full Q-46 done-bar (12 real-pg tests RAN; six-way 0-rowcount discrimination probed live; first-write-wins timestamps; TK-147 read contract + TK-148 refusal surface exist as ruled). **ASMP-2 gained a REVISIT-INVENTORY note (v0.72):** the trail writer's two-transaction discrimination path is single-process-safe only — any future multi-drainer relaxation must include it in the replacement inventory alongside the queue lease reclaim. **EP-16 (conflict detection) COMPLETE; the calendar branch is code-complete** (only the TK-16 wire remains on EP-15). TK-74 landed the Q-62 shape exactly (projection adapter over the frozen kernel, 7/7 adversarial tz probes incl. DST both directions + UTC+14, all_day excluded end-to-end, `conflict_to_payload` stamping CALENDAR_CONFLICT). **DEF-4 (v0.70):** AC2's next-WORKING-day prose reworded to next-CALENDAR-day (record honesty — no frozen type carries weekday); business-day-aware slots deferred with revisit trigger riding DEF-2's rating pass. TK-72 landed the Q-59/Q-60 shape exactly (structural no-write fake session, DST-boundary all-day proof, narrow excepts preserving the degrade path, canonical-key dedup via a real registry, TK-73 spike byte-untouched; built at production rigor though contract level is mvp — recorded). **Q-61 (v0.68): the AuthorizedSession wire was UNOWNED** (constructed nowhere; TK-53's non_goal excludes source registration) → new **TK-16** (EP-15, S; deps TK-71/72) owns `make_calendar_session` + `sources/bootstrap.py` `build_source_registry` (ASMP-2 enqueue-only; loud-skip Google-less boot; TK-75 gmail wiring inherits the pattern) + the WOMBAT_TEST_GCAL_LIVE-gated live-wire smoke. **STANDING PRE-LIVE GATE now has TWO items: TK-29 (pg pending journal) AND TK-16's live smoke — both green before the first live laptop session.** TK-15 landed the Q-58 packaging fix proven load-bearing (shadow-restore repro), all six bare-support importers qualified, TK-71 stopgap removed, 379/1 with DSN. **TK-72 briefed (Q-59, v0.65) — NEXT:** as-built TK-3 Protocol wins (registry owns the QueueItem mapping); sanctioned registry rider swaps the interim colon-join for TK-12's canonical `idempotency_key(source.id, event.event_key)` (both parked Q-35 findings on TK-72 CLEARED); AuthorizedSession over Calendar v3 REST, no google-api-python-client; transient errors caught inside poll() → log + `[]`, never raise (as-built registry degrade STOPS a raising source — a blip must not permanently kill calendar ingestion); lookahead ctor arg 48.0 AC-fixed; NO event_class on raw calendar events (ItemKind.GENERIC fallback; CALENDAR_EVENT would be a TK-41 vocabulary version bump, revisit at DEF-1); models.py promotion keeps TK-73 frozen tests green. TK-71 landed the Q-57 shape exactly (keyring-only custody on DPAPI-backed WinVaultKeyring, strong-form AC3, app boots Google-less, live smoke armed awaiting Jim's OAuth-client bring-up). **Q-58 (v0.63): the TK-71 verifier found a real repo-wide packaging bug** — tests/calendar/ shadows stdlib `calendar` once google-auth is in the tree, aborting whole-suite collection; ruled proper-packaging fix as standalone **TK-15** (tests/ becomes a real package, pythonpath entry removed, bare support.* imports qualified, TK-71's stopgap guard removed in the same pass, stdlib-wins regression test); TK-72 now depends on TK-15. **Convention from TK-15 on: no test dir may ever be importable as a bare top-level name.** The deterministic personalized gate is PRODUCTION-REAL: **TK-27** convergence built + verified with zero discrepancies (immediate arm + per-EventClass ceiling on a real DailyLedger; flush arm with min-age + durable journaled clear; select_items bit-identical-pending proof; one shared worth predicate; write-ahead pending custody; guard ENFORCING; async ratings_for composition). Full stack: 6 spikes + vocab + keystone **TK-14→TK-1→TK-13** + **TK-42/23/25** + **TK-2/152** + drain spine **TK-5→6→8→10→7** + **TK-11** + **TK-27**, v0.25–0.56; **344 tests green w/ docker pg**, ruff+mypy strict clean. DEC-13/CON-3 quiet-by-default is structurally enforced end-to-end. **TK-11 BUILT + VERIFIED (v0.54):** silence guarantee proven by full-space enumeration (zero breaches; only ACTIVE∧confident∧fresh surfaces), property test proven real by mutation (31 failures), 8h sleep-gap holds, provider degrade never raises, no baked defaults (bounds live in wombat_params.yaml v2 — 3 new presence fields), TK-4 spike deleted, all 4 real-Engine e2e variants still green. **STUB-GATE CHECKPOINT REACHED (v0.52):** the whole spine runs end-to-end on a REAL cog-worx Engine — all 4 e2e variants proven (surface→composed_output; hold→hold_report + silence; degrade→template keyless; idle→WAITING re-park). Q-53's Wait-self-edge defect in TK-5 fixed under TK-7's rider (revert reproduces StageGraphError — fix proven real). **TK-10 BUILT + VERIFIED (v0.49):** engine-driven Transition dispatch (never invokes composers; transitions tuple = declared graph edges), CON-1 payload boundary STRUCTURAL (compose_request.payload from queue_item.payload alone; adversarial zero-leakage proven), unknown kind → compose + warning, surfaced_item wire + `.strip()` rider landed. **TK-8 BUILT + VERIFIED (v0.47):** run() never raises on model failure (all 4 degrade triggers → template, degraded=True; BudgetExceeded degrades not raises), AC3 key-check at construction, prompt = payload-only, terminal Done(`wombat.composed_output`), both wires JSON-native w/ regressions. **Two v0.47 riders on TK-10:** (1) it OWNS the compose_request.payload boundary — user-facing fields ONLY, never scores/GateAction/internals, with a no-internal-keys test (ComposeStage renders payload verbatim — the wire IS the guarantee); (2) sanctioned 1-line `.strip()` AC3 whitespace hardening in compose.py (whitespace key = silent template-forever, the failure AC3 exists to prevent). **TK-6 BUILT + VERIFIED (v0.45):** presence fail-safe proven (UNKNOWN/IDLE/AWAY/low-conf/None → HOLD; only ACTIVE+confident+high surfaces), zero model calls structurally, canonical vocab imported not redefined, ONE `wombat.gate_decisions` batch artifact → review_or_speak with original queue-item dicts (TK-7 acks), Q-49 JSON-native wire fix in with TWO wire regressions locking the rule for TK-7/8/10; the TK-27 replacement seam (injected evaluate callable) as-built and waiting. **TK-5 BUILT + VERIFIED (v0.43, all_pass=true):** drain(limit) is real two-CTE SQL (not a slice; un-drained rows stay unleased); the Q-47 spine conventions are **as-built** (artifact kinds/names, shared artifacts.py helpers, Wait-from-ctx.clock, ctx-surface = clock only, stage_context_fake reusable double) **plus the as-built Provenance convention** — control-plane artifacts carry `Provenance(source="system", confidence=1.0, recorded_at=ctx.clock())`, matching cogworx's own deterministic-artifact idiom; TK-6/7/8/10 inherit it. DEC-25 (v0.27) ruled TK-13's AC4 guard shape (declared-consumer-list scan, `_sim` spikes excluded, enforcing when TK-27/28/9/49 land). **TK-42 BUILT + VERIFIED (v0.30)** per the Q-41 rulings: `async def ratings_for` (no asyncio.run), injected raw EntityKG + user_id → scope `user:<user_id>`, `resolve_event_class` via payload key + total ItemKind fallback map, wire helpers ADDED to rating/params.py. **TK-23 BUILT + VERIFIED (v0.33, independent verifier all_pass=true)** per the Q-42/Q-43 rulings: `urgency = clamp01(urgency_base + urgency_gain * raw)` / load analogous, 7 spike constants frozen as module constants, ScoringParams fully DELETED (RatingParams sole params type), AST-proven purity, payload totality (missing→silent default, invalid→default+warning), identity-params port across trigger_sim/spike tests/fixture builder bitwise-exact (TK-22/TK-26 evidence undisturbed), real 69-item fixture at 100% agreement. **TK-25 BUILT + VERIFIED (v0.36, verifier fix_confirmed=true with adversarial live re-repro)** per Q-44/Q-45: PendingJournal Protocol + InMemory double, atomic journaled clear() returning drained items, CapacityEviction returned from add(), aggregator.py pure math; the verifier-found eviction-atomicity bug FIXED (Remove-before-Add ordering, 3 non-tautological kill regressions, all three kill points coherent); TK-24 spike tests ported, spike test file deleted. **TK-2 BUILT + VERIFIED (v0.38, verifier all_pass=true, the 4 AC tests RAN against a real throwaway postgres:16)** per Q-46: DB-level idempotency, pre-insert capacity (leased rows count), epoch-based lease reclaim across restart (ack exactly-once), loud DSN-gated skip, idempotent `ensure_schema`, psycopg[binary] v3 — the canonical wombat pg conventions are now **as-built** for TK-29/TK-152. **ASMP-2 recorded:** at most ONE live draining WombatQueue process-wide (epoch reclaim sound only single-host/single-process; terminate-before-restart; inherited by TK-5/6/53/29 + any supervisor ticket). **TK-152 BUILT + VERIFIED (v0.41, verifier all_pass=true, DB tests RAN against real pg)** realizing DEC-21: pure `wombat_today(instant, tz)` (no clock read, naive-instant ValueError), load-on-start preserve-not-zero (live probe), midnight-straddle = two distinct rows by access-time computation (no timer), cross-zone determinism — the Q-15 split-definition bug is closed as-built; Q-46-conformant (injected DSN, lazy connection, idempotent migration 003). Jim commits per-ticket.
- **STANDING OBLIGATION (Q-41/v0.30):** the provisional rating-claim wire contract is now **as-built** — **TK-44 and TK-49 MUST write through the rating/params.py helpers** (predicate=RATING_CLAIM_PREDICATE, entity=EventClass value, payload=JSON {version, urgency_base, urgency_gain, load_base, load_gain}, newest active claim wins; malformed/unknown-version → ValueError, which the read seam catches → defaults + warning).
- **DEMO-MILESTONE DIRECTIVE (v0.39, Jim via coordinator):** build to the smallest end-to-end thesis demo — enqueue → drain → REAL personalized deterministic gate → surface/hold → router → mouth (or terse degrade) → output; mostly silence + journaled holds + one crisp surfaced line; stale presence = total hold; kill mid-drain = exactly-once redelivery. **MILESTONE DELIVERED (v0.57): the demo harness is BUILT and RUNS** (`scripts/demo_drain.py`, 351 tests green vs real docker pg) — audited-config boot, LIVE DEC-13 personalization (seeded claim moved 0.66→1.00), happy path (VIP→immediate, newsletter→hold, thread→flush digest), the REAL presence provider honestly holding everything in a headless session, the two Q-54 defense layers distinguishable, honesty notes every run. **Blessed correctness fix on the record:** the gate_decisions wire now carries `scored_items: list` (0..N) — the old one-item shape was a stub-gate artifact structurally unable to represent real HOLD (0) / FLUSH (N) decisions. **BUILD LOOP STOPPED per v0.39 — awaiting Jim's post-demo routing** (TK-28/TK-29 durability track vs the ingestion front toward the real morning brief). **Post-demo queue (deferred not dropped):** TK-28 (decay/midnight/kill-proof), TK-29 (pg pending journal — REQUIRED before first LIVE session; preserves Q-45 ordering + inherits the added_at wire), TK-12, TK-9, TK-3. Infra: one docker pg (queue + ledger), everything else cold-boot in-memory, DeepSeek optional (dummy key → terse degrade). Flush evaluates on item-carrying cycles (dedicated heartbeat tick = TK-28/TK-97 territory).
- **INGESTION-ARC ROUTING (v0.58, Jim's post-demo track):** build resumes toward the LIVE morning brief (FEAT-7, the v1 wedge). **TK-3 (SourceRegistry) is the confirmed first ticket** — sole dep TK-2 (done), atomic mvp/S, nothing gates it. **TK-3 is now DONE (v0.59):** built + independently verified (all 4 ACs runnable-confirmed; injected Enqueuer seam per Q-36/Q-46 with the pg-gated e2e RUN against real docker pg; ASMP-2 enqueue-only upheld by inspection; ruff+mypy clean; 356 passed/0 skipped with DSN, no regressions). TK-12 is NOT a TK-3 prerequisite but MUST land before TK-71/72/75 (EP-2 epic-dep) — slotted second. **The dependency-correct arc order:** ① TK-3 ✅ → ② TK-12 ✅ (netstring-injective key + ItemRef + task-linking convention, v0.60; TK-72/75 pollers + TK-77 stamping MUST derive through it — their dedup ACs now resolve via item_identity.py, the Q-35 identity edges' landing spot) → ③ TK-71 ✅ v0.62 → ③b TK-15 ✅ v0.64 (Q-58 packaging fix) → ④ TK-72 ✅ v0.67 (Q-59/Q-60 as-ruled; `CalendarEvent` domain type live; TK-73 spike frozen) → ⑤ TK-74 ✅ v0.70 (Q-62 as-ruled; EP-16 COMPLETE; `conflict_to_payload` is the ONE stamping helper for TK-98/99; DEF-4 next-calendar-day fallback) → ⑥ TK-146 ✅ v0.72 (Q-63 as-ruled; TK-147 read contract + TK-148 refusal surface live; carry-forwards standing: action_id derivation → TK-149 briefing, BLOCKED-row rendering → TK-147 briefing) → ⑦ TK-148 ✅ v0.74 (Q-64 as-ruled; adversarially security-verified — 7/7 fresh attacks blocked incl. permissive-policy re-bind; the verifier's 8th probe found the guarantee's exact boundary → **DEC-26**; TK-75's frozen contract documented) → ⑧ TK-75 ✅ v0.76 (Q-65 as-ruled; readonly-only token; body-key guard load-bearing, allowlist corrected to four as-built files) → ⑨ TK-76 ✅ v0.78 (Q-66 as-ruled; metadata-only proven two ways — the builder tripped the body-key guard with a docstring and rewrote, live-proving it) → ⑩ TK-16 ✅ v0.80 (Q-61/Q-67 as-ruled; EP-15 complete; pre-live gate fully armed) → ⑪ **TK-9 (NEXT — briefed, Q-68 rulings LOCKED v0.81; ALL budget seams VERIFIED against the installed framework:** BudgetPolicy/new_guard-per-drive + GuardedModel start_call/record + CF-3.0-B deferral verbatim; exception = `BudgetExceededError` (as-built compose already catches it — Q-50's zero-compose-change promise HOLDS, layer 1 = real ceilings on the unbounded bootstrap `BudgetPolicy()`); **MIGRATION 002 RELEASED — no new table**, DailySpendLedger = thin wrapper over the as-built shared DailyLedger, `spend:tokens`, TOKEN-denominated daily cap; pre-call ceiling → template degrade without model invocation; ledger read failure FAILS CLOSED to template; write failure → loud log, output stands; tokens ride composed_output as additive `tokens_spent` (engine journals it); token truth = `ModelResponse.usage` prompt+completion; ceilings = 3 new TK-13 tunables (YAML bump); one DSN-gated wrapper test = the Q-46 done-bar part**)** → ⑫ TK-29 ✅ v0.85 (Q-70 as-ruled; 5/5 adversarial durability probes incl. exact crash-prefix + seq-not-created_at ordering; **pre-live gate now = the TWO OAuth smokes only**; RISK-5 durability honestly claimable once TK-53 wires this adapter at boot) → ⑬ **TK-53 ✅ v0.87 (built + independently adversarially verified — all 5 ACs genuine, AC5 full cycle on real docker pg; RISK-5 boot obligation MET; one concern homed Q-72→TK-17 openai core-dep; briefed Q-71 rulings LOCKED v0.86, estimate S→M; ALL runtime seams VERIFIED:** `Sweeper(*, journal, fire=engine.fire_timer, clock)` / `run_forever(*, interval, lease_ttl)` / tick→`claim_due_timers`→fire (S6 replay); `Engine.run(*, run_id, session_id, pathway_id, initial, pathway_version)` with its own double-drive guard; `PathwayRegistry.register(id, graph)`. **Loop = ONE initial drive on `wombat.drain` + run_forever re-driving the Wait-self-park — no authored loop.** Three unowned wires ABSORBED with ACs: drain-pathway registration + real-gate composition (TK-29 pg journal isinstance-asserted at boot) in `bootstrap.assemble_runtime()`; SourceRegistry start/stop in `serve()`; DSN wiring via new optional `WOMBAT_PG_DSN` (serve fails loud without, Q-36). `python -m wombat` entrypoint IN scope; shutdown minimal (terminate-before-restart = documented ASMP-2 operator note); sweeper cadence = 2 new OperatingParams fields; tests tick()-driven never unbounded, DSN-gated assembly + full-cycle + shutdown; v1 restart = clean slate (real-substrate resume = recorded non_goal)**)** → TK-53 (now carries the Q-69 budget-live-at-boot AC) → TK-28 → brief cluster from TK-98. → then TK-29 → TK-53 → TK-28 → brief cluster from TK-98. *(superseded briefing note kept for the record:* Q-66 rulings LOCKED v0.77; VERDICT BUILDABLE NOW: the first Q-56 over-pull ruled at its frontier — **EP-13 dep DROPPED** (pure function + versioned rules + wire helpers; the S1 off-path property enforced by a STRUCTURAL no-import guard over the drain-spine modules, replacing the unbuildable runtime-trace AC; invocation sites belong to consumers — TK-98 gather now, nightly = EP-13's consumers later; sole dep TK-75 done); **METADATA-ONLY triage** (subject/sender/received_at, never body_text — TK-77 owns body-deep under the latch; the Q-65 guard actively enforces this: triage.py is NOT on the allowlist, so a body_text ref fails the build); rules file = own versioned artifact w/ schema-validating loud loader (not TK-13); TriageResult = frozen dataclass, closed HIGH|NORMAL band, rule-NAMES-only matched_rules, JSON-native helpers; pure-unit tests, no gating. **Q-56 over-pulls remaining: TK-77→EP-13, TK-96→EP-17/EP-30, TK-101→EP-30**)** **(TK-71 briefing record, Q-57 rulings LOCKED v0.61:** OS-keyring-as-sole-token-store (no wombat token file ever; TokenStore Protocol + injected fake; vault failure = loud ConfigurationError, never plaintext fallback) · secrets = WombatConfig optional env fields GOOGLE_OAUTH_CLIENT_ID/GOOGLE_OAUTH_CLIENT_SECRET, NOT in REQUIRED_ENV, validated at gcal-auth construction (TK-8 precedent) · CI mocked zero-network + ONE live smoke gated WOMBAT_TEST_GCAL_LIVE=1 loud-skip, refresh+scope-inspect only · one-time consent CLI `python -m wombat.integrations.gcal.auth` = **the Jim bring-up step** (Google Cloud OAuth client + browser consent; parallel, not a blocker) · deps google-auth-oauthlib + keyring CORE in pyproject (google-api-python-client is TK-72's) · seam: ONE injected CalendarAuth credentials seam consumed by TK-72; TK-71 = obtain+store+refresh+scope-guard ONLY, zero calendar reads · files_in_scope += config.py, pyproject.toml, uv.lock · the one parked TK-71 audit finding (key provenance) CLEARED. Egress: read-only Google API in-frame per locked DEC-3/DEC-9)** → calendar branch ③ TK-71 → ④ TK-72 → ⑤ TK-74 ∥ gmail branch ⑥ TK-146 → ⑦ TK-148 → ⑧ TK-75 → ⑨ TK-76 → ready-now supports ⑩ TK-53 (Sweeper, before TK-97) ⑪ TK-9 (before TK-100 via EP-5) ⑫ TK-29 (REQUIRED before first LIVE session, Q-44) ⑫b TK-16 (Q-61 source-composition wire; live smoke REQUIRED before first LIVE session) ⑬ TK-28 (before TK-96/97 via EP-9; owns the heartbeat tick) → brief cluster ⑭ TK-96 (skeleton) → ⑮ TK-98 → ⑯ TK-99 → ⑰ TK-100 → ⑱ TK-101 → ⑲ TK-97 (timer LAST). **Q-56 (parked-to-frontier):** four suspected epic-dep over-pulls on the brief cluster (TK-98→EP-17 dragging TK-77/TK-80+EP-13 dream cluster; TK-76/77→EP-13; TK-96→EP-17/EP-30 vs its stubs-acceptable/no-voice non_goals; TK-101→EP-30 vs its text-only degrade AC) — each ruled at its own /plan-ticket; a brief-cluster ticket is not built until its over-pull is explicitly ruled. **TK-3 briefing notes:** AC1's "enqueued into WombatQueue end-to-end" resolves under Q-36/Q-46 as an injected enqueue seam (unit tests stub it) + ONE DSN-gated integration check against real pg; ASMP-2 — the registry is ENQUEUE-ONLY, never drains; Q-32 namesake warning — "SourceRegistry" is wombat's OWN InputSource contract, NOT the cog-worx class; poll loops = asyncio tasks matching the async spine (await-based Wait/Sweeper world), a plan-time how.
- **De-risk spike sequence set (DEC-24, v0.20):** run the 6 RISK-* spikes BEFORE the production build. Pure-code spikes run NOW (zero input/infra): **TK-21→TK-24** (RISK-5) and **TK-41→TK-48** (RISK-4). Then **TK-21→TK-22** (RISK-1), **TK-22→TK-26** (RISK-2). **TK-73** (RISK-6) runs ahead of the poller against TK-72's CalendarEventItem type. **TK-4** (RISK-3) last (needs TK-3 + live laptop). Real-data ruling: fixtures seeded from REAL Gmail/Calendar via MCP into a **gitignored local** YAML (NG-7 = storage residency, local read is no egress); human label via a **proxy rubric from vision.md's gate** + a small one-time Jim confirmatory pass.
- 30 epics → **80 atomic tickets** (76 + TK-52/53/54 from the audit dream-cluster resolution + TK-29 from the Q-44 pending-journal split). depends_on graph **re-proven acyclic** (one cycle TK-78→TK-149→TK-151→TK-149 broke by dropping TK-149 from TK-151's deps; TK-29 is a dependency sink — edges only to TK-25/TK-2).
- **Frontier constructibility audit (2026-06-21) PRIORITY TRANCHE resolved** (v0.18): 6 CRITICAL + 17 seam-misses + 3 splits, clustered by root cause into Q-30..Q-34 + DEC-23; the 68 HIGH + 82 MEDIUM + 22 weak-atomicity findings PARKED at Q-35 (triaged just-in-time at each `/plan-ticket`). Audit file: `planning/audit-2026-06-21.md`.
- **ALL /plan-analyze red-team blockers cleared** (ISS-4/5/6 + Q-10..15). **Q-10/DEC-20 external-tier sign-off SETTLED (DEC-22)** — zero remaining human dependencies.
- **v0.9 floating MEDIUM/LOW findings now homed (v0.12):** the 8 findings the v0.9 red-team surfaced "for architect triage, not yet ticketed" (A–G) + 2 fresh (H,I) + atomicity — all raised as Q-16..Q-27 and resolved in-frame. 6 owning/coherence tickets added (TK-11 presence-harden, TK-12 item-identity, TK-13 param-store, TK-51 feedback-channel, TK-80 ReplyIntent-builder, TK-153 web-taint-split); TK-50 and TK-148 split. `/plan-analyze` complete → **ready for per-ticket build** (`/plan-ticket <id>`).

## Live blockers / live dependencies

**None remaining.** The last live dependency (Q-10 / DEC-20 external-tier sign-off) was **settled by the architect on 2026-06-20 under Jim's explicit delegation** (DEC-22 — authorization granted). What remains is a **build-time operational step** (flip the cog-worx external-tier flag in deploy config), tracked as a TK-151 bring-up acceptance criterion — not a planning blocker. TK-151 still fails closed until that flag is set, so outbound Gmail-draft + browser dispatch (TK-78/131/133/135) are correctly gated at runtime without any pending human planning decision. No CRITICAL contract issues open.

**Cleared 2026-06-20 (analyze):** ISS-4 (one canonical GateAction/item_kind enum on TK-21; consumers rewired), ISS-5 (TK-10 ComposeDispatchRouter; reflection/draft orphans closed), ISS-6 (TK-50 outcome-signal producer; RISK-7 mitigated), Q-11 (both-direction taint tags on TK-148), Q-12 (presence is a gate-level hold, not a scoring input), Q-13 (TK-78→78/79, TK-27→27/28 splits), Q-14 (epic-dep convention fixed + graph proven acyclic), Q-15 (DEC-21 wombat-day + TK-152 shared DailyLedger). All within authority except Q-10's sign-off (escalated). Two new DECs: **DEC-20** (external-tier admission), **DEC-21** (wombat-day boundary).
**Cleared earlier 2026-06-20 (tickets):** ISS-1/2/3 (src-layout, TK-9 budget, Gmail drive boundary).

## Decision log (append-only)

| Date | Decision | By | Refs |
|------|----------|-----|------|
| 2026-07-30 | **DEC-66 + EP-37 + TK-299 + DEF-13 (JIM FRAME RULING recorded; contract v2.157/v2.158): user-modeling is THE product spine — DEC-65 judged the correct v1 wedge, trajectory now contract-real.** Jim verbatim, post-DEC-65: "the whole point of this app is to learn and observe and build a model of its user. It not learning who i am is a massive problem." Checked against vision.md — the frame IS the recorded vision (compounding empirical user model, preference capture, "observant, learns to fit in the right way"), so no vision/scope change; the ruling aligns the contract's recorded trajectory. DEC-66: ONE user model, TWO halves — gate-facing empirical-behavioral (already built: behavior_events/outcomes/windows/patterns) + mouth-facing declarative (DEC-65); TK-292..TK-298 STAND UNCHANGED; observational growth (external-item derivations, behavioral claims into known_user_context, explicit capture) attaches to the pinned TK-294 store seams + ONE TK-296 render block, `source` column = provenance spine. EP-37 (FEAT-3, P2) owns it; TK-299 dream_derive minted as its first ticket (deterministic recurring-meetings + frequent-correspondents, NO LLM — the analyze gate requires epics to carry tickets) with DISPATCH GATED by DEF-13 (rescoped defer-minting→defer-dispatch, correction recorded append-only in v2.158). Steer: builders never collapse the source column; TK-297 stays chat-only — EP-37 owns other inputs. | architect | ISS-29, DEC-66, EP-37, TK-299, DEF-13, contract v2.157–158 |
| 2026-07-30 | **ISS-29 + DEC-65 + DEF-12 + TK-292..TK-298 (JIM DIRECTIVE recorded, delegated hows ruled within authority; contract v2.156): the Steward becomes a COMPANION in chat.** Jim verbatim — too stiff, jokes not understood, more personable, chat need not be about its role, "Getting to know me is important too". Diagnosis structural: one terse-steward instruction serves every chat turn; zero user knowledge in any prompt (queue DELETEs on ack); the matrix can't fix a base-role problem. Ruled: (a) FIFTH mouth `Mouth.CHAT` — pinned companion base role with assistant_name + NEW `wombat_user_name` (app-editable), policy axes granted, guard = "No preamble. " + the FULL DEC-62 charter at the strip-proof expression seam; four original mouths' DEFAULT byte-identity re-pinned (supersedes-in-part DEC-33/37's byte-identity claim, scoped-not-weakened). (b) ComposeStage selects by item_kind — instruction SELECTION only; DEC-64's compose-side-injection rejection honored. (c) Durable `wombat_user_facts` (mig 011, 200-cap evict-oldest, never purged — scratchpad's 14d purge disqualified reuse) + `wombat_chat_turns` (mig 012, user utterances only, 7d, dream-input ONLY — DEC-64's multi-turn-history rejection stands). (d) Facts ride the DEC-64 source-side payload stamp via a shared closure on ASRSource AND a new ChatSource context_hook — typed chat gains grounding parity for free. (e) Organic getting-to-know = nightly `dream_facts` (DEC-23 admission, one budget-guarded call, deterministic custody: 5/night, clamp, dedupe, CON-6 motive/clinical screen, CON-4 journal). (f) ONE conditional charter accuracy sentence — every cannot/never clause byte-retained. Explicit remember/forget = DEF-12 (Q-109(b) discipline). No new egress; gate/loop untouched; DEF-8 untouched. Batch TK-292→298. | architect | ISS-29, DEC-65, DEF-12, TK-292..298, contract v2.156 |
| 2026-07-19 | **Closing records (COMMIT — within authority; contract v2.122): the chat round-trip is PROVEN LIVE end-to-end, and the day's register closes.** (1) Driven test (boot 19:33:13, HEAD 33a6d24): send ~19:33:30 → TK-273's own log lines narrate the whole path — 19:33:56 gate decision item_kind=chat action=hold urgency=0.5225 load=0.5 → compose dispatch → DeepSeek 200 → reply rendered IN THE PANE inside the 30s window, ZERO speech_shape/TTS — the DEC-57 quiet-held path byte-exact as ruled; ISS-23 closed operator-visibly; MANUAL_TEST.md Part B (the last open item of the original E2E) complete. (2) TK-273 first evidence: the 19:33:21 boot-time decision burst shows the pending-set backlog flushing (gmail items, all hold, urgency=None) — consistent with the backlog-flush suspect; the next composed recurrence names the cluster definitively; urgency=None judged benign-until-evidence (backlog replays reach the log without fresh scores; route only if the named recurrence shows a scoring anomaly). (3) **ISS-24 → TK-274** (P2, S, mvp, EP-1): the console guard's unanchored `-m wombat` regex (wombat-console.ps1:69) matches `-m wombat.settings_app` — app-before-console launch bricks the console (reproduced 2x, screenshot-verified); fix = end-or-whitespace anchor `-m wombat(\s|$)` at every match site in BOTH scripts — the TK-238 CIM trailing-space quirk is HONORED by the anchor (trailing space IS whitespace); TK-261's port lock stays the runtime-side ASMP-2 backstop. (4) Persona watch-note, no action: the reply addressed the operator as 'wombat' — compose has no speaker-name awareness; Jim's to raise; DEC-38 policy surface is the natural home. TK-274 is the sole open ticket. | architect | ISS-23, ISS-24, TK-274, TK-273, DEC-57, TK-238, TK-261, DEC-38, contract v2.122 |
| 2026-07-19 | **ISS-23 + DEC-57 + TK-272/TK-273 + record corrections (COMMIT — within authority; contract v2.118): chat turns are GATE-ABSORBED — the real root cause of every missing chat reply.** Re-driven test (boot 18:21:48, HEAD 1e30a71): send accepted, item drained, ZERO DeepSeek calls through 18:37+ — the turn never reached compose. Source-proven: `ItemKind.CHAT → EventClass.GENERIC` (user_model.py:49; NO CHAT class in the closed vocabulary) → sub-threshold urgency (0.75 strict, wombat_params.yaml:25) → HOLD → ReviewOrSpeak terminal hold_report ack (Q-96/ISS-4 hold-silence, minted for UNSOLICITED surfacings) → absorbed into the durable pending set. PREMISE CORRECTIONS appended (never rewritten) to ISS-21/ISS-22/Q-117: every boot shows ONE recurring compose+speech_shape+Fish cluster (+8s..+100s; 8x ~10min07s on the 16:09 boot) — the 18:00:13 cluster was that item, NOT the first driven reply (also absorbed); Q-117's resolved-by-observation basis was invalid, its route-back fired same-day, re-resolved INTO DEC-57. **DEC-57 (frame-fit argued on record): the interruption-gate thesis governs UNSOLICITED attention — answering a direct operator question is not an interruption.** Chat always earns a composed TEXT reply; the gate keeps FULL voice authority (held chat = quiet text-only — no speech_shape call, no TTS; surfaced chat = today's full path byte-identical); no pending-set absorption for chat (answered ≠ deferred); hold-silence verbatim for all non-chat kinds; no new EventClass (recorded deferral); absorbed backlog not migrated (honest one-time flush if it empties). Supersedes Q-96/ISS-4 IN PART for exactly CHAT; DEC-1/13 spine untouched; realizes DEC-39's intent. TK-272 (P1, M — routing + voice suppression + pending-add guard) → TK-273 (P2, S — one INFO line per gate decision/dispatch, ids+enums only, TK-259 custody; the mystery item names itself next boot). TK-269/270/271 all stand — real defects, correctly fixed, none the cause. | architect | ISS-23, DEC-57, TK-272, TK-273, Q-117, ISS-21, ISS-22, DEC-39, DEC-13, Q-96, ISS-4, contract v2.118 |
| 2026-07-19 | **TK-271 DONE — BOARD CLOSES 188/188 (closeout — within authority; contract v2.117): the ISS-22 broker knife-edge repair LANDS (EP-32) — the timeout/resolve pair is order-independent by construction.** `chat/surface.py` — `resolve()` with a popped-but-done/cancelled future no longer returns early: it falls through and parks the text in the late slot UNCONDITIONALLY under the same oldest-first `LATE_SLOT_MAX_SIZE` eviction as `discard()` (boundedness holds on the new insertion path); in-window and truly-unknown-id paths byte-identical. `discard()` inserts the `(None, ts)` placeholder ONLY when the id is absent — it never clobbers a parked text or its refreshed TTL. 6 new tests prove all four broker ACs deterministically, including the EXACT live interleaving and its reverse converging on the same `poll_late` outcome. AC5 held — diff confined to the two files_in_scope; app/, routes, TK-266 timeout constants zero-diff. Builder bar green (25 targeted; full 1672 passed / 92 pg-gated skips; ruff; mypy strict 334 files); architect re-ran `uv run pytest -q` pre-commit. **BATCH-VERIFY PENDING (one Opus review, v2 cadence)**, then operator closeout: runtime bounce (app can stay — no pane changes) + the re-driven chat round-trip (in-window reply, or 'replied late' within ~one poll of a knife-edge miss). | architect | TK-271, ISS-22, DEC-56, TK-270, EP-32, contract v2.117 |
| 2026-07-19 | **ISS-22 + TK-271 + Q-117 resolved (COMMIT — within authority; contract v2.116): the driven chat round-trip failure is root-caused — a two-bug order-dependence in ChatReplyBroker, not id plumbing, not the pane.** Live: send 17:59:45, reply composed AND spoken 18:00:13-16 (~30s — TK-269 wake PROVEN), but no in-window reply and no late append across 5+ min of correct polling. Source diagnosis: **Bug A** — `resolve()` (surface.py:137-141) returns even when the popped future is `done()`; `asyncio.wait_for` CANCELS the future at timeout and awaits inside its cancellation handling before the handler's `discard()` (:379) runs — a resolve in that window drops the text, then discard parks `(None, ts)` = late-expected-forever (GET answers pending for the full 12-min bound; exactly the live trace at the ~+30s knife edge). **Bug B** — `discard()` (:157) unconditionally writes `(None, ts)`, clobbering any parked text. The TK-270 review's single-loop-atomicity claim was wrong across wait_for's internal await. **TK-271** (P1, S, EP-32): order-independence by construction — done/cancelled-future resolve falls through and parks the text (cap/prune on the new insertion path); discard never overwrites an existing entry; in-window and unknown-id paths byte-identical; the exact interleaving becomes a deterministic regression test. Held body id and chat.ts/ChatPane poll wiring verified SOUND. **Q-117 resolved-by-observation** per its own disposition — reply landed in seconds, no gate hold actuated, no chat-gate DEC warranted (no-evidence-free-gate-change bar); standing route-back if a chat turn is ever observably gate-held. | architect | ISS-22, TK-271, Q-117, TK-269, TK-270, DEC-56, contract v2.116 |
| 2026-07-19 | **ISS-21 + DEC-56 + TK-269/TK-270 (COMMIT — within authority; contract v2.112): chat reply latency IS the drain-pump beat.** Log-proven (runtime-20260719-160949.log): the compose→speech_shape→Fish cluster fires on a ~10min7s period (16:14:59/16:25:06/16:35:13/16:45:21); Jim's messages were accepted, held honestly at 30s, then composed AND SPOKEN minutes later — but `ChatReplyBroker` discards the future on timeout (Q-110(d)), so the pane never renders a late reply; queue fully drained, pure cadence mismatch (DEC-41's beat was shaped for background inbox work; ChatSource rides the same pump with no wake). Ruled **DEC-56**: (a) interactive enqueue wakes the pump — edge-triggered coalescing asyncio.Event seam; `_run_drain_pump`'s idle `sleep(beat)` becomes an interruptible wait; episode mechanics/ASMP-2/DEC-41 byte-identical; gmail/gcal/Sweeper deliberately do NOT wake (beat = their cadence + the lost-wake safety net) → TK-269 (P1, S, EP-1); (b) late replies delivered, never orphaned — bounded late-slot (cap+TTL, pop-on-read), held response gains the pre-computed item id (never model-visible, CON-1 intact), token-guarded GET /chat/reply/{id}, pane polls ~5s to an honest bound with a visible 'replied late' marker → TK-270 (P1, M, EP-32; supersedes Q-110(d) discard-on-timeout IN PART); (c) spoken channel stays unconditional on the text window — confirmed coherent (gating speech on text delivery couples channels for zero gain; with (b) text always lands). Rejected: global/conditional beat shortening, second pump/queue (ASMP-2/DEC-39), websocket push, longer held window. Build order: TK-269 → TK-270. | architect | ISS-21, DEC-56, TK-269, TK-270, DEC-41, DEC-39, DEC-55, TK-222, TK-266, ASMP-2, CON-1, contract v2.112 |
| 2026-07-19 | **ISS-20 + TK-268 (COMMIT — within authority; contract v2.110): live-handshake stomp incident routed to a structural guard.** A TK-267 batch-review repro run from the REPO ROOT (~15:48) inherited the live .env and rewrote the live chat-handshake.json with a dead port — Jim saw "chat is down again" while the runtime was healthy; the SECOND failure of the TK-237-era verify-outside-repo-root process rule (runtime.py:123–144 writes unconditionally; TK-261's lock guards main() and repros bypass it). Ruled a structural guard over more steer-text: **never overwrite a handshake whose recorded port still answers** — one bounded loopback connect; live answer → refuse with ONE loud WARNING and boot on (a live responder is either a forbidden second instance per ASMP-2 or someone else's live runtime); dead/absent/unparsable → write exactly as today, so crash/watchdog/restart flows are byte-identical. Deliberately NO test-mode env flag — flag discipline is exactly what failed; the guard derives from observable state. Standing process rule re-affirmed with ISS-20 as its anchor. Operational record: TK-260 watchdog's second live save (16:09:48 respawn onto TK-267 code); whole-day arc live in the 16:09 boot; board 184/184 batch-verified; open proof = Jim's spoken-summary ear-check. | architect | ISS-20, TK-268, TK-260, TK-261, TK-263, TK-237, CON-3, contract v2.110 |
| 2026-07-19 | **DEC-55 recorded (JIM DIRECTIVE — architect records intent + rules the delegated hows; contract v2.106): speech is a SUMMARY, never formatted text.** Jim verbatim-in-spirit: "not sending in formatted prompts to TTS... a summary of what we know to be summarized by deepseek flash, then sent to tts." Supersedes the TK-164/Q-96 speaks-composed-text-VERBATIM rule IN PART (spoken channel only; text channel byte-untouched). Ruled hows — (a) NEW `speech_shape` stage compose → speech_shape → speak; SpeakSink's no-model-call pin STANDS, the mouth pattern owns the call; (b) SAME DeepSeek provider/profile via `ctx.model.complete` under the existing BudgetGuard/DailySpendLedger (deepseek-chat fast/cheap tier = the "flash" intent; egress set unchanged, ASMP-1/DEC-28 intact, zero new config); (c) failure/validator-violation → silence + terminal degrade, NEVER verbatim fallback (verbatim-on-failure re-ships Jim's exact complaint); (d) all compose→speak items in scope, deterministic ack path exempt by construction; (e) fixed prompt + immutable guard suffix now, fifth persona 'speech' mouth = recorded deferral (closed four-mouth vocabulary + TK-210 eval scope); (f) no-placebo bar = deterministic post-validator (closed markdown/link/URL token classes + hard length cap), unit-testable on fakes. → **TK-267** (P1, M, EP-30, deps TK-264/265 both landed). ALSO: Jim confirmed live post-TK-264 — Fish cloned voice WORKS ("fish audio is working finally"); ISS-17 closed end-to-end. Build order: TK-267 sole open ticket after the pending TK-265/266 batch-verify; operator closeout folds the spoken-summary check in. | architect (direction: Jim) | DEC-55, TK-267, TK-164, DEC-28, DEC-38, CON-3, ASMP-1, ISS-17, contract v2.106 |
| 2026-07-19 | **ISS-18/ISS-19 + DEC-54 + TK-265/TK-266 (COMMIT — within authority; contract v2.102): the LOCAL TTS fallback path is its own boot-killer, and it evades EVERY TK-259 instrument.** Live at 12:37:45 — fallback WARNING then the log simply ENDS (no faulthandler dump, no last-gasp): a hard native-abort/TerminateProcess-class death inside SAPI/comtypes; `Pyttsx3Adapter` shares ONE boot-constructed COM engine across threads with `speak()` on the drain-pump thread (tts_adapter.py 41–47). TK-260 watchdog scored its FIRST LIVE SAVE (relaunch 12:39:28). Ruled **DEC-54**: local TTS speak moves to a short-lived isolated child process (TK-265, P1 — fresh engine per utterance, text via stdin, bounded timeout; child death/timeout → RuntimeError → existing speak-sink terminal Degraded). Guard/validate is impossible here (text input, abort invisible in-process) — this is exactly the residual DEC-53(d)'s revisit clause reserved, so DEC-53(d) is superseded IN PART for the local engine only; cloud winsound path keeps DEC-53a in-process validation. P1 because DEC-28's zero-config default is local-only voice. ISS-19 (minor, live repro): chat POST has no timeout — pane stuck 'Sending...' forever when the runtime died holding the connection → TK-266 (P2: >30s AbortController honoring surface.py's held-window, honest timed_out state, send re-enabled, short probeChat abort folded in). Chat round-trip HALF-proven (in + DeepSeek compose 200; reply delivery killed by ISS-18) — full retest after TK-264 + restart. | architect | ISS-18, ISS-19, DEC-54, TK-265, TK-266, DEC-53, DEC-28, TK-259, TK-260, contract v2.102 |
| 2026-07-19 | **ISS-17 + TK-264 (COMMIT — within authority; contract v2.101): Fish streaming-sentinel WAVs rejected by DEC-53a strict validation — paid cloned voice silently defeated.** DEC-53a itself VERIFIED LIVE first (12:17:20 boot outlived every prior death window; the 12:22:34 killer WAV — declared 4294967040 bytes in a 278572-byte body — raised cleanly, speak-sink contained it, DEC-28 select-chain fell back to local TTS and kept running). Residual routed to TK-264: sentinel-normalize BEFORE validation in `voice/playback.py` (the one provider-agnostic seam; tts.py stays transport-dumb) — IFF a RIFF/data size field carries the explicit unknown-length sentinel class (0xFFFFFFFF/0xFFFFFF00 style), patch it to the actual byte length; NON-sentinel overruns NEVER patched, so genuine truncation still raises. Ruled tighter than the proposed patch-any-overrun (which would defeat truncation detection); DEC-53a validate-before-native-call invariant intact by construction. With the whole DEC-52/DEC-53 batch landed (board 180/180 at v2.100), TK-264 is the next ready ticket. | architect | ISS-17, TK-264, TK-262, DEC-53, DEC-28, contract v2.101 |
| 2026-07-19 | **TK-262 DONE (closeout — within authority; contract v2.96): DEC-53(a) validate-before-play LANDS at the one playback seam, closing the ISS-15 segfault crash-loop by construction.** `src/wombat/voice/playback.py` gains `_validate_wav_bytes()` — ValueError naming defect class + byte length for empty buffer, malformed RIFF/WAVE framing (stdlib `wave.open` over `io.BytesIO`, cause chained), or declared frame data (`nframes*nchannels*sampwidth`) overrunning `len(wav_bytes)` — called strictly before `winsound.PlaySound`; valid buffers reach PlaySound byte-identical with SND_MEMORY; lazy winsound import untouched; stdlib-only, no sinks/tts/select/queue changes. Tests: spy-proven PlaySound-never-invoked on all three rejection classes (incl. an explicit header-intact truncated fixture for the overrun branch), old garbage-bytes fixture replaced with a real wave-built WAV (it asserted the pre-fix blind-trust behavior), SpeakSink regression proves the ValueError completes the drained item as the EXISTING terminal Degraded (spoken=False, degraded=True) + warning — containment verified as-built, zero sink code changed. Builder self-ran the full bar green (7 playback tests; 1575 passed / 92 pg-gated skips; ruff; mypy strict 328 files; diff exactly the two files_in_scope); architect re-ran pytest pre-commit. BATCH-VERIFY PENDING per the v2 cadence (one Opus review after all five batch tickets land). Board 176/180. NEXT: TK-259. | architect | TK-262, DEC-53, ISS-15, EP-31, contract v2.96 |
| 2026-07-19 | **DEC-53 + TK-262/TK-263 + TK-259 amendment (COMMIT — within authority; contract v2.95): the live-E2E CRITICAL is root-caused and routed.** ISS-15 — every boot dies exit-139 ~5min in: drain pump → speak sink → `winsound.PlaySound(SND_MEMORY)` native AV on newly-live Fish-TTS WAV bytes; the poisoned item (36 ready) replays every boot, invisible because a native crash has no traceback and the launcher Tee was already broken (ISS-12, corroborated again today). Ruled: validate-before-play at the ONE playback seam (TK-262 — stdlib `wave` parse + declared-size-fits-buffer, ValueError BEFORE the native call; SpeakSink's existing terminal-Degraded containment then completes the item, closing the crash-loop by construction; no subprocess isolation, no queue quarantine — rejected alternatives); faulthandler-into-the-per-boot-log as a BINDING TK-259 amendment (new AC — plain logging provably misses an AV); interim `WOMBAT_VOICE_ENABLED=false` mitigation recorded NOT applied (Jim's call). ISS-16 — header says Running while the runtime is dead (Header.tsx keys on handshake-file presence once at mount; mtime provably fresh on a dead port) → TK-263: real chat-port round-trip probe on a ~15s interval, any-HTTP-response-is-alive, never POST /chat; supersedes MANUAL_TEST.md's mtime heuristic. Build order re-ruled: **TK-262 → TK-259 → TK-261 → TK-260**, TK-263 independent after TK-262. Verified-working slate + gcal noted-not-a-defect recorded in the v2.95 changelog. | architect | ISS-15, ISS-16, DEC-53, TK-262, TK-263, TK-259, DEC-52, contract v2.95 |
| 2026-07-11 | **DEC-48 recorded (JIM DIRECTION, contract v2.74 — architect recording + minting within authority): supersedes-in-part DEC-47(b) — ONE agent Phase-2 pass brings app/ to the APPROVED iteration-4 design BEFORE Jim takes UI ownership.** Jim verbatim: "The UI still looks the same. Lets at least first get the UI Looking like iteration 4 and THEN ill take over." A ux-designer agent that had started implementing was STOPPED mid-run with NO code landed — Jim refined the direction: ALL implementation rides the STANDARD Sonnet build-batch workflow over real atomic tickets. FOUR minted in EP-32, strict serial (shared app/src files): **TK-248** (iteration-4 token vocabulary — surface/ink/border/brand/accent/danger/positive/focus — PLACEHOLDER colors in theme.css alone, TK-225 audits binding) → **TK-249** (approved shell: header + left rail Today/Persona/Voice&Audio/API-Keys/System + persistent collapsible 320px chat pane + Today-as-landing; settings re-housed, BEHAVIOR unchanged) → **TK-250** (GET /external/calendar client + iteration-4 event cards — time block/accent edge/next-emphasis/all-day pill — with card-shaped skeleton/degraded/empty states; also deps done TK-246) → **TK-251** (GET /external/gmail client + five-field-honest inbox cards + open-in-Gmail preload openExternal bridge + notepad timeline honest-EMPTY — no scratch route exists). HONESTY RULE bound in DEC-48(d): UI renders from the TK-246 envelopes verbatim, NOTHING invented — needs-reply/scratch-read/snippet/brief-card stay the DEC-47(c) operator-demand deferrals; DEC-47(d) read-path disciplines untouched. AFTER the pass lands, DEC-47(b) operator ownership RESUMES. Board reopens **164/168**. validate PASS (233 nodes, 195 gov); gate_check analyze PASS. NEXT: run TK-248..251 through the build-batch workflow, then the owed batch verifies, then handover to Jim. | architect | DEC-48, DEC-47, TK-248, TK-249, TK-250, TK-251, TK-225, TK-246, EP-32, contract v2.74 |
| 2026-07-11 | **DEC-47 recorded (JIM RULING ~20:15, contract v2.73 — architect recording, not deciding): the settings-screen iteration-4 design is APPROVED and the UI is now OPERATOR-OWNED.** Jim verbatim-in-spirit: "Alrighty, let's go with this [the iteration-4 design]. I'll go ahead and take over for the UI from here." (a) Approved design = planning/design/settings-screen-design-brief.md §11 + settings-screen-mock.html @ commit c6b9bb3; the TK-246 "unapproved iteration cannot bind" hold is lifted. (b) Jim builds the app/ UI himself — NO agent builds app/ UI without new explicit Jim direction; the TK-225 theme-token rule (every color via theme tokens, zero hardcoded values — DEC-39(4) lineage) is the binding implementation constraint on his build. (c) TK-246's recorded follow-up amendment (route/field normalization, possible /brief/latest, needs_reply/fyi classification, stored-snippet field) converts to an OPERATOR-DEMAND-DRIVEN deferral — Jim's UI build surfaces exactly which API adjustments he needs; agents implement backend changes on his ask, tickets minted then, nothing minted now. (d) Backend read-path disciplines (token guard, DEC-43 degrade, read-only /external/, DEC-45(d) whitelist/no-body taint) unchanged and binding on any future amendment. Board stays 164/164; standing process debt unchanged (owed batch verifies: TK-244..247, v2.42..46, v2.50..54, TK-240..243). validate PASS (229 nodes/194 gov); gate analyze PASS. | Jim (ruling) + architect (record) | DEC-47, TK-246, TK-225, DEC-39, DEC-43, DEC-45, EP-32, contract v2.73 |
| 2026-07-11 | **TK-241 CLOSED (COMMIT, contract v2.67 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-240..243).** Board 160/164 — the DEC-43/DEC-44 settings-to-Postgres arc is COMPLETE (EP-32); only the v2.66 Jim-directive arc TK-244..247 remains. As-built exactly per the standing rulings: v2.62 B holder mechanism (`_TABLE_SETTINGS_HOLDER` filled by one `get_all()` in load_config, DSN env→dotenv never-the-table, connect_timeout=3, one-WARNING degrade CON-3, `finally`-cleared on every exit path — zero-DB-I/O bare construction, v2.61 r1, AC4 pinned live), CR5-2 guards verbatim, JSON source deleted (grep-AC), lazy SettingsStore import breaks the cycle; R1 WOMBAT_SETTINGS_FILE→settings_store.py with live.py verified untouched (moot rider held); R2 rename-race FileNotFoundError log-and-continue + pg-gated race test; R3 lazy string-skipif in test_output_effects_live.py + AC5 reimport hermeticity pin (fails against the old eager code — would-have-caught bar met) + repo-wide grep proves zero import-time load_config callers today. Diff audited: exactly the five remaining files_in_scope, no creep. FLAG for the batch verify: consider a standing suite-wide structural assert to harden AC5 beyond the single-module reimport pin. Builder bar green (suite twice on throwaway pgs: 1617/11/5, all 5 stash-proven pre-existing pg-armed failures — batch-verify debt, not regressions); architect pytest pre-commit 1519/84/0 exit 0 — the v2.51-tracked env-contamination failure DIED with the file source as predicted at v2.63. validate PASS (229 nodes/193 gov); gate tickets PASS. NEXT: the owed batch verifies (TK-240..243, v2.42..46, v2.50..54), then dispatch TK-244 → TK-245 → {TK-246 ∥ TK-247}. | architect | TK-241, EP-32, DEC-43, DEC-44, CON-3, contract v2.67 |
| 2026-07-11 | **FINAL batch selected: TK-241 ALONE (COMMIT — within authority; contract v2.65).** Board 159/160 — TK-241 (WombatConfig DB source swap) is the single open ticket; landing it closes the board 160/160 and completes the DEC-43/DEC-44 arc. NO new rulings needed — the standing set fully binds: v2.61 r1 (zero-IO-at-construction, table source only in load_config), v2.61 r3 (FileNotFoundError rename-race rider — confirmed unlanded, settings_store.py:215 bare `path.rename`), v2.62 B (`_SettingsTableSource` holder in the JSON source's exact slot, CR5-2 verbatim, holder cleared in `finally`), v2.62 A as amended by v2.63 (WOMBAT_SETTINGS_FILE definition relocates config.py→settings_store.py, settings_store drops its from-config import at :40; the live.py rider is MOOT — builder must NOT touch live.py), v2.64 (test_output_effects_live.py:115 collection-time `load_config()` goes lazy + the class-closing hermeticity AC). All seams re-verified live (config.py :50/:54/:75/:124/:128-147/:256/:277; __main__ `_resolve_pg_dsn` :47-53); NO cog-worx seams in this ticket's path. Deferred: nothing — nothing else is open; the owed batch verifies (TK-240..243, v2.42..46, v2.50..54) fire after TK-241 lands. validate PASS (224 nodes/191 gov); gate analyze PASS. | architect | TK-241, DEC-43, DEC-44, contract v2.65 |
| 2026-07-11 | **ORCHESTRATOR ruling unblocking TK-241 recorded (COMMIT — contract v2.64, within authority; DEC-43/DEC-44 frame held; the final open ticket).** The builder proved live that with the v2.62 ruling-B DSN resolution, `tests/persona/test_output_effects_live.py`'s unguarded module-collection-time `load_config()` call would dial the LIVE runtime db (repo-root .env, port 5436) and SELECT from production `wombat_settings` on every ordinary repo-root pytest — the standing never-touch-live-db violation. Ruled (builder option (a), strengthened to close the class): **(1)** v2.62 ruling B STANDS unchanged — the production runtime boots from .env (TK-186 lineage); stripping .env DSN pickup would make the table source silently inert in the shipped deployment, a forbidden placebo. The TEST is the defect. **(2)** the test file joins TK-241 `files_in_scope` — its `load_config()` call goes lazy (fixture/test body under chdir(tmp_path) and/or DSN scrub) so collection performs zero I/O; the env-gated live-eval gating semantics preserved when armed. **(3)** class closed, not the instance — NEW TK-241 hermeticity AC: bare collection + the default unarmed suite performs ZERO load_config()-driven DB connections from repo cwd, pinned by a runnable structural check (collection-import test, AST/grep no-import-time-load_config assert, and/or conftest guard; equivalents acceptable iff they'd have caught this exact defect) — the TK-202/Q-103 suite-hermeticity posture applied to the table source. TK-241 budget amended (third authorized rider). Governance-only; validate PASS (224 nodes/191 gov); gate analyze PASS. | architect | TK-241, DEC-43, DEC-44, TK-202, Q-103, contract v2.64 |
| 2026-07-11 | **Final-two batch selected (TK-241 → TK-243) + two TK-241 pre-build rulings (COMMIT — within authority; contract v2.62).** Board 158/160 (TK-240 c923ce3, TK-242 e437b5a landed); only the DEC-43 boot-read (TK-241) and hot-apply (TK-243) halves remain. **Ruling A (suite-red ordering hazard):** `WOMBAT_SETTINGS_FILE`'s canonical definition relocates to `settings_store.py`; `persona/live.py`'s one import line flips there as an authorized one-line rider joining TK-241 `files_in_scope` — otherwise TK-241's removal of the constant from config.py breaks live.py at import time until TK-243. **Ruling B (AC4 zero-IO mechanism):** pydantic init kwargs are the WRONG precedence tier, so the sanctioned shape is load_config-fetched table rows (DSN via os.environ→dotenv_values, the TK-242 `_resolve_pg_dsn` pattern; short connect_timeout; one-WARNING treated-as-absent degrade) published to a module-level holder consumed by a `_SettingsTableSource` in the JSON source's exact slot (init > env > .env > table > defaults), CR5-2 guards ported verbatim, holder cleared in `finally` — bare `WombatConfig()` sees an empty source, zero DB I/O at construction by construction. TK-243 rides v2.61 ruling 2 unchanged (fully lazy; pins/axes hydrate on the FIRST `poll_settings()` Sweeper beat). Sweeper clock-once-per-beat re-verified against installed cog-worx `sweeper.py:72-77`; v2.58(a) chdir test hygiene binds both tickets. validate PASS (224 nodes/191 gov); gate analyze PASS. | architect | TK-241, TK-243, DEC-43, DEC-44, contract v2.62 |
| 2026-07-11 | **DEC-43 batch RE-DISPATCHED under the DEC-44 shape + two pre-build rulings (COMMIT — within authority; contract v2.58).** Batch = **TK-240 → TK-241 → TK-242 → TK-243** (the whole 156/160 remainder, strict dependency order; none needs the operator OAuth bring-up). All seams re-verified live: schema_preflight still FIVE entries (40–44); config.py APP_EDITABLE_FIELDS :54 / _AppEditableJsonSettingsSource :75 / settings_customise_sources :257 / SecretStr-drop :124; api.py _read_settings :96 / _write_settings :111 / create_app :150; settings_app __main__ main() :36; live.py ctor :99 + mtime machinery; runtime.py _sweeper_clock :88 → poll_settings_file :101, serve() :288 / assemble_runtime :314; installed cog-worx sweeper.py:76 confirmed clock-once-per-beat (framework untouched). RULING (a) TEST HYGIENE, binding on all four tickets: every test constructing/exercising import_legacy_settings_file or the serve() import wiring MUST monkeypatch.chdir(tmp_path) (the _no_env_file precedent, test_runtime.py:92) — no test may ever let the import see the repo root where Jim's REAL wombat.settings.json lives (the file the DEC-44 hazard ate twice). RULING (b) IMPORT PLACEMENT: the serve() import call lands AFTER assemble_runtime returns, BEFORE _drive_and_serve; the one-migration-boot lag for non-persona fields is ACCEPTED self-healing (DEC-32 restart-to-apply; persona rows hot-apply on the first Sweeper beat via TK-243) — the builder must NOT move the import ahead of load_config. Also verified: TK-243 files_in_scope already carries all three v2.56 rename call-site files (no drift); test_serve_boot drives assemble_runtime, never serve(), so the pg-gated suite stays migration-free. validate PASS (224 nodes/191 gov); gates tickets+analyze PASS. The owed batch verifies (v2.42..46, v2.50..54) remain outstanding. | architect | DEC-43, DEC-44, TK-240..243, contract v2.58 |
| 2026-07-11 | **DEC-44 recorded (COMMIT — ORCHESTRATOR-ENDORSED ruling, decided upstream and recorded here, not re-litigated; contract v2.57).** The DEC-43 one-time settings-file import is UNFOLDED from ensure_schema into an EXPLICIT OPT-IN — `import_legacy_settings_file` in settings_store, called ONLY by the two production entry points: the runtime serve() boot path (runtime.py, after assemble_runtime's schema preflight — TK-240 wires it) and the settings_app `__main__` entry (after its own ensure_schema — TK-242 wires it). Bare ensure_schema/ensure_all_schemas — and therefore the whole pg-gated test suite and any ad-hoc bring-up — stays migration-free by default. HAZARD (proven live, twice): the folded-in sixth-entry shape let any repo-cwd test run against an empty wombat_settings table (throwaway WOMBAT_TEST_PG_DSN db) consume-and-rename Jim's REAL wombat.settings.json into a throwaway database — fired twice in batch wf_98c1b7e8-4d7 during TK-240 builder verification; the batch returned all four tickets blocked. Everything else in DEC-43 stands (KV JSONB table, sixth-entry ensure_schema itself, precedence, loud degrade, .migrated rename, file source removed). Tickets normalized: TK-240 title/intent/budget recast + NEW AC pinning no-import-on-bare-schema (runnable: empty db + file in cwd → file byte-untouched, zero rows) + files_in_scope += src/wombat/runtime.py + tests/unit/test_runtime.py; TK-242 intent/AC3 gain the `__main__` second call site; TK-241/243 untouched. validate PASS (224 nodes/191 gov); gate PASS (analyze). NEXT: re-dispatch the batch under the DEC-44 shape. | orchestrator (ruling) + architect (record) | DEC-44, DEC-43, TK-240, TK-242, NG-3, contract v2.57 |
| 2026-07-11 | **DEC-43 batch selected + pre-build ruling (COMMIT — within authority; contract v2.56).** Batch = **TK-240 → TK-241 → TK-242 → TK-243** (the whole board remainder; strict dependency chain rooted at TK-240; TK-242 depends only on TK-240 but is sequenced third to keep settings-app degrade testable against the real store shape before the persona swap). Pre-build ruling: TK-243 files_in_scope normalized += `src/wombat/runtime.py`, `tests/unit/test_runtime.py`, `tests/pathways/test_dream_persona_stage.py` — the `poll_settings_file → poll_settings` rename call sites (runtime.py:101 Sweeper beat + two test files); estimate stands M. All briefing seams verified live (schema_preflight 5 entries; config.py :54/:75; api.py :96/:111/:150; live.py ctor + mtime machinery). Deliberately NOT picked: nothing — these four are the only open tickets; the owed batch verifies (v2.42..46, v2.50..54) are process debt for the loop, not tickets. validate PASS (224 nodes/190 gov); gates tickets+analyze PASS. | architect | DEC-43, TK-240..243, contract v2.56 |
| 2026-07-11 | **DEC-43 recorded (JIM DIRECTIVE — pushback heard and overruled; architect records intent + rules the delegated how; contract v2.55).** App-editable settings move OUT of wombat.settings.json INTO Postgres, superseding-in-part DEC-32's tier-2 file clause (keyring tier, .env operator tier, and restart-to-apply all stand). Ruled HOW: **key/value JSONB table** `wombat_settings(key PK, value JSONB, updated_at)` — one idempotent CREATE per NG-3 (per-field columns would need ALTER choreography the repo has no machinery for; Jim's one-table intent held in spirit); scope = APP_EDITABLE_FIELDS + wombat_persona_pins ONLY (DSN/secrets never in the table); precedence slot unchanged (env > .env > TABLE > defaults) with the DB source self-reading WOMBAT_PG_DSN from env/.env (chicken-and-egg resolved; DSN-less boots byte-unchanged, unreachable pg = one warning + defaults per CON-3); settings-app degrade = LOUD READ-ONLY (GET nulls+storage_unavailable, PUT 503, never bare 500); persona hot-apply retargets to per-Sweeper-beat value-diff SELECT; one-time empty-table-guarded import then rename to `.migrated` (never deleted — removal discipline); file source removed cleanly. **Four tickets minted (EP-32, P1, production): TK-240 (store+table+sixth preflight entry+import) → TK-241 (config DB source) → TK-242 (settings API over store) → TK-243 (LivePersona persist/poll).** Board 156/160. validate PASS (224 nodes/190 gov); gate PASS. | Jim (frame) + architect (how) | DEC-43, DEC-32, TK-240..243, NG-3, CON-3, contract v2.55 |
| 2026-07-10 | **EP-33 closeout + CR4-discharge batch selected + Q-107 pre-build rulings (COMMIT — within authority; sequencing + ticket-level how; contract v1.91).** Batch = **TK-217 → TK-218 → TK-215 → TK-216 → TK-210**: the two CR4 hygiene tickets head per the standing Q-106(e) ruling (TK-217 contextualizes the false healthy-cloud "voice output disabled" warning at the fallback-slot call sites select.py:204/249; TK-218 docs-only DEC-28 docstring discharge), then the EP-33 tail closes the epic — TK-215 gate personality_band (PersonalityBand nested frozen params model, yaml v6→7 block minimal +0.10/balanced 0.00/forward −0.10/floor 0.60/cap 0.95 PROVISIONAL under DEF-1; pure `effective_urgency_threshold` beside `is_surfacing_worthy` in trigger.py; Gate gains OPTIONAL `threshold_fn` evaluated PER SCORED ITEM at BOTH predicate sites in pipeline.py — files_in_scope normalized += gate/pipeline.py, estimate S→M; bootstrap closure over `live_persona.matrix.proactivity`, construction order verified :624 before :665; one-predicate invariant deliberate — select_items sees the same effective threshold), TK-216 degrade persona deltas (TemplateComposer optional live_persona — the ONE construction site bootstrap.py:355, files_in_scope normalized += bootstrap.py; brief delta ONLY at the degraded fallback assignment, `render_brief_lines` stays pure/shared, single-source invariant restated as fallback = persona_wrap(body) with identity at DEFAULT; directness/humor byte-no-op pinned), TK-210 LAST (EP-33's done-bar; cogworx.eval verified in source = spike-gate corpus statistics, ruled NOT a fit — plain pytest harness gated `WOMBAT_TEST_PERSONA_EVAL_LIVE=1` + DeepSeek creds, real stack via `_deepseek_spec` + `build_model(spec, guard=BudgetPolicy().new_guard())`, N=3 recorded verdict rule per axis, proactivity exempt via TK-215's deterministic tests, unmeasurable axis = FAILING test naming it). EP-34 pre-rulings recorded now (Q-107d): TK-212 trail = one logger.info line BEFORE LivePersona.set (process log = the CON-4 trail for a LOCAL reversible config act; pg ActionTrail stays external-tier); ack rides make_speak_callable as an optional ASRSource speak callable. **Deliberately deferred:** TK-211/TK-212 (EP-34 kept together next batch), TK-197..201 (EP-32 remainder after EP-34 per the recorded sequencing), TK-213/214 (EP-35 P3), browser P3, DEF-7 smokes (operator). validate PASS (199 nodes/171 gov); gate PASS. | architect | Q-107, Q-106, TK-217/218/215/216/210, EP-33, EP-34, DEC-37, DEC-28, contract v1.91 |
| 2026-07-10 | **CR4 register recorded (COMMIT — docs-only, within authority; contract untouched, routing deferred to the next governance step).** Fourth cross-cutting review pass (EP-31 voice provider arc, `a82c8ac..HEAD`: TK-189..TK-195) written to `planning/code-review-2026-07-10.md`, all 3 findings CONFIRMED (executable repro / source+governance verification), ZERO critical, ZERO major, zero plausible-but-unconfirmed — the cleanest register yet, consistent with EP-31's verified closeout: **CR4-1 MINOR** healthy-cloud TTS boot emits a false "voice output disabled for this boot" WARNING (`voice/select.py:137`; `build_tts_adapter` eagerly constructs the local fallback at :249, and when pyttsx3 is absent on a cloud-only install the failed-fallback log claims voice is off while the cloud primary is fully working — reproduced); **CR4-2 MINOR** `sinks/tts_adapter.py:7` docstring still asserts "the ONE concrete adapter ... no cloud TTS" — the exact absolute DEC-28 named this file to amend via the provider tickets; TK-191/192 shipped three cloud TTSAdapters yet the file was never touched (git-log proven); **CR4-3 MINOR** `sources/asr.py:38` still carries the unqualified "no audio or transcript ever leaves the machine" — DEC-28's default-config rescope also named asr.py; TK-189/190/193 made opt-in cloud STT real yet the amendment went undischarged (systematic miss of the DEC-28 docstring clause, paired with CR4-2). Suggested routing in-register: CR4-1 small P3 log-fix ticket; CR4-2+CR4-3 one docs-fix ticket discharging DEC-28's amendment clause. NEXT: route CR4-* into governance/tickets in the next governance step. | architect | CR4-1..3, DEC-28, EP-31, planning/code-review-2026-07-10.md |
| 2026-07-09 | **CR3 register recorded (COMMIT — docs-only, within authority; contract untouched, routing deferred to the next governance step).** Third cross-cutting review pass (reflection arc + voice foundation surface) written to `planning/code-review-2026-07-09b.md`, all 4 findings CONFIRMED (live repro / executable repro / source verification), zero plausible-but-unconfirmed: **CR3-1 CRITICAL** fresh-database first boot crashes — no composition path runs `ensure_schema` (LIVE incident 2026-07-09 ~19:05 at bootstrap.py:610 on the fresh docker volume; operator-side manual ensure_schema x5 workaround applied; structural pre-flight fix owed); **CR3-2 MAJOR** `dream_pattern` enqueue guarded by `except QueueFullError` only, so any other pg error (e.g. reaped idle connection → OperationalError) crashes the whole nightly dream run — the lone never-block-the-terminal parity hole among the dream stages (siblings all broad-catch), hidden by a QueueFullError-only test; **CR3-3 MINOR** `load_psychology_kb` numeric coercions (loader.py:131/:136) raise bare ValueError/TypeError that ESCAPE the boot `(FileNotFoundError, KBValidationError)` degrade path — a hand-edit KB numeric typo crashes whole-process boot instead of degrading to empty KB; **CR3-4 MINOR** tests/unit/test_bootstrap.py:93 delenv is ineffective against the `.env` file source → suite red on the live machine (Q-103/TK-202 hermeticity family; product code correct). Suggested routing in-register: CR3-1 new P1 ticket, CR3-2 new P2 ticket, CR3-3 P3/KB-hygiene, CR3-4 fold into TK-202. NEXT: route CR3-* into governance/tickets, then resume TK-189..TK-195 + EP-32. | architect | CR3-1..4, TK-202, Q-103, planning/code-review-2026-07-09b.md |
| 2026-07-09 | **TK-186 recorded DONE (95/119, v1.66) + files_in_scope normalized (COMMIT — within authority; closeout on independent verifier PASS).** The load_config .env-only-boot live-bug fix landed exactly per the ticket's complexity_budget — one function reshaped, loud-boot contract and message shape preserved (first missing REQUIRED_ENV var named verbatim), no new fields/deps, no bootstrap edits. tests/unit/test_bootstrap.py added to files_in_scope: its two AC2 boot tests NEEDED monkeypatch.chdir hardening because they previously passed only via the buggy behavior the ticket exists to fix — a record-honesty normalization, not creep. Verifier evidence recorded as re-derived-not-trusted (builder's tally was materially off). **Closeout pytest gate on the LIVE machine = 953/57/14-failed — all 14 STASH-PROVEN pre-existing + environment-dependent (identical 14 fail on base with TK-186 stashed; the v1.62 precedent): the staged bring-up environment (real repo-root .env + installed [voice] extras) leaks into 12 non-hermetic env-clearing tests and breaks 4 lib-not-installed simulations. TK-186 adds ZERO failures and fixes 2. Raised Q-103 (suite hermeticity vs the live operator machine) and RESOLVED it into NEW TK-202 (EP-1, P1, S — TEST-ONLY hardening: chdir/env-scrub isolation for the 12 env-leak tests + robust import-block shims for the 4 lib-absence simulations; two-environment AC pair pins live machine AND clean checkout, the Q-46 bar; the Q-72→TK-17 concern-becomes-ticket precedent; board 95/120). Until TK-202 lands, the live-machine closeout gate is read against the 14 known Q-103 failures with stash-proof required.** Boot side of Jim's standing-process bring-up now clear. | architect | TK-186, TK-202, EP-31, EP-1, Q-102, Q-103, contract v1.66 |
| 2026-07-09 | **v1.33 batch selected + Q-91 pre-build rulings (COMMIT — within authority; sequencing + ticket-level how + the Q-61 unowned-wire pattern).** Batch = **TK-49 → TK-151 → TK-80 → TK-149 → TK-78**: closes the adaptation chain at the tuner (TK-49's seven deps ALL done post-v1.27-batch) and opens the outbound Gmail-draft track bottom-up in dependency order (TK-151 has no unbuilt deps; TK-149 depends on TK-151; TK-78 depends on TK-149+TK-151; TK-80 independent). **Q-91 rulings:** TK-49 — dream graph extended `dream_consolidate → dream_outcome → dream_tune → dream_run` (DreamOutcomeStage.transitions repointed; `build_dream_pathway` gains required `tune` arg; sanctioned mechanical e2e graph-shape updates); math = TK-48 spike verbatim (net_signal=(LB−(IGN+REG))/total, delta=clamp(gain·signal,±delta_bound), urgency_base+=δ / load_base−=δ, band-clamped; gains NOT tuned in v1); RECALL_WINDOW_NIGHTS=7 module constant over value-JSON `resolved_at` (not part of the TK-48 joint block); `record_rating_params` widened additively w/ optional SourceDeclaration (Q-41 payload stays closed). TK-151 — helper-binding shape verified in source (engine getattr seam); fail-closed structural; bootstrap.py dropped. TK-80 — pure builder; reply-worthy==HIGH; recipient=sender only; 280-char excerpt; 6th guard-allowlist file. TK-149 — pattern proven w/ stubs; action_id=`run_id:stage_name` (Q-63 carry-forward discharged); approve=`ctx.dispatch`+mark_dispatched / reject=mark_cancelled; AC6 refusal = TierViolation (verified). TK-78 — compose.py sibling; untagged external fn-capability; send never registered; freezes `draft_dispatch`. **Unowned outbound live wiring → NEW TK-177** (EP-18, S; emission+enqueue+DRAFT-route+dispatch-edge; deps TK-78/79/80/151). **Deliberately NOT picked:** TK-79 (dep TK-78, cap 5), TK-177 (deps in this batch), browser/voice/reflection clusters (off the wedge), anything needing Jim's OAuth bring-up (operator step). files_in_scope normalized TK-49/TK-151/TK-80. validate PASS (152 nodes/141 gov); gate PASS. 95 tickets. | architect | Q-91, TK-49/151/80/149/78, TK-177, Q-63, Q-65, DEC-26, contract v1.33 |
| 2026-07-09 | **Post-review batch selected + Q-84/Q-85 pre-build rulings (COMMIT — within authority; sequencing + ticket-level how).** Batch = **TK-43 → TK-77 → TK-44 → TK-46 → TK-52**: opens BOTH standing tracks at once — the claim-vocabulary/write-seam pair (TK-43→TK-44, the substrate every EP-12/EP-14 ticket writes through) and the EP-13 dream track (TK-46 scaffold → TK-52 nightly trigger, riding the fresh TK-97 Wait-self-park+Sweeper precedent), with TK-77 (the last Q-56 over-pull) ruled ready and slotted independent of both chains. **Q-84 (TK-77):** EP-13 dep DROPPED (Q-66 pattern — pure extractor, consumers wire later; Q-56 now FULLY closed); AC3 taint = structural drive-level check through real Registry+ToolGate (DEC-26 named binding constraint); AC4 = TK-76 no-import guard replacing the runtime trace; task_extractor.py = FIFTH Q-65 allowlist file; TaskItem identity via TK-12 `derive_task_natural_id`; ItemKind stays GENERIC. **Q-85 (TK-46/52):** TK-52's stale `start_run`/self-rearm prose reconciled to the as-built truth — mechanism = TK-97 verbatim (`wombat.dream_schedule` self-parking DreamTimerStage + raising terminal stub, `dream:run` fence row on the SAME composed DailyLedger, night-keyed `wombat-dream-<date>` run_id, mark-after-COMPLETED, Sweeper re-drive, third boot drive in serve()); one new OperatingParams field `nightly_dream_time` (YAML v5); TK-46 registers `wombat.dream` unconditionally in assemble_runtime (+`RuntimeBundle.dream_pathway_id`). files_in_scope amended on TK-77/46/52. **All TK-44 cog-worx seams re-verified in source 2026-07-09:** `user_model(kg, registry, user_id, *, owner)` → ScopedKG over `claim_write_token` (S7); `assert_fact(*, subject, predicate, obj, epistemic_type, source: SourceDeclaration, created_by, ...)` async → claim id; EntityKG first-write-wins epistemic level + `claims_about` newest-first (matches TK-42's read); `InMemoryEntityKG` double. **Deliberately NOT picked:** TK-47/TK-54 (blocked on TK-150), TK-51→50→45→49 (blocked on TK-147/TK-161), outbound TK-80/78/79 (needs TK-149/151), anything needing Jim's OAuth bring-up. validate PASS (149 nodes/135 gov); gate PASS. | architect | Q-84, Q-85, TK-43/44/46/52/77, Q-56, TK-97, DEC-26, contract v1.15 |
| 2026-07-03 | **Q-80 AMENDED same day — TK-97 timer-pathway stage-count correction (COMMIT — builder-flagged seam, runnable-proven, within authority; behavior-preserving HOW fix).** The builder proved `StageGraph.__init__` (cog-worx `loop/graph.py:74`) UNCONDITIONALLY requires a reachable terminal stage — the ruled pure self-edge single-stage `wombat.brief_schedule` graph raises `StageGraphError` at every construction; no opt-out. Verified against graph.py + the in-repo TK-53 precedent (`tests/unit/test_runtime.py` `_WaitForeverStage`/`_TerminalStage` — the identical eternal-self-park problem, solved identically) and BLESSED: `BriefTimerStage.transitions = ('brief_timer', 'brief_timer_terminal')` (self-edge + declared-but-never-taken stub edge; run() always returns `Wait(to='brief_timer')`); new `BriefTimerTerminalStage` stub (transitions=(), run() raises RuntimeError if ever entered — self-enforcing) homed in `pathways/brief_pathway.py` (graph-construction artifact, not scheduling behavior); `build_brief_schedule_pathway(timer_stage)` = `StageGraph([timer_stage, BriefTimerTerminalStage()], entry=timer_stage.name)` — internally two stages, one never reached. Everything else in Q-80 stands verbatim; ACs/files/deps/estimate unchanged. validate PASS; gate PASS. **TK-97 remains build-ready.** | architect | Q-80, TK-97, TK-53, contract v1.02 |
| 2026-07-03 | **TK-97 briefed (COMMIT — Q-80, /plan-ticket pass, within authority; the FINAL ingestion-arc ticket).** Every seam verified AS-BUILT; three headline rulings. **(1) Mechanism structurally forced to (b):** cog-worx has NO standalone cron-timer — `Timer` is run-bound (`run_id:step_index`, armed only by the engine committing a `Wait`; `fire_timer` re-drives an existing WAITING run) — so the brief timer is a **Wait-self-parking scheduler stage** (single-stage pathway `wombat.brief_schedule`, self-edge, the TK-5 idle idiom) re-driven by the existing TK-53 Sweeper; boot entry = crash-miss catch, Sweeper claim of the overdue Wait = sleep-miss catch; TK-97 sets the scheduling precedent EP-13 reuses. **(2) Exactly-once reconciled:** fence-READ (`BriefRunLedger` over the SAME composed DailyLedger, row `brief:run`, DEC-21 boundary) gates each due entry; MARK only after the fired brief run returns COMPLETED; **day-keyed run_id (`wombat-brief-<date>`)** makes TK-101's proven file-marker close the crash-after-Deliver-before-mark double window. **(3) AC4 reworded for record honesty:** resume-from-last-journaled-stage is unachievable across restart under the v1 cold-boot substrate (TK-53's recorded clean-slate non_goal) — the honest v1 guarantee is exactly-once **DELIVERY** (not-zero-not-two stands at the user-facing level); cross-restart mid-run resume rides the TK-53 deferral. morning_brief_time already homed (TK-13 YAML constant — non_goal clarified, no params change). Node amendments: deps EP-9→TK-28, files 3→5 (+bootstrap.py +runtime.py), complexity_budget rider; estimate M stands. validate PASS (140 nodes/125 gov); gate PASS. **TK-97 build UNBLOCKED — when it lands, FEAT-7 is code-complete pending Jim's OAuth bring-up + the two live smokes.** | architect | Q-80, TK-97, TK-53, TK-101, DEC-21, contract v1.01 |
| 2026-07-03 | **TK-101 recorded DONE (43/83) + header-format deviation ACCEPTED as-built (COMMIT — within authority, cosmetic HOW).** BriefDeliverStage built + independently adversarially verified (PASS). The bracketed run-marker header ([run=<id>] delivered_at=<iso>) accepted over the Q-78 decorative line: AC1’s actual wording fully met (tz-local, readable, run-keyed), structurally cleaner idempotency key, parity = churn on a passing terminal stage; recorded as the v1 header shape. EP-30 narrowed to the injected speak seam — Q-56 fully discharged on the built path. Brief cluster stages COMPLETE; TK-96 assembly next, TK-97 timer last. | architect | TK-101, Q-78, Q-56, contract v0.98 |
| 2026-07-03 | **TK-28 briefed (COMMIT — Q-73, /plan-ticket pass, within authority).** Every seam verified AS-BUILT; three discoveries drove the rulings. **(1) The midnight reset is already STRUCTURAL** — CeilingLedger keys one DailyLedger row per `ceiling:<class>` per wombat_date, computed at ACCESS time (DEC-21/TK-152): a new day = fresh row at 0, sleep-safe, no timer. TK-28 builds the exactly-once **OBSERVATION**: `DayRollover` (gate/decay.py) = in-memory last-seen day + durable pg marker (`DailyLedger.increment("rollover:gate")`; only the upsert returning value 1 emits `LedgerReset`) — exactly once across sleep AND restart; `DailyLedger.today()` added so the Q-15 boundary source stays singular. **(2) Decay basis = the journaled `added_at` rider, NOT GateItem.created_at** (the only durably journaled instant = the only restart-consistent basis; stale TK-21 models.py comment corrected comment-only); removal via the JOURNALED `PendingSet.remove()` path so decayed items never resurrect on rebuild (RISK-5); DecayEvent reused as-built from models.py; new accessor `list_with_added_at()`. **(3) AC3 concretized (text stands verbatim):** replay never touches the ceiling; restart never refreshes the day budget; surface/hold paths structurally disjoint (no kill point double-represents an item); decay kill points via the TK-24 `_KillSwitchJournal` precedent; ONE DSN-gated ledger restart proof. Wiring: rollover-check → decay() → byte-untouched TK-27 arms, running regardless of `surfacing_permitted` and on empty batches; **heartbeat arc-note DISCHARGED** (decay-before-arms makes staleness-never-surfaces hold without an idle heartbeat; residual flush-on-idle latency fails toward MORE silence — revisit at TK-97). Wake-burst test = one pipeline call per backlog item (the as-built pipeline returns on first surface, Q-51 batch_size=1); shared fake clock with epoch + datetime views. Gate ctor args REQUIRED never optional (the Q-69 orphaned-budget lesson). files_in_scope 2→13; estimate M confirmed. validate PASS (140 nodes/118 gov); gate PASS. **TK-28 build UNBLOCKED — the last support before the brief cluster (TK-98 next, its briefing rules Q-56 over-pull #3).** | architect | Q-73, TK-28, DEC-21, RISK-2/5, contract v0.89 |
| 2026-07-02 | **Ingestion-arc sequencing ruled (COMMIT — Q-56, routed sequencing decision, within authority).** Jim chose the ingestion arc → live morning brief as the post-demo track. **TK-3 confirmed GO first** (dep TK-2 done; atomic; no gating governance); TK-12 confirmed not a TK-3 prerequisite but mandatory before the first real poller (EP-2 epic-dep on TK-71/72/75) — slotted second. Full 19-step dependency-correct order recorded in Current state (calendar branch TK-71→72→74 ∥ gmail branch TK-146→148→75→76 — the taint chain PRECEDES the GmailPoller by declared design, ISS-3/S10; supports TK-53/9/29/28; brief cluster TK-96→98→99→100→101→97-timer-last). **Q-56 raised + resolved as PARKED-to-frontier (Q-35 pattern):** four suspected epic-dep over-pulls on the brief cluster (TK-98→EP-17 drags TK-77/80 + transitively the EP-13 dream cluster; TK-76/77→EP-13; TK-96→EP-17/EP-30; TK-101→EP-30) each ruled at its own /plan-ticket with seam context, not batch-edited blind. Sequencing constraints restated as binding: TK-29 before first LIVE session (Q-44); TK-53 before TK-97; TK-9 before TK-100; TK-28 before TK-96/97. No ticket/graph/DEC/scope change. validate PASS; gate_check analyze PASS. | architect | Q-56, TK-3, TK-12, Q-14/35/44/55, contract v0.58 |
| 2026-06-20 | Architect-of-record established as full-delegate within the locked frame (DEC-1..19 + constitution); decisions route here. | Jim | — |
| 2026-06-20 | **ISS-1 resolved (COMMIT).** Normalized 72 bare `wombat/...` `files_in_scope` (+ TK-1 prose) → `src/wombat/...`; `tests/...` left as-is (correct for the layout). Mechanical, no frame impact. | architect | ISS-1, contract v0.8 |
| 2026-06-20 | **ISS-2 resolved (COMMIT, re-scope TK-9).** Two-layer mouth budget: cog-worx per-drive `BudgetGuard` + NEW wombat-owned Postgres `DailySpendLedger` for the daily-cumulative cap (CF-3.0-B is cog-worx's deferred concern). S→M. **CST-3 left intact** — it never mandated a daily ceiling, so no constraint meaning changed; not an escalation. | architect | ISS-2, TK-9, CST-3 (unchanged), contract v0.8 |
| 2026-06-20 | **ISS-3 resolved (COMMIT, name the boundary).** Tainted ingest drive (TK-75) never carries `gmail.drafts.create`; outbound (TK-78) runs in a fresh untainted drive over a sanitized ReplyIntent. Positive + negative drive-boundary ACs added. Realizes DEC-19; no DEC changed. External-tier *admission* stays open as Q-10. | architect | ISS-3, TK-75, TK-78, DEC-19, Q-10, contract v0.8 |
| 2026-06-20 | **ISS-4 resolved (COMMIT).** Defined ONE canonical closed `GateAction`={hold,surface_immediate,surface_flush} + `item_kind` enum on TK-21; rewired TK-6/7/27/99/100/164/165 off their private `surface`/`speak-event`/`text-only` vocabularies. Speak-vs-text is now a sink concern off `voice_enabled`. Realizes DEC-13; no DEC changed. | architect | ISS-4, TK-21, contract v0.10 |
| 2026-06-20 | **ISS-5 resolved (COMMIT, new ticket).** Added **TK-10 ComposeDispatchRouter** (EP-4) routing surfaced items by `item_kind` → brief/reflection/draft/generic composers. TK-7 delegates routing; TK-114/TK-78 depend on TK-10; TK-100 brief exempt (scheduled pathway). Closes the reflection/draft compose orphans. | architect | ISS-5, TK-10, TK-7, contract v0.10 |
| 2026-06-20 | **ISS-6 resolved (COMMIT, new ticket); RISK-7 → mitigated.** Added **TK-50 Outcome-signal producer** (EP-12, P1): feedback affordance + deterministic local-state inference make LOAD_BEARING/REGRETTED/IGNORED all producible. TK-45 + TK-49 depend on it so EP-14 can't build without a real gradient. Realizes FEAT-3/DEC-15; read-only posture + no new egress. | architect | ISS-6, RISK-7, TK-50, TK-49, contract v0.10 |
| 2026-06-20 | **Q-10 resolved (COMMIT the wiring) + ESCALATED (the sign-off).** Authored **DEC-20**: admit `external` tier ONLY for the approved AwaitHuman dispatch stages; engine default stays {read,write}. Added **TK-151** (per-stage tier policy, fails closed). The cog-worx external-tier **sign-off is Jim's** — the one live human dependency; outbound dispatch blocked until enabled. DEC realizes DEC-19, within authority; sign-off escalated. | architect | Q-10, DEC-20, TK-151, contract v0.10 |
| 2026-06-20 | **Q-11/Q-12 resolved (COMMIT).** Q-11: both-direction taint-tag AC on TK-148 (trusted local reads must NOT spuriously taint). Q-12: recorded non_goal — presence is a gate-level hold, not a scoring input (TK-22), consistent with DEC-13. | architect | Q-11, Q-12, TK-148, TK-22, contract v0.10 |
| 2026-06-20 | **Q-13 resolved (COMMIT, splits).** TK-78 → TK-78 (compose→draft→AwaitHuman) + TK-79 (dispatch_approved+trail+degrade). TK-27 → TK-27 (arms+ceiling) + TK-28 (decay+midnight-reset+kill-proof). | architect | Q-13, TK-78/79, TK-27/28, contract v0.10 |
| 2026-06-20 | **Q-14 resolved (COMMIT).** Fixed convention "epic dep = all child tickets" and **proved the expanded 69-ticket graph acyclic** (Kahn sort). Broke 3 real safety/integration cycles by normalizing offending epic-deps to ticket-ids (TK-148→TK-146; TK-75/77→TK-148; TK-78→TK-149/151; TK-146/149 drop EP-18/26; TK-164 drops EP-20). | architect | Q-14, contract v0.10 |
| 2026-06-20 | **Q-15 resolved (COMMIT).** Authored **DEC-21**: wombat-day = civil-local date in a configured tz, computed at access time (survives sleep). Added **TK-152** shared DailyLedger primitive; TK-9/27/28/97 consume it; fixed TK-101's UTC header. No locked DEC fixed a tz — within authority. | architect | Q-15, DEC-21, TK-152, contract v0.10 |
| 2026-06-20 | **v0.9 floating findings + atomicity homed (COMMIT, v0.12).** Gave a home to the 8 never-ticketed v0.9 red-team MEDIUM/LOW findings (A–G) + 2 fresh (H,I) + atomicity, as Q-16..Q-27, all resolved in-frame (no DEC/constitution/v1-scope touched). Real gaps → owning tickets: **TK-80** ReplyIntentBuilder (the sanitized artifact ISS-3/TK-78 rested on but nothing built), **TK-11** presence-hold hardening (prototype-under-production-gate), **TK-12** canonical item identity (outcome-label binding across sources), **TK-13** production parameter store (spike-recommended constants nobody persisted), **TK-51** feedback input channel (TK-50's interactive half had nowhere to land). Coherence fixes: TK-45 PENDING-hot/terminal-nightly (I), TK-26 flush 2→1 + class split (B), TK-28 wake-burst AC (G), TK-150 same-host residency not literal-localhost (E). Splits: TK-50→TK-50+TK-51 (Q-21), TK-148→TK-148+TK-153 (Q-26). Left as-is: TK-149/TK-9/TK-132 (Q-27, deliberate cohesion). +6 tickets (75 total), +12 governance. Graph re-proven acyclic. validate PASS; gate_check analyze PASS. | architect | Q-16..Q-27, TK-11/12/13/51/80/153, contract v0.12 |
| 2026-06-21 | **Q-29 resolved (COMMIT — TK-1 constructibility, re-scope + new ticket).** /plan-ticket TK-1 surfaced that cog-worx `Engine.__init__` requires 4 seams with no defaults (journal/graph_store/latent + pathways) that TK-1 named nowhere and NO ticket provisioned — AC1 was not constructible and the cog-worx substrate stores were an orphan. Ruled **blend (c)+(b)**: TK-1 → **pure wiring over an injected `SubstrateBundle`**; new leaf **TK-14** (EP-1, P1, mvp, `depends_on []`) owns store construction — cold-boot cog-worx **in-memory doubles** as the v1 zero-infra default + a config-selected **real-adapter** swap (timescale/neo4j/pg) behind the same seam, DSN from TK-13, on-host residency from TK-150. Rejected (a) real-DB-in-keystone: violates TK-1's mvp/no-DB-touch budget and inverts the spine. TK-1 AC1/intent/budget/non_goals amended to assert all 4 seams wired; `TK-1 depends_on +TK-14`. Within authority (no DEC/constitution/v1-scope touched). +1 ticket (76), +1 gov (Q-29). validate PASS; gate_check analyze PASS. | architect | Q-29, TK-1, TK-14, TK-13, TK-150, contract v0.14 |
| 2026-06-21 | **Q-28 resolved (COMMIT — record-integrity).** Gave a governance home to the v0.12 incidental finding: Q-14's resolution cited a proof script `planning/.q14_acyclicity_check.js` that is NOT in the repo (acyclicity was proven INLINE via Kahn). Chose to **soften the citation**, not commit a fabricated script — the contract-as-honest-record thesis demands prose match what was done, and validate.js already enforces the literal-edge cycle check. Edited Q-14 resolution text; raised+resolved Q-28. No DEC/scope/graph change; graph remains acyclic. validate PASS; gate_check analyze PASS. | architect | Q-28, Q-14, contract v0.13 |
| 2026-06-21 | **Frontier audit PRIORITY TRANCHE resolved (COMMIT, v0.18).** 6 CRITICAL + 17 seam-misses + 3 splits clustered: **Q-30** gate `select_items` selection seam (brief items never hit the live pending set; TK-25 journaled clear); **Q-31** pattern_id reaches the cog-worx contributor by per-turn construction (SlotContent chunks, not a keyed dict); **Q-32** taint/tier naming fixed to real cog-worx (`untrusted-source` tag, `TierViolation`, `blocked_by_taint`+`cancelled_at` on TK-146, TK-151 per-stage `tool_policy` + structural fail-closed); **Q-33 + DEC-23** dream off-path LLM inference admitted (gate stays model-free), TK-54 dream-substrate provider added, TK-46 split → TK-46 scaffold + TK-52 trigger + TK-53 Sweeper-boot; **Q-34** RecallStack→ScopedKG point-read, TK-13→`params.py/OperatingParams` (collision w/ TK-1 fixed). HIGH/MEDIUM parked at **Q-35**; **Q-36** cold-boot = cog-worx-substrate property (no in-memory queue dev mode). +3 tickets (79), +1 DEC, +7 Q. Graph re-proven acyclic. validate PASS; gate_check analyze PASS. | architect | Q-30..Q-36, DEC-23, TK-25/27/46/47/52/53/54/75/98/99/101/111/112/114/117/118/131/136/146/148/149/151, contract v0.18 |
| 2026-06-20 | **Q-10 / DEC-20 SETTLED (COMMIT — routed decision, delegated by Jim).** Jim explicitly delegated the external-tier sign-off to the architect. Authored **DEC-22** (supersedes DEC-20's 'sign-off is Jim's/pending' STANDING only; DEC-20 substance stands): the external-tier admission **authorization is GRANTED**. Drew the bright line — (a) the planning authorization is mine and now settled; (b) flipping the cog-worx external-tier flag in deploy config is a **build-time op step** on TK-151, not a planning blocker. Q-10 → fully resolved (no longer escalated); phantom human blocker removed. Updated TK-151 so the flag is a documented bring-up step and the binding still fails closed until set (no runtime self-grant; AwaitHuman + taint still apply). No frame/scope/capability-set change. validate PASS; gate_check analyze PASS. **Zero remaining human dependencies.** | architect | Q-10, DEC-20, DEC-22, TK-151, contract v0.11 |
| 2026-06-21 | **Spike-outcome reconciliation (COMMIT, v0.23 — Q-39/Q-40, within authority).** Two code/architecture items the de-risk build (v0.22) surfaced. **Q-39 file-layout drift:** chose canonical homes + made `files_in_scope` match the as-built tree (no physical moves needed — build never wrote to the old planned paths, so drift was purely contract-side). **TK-41 production leaf → `src/wombat/rating/params.py`** (NOT user_model/): RatingParams is the shared rating *vocabulary* (gate TK-23 + read seam TK-42 + tuner TK-49), co-located with the tuner; user_model/ stays the claims read/write package. TK-49 prod tuner → `rating/rating_tuner.py`; TK-42 annotated to import RatingParams from rating/. Throwaways adopt as-built (TK-48 rating/tuner_sim.py, TK-4 presence/probe.py, TK-73 calendar/*, TK-22 scoring_fixture.real.yaml; TK-23 AC softened off the gitignored file). `CalendarEventItem` ruled a shared DOMAIN type at `calendar/models.py` (TK-72 poller stays under gcal/, type lives in calendar/). **Q-40 persist TK-48 finding:** the RISK-4 ablation proved floor/ceiling clamps ALONE do not bound the surfacing RATE — band+gain+delta_bound+ceiling are a JOINT set. TK-13 OperatingParams now owns the 5 constants as one coherent block ([0.35,0.65]/0.05/0.20/12.0/day) + new AC; TK-49 reads the whole block (new AC: no inline literal, delta clamped to ±delta_bound before band clamp). TK-48 constants LOCKED; TK-26 thresholds (0.75/1.0/3) recorded PROVISIONAL pending Jim's RISK-1/2 label pass. +Q-39/Q-40 resolved. No DEC/constitution/v1-scope/graph change. 66 tests stay green; validate PASS; gate_check analyze PASS. | architect | Q-39, Q-40, TK-41/42/48/49/73/72/4/22/23/13/26, contract v0.23 |
| 2026-06-21 | **Spike-disposition pass (COMMIT, v0.24 — within authority).** Routed by Jim ("decisions route to architect, not back to me"). Gave every de-risk spike an owned disposition so the build is unblocked and nothing parks on Jim's desk. RISK-4/5 already retired (running code). The 4 human/live-gated spikes → **(a)ACCEPT the automated/proxy proof + (b)DEFER the residual**: **TK-22/RISK-1 + TK-26/RISK-2** (scoring) — architecture+mechanism proven on real-data fixture, no systematic error; calibration-vs-Jim DEFERRED as **DEF-1** (trigger: first real brief with VIP/known-human mail — the real window had zero, so a label pass can't sample the target population yet; constants PROVISIONAL in TK-13, relabel = YAML edit). **TK-73/RISK-6** (slots) — ≥3 valid slots/conflict proven, clears TK-74; ≥80%-reasonable UX-bar DEFERRED as **DEF-2** (trigger: first real conflict in a brief). **TK-4/RISK-3** (presence) — conservative-on-stale HOLD (safety property) proven, fails-safe to more silence; >90%-accuracy DEFERRED as **DEF-3** (trigger: first live laptop session). All 6 spike tickets → `done`; RISK-1/2/3/6 → mitigated, RISK-4/5 → retired. +3 deferrals (DEF-1/2/3), each with a revisit_trigger. No DEC/constitution/v1-scope/graph change. validate PASS; gate_check analyze PASS. **Next-build directive issued:** TK-13 → TK-23/25/27 gate spine; TK-14→TK-1 keystone in parallel. | architect | DEF-1/2/3, RISK-1..6, TK-22/24/26/48/73/4, TK-13, contract v0.24 |
| 2026-06-21 | **TK-13 AC4 guard shape ruled (COMMIT — DEC-25, routed build decision, within authority).** Building TK-13, AC4's no-competing-literal guard collided with frozen-spike reality: `rating/tuner_sim.py` (TK-48) legitimately hard-codes the exact LOCKED constants TK-13 owns because it is their provenance source (RISK-4 retired), and the named production consumers (gate TK-27/28, ledger TK-9, tuner TK-49) aren't built yet. RULED: (a) EXCLUDE `_sim` spike files from the scan — `tuner_sim.py` is the frozen SOURCE, not a competing definition; no retrofit (spikes frozen per DEC-24); (b) guard scans a DECLARED LIST of production consumer paths, asserts clean vacuously while absent, becomes ENFORCING the moment TK-27/28/9/49 land + import from OperatingParams; the reciprocal import obligation lives on those consumers' own ACs (TK-49 already, Q-40) so it can't stay silently vacuous. Edited TK-13 AC4 + added DEC-25. Coherent w/ DEC-24/Q-39/Q-40. No DEC/constitution/v1-scope/ticket/graph change. validate PASS; gate_check analyze PASS. | architect | DEC-25, TK-13, TK-48/26/27/28/9/49, contract v0.27 |
| 2026-07-02 | **TK-42 build-briefing cluster ruled (COMMIT — Q-41, routed build decision, within authority).** Four code-confirmed gaps between TK-42's text and the as-built tree. (1) ItemKind→EventClass BLOCKER: REJECTED a bare mapping table (lossy — no ItemKind maps to CALENDAR_CONFLICT; the v1 wedge's flagship rated class would silently degrade to GENERIC, gutting the DEC-13 personalized-gate thesis) and rejected changing `ratings_for(item)`'s signature. RULED: event class travels in `GateItem.payload["event_class"]` (TK-21's designed extension point — canonical model NOT amended, ItemKind stays composition-routing-only per ISS-4); TK-42 owns `resolve_event_class(item)` with the total ItemKind fallback map (BRIEF→MORNING_BRIEF, REFLECTION→REFLECTION, DRAFT→DRAFT_REPLY, GENERIC→GENERIC); conflict-item producers stamp the key (homed just-in-time per Q-35). (2) user_id: INJECTED constructor arg forming `user:<user_id>` — as-built TK-13 has no user_id field; a one-field OperatingParams addition is pre-authorized under TK-13's ownership when the composition root wires UserModel. (3) `ratings_for` is **async def** — claims_about AND cog-worx `Stage.run` are async; the briefing's sync `asyncio.run` facade REJECTED because it would RuntimeError inside the running Stage loop and be swallowed by AC3's broad except into defaults-forever (invisible thesis defeat); the sync gate ScoreFns receive already-awaited params (TK-23 AC1 reworded). (4) Wire shape: Claim.payload is a plain str + TK-44 unbuilt → TK-42 defines the PROVISIONAL wire contract homed in `rating/params.py` (RATING_CLAIM_PREDICATE + to/from_claim_payload; JSON {version, urgency_base, urgency_gain, load_base, load_gain}; entity=EventClass value; newest-active wins; malformed→defaults+warn); TK-44/TK-49 MUST write through the helpers (TK-44 annotated). TK-42 budget/ACs/files_in_scope amended. No DEC/constitution/v1-scope/dep-graph change. validate PASS; gate_check analyze PASS. | architect | Q-41, TK-42, TK-21/23/44/49/13/74, contract v0.29 |
| 2026-07-02 | **TK-23 composition ambiguity ruled (COMMIT — Q-42, routed build decision, within authority).** The spike `ScoringParams` (7 heuristic fields) vs production `RatingParams` (4 base/gain fields) shape mismatch, ruled before code. (1) **Composition:** spike signal computation KEPT, its 7 constants FROZEN as module-level heuristic constants in gate/scoring.py (the hardened SHAPE — NOT moved to TK-13 OperatingParams: not operating thresholds, and a variable weights object is a tuning surface TK-23 forbids); personalization is the affine map `urgency = clamp01(urgency_base + urgency_gain * raw_urgency)` (raw = 0.55·time + 0.45·sender) and `cognitive_load = clamp01(load_base + load_gain * raw_load)` (raw = 0.6·density + 0.4·depth) — matches both the RatingParams docstrings and the TK-48 tuner-sim model (bases are the additive lever the RISK-4 proof moved). Strict AC2 monotonicity PROVEN for the whole TK-13 band (a >4h item never exceeds raw 0.45, so no clamp tie while base + 0.45·gain < 1.0; band worst case 0.9425); gain=0 type-legal but degenerate, AC2 specified over gain>0. (2) **AC1 "solely" = purity clause** (no I/O/clock/randomness/presence/mutable state), not no-named-constants; no TK-13 AC4 guard conflict. (3) **ScoringParams DELETED**; signature takes RatingParams; TK-22 spike tests ported IN PLACE with identity params (base=0, gain=1) reproducing spike scores EXACTLY — the port IS the behavioral-equivalence proof; RISK-1/DEF-1 posture undisturbed. (4) **AC3's ratings_for-fallback lives on TK-42** (as-built, never raises; scoring never calls it per Q-41); TK-23 AC3 reworded to payload TOTALITY (missing key → silent quiet default; invalid value → default + warning — the warning IS the hardening). TK-23 budget/AC1–AC4/files_in_scope amended (+test_scoring_spike.py). validate PASS; gate_check analyze PASS. | architect | Q-42, TK-23, TK-22/41/42/13/48, contract v0.31 |
| 2026-07-02 | **TK-27 CONVERGENCE ruled (COMMIT — Q-55, routed build decision, within authority).** The whole run converges: (1) Gate REBUILT IN PLACE in pipeline.py as the thin ASYNC orchestrator — `Gate(*, user_model, pending_set, ceiling, urgency_threshold, load_flush_threshold, flush_min_age_seconds, clock, on_event)`, skeleton pending dict replaced by TK-25's PendingSet, TK-21 tests ported, decay stays TK-28. (2) `async pipeline(items, *, surfacing_permitted=True)`: await ratings_for per item → bind into pure sync urgency/cognitive_load (Q-41/42); presence computed by the CALLER (Q-12/Q-54) — False suppresses BOTH arms but items still score+accumulate. (3) ONE decision/call: immediate arm (permitted ∧ u>threshold ∧ ceiling.allow(event_class)) → record+surface_immediate; else durable `add(scored, added_at=clock())`; flush arm EVERY call incl. `pipeline([])` (load>threshold ∧ oldest≥min-age) → all-pending urgency-desc + durable journaled clear; events via injected on_event. (4) Ceiling per EVENT CLASS (`ceiling:<value>`; DEC-13 key); CeilingProtocol → un-gated arm tests; ledger tests DSN-gated. (5) **Q-30 predicate defined:** `is_surfacing_worthy = u > urgency_threshold` — ONE function shared by the immediate arm + async select_items (mutates nothing); Q-30's phrasing incoherence resolved on the record (flush inclusion = membership-in-pending). (6) **Min-age gap found+closed:** PendingSetAdd record gains `added_at` (sanctioned TK-25 rider; NOT on canonical ScoredItem; legacy replay 0.0; TK-29 inherits the wire free). (7) TK-13 AC4 guard ENFORCING (+pipeline/trigger/ceiling paths). (8) **Custody switchover:** hold = durable PendingSet custody BEFORE queue ack — the Q-52 crash window collapses to idempotent re-add; review_or_speak structurally unchanged. Demo shapes ruled for the post-TK-27 harness step (async-batch GateStage seam; flush = synthesized terse digest; batch_size=1 stands; no-heartbeat-while-Waiting recorded → TK-28/97). | architect | Q-55, TK-27, TK-21/23/25/26/42/152/13/11/28/29/99/100, Q-30/41/44/46/48/51/52/54, DEC-13, contract v0.55 |
| 2026-07-02 | **TK-11 presence hardening ruled (COMMIT — Q-54, routed build decision, within authority).** (1) **Homes:** types + hardened classify/read_idle_ms + the Q-49 PROVIDER → `sources/presence.py`; the pure canonical predicate `presence_hold(snapshot, now, *, staleness_ceiling_s, confidence_floor)` → `gate/presence_hold.py` (keyword-REQUIRED bounds, no baked defaults — the spike's baked defaults were the drift risk; defaults live in the versioned YAML). (2) **AC4 flip rider sanctioned** (import-line-only): gate/gate.py, stages/gate_stage.py, tests/unit/test_gate.py, the e2e. (3) **Spike SUPERSEDED** (Q-43/TK-25 precedent): probe.py + test_probe.py DELETED — logic hardens into production; a kept copy = dead second predicate + duplicate types; TK-4's record intact (poc.result + DEF-3). (4) **Config:** 3 OperatingParams fields added NOW under the DEF-3-pre-committed TK-13 home (staleness 300.0 / confidence-floor 0.5 / idle-threshold 60.0; YAML version bump = the designed change path); injected, no inline literals. (5) **States** active|idle|away|unknown, AWAY = idle≥1800s as a DESCRIPTIVE module constant (property test: IDLE/AWAY hold identically — deliberately NOT a TK-13 param). **Now-seam = two layers:** provider degrade at provision is the primary runtime guard (age-0 call sites inert BY DESIGN, Q-49); the predicate keeps its own explicit-now staleness check as DEFENSE IN DEPTH, property-tested directly incl. the sleep-gap case. | architect | Q-54, TK-11, TK-4/6/13/27/28, DEF-3, Q-17/43/49/53, DEC-6, contract v0.53 |
| 2026-07-02 | **TK-5 Wait-self-edge defect ruled (COMMIT — Q-53, cross-ticket corrective, within authority).** TK-7's real-Engine e2e caught a **shipped defect in done/committed TK-5**: `DrainQueueStage.transitions=("gate",)` omits the Wait-to-self edge, so the runtime declared-route guard raises `StageGraphError` on the idle path — invisible to 191 green stage-level tests because StageContextFake never exercises the graph guard; breaks the demo idle loop. RULED: (1) **fix confirmed** — `transitions=("gate","drain_queue")`; Wait-to-self IS the intended idle idiom (committed Wait replays as advance, Sweeper re-drives); the defect is a missing edge DECLARATION. (2) **Sanctioned cross-ticket rider on TK-7** (v0.47 pattern; TK-5 stays done, corrected honestly, append-only preserved; TK-7 scope += drain_queue.py + its test; idle e2e xfail(strict) must flip to a REAL pass before TK-7 records). (3) **Spine convention:** `transitions` MUST declare EVERY returnable StageResult destination incl. Wait-to-self edges (TK-10's derive-from-map idiom is the model); any Wait/self-park stage needs ≥1 real-Engine/StageGraph route check — TK-11/TK-27 inherit. META: the Q-52 honest-full-stack ruling did exactly its job; the builder's no-silent-patch + loud strict-xfail discipline is endorsed as the pattern for cross-ticket defects found mid-build. | architect | Q-53, TK-5, TK-7, TK-11/27, Q-47/Q-52, contract v0.51 |
| 2026-07-02 | **TK-7 ReviewOrSpeak + e2e harness ruled (COMMIT — Q-52, routed build decision, within authority).** (1) **AC2 vs stages-never-journal reconciled:** hold → terminal `Done(Artifact kind='wombat.hold_report', data={holds:[{item_id,item_kind,reason,urgency,load}]})` — the engine journals every StageResult BEFORE advancing (verified S6), so the reason is durable with no direct write; scores may ride it (journal = internal audit record, not the mouth wire; CON-1 enforced at TK-10's boundary). (2) **Two exits, one result:** at batch_size=1 a batch is EITHER one hold (Done) OR one surface (ack + Transition w/ surfaced_item) — the mixed case is **structurally unreachable and now load-bearing** (one result = one artifact); defensive >1: ack all, forward first, LOUD warnings name non-forwarded ids; superseded at TK-27. (3) **Ack timing:** inside run() on BOTH branches (decide→ack→return); the ack-before-engine-journal crash window ACCEPTED at stub custody — both failure modes degrade toward SILENCE (double-surface impossible post-ack; ack idempotent), and ack-after-commit is structurally unavailable (a stage can't observe the engine commit); custody replaced at TK-27. (4) Queue = the SAME injected instance as drain (ASMP-2). (5) **E2E = honest full stack, DSN-gated:** real Engine + cold_boot InMemoryJournal + all five stages + real pg queue + REAL ComposeStage w/ registered deterministic FakeModel + degrade variant (keyless path); asserts queue-empty, journal artifacts, one router Transition, second-drive Wait. This e2e IS the v0.39 demo skeleton. | architect | Q-52, TK-7, TK-5/6/8/10/27, ASMP-2, Q-46/47/49/50/51, contract v0.50 |
| 2026-07-02 | **TK-10 dispatch model + batch single-ization ruled (COMMIT — Q-51, routed build decision, within authority).** (1) **Option (a) confirmed:** the router is a Stage returning `Transition(to=<composer stage name by item_kind>, output=compose_request)` — the ENGINE drives the composer; "invoked once" tested as `Transition.to ==` the stub composer's name; router never calls composers in run() (option (b) rejected — breaks the engine model + TK-8's as-built input). (2) **Batch:** the router reads a NEW single-item wire **`wombat.surfaced_item`** produced by TK-7 — NOT the raw gate batch. TK-7 owns single-ization: acks+journals every HOLD (stub-spine custody = dispose-quietly; PendingSet custody arrives at TK-27), acks the ONE forwarded item (safe — Q-50 compose never fails). One-surfaced-per-cycle is an **mvp composition rule: drain batch_size=1** (the Q-47 injected arg) — the load-bearing reason is the **stranded-lease trap**: same-epoch leased rows are never re-drained (Q-46), so deferring a surplus surfaced item would strand it until restart. Defensive: >1 surfaced → forward first + LOUD warning naming unrouted ids (journaled, never silent). Explicitly superseded at TK-27 (PendingSet custody + consolidated flush + ceiling-bounded immediate); **TK-27's briefing MUST revisit batch_size=1.** Wire helpers added to artifacts.py in TK-10's build; TK-7 produces through them later. | architect | Q-51, TK-10, TK-7/8/6/5/27, Q-46/47/48/49/50, contract v0.48 |
| 2026-07-02 | **TK-8 mouth-wiring cluster ruled (COMMIT — Q-50, routed build decision, within authority).** (1) **Model:** `run()` uses `ctx.model.complete` — the engine assembles the per-drive model from TK-1's DeepSeek spec (engine `model_profile='deepseek'`; no separate mouth profile in v1; TK-9's BudgetGuard later attaches via the same `guard=` seam with zero compose change); the stage never calls build_model, no client at import. **AC3 at construction:** `__init__(*, config: WombatConfig, template_composer, timeout_seconds=2.0)` validates `deepseek_api_key` non-empty → ConfigurationError (also guards blank-string env). (2) **Input wire defined now** (TK-10 produces it later, zero rework): `wombat.compose_request` `{item_id, item_kind value, payload}` — one surfaced item per invocation; **NO scores/GateAction/queue internals may cross the wire** — the never-expose-the-model non_goal enforced STRUCTURALLY at the wire, not by prompt discipline alone. (3) **Prompt:** system = fixed terse steward instruction; user = payload fields + item_kind only; AC1 asserts no internal keys captured; TemplateComposer = pure deterministic fallback from the SAME fields. (4) **Degrade:** `asyncio.wait_for` 2.0s (AC-fixed, not a TK-13 tunable); Timeout/provider errors/BudgetExceeded (degrade NOT raise — the mouth never breaks the drain loop)/empty text ALL → template `degraded=True`; run() never raises. (5) **Terminal `Done`** with `wombat.composed_output {text, item_id, item_kind, degraded}`; EP-30 deliver flips Done→Transition later; both new wires JSON-native + dumps regressions (Q-49). files_in_scope += artifacts.py, stage_context_fake.py. | architect | Q-50, TK-8, TK-1/9/10/7, Q-47/48/49, contract v0.46 |
| 2026-07-02 | **TK-6 verifier follow-ups ruled (COMMIT — Q-49, routed build decision, within authority).** (1) **JSON-native wire rule, fixed IN TK-6 before done:** `item_kind` serializes as `.value` (reconstructed `ItemKind(value)`), mirroring `action`; generalized — `Artifact.data` MUST be plain-JSON-native (enums as `.value`) with a `json.dumps` round-trip regression. The shared wire-helper module cannot be internally inconsistent, and correctness must not depend on WHICH journal adapter serializes (TimescaleJournal pydantic-coerces; the demo's InMemoryJournal path is uncleared). TK-7/8/10 inherit. (2) **Staleness seam ruled — freshness resolves at the PROVIDER:** the stage-side presence_provider (impure, may read the clock) applies the staleness ceiling at provision time, degrading stale → unknown/confidence=0.0; the pure `evaluate` keeps its time-free signature UNCHANGED (gate stays clock-free per Q-12/Q-42; unknown→hold already structurally holds); TK-11 homes the canonical staleness logic (property test drives `presence_hold(snapshot, now)` directly); TK-27 inherits the provider seam. The now-param-on-evaluate alternative REJECTED (clock leaks into the pure decision fn + signature migration). The stale-branch inertness at the evaluate call site is BY DESIGN — recorded so TK-11/TK-27 briefings don't silently assume it solved. (3) TK-6 budget aligned to the as-built `stub_evaluate(gate_item, presence, *, urgency_threshold)`. | architect | Q-49, TK-6, TK-7/8/10/11/27, Q-47/Q-48/Q-12/Q-42, contract v0.44 |
| 2026-07-02 | **TK-6 GateStage cluster ruled (COMMIT — Q-48, routed build decision, within authority).** (1) `gate/decision.py` DROPPED — GateDecision/GateAction/ScoredItem are TK-21 canonical (ISS-4: imported, never redefined); `gate/gate.py` = `gate_item_from_queue_item` + `stub_evaluate`. (2) **STUB scoring stands** over wiring TK-23 now: TK-6's intent is structural silence-default in the WIRING; real scoring is TK-27's designed convergence (Q-41 async RatingParams composition; the demo milestone is AT TK-27; wiring it here = duplicate-then-rebuild). Deliverable = the replacement seam: GateStage takes an injected `evaluate: (GateItem, PresenceSnapshot|None) -> GateDecision`; stub reads `payload['stub_urgency']` (high 0.9 / low 0.1, DEFAULT LOW = quiet default) vs an injected `urgency_threshold` (composition passes OperatingParams.urgency_threshold, no inline literal); decisions reuse canonical `GateDecision(action, items=(ScoredItem,))`. (3) **Presence interim:** consume the TK-4 spike's PresenceSnapshot + proven conservative `presence_hold` from probe.py, applied BEFORE scoring (Q-12); unknown/stale/low-conf → HOLD; TK-11 flips the import to the hardened predicate (its existing AC4 — no new debt). (4) **Stage seam per Q-47:** in via `last_output('drain_queue')` + shared helper; out = ONE batch artifact `wombat.gate_decisions` → `review_or_speak`, each entry carrying the original queue-item dict so TK-7 can ack holds; to/from helpers ADDED to stages/artifacts.py, TK-7 must deserialize through them. files_in_scope: −decision.py, +artifacts.py. | architect | Q-48, TK-6, TK-21/23/27/11/4/5/7, Q-47/Q-41/Q-12, contract v0.42 |
| 2026-07-02 | **TK-5 Stage-integration conventions ruled (COMMIT — Q-47, routed build decision, within authority).** First cog-worx Stage integration; conventions propagate to TK-6/7/8/10. (1) **Blocker fixed at the queue:** `drain(limit: int | None = None)` — FIFO SQL LIMIT leasing ONLY N rows (None = drain-all, TK-2 tests untouched); in-memory slicing REJECTED (orphans leased rows); new gated limit-leases-exactly-N test; TK-5 scope += queue.py/test_queue.py. (2) **Canonical Artifact convention:** kind = `wombat.<noun>` (`wombat.drained_batch`/`wombat.drain_heartbeat`); produced_by = canonical snake_case stage name verbatim (`drain_queue`/`gate`/`review_or_speak`/`compose_dispatch`/`compose`); batch data = `{items:[QueueItem-dict...]}`; kind constants + to/from helpers homed ONCE in `stages/artifacts.py`, TK-6 MUST deserialize through them (Q-41 wire-helper principle at the stage seam). (3) `Wait(to=itself, wake_at=ctx.clock()+poll_interval` — NEVER wall-clock; idle Artifact `wombat.drain_heartbeat` data={}); interval/batch_size injected ctor args, TK-13 fields pre-authorized. (4) `build_drain_pathway(*stages)` builder; NO placeholder sink in src (test-local); TK-7 owns full assembly. (5) Stage touches ONLY `ctx.clock()`; queue injected (ASMP-2 single instance); stage never acks (TK-7 acks) nor journals (engine journals StageResults); cog-worx ships NO StageContext double (verified) → wombat builds `tests/support/stage_context_fake.py` (full Protocol, NotImplementedError except clock — reusable spine double, loud on ctx creep); AC3 proven at the queue layer (fresh-epoch redelivery). | architect | Q-47, TK-5, TK-2/6/7/8/10/13, ASMP-2, contract v0.40 |
| 2026-07-02 | **Single-live-queue invariant recorded (COMMIT — ASMP-2, routed governance call, within authority).** The TK-2 verifier confirmed + reproduced that the Q-46 epoch-based lease reclaim is sound ONLY single-host/single-process (DEC-6/DEC-8 frame): two simultaneously-live WombatQueue instances steal each other's leases → genuine double-surfacing, the exact too-loud failure the gate exists to prevent. Ruled it WORTH recording (not left as module docs): the invariant is load-bearing for the quiet-thesis and invisible at the future call sites that could violate it — an overlap-restart supervisor or second drain loop would pass every existing test while silently breaking production. **ASMP-2 (accepted):** at most ONE live draining WombatQueue process-wide; drain assembly (TK-5/6) constructs exactly one (composition-root singleton, TK-1 engine-guard posture); supervisors terminate-before-restart; TK-53 + pathways enqueue-only; TK-29 inherits the same premise. Multi-process post-v1 = replace the reclaim (lease timeout / pg advisory lock), a deliberate revisit, never a patch. | architect | ASMP-2, TK-2, TK-5/6/29/53, DEC-6/DEC-8, Q-46, contract v0.38 |
| 2026-07-02 | **TK-2 pg-conventions cluster ruled (COMMIT — Q-46, routed build decision, within authority).** TK-2 ESTABLISHES wombat's canonical Postgres conventions (TK-29/TK-152 + all future DB tickets inherit). (1) **Test convention (load-bearing):** DB-state tests are REAL-Postgres tests gated on `WOMBAT_TEST_PG_DSN` — loud pytest.skip when absent (fresh clone never fails, never fakes); a DB ticket flips to done ONLY after the independent verifier RUNS the gated suite against a real/throwaway docker Postgres (Docker 29.x confirmed on the dev machine; done-bar = runnable check, not CI-resident — the TK-23 local-real-fixture precedent). REJECTED testcontainers (unrequested dep) and psycopg/SQL fakes (a driftable Python re-implementation of ON CONFLICT proves nothing — distinct from TK-25's double, which carried TK-24-proven semantics while the wire deferred to TK-29; the queue's semantics ARE the SQL). (2) psycopg v3 (`psycopg[binary]`); files_in_scope += pyproject.toml + uv.lock. (3) NO migration framework (NG-3): numbered idempotent .sql + per-module `ensure_schema(conn)` via importlib.resources, called by tests + composition root. (4) Cold-boot coherence = **Q-36, already ruled** (queue REQUIRES pg; no in-memory/no-op variant); TASK-15's dangling pointer fixed. (5) DSN injected constructor arg (queue owns ONE lazy connection, no pooling, DEC-6); `max_size` injected int, `queue_max_size` pre-authorized as a TK-13 one-field addition at drain wiring (Q-41 pattern). AC3 mechanism ruled: **epoch-based lease reclaim** (per-instance uuid; own-epoch rows never re-returned within a run, different-epoch rows reclaimed — sound under single-host single-process; restart = new epoch = redelivery; ack deletes exactly once; no timeouts/clocks). validate PASS; gate_check analyze PASS. | architect | Q-46, TK-2, TK-29/152/13, Q-36/Q-41/Q-44, contract v0.37 |
| 2026-07-02 | **TK-25 eviction-atomicity bug ruled (COMMIT — Q-45, routed build decision, within authority).** Verifier LIVE-REPRODUCED a real RISK-5 break the 129-green suite was silent on: the evicting `add()` journaled `Add(new)` then `Remove(evicted)` as TWO independent appends — a kill between them rebuilds to max_pending+1 (AC3 violated across a crash). RULED **Remove-before-Add ordering** over a compound atomic record: (a) the size bound holds by ordering alone (only intermediate durable state = max_pending−1, never over); (b) the interrupted add() never returned, so aborting the uncommitted incoming item is clean write-ahead semantics, not loss (TK-24's contract protects COMMITTED mutations); (c) DECIDING — whole-system convergence: the TK-2 at-least-once queue never got the ack, redelivers the item on restart, and the re-run add() lands it in the max_pending−1 set — converging to exactly the compound-record end state with zero new record types (idempotent replay makes redelivery duplication-safe); the narrow non-queue residual fails toward SILENCE (product-safe direction, same posture as DEF-3); (d) the ordering rides journal append order, so TK-29's pg wire needs NO schema change. AC3 sharpened with the crash clause + two mandatory regression scenarios (kill BETWEEN appends → rebuild ≤ max_pending, uncommitted absent, load consistent; kill AFTER both → new present, evicted absent, size == max_pending); module docstring must state the ordering + abort semantics. Recorded (not a silent build-fix) because the ordering is a WIRE-level durability contract TK-29 inherits and AC3 was proven under-specified. validate PASS; gate_check analyze PASS. | architect | Q-45, TK-25, TK-29, TK-24/TK-2, Q-44, contract v0.35 |
| 2026-07-02 | **TK-25 persistence-seam cluster ruled (COMMIT — Q-44, routed build decision, within authority).** Four HOW rulings + one new sibling, grounded in the TK-24 seam-miss (cog-worx Journal is a positional step/run WAL — cannot host PendingSet ops). (1) **Seam:** wombat-owned `PendingJournal` Protocol in gate/pending_set.py (append/replay; frozen records `PendingSetAdd` carrying FULL ScoredItem fields so rebuild restores cumulative_load exactly, `PendingSetRemove`, ONE atomic `PendingSetClear`) + `InMemoryPendingJournal` double — NOT in SubstrateBundle (wombat-DOMAIN gate state, constructor-injected; Q-36/TK-2 precedent; pg_episodes/claims reuse rejected as seam abuse). Real pg adapter SPLIT to **new TK-29** (EP-8, P1, S, production; deps TK-25+TK-2): building psycopg SQL in TK-25 would invent wombat pg conventions ahead of TK-2 and ship production code with no CI-runnable check; TK-29 verifies the wire via a `WOMBAT_TEST_PG_DSN`-gated round-trip (skip in CI) and MUST land before the first live laptop session (RISK-5 honesty). (2) **Kill ACs in CI:** exact TK-24 mechanism (journal object survives, instance discarded, raise-after-N-appends wrapper); `clear()` = single write-ahead Clear record then wipe — before/after-append atomicity IS the exactly-once proof; 10 TK-24 scenarios PORTED to tests/gate/test_pending_set.py, superseded spike test file DELETED (Q-43 freeze principle). (3) **MAX_PENDING:** already `OperatingParams.max_pending` (as-built TK-13) — no TK-13 change; constructor-injected, no module literal, no guard conflict. (4) **Events/split:** `CapacityEviction` frozen dataclass in pending_set.py RETURNED from add() (mirrors as-built `Gate.decay() -> list[DecayEvent]`; canonical models.py untouched); aggregator.py = pure stateless math (cumulative_load exact sum, lowest-urgency selection), pending_set.py = durable stateful set, `cumulative_load()` delegates. TK-25 budget/files_in_scope amended; +TK-29 (80 tickets, 137 nodes, graph acyclic — TK-29 is a sink). validate PASS; gate_check analyze PASS. | architect | Q-44, TK-25, TK-29, TK-24/2/13/21/27, Q-36/Q-43, contract v0.34 |
| 2026-07-02 | **Q-42 follow-up coupling ruled (COMMIT — Q-43, routed build decision, within authority).** Builder flagged that Q-42's ScoringParams deletion ImportErrors the TK-26 frozen spike `trigger_sim.py` + regresses 3 baseline tests; architect grep found a THIRD consumer the report missed — `tests/gate/fixtures/_build_scoring_fixture.py`, the committed fixture builder load-bearing for TK-23's OWN AC4 CI regeneration path. RULED option (a) extended to all three: mechanical IDENTITY-PARAMS PORT inside TK-23 — `score_day` takes `RatingParams`; test_trigger_sim.py + the fixture builder pass `RatingParams(urgency_base=0.0, urgency_gain=1.0, load_base=0.0, load_gain=1.0)`, which reproduces spike raw scores EXACTLY (Q-42), so TK-26's recorded finding + fixture labels stay the true record (freeze SUBSTANCE untouched, plumbing only). Spike-freeze principle refined coherently with DEC-25: freeze forbids re-deriving spike logic / retrofitting spikes onto production stores — not mechanical type-ports forced by Q-39's in-place hardening of scoring.py with proven score-identity. REJECTED shim (a second params type = the drift Q-42 kills; dead type in a TK-13-guard-scanned file; deletion chore parked on TK-27) and accepted-regression (red tests on the green baseline violate the runnable done-bar). TK-23 files_in_scope += trigger_sim.py, test_trigger_sim.py, fixtures/_build_scoring_fixture.py; TK-27 inherits RatingParams-speaking call sites — no migration debt. validate PASS; gate_check analyze PASS. | architect | Q-43, Q-42, TK-23, TK-26/22/27, contract v0.32 |
| 2026-06-21 | **De-risk spike sequence set (COMMIT — DEC-24, Jim's directive, delegated).** Decided the order to run the 6 RISK-* spikes BEFORE the production build, and the real-data/human-label policy. **Run-order:** spike deps resolve to LEAF type/vocab modules (TK-21/TK-41/TK-72-type/TK-3) — REAL but CHEAP, so HONORED not relaxed (relaxing → spikes invent throwaway types production can't reuse). Pure-code spikes run NOW: **TK-21→TK-24** (RISK-5), **TK-41→TK-48** (RISK-4), zero input/infra. Then **TK-21→TK-22** (RISK-1), **TK-22→TK-26** (RISK-2). **TK-73** (RISK-6) ahead of the poller on TK-72's CalendarEventItem type. **TK-4** (RISK-3) last (TK-3 + live laptop). **Real-data ruling (product/privacy, grounded in vision.md):** HYBRID — fixtures seeded from REAL Gmail/Calendar via the connected MCP tools into a **gitignored local de-identified** YAML; NG-7 scopes residency to STORAGE so a local read is no egress (distinct from ASMP-1 DeepSeek exception); hand-authored-only is circular. Human label via a **proxy rubric derived from vision.md's gate** (relevance∧importance∧user-state∧confidence; default-hold) + a small one-time Jim confirmatory pass, not from-scratch labeling. **Edits:** +DEC-24, +Q-37/Q-38 resolved; TK-22 budget gained provenance+rubric+run-order notes; .gitignore +throwaway-real-data ignore. No dep/graph/DEC/scope change. validate PASS; gate_check analyze PASS. | architect | DEC-24, Q-37, Q-38, TK-22, NG-7, ASMP-1, contract v0.20 |
| 2026-07-03 | **TK-53 recorded DONE (COMMIT, v0.87 — build+verification record, within authority) + Q-72 homed → new TK-17.** TK-53 built + INDEPENDENTLY adversarially verified (PASS_WITH_CONCERNS, nothing blocking; builder correctly did not touch the contract — Q-69 rule holding). wombat is one standing process: `python -m wombat` → serve() (WOMBAT_PG_DSN required loud, not in REQUIRED_ENV) → `assemble_runtime()` registers `wombat.drain` + composes the REAL gate over the TK-29 PgPendingJournal (isinstance-verified) + LIVE budgets (Q-69 AC proven meaningful vs unbounded defaults) → one initial drive → Sweeper.run_forever — no authored loop (DEC-12); minimal finally shutdown; terminate-before-restart = documented ASMP-2 operator note; runtime.py registers nothing (Q-71 split honored). All 5 ACs genuinely verified (AC5 full cycle RAN on throwaway docker postgres:16, tick()-driven); mypy 13 errors all pre-existing (git-stash-proven); 506/4 with DSN; scope exact. **RISK-5 resolution UPDATED: the Q-70 boot-wiring obligation is DISCHARGED — durability now honestly claimable for the live runtime.** The one verifier concern homed as **Q-72 → TK-17** (EP-1, P1, S, dep TK-53, files pyproject.toml+uv.lock): `openai` (cog-worx providers extra) undeclared — Engine._build_context imports it eagerly before the degrade path, breaking clean-sync DSN-gated boot tests AND clean-install serve(); ruled a CORE-dependency TICKET, not a deferral (it is a hard runtime dependency and a deferral would leave the Q-46 clean-checkout done-bar broken for every ticket after TK-53). 37/83 done; graph acyclic (TK-17 leaf). Pre-live gate unchanged (the two OAuth smokes). validate PASS (140 nodes, 117 governance); gate_check analyze PASS. NEXT: TK-17 (tiny) + TK-28, then the brief cluster from TK-98. | architect | TK-53, TK-17, Q-72, Q-69/70/71, RISK-5, ASMP-2, Q-46, contract v0.87 |
| 2026-07-03 | **TK-28 recorded DONE (COMMIT, v0.90 — build+verification record, within authority); the DEC-13/RISK-2 quiet-across-midnight posture CLOSED.** Built exactly per the Q-73 rulings (13 files, 10 Gate ctor sites) + independently adversarially verified PASS (builder correctly did not touch the contract — Q-69 rule holding). `decay_stale` over the journaled `added_at` with removal through the JOURNALED `PendingSet.remove()` — decayed items can never resurrect on rebuild (RISK-5 reinforced); `DayRollover` exactly-once `LedgerReset` only when `DailyLedger.increment("rollover:gate")` returns 1 — the verifier ATTACKED the exactly-once claim and disproved the fixed-key-fires-once-ever suspicion (composite `(ledger_name, wombat_date)` PK, migration 003) with its OWN 3-boundary + genuine-reconnect probe on real docker pg (every new-day first observation = exactly one reset; same-day repeats + mid-day restart silent); kill-consistency four sub-proofs genuine, not tautological (incl. exhaust-ceiling-to-N → kill → rebuild → post-restart ≤ ceiling−N, and TK-24 `_KillSwitchJournal` kill-mid-decay convergence); wake-burst K=5>ceiling=2 past midnight+ttl → exactly 2 SURFACE_IMMEDIATE + ONE LedgerReset + both pre-sleep items decayed; pipeline purely additive (TK-27 arms/flush byte-identical, `git diff -U0` proven); ctor args REQUIRED never optional (the Q-69 orphaned-budget lesson); `LedgerReset` homed in decay.py; `today()` keeps the Q-15 boundary source singular. 476/37 no-DSN, 519/4 with DSN; ruff clean; mypy zero introduced; files touched = scope exactly. Contract edits: TK-28 status→done; RISK-2 resolution annotated (posture closed; threshold-VALUE residual stays DEF-1, unchanged); changelog v0.90. Heartbeat arc-note discharge STANDS (Q-73). **39/83 done; EP-9 support work COMPLETE — NEXT: the brief cluster from TK-98; TK-98's briefing routes to architect FIRST and MUST rule Q-56 over-pull #3 (TK-98→EP-17 dragging TK-77/TK-80 + transitively the EP-13 dream cluster).** validate PASS (140 nodes/118 gov); gate PASS; append-guard verified green vs HEAD. | architect | TK-28, Q-73, RISK-2/5, DEC-13/21, DEF-1, Q-56, contract v0.90 |
| 2026-07-03 | **TK-98 briefed (COMMIT — Q-74, /plan-ticket pass, within authority) — the BRIEF CLUSTER OPENED; Q-56 over-pull #3 CLOSED.** TK-98 `depends_on: EP-15/EP-17` narrowed to **TK-72 + TK-75 + TK-76** (all done) — todo TK-77/TK-80 and the transitive EP-13 dream cluster no longer falsely block the FEAT-7 payoff. Remaining Q-56 over-pulls (ruled at their own briefings): TK-96→EP-17/EP-30, TK-101→EP-30, TK-77→EP-13. Seams verified as-built; two findings drove the rulings. **(1) The pollers' `poll()` NEVER raises** (Q-59/Q-65 degrade-to-`[]` posture) — AC2's unavailable flags can't ride it (conflates unavailable with zero-events), and the SourceRegistry is enqueue-only (ASMP-2), so TK-98 **extracts a raising fetch method on each poller** (`CalendarPoller.fetch_window` / `GmailPoller.fetch_recent`; `poll()` becomes a thin wrapper, behavior byte-equivalent, existing poll tests the untouched regression net); the stage injects two zero-arg callables + composition-loaded `triage_rules` (triage.py's own documented plan). **(2) BriefPayload = frozen + JSON-native with a METADATA-ONLY gmail slice** (GmailBriefItem = subject/sender/received_at + triage result, never the guarded body key — brief files NOT on the Q-65 allowlist, so metadata-only is STRUCTURAL for the whole brief pipeline) **and NO conflict field** — the intent/non_goal tension resolved data-vs-logic: the calendar_events slice IS the raw candidate set; detection runs downstream (TK-99 deps TK-74; TK-96's pathway names DetectConflicts). Stage: `brief_gather`, `transitions=("brief_force_flush",)` (declare-ahead precedent — TK-99's stage must take that name), returns Transition + Artifact `wombat.brief_payload` (constant added to artifacts.py), system Provenance; never touches ctx.journal (AC3 confirmed against compose/gate as-built). AC2 = per-source broad except (token expiry = google-auth RefreshError, not RequestException) → empty slice + flag; triage inside the gmail block; both-down still returns a flagged payload, never raises. Windows: gcal 48h lookahead / gmail 24h lookback — pollers own all time math, stage stamps only `generated_at`. TK-98 amended: deps narrowed, files_in_scope 2→5, estimate S→M. validate PASS (140 nodes/119 gov); gate PASS. **TK-98 BUILD-READY — then TK-99.** | architect | Q-74, Q-56, TK-98, TK-72/75/76, contract v0.91 |
| 2026-07-03 | **TK-98 recorded DONE (COMMIT, v0.92 — build+verification record, within authority) — the brief cluster's first stage is REAL.** Built exactly per the Q-74 rulings + independently adversarially verified PASS (builder correctly did not touch the contract — Q-69 rule holding). NEW `domain/brief_payload.py` (frozen JSON-native `GmailBriefItem` metadata+triage-only + `BriefPayload` with per-source `_unavailable` flags, NO conflict field) + NEW `stages/brief_gather_stage.py` (`brief_gather`, transitions=`(brief_force_flush,)`, ctor-injected fetch/triage/clock seams, per-source broad-except degrade, returns Transition + `wombat.brief_payload` Artifact, never touches ctx.journal); both pollers gained RAISING `fetch_window`/`fetch_recent` with `poll()` a diff-proven byte-equivalent extract-method wrapper (existing poll tests untouched — the Q-74 regression net held; test files pure-append). **Verification strong form:** the no-body-key property proven at RUNTIME (SECRET-MARKER body through the REAL stage+triage → absent from item, fields, and `json.dumps(artifact.data)`; explicit keyword construction, no asdict/splat; Q-65 AST guard covers the new files, no allowlist addition — metadata-only stays STRUCTURAL); AC2 never-raises + per-source independence, AC3 no-journal (load-bearing spy), AC1 verbatim all verified; ruff clean, mypy zero new, 506/37, scope exact. Contract edits: TK-98 status→done; changelog v0.92. **40/83 done. NEXT: TK-99 (BriefForceFlushStage) — its briefing must settle: where DetectConflicts (TK-74 kernel via `conflict_to_payload`) is invoked, the `gate.select_items` seam over the BriefPayload's OWN items (threshold-free, pending-set-PRESERVING per Q-30/Q-55), the sealed `BriefDecisionArtifact` stamped canonical `item_kind=brief` (TK-21/ISS-4 enum), and the audit-CRITICAL invariant that brief items NEVER enter or clear the live drain pending set.** validate PASS (140 nodes/119 gov); gate PASS; append-guard verified GREEN vs HEAD (checkWrite, zero violations). | architect | TK-98, Q-74, Q-56/65/69, TK-72/75/76, TK-99, contract v0.92 |
| 2026-07-03 | **TK-99 briefed (COMMIT — Q-75, /plan-ticket pass, within authority) — the DetectConflicts placement ruled head-on, option (a).** Conflict detection FOLDS INTO BriefForceFlushStage: the stage runs the pure TK-74 kernel `detect_conflicts(payload.calendar_events, tz)` itself (tz = ctor-injected ZoneInfo, the DEC-21 zone) and feeds events + derived conflicts + gmail items through ONE `gate.select_items` call — **NO separate DetectConflicts graph node ever exists**; TK-98's committed `transitions=('brief_force_flush',)` stays honest and TK-96's named DetectConflicts step is a LOGICAL step realized inside brief_force_flush (brief pathway = FOUR stages: gather → force_flush → compose → deliver; TK-96's briefing inherits the note). Seams verified as-built: `Gate.select_items` non-mutating (worth-predicate-only — 'threshold-free' clarified as free of load/min-age/ceiling/presence, NOT of `is_surfacing_worthy`); `ItemKind.BRIEF` canonical; `conflict_to_payload` stamps `event_class=calendar_conflict` (the Q-41 REQUIRED path). Key rulings: the stage injects the NARROW async `select_items` callable, never the whole Gate (live pending set STRUCTURALLY unreachable — the strongest form of the audit-CRITICAL invariant); three-family GateItem adaptation (events: `is_timed` + `seconds_to_event` derived from the JOURNALED `generated_at` — replay-stable, never ctx.clock — + `sender_class=self`; conflicts: `conflict_to_payload` + `self`; gmail: PriorityBand HIGH→known_human / NORMAL→automated bridge); sealed `BriefDecisionArtifact` = frozen dataclasses + round-trip wire helpers, `BriefBucket{recap=gmail, conflict, prep=calendar events}`, render = canonical wire forms ONLY (scoring keys never cross — Q-50 boundary), degrade flags carried, stamped `item_kind=brief`; `BRIEF_DECISION='wombat.brief_decision'` added to artifacts.py (Q-47); `transitions=('brief_compose',)` freezes TK-100's stage name; the audit-CRITICAL proof spelled out (real Gate + 2-item pending set: identical `list()`, exact `cumulative_load()`, ZERO new journal records, spy ceiling never called). TK-99 amended: budget riders, estimate S→M, files 2→3 (+artifacts.py). validate PASS (140 nodes/120 gov); gate PASS; append-guard checkWrite GREEN vs HEAD. **TK-99 BUILD-READY.** | architect | Q-75, TK-99, TK-98/100/96, TK-74/27, Q-41/47/50, DEC-26, contract v0.93 |
| 2026-07-03 | **TK-99 recorded DONE (COMMIT, v0.94 — build+verification record, within authority) — the brief cluster's second stage is REAL; one verifier concern HOMED → Q-76.** Built exactly per the Q-75 rulings + independently adversarially verified PASS_WITH_CONCERNS (nothing blocking; builder correctly did not touch the contract — Q-69 rule holding). NEW `stages/brief_force_flush_stage.py` (reads BriefPayload via `ctx.last_output('brief_gather')`; DetectConflicts folded in per ruling (a) — runs the pure TK-74 kernel itself, no graph node; three-family adaptation all stamped ItemKind.BRIEF w/ artifact-local ids; ONE injected NARROW select_items callable + tz — never the whole Gate; Transition(brief_compose) + `wombat.brief_decision` Artifact; never touches ctx.journal) + NEW `domain/brief_decision_artifact.py` (frozen+slots buckets {recap,conflict,prep}, exact wire round-trip, degrade flags carried, NO scoring keys — Q-50). **Verification strong form — the audit-CRITICAL invariant PROVEN:** real Gate over InMemoryPendingJournal-backed PendingSet w/ 2 unrelated items → after run() `list()` bit-identical (non-tautological — fresh list each call), `cumulative_load()` equal, ZERO new journal records, spy ceiling never called; select_items re-confirmed non-mutating; ctor cannot reach any pending-mutation path. Also: scoring keys never cross (recursive scan), seconds_to_event replay-stable, buckets disjoint, unworthy items absent, FrozenInstanceError nested+top, no-journal spy load-bearing; ruff clean, mypy zero new, 519/37 (13 new), scope exact. Contract edits: TK-99 status→done; **NEW Q-76 HOMED-to-frontier (the Q-56 parked-pointer pattern — resolved as 'rule at TK-100's briefing', NOT on the merits):** conflict_to_payload drops the overlap start/end minutes (DAY-level only) — compose can name WHICH events conflict on WHAT DAY but not the time window unless the same events independently land in prep; TK-100's briefing rules (a) day-level documented / (b) cross-ref prep by event_id / (c) amend TK-74 (reopens a done ticket — needs recorded justification); changelog v0.94. **41/83 done. NEXT: TK-100 (BriefComposeStage, EP-20, deps EP-5 + TK-99) — briefing must settle: the EP-5 mouth seam via `bootstrap.build_compose_stage` behind the BudgetGuard (the Q-69 budgets-live lesson — never hand-rolled), the fixed terse-brief template, the S8 template fallback when the mouth is unavailable, the CON-1 sealed-artifact-contents-ONLY prompt boundary, and the Q-76 ruling.** validate PASS (140 nodes/121 gov); gate PASS; append-guard checkWrite GREEN vs HEAD (zero violations). | architect | TK-99, Q-75, Q-76, TK-100, TK-74, Q-50/56/69, contract v0.94 |
| 2026-07-03 | **TK-100 briefed (Q-77 rulings LOCKED, v0.95 — /plan-ticket pass, within authority; Q-76 RULED ON THE MERITS) — the brief cluster reaches the mouth; TK-100 BUILD-READY.** All seams verified as-built (compose.py, bootstrap.py, brief_decision_artifact.py — NOTE: the sealed artifact is BriefBucket{recap GmailBriefItem / conflict wire-dicts / prep CalendarEvent}, canonical wire forms ONLY, no per-item scores, no generated_at — Q-50 already structural; conflict_to_payload day-level confirmed; FakeModel already records calls). **Q-76 = OPTION (b) enrich-from-sealed-prep:** conflict lines cross-ref the sealed prep bucket by event_id — both present → each event´s OWN sealed start–end local times rendered; either missing → honest day-level line; NO overlap-window computation (no TK-74 kernel re-derivation in a renderer), option (c) REJECTED (reopening done TK-74 unjustified — precise times already live in the artifact whenever both events were independently selected; CON-1 holds); residual rides DEF-1. **EP-5 narrowed** (last epic-dep): children = exactly TK-8+TK-9, both done → depends_on TK-8+TK-9+TK-99. **Mouth seam:** SIBLING stage (compose.py untouched); ctx.model.complete (S4) only; layer-1 per-drive BudgetGuard AUTOMATIC on the bootstrap Engine; layer 2 = ctor spend_ledger/ceiling (default None) wired LIVE by NEW bootstrap.build_brief_compose_stage mirroring build_compose_stage — SAME spend:tokens row, ONE daily token cap across drain+brief (DEC-21/Q-15); BINDING: TK-96/97 assemble via build_engine + the factory, never hand-rolled (Q-69 made structural; factory wiring test = TK-53 precedent); ctor also config (TK-8 AC3 blank-key check), tz ZoneInfo (TK-99 precedent), timeout 10.0s composition constant. **Template:** compose/brief_template.py (layout NORMALIZED — no templates/ pkg; Q-39-precedent correction): BRIEF_SYSTEM_INSTRUCTION + ONE pure render_brief_lines(artifact, *, tz)→str; prompt user-msg == S8 fallback == that string EXACTLY (cannot drift); sections = degrade lines / Conflicts / Prep / Recap, quiet line when empty; Q-50 user-facing-only (urgency_score/priority_band/matched_rules/event_class/ids NEVER rendered). **Degrade = compose.py catch-set parity** (CancelledError re-raised; broad Exception incl. BudgetExceededError → template; blank → template; ceiling/read-fail → template WITHOUT call; write-fail → output stands); run() never raises, ALWAYS Transition(brief_deliver) — transitions=(brief_deliver,) FREEZES TK-101´s stage name. **Output wire** BRIEF_TEXT=wombat.brief_text {text,degraded,tokens_spent} via new artifacts.py helpers (engine journals; TK-101 reads via helper); no domain type. Node amendments: deps EP-5→TK-8+TK-9; files 2→4 (+artifacts.py +bootstrap.py, template path corrected); estimate S→M; Q-77 riders in complexity_budget. Q-76 resolution gained the merits pointer. validate PASS (140 nodes/122 gov); gate PASS. **NEXT: build TK-100** → TK-101 (EP-30 over-pull rules at its briefing) → TK-97. | architect | Q-77, Q-76, TK-100, TK-8/9, TK-99/101, TK-96/97, Q-50/69, DEC-26, contract v0.95 |
| 2026-07-03 | **TK-100 recorded DONE (COMMIT, v0.96 — build+verification record, within authority) — the brief cluster reaches the mouth; the brief pathway is one stage from complete.** BriefComposeStage built exactly per the Q-77 rulings and INDEPENDENTLY adversarially verified (PASS, nothing blocking; builder did not touch the contract — the Q-69 process rule holding). Highlights: CON-1 proven by the verifier's own adversarial render (stuffed scoring keys/raw ids → zero matches in the prompt); single-source template (one render_brief_lines call = model message = S8 fallback, proven via the failure path); Q-76 option (b) delivered both directions; exactly-once mouth call; S8 catch-set parity with compose.py (diff + independent CancelledError probe); build_brief_compose_stage wires the budget LIVE on the shared spend:tokens row; tokens_spent honest (None-vs-0 divergence documented, deliberate). 540 passed / 37 skipped, ruff clean, zero new mypy; scope exact + the factory test correctly homed in tests/unit/test_runtime.py. Contract: TK-100 status→done, changelog v0.96, done count 42/83; Q-76 + Q-77 confirmed resolved on the record (no new governance). validate PASS (140 nodes/122 gov); gate PASS. **NEXT: brief TK-101** — must rule the EP-30 voice over-pull (last Q-56 parked item on the built path), the text sink seam, no-second-brief, and the terminal transition. | architect | TK-100, Q-77, Q-76, Q-69, Q-56, TK-101, CON-1, contract v0.96 |
| 2026-07-03 | **TK-101 briefed (COMMIT — Q-78, /plan-ticket pass, within authority; the DEC-6/DEC-7 escalation boundary explicitly checked and NOT hit).** **(1) EP-30 over-pull NARROWED — the LAST Q-56 parked item on the built path is discharged.** EP-30 is entirely unbuilt (TK-164/165 todo, no sinks/ pkg); deps [EP-30, TK-100]→[TK-100]. The dep becomes an INJECTED SPEAK-SINK SEAM: ctor `voice_enabled: bool` + `speak: Callable[[str], None] | None` (default None) — True+wired → speak(BriefText.text) exactly once verbatim (AC2); True+None → warning + text-only (AC3); False → silent; raising speak → warning, text delivery stands. Voice delivery STAYS in v1: TK-164 later binds its real TTS adapter into this seam at composition — DEC-7 untouched, NO Jim escalation needed. Remaining parked over-pulls: TK-77→EP-13, TK-96→EP-17/EP-30 (each at its own frontier). **(2) AC4 idempotency ruled against the AS-BUILT engine:** cog-worx replays only COMMITTED steps — in the write-then-crash window resume RE-ENTERS run() (engine doc: side effects not protected); ctx.last_output is None there; StageContext exposes run_id but NOT step_index (AC4 parenthetical corrected on the record). Mechanism = **run_id-keyed FILE-MARKER**: header embeds `run=<run_id>`; pre-append scan finds it on replay → skip append/echo/speak, return the same terminal Done with replay=true (re-journals, never re-writes; sound — the linear pathway visits brief_deliver once per run). **Convention HOLDS: the stage never touches ctx.journal** — AC1's journal recording = the returned Done artifact committed by S6 commit-before-advance; no sanctioned exception. **(3) Sink/config:** ctor sink_path+tz (DEC-21 zone header, never bare UTC/Q-15); WombatConfig OPTIONAL `WOMBAT_BRIEF_PATH` + `WOMBAT_VOICE_ENABLED` (not REQUIRED_ENV; OperatingParams rejected — deployment config); NEW `bootstrap.build_brief_deliver_stage` (blank path → ConfigurationError; TK-96/97 MUST assemble via it — the Q-69 lesson); sink write failure raises LOUD (marker-absent resume retries). **(4) Terminal shape frozen:** name `brief_deliver`, transitions=() (ends the brief pathway), Done → BRIEF_DELIVERED wire {delivered_at tz-local, voice_spoken, replay} via new artifacts.py helpers; input via brief_text_from_artifact_data; no model (CON-2); no StageToolPolicy (DEC-26). Node: files 1→4 (+artifacts.py +config.py +bootstrap.py); estimate S→M; Q-78 riders. validate PASS (140 nodes/123 gov); gate PASS. **TK-101 BUILD-READY → then TK-96 → TK-97 (timer last).** | architect | Q-78, Q-56, TK-101, TK-100, TK-164, TK-96/97, Q-69/47/15, DEC-7, DEC-21, DEC-26, contract v0.97 |
| 2026-07-03 | **TK-96 briefed (Q-79 rulings LOCKED, v0.99 — /plan-ticket pass, within authority) — the ASSEMBLY ticket; three stale-scope reconciliations recorded; TK-96 BUILD-READY.** **(1) FOUR stages, not five:** the committed graph is brief_gather → brief_force_flush → brief_compose → brief_deliver (terminal); DetectConflicts was folded in at Q-75 — TK-96 complexity_budget amended, AC meaning stands (conflict note = force-flush-derived conflicts per Q-76/Q-77 option b). **(2) files_in_scope normalized to the pure assembly surface** — the three DONE stage files REMOVED (TK-96 must NOT re-edit built stages); scope = NEW pathways/brief_pathway.py (BRIEF_PATHWAY_ID "wombat.brief" + build_brief_pathway graph assembler on the drain_pathway.py idiom + PUBLIC brief_trigger_artifact seed — the runtime.py drain-tick precedent; gather verified to ignore the seed) + sources/bootstrap.py (behavior-equivalent _maybe_build_* poller extraction + NEW build_brief_fetches; unwired source = RAISING placeholder → honest degraded brief on a Google-less boot, the Q-61/Q-67 posture) + bootstrap.py (assemble_runtime registers wombat.brief via the MANDATORY build_brief_compose_stage/build_brief_deliver_stage factories + the SAME composed gate.select_items; CONDITIONAL registration — blank WOMBAT_BRIEF_PATH → loud skip, existing boots unbroken; additive RuntimeBundle.brief_pathway_id = the TK-97 entrypoint). **(3) The LAST TK-96 Q-56 over-pull DISCHARGED:** all six epic deps narrowed to 13 DONE tickets (TK-98/99/100/101 + TK-27 + TK-72/TK-16 + TK-74 + TK-75/76 + TK-8/9 + TK-53); EP-30 DROPPED (voice = the Q-78 injected speak seam). **Q-56 residue is now exactly ONE: TK-77 → EP-13** (rules at its own frontier). Tests ruled NO-DSN (in-memory pathway; select_items pending-preserving per TK-99): real-Engine e2e on the register_factory fake-model seam (the test_drain_pathway_e2e.py precedent) proves AC1 (exactly one [run=…] header + event + email + conflict variant) and AC3 (raising mouth → delivered text == render_brief_lines EXACTLY, the Q-77 template==fallback guarantee); AC2 CON-1 via manual-chain BRIEF_DECISION snapshot identity. Estimate stays M. NEXT: build TK-96 → TK-97 (timer, LAST). |
| 2026-07-03 | **TK-96 recorded DONE (44/83) (COMMIT, v1.00 — build+verification record, within authority) — THE WALKING-SKELETON BRIEF PATHWAY IS ASSEMBLED; the FEAT-7 morning-brief chain fires end-to-end.** Built exactly per the Q-79 rulings + independently adversarially verified (PASS, nothing blocking; builder did not touch the contract). `wombat.brief` registered in assemble_runtime off the SAME composed gate as drain (select_items=gate.select_items — ONE gate, ONE pending set, ASMP-2 held; verifier proved no second Gate constructed); compose/deliver via the mandatory bootstrap factories (shared spend:tokens row + loud sink LIVE); brief_pathway.py mirrors the drain pathway (no bootstrap import, no cycle) with PUBLIC brief_trigger_artifact; build_brief_fetches degrades unwired sources to *_unavailable (honest degraded brief on a Google-less boot — proven e2e); conditional registration on WOMBAT_BRIEF_PATH (blank → loud skip, drain unaffected); additive RuntimeBundle.brief_pathway_id = the TK-97 entrypoint. AC1 REAL: a real cog-worx Engine walked all four stages via the registered graph, exactly one brief per invocation. 563/37, ruff clean, zero new mypy, scope exact, no stage-file edits. Q-56 residue now EXACTLY TK-77→EP-13. **TK-97 (durable once-daily timer) is the LAST ticket of the arc.** | architect | TK-96, Q-79, Q-56, ASMP-2, contract v1.00 |
| 2026-07-03 | **TK-97 recorded DONE (45/83) (COMMIT, v1.03 — build+verification record, within authority) — FEAT-7, THE MORNING-BRIEF WEDGE, IS CODE-COMPLETE.** The final ingestion-arc ticket built under Q-80-as-amended (v1.02 terminal-stub) + independently adversarially verified (PASS — nothing blocking): fence-READ/mark-after-COMPLETED proven on real pg by the verifier's own script; AC4 BOTH crash windows genuine (day-keyed run_id `wombat-brief-<date>` → TK-101 marker suppresses duplicate delivery — structural); AC2 sleep-jump + cold-boot miss-catch; full DST battery; honest exactly-once-DELIVERY (no over-claim). 590/38 no-DSN, 639/4 docker pg, ruff clean, zero new mypy, scope exact. One non-blocking note (naive-datetime not runtime-guarded — codebase aware-clock convention) recorded, no action. All EP-19 (TK-96/97/98/99) + EP-20 (TK-100/101) children done; epic/feature statuses left todo per the EP-15/EP-16 precedent (FEAT-7 flips after live verification). **Remaining gate to the first LIVE brief = Jim's OAuth bring-up + the two armed live smokes (gcal + gmail) — operator step, not a build blocker.** Q-56 residue: exactly TK-77→EP-13. | architect | TK-97, Q-80, FEAT-7, EP-19, EP-20, TK-101, contract v1.03 |
| 2026-07-08 | **2026-07-06 CODE REVIEW ROUTED (COMMIT, v1.04 — within authority, nothing escalated, all 17 findings homed).** The whole-codebase review (planning/code-review-2026-07-06.md, CR-1..CR-17) dispositioned. **HEADLINE — CR-1 (S1): the pg pending journal is written but NEVER REPLAYED at boot** — bootstrap builds PendingSet cold (bootstrap.py ~332); rebuild_from_journal has zero production callers; held-then-acked items are silently LOST across restart. Ownership resolved at **Q-81**: neither Q-70/TK-29 nor TK-53 owned replay-at-boot (TK-53's AC = isinstance only) — the v0.85/v0.87 "RISK-5 durability honestly claimable" rider was OVER-CLAIMED until **NEW TK-166 (P1, EP-8)** lands (boot replay + kill-between-queue-ack-and-restart AC). **CR-2 (S1) ruled via NEW DEC-27:** wire-derived free text in the brief (subject/sender AND calendar titles) = UNTRUSTED DISPLAY DATA — sanitize+delimit at the ONE render_brief_lines boundary (option (a); sealed-decision-only rejected — guts the recap); body taint latch untouched; → **TK-167 (P1, EP-20)** with an adversarial AC. **NEW FIX TICKETS (9 total, 83→92):** TK-168 (CR-3 stored-token scope guard, gmail+gcal), TK-169 (CR-4 DayRollover _last_seen after increment), TK-170 (CR-5 gcal missing-items + truncation warning), TK-171 (CR-6+9 degraded-recovers + strict-> predicate alignment), **TK-174 (CR-8 urgency-aware eviction — ruled at Q-82 as a deliberate TK-25 semantics change: min across held∪incoming, drop-incoming = zero journal records, tie→incumbent)**, TK-172 (CR-10 batch-size-1 guard + CR-12 router fallback edge — CR-12 RE-VERIFIED by me in source: latent, fenced by the sole GENERIC→compose bootstrap map), TK-173 (CR-13..16 hygiene batch, prototype: README refresh, tests/ mypy debt, close all 3 assembly DailyLedgers, duplicate-at-capacity → ALREADY_QUEUED). **ACCEPTED/DEFERRED:** RISK-8 (CR-11 engine memo — accepted under ASMP-2, added to its revisit inventory), RISK-9 (CR-2 rider triage keyword gameability — bounded to recap prominence, revisit rides DEF-1), DEF-5 (CR-7 sync enqueue — trigger: 3rd poller / sub-60s cadence), DEF-6 (CR-14 growth remainder + CR-17 prose trim — post-v1; queue-duplicate edge NOT deferred, TK-173 owns it). **SEQUENTIAL BUILD ORDER: TK-169 → TK-171 → TK-172 → TK-166 (P1 lands this run) → TK-174 → TK-168 → TK-170 → TK-167 → TK-173.** validate PASS (149 nodes/132 gov); gate PASS. | architect | CR-1..17, Q-81, Q-82, DEC-27, RISK-8/9, DEF-5/6, TK-166..174, RISK-5, ASMP-2, contract v1.04 |
| 2026-07-08 | **TK-169 recorded DONE (46/92) (COMMIT, v1.05 — build+verification record, within authority) — the FIRST v1.04 review-fix ticket lands (CR-4).** DayRollover.check() now stamps `_last_seen` only AFTER `DailyLedger.increment(rollover:gate)` returns — a transient pg error on the first check of a new wombat-day still propagates fail-loud (unchanged) but leaves `_last_seen` untouched, so the next same-day check is a GENUINE retry and the day's LedgerReset can no longer be permanently swallowed (the DEC-21 boundary intact). Exactly the briefed one-line reorder + comment; scope = files_in_scope exactly (gate/decay.py + tests/gate/test_decay_and_reset.py). AC1 runnable via _FlakyDailyLedger (first-call raise → same-day retry → exactly-once LedgerReset via value==1 → quiet third call); AC2 the TK-28 exactly-once battery untouched — 591 passed/38 skipped, ruff clean, zero new mypy. No retry/backoff machinery (the next natural check IS the retry). validate PASS; gate PASS. NEXT per the v1.04 order: TK-171. | architect | TK-169, CR-4, contract v1.05 |
| 2026-07-08 | **TK-171 recorded DONE (47/92) (COMMIT, v1.06 — build+verification record, within authority) — the second v1.04 review-fix ticket lands (CR-6 + CR-9).** (1) `SourceRegistry._poll_loop` success branch now `discard`s the source from `_degraded` — `degraded_sources` tracks only the MOST RECENT poll() outcome (docstring true again); a single blip no longer marks a source degraded forever. AC1 runnable (flaky source in-after-fail, out-after-success; no timing race — discard lands in the same event-loop tick as the poll-count bump). (2) The boundary predicate is now STRICT `urgency > threshold` EVERYWHERE: production `trigger.is_surfacing_worthy` was already strict and is UNCHANGED; `trigger_sim.evaluate_day` + the TK-21 `stub_evaluate` flipped `>=` → `>`, the `wombat_params.yaml` comment corrected (VALUE 0.75 untouched — DEF-1 owns calibration) — exact-equal urgency HOLDS in all three, so the sim that justified the shipped thresholds agrees with production at the boundary. Stub NOT retired (non_goal held); pipeline/decision logic untouched. 593/38, ruff clean, zero new mypy (the 3-file baseline stands); diff = 6 files, a strict subset of files_in_scope (test_gate.py needed no edit). validate PASS; gate PASS. NEXT per the v1.04 order: TK-172. | architect | TK-171, CR-6, CR-9, DEF-1, contract v1.06 |
| 2026-07-08 | **TK-172 recorded DONE (48/92) (COMMIT, v1.07 — build+verification record, within authority) — the third v1.04 review-fix ticket lands (CR-10 + CR-12).** Both latent fences are now OWNED invariants, zero behavior change at current composition. (1) `_guard_drain_batch_size` in `bootstrap.py`, called at the exact point `_DRAIN_BATCH_SIZE` is consumed to construct `DrainQueueStage` (guard and constant structurally cannot diverge) — any batch size ≠ 1 raises a loud ValueError NAMING the mid-batch-surface/whole-batch-ack coupling (pipeline returns mid-iteration on SURFACE_IMMEDIATE while gate_stage pairs the one decision with EVERY drained item and review_or_speak acks wholesale); batch 1 = no-op, assembly proven unchanged. (2) `ComposeDispatchRouter.transitions` now derived from the map values UNION the compose fallback constant — `Transition(to=compose)` on an unknown kind is ALWAYS a declared graph edge even under a future compose-omitting map (proven directly; today's map contains compose, so fence not behavior change). 597/38, ruff clean, zero new mypy (baseline stands); diff = files_in_scope exactly; no gate/pipeline.py change. validate PASS; gate PASS. NEXT per the v1.04 order: TK-166 (the P1 — boot replay of the pg pending journal). | architect | TK-172, CR-10, CR-12, contract v1.07 |
| 2026-07-08 | **TK-174 recorded DONE (49/92) (COMMIT, v1.08 — build+verification record, within authority) — the fourth v1.04 review-fix ticket lands (CR-8, the Q-82 ruling made real). Sequencing note recorded honestly: landed AHEAD of TK-166 (the v1.04 order put the P1 next) — harmless, sole dep TK-25 done, files disjoint; TK-166 stays NEXT.** The capacity path in `PendingSet.add()` now evicts the minimum-urgency item across held∪incoming: exactly one new branch — `if item.urgency <= evicted.urgency` (tie → incumbent, deterministic under replay) the INCOMING item is dropped with ZERO journal records and `_items`/`_added_at` untouched, returning `CapacityEviction` naming the incoming item (observability preserved); else the pre-existing Remove-before-Add journaled eviction runs byte-identical (Q-45 kill-window ordering untouched). All four ACs independently re-derived (verifier's own repro + suite): less-urgent-newcomer dropped w/ zero records; strictly-less-urgent-held evicted as-built; tie → incumbent; drop-incoming then `rebuild_from_journal` → identical items + cumulative_load, ALL TK-25 kill/eviction-atomicity regressions green (25 passed in the gate test file). 601/38, ruff clean, zero new mypy (13-error tests/ baseline stands, src strict-clean); diff = files_in_scope EXACTLY (pending_set.py + its test). The gate thesis now holds at capacity — the pending set provably carries the most load-bearing backlog into the flush/brief. NEXT: TK-166 (P1 — boot replay of the pg pending journal). | architect | TK-174, CR-8, Q-82, Q-45, TK-25, TK-166, contract v1.08 |
| 2026-07-08 | **TK-168 recorded DONE (50/92) (COMMIT, v1.09 — build+verification record, within authority) — the fifth v1.04 review-fix ticket lands (CR-3). Sequencing note recorded honestly: landed AHEAD of TK-166 (second such deviation after TK-174) — harmless, deps TK-71/TK-75 done, files disjoint; TK-166 stays NEXT.** The OAuth readonly-scope guard now bites on the STORED-token path in BOTH gmail and gcal `get_credentials()`: parse the vaulted token JSON into `stored_info`, assert `stored_info.get("scopes")` readonly BEFORE `Credentials.from_authorized_user_info(...)` — killing the tautological check (`from_authorized_user_info` overwrites `creds.scopes` with the passed constant, so the old post-construction assert compared the readonly constant against itself; an over-scoped vaulted token — older consent, manual edit, future widening without re-consent — sailed through). Post-refresh assert retained verbatim (gmail line 160, gcal line 145); fresh-consent path untouched. AC1/AC2 runnable both integrations (gmail.modify / calendar write scope → ScopeViolationError before any credential use; refresh transport + InstalledAppFlow never invoked, vault untouched); AC3 full auth suites 18 passed / 2 env-gated live-smoke skips. Adversarial revert probe: fix reverted → over-scoped-token tests FAIL (bypass reproduced) — the tests are load-bearing. 603/38, ruff clean, zero new mypy (13-error tests/ baseline stands, all four edited files clean); diff = files_in_scope EXACTLY. The DEC-3/DEC-9 read-only posture now enforced against what the token ACTUALLY granted at every load. NEXT: TK-166 (P1 — boot replay of the pg pending journal). | architect | TK-168, CR-3, TK-71, TK-75, TK-166, contract v1.09 |
| 2026-07-08 | **TK-170 recorded DONE (51/92) (COMMIT, v1.10 — build+verification record, within authority) — the sixth v1.04 review-fix ticket lands (CR-5). Sequencing note recorded honestly: landed AHEAD of TK-166 (third such deviation after TK-174/TK-168) — harmless, deps TK-72/TK-98 done, files disjoint; TK-166 stays NEXT.** Both briefed fixes landed in `CalendarPoller.fetch_window` (the RAISING seam BriefGatherStage consumes): (1) `response.json()["items"]` → `body.get("items") or []` — Google omits `items` on an empty window (gmail-poller parity), so an EMPTY calendar now renders as an empty calendar (`calendar_unavailable=False`), never the wrong Calendar-is-unavailable line (DEC-16 brief honesty; the old KeyError fell into gather’s broad except and flagged unavailable); (2) loud WARNING naming the gcal source when `nextPageToken` is present — full pagination stays the non_goal, loud truncation = the v1 floor for the 48h window. Verified: AC1 at both poller and gather level (gather test proves fetch `[]` → `calendar_unavailable=False`), AC2 warning proven, AC3 `poll()` never-raises contract unchanged; 605/38, ruff clean, zero new mypy. **`files_in_scope` normalized to the as-built truth** (+`tests/stages/test_brief_gather_stage.py`, the gather-level AC1 home) — scope-normalization authority. Contract: TK-170 → done, files_in_scope +1, changelog v1.10 |
| 2026-07-08 | **TK-166 builder-flagged design fork RULED (COMMIT, Q-83/v1.11 — within authority, ticket-level how; CR-1/Q-81 replay-at-BOOT demand unchanged; the AC2 crash-recovery proof intact).** The builder correctly stopped and reverted: the briefed constructor swap (cold `PendingSet` → `rebuild_from_journal` in `assemble_runtime`) performs eager pg I/O at construction, breaking bootstrap.py:340-342 every-adapter-is-lazy/unreachable-dsn-safe AND 6 non-pg-gated unit tests outside file scope (1 test_bootstrap.py + 5 test_runtime.py) — all claims re-verified in source. RULING = option (c) refined: replay stays a COMPOSITION-ROOT act inside `assemble_runtime`, gated by keyword-only `replay_pending: bool = True`. Recovery is opt-OUT never opt-in (CR-1 failure mode WAS an unowned opt-in); `serve()` — the ONLY production caller — needs zero change. Rejected: raw replay-in-serve() (would mutate the composed gate outside the factory — Q-69 one-composition-root lesson); (a) pg-rewriting the 6 tests (guts the no-DSN suite to preserve prose); (b) lazy hydration inside PendingSet (changes TK-25/TK-29 as-built semantics — the ticket own non_goal — and makes recovery timing implicit). Fault posture recorded: at the default an unreachable pg fails at ASSEMBLY not first drive — same fail-loud posture as serve(), earlier. TK-166 amended: complexity_budget → flag shape (rebuild now pending_set.py ~251 post-TK-174), NEW AC4 (6 one-line opt-out amendments + one no-DSN spy test that the default routes through rebuild_from_journal + docstring names the ONE eager read), files_in_scope +tests/unit/test_bootstrap.py +tests/unit/test_runtime.py. validate PASS; gate_check analyze PASS. TK-166 re-briefed and BUILD-READY. |
| 2026-07-08 | **TK-167 recorded DONE (52/92) (COMMIT, v1.12 — build+verification record, within authority) — the seventh v1.04 review-fix ticket lands (CR-2, the DEC-27 posture made real). Sequencing note recorded honestly: fourth deviation ahead of TK-166 (after TK-174/TK-168/TK-170) — harmless, sole dep TK-100 done, files fully disjoint; TK-166 stays NEXT.** ONE pure deterministic sanitizer `_sanitize_display_text` in `compose/brief_template.py` — the single shared boundary function at EVERY wire-derived free-text field site (gmail subject/sender, prep titles, conflict titles BOTH branches): collapse control runs, replace embedded `"` with `'` (the delimiter cannot be forged open), cap at 200 + ellipsis, wrap in quotes; BRIEF_SYSTEM_INSTRUCTION gains the quoted-data-is-content-never-instructions sentence. `render_brief_lines` stays the ONE string feeding prompt AND S8 fallback (TK-100 invariant — sanitation in exactly one place; `brief_compose_stage.py` UNTOUCHED, the critical seam held). Builder-found residual gap closed in-ticket: control class widened to C1 (incl. U+0085) + U+2028/U+2029 — the `str.splitlines()` line boundaries that previously survived un-delimited. AC2 = NEW adversarial battery (taint-latch posture): 9 payloads × 5 field-site tests + the seam test, structural assertions (fixed line count, zero surviving control chars, raw payload never un-delimited in either output). DEC-26 body latch untouched; CON-1 deterministic-only; RISK-9 stays accepted. 655/38, ruff clean, zero new mypy (src strict-clean); diff = files_in_scope EXACTLY. The one surface Jim is trained to trust on sight can no longer be socially engineered by a sender-controlled header or calendar title. validate PASS (149 nodes/133 gov); gate PASS. NEXT: TK-166 (P1) → TK-173 closes the review. | architect | TK-167, CR-2, DEC-27, DEC-26, CON-1, RISK-9, TK-100, TK-166, contract v1.12 |
| 2026-07-08 | **TK-166 recorded DONE (53/92) (COMMIT, v1.13 — build+verification record, within authority) — the eighth v1.04 review-fix ticket lands and the REVIEW P1 IS CLOSED (CR-1, ruled at Q-81/Q-83): boot replay of the pg pending journal — held items now SURVIVE RESTART; RISK-5's honest-durability claim RESTORED.** The Q-83 flag shape landed exactly: `assemble_runtime` gains keyword-only `replay_pending: bool = True` — the DEFAULT routes the gate's pending set through `PendingSet.rebuild_from_journal` (ONE eager pg read at the composition root; recovery opt-OUT never opt-in; `serve()`, the only production caller, unchanged); `replay_pending=False` keeps the connection-free cold constructor for tests/tooling; the bootstrap docstring names this ONE exception to every-adapter-is-lazy. ZERO PendingSet/PgPendingJournal changes (TK-25/TK-29 as-built; non_goals held). INDEPENDENTLY VERIFIED PASS after one honest repair round: the verifier caught AC1/AC2 asserting on an independent `rebuild_from_journal` re-read — a tautology invariant to the wiring (bundle.pending_journal is built unconditionally); rewritten to a `_spy_on_gate_pending_set` monkeypatch asserting on THE gate's OWN pending set as assembled — load-bearing. AC2 crash-recovery genuine on pg (held+journaled+queue-acked item, process dropped without teardown → second `assemble_runtime` over the same DSN carries it into the flush/select path). AC4: the 6 fake-DSN unit tests opted out one line each + the no-DSN structural spy test. Scope = files_in_scope exactly (bootstrap.py + 3 test files); ruff clean; zero new mypy (13-error tests/ baseline stands); 657/39 no-DSN, 708/4 on throwaway docker pg (only live Google smokes skipped). The CST-1 nothing-lost posture now holds across restart — written AND replayed. validate PASS (149/133); gate PASS. NEXT per the v1.04 order: TK-173 (hygiene batch, LAST) closes out the 2026-07-06 review. | architect | TK-166, CR-1, Q-81, Q-83, RISK-5, CST-1, TK-25/29, Q-69, contract v1.13 |
| 2026-07-08 | **TK-173 DONE (54/92) — the review-fix arc is COMPLETE (contract v1.14).** The hygiene batch lands built + independently verified (PASS; Q-69 rule holding — builder never touched the contract): (1) CR-13 README rewritten to the as-built standing runtime (serve entrypoint, env posture incl. WOMBAT_PG_DSN required-loud, three pathways, LLM-is-a-mouth thesis, pg/live test gating) — stale bootstrap-stub text gone; (2) CR-14 mypy strict now exits 0 over all 167 files, src AND tests — the 13-error tests/ baseline carried since v1.04 is cleared; (3) CR-15 assemble_runtime constructs ONE DailyLedger shared into build_compose_stage + build_brief_compose_stage via a new optional daily_ledger seam, so the EXISTING runtime.py:100 teardown closes every assembly-built ledger (share, not a close-registry; runtime.py rightly untouched); (4) CR-16 queue.enqueue checks idempotent-duplicate BEFORE the capacity refusal — duplicate at max_size returns ALREADY_QUEUED, new key still raises QueueFullError. Verifier own run: 710 passed / 4 skipped (only live smokes; pg-gated RAN on throwaway postgres:16), ruff clean. Every routed CR fix ticket is done; the rest of CR-1..17 stands accepted/deferred (DEF-6). Plan returns to the main build order. | architect | TK-173, CR-13..16, DEF-6, contract v1.14 |
| 2026-07-09 | **TK-43 recorded DONE (55/92) (COMMIT, v1.16 — build+verification record, within authority) — the FIRST v1.15-batch ticket lands and EP-11 opens: the closed, motive-free claim vocabulary (the schema wall CON-6/NG-1 demanded, structural not runtime-string-checked).** BUILT + INDEPENDENTLY VERIFIED (PASS — verifier re-ran the full quality bar and every AC runnable check plus adversarial probes; builder never touched the contract, the Q-69 rule holding). `src/wombat/user_model/claims.py`: `ClaimPredicate(StrEnum)` = EXACTLY 6 members (BEHAVIOR_OBSERVED, OUTCOME_PENDING, OUTCOME_LOAD_BEARING, OUTCOME_REGRETTED, OUTCOME_IGNORED, RATING_PARAMS — value-matching the as-built `rating/params.py` wire constant WITHOUT importing it, preserving TK-41's dependency direction; rationale in the module docstring) + frozen/slots `Claim` (predicate typed `ClaimPredicate` not str; `__post_init__` TypeError on ANY non-enum predicate including a raw string equal to a valid value — the runtime wall behind the mypy wall; naive `observed_at` → ValueError per the aware-clock convention). Pure Python, no I/O, no cog-worx import. AC2 proven adversarially: no MOTIVE_* member (count=6 by enum iteration); raw strings + None all TypeError; hand-rolled string = mypy call-site error. AC3: no name/docstring/field implies intent/reason/motive. QUALITY (verifier's own run): 665/39 (skips all pg-gated), ruff clean, mypy strict Success 169 files src+tests (the post-TK-173 zero-error bar HELD); scope = exactly the two new files, no creep. TK-44's TK-43 dep satisfied. NEXT per v1.15: TK-77. | architect | TK-43, EP-11, CON-6, NG-1, TK-41, TK-44, Q-69, contract v1.16 |
| 2026-07-09 | **TK-77 recorded DONE (56/92) (COMMIT, v1.17 — build+verification record, within authority) — the SECOND v1.15-batch ticket lands (EP-17): GmailTaskExtractor, deterministic off-path task extraction from message bodies, built EXACTLY to the Q-84 as-ruled shape.** BUILT + INDEPENDENTLY VERIFIED (PASS — verifier re-ran the full quality bar + every AC runnable check; builder never touched the contract, the Q-69 rule holding). `task_extractor.py`: frozen+slots `TaskItem` (ItemRef + description + deadline_signal + source_message_id), JSON-native wire helpers, pure `extract_tasks(item)` over a module-constant closed rule table + separate deterministic deadline scan — no LLM, no I/O, no clock. Identity rider honored: TK-12 `derive_task_natural_id(message_id, task-<ordinal>)`, replay-stable ordinals → byte-identical re-extraction ids (dedup proven); ItemKind stays GENERIC; enqueueing stays with the future dream-pathway consumers. AC3 as-ruled proven STRUCTURALLY: real Registry+ToolGate+dispatch_one — the sanctioned READ_EMAIL_BODY_CAPABILITY read latches taint False→True (content-independent, benign AND injection-shaped bodies), extract_tasks adds NO second body access path; DEC-26 held, no StageToolPolicy constructed. AC4 as-ruled: the TK-76-pattern AST no-import guard (zero openai/httpx/requests/cogworx.model/wombat.compose imports) — off-path enforced at build time. Q-65 guard: task_extractor.py = the FIFTH `_SANCTIONED_PATHS` entry (+5 lines in the guard file, exactly as sanctioned). QUALITY (verifier's own run): 677/39 (skips pg-DSN-gated only), ruff clean, mypy strict Success 171 files (zero-error bar HELD); diff = files_in_scope EXACTLY (2 new + guard +5) — no triage.py, no ItemKind, no wiring, no creep. validate PASS (149 nodes/135 gov); gate PASS. NEXT per v1.15: TK-44 (the user-model write seam). | architect | TK-77, Q-84, Q-65, Q-69, DEC-26, TK-12, TK-76, TK-148, DEF-1, TK-44, contract v1.17 |
| 2026-07-09 | **TK-44 recorded DONE (57/92) (COMMIT, v1.18 — build+verification record, within authority) — the THIRD v1.15-batch ticket lands (EP-11): ObservationWriter, the single behavior/outcome write seam into the cog-worx user_model scope.** BUILT + INDEPENDENTLY VERIFIED (PASS — verifier re-ran the full quality bar + every AC check and re-inspected the live cog-worx seam signatures; builder never touched the contract, the Q-69 rule holding). ScopedKG minted once at construction via `user_model(entity_kg, scope_registry, user_id, owner='wombat.observation_writer')` — the S7 single-writer token under ONE fixed owner, so a second writer on the same scope fails structurally (TK-42's reader stays tokenless — no reader/writer conflict); `SourceDeclaration(kind='system', ref=owner)` so every minted claim traces to the module; EntityKG + ScopeRegistry INJECTED per the non_goal. `record(claim)`: isinstance-ClaimPredicate re-check BEFORE any I/O (TypeError — the runtime backstop behind the TK-43 wall, catching duck-typed stand-ins), obj = ONE JSON envelope `{value, event_id}` (claim.value carried opaque — assert_fact takes one obj string); store errors logged + RE-RAISED, never dropped (AC3 — the journal can catch it). `record_rating_params`: writes EXACTLY through rating/params.py's RATING_CLAIM_PREDICATE + to_claim_payload — the Q-41 v0.30 STANDING OBLIGATION honored, read/write structurally cannot drift. Quality (verifier's own run): 682/39 (skips pg-DSN-gated only), ruff clean, mypy strict Success 173 files (the zero-error bar HELD); diff = the two new files exactly (files_in_scope normalized +test file, scope-normalization authority). One cosmetic architect fix at close-out: module docstring EP-10 → EP-11 (matches the contract parent). TK-45's TK-44 dep satisfied (the outcome chain still waits on TK-147/TK-161 per v1.15). validate PASS (149 nodes/135 gov); gate PASS. NEXT per v1.15: TK-46 (dream-pathway registration). | architect | TK-44, EP-11, Q-41, Q-69, TK-42, TK-43, TK-45, TK-46, contract v1.18 |
| 2026-07-09 | **TK-46 recorded DONE (58/92) (COMMIT, v1.19 — build+verification record, within authority) — the FOURTH v1.15-batch ticket lands and EP-13 OPENS: the DreamPathway off-path scaffold, built EXACTLY to the Q-85 as-ruled shape.** BUILT + INDEPENDENTLY VERIFIED (PASS — verifier independently re-derived and re-executed every check, brought up its own throwaway postgres:16 for the DSN-gated suite; builder never touched the contract, the Q-69 rule holding). `pathways/dream_pathway.py` mirrors brief_pathway.py exactly: `DREAM_PATHWAY_ID = "wombat.dream"`, terminal no-op `DreamScaffoldStage` (transitions=(), Done with a `wombat.dream_report` Artifact, NO model call, NEVER touches ctx.journal), `build_dream_pathway(stage=None)` injectable-stage seam (serves AC2's error injection), `dream_trigger_artifact(now)` mirroring the brief seed. `assemble_runtime` registers `wombat.dream` UNCONDITIONALLY per Q-85 (no env conditional — the WOMBAT_BRIEF_PATH-gated brief pair untouched) + additive `RuntimeBundle.dream_pathway_id` (the TK-52 entrypoint); bootstrap diff strictly additive. AC1(a) via the TK-166 connection-free assembly pattern; AC1(b)+AC2 in the new DSN-gated e2e: a triggered dream run journals as a NEW cog-worx run with the drain heartbeat uninterrupted, and an erroring injected dream stage leaves the hot path unaffected — off-path isolation proven both ways. **Scope normalized: `tests/unit/test_runtime.py` added to files_in_scope** — two pre-existing hand-rolled RuntimeBundle(...) constructions gain the required new field (the brief_schedule_pathway_id precedent, mechanical not creep). Quality (verifier's own run): ruff clean; mypy strict Success 175 files (zero-error bar HELD); 683/40 no-DSN (skips all pg-gated); dream-e2e + drain-e2e + serve_boot 10/10 on throwaway pg. Non-goals held: no tuner, no reconciler/extractor (TK-47), no recurrence/fence (TK-52), Sweeper stays TK-53's; no serve()/runtime.py change. validate PASS (149 nodes/135 gov); gate PASS. NEXT per v1.15: TK-52 (nightly trigger + fence — the batch's LAST ticket). | architect | TK-46, EP-13, Q-85, Q-33, Q-69, TK-52, TK-47, TK-97, TK-166, contract v1.19 |
| 2026-07-09 | **TK-52 recorded DONE (59/92) (COMMIT, v1.20 — build+verification record, within authority) — the v1.15 batch COMPLETES: dream nightly recurring trigger + once-per-night idempotency fence, the TK-97 scheduler precedent VERBATIM per Q-85.** BUILT + INDEPENDENTLY VERIFIED (PASS — verifier re-ran the full quality bar and every AC's runnable check on its own throwaway postgres:16; builder did not touch the contract). Shape: NEW `src/wombat/pathways/dream_trigger.py` — DreamRunLedger (thin wrapper over the shared TK-152 DailyLedger, row `dream:run`), DreamTimerStage (Wait-self-parking: fence-read gates the fire, mark_ran only after RunStatus.COMPLETED, UNCONDITIONAL re-park regardless of outcome — recurrence holds even on a raising fire), DreamTimerTerminalStage + build_dream_schedule_pathway; `brief_schedule.is_due/next_fire_at` reused AS-IS (fire time already parameterized — no fork, DEC-21 boundary). bootstrap: `fire_dream` closure with night-keyed run_id `wombat-dream-<wombat_date>` (Engine.start_run idempotent per night), registered UNCONDITIONALLY per Q-85 via additive `RuntimeBundle.dream_schedule_pathway_id`. runtime serve(): a THIRD initial drive arms the dream timer at boot (crash-miss catch), mirroring the brief drive exactly. params v4→5: single fixed `nightly_dream_time` (non_goal cap held). ACs: `tests/integration/test_dream_schedule_e2e.py`, 6 DSN-gated tests all PASSED on real pg — AC1 re-arm on success AND raising fire (ledger unmarked); AC2 same-night double-drive fires once + engine run_id guard dedupes a direct double fire + crash-mid-fire restart → single run for the night; AC3 slept-over night fires exactly once on wake. SCOPE NORMALIZED (+`tests/unit/test_runtime.py`, +`tests/unit/test_wombat_params.py` — mechanically required by the new bundle field + params bump, TK-46 precedent). QUALITY (verifier's own run): ruff clean; mypy strict 177 files; 743 passed / 4 skipped (only the Google-live smokes) / 0 failed — an initial 58-error run was a foreign container squatting port 5434, environment artifact NOT a defect. validate PASS (149 nodes/135 gov); gate PASS (analyze). NEXT: batch complete — scope the next batch from the frontier (dream content TK-47/49 held on TK-150). | architect | TK-52, EP-13, Q-85, TK-97, TK-152, DEC-21, TK-46, TK-53, contract v1.20 |
| 2026-07-09 | **NEXT BUILD BATCH SELECTED + BRIEFED (COMMIT, v1.21 — batch selection + four pre-build rulings + one unowned-wire ticket, all within authority; nothing escalated).** Batch (≤5, dependency-ordered): **① TK-147 → ② TK-161 → ③ TK-51 → ④ TK-150 → ⑤ TK-50** — the outcome chain my v1.15 notes flagged, re-derived from the current graph (TK-147/TK-161 ready-now unblock TK-51; TK-51 unblocks TK-50; TK-150 ready-now un-holds TK-54→TK-47). Every cited seam re-verified in the as-built tree + installed cog-worx (trail schema read contract; poll-only InputSource/SourceRegistry; substrate.ResidencyCheck; WombatConfig; ClaimPredicate; OpenAICompatModel/httpx). **Q-89** (TK-147): BLOCKED rows render once (absorbing); sidecar JSON cursor resolves no-raw-IDs vs restart-dedup; transition lines self-identifying (indented, summary repeated); reader strictly via trail/schema.py constants; DSN-gated real-pg done-bar. **Q-86** (TK-161+TK-51): as-built poll-only contract KEPT — base.py gains concrete PushSource (push buffers, poll drains; ASR + feedback both ride it; registry untouched + one additive source_ids accessor); TK-51 affordance = pure token (live emission point does not exist yet); FeedbackSignal frozen wire {kind feedback, item_ref, response}; AC3 fold-proof split to TK-50 (same batch); **the drain-side landing of a feedback QueueItem was UNOWNED (would be gate-scored → pending → brief pollution) → NEW TK-175** (EP-12, P1, M; pre-scoring diversion → BEHAVIOR_OBSERVED via ObservationWriter + ack; boot registration; nightly inference→labeling insertion — the Q-61 precedent; 92→93 tickets). **Q-87** (TK-150): epic deps EP-1/EP-15/EP-17 narrowed to 8 done write-path owners — READY NOW; guard exports residency_check matching substrate.ResidencyCheck BY SIGNATURE + check_config called FIRST in serve(); as-built surfaces = psycopg/keyring/files; httpx-transport interceptor, canned responses, deepseek_base_url = the ONE unchecked egress (ASMP-1); injected resolver for the Q-25 battery; files += runtime.py. **Q-88** (TK-50): EP-13 dep → TK-46+TK-52 (built); PURE engine, injected typed inputs (CalendarSnapshotDelta/DraftFate/ItemResolution/FeedbackSignal), Outcome set defined here (TK-45 maps to ClaimPredicate later), feedback>inference>IGNORED precedence, Q-66 structural no-import guard both directions, NG-1 named binding constraint (whether, never why). **Deliberately NOT picked:** TK-45/TK-49/TK-54/TK-47/TK-175 (next batch — chain converges at TK-49), TK-151/TK-80/TK-78/79 (outbound track opens after the adaptation arc), TK-164/TK-111/TK-115 (P2), browser (P3), anything needing OAuth (operator step). Deps/files normalized on TK-50/TK-150/TK-161. validate PASS (150 nodes/139 gov); gate PASS (analyze). | architect | Q-86/87/88/89, TK-147/161/51/150/50, TK-175, Q-61/66/84/25, ASMP-1, NG-1, contract v1.21 |
| 2026-07-09 | **TK-150 recorded DONE (63/93) (COMMIT, v1.25 — build+verification record, within authority) — the FOURTH v1.21-batch ticket lands (EP-28): the same-host storage-residency guard, structurally enforced to the Q-25/Q-87 as-ruled shape; NG-7/CON-7/ASMP-1's storage boundary is now RUNNING CODE.** BUILT + INDEPENDENTLY VERIFIED (PASS — verifier re-ran the full quality bar AND every AC against its own throwaway postgres:16; builder never touched the contract, the process rule holding). NEW `safety/local_residency.py`: `RemoteStorageConfigError`; `make_residency_check(resolver=, local_addrs=)` (injected stdlib-socket-default resolver, Q-87 ruling 3) → `residency_check` = the ONE production predicate, assignable to the as-built `substrate.ResidencyCheck` — the TK-14 dependency-inversion seam finally has its real predicate, so **the deliberate TK-54/TK-47 hold-on-TK-150 is RELEASED**; `check_config(config)` applies it to `wombat_pg_dsn` ONLY, refusal names the key; `deepseek_base_url` never touched (the ASMP-1 egress exception held structurally). Residency = the Q-25 definition, not a localhost string match: unix-socket/bare-path DSNs inherently local; loopback + literal localhost pass without resolution; IP literals skip DNS; hostnames pass iff any resolved addr is loopback or a this-host interface — Docker-bridge-to-self ACCEPTED, LAN-other-host REFUSED. `serve()` runs `check_config` FIRST after `load_config`, before the DSN-required check (Q-87 ruling 4) — off-host DSN aborts boot before `assemble_runtime`. All 5 ACs verifier-re-executed: pg-gated recording-wrapper battery over queue enqueue / DailyLedger.increment / PgPendingJournal.append / ActionTrailWriter.record_proposal actually RAN (every psycopg.connect DSN passes the check); real ComposeStage over the real bootstrap-wired DeepSeek model with httpx intercepted → dialed_hosts == [fake_host] EXACTLY once, zero elsewhere; off-host DSN refused naming the key AND same-host non-localhost peer accepted; KeyringTokenStore token never on wire; `build_substrate` refuses BEFORE any adapter ctor. Quality (verifier's own run): ruff clean; mypy strict Success 185 files incl. tests (zero-error bar HELD); 782 passed / 4 skipped with pg wired (only the Google-live smokes); no-DSN posture holds (pg battery loud-skips). Scope: 2 new files + runtime.py's serve() check EXACTLY; config.py sits in files_in_scope but is genuinely untouched (no new config value needed). Non-goals held: no TLS enforcement, no DeepSeek retention audit, no dev-tooling residency. validate PASS (150 nodes/139 gov); gate PASS. NEXT per the locked v1.21 order: TK-50 (the batch's last ticket). | architect | TK-150, EP-28, Q-87, Q-25, ASMP-1, NG-7, CON-7, TK-14, TK-54, TK-47, contract v1.25 |
| 2026-07-09 | **TK-50 recorded DONE (64/93) (COMMIT, v1.26 — build+verification record, within authority) — the v1.21 batch COMPLETES (5/5) with its LAST ticket (EP-12): the outcome-signal inference engine, built EXACTLY to the Q-88 as-ruled shape; RISK-7's near-constant-IGNORED training stream is BROKEN.** BUILT + INDEPENDENTLY VERIFIED (PASS — verifier re-executed the full quality bar and every AC; builder never touched the contract, the process rule holding). NEW `src/wombat/user_model/outcome_inference.py`: a PURE deterministic engine — no LLM, no IO, imports only stdlib + `wombat.user_model.feedback_source`; grep-verified NO other src module imports it, so the off-path/nightly guarantee (Q-66 posture) holds structurally in BOTH directions. Closed `Outcome` StrEnum (LOAD_BEARING/REGRETTED/IGNORED) defined HERE per the Q-88 ruling — does NOT import `claims` (TK-45 maps onto `ClaimPredicate.OUTCOME_*` later); frozen injected input records `CalendarSnapshotDelta`/`DraftFate`/`ItemResolution` (disposition / aware-datetime-validated resolved_at / ttl_expired); `OutcomeSignal(item_ref, outcome, source, rule_name)` with source a closed `Literal["feedback","inferred"]`; `infer_outcomes(resolutions, feedback=, calendar_deltas=, draft_fates=) -> tuple[OutcomeSignal, ...]` with FIXED fold precedence feedback > inference > IGNORED-default and the three closed rules: flagged_conflict_disappeared → LOAD_BEARING; draft_deleted_unsent → REGRETTED; ignored_default → IGNORED. All 4 ACs verifier-re-executed, incl. the ≥20-item fixture yielding a non-degenerate gradient (≥1 LOAD_BEARING + ≥1 REGRETTED) — TK-49's RatingTuner has real signal to train on, and TK-51's AC3 fold-proof (deferred here per Q-86) is DISCHARGED. Quality (verifier's own run): ruff clean; mypy strict Success 187 files incl. tests; 736 passed / 47 skipped (all pg-gated/live, expected no-DSN); 20/20 new tests. Scope = the 2 files_in_scope exactly. Non-goals held: no motive inference (NG-1 — whether, never why), no calendar/Gmail writes, no claim write (TK-45's), no live producers/drain wiring (TK-175's per Q-86/Q-88 — the engine is code-complete but not boot-invoked until TK-175/TK-45, recorded honestly). validate PASS (150 nodes/139 gov); gate PASS (analyze). NEXT: batch complete — scope the next batch from the convergence chain TK-45 → TK-175 → TK-54 → TK-47 → TK-49 (the TK-54/TK-47 hold released by TK-150). | architect | TK-50, EP-12, Q-88, Q-86, Q-66, RISK-7, ISS-6, NG-1, TK-51, TK-45, TK-49, TK-175, contract v1.26 |
| 2026-07-09 | **CONVERGENCE BATCH SELECTED + BRIEFED (COMMIT, Q-90/v1.27 — batch selection + pre-build ruling set + one ticket split, all within authority; nothing escalated).** Batch (≤5, dependency-ordered, re-derived from the current graph): **① TK-45 → ② TK-176 (NEW) → ③ TK-175 (re-scoped) → ④ TK-54 → ⑤ TK-47**; TK-49 (the RatingTuner, where the chain converges) deliberately NEXT batch — it depends on TK-47 and the cap is 5; its clamp constants are already homed in params v5, no pre-work needed. Every cited seam re-verified against the installed cog-worx + as-built tree. **Q-90 findings/rulings:** (1) TK-45's AC2 'Defeat/SUPERSEDES write surface' has NO as-built referent — ScopedKG exposes assert_fact/add_evidence/invalidate only (Defeat = the reconciler's commit_reconciliation surface); supersede NORMALIZED to invalidate-then-assert through a widened ObservationWriter (its own docstring records 'TK-45 widens this'; observation_writer.py + tests added to TK-45 scope). (2) claims_about(entity) is subject-keyed — item_ref-subject OUTCOME claims could never be enumerated; claim shape FIXED class-subject-keyed (subject=EventClass.value, value JSON {item_ref, outcome, source, rule_name, disposition, resolved_at}, event_id) serving BOTH the per-item supersede and TK-49's per-class corpus read on the one as-built seam. (3) The hot-path OUTCOME_PENDING stamp was an UNOWNED wire; adding it tipped TK-175 past its own pre-authorized 'may split' flag → **NEW TK-176** (drain-side: shared-KG hoist — bootstrap's inline throwaway InMemoryEntityKG becomes ONE shared entity_kg+ScopeRegistry+ObservationWriter with UserModel on the same instance; injected absorb_feedback + stamp_resolution seams on GateStage, None defaults; FeedbackInputSource registration gated on NEW optional WOMBAT_FEEDBACK_FILE); TK-175 re-scoped to the dream-side collect/infer/label pass with the v1 collection shape ruled (EventClass enumeration; ttl_expired=True at pass time; calendar_deltas/draft_fates EMPTY live — recorded residual, producers deferred to TK-79/later, rules stay fixture-proven per TK-50). Dream graph end-state FIXED: dream_consolidate → dream_outcome → dream_run terminal (scaffold stays terminal; each ticket inserts upstream, isolation proofs keep passing). (4) TK-54 AC2 normalized — deepseek endpoint EXEMPT from residency (ASMP-1/Q-87), BudgetGuard bound REAL via cogworx build_model(spec, guard=) composition (BudgetGuardedModel fires pre-network), dream_budget_max_usd/max_calls = new OperatingParams (params.py + yaml v6 in scope); verified ctor seams recorded: CoherenceReconciler(entity_kg, store, oracle).tick()→ReconcilerStats never-raise; ClaimExtractor(journal, entity_kg, model, source_registry).tick()→int FAIL-STALL-raises; ModelConsistencyOracle(model, tier='flash'); InMemoryEntityKG implements EntityKG AND CoherenceStore (cold-boot store = the shared TK-176 instance); extractor's source_registry = cogworx.knowledge.source_registry.SourceRegistry, NOT wombat's (name collision flagged). (5) TK-47: bounded run-to-drain (terminate on zero-work both sweepers; hard max-ticks cap; extractor stall → catch, log LOUD, transition ON so a stalled extractor never kills outcome/tuning downstream); bootstrap.py + e2e test added to scope. **Deliberately NOT picked:** TK-49 (next batch, converges the chain), TK-151/TK-80/TK-78/79 (outbound track after the adaptation arc), TK-164/TK-111/TK-115 (P2), browser (P3), anything needing Jim's OAuth (operator step). validate PASS (151 nodes/140 gov); gate PASS (analyze). | architect | Q-90, TK-45, TK-176, TK-175, TK-54, TK-47, TK-49, Q-86/87/88, Q-41, Q-39, ASMP-1, DEF-1, contract v1.27 |
| 2026-07-09 | **TK-45 recorded DONE (65/94) (COMMIT, v1.28 — build+verification record, within authority) — the FIRST v1.27/Q-90 convergence-batch ticket lands (EP-12): OutcomeLabeler writing OUTCOME_* claims through a widened ObservationWriter, built EXACTLY to the Q-90 as-ruled shape (supersede = ScopedKG invalidate-then-assert; claims class-subject-keyed, subject=EventClass.value, item identity in value JSON).** BUILT + INDEPENDENTLY VERIFIED (PASS — verifier independently re-derived and re-executed the full quality bar and every AC: ruff clean; mypy strict Success 189 source files src+tests; 743 passed / 47 expected pg-gated skips; 7/7 new tests; adversarial probes; scope = the 3 briefed files exactly — observation_writer.py +40/-6 adding ONLY record_superseding + docstring updates, NEW outcome_labeler.py + tests/user_model/test_outcome_labeler.py; no contract edit by the builder). record_superseding = TypeError-before-I/O → invalidate (ValueError propagates) → the exact record() path; stamp_pending = ONE OUTCOME_PENDING, no terminal on the hot path (Q-22 held); label_terminal = PENDING invalidated (valid_to set) + sole-active terminal claim; three-member Outcome→ClaimPredicate map w/ source/rule_name in value JSON; corpus queryable via claims_about by event-class subject w/ item_ref + event_id — the shape TK-49 reads. Committed as its own commit (code + contract, c357899). NEXT per the locked batch order: TK-176 → TK-175 → TK-54 → TK-47. |
| 2026-07-09 | **TK-176 recorded DONE (66/94) (COMMIT, v1.29 — build+verification record, within authority) — the SECOND v1.27/Q-90 convergence-batch ticket lands (EP-12): outcome-loop drain-side wiring, all four wires over existing pieces.** (1) optional `wombat_feedback_file` config (NOT REQUIRED_ENV); (2) bootstrap hoist → ONE shared user-scope trio (entity_kg + ScopeRegistry + ObservationWriter) + OutcomeLabeler, UserModel over the SAME instance, additive RuntimeBundle fields; (3) GateStage's two injected optional async seams — `absorb_feedback` (pre-scoring diversion → BEHAVIOR_OBSERVED + ack; byte-identical baseline; raise-tolerant) + `stamp_resolution` (OUTCOME_PENDING post-decision, Q-90 class-subject shape, no terminal on hot path — Q-22 held); (4) WOMBAT_FEEDBACK_FILE-gated FeedbackInputSource registration w/ loud-skip. The previously UNOWNED PENDING-stamp wire now OWNED + LIVE. Independently verified PASS (ruff clean; mypy strict 190 files; 749/48 no-DSN; 816/4 full on throwaway pg-16). files_in_scope normalized += tests/unit/test_runtime.py (as-built mechanical ctor updates, TK-46/TK-52 precedent — no scope change). Committed as its own commit (code + contract, cccac2a). NEXT per the locked batch order: TK-175 → TK-54 → TK-47. | architect | TK-176, Q-90, Q-22, TK-45, TK-51, TK-16, EP-12, contract v1.29 |
| 2026-07-09 | **TK-175 recorded DONE (67/94) (COMMIT, v1.30 — build+verification record, within authority) — the THIRD v1.27/Q-90 convergence-batch ticket lands (EP-12): outcome-loop dream-side wiring — DreamOutcomeStage at the entry of `wombat.dream` (dream_outcome → dream_run, the Q-90 end-state), the nightly collect/infer/label pass. THE OUTCOME LOOP IS NOW CLOSED end-to-end:** capture (TK-176) → nightly infer/label (TK-175) → terminal OUTCOME_* corpus; only tune() (TK-49, next batch) remains to consume it. Stage keyword-injected with the shared user-scope entity_kg + TK-45 OutcomeLabeler + user_id; run() = closed EventClass enumeration → claims_about(class.value) ACTIVE OUTCOME_PENDING → envelope double-parse → ItemResolution(ttl_expired=True, v1 rule); per-item feedback = newest ACTIVE BEHAVIOR_OBSERVED via claims_about(item_ref); TK-50 infer_outcomes (calendar_deltas/draft_fates EMPTY live — the recorded Q-90 residual, producers TK-79+) → labeler.label_terminal invalidate-then-assert. No ctx.journal touch, no model call, loud non-fatal errors. AC1 proven on the real engine driving the registered dream graph, in-memory AND real pg (PENDING invalidated, active LOAD_BEARING for the feedback item, IGNORED for the no-feedback item, corpus enumerable per class — the TK-49 read shape); AC2 empty corpus → clean COMPLETED, zero writes, TK-46 isolation proofs still pass. Independently verified PASS (ruff clean; mypy strict 191 files; 751/48 no-DSN; 818/4 full on throwaway pg-16 port 5599 — matches builder exactly; no contract edit by the builder). files_in_scope normalized += the two dream e2e tests (mechanical graph-shape updates for the new node, TK-46/TK-52 precedent — no scope change). Committed as its own commit (code + contract). NEXT per the locked batch order: TK-54 → TK-47. | architect | TK-175, Q-90, TK-176, TK-50, TK-45, TK-49, TK-79, EP-12, contract v1.30 |
| 2026-07-09 | **TK-54 recorded DONE (68/94) (COMMIT, v1.31 — build+verification record, within authority) — the FOURTH v1.27/Q-90 convergence-batch ticket lands (EP-13): the dream substrate provider — the off-path consolidation collaborators TK-47's sweepers require but no ticket constructed (the audit-CRITICAL/Q-29 seam), built EXACTLY to the Q-90/v1.27 as-normalized shape.** NEW `pathways/dream_substrate.py`: frozen `DreamSubstrate(store, oracle, model, source_registry)` + `build_dream_substrate(entity_kg, spec, params, client=None)` — consolidation Model via cog-worx `build_model` behind a REAL `BudgetGuard(max_usd=dream_budget_max_usd, max_calls=dream_budget_max_calls)` (DEC-23 off-path admission budgeted; the GATE stays model-free, NG-4); `ModelConsistencyOracle(model, tier="flash")`; cog-worx `knowledge.source_registry.SourceRegistry` with wombat's own `system`/`wombat.dream` declaration (the flagged name collision handled by explicit module-path import); entity_kg REUSED as CoherenceStore via explicit `typing.cast` (Protocol gap recorded, not hidden — both concrete KGs implement both on one class); NO `safety.local_residency` import (deepseek endpoint EXEMPT, ASMP-1/Q-87). Params v5→6: +dream_budget_max_usd (0.10) / +dream_budget_max_calls (20), yaml v6 w/ DEC-23 comment. Independently verified PASS (AC1: all four non-None, real CoherenceReconciler AND ClaimExtractor construct, ZERO network — spy client never called; AC2: max_calls=0 and max_usd=0.0 both raise BudgetExceeded PRE-network; ruff clean; mypy 193 files src+tests; 756/48 no-DSN; scope = the 5 files_in_scope exactly; no contract edit by the builder). Committed as its own commit (code + contract + this brief). NEXT per the locked batch order: TK-47 (the batch's LAST ticket) — the reconciler/extractor DRIVE logic over this substrate. | architect | TK-54, Q-90, Q-29, DEC-23, NG-4, ASMP-1, Q-87, TK-47, TK-49, EP-13, contract v1.31 |
| 2026-07-09 | **TK-47 recorded DONE (69/94) (COMMIT, v1.32 — build+verification record, within authority) — the FIFTH and LAST v1.27/Q-90 convergence-batch ticket lands (EP-13): DreamConsolidationStage, the reconciler/extractor DRIVE logic over the TK-54 substrate — THE Q-90 CONVERGENCE BATCH IS COMPLETE (5/5).** `wombat.dream` entry stage `dream_consolidate` → dream_outcome → dream_run (the ruled end-state graph). Keyword-injected reconciler+extractor (never constructed in-stage — TK-54's arrive budget-guarded, DEC-23/DEC-18 posture); bounded run-TO-DRAIN loop, MAX_TICKS=100 structural cap; per tick `reconciler.tick()` (never-raises per cog-worx source) then `extractor.tick()` try/except — extractor raise → loud STALLED log, stalled=True, transition STILL taken (stall wraps ONLY the extractor per the v1.27 ruling; a stalled extractor never kills outcome/tuning downstream); zero-work termination = ONE iteration with subjects_processed==0 AND count==0 (verifier confirmed per-tick local, not accumulator). Bootstrap wires the shared cold-boot substrate.journal + cogworx.knowledge source_registry (name collision honored). Independently verified PASS (all 3 AC clusters re-executed: drain/merge+claim-extraction, clean-night ticks==1 zero model calls, stall-still-transitions; ruff clean; mypy strict 194 files; 759/48 no-DSN + 826/4 on throwaway pg-16; scope = 3 briefed files + 3 flagged mechanical test edits, files_in_scope normalized — TK-176 precedent; no contract edit by the builder). Committed as its own commit (code + contract, 23a657e). **The outcome/consolidation arc is built end-to-end; NG-4 intact (gate model-free — all admitted inference off-path, budgeted). NEXT: scope the next batch — the chain converges at TK-49 (RatingTuner over the drained corpus).** | architect | TK-47, Q-90, TK-54, TK-176, DEC-23, DEC-18, NG-4, TK-49, EP-13, contract v1.32 |
| 2026-07-09 | **TK-49 recorded DONE (70/94) (COMMIT, v1.34 — build+verification record, within authority) — the FIRST v1.33/Q-91 batch ticket lands and EP-14 (adaptation) CLOSES: RatingTuner — bounded, floored/ceilinged rating-parameter updates over the drained outcome corpus; THE ADAPTATION CHAIN IS CLOSED END-TO-END.** `wombat.dream` = dream_consolidate → dream_outcome → **dream_tune** → dream_run; NEW `rating/rating_tuner.py` (keyword-injected entity_kg/writer/params/user_id/clock) reads the RECALL_WINDOW_NIGHTS=7 terminal OUTCOME_* corpus per EventClass via `claims_about` (the Q-34 read), TK-48 spike math verbatim (net_signal → gain·signal clamped ±delta_bound → band clamp), ALL FIVE bounds from `OperatingParams.rating_tuner` (ast test proves none of 0.35/0.65/0.05/0.20 inlined — the joint-set/RISK-4 finding held structurally); writes via `ObservationWriter.record_rating_params`'s NEW additive keyword-only `source` SourceDeclaration (ref `wombat.rating_tuner:<night>`; None = byte-identical prior behavior); empty in-window corpus → ZERO writer calls. Independently verified PASS (766/48 no-DSN, ruff clean, mypy Success 196 files src+tests; 7/7 new tests covering every AC incl. exact clamps 0.65/0.35 + the behavioral delta_bound-not-gain proof; graph membership+COMPLETED proven non-gated on the real Engine, exact-order in the pg-gated e2e; adversarial — production math == tuner_sim.py verbatim, sole deviation = the ruled empty→no-write). files_in_scope normalized += the 3 dream integration tests + test_outcome_loop_wiring (mechanical 4-stage-graph updates, TK-176/TK-47 precedent); builder did not touch the contract — the process rule holding. NEXT = TK-151 (per-stage external tier policy — the outbound track opens). | architect | TK-49, EP-14, Q-91, Q-34, TK-48, RISK-4, DEC-23, contract v1.34 |
| 2026-07-09 | **TK-149 recorded DONE (73/95) (COMMIT, v1.37 — build+verification record, within authority) — the FOURTH v1.33/Q-91 batch ticket lands (EP-28): approval-gate wiring — the shared two-step dispatch base (journal → AwaitHuman → dispatch_approved); DEC-19's review-before-send is now a reusable structural pattern.** Built EXACTLY to the Q-91 ruling (STUB stages over the shared base; action_id = `run_id:stage_name` — Q-63 carry-forward DISCHARGED; `ctx.read_human_input`; Speak/browser live wiring OUT — TK-177 owns the live wire). `dispatch_base.py` ProposeDispatchStage: trail row via `writer.record_proposal` BEFORE the AwaitHuman return; `dispatch_approved.py` DispatchApprovedStage self-admits via the ONE sanctioned `bind_external_tier` call site (TK-151); reject-safe on absent/malformed decision. Independently verified PASS (verifier re-derived + re-executed the full bar and every AC on a clean tree: ruff clean; mypy Success 203 files; 790/49 no-DSN, pg durability tests skip LOUD as designed; 9/9 tests vs the real Engine + Registry/ToolGate on throwaway pg-16; scope = the 3 files_in_scope exactly, no contract edit by the builder; DEC-26 re-proven repo-wide — sole external-admitting StageToolPolicy stays tier_policy.py's constant): park/approve-exactly-once-idempotent/reject-cancelled/second-payload-same-pattern/durable-pending-row-across-restart/direct-bypass→TierViolation (Q-32 as-ruled). NON-GOALS held (no UI, no retry, NG-5). Committed as its own commit (code + contract + this brief). NEXT per the locked v1.33 order: TK-78 (DraftComposer, the batch's LAST ticket). | architect | TK-149, EP-28, Q-91, Q-63, Q-32, DEC-19, DEC-26, TK-146, TK-151, TK-78, TK-177, NG-5, contract v1.37 |
| 2026-07-09 | **TK-78 AC1 RULING RECORDED + Q-92 (COMMIT, v1.38 — orchestrator's ruling under Jim's delegation, independently source-verified by the architect; AC amendment + as-built normalizations, within authority; taint invariant/DEC-19/ISS-3 untouched and strengthened-as-written).** The TK-78 builder's block was FACTUALLY CORRECT: cog-worx `dispatch_one` latches taint PRE-invoke (router.py pipeline step ③; policy.py TaintState — any untagged external latches), so AC1's literal 'tainted is False at AwaitHuman-park' was impossible for the untagged external `gmail.drafts.create`; Q-91 pt-6's 'post-invoke' prose had the order wrong (same harmless-by-order conclusion). RULING ADOPTED (option a): AC1 amended — pre-dispatch fresh-drive baseline False (the ISS-3 premise proof) + tainted True at park + post-latch external-dispatch-REFUSED probe (harmless-by-order proven in test, not prose); option b (trusted-output tag) REJECTED — no weakening of the structural taint defense for test cosmetics. Q-92 also rules the as-built shapes: DraftComposer standalone (not a ProposeDispatchStage subclass — non-overridable run(), no pre-park dispatch hook, journal-ordering inversion) + owns make_drafts_create_capability; TK-79 draft_dispatch = thin terminal zero-dispatch stage (approved-for-send → DISPATCHED via mark_dispatched; refusal = record_refusal + loud raise; ask_step_index REQUIRED — answer lands at the PARKED step's positional index, engine.py:696); TK-177 emission point → the Gmail source seam (no body-deep ingest drive is boot-wired; wrapper InputSource over GmailPoller, key=derive_key('gmail','reply:<msgid>'); ingest-drive live wire = recorded residual) + drafts.create registered into the ONE build_engine Registry. Build order corrected TK-78 → TK-79 → TK-177 (TK-177 depends_on TK-79; the proposed 177-first was unbuildable). validate PASS (152 nodes/142 gov); gate PASS (analyze). | architect | Q-92, Q-91, TK-78, TK-79, TK-177, TK-149, TK-151, TK-80, ISS-3, CON-5, contract v1.38 |
| 2026-07-09 | **TK-78 recorded DONE (74/95) (COMMIT, v1.39 — build+verification record, within authority) — the FIFTH and LAST v1.33/Q-91 batch ticket lands (EP-18): DraftComposer — compose → journal → `gmail.drafts.create` → AwaitHuman park; wombat NEVER sends; the batch is COMPLETE (5/5).** Built exactly to the Q-92 as-built rulings: standalone compose.py-parity stage (NOT a ProposeDispatchStage subclass — the base's non-overridable run() would invert journal-before-Gmail ordering); ProposalWriter + the `run_id:stage_name` action_id convention reused as imports; journal BEFORE the exactly-one drafts.create dispatch; TierViolation → record_refusal + loud re-raise; AwaitHuman(to=`draft_dispatch`) freezes TK-79's stage name, Artifact kind `wombat.draft_proposal`; external tier self-admitted in __init__ via the one sanctioned bind_external_tier site (TK-151); owns make_drafts_create_capability(session). Independently verified PASS: ruff clean; mypy Success 205 files src+tests; 801 passed / 49 skipped (baseline 790 + 11 new; all skips WOMBAT_TEST_PG_DSN-gated); all four ACs re-derived on the REAL cog-worx Registry/ToolGate/dispatch_one + a real Engine — AC1 pre-dispatch taint-False baseline (the ISS-3 premise) + journal-before-Gmail event order + exactly-one drafts.create + post-latch external dispatch REFUSED (the Q-92 harmless-by-order probe) + zero gmail.messages.send + engine-half AWAITING_HUMAN with a raising draft_dispatch stub never executing; AC2 ISS-3 negative via the real tagged READ_EMAIL_BODY_CAPABILITY → TierViolation, zero drafts.create, exactly one record_refusal, re-raised loud; AC3 resume keeps the park, call count unchanged; AC4 mouth-down degrade → template body, draft still created + still held. Scope = the 2 files_in_scope exactly; builder did not touch the contract. NEXT = TK-79 → TK-177 (the Q-92 corrected order). | architect | TK-78, Q-92, Q-91, ISS-3, DEC-19, CON-5, NG-5, TK-151, TK-149, TK-79, TK-177, EP-18 |
| 2026-07-09 | **TK-79 test-construction impossibility RULED + Q-93 (COMMIT, v1.40 — orchestrator ruling under Jim delegation, independently source-verified by the architect; PURE test-construction normalization, within authority; no stage design, locked decision, or v1 scope touched).** The TK-79 builder block was FACTUALLY CORRECT on all three claims, each re-verified: (1) DraftComposer.run() unconditionally awaits ctx.last_output(compose_dispatch) first and raises on None (draft_composer.py:181-184); (2) cog-worx Engine.run() accepts initial= but never references it in its body — start_run journals ids+fingerprint only, _drive walks from EMPTY step history (engine.py:438-466) — so the briefed two-stage graph (real DraftComposer at entry, ask_step_index=0) is UNSATISFIABLE; (3) TK-78 frozen _stub_graph (test_draft_composer.py:269-273) is the established working construction with DraftComposer at step index 1. RULING ADOPTED: TK-79 AC1/AC2 amended to the 3-stage stub graph (compose_dispatch stub -> real DraftComposer -> real draft_dispatch) with ask_step_index=1 (answer lands at the PARKED step index, engine.py:696). Q-92 stage design UNCHANGED + VINDICATED (ask_step_index REQUIRED graph-position-sensitive ctor arg — this incident is the proof); builder drafted stage shape conforms and stands (terminal, zero dispatch every path, approve->mark_dispatched, reject->mark_cancelled, absent/malformed->record_refusal+loud raise). TK-177 briefing stands as-is; cascade clears when TK-79 lands. validate PASS (152 nodes/143 gov); gate PASS (analyze). | architect | Q-93, Q-92, TK-79, TK-177, TK-78, TK-149, CON-5, ISS-3, contract v1.40 |
| 2026-07-09 | **TK-79 DONE (75/95, v1.41 — recording the build + independent verification; within authority, status flip + record only).** `draft_dispatch` terminal stage built EXACTLY to the Q-92/Q-93 as-ruled shape: terminal `transitions=()`, ZERO capability dispatch on every path (never-send structural — CON-5/NG-5; the generic DispatchApprovedStage not reused, its approve path would double-create the pre-park draft; ApprovalTrailWriter + MissingApprovalAnswer imported not reimplemented), approve→mark_dispatched (TrailStatus.DISPATCHED per Q-92), reject→mark_cancelled, absent/malformed→record_refusal + loud raise, `action_id = run_id:propose_stage_name` matching DraftComposer's record_proposal row, `ask_step_index` REQUIRED-no-default (Q-93 vindicated), NO bind_external_tier (unearned for a dispatch-less stage). Verifier PASS: 810/49 (baseline 801 + 9 new, all skips DSN-gated), ruff clean, mypy 207 files; AC1/AC2 on a REAL Engine over the Q-93 3-stage construction with ask_step_index=1 — drafts.create pinned at the single pre-park call on both paths, send spy empty, trail finalized/cancelled on the shared action_id; AC3 bypass refused loud with zero dispatches + no tool_policy attribute. Scope = the 2 files_in_scope exactly; builder did not touch the contract. The Q-93 cascade clears. NEXT = TK-177 (outbound live wiring — the last EP-18 ticket). validate PASS (152 nodes/143 gov); gate PASS (analyze). | architect | TK-79, Q-93, Q-92, TK-78, TK-149, TK-147, TK-177, CON-5, NG-5, NG-8, EP-18, contract v1.41 |
| 2026-07-09 | **TK-177 DONE (76/95, v1.42 — recording the build + independent verification; within authority, status flip + record only). EP-18 CLOSES — the outbound Gmail-draft track is boot-wired end-to-end; wombat NEVER sends (CON-5/NG-5).** Built EXACTLY to the Q-92 as-ruled shape, wiring ONLY over TK-78/TK-79/TK-80 (no domain logic changed). WIRE 1: `GmailWithReplyIntents` wrapper (id `gmail`) replaces the bare GmailPoller in `_maybe_register_gmail` (same Q-67 guards + loud-skip; `build_brief_fetches` untouched) — per-event `from_payload → triage_message → reply_intent.build`, second SourceEvent `reply:<message_id>` (item_kind=draft) only on HIGH; TK-12 key via the registry's existing `derive_key('gmail','reply:<msgid>')`, re-poll ALREADY_QUEUED. WIRES 2+3: `build_engine` gains optional `capability_registry` (bare-Registry default, byte-identical for existing callers); drafts.create registration + DRAFT route + `draft_dispatch` edge gated on ONE Q-67 check (creds AND stored token) — all-or-nothing loud-skip (TK-16 pattern), Google-less boot leaves the drain graph byte-identical to the 5-stage build; `build_draft_composer_stage` factory (Q-69 lesson); `ask_step_index = pre_dispatch_stages.index(draft_composer_stage)` computed never hardcoded (Q-93 honored); ONE shared ActionTrailWriter across composer + dispatch. Verifier PASS: ruff clean, mypy 208 files, FULL suite on throwaway pg-16 = 882 passed; all 3 ACs re-executed (extra draft item enqueued exactly once + re-poll idempotent; real-Engine e2e through the real gate to AwaitHuman park in a fresh untainted drive with a fake Gmail capability; Google-less loud-skip leaves drain + brief unaffected); scope = the 3 files_in_scope exactly; builder did not touch the contract. The DEC-19 outbound arc is watchable the moment Jim's OAuth bring-up lands. validate PASS (152 nodes/143 gov); gate PASS (analyze). | architect | TK-177, Q-92, Q-93, TK-78, TK-79, TK-80, TK-151, TK-12, TK-16, CON-5, NG-5, EP-18, DEC-19, contract v1.42 |
| 2026-07-09 | **2026-07-09 CODE REVIEW ROUTED (COMMIT, v1.43 — within authority, nothing escalated; orchestrator-authorized under Jim's delegation).** All 11 CR2 findings homed: +8 fix tickets TK-178..TK-185 (order TK-179 → TK-178 → TK-183 → TK-180 → TK-181 → TK-182 → TK-184 → TK-185; P1s = TK-179 draft-approval position-independent answer read ruled at Q-94, superseding the Q-92/Q-93 ask_step_index shape, + TK-178 hostaddr residency bypass); CR2-7 folded into TK-182 (same seam); CR2-9 accepted as RISK-10 (ASMP-2-fenced cog-worx at-least-once contract); CR2-11 ruled at Q-95 (clamp correct, TK-41 defaults reconcile into the band). validate + gate_check PASS. | architect | Q-94, Q-95, RISK-10, TK-178..TK-185, CR2-1..CR2-11 |
| 2026-07-09 | **TK-179 recorded DONE (77/103) (COMMIT, v1.44 — build+verification record, within authority; status flip + record only) — the FIRST CR2 fix ticket lands and the review P1 (CR2-2, ruled at Q-94) is CLOSED: draft approval is position-independent in the standing runtime.** DraftDispatchStage + generic DispatchApprovedStage drop `ask_step_index`; the parked proposal step is located BY STAGE IDENTITY from the run's committed step history (`ctx.journal.load_run` → last step with `stage_name == propose_stage_name` → `read_human_input` at its own step_index — the `StageContext.last_output` idiom; engine records the answer at the parked step's step_index, engine.py:695-696, so the lookup provably matches regardless of accumulated idle Waits); no-propose-step → refusal + loud raise (fail closed); bootstrap's precomputed `draft_ask_step_index` and its false fresh-run comment removed. Supersedes the Q-92/Q-93 ctor-arg shape per the Q-94 ruling; NO cog-worx change; CON-5/NG-5 never-send held (zero capability dispatch on every path unchanged). Independently verified PASS — 886 passed / 4 skipped (pg-gated tests RAN on a throwaway postgres-16), ruff clean, mypy strict 208 files; scope = the 6 files_in_scope exactly; the idled-drain e2e pins the register's repro (approve → DISPATCHED, zero refusal rows, run not stranded). Residual stands at Q-94 (genuinely absent answer still raises loud — the deliberate bypass posture). NEXT = TK-178 (CR2-1, the second P1). |
| 2026-07-09 | **TK-178 recorded DONE (78/103) (COMMIT, v1.45 — build+verification record, within authority; status flip + record only) — the CR2-1 S2 residency bypass is CLOSED (EP-28): the guard now checks libpq `hostaddr=`, the address libpq actually dials, so a DSN cannot silently persist wombat data off-host past CON-7/NG-7.** `_extract_host` (safety/local_residency.py) surfaces `hostaddr` from all three DSN forms it already parses — URL query key, keyword token, and the bare `hostaddr=... dbname=...` no-host form (previously mis-read as an inherently-local unix socket) — with hostaddr taking precedence over host/`?host=` whenever present (host then only auth/TLS SNI); hostaddr-absent DSNs are byte-identical prior behavior. One fix covers BOTH call sites (check_config + substrate.build_substrate) via the shared residency_check predicate. INDEPENDENTLY VERIFIED PASS: AC1 the register's three exact repro DSNs (URL `?hostaddr=8.8.8.8`, keyword `host=localhost hostaddr=8.8.8.8`, bare `hostaddr=8.8.8.8`) all REFUSED naming wombat_pg_dsn; AC2 the same shapes with `hostaddr=127.0.0.1` accepted (no false refusal); AC3 prior URL/keyword/unix-socket/literal-IP/localhost/userinfo tables green unchanged; plus a parametrized hostaddr-precedence pin. Quality bar: 823 passed / 50 skipped (all pg-DSN-gated), ruff clean, mypy clean whole-project (208 files). Scope = files_in_scope exactly; non_goals held (multi-A any-vs-all resolver untouched — TK-183/CR2-6 next, same file by design; deepseek_base_url ASMP-1 exemption untouched). validate PASS (160/146); gate PASS. NEXT per the CR2 order: TK-183. | architect | TK-178, CR2-1, CON-7, NG-7, EP-28, TK-183, ASMP-1, contract |
| 2026-07-09 | **TK-183 recorded DONE (79/103) (COMMIT, v1.46 — build+verification record, within authority; status flip + record only) — the CR2-6 multi-A / DNS-rebinding false accept is CLOSED (EP-28): the residency predicate now requires ALL resolved addresses local, not merely one.** `make_residency_check` (safety/local_residency.py) — `resolved and all(_addr_is_local(addr, locals_now) for addr in resolved)` replaces the `any()`, so a co-resolved loopback record (multi-homed hostname, or a DNS-rebinding response appending a loopback address) can no longer smuggle a genuinely remote endpoint past CON-7/NG-7 (libpq dials resolved addresses in order and may pick the remote record — rationale now a code comment at the predicate); an EMPTY resolution list also fails closed with RemoteStorageConfigError naming wombat_pg_dsn. Literal-IP/unix-socket/no-host/localhost fast paths byte-identical untouched (verified at source). INDEPENDENTLY VERIFIED PASS: AC1 both register repros `['8.8.8.8','127.0.0.1']` and `['203.0.113.9','127.0.0.1']` REFUSED with local_addrs=['127.0.0.1','::1'] (plus a loopback-FIRST order-independence probe, also REFUSED); AC2 empty resolution → refused naming the key; AC3 only-local resolutions (single + multiple) still accepted, whole suite green — the Q-25 Docker-bridge-to-self and same-host cases hold. Quality bar: 827 passed / 50 skipped (all WOMBAT_TEST_PG_DSN-gated), ruff clean, mypy clean whole-project (208 files). Scope = the 2 files_in_scope exactly (one predicate line + comment; 3 new tests); non_goal held (no connect-time DNS re-resolution pinning — the boot-time defense-in-depth boundary stands). validate PASS (160/146); gate PASS. NEXT per the CR2 order: TK-180 (dream BudgetGuard per-night). | architect | TK-183, CR2-6, CON-7, NG-7, EP-28, Q-25, TK-180, contract v1.46 |
| 2026-07-09 | **TK-180 recorded DONE (80/103) (COMMIT, v1.47 — build+verification record, within authority; status flip + files_in_scope path normalization + record only) — the CR2-3 S2 silent consolidation death is CLOSED (EP-13): the dream substrate's BudgetGuard is per-NIGHT, not process-lifetime.** `DreamSubstrate.model` = new `_NightBudgetedModel` wrapper (structural Model protocol) lazily rebuilding the inner `build_model(spec, guard=..., client=...)` composition with a FRESH BudgetGuard on the first touch of a new wombat-night; night key `wombat_today(clock(), tz)` reused from daily_ledger (DEC-21); new injectable `clock`/`tz` keywords on `build_dream_substrate` default to real UTC so bootstrap's call site is unchanged; collaborators constructed once at boot over the wrapper (TK-47 shape held — the oracle shares the SAME wrapper, one per-night budget for both sweepers); within-night accumulation unchanged (correct under the TK-52 once-per-night fence); params.yaml comments corrected to per-night. INDEPENDENTLY VERIFIED PASS: verifier's own adversarial repro over the real substrate + `load_operating_params` defaults — 20 completes succeed on night N, call 21 REFUSED with the ceiling error, first call on night N+1 SUCCEEDS (the register's failure mode — refused forever after 20 cumulative calls — dead); existing zero-network + pre-network-refusal tests green; ruff clean, mypy Success 208 files, 828 passed / 50 skipped (all documented gates). Scope = 3 files; files_in_scope normalized `config/wombat_params.yaml` → as-built `src/wombat/wombat_params.yaml`; bootstrap.py/dream_pathway.py untouched. Non-goals held: no cog-worx BudgetPolicy/new_guard seam adoption (DEC-23 off-path composition stands), no ceiling re-sizing, no guard-counter mutation. validate PASS (160/146); gate PASS. NEXT per the CR2 order: TK-181. | architect | TK-180, CR2-3, DEC-21, DEC-23, EP-13, TK-52, TK-47, TK-54, TK-181, contract v1.47 |
| 2026-07-09 | **TK-181 recorded DONE (81/103) (COMMIT, v1.48 — build+verification record, within authority; status flip + record only) — the CR2-4 at-capacity redelivery eviction is CLOSED (EP-8): `PendingSet.add` runs a membership guard FIRST, before the capacity/eviction branch, so an at-least-once redelivered already-held item can never evict a DIFFERENT durably-held item.** Unchanged scored fields → true no-op (no journal append, no mutation — `added_at` is not a scored field, so the age clock is not reset; `oldest_added_at` verified untouched); changed scored field → held entry refreshed in place, exactly ONE additional PendingSetAdd journaled (replay stays last-write-wins per item_id); always returns None on this path, never CapacityEviction. TK-27's redelivery-collapses-to-harmless-re-add claim is now TRUE at capacity too; new-id eviction logic byte-unchanged (TK-174 urgency-aware eviction + Q-45 Remove-before-Add stand). INDEPENDENTLY VERIFIED PASS via the verifier's own script: AC1 register's exact cap-3 a(u=5)/b(u=1)/c(u=3) redeliver-c repro → b still held, size 3, zero Removes, None returned (the CR2-4 scenario reproduced against current code and dead); AC2 rebuild_from_journal keeps all three; AC3 urgency 3→9 re-add → +1 Add, 0 Removes, no eviction; AC4 TK-174 + Q-45 crash-mid-eviction suites green unchanged. Quality bar: 832 passed / 50 skipped (all DSN-gated), ruff clean, mypy Success 208 files src+tests. Scope = the 2 files_in_scope exactly; non-goals held (no queue-side dedup — the set stays the single custody owner per Q-51/Q-52; no journal compaction, DEF-6). validate PASS (160/146); gate PASS. NEXT per the CR2 order: TK-182 (feedback-file tolerant decode + rotation-aware offset). | architect | TK-181, CR2-4, CST-1, EP-8, TK-27, TK-174, Q-45, Q-51, Q-52, DEF-6, TK-182, contract v1.48 |
| 2026-07-09 | **TK-182 recorded DONE (82/103) (COMMIT, v1.49 — build+verification record, within authority; status flip + record only) — the CR2-5 permanent wedge + the CR2-7 silent truncation skip are CLOSED together (EP-12): the explicit-feedback file tail now decodes utf-8 tolerantly (a bad byte → a warned+skipped malformed line; `poll()` never raises for file content, CON-3), protects already-drained pushed events behind a try/except OSError around the file read (a failed read logs loud and yields nothing for the file portion — pushed events still emit), and tracks `_last_size` so detected byte-size shrinkage (truncation/rotation) resets `_lines_read` to 0 and re-reads from the start — bounded duplicates share `event_key` and dedup at the queue (ALREADY_QUEUED).** Verifier independently re-proved the strict-vs-tolerant decode on the register's exact repro, the OSError push-preservation path, and the 27→18-byte shrinkage reset; quality bar green (ruff clean; mypy Success 208 files; 835 passed / 50 skipped, all DSN-gated). Scope = the 2 files_in_scope exactly; non-goals held (no inotify/watchdog — the poll-tail shape stands; no feedback-semantics change). validate PASS; gate PASS. NEXT per the CR2 order: TK-184 (review hygiene batch). | architect | TK-182, CR2-5, CR2-7, CON-3, EP-12, TK-184, contract v1.49 |
| 2026-07-09 | **TK-184 recorded DONE (83/103) (COMMIT, v1.50 — build+verification record, within authority; status flip + record only) — the CR2-8 + CR2-10 hygiene pair is CLOSED and the CR2-9/RISK-10 acknowledgment is in code (EP-1): atomic sidecar write (mkstemp + os.replace, temp cleaned on failure) + corrupt-sidecar → `{}` with a loud warning (the Q-89 duplicate-on-loss fallback — render() never raises); `RuntimeBundle.action_trail_writer` closed in the runtime teardown finally like every other assembled adapter (the TK-173/CR-15 leak class, now removed for the trail writer; Google-less boot unchanged); draft_composer docstring names the cross-instance at-least-once re-create window + the ASMP-2 fence. Independently verified PASS (914 passed / 4 skipped on a throwaway pg-16 with the DSN set — pg-gated trail tests RAN; ruff clean; mypy Success 208 files; scope = the 6 files_in_scope exactly; no contract edit by the builder). Non-goals held (no run-lease/reaper — RISK-10 stands; no renderer daemon wiring). NEXT and LAST in the CR2 order = TK-185 (Q-95 clamp-band default reconciliation). |
| 2026-07-09 | **TK-185 recorded DONE (84/103) (COMMIT, v1.51 — build+verification record, within authority; status flip + record only) — the CR2-11 first-night snap-against-signal is CLOSED and the 2026-07-09 code-review fix run COMPLETES 8/8 (EP-10, ruled at Q-95: the clamp band stands, the defaults move).** The three out-of-band TK-41 per-class rating defaults now sit inside the RatingTuner's locked [0.35,0.65] band (`rating/params.py`): CALENDAR_CONFLICT.urgency_base 0.7→0.65, MORNING_BRIEF.load_base 0.3→0.35, REFLECTION.urgency_base 0.3→0.35 — ordinal differentiation (elevated/neutral/muted) preserved; the first non-empty-corpus tune can no longer snap a param against the outcome signal (the REFLECTION sign inversion is dead), so the DEC-13 adaptive-gate thesis holds from the first tune night. New AC1 direction test proves for EVERY EventClass that an all-load-bearing corpus never lowers urgency_base and an all-ignored corpus never raises it on the first night; new AC2 test pins every class's urgency_base/load_base in [clamp_floor, clamp_ceiling] + cross-class ordinal ordering. Non-goals held: no clamp-band change (locked jointly with the 12/day surfacing ceiling), no delta-only clamping (rejected at Q-95), no TK-49/TK-48 constant changes. Independently verified PASS — 841 passed / 51 skipped (all DSN gates), ruff clean, mypy Success 208 files; verifier adversarially reverted the defaults and reproduced both register failures exactly. Scope = the 3 files_in_scope exactly; no contract edit by the builder. **The CR2 batch closes 8/8: TK-179, TK-178, TK-183, TK-180, TK-181, TK-182, TK-184, TK-185 — every 2026-07-09 review finding dispositioned in code or accepted (RISK-10).** |
| 2026-07-09 | **POST-SPINE BATCH SELECTED + PRE-BUILD RULED (COMMIT, Q-96/Q-97/Q-98, v1.52 — batch selection + ruling set + files/AC normalizations, all within authority; nothing escalated).** The spine is complete (84/103); the remaining board is exactly five off-spine clusters. FIRST-LIVE-WEEK batch (≤5, dependency-ordered, readiness re-derived from the current graph — all deps done): **① TK-164 → ② TK-165 (voice-out, EP-30) → ③ TK-111 (behavior log, EP-21) → ④ TK-162 → ⑤ TK-163 (voice-in, EP-29)**. Rationale: voice-out is the highest user-visible payoff the moment Jim's OAuth bring-up lands (the morning brief SPOKEN via TK-101's waiting seam + drain surfacings spoken); TK-111 is time-critical (the reflection arc needs days of accumulated events — every un-built night delays EP-22's payoff, so the writer must be live from the first live week); voice-in completes FEAT-12's input half. **Q-96 (TK-164/165):** pyttsx3 local TTS behind a lazy TTSAdapter + NEW pyproject [voice] extra; SpeakSink = drain terminal `speak` via the TK-8-RESERVED compose Done→Transition flip (verified: no src consumer of last_output('compose'); Degraded(to=None) terminates like Done, engine.py:825); hold-silence is STRUCTURAL (holds acked at ReviewOrSpeak, never routed to compose) — TK-164 AC1/AC2 + TK-165 AC2 amended in place; the SAME adapter binds into build_brief_deliver_stage's speak seam (the Q-78 promise DISCHARGED); wire = Done(wombat.spoken_output). **Q-97 (TK-162/163):** faster-whisper local backend (weights download = install-time, no user-data egress, ASMP-1 intact), poll-shaped WOMBAT_ASR_DROP_DIR watched drop-directory (REFINES Q-86 — PushSource stands for genuinely push-shaped producers; registry untouched), injected Transcriber (micless tests), Q-67 loud-skip `_maybe_register_asr`, event_key = file-bytes sha256, payload user-facing-only, ItemKind.GENERIC; AC3 accessor corrected to source_ids. **Q-98 (TK-111):** DreamBehaviorLogStage inserted dream_tune→dream_behavior_log→dream_run (Q-91 one-terminal posture preserved; DreamTuneStage-parity loud-never-block failure posture); rows from the ACTIVE terminal OUTCOME_* class-subject claims (payload {item_ref,outcome,source,rule_name,resolved_at}, outcome_labeler.py:117-127); PK = item_ref (the canonical TK-12 key), source_id via NEW pure `split_idempotency_key` inverse in item_identity.py; duration_seconds NULL in v1 (honest — no signal exists); migration src/wombat/migrations/006 (ONE migrations home — files normalized, estimate S→M); Q-46 pg conventions; motive-freedom stays TK-43's type wall. **Deliberately deferred:** browser EP-25/26 (P3 + TK-151 deploy-flag bring-up + largest dep surface), TK-112–114 (need accumulated TK-111 data), TK-115–118 (no live-week visibility — natural next batch), TK-153 (blocked on TK-133). Every cited framework seam re-verified against ../cog_worx/cog-worx today. validate PASS (160 nodes/149 gov); gate PASS (analyze). | architect | Q-96, Q-97, Q-98, TK-164, TK-165, TK-111, TK-162, TK-163, Q-78, Q-86, Q-91, ISS-4, TK-8, TK-101, contract v1.52 |
| 2026-07-09 | **TK-164 recorded DONE (85/103) (COMMIT, v1.53 — build+verification record, within authority; status flip + record only) — voice-out opens the post-spine batch (EP-30): SpeakSink terse local-TTS output + the TK-101 brief speak-seam bind, built exactly per the Q-96 ruling.** New `src/wombat/sinks/` package: tts_adapter.py (TTSAdapter Protocol + Pyttsx3Adapter, pyttsx3 lazy-imported at construction, never at module import — a TTS-less machine boots clean) and speak.py (SpeakSink terminal drain-graph stage, name=`speak`, transitions=(); reads ctx.last_output('compose') and speaks the composed text VERBATIM exactly once iff voice_enabled AND adapter present; Degraded(to=None) with reason on ANY adapter failure, CancelledError re-raised; silent Done(spoken=false) on voice-off/no-adapter — voice is additive, never required for capability, CON-3 held: no model call, no journal touch, SpeakSink decides nothing). compose.py takes the TK-8-reserved flip transitions () → ('speak',) forwarding the byte-identical composed_output artifact; artifacts.py gains the `wombat.spoken_output` wire kind + round-trip helpers; bootstrap.py gains build_speak_sink(config). Structural hold-silence (Q-96) verified collision-free: ReviewOrSpeakStage.transitions == ('compose_dispatch',) — no clash with the new `speak` name; holds are acked at review_or_speak and can never reach compose/speak (32 review_or_speak regressions green). INDEPENDENTLY VERIFIED PASS — every AC's runnable check re-executed by the verifier: AC1 speaks-once-verbatim; AC2 hold/voice-off/no-adapter all silent; AC3 RuntimeError + lazy-import ImportError both → Degraded(to=None), never an escaped exception. Quality bar: 858 passed / 51 skipped (all WOMBAT_TEST_PG_DSN-gated), ruff clean, mypy Success 213 files src+tests. Scope = files_in_scope exactly; non-goals held (no cloud TTS, no ambient speech, no gate logic in the sink). validate PASS (160/149); gate PASS. NEXT in batch: TK-165. | architect | TK-164, Q-96, EP-30, TK-8, TK-101, CON-3, ISS-4, TK-165, contract v1.53 |
| 2026-07-09 | **TK-165 recorded DONE (86/103) (COMMIT, v1.54 — build+verification record, within authority; status flip + record only) — EP-30 VOICE-OUT CLOSES (both tickets done): SpeakSink gate-only tests land exactly as ruled at Q-96-as-amended.** New real test package tests/sinks/ (TK-15 convention — __init__.py; coexists with TK-164's tests/unit/test_speak_sink.py, both collected, no collision — 15 passed together) with 6 binary stub-count tests over a recording stub TTS adapter: AC1 speaks-once-VERBATIM on a gate-surfaced composed_output with voice on (call_count==1, exact text — the artifact-builder positional order verified against src so the assertion is real; Done wombat.spoken_output spoken=true degraded=false); AC2 Q-96 structural hold-silence — last_output('compose')=None → RuntimeError (TK-101 fail-loud precedent) AND call_count==0, the review_or_speak hold-ack regression cited not duplicated; AC3 voice_enabled=false → call_count==0, Done spoken=false (voice additive); AC4 parametrized ImportError + RuntimeError → Degraded(to=None) with reason, spoken=false/degraded=true, no escape, compose artifact byte-for-byte unaffected. INDEPENDENTLY VERIFIED PASS — verifier re-ran the full quality bar + every AC's runnable check: 864 passed / 51 skipped (all WOMBAT_TEST_PG_DSN gates), ruff clean, mypy Success 215 files src+tests. Scope = the 1 files_in_scope entry + package __init__.py, zero src edits, no contract edit by the builder. Non-goals held (no audio hardware, no e2e gate integration). The voice-out surface is built AND pinned: the sink can only speak what the deterministic gate already surfaced (CON-3/DEC-13). validate PASS (160/149); gate PASS (analyze). NEXT in the post-spine batch: TK-111 (behavioral event log, EP-21). | architect | TK-165, TK-164, Q-96, EP-30, TK-101, TK-15, CON-3, DEC-13, TK-111, contract v1.54 |
| 2026-07-09 | **TK-111 recorded DONE (87/103) (COMMIT, v1.55 — build+verification record, within authority; status flip + record only) — EP-21 OPENS: the durable, motive-free behavioral event log lands exactly as ruled at Q-98.** New `src/wombat/behavior/event_log.py` — BehaviorEventLog (Q-46 conventions verbatim: injected DSN, lazy psycopg v3 connection, module-level ensure_schema, close()-able) with upsert (INSERT..ON CONFLICT (idempotency_key) DO UPDATE) + events_between ascending read; new migration `src/wombat/migrations/006_behavior_events.sql` (next free slot after 001/003/004/005; ONE migrations home per Q-98e) — `wombat_behavior_events(idempotency_key TEXT PK, event_type, source_id, timestamp_utc, outcome_label, duration_seconds NULLABLE)`, NO motive column (motive-freedom stays TK-43's type-level wall, no second runtime gate). New pure inverse `split_idempotency_key(key) -> (source_id, natural_id)` in domain/item_identity.py (netstring decode, ValueError on malformed → row warned+skipped, never a partial write). New `DreamBehaviorLogStage` between dream_tune and dream_run (DreamTuneStage.transitions flipped to `('dream_behavior_log',)`; dream_run stays the ONE reachable terminal — Q-91 posture preserved); reads per-EventClass ACTIVE terminal OUTCOME_* claims via the DreamOutcomeStage claims_about idiom; row mapping per Q-98c (item_ref→PK, resolved_at→timestamp_utc, predicate→outcome_label, duration NULL in v1 — honest); DreamTuneStage failure parity (caught, LOUD, still transitions). Lifecycle: constructed UNCONDITIONALLY in assemble_runtime over the SAME runtime DSN + shared entity_kg/_RUNTIME_USER_ID, exposed as `RuntimeBundle.behavior_event_log`, closed in runtime.py teardown (TK-184 pattern). AC4 no-dashboard held STRUCTURALLY (only importers = dream_pathway.py + bootstrap.py, NG-3). VERIFIED PASS independently (throwaway docker pg :5540, DSN set so pg-gated tests genuinely RAN — pytest 969/4-skipped (only Google live smokes), ruff clean, mypy strict Success 221 files; AC1 upsert idempotency proven concretely; scope = 11 modified + 4 new, no builder contract edit). The corpus accumulates from the first live night — the time-value reason it was picked now (TK-112/TK-113 need weeks of events). validate PASS (160 nodes/149 gov); gate `tickets` PASS. NEXT in the post-spine batch: TK-162/TK-163 (voice-in, EP-29). |
| 2026-07-09 | **TK-162 recorded DONE (88/103) (COMMIT, v1.56 — build+verification record, within authority; status flip + record only) — VOICE-IN LANDS: ASRSource, the poll-shaped drop-directory InputSource over local faster-whisper, exactly as ruled at Q-97.** New `src/wombat/sources/asr.py` — Transcriber Protocol + FasterWhisperTranscriber (lazy import inside __init__ only) + ASRSource implementing InputSource DIRECTLY (poll-shaped, the Q-86-refining ruling — NOT PushSource): poll() scans WOMBAT_ASR_DROP_DIR non-recursively for wav/m4a/mp3/flac (case-insensitive), sha256(bytes) = event_key (content-hash dedupe across restarts via ALREADY_QUEUED), injected transcriber, move to processed/ or failed/ on caught per-file error (one bad file never kills the source; scan-level error degrades to [] loudly); payload {transcript, captured_at} ONLY (CON-1; no event_class → ItemKind.GENERIC fallback). Config gains optional wombat_asr_drop_dir + wombat_asr_model (not REQUIRED_ENV); `_maybe_register_asr` in sources/bootstrap.py with the exact Q-67 loud-skip shape (unset dir OR missing lib → loud skip, boot continues); faster-whisper appended to the SAME [voice] pyproject extra as pyttsx3 (never core — the Q-46/Q-72 clean-checkout bar holds). REGISTRATION-NOT-REWRITE HELD: registry.py + base.py EMPTY diffs, verifier grep of registry.py for 'asr' = zero hits — enqueue rides the ONE generic path via the canonical TK-12 key. INDEPENDENTLY VERIFIED PASS — every AC re-derived: AC1 one dropped .wav on a real SourceRegistry → exactly one QueueItem w/ canonical key; re-poll after move → []; identical-bytes re-drop → identical event_key (verifier's own 2-file probe); AC2 lesion voice-absent boot unaffected (+2 new bootstrap lesion tests); AC3 source_ids contains 'asr', no conditional logic. Quality bar: pytest 900/55 (all DSN gates), ruff clean, mypy Success 223 files strict src+tests. Scope = files_in_scope exactly + tests + uv.lock; no contract edit by the builder. Non-goals held (no wake-word/continuous listening — push-to-activate drop; no cloud ASR, CST-2; model-weight download = install-time, no user-data egress, ASMP-1 intact; no TTS). validate PASS (160/149); gate PASS (analyze). NEXT and LAST in the post-spine batch: TK-163 (ASR smoke + lesion, closes EP-29). | architect | TK-162, TK-163, Q-97, Q-86, Q-67, EP-29, TK-12, CON-1, CST-2, ASMP-1, contract v1.56 |
| 2026-07-09 | **TK-163 recorded DONE (89/103) (COMMIT, v1.57 — build+verification record, within authority; status flip + record only) — EP-29 VOICE-IN CLOSES and the post-spine first-live-week batch COMPLETES 5/5 (TK-164, TK-165, TK-111, TK-162, TK-163).** ASR smoke + lesion tests land exactly per Q-97-as-ruled: NEW `tests/sources/test_asr_source.py` (5 tests) proving voice-in works MICLESS and voice absence degrades cleanly. AC1 (2 tests) — fake Transcriber + one dropped file through a REAL SourceRegistry poll tick over a recording enqueuer → exactly ONE QueueItem, idempotency_key == derive_key('asr', sha256(bytes)) via the canonical TK-12 function, transcript carried; no-asr-specific-dispatch proven STRUCTURALLY ('asr' never appears in registry.py — one generic enqueue call site, verifier re-derived at source); second poll enqueues nothing (real loop after processed/ move + deterministic direct poll/poll test). AC2 (2 tests, S8 lesion) — WOMBAT_ASR_DROP_DIR unset (config default None) → 'asr' absent from source_ids, exactly one WARNING naming the env var (filter specific — gcal/gmail/feedback skips excluded); calendar item drains normally with voice absent, nothing raises on missing ASR (CON-3; voice additive, never required). Plus per-file-failure lesion — transcription failure warns loud, moves to failed/, never kills the source. INDEPENDENTLY VERIFIED PASS — verifier re-ran the full bar: pytest 905/55 (all DSN gates), ruff clean, mypy Success 224 files strict src+tests, targeted file 5 passed. Scope = the single files_in_scope entry EXACTLY (one new test file, zero src edits, no contract edit by the builder). Non-goals held (no real mic/audio hardware, no TTS). validate PASS (160/149); gate PASS (analyze). FEAT-12's input half is built and pinned; the batch board is CLEAR — next candidates: reflection arc TK-112..TK-118 (over accumulating TK-111 data), browser EP-25/26 (deferred per v1.52), TK-153 (blocked on TK-133). | architect | TK-163, TK-162, Q-97, EP-29, FEAT-12, TK-12, CON-3, contract v1.57 |
| 2026-07-09 | **REFLECTION-ARC BATCH SELECTED + PRE-BUILD RULED (COMMIT, Q-99, v1.58 — batch selection + ruling set + files_in_scope normalization, all within authority; nothing escalated).** Post-spine batch complete (89/103); the buildable board is exactly the reflection arc TK-112..TK-118 (EP-21..24) — browser EP-25/26 stays deferred (P3 + TK-151 deploy-flag bring-up), TK-153 blocked on TK-133, OAuth live smokes = operator step. Batch of 5, dependency-ordered: **① TK-115 (KB YAML + schema + loader — the arc's only leaf) → ② TK-116 (pattern_warrants_nudge pure fn) → ③ TK-117 (extract_phrasing_hints pure lookup) → ④ TK-112 (window detector + nightly write stage) → ⑤ TK-113 (pattern detector + gated reflection enqueue)**. TK-114 + TK-118 deferred as ONE coupled pair (TK-114 owns TK-118's per-turn contributor construction and depends on TK-113 anyway; next batch gets a fresh cog-worx contributor-seam verification + the ReflectionCompose routing ruling against compose_dispatch_router). **Q-99 rules the five cross-ticket seams (all verified at as-built source):** (a) TK-115 = NO pydantic runtime dep (clean-checkout bar); frozen dataclass KBEntry + module-local ValidationError(ValueError); packaged YAML via importlib.resources; closed operator set; tests/kb/ pytest = the CI check. (b) CLOSED v1 metric vocabulary {switch_rate, window_count, event_count} binds TK-115 seeds, TK-113 derivation, TK-116 evaluation (missing metric → False, empty KB → False — silence default). (c) NEW ClaimPredicate.PRODUCTIVITY_WINDOW (deliberate ruled vocabulary change; behavior-aggregate, CON-6/NG-1 wall intact); one claim per night, subject productivity_window:<wombat-date>, via the shared ObservationWriter; newest-ACTIVE point read by date. (d) Detector = pure partition of events into gap-split (30-min) non-overlapping windows; frozen WindowSummary, no motive field. (e) Dream-graph DOUBLE SPLICE: dream_behavior_log → dream_window (TK-112) → dream_pattern (TK-113) → dream_run — one reachable terminal preserved (Q-91/Q-98 posture), DreamTuneStage failure parity. (f) TK-113 enqueues via an INJECTED enqueue callable (TK-99 precedent; bootstrap passes the ONE WombatQueue.enqueue, ASMP-2 held); idempotency_key('wombat.reflection', wombat-date) = STRUCTURAL once-per-night (re-fire → ALREADY_QUEUED; residual post-ack re-fire converges via TK-181's idempotent pending re-add — recorded, not re-engineered); payload motive-free with item_kind/event_class 'reflection' — BOTH already in the closed vocabularies (no bump; TK-185 put REFLECTION at the muted band edge); NO gate bypass (enqueue-only, DEC-13 supremacy). files_in_scope normalized: TK-112 += claims.py + dream_pathway.py + bootstrap.py; TK-113 += dream_pathway.py + bootstrap.py. validate PASS (160 nodes/150 gov); gate PASS (analyze). | architect | Q-99, TK-112, TK-113, TK-115, TK-116, TK-117, TK-114, TK-118, Q-98, TK-181, TK-185, TK-99, DEC-13, CON-6, NG-2, NG-3, contract v1.58 |
| 2026-07-09 | **TK-115 recorded DONE (90/103) (COMMIT, v1.59 — build+verification record, within authority; status flip + record only) — THE REFLECTION-ARC BATCH OPENS 1/5 (EP-23): the psychology KB lands exactly per the Q-99a/b rulings.** New `src/wombat/kb/` — schema.py with NO pydantic (frozen dataclasses `GateCondition{metric,operator,threshold}` + `KBEntry{pattern_id,description,gate_condition,phrasing_hints,autonomy_level,evidence_tag,version}`, module-local `ValidationError(ValueError)`, and the CLOSED `METRIC_VOCABULARY` {switch_rate, window_count, event_count} + `OPERATOR_VOCABULARY` {>,>=,<,<=,==} that TK-116 imports; the ticket's stale 'one Pydantic schema' complexity_budget prose is superseded by Q-99a — recorded in the v1.59 changelog, node text not edited); loader.py `load_psychology_kb(path=None) -> list[KBEntry]` over the packaged `psychology_kb.yaml` via importlib.resources (the event_log ensure_schema idiom; explicit path tests-only), validating version + every field + vocab membership + non-empty phrasing_hints — missing/malformed raises clean FileNotFoundError/ValidationError → gate conditioning falls back to no-nudge (safe default); seed entries all human-evidence-tagged. Verifier independently re-derived every AC: AC1 typed load + parametrized missing-field/out-of-vocab/empty-hints raises + default==explicit path; AC2 recursive raw-YAML walk (no generated_by/model_authored key; TECH-13/CON-6); AC3 clinical-language linter (NG-2); AC4 clean failure. Grep proves pydantic appears only in docstring prose, never imported (clean-checkout bar held). 927 passed / 55 skipped (905 + exactly the 22 new kb tests; skips = documented DSN/Google-live gates), ruff clean, mypy strict Success 229 files. validate PASS (160 nodes/150 gov); gate PASS. NEXT: TK-116. | architect | TK-115, Q-99, EP-23, TECH-13, CON-6, NG-2, contract v1.59 |
| 2026-07-09 | **TK-116 recorded DONE (91/103) (COMMIT, v1.60 — build+verification record, within authority; status flip + record only) — REFLECTION-ARC BATCH 2/5 (EP-23): the KB gate-conditioning hook lands exactly per the Q-99b/h ruling.** New src/wombat/kb/gate_conditioning.py — ONE pure `pattern_warrants_nudge(metrics, kb) -> bool` (the exact ruled signature — pure bool, NO pattern_id in the return; TK-113 resolves which pattern via single-entry iteration): absent metric key = not-satisfied, silently continues (CON-3 — the function never raises); the closed operator vocabulary {>,>=,<,<=,==} dispatched via stdlib operator functions (safe by construction — TK-115's loader validates every operator at load time, before any entry can reach this function); True on first match, False on no match or empty KB (no KB = no nudges = the system defaults to silence). Zero model/LLM/clock/IO/logging — pure comparison (CON-1/CON-2/NG-4 no-LLM, CON-6/NG-1 no motive, NG-2 no clinical matching all held structurally; surfacing stays the gate's, EP-9). Independently verified PASS — the verifier re-ran the full quality bar itself (pytest 938 passed / 55 skipped, every skip the documented WOMBAT_TEST_PG_DSN gates; ruff clean src+tests; mypy Success 231 files, zero errors) and hand-traced all 10 AC4 fixtures against the real 7-entry seed KB (6 True / 4 False; the boundary fixture correctly False under strict operators; deterministic over two passes); all 5 closed operators exercised parametrized. Scope exactly the single files_in_scope entry + tests/kb/test_gate_conditioning.py; no contract edit by the builder — the process rule holding. validate PASS (160 nodes/150 gov); gate PASS. NEXT in batch: TK-117 (phrasing-hint extractor, EP-24). | architect | TK-116, Q-99, EP-23, CON-3, TK-115, contract v1.60 |
| 2026-07-09 | **TK-117 recorded DONE (92/103) (COMMIT, v1.61 - build+verification record, within authority; status flip + record only) - REFLECTION-ARC BATCH 3/5 (EP-24): the phrasing-hint extractor lands exactly per the Q-99 build order.** New src/wombat/kb/phrasing_hints.py - ONE pure `extract_phrasing_hints(pattern_id, kb) -> list[str]`: deterministic first-match-wins lookup over the TK-115 KBEntry sequence (documented tie-break rule - loader.py does not enforce pattern_id uniqueness; the 7 seed ids verified unique today); unknown pattern_id or empty KB -> [] with no exception (TK-114 safe default prompt); always a FRESH list(entry.phrasing_hints) - no shared mutable state, identical inputs give equal non-identical lists; zero model calls, zero I/O (CON-1/NG-4); the no-verbatim-in-output guard stays TK-114/TK-118 and the no-clinical-language-in-hint-DATA guard stays TK-115, exactly as the trimmed AC3 rules. Independently verified PASS - verifier re-derived every AC under own execution (AC1 all 7 seed pattern_ids -> non-empty 2-item deterministic hints over the REAL load_psychology_kb(); AC2 unknown/empty -> []; AC3 equal-but-fresh lists); 945 passed / 55 skipped (all WOMBAT_TEST_PG_DSN gates), ruff clean, mypy Success 233 files, tests/kb 40 passed; scope exactly files_in_scope + tests/kb/test_phrasing_hints.py, no contract edit by builder, no scope creep. NEXT in batch: TK-112 (productivity-window detector + nightly write stage, EP-22). |
| 2026-07-09 | **TK-112 recorded DONE (93/103) (COMMIT, v1.62 - build+verification record, within authority; status flip + record only) - REFLECTION-ARC BATCH 4/5 (EP-21): the productivity-window detector + nightly write land exactly per the Q-99c/e rulings.** New src/wombat/behavior/window_detector.py - ONE pure `detect_productivity_windows(events) -> list[WindowSummary]` (sort by timestamp_utc; split into maximal runs wherever the inter-event gap strictly exceeds module constant WINDOW_GAP_SECONDS=1800.0; WindowSummary frozen + motive-free: start_utc/end_utc/event_count/switch_rate/outcome_mix, NO motive field per CON-6/NG-1; empty log -> [] no-raise; window_summary_to_dict = the JSON-native wire shape). New src/wombat/behavior/stages/write_window_summaries.py - `WriteWindowSummariesStage` (name=`dream_window`): reads store.events_between(now-14d, now), runs the detector, and ONLY when non-empty writes ONE Claim via the injected ObservationWriter (subject `productivity_window:<wombat-date>`, predicate = the ruled NEW closed-enum ClaimPredicate.PRODUCTIVITY_WINDOW in claims.py, value = JSON window-dict list) - retrievable by date without a full log scan (AC2 per the audit split fix; the READ belongs to TK-113). Dream graph takes the Q-99e splice: dream_consolidate -> dream_outcome -> dream_tune -> dream_behavior_log -> dream_window -> dream_run (dream_run stays the ONE reachable terminal; dream_pattern arrives with TK-113); the stage is homed with the event log it reads (wombat.behavior.stages), not in dream_pathway.py; bootstrap wires SHARED store/writer instances. Independently verified PASS - verifier re-derived every AC under own execution (AC1 partition/no-overlap invariant + covered-time sum with None-duration->0; AC2 date-keyed persistence round-trip of this ticket's own write; AC3 empty-log clean continue; AC4 no-surface/render/dashboard guard, NG-3); ruff clean; mypy Success 239 source files (src+tests strict); pytest with throwaway postgres:16 DSN -> 1034 passed / 4 skipped (live-Google only) / 1 failed = test_outbound_wiring_e2e.py::test_ac3_google_less_boot_loud_skips_outbound_wiring, PRE-EXISTING + UNRELATED (fails identically on base with TK-112 stashed; env-dependent Gmail-OAuth warning-branch assertion in an untouched file). Scope exactly the 5 files_in_scope + behavior/__init__ docstring + 6 mechanically-updated regression tests; no contract edit by builder, no migrations, no new pg table. validate PASS (160 nodes/150 gov); gate PASS. NEXT in batch (5/5, LAST): TK-113 (pattern detector + gated once-per-night reflection enqueue, EP-22). | architect | TK-112, Q-99, EP-21, TK-111, TK-113, CON-6, NG-1, NG-3, contract v1.62 |
| 2026-07-09 | **TK-113 recorded DONE (94/103) (COMMIT, v1.63 - build+verification record, within authority; status flip + record only) - REFLECTION-ARC NIGHTLY BATCH COMPLETE 5/5 (EP-22): the pattern detector + gated once-per-night reflection enqueue land exactly per the Q-99 rulings.** New src/wombat/behavior/stages/pattern_detector.py - `PatternDetectorStage` (name=`dream_pattern`), deterministic, spliced dream_window -> dream_pattern -> dream_run (the second half of the Q-99e double splice; dream_run stays the ONE reachable terminal). Point-reads the ACTIVE productivity_window claim TK-112 wrote (TK-112 owns the write, TK-113 owns the read); derives the closed 3-key metrics {switch_rate, window_count, event_count}; matches the injected psychology KB in file order via TK-116 pattern_warrants_nudge; enqueues AT MOST ONE pattern_reflection QueueItem per wombat-night through the injected SHARED WombatQueue.enqueue, keyed idempotency_key('wombat.reflection', date) - same-night re-fire structurally collapses to ALREADY_QUEUED (once-per-night is STRUCTURAL, not counted). claims read/parse failure + QueueFullError caught, logged loud, counted as errors, NEVER block the transition to dream_run (CON-3). No gate/pending/journal collaborator, no model call (NG-4); payload exactly {item_kind, event_class, kind, pattern_id, window_ref, date} - NO motive/clinical/why (CON-6/NG-1/NG-2); ctor params {entity_kg, kb, enqueue, user_id, tz} = NO gate-bypass ability - the item takes the standard Urgency x Cognitive Load gate like any other (real payload flows gate_item_from_queue_item -> ItemKind.REFLECTION, resolve_event_class -> EventClass.REFLECTION). Independently verified PASS - full quality bar under the verifier's own execution: pytest 963 passed / 57 skipped (all WOMBAT_TEST_PG_DSN-gated, expected); ruff clean; mypy no issues 241 source files (src+tests); throwaway postgres:16 on :5439 ran EVERY pg-gated test green incl. test_pg_gated_real_wombat_queue_enqueue_bind (real WombatQueue: QUEUED then ALREADY_QUEUED on re-fire, result2 enqueued==0); AC1 idempotency key re-computed independently; AC2 KB thresholds re-derived (switch_rate 0.8 trips rapid_context_switching, first in file order > 0.6; 0.3/2/10 trips none of the 7 seeds - zero enqueues AND Transition(to='dream_run') both paths). Scope = files_in_scope (pattern_detector.py NEW; dream_pathway.py + bootstrap.py splice/wiring) + its test module + mechanically-updated regression tests; no contract edit by builder, no migrations, no new pg table. validate PASS (160 nodes/150 gov); gate PASS. REFLECTION-ARC REMAINING: the deferred TK-114+TK-118 compose/contributor pair (morning render half) - needs the fresh cog-worx contributor-seam verification + the compose_dispatch_router bind ruling before build. | architect | TK-113, Q-99, EP-22, TK-112, TK-116, CON-3, CON-6, NG-1, NG-2, NG-4, contract v1.63 |
| 2026-07-09 | **GOVERNANCE RECORDING v1.64 — Jim's voice-provider + companion-app direction (COMMIT; DEC-28/29/30 within the Jim-originated frame, DEC-31 OPERATOR-DECIDED, DEC-32 + Q-100 within authority).** DEC-28: voice egress = structural per-provider user opt-in (config selection AND user-supplied key or NO construction); DEFAULT config keeps exactly ONE egress (DeepSeek); degrade strictly cloud→local; supersedes-in-part Q-96/Q-97 local-only absolutes (rescoped to default-config invariants); storage residency (CST-2/NG-7) untouched. DEC-29: NG-3 CLARIFIED not superseded — settings app admitted with a zero-analytics hard non-goal. DEC-30: distribution seed = config surface only in v1 (no accounts/tenancy/packaging; ASMP-2 stands). DEC-31: Electron + React (Vite+TS) shell, Jim's ruling (app-as-wombat's-front-end trajectory incl. future 3D avatar); architect's SPA alternative recorded considered-and-rejected-by-operator (dissent-in-brief in the ADR); accepted costs = Node scoped to app/, Chromium update cadence (RISK-11), eventual electron-builder; v1 dev-launch only; Python settings API = the entire backend, spawned by the Electron main, port+token via contextBridge only. DEC-32: keyring secrets + wombat.settings.json non-secrets (env > .env > settings.json > defaults) + .env never app-written; restart-to-apply. Q-100 resolved: thin httpx REST, no vendor SDKs, one [voice-cloud] extra. Q-101 resolved-by-routing → DEF-7 (JIM-OWNED: per-provider live-smoke keys; armed loud-skipping smokes, Fish first). RISK-11 accepted. NEW: FEAT-13; EP-31 = TK-186 (load_config .env-only-boot fix, FIRST — live bug) → TK-187 → TK-188 → TK-189 → TK-190 → TK-191 → TK-192 → TK-193 → TK-194 → TK-195; EP-32 = TK-196 → TK-197 → TK-198 → TK-199 → TK-200 → TK-201. Board 94/119. validate VALID (179 nodes, 159 governance); gate_check analyze PASS. Creep flagged OUT: streaming/wake-word STT, voice-catalog preview, app-editing DeepSeek/Google/DSN, packaging/installer, hot reload, the 3D avatar build itself, CI gating on app/. | architect (Jim direction via coordinator) | DEC-28..DEC-32, Q-100, Q-101, DEF-7, RISK-11, FEAT-13, EP-31, EP-32, TK-186..TK-201 |
| 2026-07-09 | **EP-31-OPEN + REFLECTION-RENDER BATCH SELECTED + PRE-BUILD RULED (COMMIT, Q-102, v1.65 — batch selection + ruling set + files_in_scope normalization, all within authority; nothing escalated; orchestrator-authorized sequencing under Jim's delegation).** Batch of 5, dependency-ordered: **① TK-186 (load_config drops the pre-pydantic os.environ REQUIRED_ENV check — the live bug that bit Jim 2026-07-09; config.py L75 verified) → ② TK-118 (PhrasingHintContributor) → ③ TK-114 (ReflectionComposeStage — the coupled pair closes FEAT-8's morning-render half) → ④ TK-187 (voice/persona config fields) → ⑤ TK-188 (voice key vault on the gcal TokenStore pattern)**. Rationale: the live bug goes first; the twice-deferred reflection pair closes an arc whose nightly half (TK-113) already enqueues items nothing renders properly; TK-187/TK-188 open EP-31's foundation and are buildable without Jim's provider keys (DEF-7). **Q-102 (every seam verified at ../cog_worx/cog-worx source today):** contribute is ASYNC (request, allocation) → SlotContent{chunks: SlotChunk{text,key,source_slot}}; ContextRequest frozen, NO pattern_id (Q-31 holds — pattern_id travels by per-turn construction); **assemble() never reads request.instructions** (only the task slot is synthesized) → the no-clinical system prompt rides a static head-band contributor; dedicated PER-TURN LOCAL ContextAssembler in ReflectionCompose.run() = the structural TK-118 AC3 scoping; slots instructions(head,req)/reflection_hints(head,pref)/task(tail,req); head band → ONE system message, so hints are prompt guidance never task text; bind = composer_by_kind[ItemKind.REFLECTION]='reflection_compose' unconditional (DraftComposer precedent; router fallback keeps unknown kinds safe); **TERMINAL Done(COMPOSED_OUTPUT, ItemKind.REFLECTION)** — no speak routing in v1 because SpeakSink's wire is stage-name-addressed last_output('compose') (verified); voice reflections = flagged follow-up, refused as creep; degrade posture = ComposeStage parity (2.0s wait_for; ANY assembler/mouth/blank failure → local pure pattern_id-derived terse fallback — NOT TemplateComposer's payload dump; never raises; CancelledError re-raised); bootstrap KB load caught-loud → empty-KB construction (boot never dies for a reflection reason, CON-3); TK-186/187/188 need no new ruling (DEC-32 env→keyring→absent order, Q-100 transport, config.py optional-field precedent bind them). **Deliberately deferred:** TK-189..TK-195 + EP-32 (next batch after the arc-close cross-cutting review; chain-gated on TK-188/TK-193 anyway), browser EP-25/26 (P3 + TK-151 deploy-flag bring-up), TK-153 (blocked on TK-133), per-provider live smokes (DEF-7, Jim-owned operator step). validate PASS (179 nodes/160 gov); gate PASS (analyze). | architect | Q-102, Q-31, Q-50, Q-99, Q-100, DEF-7, TK-186, TK-118, TK-114, TK-187, TK-188, CON-3, NG-2, EP-31, FEAT-8, contract v1.65 |
| 2026-07-09 | **TK-118 recorded DONE (96/120) (COMMIT, v1.67 - build+verification record, within authority; status flip + record only) - REFLECTION-RENDER BATCH 2/5 (EP-24): the phrasing-hint ContextContributor lands exactly per the Q-102 seam rulings.** New src/wombat/kb/contributors/ - PhrasingHintContributor on the REAL async cog-worx ContextContributor Protocol (isinstance-proven); pattern_id bound BY CONSTRUCTION (frozen ContextRequest has no pattern_id - Q-31 holds); contribute() wraps TK-117 extract_phrasing_hints into one SlotChunk per hint (reflection_hints slot); empty -> status=empty no-raise; any KB exception -> status=degraded NEVER raised (S8/CON-3); scoping STRUCTURAL via TK-114 dedicated per-turn reflection assembler (nothing global); zero model calls/IO (NG-4/CON-1); no clinical/motive language of its own (NG-2/CON-6). Independently verified PASS - AC1 on the real seam (real seed KB + real ContextAssembler, assembled head system message carries every hint); mypy Success 245 files strict-clean; ruff clean; tests/kb 50 passed; full suite 963/57/14-failed = EXACTLY the known Q-103 pre-existing environment set, re-proven by moving TK-118 files out of the tree (12/12 rerun subset still fail with zero TK-118 code) - read per the v1.66 Q-103 closeout protocol pending TK-202. Board 96/120. NEXT in batch (3/5): TK-114 (ReflectionComposeStage - the morning-render half of FEAT-8). | architect | TK-118, TK-117, TK-114, EP-24, Q-102, Q-31, Q-103, contract v1.67 |
| 2026-07-09 | **TK-114 recorded DONE (97/120) (COMMIT, v1.68 - build+verification record, within authority; status flip + record only) - REFLECTION-RENDER BATCH 3/5: the morning-render half of FEAT-8 CLOSES (the ISS-5 reflection path reaches the user).** ReflectionComposeStage per the Q-102 rulings: terminal (transitions=()), per-turn three-slot ContextAssembler (instructions/reflection_hints via seeded TK-118 contributor/synthesized task), exactly ONE bounded ctx.model.complete; ANY failure -> local deterministic humanized-pattern_id fallback, never raises except CancelledError (CON-3). Bootstrap: unconditional composer_by_kind[ItemKind.REFLECTION], caught-loud load_psychology_kb -> kb=[] safe default, both drain-graph branches. Independently verified PASS - scope clean (4 files, no contract touch); ruff clean; mypy 247 files strict; 13/13 tests; AC1 one-call + no pattern_id/window_ref in the user message (CON-1/Q-50); AC2 classifier clean both paths (NG-2/CON-6); AC3 exception/timeout/assembly-failure all degrade non-blank, model never called on assembly failure; AC4 kb=[] safe default + hints never verbatim (discharges TK-118 AC4). Closeout gate 976/57/14 = exactly the Q-103 environment set, zero new failures (v1.66 protocol pending TK-202). Board 97/120. NEXT in batch (4/5): TK-187 (voice/persona config surface, EP-31). | architect | TK-114, TK-118, TK-117, EP-22, EP-24, FEAT-8, ISS-5, Q-102, Q-50, Q-103, contract v1.68 |
| 2026-07-09 | **TK-187 recorded DONE (98/120) (COMMIT, v1.69 - build+verification record, within authority; status flip + record only) - v1.65 BATCH 4/5 (EP-31): the voice/persona config surface lands per the DEC-28 structural-opt-in frame.** config.py +8 OPTIONAL fields NOT in REQUIRED_ENV (stt/tts provider Literal local|deepgram|elevenlabs|fish default local - closed vocabulary at field level, typo fails loud at boot; tts_voice_id; stt_model cloud-override distinct from local asr_model; 3 SecretStr|None key overrides; assistant_name default Steward); load_config ValidationError handling extended past the TK-186 missing-var path to name any offending field upper-case in ConfigurationError; .env.example commented lines + 3 tests. Independently verified PASS - scope exactly the 3 files_in_scope, zero provider/keyring/prompt/UI code (deferred to TK-193/TK-188/TK-194/EP-32); ruff clean; mypy Success 247 files src+tests strict (standalone import-untyped hit diagnosed as an out-of-project run artifact); AC1 fully-offline defaults byte-preserved, AC2 populated+typed masked SecretStr, AC3 nonsense provider fails loud naming the variable. Architect closeout gate 979/57/14 = exactly the Q-103 environment set (979 = 976 + 3 new; zero new failures), v1.66 protocol pending TK-202. The DEC-28 zero-egress default is now a TYPED invariant at the config layer. Board 98/120. NEXT in batch (5/5, LAST): TK-188 (voice provider key vault, EP-31). | architect | TK-187, TK-186, TK-188, TK-193, TK-194, EP-31, EP-32, DEC-28, Q-103, contract v1.69 |
| 2026-07-09 | **TK-188 recorded DONE (99/120) (COMMIT, v1.70 - build+verification record, within authority; status flip + files_in_scope normalization + record only) - v1.65 BATCH 5/5 COMPLETE (EP-31): the voice provider key vault lands exactly per DEC-32 on the gcal token_store pattern.** New src/wombat/voice/key_store.py - VoiceKeyStore Protocol (get/set/delete, per-provider) + KeyringVoiceKeyStore adapter (service wombat, account voice-<provider>-api-key, the SAME OS credential vault the Google OAuth tokens use; WRITE failures raise loud VoiceKeyStoreError for TK-197 to surface, delete-of-absent is a no-op) + resolve_provider_key as the ONE resolution seam (non-blank env override wins > keyring > None; ANY vault READ failure = exactly one loud WARNING + None, never crashes boot - CON-3). Independently verified PASS - all three ACs re-derived under the verifier own execution; scope exactly the declared new files; ruff clean; mypy Success 251 files src+tests strict. files_in_scope normalized += tests/voice/__init__.py (the TK-15 real-package convention). Closeout gate 988/57/14-failed = EXACTLY the known Q-103 environment set (988 = 979 + 9 new; zero new failures), read per the v1.66 protocol pending TK-202. The v1.65 batch is CLOSED (TK-186/TK-118/TK-114/TK-187/TK-188). Board 99/120. NEXT - the arc-close review pass, then the provider chain TK-189..TK-195 + EP-32. | architect | TK-188, TK-187, TK-197, TK-202, EP-31, DEC-32, CON-3, Q-103, contract v1.70 |
| 2026-07-09 | **CR3 ROUTED + FIX-THEN-VOICE BATCH SELECTED + PRE-BUILD RULED (COMMIT, Q-104, v1.71 — review routing + batch selection + ruling set + files_in_scope normalization, all within authority; nothing escalated; orchestrator-authorized sequencing under Jim's delegation).** All 4 CONFIRMED pass-3 findings routed: CR3-1 → NEW TK-203 (P1, EP-1: schema pre-flight — ensure_all_schemas(dsn) in new src/wombat/schema_preflight.py over the five verified packaged ensure_schema functions, called first in assemble_runtime gated on replay_pending; the 2026-07-09 ~19:05 live first-boot UndefinedTable crash gets its structural fix); CR3-2+CR3-3 → NEW TK-204 (P2, EP-22, TK-171/TK-172 paired-finding precedent: dream_pattern enqueue guard broadened to except-Exception beside the kept QueueFullError arm — engine default verified at installed cog-worx retry.py DEFAULT_RETRY_POLICY retryable=(), an uncaught raise is RUN_CRASHED — plus kb/loader.py numeric coercions re-raised as ValidationError naming entry/field); CR3-4 → folded into TK-202 (intent amended). Pattern-setter rulings: TK-189 transport → NEW src/wombat/voice/transport.py, lazy httpx, voice-cloud extra (httpx only, Q-100), api_key as plain ctor arg; TK-191 AudioPlayer Protocol + WinsoundPlayer (stdlib), raise-into-SpeakSink degrade verified at speak.py:79-96. BATCH order TK-204 → TK-203 → TK-202 → TK-189 → TK-191 (TK-204 first — tonight is the first real dream run); after TK-202, the Q-103 known-14 closeout allowance ENDS. Deferred: TK-190/192/193/194/195 + EP-32, browser EP-25/26 + TK-153, DEF-7 live smokes. |
| 2026-07-09 | **TK-204 recorded DONE (100/122) (COMMIT, v1.72 - build+verification record, within authority; status flip + record only) - FIX-THEN-VOICE BATCH 1/5 (EP-22): reflection-arc degrade parity per the Q-104 CR3-2+CR3-3 routing - the live hole ahead of tonight first real dream run is CLOSED.** pattern_detector.py gains a broad except-Exception arm after the verbatim-kept QueueFullError arm on the same try around self._enqueue(item) (log loud with pattern_id + exc_info, errors += 1, fall through to the unchanged Transition to dream_run) - run() can never raise for an enqueue reason, so a generic pg fault degrades like every sibling dream stage instead of RUN_CRASHED; no retry_policy; CancelledError untouched. kb/loader.py float(threshold)/int(file_version) coercions wrapped and re-raised as the loader ValidationError naming entry/field, chained from exc - a KB numeric typo now degrades to the empty-KB boot via bootstrap existing catch sites instead of killing the process; seed YAML untouched. Independently verified PASS - both ACs + full quality bar re-executed; scope exactly the 4 files_in_scope; ruff clean; mypy Success 251 files strict. Closeout gate 993/57/14-failed = 988 + 5 new passing, ZERO new failures (Q-103 environment set, v1.66 protocol - allowance ends after TK-202). Board 100/122. NEXT in batch (2/5): TK-203 (schema pre-flight, EP-1, P1). | architect | TK-204, TK-203, TK-202, TK-113, TK-115, EP-22, Q-103, Q-104, CON-3, contract v1.72 |
| 2026-07-09 | **TK-203 recorded DONE (101/122) (COMMIT, v1.73 - build+verification record, within authority; status flip + files_in_scope normalization + record only) - FIX-THEN-VOICE BATCH 2/5 (EP-1, P1): the schema pre-flight lands per the Q-104 CR3-1 routing - the 2026-07-09 ~19:05 live first-boot UndefinedTable crash gets its structural fix; first boot and every later boot are now the same command.** New src/wombat/schema_preflight.py - ensure_all_schemas(dsn): ONE context-managed psycopg connection runs the FIVE packaged ensure_schema(conn) functions (queue, domain/daily_ledger, gate/pending_journal_pg, behavior/event_log, trail/schema), commits, closes; each CREATE ... IF NOT EXISTS so a second call is a safe no-op - no migration framework/version table/cog-worx substrate schema (non_goals held). assemble_runtime calls it as the FIRST pg act gated on replay_pending, immediately before the TK-166 eager replay (the exact crash site); replay_pending=False stays connection-free as documented. Independently verified PASS - scope exactly the 5 files (2 new + bootstrap.py + 2 mechanical test companions; files_in_scope normalized += tests/unit/test_bootstrap.py + tests/behavior/test_event_log.py, architect act, v1.66 precedent), no contract/.env/live-db touch; ruff clean; mypy Success 253 files src+tests strict; AC1 on throwaway postgres:16 - all five tables created (names cross-checked against migration DDL 001/003/004/005/006), second call idempotent; AC2 unreachable-dsn replay_pending=False assembles connection-free; AC3 pre-existing rows untouched. Architect closeout gate 994 passed / 61 skipped / 14 failed = 993 + 1 non-gated new (+4 pg-gated skips without DSN), ZERO new failures - exactly the known Q-103 environment set, stash-proven, v1.66 protocol (allowance ENDS after TK-202). validate PASS (182 nodes/162 gov); gate PASS (analyze). Board 101/122. NEXT in batch (3/5): TK-202 (suite hermeticity - after it the live-machine closeout gate must read FULLY green). | architect | TK-203, TK-202, TK-166, CR3-1, Q-104, Q-103, Q-46, EP-1, contract v1.73 |
| 2026-07-09 | **TK-202 recorded DONE (102/122) (COMMIT, v1.74 - build+verification record, within authority; status flip + record only) - FIX-THEN-VOICE BATCH 3/5 (EP-1): suite hermeticity vs the live operator machine lands per the Q-103 resolution + the Q-104 CR3-4 fold - THE KNOWN-14 CLOSEOUT ALLOWANCE ENDS; the live-machine gate must now read FULLY green, and it DOES (1008 passed / 61 skipped, every skip the documented WOMBAT_TEST_PG_DSN gate).** TEST-ONLY, zero src edits: FIX 1 env-file isolation (8 tests) - monkeypatch.chdir(tmp_path) inline (the CR3-4 test in tests/unit/test_bootstrap.py + tests/sources/test_asr_source.py) or via a new opt-in non-autouse _no_env_file fixture (tests/unit/test_runtime.py), so pydantic-settings' CWD-relative env_file=.env can never leak operator values (TK-186 precedent, no autouse magic); FIX 2 robust lib-absence shim (6 tests) - _simulate_absent (sys.modules delitem + meta-path _BlockedFinder raising ModuleNotFoundError, duplicated locally per file) replaces the assume-not-installed pyttsx3/faster-whisper pattern. Independently verified PASS (re-derived, builder claims not trusted): scope exactly the six files_in_scope, all under tests/, src+planning diff empty, the stray dirty config.py/test_config.py reverted; ruff clean; mypy Success 253 files src+tests strict; LOAD-BEARING stash proof - the diff stashed reproduces EXACTLY the known 14 on base, the fix drives them to 0; AC2 clean-checkout neutrality by construction (chdir-guarded tests already run in the empty-tmp_path no-.env regime). The v1.66 read-against-known-14 protocol is RETIRED - any future closeout red is a REAL failure. Board 102/122. NEXT in batch (4/5): TK-189 (voice transport primitive + Deepgram STT pattern-setter, EP-31). | architect | TK-202, TK-186, TK-189, CR3-4, Q-103, Q-104, Q-46, EP-1, contract v1.74 |
| 2026-07-09 | **TK-189 recorded DONE (103/122) (COMMIT, v1.75 - build+verification record, within authority; status flip + files_in_scope normalization (adds uv.lock, as-built) + record only) - FIX-THEN-VOICE BATCH 4/5 (EP-31): the cloud-STT pattern-setter lands per the Q-100/Q-104 pre-build rulings - the thin-REST-no-vendor-SDK shape every later cloud provider (TK-190/TK-191/TK-192) rides is SET.** NEW src/wombat/voice/transport.py (VoiceTransport Protocol + VoiceTransportError + HttpxVoiceTransport; httpx LAZY-imported inside __init__ per the Q-46/Q-72 clean-checkout bar; explicit 30s timeout; non-2xx raises) + NEW src/wombat/voice/stt.py (DeepgramTranscriber implementing the EXISTING TK-162 Transcriber Protocol verbatim; plain api_key arg - key resolution stays TK-188/TK-197 territory; ONE POST to api.deepgram.com/v1/listen, ?model= when set; malformed body raises DeepgramTranscriptionError, transport failure propagates - never a lying empty transcript; ASRSource failed/ path and sources/asr.py untouched). pyproject voice-cloud extra = httpx>=0.27 ONLY (Q-100). DEC-28 structural opt-in preserved: zero construction in src outside the definitions + the intended default-transport fallback (which itself raises ImportError on a checkout without the extra - AC3). Verifier independently re-derived all ACs (fake transport, httpx-blocked subprocess); 13/13 provider tests; ruff+mypy clean; closeout gate FULLY GREEN under the post-Q-103 zero-allowance bar (1021 passed / 61 documented PG-DSN skips / 0 failed). NEXT in batch (5/5): TK-191 (Fish Audio TTS + playback, EP-31). |
| 2026-07-09 | **TK-191 recorded DONE (104/122) (COMMIT, v1.76 - build+verification record, within authority; status flip + record only) - FIX-THEN-VOICE BATCH 5/5 COMPLETE (EP-31): the cloud-TTS pattern-setter lands per the Q-104 rulings - Jim's own configured Fish Audio path.** NEW src/wombat/voice/playback.py (AudioPlayer Protocol + WinsoundPlayer - stdlib winsound lazy-imported at construction, zero new dep per CST-1, PlaySound/SND_MEMORY; off-Windows/absent construction raises loud, import stays clean) + NEW src/wombat/voice/tts.py (FishAudioTTSAdapter implementing the EXISTING sinks.tts_adapter.TTSAdapter Protocol VERBATIM: ONE Bearer-auth POST to https://api.fish.audio/v1/tts carrying {text, reference_id=voice_id, format=wav}, returned bytes played EXACTLY once; api_key/voice_id are plain constructor args - resolution/selection stay TK-188/TK-193; Jim's real reference id is NEVER a code default, grep-proven, DEF-7 live-smoke territory only). Failure semantics per CON-3 - NO try/except in the adapter; transport/player failures raise into SpeakSink's as-built terminal Degraded(to=None) with the composed artifact byte-identical (TK-165 parity, proven end-to-end against the REAL sink, voice_enabled=True). Defaults mirror TK-189 - eager-at-__init__ HttpxVoiceTransport/WinsoundPlayer only when not injected (AC3 import-clean/construct-raises). INDEPENDENTLY VERIFIED PASS - all 3 ACs re-derived under the verifier's own execution; tests/voice 33/33 EXECUTED on win32; ruff clean; mypy Success 260 files src+tests strict; closeout gate FULLY GREEN 1032 passed / 61 documented PG-DSN skips / 0 failed. THE BATCH IS CLOSED (TK-204/TK-203/TK-202/TK-189/TK-191). NEXT: arc-close review pass, then TK-190/TK-192 -> TK-193 (both pattern-setters now done) -> TK-194/TK-195 + EP-32. | architect | TK-191, TK-189, TK-192, TK-193, Q-104, Q-100, CON-3, DEC-28, DEF-7, EP-31, contract v1.76 |
| 2026-07-09 | **EP-31 CLOSEOUT BATCH SELECTED + PRE-BUILD RULED (COMMIT, Q-105, contract v1.77 — within authority: pipeline-level how inside DEC-28/DEC-32/Q-100/Q-104/CON-3/CST-1; no locked DEC touched; orchestrator-authorized sequencing under Jim's delegation).** Batch (build order): **TK-190 (ElevenLabs Scribe + Fish STT) → TK-192 (ElevenLabs + Deepgram Aura TTS) → TK-193 (voice/select.py selection + strictly cloud→local fallback bound into bootstrap) → TK-194 (assistant-name threading) → TK-195 (egress lesion proof)** — closes EP-31; every prerequisite (TK-187/TK-188/TK-189/TK-191) is done, all five re-derived READY and atomic from the current graph. Rulings (facts verified at commit 955febc): (a) VoiceTransport.post is content=/json=-only as-built → TK-190 adds OPTIONAL additive data=/files= multipart params (ElevenLabs Scribe requires multipart); files_in_scope += voice/transport.py. (b) STT wire pins (best-effort per Q-104(d), truthed by the DEF-7 live smokes): Scribe POST /v1/speech-to-text xi-api-key multipart file+model_id (default scribe_v1) parse 'text'; Fish POST /v1/asr Bearer + application/msgpack with a private ~10-line fixmap {'audio': bytes} hand-encoding — voice-cloud stays httpx-ONLY (Q-100), no msgpack dep. (c) TTS pins: ElevenLabs POST /v1/text-to-speech/{voice_id}?output_format=pcm_16000 returns HEADERLESS PCM → private stdlib-wave RIFF wrap in voice/tts.py (zero new dep, CST-1) then exactly ONE player.play; Aura POST /v1/speak?model=<voice_id|aura-asteria-en>&encoding=linear16&sample_rate=24000&container=wav plays directly; both adapters keep the no-try/except CON-3 raise-into-SpeakSink posture. (d) TK-193: build_transcriber/build_tts_adapter(config, key_store=None) in NEW voice/select.py; keys ONLY via resolve_provider_key (TK-188/DEC-32); local paths byte-preserve _maybe_register_asr and _construct_tts_adapter semantics; cloud gaps (key/voice_id/extra) → ONE loud log + local path, never a boot failure; constructed cloud primary wrapped in FallbackTranscriber/FallbackTTSAdapter (WARN + one local try; propagate into ASRSource failed/ / SpeakSink Degraded when local absent); direction STRICTLY cloud→local (DEC-28, structural test); reroutes = sources/bootstrap._maybe_register_asr + bootstrap.build_speak_sink/make_speak_callable, still gated on wombat_voice_enabled. (e) TK-194: the three verified constants (stages/compose.py:57, compose/brief_template.py:47 consumed by brief_compose_stage.py:94, gmail/draft_composer.py:83) become pure builders 'You are {name}, a quiet steward …' with remainder byte-identical and module defaults kept for existing pins; DraftComposer gains OPTIONAL assistant_name threaded via build_draft_composer_stage; files_in_scope normalized (+brief_compose_stage.py +bootstrap.py +tests/stages/test_brief_compose_stage.py +tests/integrations/gmail/test_draft_composer.py; phantom tests/stages/test_compose.py → tests/unit/test_compose_stage.py). (f) TK-195 AC1 'httpx never imported' SCOPED to the voice surface (fresh-subprocess build of sources+sinks under defaults) because the sanctioned DeepSeek mouth's openai client may import httpx per-drive — the DEC-28 one-default-egress invariant is the thing proven. DEFERRED: EP-32 (TK-196..201, next arc after the EP-31 arc-close review pass), browser EP-25/26 + TK-153 (P3), DEF-7 live smokes (operator-owned, Fish first). | architect | Q-105, TK-190, TK-192, TK-193, TK-194, TK-195, Q-100, Q-104, DEC-28, DEC-32, DEF-7, CON-3, CST-1, EP-31, contract v1.77 |
| 2026-07-09 | **TK-190 CLOSED (COMMIT, contract v1.78 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched).** ElevenLabs Scribe + Fish Audio STT providers riding the TK-189 pattern — board 105/122. ElevenLabsScribeTranscriber (POST multipart, xi-api-key header, file + model_id form fields, parses top-level text) and FishAudioTranscriber (POST Bearer + application/msgpack, private ~10-line msgpack fixmap-1/fixstr/bin32 hand-encoding — voice-cloud stays httpx-ONLY per Q-100, no msgpack dep) in src/wombat/voice/stt.py behind the existing sources.asr Transcriber Protocol. The Q-105(a) ruling shipped as specified: ADDITIVE optional data=/files= multipart params on VoiceTransport.post + HttpxVoiceTransport.post, existing content=/json= callers byte-untouched; DeepgramTranscriber production code untouched. Failure posture identical to TK-189 — own *TranscriptionError on missing/malformed text, VoiceTransportError propagates on non-2xx; lazy-import contract holds (import clean, construction alone raises without the extra). Scope audit clean: two-line additive fake-transport signature bumps in tests/voice/test_stt_deepgram.py + test_tts_fish.py accepted as Protocol-widening conformance, not creep; no selection/key wiring (TK-193/TK-188 untouched), no TTS, no vendor SDK. INDEPENDENTLY VERIFIED PASS (verifier re-derived + executed the full bar; every AC passed). Quality bar: uv run pytest -q 1051 passed / 61 documented WOMBAT_TEST_PG_DSN skips / 0 failed; ruff clean; mypy strict Success 261 files src+tests. NEXT: TK-192 (ElevenLabs + Deepgram Aura TTS riding TK-191). | architect | TK-190, TK-189, TK-192, Q-105, Q-100, DEC-28, EP-31, contract v1.78 |
| 2026-07-09 | **TK-192 CLOSED (COMMIT, contract v1.79 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched).** ElevenLabs + Deepgram Aura TTS providers riding the TK-191 pattern — board 106/122; the launch TTS roster is COMPLETE and TK-193 (selection + cloud→local fallback) is fully unblocked. ElevenLabsTTSAdapter (POST /v1/text-to-speech/{voice_id}?output_format=pcm_16000, xi-api-key header, {text} + model_id only when model set; headerless 16-bit mono 16kHz PCM wrapped by the new private _wrap_pcm16_mono_16k_as_wav — stdlib wave over io.BytesIO, CST-1 zero new dependency — before the ONE play) and DeepgramAuraTTSAdapter (POST /v1/speak?model=<voice_id|aura-asteria-en>&encoding=linear16&sample_rate=24000&container=wav, Authorization Token header, {text}; WAV response played VERBATIM, no wrap) in src/wombat/voice/tts.py behind the existing sinks.tts_adapter TTSAdapter Protocol, over the shared TK-189 VoiceTransport + TK-191 AudioPlayer seams. Plain api_key/voice_id/model ctor args (resolution/selection stay TK-188/TK-193); lazily-defaulted transport/player keep the module import-clean without the voice-cloud extra; ZERO try/except — transport/player failures RAISE into SpeakSink's terminal Degraded (CON-3, TK-191 parity, proven against the real sink, parametrized over BOTH adapters). Wire pins verified verbatim against the Q-105(c) ruling. INDEPENDENTLY VERIFIED PASS (verifier re-executed the full quality bar + every AC; 17/17 provider tests; wrap helper proven to emit a real RIFF/WAVE image whose frames round-trip to the input PCM; exactly-one POST + exactly-one play for both; Aura bytes == response verbatim). Scope exactly the 2 files_in_scope (tts.py +116/-1, new tests/voice/test_tts_providers.py) — no contract/config/keyring/SpeakSink/selection touch, no new dependency. Architect closeout gate: uv run pytest -q FULLY GREEN pre-commit under the post-Q-103 zero-allowance bar. NEXT in the Q-105 batch: TK-193. | architect | TK-192, TK-191, TK-189, TK-193, Q-105, DEC-28, CON-3, CST-1, EP-31, contract v1.79 |
| 2026-07-09 | **TK-193 CLOSED (COMMIT, contract v1.80 — within authority: ticket closeout on independent verifier PASS + files_in_scope normalization; no locked DEC touched).** Provider selection + strictly cloud→local fallback lands per the Q-105(d) ruling — board 107/122; the DEC-28 structural opt-in is now the ONE construction path for every voice provider. NEW src/wombat/voice/select.py: build_transcriber/build_tts_adapter(config, key_store=None) honoring wombat_stt_provider/wombat_tts_provider — local default returns the EXACT existing wirings (FasterWhisperTranscriber/Pyttsx3Adapter) BEFORE any key resolution (no cloud construction, no key read — proven with raise-if-constructed cloud classes + an unreadable key store); cloud constructed ONLY when resolve_provider_key (TK-188) resolves the user key, then wrapped in FallbackTranscriber/FallbackTTSAdapter (ANY runtime exception → loud WARN + exactly one local try; structural test proves no local→cloud path); any bootstrap gap (key/extra/construction) → ONE loud log naming the gap + local default, never a boot failure (CON-3, Q-67 loud-skip). Reroutes: sources/bootstrap._maybe_register_asr delegates to build_transcriber; bootstrap build_speak_sink/make_speak_callable route through build_tts_adapter (replacing _construct_tts_adapter), still gated on wombat_voice_enabled — voice-off constructs NOTHING. INDEPENDENTLY VERIFIED PASS (one repair round: the boot-never-fails test's fallthrough to the REAL OS keyring fixed by monkeypatching select.KeyringVoiceKeyStore to the fake — neither bootstrap seam threads key_store; Q-57(a) never-the-real-keyring held). Quality bar (verifier's own execution): 1084 passed / 61 documented PG-DSN skips / 0 failed; ruff clean; mypy strict Success 264 files src+tests. files_in_scope normalized += tests/unit/test_runtime.py (mechanical reroute companion, v1.66 precedent); no config.py change needed (TK-187 fields suffice, mypy-proven). validate PASS (182 nodes/163 gov); gate PASS (analyze). NEXT in the Q-105 batch: TK-194 (assistant-name threading), then TK-195 (egress lesion proof) closes EP-31. | architect | TK-193, TK-188, TK-187, TK-194, TK-195, Q-105, Q-67, DEC-28, CON-3, EP-31, contract v1.80 |
| 2026-07-09 | **PERSONALITY MATRIX RECORDED — FEAT-14/EP-33..35/TK-206..214/DEC-33..36/DEF-8 + TK-200 amendment (COMMIT, v1.81 — recording a JIM-APPROVED proposal; the approving architect session was lost to a power cut before it could record, and the sync to this brief never landed either, so the record was reconstructed from the orchestrator's authoritative summary; nothing here is architect-invented scope).** FEAT-14 = "a way to tune the personality so people can have fun with it" — name + voice + services + personality = the user-facing identity kit. **DEC-33:** 5 axes as named enum levels — brevity (terse|balanced|expansive), warmth (reserved|neutral|warm), directness (gentle|plain|blunt), humor (none|dry|playful), proactivity (minimal|balanced|forward); DEFAULT_MATRIX = (terse, reserved, plain, none, minimal) = today's steward; proactivity modulates optional content WITHIN gate-cleared output only; constitution-excluded axes NAMED as excluded (interruption-eagerness → CON-2/3, motive-empathy → CON-6, persuasion/sycophancy → CON-1, action-initiative → CON-5). **JIM'S EMPHASIS 1 (carried by TK-207):** the persona builder is a PURE FUNCTION ("math algo") — instruction_for(mouth, matrix) = base_role + length_clause + register_clause + hedging_clause + humor_clause + immutable guard_suffix; today's per-mouth text SPLITS into base_role + guard_suffix at a recorded seam, every DEFAULT clause renders empty, so the DEFAULT matrix reproduces today's prompts BYTE-FOR-BYTE (pinned against the live TK-194 builders; exhaustive 243-combination property tests; guard_suffix present at EVERY matrix value). **JIM'S EMPHASIS 2 (carried by TK-210, EP-33's done-bar):** OUTPUT-EFFECT verification, not just prompt deltas — an env-gated live-eval harness (DEF-7 armed-loud-skip pattern; ride cog-worx eval/_live where it fits) samples REAL mouth output per level and asserts MEASURABLE properties: brevity length/line-count monotone terse<balanced<expansive; warmth greeting presence (warm) vs absence (reserved) on the brief; directness hedge-lexicon presence/absence; humor aside ONLY when permitted AND non-urgent; forbidden-term classifiers pass at ALL levels; any unmeasurable axis is FLAGGED in governance, never shipped as placebo. **DEC-34 (JIM AUTHORITY):** hot-apply via LivePersona (TK-209) — one composition-root-owned matrix authority, mouths read instruction at RENDER time, set() applies next turn + persists to settings.json; supersedes-in-part DEC-32 restart-to-apply for IN-PROCESS actuation only (voice commands, dream_persona); settings-app persona edits stay restart-to-apply (cross-process; file-watcher = flagged follow-up). **DEC-35:** deterministic CLOSED voice-command grammar (TK-211), pre-queue interception at ASRSource (TK-212) — matched utterances trail-logged then CONSUMED, never enqueued/gate-rated/mouth-rendered; no spoken confirmation (gate's monopoly). **DEC-36 + DEF-8:** learning = explicit closed-lexicon feedback tokens only (TK-213, persona_feedback events riding the behavior-event machinery, motive-free) aggregated nightly by dream_persona (TK-214) under RatingTuner-pattern custody — at most ONE clamped step per axis per night on a clear thresholded signal, journaled; automatic tone-adaptation DEFERRED (DEF-8). **TK-200 AMENDED:** persona axis selects join the settings UI (restart-notice honesty per the DEC-34 carve-out); depends_on += TK-208. **SEQUENCING:** EP-33 (TK-196 pulled forward as persistence prerequisite) → EP-34 → EP-32 remainder → EP-35 (P3). TK-205 deliberately unassigned (approved id block starts at TK-206). Board 107/131 (tickets 122 → 131). validate PASS (195 nodes/168 gov); gate PASS (analyze). Working tree's in-flight TK-194 src/tests changes untouched — planning-only commit. | architect | FEAT-14, EP-33, EP-34, EP-35, TK-206..TK-214, DEC-33, DEC-34, DEC-35, DEC-36, DEF-8, TK-200, TK-196, TK-194, TK-207, TK-209, TK-210, CON-1..6, contract v1.81 |
| 2026-07-09 | **TK-194 CLOSED (COMMIT, contract v1.82 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched).** Configurable assistant name lands per the Q-105(e) ruling — board 108/131. The three fixed steward system-instruction constants (stages/compose.py, compose/brief_template.py consumed by brief_compose_stage.py, gmail/draft_composer.py) become tiny pure builders taking config.wombat_assistant_name (TK-187 field, default 'Steward'); construction sites threaded via bootstrap (DraftComposer gains an OPTIONAL assistant_name arg). Default rendering proven byte-faithful: the verifier reconstructed all three pre-ticket literals from git HEAD and compared programmatically — remainder BYTE-IDENTICAL on all three, and BRIEF_SYSTEM_INSTRUCTION == brief_system_instruction('Steward') so the DEC-27 substring pins stay green untouched. The name is DISPLAY/persona ONLY — under WOMBAT_ASSISTANT_NAME=Marvin it appears in all three built system instructions and is asserted ABSENT from user_msg.content; never parsed, never in the gate, never an event field (the gate-is-deterministic thesis holds). reflection_compose.py's own _SYSTEM_INSTRUCTION correctly untouched per the ruled out-of-scope; this is also the seam TK-207's persona clause-algebra will later generalize (its byte-identity default emphasis rides the same proof pattern). INDEPENDENTLY VERIFIED PASS — the original builder session died before reporting; the verifier trusted nothing and re-derived every AC under own execution. Quality bar: uv run pytest -q 1092 passed / 61 documented PG-DSN skips / 0 failed (architect re-ran green at closeout); ruff clean; mypy strict Success 264 files src+tests. Scope exactly the 9 files_in_scope (5 src, 4 test). validate PASS (195 nodes/168 gov); gate PASS (analyze). NEXT and LAST in the Q-105 batch: TK-195 (egress lesion proof) closes EP-31. | architect | TK-194, TK-187, TK-195, TK-207, Q-105, DEC-27, EP-31, contract v1.82 |
| 2026-07-09 | **TK-195 CLOSED (COMMIT, contract v1.83 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched). EP-31 IS CLOSED — all ten TK-186..TK-195 done.** The voice egress lesion suite lands per the Q-105(f) scoping — board 109/131 — and delivers the DEC-28 structural proof: the DEFAULT configuration is PROVABLY offline-but-for-DeepSeek. Tests ONLY: NEW tests/voice/test_egress_lesion.py, ZERO src edits (builder bound to the as-built TK-193 names — build_transcriber/build_tts_adapter, FallbackTranscriber/FallbackTTSAdapter). AC1 default-zero-egress proven NON-VACUOUSLY on two independent axes: a raise-on-__init__ sentinel over all SIX cloud classes proves zero cloud construction while all five voice-surface builders (build_source_registry, build_speak_sink, make_speak_callable, build_transcriber, build_tts_adapter) run under the default config, and a subprocess proof shows httpx ABSENT from sys.modules after the full default voice assembly (verified load-bearing: httpx IS installed and constructing a real DeepgramTranscriber pulls it in via HttpxVoiceTransport.__init__; env/.env leakage FAILS the subprocess via chdir + in-subprocess default-config asserts — never a false pass). AC2/AC3 cloud-down degrade proven on BOTH axes and BOTH fallback slots: STT local-present → exactly one fallback call + processed/, local-absent → two files to failed/ with no crash and siblings unaffected; TTS local-present → Done + one call, local-absent → Degraded(to=None) spoken=False/degraded=True with the composed_output artifact byte-unaffected (TK-165 parity). BRIEFING CORRECTION recorded in the changelog: Q-105(f)'s cited subprocess-proof precedent in tests/voice/test_stt_deepgram.py does not exist (in-process meta-path shim there; repo-wide zero subprocess tests) — citation wrong, ruling substance right, builder proceeded correctly. INDEPENDENTLY VERIFIED PASS; quality bar (verifier's own execution): uv run pytest -q 1098 passed / 61 documented PG-DSN skips / 0 failed; ruff clean; mypy strict Success 265 files src+tests; architect re-ran green pre-commit. validate PASS (195 nodes/168 gov); gate PASS (analyze). NEXT: the EP-31 arc-close review pass per cadence, then EP-33 (persona core, TK-196 pulled forward as the persistence prerequisite) per the v1.81 sequencing. | architect | TK-195, TK-193, TK-165, Q-105, DEC-28, CON-3, EP-31, EP-33, TK-196, contract v1.83 |
| 2026-07-09 | **PERSONA RECORDING GAP-FIX vs the RECOVERED full proposal (COMMIT, v1.84 — within authority: restoring Jim-APPROVED substance the v1.81 summary-based record dropped or inverted; all v1.81 decision texts stand untouched, DEC-37 supersedes only where named).** The full proposal was recovered from the proposing architect's transcript; the substance audit found ten gaps. HEADLINE — proactivity had been recorded as a PLACEBO (prompt-side 'optional content' with NO carrying ticket, and DEC-33's 'never gate thresholds' clause inverted the approved design): DEC-37(a) restores the actuation — a bounded deterministic urgency_threshold offset in a recorded personality_band block in wombat_params.yaml (minimal +0.10 / balanced 0.00 / forward −0.10 PROVISIONAL under DEF-1; hard floor 0.60 / cap 0.95; human-edited/versioned gate-constant custody, never tuner-written; composes with RatingTuner, the band bounds the combined effect; CON-3 intact — no axis creates a surfacing; NG-3 intact — no cap raised; DEFAULT proactivity → balanced = zero offset = today's gate), carried by NEW TK-215 (gate reads the live level through LivePersona at scoring time, next-scored-item semantics). ALSO RESTORED — degrade-path brevity/warmth variants + recorded directness/humor model-path-only asymmetry (NEW TK-216); reflection as the FOURTH mouth with its CON-6/NG-1 bars as the immutable guard_suffix (TK-207/209); humor barred from Gmail drafts + reflection at every level and trimmed to the approved none|dry (v1.81's 'playful' was recorder-added, struck); exclusion list completed (+coaching/clinical NG-2, +persistence/nagging NG-3); deterministic one-line command ack (supersedes-in-part DEC-35's silence clause); settings-app persona edits hot-apply via an mtime poll on the existing timer beat, last-write-wins ruled acceptable (supersedes-in-part DEC-34's cross-process carve-out for persona fields ONLY; TK-200 shows hot-apply, not the restart notice); learning custody completed (≥2 in-window signals, 7-day explicit-set pin — explicit outranks learning, dream_persona AFTER dream_tune); Jim's verbatim 'observant, learns to fit in the right way' framing added to FEAT-14 + vision.md. The full persona design intent is now synced into THIS brief (the 'Persona design' section) so a lost session can never orphan it again. Board 109/133 (tickets 131 → 133). validate PASS (197 nodes/169 gov); gate PASS (analyze). Working tree was clean at df7dc8d pre-amendment; planning-only commit. | architect | DEC-37, TK-215, TK-216, TK-206, TK-207, TK-209, TK-210, TK-212, TK-213, TK-214, TK-200, FEAT-14, EP-33, DEC-33..36, DEF-1, DEF-8, CON-3, NG-2, NG-3, contract v1.84 |
| 2026-07-10 | **CR4 ROUTED + PERSONA-CORE BATCH SELECTED + PRE-BUILD RULED (COMMIT, Q-106, v1.85 — within authority: review routing + pipeline-level how; no locked DEC touched; orchestrator-authorized sequencing under Jim's delegation).** CR4 (pass 4, EP-31 voice arc — all three findings MINOR, zero critical/major) routes to two NEW P3 EP-31 tickets that head the NEXT batch: TK-217 (CR4-1 — the false 'voice output disabled' warning on the healthy-cloud-primary path in voice/select.py) and TK-218 (CR4-2/3 — docs-only discharge of DEC-28's named docstring amendments on sinks/tts_adapter.py + sources/asr.py). BATCH (5, build order): TK-206 → TK-196 → TK-207 → TK-208 → TK-209 — the EP-33 persona-core spine per the v1.81 sequencing (TK-196 pulled forward as the persistence prerequisite); it closes at LivePersona, unblocking TK-210/211/212/215/216 AND the TK-197+ settings-app chain next. Q-106 rulings, every seam source-verified: (a) TK-207 Mouth enum {compose,brief,draft,reflection} + recorded per-mouth base_role/guard_suffix seam (guards = today's closing non-negotiable sentences; reflection's CON-6/NG-1 bar block IS its guard and its base has NO name slot — the name never renders there); clauses insert between base and guard, every DEFAULT clause renders the empty string, byte-identity arbitrated against the LIVE constants (compose.py:60 / brief_template.py:51 / draft_composer.py:85 / reflection_compose.py:74). (b) TK-196 = subclassed pydantic-settings JsonConfigSettingsSource (verified installed 2.14.2) filtered to a module-level APP_EDITABLE_FIELDS allowlist (TK-208 appends its five persona fields to the SAME tuple); source order (init, env, dotenv, json, file_secret) pins env > .env > settings.json > defaults with test kwargs highest; SecretStr keys in the file DROP with one loud WARNING each; missing file silent, malformed file loud-then-absent — the app file can never fail a boot (CON-3). (c) TK-208 matrix_from_config homes in persona/matrix.py duck-typed (no import cycle). (d) TK-209 mouths gain an OPTIONAL live_persona arg (None = today's frozen-at-init construction, existing tests stand); the DEC-37(g) mtime poll rides wombat's OWN Sweeper clock callable in runtime.py:107 — verified once-per-beat at installed cogworx sweeper.py:72-77, zero cog-worx touch; files_in_scope normalized += runtime.py + tests/unit/test_runtime.py. DELIBERATELY DEFERRED — TK-210..216 (dep chains through TK-209/TK-211), EP-32 remainder (TK-197..201), browser EP-25/26 + TK-153 (P3), DEF-7 live smokes (operator-owned). validate PASS (199 nodes/170 gov); gate PASS (analyze). Board 109/135 — governance only. | architect | Q-106, TK-206, TK-196, TK-207, TK-208, TK-209, TK-217, TK-218, DEC-28, DEC-32, DEC-33, DEC-34, DEC-37, CON-3, EP-31, EP-33, contract v1.85 |
| 2026-07-10 | **TK-206 CLOSED (COMMIT, contract v1.86 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched).** PersonaMatrix domain type lands as the persona foundation per DEC-33-as-amended-by-DEC-37 — board 110/135, persona-core batch 1/5 (Q-106). NEW pure package src/wombat/persona/ (matrix.py + __init__.py + test package): five closed StrEnum axes (Brevity terse\|balanced\|expansive, Warmth reserved\|neutral\|warm, Directness gentle\|plain\|blunt, Humor none\|dry — EXACTLY two, the DEC-37(c) 'playful' strike is now code, Proactivity minimal\|balanced\|forward); PersonaMatrix frozen+slots and hashable; DEFAULT_MATRIX = (terse, reserved, plain, none, BALANCED) — proactivity default balanced per DEC-37(a) = zero gate offset = today's steward exactly. from_strings/to_strings round-trip identity over the plain-lowercase wire format (TK-208's consumer surface); parse failures raise a loud ValueError naming BOTH axis and value. Module docstring names all six DEC-33+DEC-37(b) constitution-excluded axes verbatim with their ids (CON-1/2/3/5/6, NG-2/3) — exclusion is code-adjacent, not lore. Non-goals held: no prompt rendering (TK-207), no config fields (TK-208), zero gate/surfacing influence. INDEPENDENTLY VERIFIED PASS — scope exactly the 4 files_in_scope, no contract touch by the builder; verifier's own execution: ruff clean, mypy strict Success 269 files src+tests, uv run pytest -q 1110 passed / 61 documented PG-DSN skips / 0 failed; architect closeout gate re-run green pre-commit. validate PASS (199 nodes/170 gov); gate PASS (analyze). NEXT in the Q-106 batch (2/5): TK-196 (settings.json persistence prerequisite). | architect | TK-206, TK-196, TK-207, TK-208, Q-106, DEC-33, DEC-37, CON-1..6, NG-2, NG-3, EP-33, contract v1.86 |
| 2026-07-10 | **TK-196 CLOSED (COMMIT, contract v1.87 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched).** wombat.settings.json lands as a READ source under the pinned precedence per the Q-106(b) ruling — board 111/135, persona-core batch 2/5 (EP-32 ticket pulled forward as the persona-persistence prerequisite). src/wombat/config.py gains `_AppEditableJsonSettingsSource` — a subclassed pydantic-settings (2.14.2) JsonConfigSettingsSource filtered to the NEW module-level `APP_EDITABLE_FIELDS` allowlist (5 admitted non-secret fields: stt/tts provider, tts_voice_id, stt_model, assistant_name; TK-208 appends its persona fields to this SAME tuple, TK-197 validates PUTs against it) — wired via `settings_customise_sources` → (init, env, dotenv, json, file_secret): direct-construction kwargs stay highest, env > .env > settings.json > defaults, behavior-preserving for every caller not using the file. Structural guards, loud-not-silent: any SecretStr-typed key in the file DROPS with exactly one WARNING naming it (all 5 secret fields detected); non-admitted keys drop silently; malformed JSON → one WARNING + treated-as-absent; missing file silent — the app file can NEVER fail a boot (CON-3). ONE REPAIR ROUND — the verifier caught a boot-bricker: a valid-JSON non-object top level (array/null/…) raised through the base class out of every load_config(); `_read_file` now isinstance-checks dict → same loud treated-as-absent posture, parametrized regression test added. .gitignore covers the file. Verifier's own run: pytest 1117 passed / 61 documented DSN skips / 0 failed; ruff + mypy strict clean (269 files src+tests); scope exactly the 3 files_in_scope. Non-goals held — no write path (TK-197 owns writes), no hot reload (restart-to-apply stands; the persona mtime-poll hot-apply carve-out is TK-209's). NEXT in the Q-106 batch (3/5): TK-207 (clause-algebra builder, EP-33). |
| 2026-07-10 | **TK-207 CLOSED (COMMIT, contract v1.88 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched).** The pure-function persona builder lands per Jim's 'math algo' frame (DEC-33/DEC-37, Q-106(a) seam ruling) — board 112/135, persona-core batch 3/5. NEW src/wombat/persona/builder.py: closed `Mouth` enum (COMPOSE/BRIEF/DRAFT/REFLECTION) + `instruction_for(mouth, matrix, assistant_name) -> str` composing base_role + clauses + guard_suffix, single-space-joined, all clause text in module-level tables — ZERO IO/config/model calls (imports: stdlib enum + persona.matrix only). Every DEFAULT-level clause renders the EMPTY string, so at DEFAULT_MATRIX the output is BYTE-IDENTICAL to the four live oracles (compose/brief/draft TK-194 builders + reflection_compose._SYSTEM_INSTRUCTION) — the oracles are UNTOUCHED and the tests import them live, no fixture copies. REFLECTION has NO name slot (assistant_name never renders there, any input). Guard suffixes verbatim per mouth: compose 'No preamble.'; brief + the DEC-27 quoted-data sentence; draft 'No preamble, no signature.'; reflection = the full clinical-language bar block — its CON-6/NG-1 bars ARE the guard (DEC-37(d)). Humor clauses consulted ONLY for compose/brief — never draft or reflection at ANY level (DEC-37(c)); Proactivity has NO clause table — designed prompt-layer no-op, actuation gate-side (TK-215). INDEPENDENTLY VERIFIED PASS — AC1 byte-identity re-derived by the verifier without trusting the builder's tests (live-source == over 5 names incl. empty/HTML-ish/accented; seam splits hand-checked against read source); AC2 exhaustive over all 162 combos per mouth (guard containment, determinism, every applicable non-default level changes output, humor absent from draft/reflection). Scope = exactly the 2 new files_in_scope; quality bar (verifier's own run): pytest 1156 passed / 61 documented DSN skips / 0 failed, ruff clean, mypy strict Success 271 files src+tests. Non-goals held — no call-site rewiring (TK-209), no output-effect measurement (TK-210). validate PASS (199 nodes/170 gov); gate PASS (analyze). NEXT in the Q-106 batch (4/5): TK-208 (persona config fields + matrix_from_config, EP-33). | architect | TK-207, TK-206, TK-194, TK-208, TK-209, TK-210, TK-215, Q-106, DEC-27, DEC-33, DEC-37, CON-6, NG-1, NG-2, EP-33, contract v1.88 |
| 2026-07-10 | **TK-208 CLOSED (COMMIT, contract v1.89 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched).** The persona config surface lands — board 113/135, persona-core batch 4/5 (Q-106). WombatConfig gains five OPTIONAL `wombat_persona_*` Literal fields (brevity/warmth/directness/humor/proactivity) over the DEC-33 closed level names as amended by DEC-37; defaults = DEFAULT_MATRIX exactly (proactivity default balanced per DEC-37(a) = zero gate offset), deliberately NOT in REQUIRED_ENV — the zero-config boot carries today's steward, with a TK-208/DEC-33/DEC-37 comment block in the TK-187 style over the fields. The five names APPEND to the TK-196 `APP_EDITABLE_FIELDS` tuple, so they ride the pinned precedence (env > .env > settings.json > defaults) and become settings-app-editable the moment TK-197 lands — no separate persona store. `matrix_from_config` homes in persona/matrix.py duck-typed per Q-106(c): a `_PersonaConfigLike` Protocol with read-only properties (covariant against the Literal-typed fields, mypy-proven over a real WombatConfig at the call sites) delegating to the existing from_strings path; NO wombat.config import (runtime test + AST guard — matrix.py stays pure, no cycle). .env.example documents the five vars with their closed vocabularies. INDEPENDENTLY VERIFIED PASS — scope exactly the 5 files_in_scope, no contract touch by the builder, zero scope creep; all three ACs executed (no-env defaults give matrix_from_config == DEFAULT_MATRIX; humor=dry + brevity=expansive carry with the other three axes at DEFAULT; warmth=nonsense fails loud at boot naming the field); verifier's own run: pytest 1164 passed / 61 documented DSN skips / 0 failed, ruff clean, mypy strict Success 271 files src+tests. Non-goals held — no hot-apply (TK-209), no settings UI (TK-200 amendment), no settings-API change. validate PASS (199 nodes/170 gov); gate PASS (analyze). NEXT in the Q-106 batch (5/5, batch close): TK-209 (LivePersona hot-apply seam) — all three deps (TK-207/TK-208/TK-196) now done. | architect | TK-208, TK-206, TK-196, TK-197, TK-200, TK-207, TK-209, Q-106, DEC-33, DEC-37, EP-33, contract v1.89 |
| 2026-07-10 | **TK-209 CLOSED (COMMIT, contract v1.90 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched). THE Q-106 PERSONA-CORE BATCH IS COMPLETE (5/5).** The LivePersona hot-apply seam lands per DEC-34-as-amended-by-DEC-37 — board 114/135. NEW src/wombat/persona/live.py: LivePersona is the ONE composition-root-owned runtime matrix authority — instruction(mouth) delegates to the TK-207 pure builder over the CURRENT in-memory matrix; set(matrix) swaps the attribute immediately (single-process asyncio plain swap) then best-effort persists ONLY the five wombat_persona_* keys to the settings.json tier (other keys preserved; never .env/secrets) — persist failure = matrix still applied + exactly one loud WARNING, never raises (CON-3); poll_settings_file() = cheap os.stat mtime check riding the EXISTING Sweeper beat per Q-106(d) (no new scheduler; TK-197 zero-runtime-imports invariant untouched), partial-key reloads keep current values, exceptions loud-WARN-never-raise; read-only matrix property = the TK-215 gate-side read seam. All FOUR mouths (ComposeStage, BriefComposeStage, DraftComposer via bootstrap, ReflectionComposeStage) took an OPTIONAL live_persona=None kwarg (None = today's frozen-at-init path, existing call sites stand); assemble_runtime builds LivePersona from matrix_from_config, threads the SAME instance to all four mouths, and exposes it on the bundle (the TK-212/TK-214 write seam). INDEPENDENTLY VERIFIED PASS — all four ACs verifier-re-executed: AC1 byte-identity to the four live oracles THROUGH the reroute at both LivePersona and assemble_runtime levels + same-instance threading; AC2 set(humor=dry) applies next turn, five keys land in settings.json; AC3 write-failure loud-not-fatal; AC4 external settings edit hot-applies on the next timer beat (DEC-37 mtime poll). Scope exactly the 13 files_in_scope (2 new + 11 modified), builder never touched the contract. Quality (verifier's own run): pytest 1188 passed / 61 documented DSN skips / 0 failed; ruff clean; mypy strict Success 273 files; touched suites in isolation 105 passed. Non-goals held — zero gate/queue influence, no voice commands (EP-34), no non-persona hot-reload (DEC-34 carve-out stands). validate PASS (199 nodes/170 gov); gate PASS (analyze). UNBLOCKED NEXT: TK-210/TK-212/TK-215 + the TK-197+ settings-app chain; TK-217/TK-218 head the next batch per Q-106. | architect | TK-209, TK-206, TK-196, TK-207, TK-208, TK-210, TK-212, TK-214, TK-215, TK-197, Q-106, DEC-34, DEC-37, CON-3, EP-33, contract v1.90 |
| 2026-07-10 | **TK-217 CLOSED (COMMIT, contract v1.92 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched).** CR4-1 discharged — board 115/135, Q-107 batch 1/5 (EP-31, P3). The two local voice builders in voice/select.py (`_build_local_transcriber`/`_build_local_tts`) gain a keyword-only `role: Literal['primary','fallback']='primary'` LOG-ROUTING discriminator; exactly the two fallback-slot call sites (the FallbackTranscriber/FallbackTTSAdapter slots filled after a healthy cloud primary is constructed) pass role='fallback' and log the contextualized 'local ASR/TTS fallback unavailable ... the cloud primary remains active' message, so a legitimate cloud-only install (voice-cloud extra without the voice extra) no longer sees the false 'voice output disabled for this boot' operator signal; ALL seven other call sites (local-primary + every cloud-bootstrap-gap degrade) keep the default role with BYTE-IDENTICAL messages. Zero selection/fallback semantic change (TK-193 stands), no new log surface — the routing-only complexity_budget held. Independently verified PASS: verifier re-derived the 9-call-site map (exactly 2 fallback-role, both behind a live cloud primary) and re-executed all 3 ACs via the new -k tk217 tests; quality bar under verifier execution — pytest 1191 passed / 61 documented PG-DSN skips, ruff clean, mypy strict Success 273 files. Scope exactly the 2 files_in_scope; builder did not touch the contract. NEXT per Q-107: TK-218. | architect | TK-217, EP-31, Q-107, Q-106, DEC-28, TK-193, CON-3 |
| 2026-07-10 | **TK-218 CLOSED (COMMIT, contract v1.93 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — DEC-28's own amendment clause is what this ticket DISCHARGES).** CR4-2/CR4-3 discharged — board 116/135, Q-107 batch 2/5 (EP-31, P3, docs-only). The two module docstrings DEC-28 named for amendment are rescoped: sinks/tts_adapter.py now describes Pyttsx3Adapter as the LOCAL DEFAULT adapter and states no cloud TTS exists ONLY in the DEFAULT configuration, naming the cloud adapters (FishAudioTTSAdapter/ElevenLabsTTSAdapter/DeepgramAuraTTSAdapter in wombat.voice.tts, same TTSAdapter Protocol) and the structural opt-in seam wombat.voice.select.build_tts_adapter (selected AND key resolves, else no cloud construction; degrade strictly cloud-to-local, never the reverse); sources/asr.py qualifies 'no audio or transcript ever leaves the machine' as the DEFAULT-configuration (local FasterWhisperTranscriber) invariant and names the opt-in cloud STT providers (ElevenLabsScribeTranscriber/FishAudioTranscriber in wombat.voice.stt) constructed ONLY via wombat.voice.select.build_transcriber. The docs now tell the DEC-28 truth the TK-195 lesion suite already proves structurally. INDEPENDENTLY VERIFIED PASS — AC1 grep leaves only the qualified lines (tts_adapter.py:9, asr.py:38), no unqualified absolute remains; AC2 diff exactly the two files (8/2 + 6/2), every changed line inside the triple-quoted docstrings, zero executable/comment lines; adversarial check confirmed every named class real, no TK-217/TK-212 bleed. Quality bar (verifier's own run; architect re-ran pre-commit): pytest 1191 passed / 61 documented PG-DSN skips / 0 failed; ruff clean; mypy strict Success 273 files. validate PASS (199 nodes/171 gov); gate PASS (analyze). NEXT per Q-107: TK-215 (gate personality_band, EP-33). | architect | TK-218, TK-217, TK-195, EP-31, Q-107, DEC-28, CST-2, ASMP-1, contract v1.93 |
| 2026-07-10 | **TK-215 CLOSED (COMMIT, contract v1.94 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this ticket DISCHARGES DEC-37(a)'s recovered gate actuation).** Board 117/135, Q-107 batch 3/5 (EP-33, P2). Proactivity now actuates as the Jim-approved bounded deterministic urgency_threshold offset: frozen PersonalityBand (minimal/balanced/forward/floor/cap, all REQUIRED floats) on OperatingParams under the fail-loud discipline, OPERATING_PARAMS_VERSION 6→7; wombat_params.yaml v7 personality_band block (minimal +0.10 / balanced 0.00 / forward −0.10, floor 0.60 / cap 0.95) carrying the file's PROVISIONAL / HUMAN-EDITED-ONLY legend — DEF-1 calibration custody, no tuner or runtime path writes it. Pure effective_urgency_threshold(base, proactivity, band) = clamp(base + offset(level), floor, cap) beside is_surfacing_worthy (no I/O, no clock, zero LLM — NG-4/CON-1). Gate ctor gains OPTIONAL threshold_fn (default None = byte-identical for every existing caller), evaluated PER SCORED ITEM at both predicate sites (pipeline() immediate arm + select_items() worth filter); bootstrap closes over live_persona.matrix.proactivity so the gate reads the LIVE level at scoring time (next-scored-item semantics, the TK-209 read seam — no restart). Independently verified PASS — all four ACs re-executed: fail-loud on missing block; BALANCED reproduces the threshold_fn=None gate item-by-item (true no-op, zero-config gate = today's gate); bounded weakly-monotone sweep + every cap/ceiling byte-untouched (NG-3); live set(MINIMAL) between two pipeline() calls flips SURFACE_IMMEDIATE→HOLD. Adversarial recomputation against shipped config (MINIMAL→0.85, FORWARD→0.65) + extreme-base clamp sweep held. Quality bar (verifier's own run; architect re-ran pre-commit): pytest 1198 passed / 61 documented PG-DSN skips / 0 failed; ruff clean; mypy strict Success 274 files. files_in_scope normalized to reality (+= tests/unit/test_wombat_params.py, −= stages/gate_stage.py — the seam lives in gate/pipeline.py per v1.91). validate PASS (199 nodes/171 gov); gate PASS (analyze). NEXT per Q-107: TK-216 (degrade-path persona deltas). | architect | TK-215, TK-209, TK-216, EP-33, Q-107, DEC-37, DEF-1, NG-3, NG-4, CON-1, CON-3, contract v1.94 |
| 2026-07-10 | **TK-216 CLOSED (COMMIT, contract v1.95 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this ticket DISCHARGES DEC-37(e)'s degrade-path persona deltas per the Q-107(b) ruled shape).** Board 118/135, Q-107 batch 4/5 (EP-33, P2). The deterministic no-model path now honors ONLY the two axes a template can honestly express. TemplateComposer (compose/templates.py) gains an OPTIONAL live_persona ctor arg (default None) reading matrix.brevity FRESH on every render() — hot-apply parity with the four mouths (TK-209): None/TERSE renders TODAY'S EXACT one-line bytes (regression pin); BALANCED = kind-header line + one sorted key/value line per field; EXPANSIVE appends the fixed module-level closing constant. brief_template.py gains persona_degrade_wrap(body, matrix) wrapping render_brief_lines' output at the S8 DEGRADED branch ONLY — IDENTITY at DEFAULT_MATRIX (the TK-100 single-source invariant restated: fallback = persona_degrade_wrap(body, matrix)); BALANCED prepends the fixed brief header line, EXPANSIVE also appends the fixed closing line, warmth=WARM prepends the ONE fixed greeting line ahead of everything (RESERVED/NEUTRAL add nothing). Directness/Humor have NO degrade variant BY RULING (a template cannot honestly hedge or joke — the honest asymmetry of DEC-37) and are NEVER READ at either degrade site, pinned by new tests/persona/test_degrade_variants.py. bootstrap.build_compose_stage threads the SAME live_persona into TemplateComposer at the ONE production construction site (the v1.91 files_in_scope normalization held); stages/compose.py correctly UNTOUCHED — the model USER message still calls format_payload_fields directly, model-path bytes never move. Independently verified PASS — all three ACs re-executed: byte-identity at DEFAULT/None re-derived independently; BALANCED/EXPANSIVE + WARM proven as a RENDER-TIME read (real LivePersona.set() between two render() calls on the SAME composer flips output to the documented constants); any directness/humor level leaves both sites byte-unchanged. Scope exactly the briefed 6 files (5 modified + 1 new test module); all other TemplateComposer constructions are tests defaulting None → DEFAULT_MATRIX → TERSE → original bytes. Quality bar (verifier's own run; architect re-ran pre-commit): pytest 1225 passed / 61 documented PG-DSN skips / 0 failed; ruff clean; mypy strict Success 275 files; targeted AC suite 120 passed. Non-goals held: zero model calls in the degrade path, no hedging/humor simulation, zero gate/queue touch (CON-3/NG-4). validate PASS (199 nodes/171 gov); gate PASS (analyze). NEXT per Q-107: TK-210 (env-gated live output-effect eval harness — CLOSES EP-33). | architect | TK-216, TK-209, TK-215, TK-210, TK-100, EP-33, Q-107, DEC-37, NG-4, CON-1, CON-3, contract v1.95 |
| 2026-07-10 | **TK-210 ADJUDICATED (COMMIT 9c9abe1, contract v1.96 — within authority: the no-placebo mandate is TK-210/EP-33's own recorded bar; no locked DEC touched).** The trip-wire FIRED on the first armed live run and is ruled to have WORKED. ISS-7 raised naming the findings: humor dry-vs-none does NOT separate (majority=False both levels, builder-admitted), brevity BALANCED does not order strictly (terse<expansive holds), brief-half humor UNKNOWN (aborted — a harness defect). The builder's silent brevity-narrowing = a placebo violation (humor's left-red handling was correct; the two MUST be symmetric). Ruled CLAUSE-STRENGTH, not concept — 'A touch of dry humor is welcome.' cannot move a mouth also instructed to be terse. TK-210 amended: AC2 symmetric fail-loud/no-narrowing + NEW AC4 brief-half executes independently with explicit per-axis-per-mouth verdicts. The two untracked harness files ruled KEPT IN-TREE uncommitted as the next builder's starting point. TK-210 stays todo, re-lands next batch. |
| 2026-07-10 | **JIM'S PERSONA-BUILDER REDESIGN RECORDED (COMMIT 422df17, contract v1.97 — DEC-38, JIM AUTHORITY verbatim-in-spirit; architect recorded intent + minted carriers, no re-derivation).** 'Be bold... our current algo for the prompt is a bit primitive... hard-coding something akin to a switch statement where there is likely to be growth.' (1) Humor UNBARRED structurally — per-mouth behavior becomes POLICY DATA (supersedes-in-part DEC-37(c); shipped default = today's placement exactly, zero behavior change). (2) Strategy seam render(mouth, matrix, cues) -> Expression; v1 strategy = existing clause algebra + EMPTY cues; typed extensible cues bag (mood/scene/temporal/tone/INTONATION). (3) Expression intonation-ready — a TYPE that grows TTS delivery hints, never assumed a bare string. (4) Tweakability first-class — ONE versioned persona_policy.yaml (params custody, fail-loud, human-edited); TK-210's permitted-set derives from the SAME policy so tweaks are never silently outside eval. (5) Seam-enforced invariants: purity, DEFAULT byte-identity, guard_suffix unremovable by any strategy/cue, measurable-or-flagged. NEW TK-219 -> TK-220 -> TK-221; TK-210 deps += TK-219/TK-221; EP-33 amended; ISS-7 resolved-by-routing (enforcement now lives in runnable ACs — a non-separating axis still requires a NEW governance record before TK-210 lands). |
| 2026-07-10 | **JIM'S EP-32 APP RESCOPE + DESIGN SYSTEM RECORDED (COMMIT cf8a05e, contract v1.98 — DEC-39, JIM AUTHORITY verbatim-in-spirit; chat/audio mechanics = architect within-frame calls).** (1) CHAT rides the FULL queue/gate/mouth discipline: ChatSource on the TK-161 PushSource pattern, own chat item kind through the deterministic gate, compose-mouth replies over the stage-name-addressed output wire (reply-sink mechanics -> pre-build Q); RUNTIME-owned 127.0.0.1+token chat surface w/ data-dir handshake — the settings API keeps zero-runtime-imports (supersedes-in-part DEC-31's entire-backend clause + DEC-30's config-surface-ONLY sizing clause; DEC-29 zero-analytics REAFFIRMED). (2) Settings stand; DEC-38 policy tweak surface stays file-based v1 (app editor = flagged follow-up). (3) Mic capture -> existing drop-dir/ASRSource path (zero new Python ingest), device-select + mute renderer-local, voice on/off via settings w/ restart notice; OUTPUT VOLUME EXCLUDED v1 — no winsound gain seam, and the no-placebo bar EXTENDS TO CONTROLS (no dead slider). (4) Design requirements BINDING on TK-225: bright color-theory TOKENIZED palette, COMPLETE Tailwind theme (never MUI), NO gradients, icons, very-little-animation allowlist, scale rules in app/DESIGN.md, not-ai-slop; design latitude granted inside them. NEW TK-222/223/224/225; TK-198/TK-200/EP-32 amended. Board 118/142. NEXT BATCH RECOMMENDED: TK-219 -> TK-220 -> TK-221 -> TK-210 (closes EP-33); CR5 review pass AFTER it (the redesign reopens the persona surface — one settled-arc review), then EP-34 pair, then the EP-32 chain. |
| 2026-07-10 | **EP-33 REDESIGN-CLOSE BATCH SELECTED + PRE-BUILD RULED (COMMIT, Q-108, contract v1.99 — within authority: ticket-level how inside DEC-38/DEC-37/ISS-7/CON-1/CON-3; no locked DEC touched; orchestrator-authorized under Jim's delegation).** BATCH (4, build order): TK-219 → TK-220 → TK-221 → TK-210 — the v1.98 recommended sequence, closing EP-33. Deliberately 4-not-5 so the CR5 persona-arc review pass sits immediately after (one settled-arc review; TK-211 would smear the boundary). Rulings: (a) TK-219 strategies render BODY ONLY — the seam appends the guard_suffix (structural unremovability, DEFAULT byte-identity by construction); assistant_name = strategy-construction state (DEC-38 signature verbatim); instruction_for/LivePersona.instruction stay str-compatible delegates. (b) TK-220 persona_policy.yaml on the params.py custody discipline (importlib.resources, PERSONA_POLICY_VERSION=1 lock-step, fail-loud); closed vocab = the FOUR prompt axes (proactivity rejected — gate-side); loader enforces DEFAULT-level clauses EMPTY (byte-identity structural at load); guard_suffix is NOT policy. (c) TK-221 verdict rule = the in-tree fixtures' recorded constants (SAMPLES_PER_LEVEL=9, superseding the stale N=3 phrase); budget 3 live rounds/axis then mandatory governance record before TK-210 lands; restores the full brevity ordering assertion; live arming per the standing Jim sanction. (d) TK-210 permitted-set from load_persona_policy() (DEC-38(4)); symmetric fail-loud verdicts; one parametrized test per axis-mouth pair (AC4); the two ISS-7 kept-in-tree files are the starting point and get committed. Every model seam re-verified in installed cog-worx source (build_model registry.py:336, BudgetPolicy.new_guard budget.py:105, ChatMessage/Model.complete base.py). validate PASS (206 nodes/175 gov); gate PASS (analyze). Board 118/142 — governance only. | architect | Q-108, TK-219, TK-220, TK-221, TK-210, TK-211, TK-212, EP-33, EP-34, DEC-37, DEC-38, ISS-7, Q-107, CON-1, CON-3, DEF-7, contract v1.99 |
| 2026-07-10 | **TK-219 CLOSED (COMMIT, contract v2.00 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this ticket DISCHARGES DEC-38(2)/(3)/(5) per the Q-108(a) ruled shape).** Board 119/142, Q-108 batch 1/4 (EP-33, P2). The Expression strategy seam lands: NEW src/wombat/persona/expression.py — frozen Expression (instruction text v1; the TYPE pinned as intonation-ready, never assumed a bare string), frozen all-optional Cues bag (mood/scene/temporal/tone/intonation) + EMPTY_CUES, RenderStrategy Protocol render(mouth, matrix, cues) → Expression, and the seam render_expression(...) which appends the mouth's guard_suffix to the strategy's BODY — the _GUARD_SUFFIX table moved into expression.py and is consumed ONLY by the seam, so no strategy or cue can omit/mangle it (structural unremovability, DEFAULT byte-identity by construction). builder.py's clause algebra became ClauseAlgebraStrategy (frozen; assistant_name = construction state, DEC-38 signature verbatim); instruction_for stays a thin str-compatible delegate — every call site byte-unaffected; live.py docstring-only; Mouth referenced in expression.py under TYPE_CHECKING only (no cycle). Independently verified PASS — AC1 byte-identity re-derived against all four LIVE oracles (untouched) for all four mouths, delegate byte-matches; AC2 adversarial guard-mangling strategy still yields the verbatim guard (seam-enforced) + determinism; AC3 additive cue growth proven, runtime constructs EMPTY_CUES everywhere. Scope exactly the briefed files (3 modified + 2 new); the two ISS-7 kept-in-tree harness files untouched (commit with TK-210 per Q-108(d)); no contract touch by the builder. Quality bar (verifier's own run; architect re-ran pytest green pre-commit): pytest 1253 passed / 66 documented DSN-gated skips / 0 failed; ruff clean; mypy Success 279 files; targeted persona suite 77 passed. validate PASS (206 nodes/175 gov); gate PASS (analyze). NEXT per Q-108: TK-220 (persona policy-as-data). | architect | TK-219, TK-220, TK-221, TK-210, EP-33, Q-108, DEC-38, ISS-7, CON-1, contract v2.00 |
| 2026-07-10 | **TK-220 CLOSED (COMMIT, contract v2.01 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this ticket DISCHARGES DEC-38(1)/(4) per the Q-108(b) ruled shape).** Board 120/142, Q-108 batch 2/4 (EP-33, P2). Persona policy-as-data lands: NEW src/wombat/persona/persona_policy.yaml (version 1; mouth_axes — compose/brief carry all four prompt axes, draft/reflection carry brevity/warmth/directness with the outbound-voice and NG-2-adjacency rationales recorded as YAML comments, so the DEC-37(c) humor walls are now policy DEFAULTS, not code bars; clauses = a FAITHFUL byte-exact transcription of the pre-TK-220 _LENGTH/_REGISTER/_HEDGING/_HUMOR_CLAUSES module tables, every DEFAULT-level entry empty) + NEW src/wombat/persona/policy.py (PERSONA_POLICY_VERSION=1 lock-step; frozen PersonaPolicy; PersonaPolicyError; load_policy via importlib.resources w/ explicit-path fixture support on the params.py precedent; default_policy() = lazily-loaded process-cached singleton, restart-to-apply v1, NO runtime writer). Closed vocabularies enforced fail-loud NAMING the offense — exactly four mouths, exactly the FOUR prompt axes (proactivity REJECTED anywhere — gate-side per TK-215), matrix-enum level names, version mismatch loud, and the DEC-38(5) byte-identity invariant made STRUCTURAL at load (DEFAULT-level clause MUST be empty). ClauseAlgebraStrategy's policy field defaults to default_policy() and renders in the fixed _AXIS_ORDER filtered by mouth_axes; guard_suffix stays SEAM-owned (TK-219 — doc references only, no YAML entry). Independently verified PASS — AC1 byte-identity pins compare against the four INDEPENDENT live oracles (a bad transcription is genuinely caught), full 162-matrix property tests + humor-absent-from-draft/reflection pass fed from default_policy(), YAML clauses line-by-line compared to the old tables (byte-exact); AC2 a fixture policy granting humor to draft renders it with ZERO code changes, guard_suffix still present; AC3 missing/malformed/unknown-key loads fail LOUD, no silent fallback. Scope clean (2 modified + 3 new files; the two ISS-7 kept-in-tree harness files untouched, commit with TK-210). Quality bar (verifier's own run; architect re-ran pytest green pre-commit): pytest 1281 passed / 66 documented DSN-gated skips / 0 failed; ruff clean; mypy Success 281 files. Non-goals held: no hot-reload, no settings-app editor, no clause-strength retuning (TK-221). validate PASS (206 nodes/175 gov); gate PASS (analyze). NEXT per Q-108: TK-221 (clause-strength calibration). | architect | TK-220, TK-219, TK-221, TK-210, TK-215, EP-33, Q-108, DEC-38, DEC-37, ISS-7, NG-2, contract v2.01 |
| 2026-07-10 | **TK-221 CLOSED (COMMIT, contract v2.02 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this ticket DISCHARGES ISS-7's measurability finding under DEC-38(5)'s measurable-or-flagged invariant).** Board 121/142, Q-108 batch 3/4 (EP-33, P2). Clause-strength calibration lands on the MEASURABLE-SEPARATION branch — both ISS-7 axes now move the live mouth under the recorded, UNWEAKENED verdict rule, via data-only edits inside the Q-108(c) 3-round/axis budget. persona_policy.yaml: humor.dry names the mechanism explicitly ('Work in one dry, understated aside, set off in parentheses or dashes.' — the vague 'A touch of dry humor is welcome.' had produced majority=False at both levels); brevity.balanced = an explicit two-to-three-full-sentences ask, brevity.expansive = a four-to-six-sentence short paragraph, so the three levels hold a strictly ordered chain (live medians ~98/171/334 chars); every change carries its ISS-7/Q-108(c) rationale as YAML comments; DEFAULT-level clauses stay EMPTY (loader-enforced — byte-identity untouched by construction, test_builder 39 passed). Harness fixture/lexicon constants tuned honestly in the two ISS-7 in-tree files; the brevity assertion RESTORED to the literal terse<balanced<expansive three-level ordering at N=9 (round-1 narrowing reverted per Q-108(c)). Binding rules held: SAMPLES_PER_LEVEL=9 / strict majority_verdict / strict-monotone median_response_length / has_humor_aside all intact; zero builder/seam/mouth/policy.py code change. INDEPENDENTLY VERIFIED PASS — AC1 re-executed live (real DeepSeek creds) on TWO separate non-concurrent runs (101s/104s, 3 passed each — not a knife-edge): humor separates dry-vs-none on compose AND brief, brevity orders strictly; AC2 scope exactly the 3 files_in_scope. Quality bar (verifier's own run; architect re-ran pytest green pre-commit): pytest 1281 passed / 66 documented DSN-gated skips / 0 failed; ruff clean; mypy Success 281 files. WITHIN-FRAME RULING recorded in the v2.02 changelog: the two ISS-7 kept-in-tree harness files commit WITH TK-221 (its files_in_scope, materially edited by it), amending Q-108(d)'s commit-with-TK-210 phrasing — TK-210's structural re-land obligation unchanged, now from committed state. The non-separation contingency (mandatory governance record) never triggered. validate PASS (206 nodes/175 gov); gate PASS (analyze). NEXT per Q-108: TK-210 (harness re-land — CLOSES EP-33), then the CR5 persona-arc review pass. | architect | TK-221, TK-220, TK-219, TK-210, EP-33, Q-108, DEC-38, ISS-7, CON-1, contract v2.02 |
| 2026-07-10 | **TK-210 CLOSED — EP-33 CLOSED (COMMIT, contract v2.03 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this lands the DEC-38(4) policy-derived permitted-set and completes the DEC-38(5)/ISS-7 measurable-or-flagged bar; EP-33 done-bar reached).** Board 122/142, Q-108 batch 4/4 (EP-33, P2). The output-EFFECT harness re-lands against the v1.96 amended ACs: permitted-set derived at collection time from default_policy().mouth_axes (HUMOR_PRESENT_MOUTHS/HUMOR_ABSENT_MOUTHS — the same loaded policy the builder renders with; operator policy tweaks land inside eval coverage automatically, never silently outside); AC4 half-independence — each humor mouth its own parametrized case, every axis-mouth pair yields an explicit pass/loud-fail/loud-skip verdict in one run. Verdict rule UNWEAKENED (N=9, strict majority, strict-monotone median); brevity asserted across all three levels. ONE file edited (test_output_effects_live.py); eval_fixtures.py verified already-conformant, untouched. Independently verified PASS — AC1 no-creds 2 passed / 7 loud skips all naming WOMBAT_TEST_PERSONA_EVAL_LIVE, zero network; AC2 armed live (real DeepSeek creds, standing sanction) 9 passed in 161.64s; AC3 trip-wire mechanism proven ungated; AC4 structural. Quality bar (verifier own run; architect re-ran pytest green pre-commit): pytest 1281 passed / 68 documented skips / 0 failed; ruff clean; mypy Success 281 files. validate PASS (206 nodes/175 gov); gate PASS (analyze). NEXT per v1.98 sequencing: the CR5 persona-arc review pass, then the EP-34 pair (TK-211/TK-212). | architect | TK-210, TK-221, TK-220, TK-219, TK-215, EP-33, Q-108, DEC-37, DEC-38, ISS-7, DEF-7, contract v2.03 |
| 2026-07-10 | **CR5 register recorded (COMMIT — docs-only, within authority; contract untouched, routing deferred to the next governance step).** Fifth cross-cutting review pass (persona arc, `a8b2dba..HEAD`: EP-33 complete TK-206/207/208/209/215/216/219/220/221/210 + TK-196 settings source + CR4 fixes TK-217/218) written to `planning/code-review-2026-07-10b.md`, all 3 findings CONFIRMED (executable repro on this box + source verification), ZERO critical, zero plausible-but-unconfirmed. Both majors are settings.json boot-safety holes in TK-196's source: **CR5-1 MAJOR** the app-editable JSON source reads with locale encoding (cp1252 — `config.py:210` passes no `json_file_encoding`) while every `persona/live.py` writer/reader uses UTF-8; reproduced silent mojibake of a non-ASCII `wombat_assistant_name` into all four mouths' prompts/TTS AND an uncaught `UnicodeDecodeError` boot brick on valid UTF-8 JSON (the `config.py:78` guard catches only `JSONDecodeError`), violating the CON-3 "app file never fails boot" guarantee; **CR5-2 MAJOR** an out-of-vocab persona value in settings.json (e.g. `humor: playful`) passes the allowlist filter unvalidated (`config.py:96-99`), fails the `Literal` check, and `load_config` bricks the ENTIRE standing process at next restart — while the running LivePersona poll tolerates the identical value with one WARNING (the exact boot-vs-hot-apply asymmetry; reproduced); **CR5-3 MINOR** `poll_settings_file` advances the mtime cursor (live.py:107) BEFORE read/parse/apply, so one transient read/parse failure on a settled file permanently drops that edit generation (inverse of `set()`'s correct defer-into-else at :98; deterministic repro). Suggested routing in-register: CR5-1+CR5-2 one P2 settings-file-robustness ticket on `config.py` (utf-8 pin + guard widen + per-value validate-or-drop-loud); CR5-3 small P3 cursor-defer fix. NEXT: route CR5-* into governance/tickets, then the EP-34 pair per the Q-108 sequencing. | architect | CR5-1..3, TK-196, TK-208, TK-209, EP-33, CON-3, planning/code-review-2026-07-10b.md |
| 2026-07-10 | **CR5 ROUTED + NEXT BATCH SELECTED + PRE-BUILD RULED (COMMIT, contract v2.04 — within authority: CR routing, ticket minting, sequencing, ticket-level how; no locked DEC touched; orchestrator-authorized under Jim's delegation).** CR5 register routing ADOPTED as suggested: NEW **TK-226** (EP-32, P2 — settings-file boot robustness: utf-8 pin on the app-editable JSON source, decode-guard widened to UnicodeDecodeError/OSError, per-value TypeAdapter validate-or-drop-loud; closes CR5-1+CR5-2) + NEW **TK-227** (EP-33, P3 — poll_settings_file cursor-defer with once-per-generation warning; closes CR5-3). Board 122/144. **BATCH (5, build order): TK-226 → TK-211 → TK-212 → TK-197 → TK-198** — the boot-brick fix HEADS the batch because TK-197 starts writing the very file whose read path it repairs; then the EP-34 pair per the standing Q-108 sequencing (CR5 pass landed; Q-107(d) ack/trail pre-rulings reaffirmed — trail = logger.info before LivePersona.set, ack = fixed template via make_speak_callable, degrade-loud); then the EP-32 chain head. Q-109 pins: TK-211 exact-match closed grammar (GRAMMAR_VERSION=1, enum-declaration-order stepping, saturating pure apply, seeded-stdlib-random property test — no hypothesis dep); TK-212 optional ASRSource command_hook + make_persona_command_hook in sources/bootstrap.py, never-raises consume, ONE shared make_speak_callable result; TK-197 settings-app extra + dev-group fastapi/uvicorn/httpx, create_app injection, X-Wombat-Token 401, APP_EDITABLE_FIELDS lock-step drift test, write-only never-echoed keys, pre-bound-socket single-line handshake, structural no-runtime-import; TK-198 electron+vite+react+ts+tailwind4+vitest, exported testable webPreferences, loopback CSP assert, AC3 probe-scoping recorded (full probe = TK-201). NO cog-worx seam load-bearing in this batch (verified). DEFERRED: TK-227, EP-32 remainder TK-225→TK-199→TK-222 (reply-sink pre-ruling due at its batch)→TK-200/223/224→TK-201, TK-213/214 (EP-35 last), browser P3s, operator OAuth/DEF-7 smokes. validate PASS (208 nodes/176 gov); gate PASS (analyze). | architect | TK-226, TK-227, TK-211, TK-212, TK-197, TK-198, Q-109, Q-108, Q-107, DEC-39, CON-3, CON-4, contract v2.04 |
| 2026-07-10 | **TK-226 CLOSED (COMMIT, contract v2.05 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this discharges CR5-1+CR5-2 per the Q-109(a) ruled mechanics).** Board 123/144, Q-109 batch 1/5 (EP-32, P2). Settings-file boot robustness lands in `_AppEditableJsonSettingsSource` (config.py only): (1) `json_file_encoding="utf-8"` pinned at the construction site — reads now match every writer of `wombat.settings.json`; (2) `_read_file`'s treated-as-absent guard widened to `(JSONDecodeError, UnicodeDecodeError, OSError)` with ONE loud WARNING — the CON-3 never-fails-boot guarantee now covers the decode/IO paths; (3) per-value validate-or-drop-loud in `__call__` via cached pydantic TypeAdapters (`_FIELD_TYPE_ADAPTERS`/`_type_adapter_for`) — an out-of-vocab admitted value (e.g. humor `playful`) drops to the field default with one WARNING naming field+value instead of bricking `load_config` at restart; env/.env strictness UNTOUCHED (AC4 — env mistakes still fail loud). Verifier re-derived all ACs independently (incl. the cp1252 mojibake/UnicodeDecodeError repro of the CR5-1 premise); scope exact to the two briefed files; pytest 1286 passed/68 skipped, ruff clean, mypy clean (281 files). The boot-vs-hot-apply asymmetry is gone; TK-197 may now write the file safely. NEXT: TK-211. |
| 2026-07-10 | **TK-211 CLOSED (COMMIT, contract v2.06 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this lands the DEC-35 closed-grammar bar per the Q-109(b) ruled pins).** Board 124/144, Q-109 batch 2/5 (EP-34, P2). The closed persona voice-command grammar lands: NEW pure src/wombat/persona/commands.py — GRAMMAR_VERSION=1; frozen PersonaCommand carrying exactly ONE of {step ±1, set_level, reset} (validated in __post_init__); GRAMMAR tuple with full coverage (one up + one down step utterance per axis, one set-level utterance per declared level of every axis, one whole-matrix 'reset persona'); step direction = enum DECLARATION order exactly ('be more brief' = brevity −1 toward TERSE, 'be warmer' = warmth +1 toward WARM, 'be funnier'/'no jokes' = humor ±1, 'be more proactive' = +1). parse_persona_command = normalize (casefold + strip + ASCII-punctuation-fold + whitespace collapse) then EXACT-EQUALITY lookup against a precomputed normalized dict — no substring/prefix/fuzzy/semantic matching; anything outside the table is None. apply() pure + saturating (clamps at ends, never wraps/raises; reset → DEFAULT_MATRIX; input matrix never mutated). Independently verified PASS — AC1 every utterance + variants yields its documented command, one-axis-one-step clamped both ends (directions re-derived against the enums; saturation at BOTH ends of EVERY axis); AC2 near-misses ('be brief about the weather', 'brief me') and the Q-109(b) seeded-stdlib-random property test all None; AC3 zero IO / zero model calls by AST inspection. Scope exactly the two new files_in_scope; no matrix.py touch; no contract touch by the builder. Quality bar (verifier own runs; architect re-ran pytest green pre-commit): pytest 1343 passed / 68 documented DSN-gated skips / 0 failed; targeted suite 57 passed; ruff clean; mypy strict Success 283 files. Non-goals held: no ASRSource wiring (TK-212 owns the interception), no LLM/fuzzy matching (DEC-35/CON-1). validate PASS (208 nodes/176 gov); gate PASS (analyze). NEXT per the v2.04 batch order: TK-212 (pre-queue interception at ASRSource). |
| 2026-07-10 | **TK-212 CLOSED — EP-34 CLOSES (COMMIT, contract v2.07 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this lands the DEC-35 pre-queue interception + the DEC-37 deterministic ack per the Q-109(c)/Q-107(d) pins; both EP-34 children now done, epic status field stays as-recorded per the EP-15/EP-16 precedent).** Board 125/144, Q-109 batch 3/5 (EP-34, P2). Matched persona voice commands are now CONSUMED at the source boundary — applied via LivePersona, trailed, acked — never enqueued, never gate-rated, never reaching the mouth; unmatched speech flows byte-unchanged. ASRSource gains ONE optional ctor kwarg `command_hook: Callable[[str], bool] | None` (default None = byte-identical pre-ticket path), evaluated in `_process_one` right after transcribe() succeeds and BEFORE the SourceEvent is built — True consumes (file still moves to processed/, no SourceEvent); None/False falls through unchanged. `make_persona_command_hook(live_persona, speak)` in sources/bootstrap.py: one INFO trail line (phrase, axis, old→new) strictly BEFORE `live_persona.set(apply(...))` (CON-4); set guarded (raise → one loud WARNING naming LivePersona.set, utterance still consumed, source keeps transcribing — CON-3); then exactly ONE fixed-template ack via the shared speak callable ('<Axis> is now <level>.' / 'Persona reset to defaults.'), zero model calls, no AwaitHuman, same guard (speak raise → one WARNING, never blocks); the hook never raises. build_source_registry/_maybe_register_asr thread live_persona/speak; the composition root passes the ONE shared make_speak_callable result. Independently verified PASS — AC1 'be warmer' → events==[], real warmth RESERVED→NEUTRAL, exactly ['Warmth is now neutral.'], trail-before-set order-asserted, file in processed/; reset variant pinned. AC2 'buy milk tomorrow' → one SourceEvent, payload equality-pinned to the pre-ticket wire shape, persona untouched. AC3 set-raise and speak-raise each → one WARNING, command consumed, source keeps processing. All 7 new tests execute and pass. Scope = exactly 5 files; files_in_scope normalized += tests/sources/test_bootstrap.py (the standing Q-109(c) ruling). Quality bar (verifier own run; architect re-ran pytest green pre-commit): pytest 1350 passed / 68 documented DSN-gated skips / 0 failed; ruff clean; mypy Success 283 files. validate PASS (208 nodes/176 gov); gate PASS (analyze). NEXT per the v2.04 batch order: TK-197 (settings API, EP-32 chain head). |
| 2026-07-10 | **TK-197 CLOSED (COMMIT, contract v2.08 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this lands the DEC-31 settings backend under the DEC-30 loopback+token posture and the DEC-32 write-only key discipline, per the Q-109(d) ruled pins).** Board 126/144, Q-109 batch 4/5 (EP-32, P2 — the EP-32 chain head). The Settings API lands: NEW src/wombat/settings_app (FastAPI+uvicorn under the NEW settings-app optional extra exactly as ruled; fastapi/uvicorn/httpx dev-group; uv.lock in scope — never core deps). create_app(settings_path, key_store, token) injection factory; every route requires X-Wombat-Token == the per-launch token (401 with a fixed generic detail on all three routes, never 404). GET /settings = {settings: each APP_EDITABLE_FIELDS field → value-or-null, keys: elevenlabs/deepgram/fish → presence booleans ONLY} — raw-JSON read (never via WombatConfig), broken-file-never-500s guard + per-provider try/except→False (CON-3); no secret in any response body ever. PUT /settings validates a hand-written pydantic mirror (extra=forbid, WombatConfig's closed Literal vocab) held by the ruled APP_EDITABLE_FIELDS lock-step drift tests, then UTF-8 read-modify-write overlaying only explicitly-sent fields (the persona/live.py _persist convention). PUT /keys/{provider} is WRITE-ONLY into the TK-188 vault; failure detail fixed/generic, never derived from the key or the exception. __main__ pre-binds (127.0.0.1, 0), prints EXACTLY ONE {port, token} handshake JSON line (flushed, first on stdout) for TK-199, then serves on the pre-bound socket; structural no-runtime-import test (subprocess-clean) — the app runs while serve() is down. Independently verified PASS — every AC re-derived and executed by the verifier: AC1 401 discipline + GET shape + stored-key absence; AC2 file update + fake-keyring landing + no-secret-echo; AC3 structural import graph + 127.0.0.1 pin + exactly-one-parseable-handshake-line. Scope exact (only pyproject.toml/uv.lock modified + the two new dirs; pyproject diff = exactly the ruled extra + dev-group). Quality bar (verifier own runs; architect re-ran pytest green pre-commit): pytest 1370 passed / 68 documented DSN-gated skips / 0 failed; targeted settings_app suite 20 passed; ruff clean; mypy Success 288 files. Non-goals held: no static assets, no auth beyond loopback+token (DEC-30), no DeepSeek/Google/DSN editing (DEC-32 operator-surface). validate PASS (208 nodes/176 gov); gate PASS (analyze). NEXT per the v2.04 batch order: TK-198 (Electron shell scaffold, batch 5/5). |
| 2026-07-10 | **TK-198 CLOSED (COMMIT, contract v2.09 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this lands the DEC-31 Electron shell with the security posture pinned by config-assert tests).** app/ is the repo's FIRST JS toolchain — Electron main + preload + Vite/React/TS renderer, Tailwind wired at the scaffold (DEC-39; token/theme system stays TK-225), Node strictly an app/-only dev toolchain, never a wombat-core dep. Posture baked AND test-asserted: window-options pins contextIsolation=true / nodeIntegration=false / sandbox=true + the preload path (vitest); the security test parses index.html's CSP and asserts connect-src = self + loopback ONLY ('*' and https rejected) and main.ts loadFile-only — no loadURL, no remote content ever; built dist/index.html preserves the CSP. Renderer boot runs assertNoNodeGlobals() (errors AND throws on reachable window.require/window.process); the live in-window Playwright probe is explicitly deferred to TK-201, not overclaimed. Verifier ran the whole bar (Node 24.11.1): install 0 vulns, build green, electron . spawned the full window tree and killed clean with zero lingering processes, vitest 9/9, tsc clean, adversarial grep zero posture escapes. Scope exact — 13 source files incl. package-lock.json committed; .gitignore extended (app/node_modules, dist, dist-electron, out never land). Python bar untouched; architect re-ran pytest green pre-commit. Board 127/144; the v2.04 batch is COMPLETE. NEXT: governance selects the EP-32 continuation — TK-199 (main-process → settings-API lifecycle) is the natural head, sole dep now done. | architect | TK-198, EP-32, DEC-31, DEC-39, DEC-30, RISK-11 |
| 2026-07-10 | **EP-32-BODY BATCH SELECTED + PRE-BUILD RULED (COMMIT, contract v2.10, Q-110 — within authority: sequencing + ticket-level how inside DEC-29/31/32/37/39 + CON-1/3/7; DEC-39(1) explicitly delegated the chat mechanics to this ruling; no locked DEC touched; orchestrator-authorized under Jim's delegation).** Batch (build order): **TK-227 → TK-225 → TK-199 → TK-222 → TK-200** — the Q-109-recorded EP-32 chain continued verbatim; deferred TK-227 picked up as the fix-first head (TK-200's hot-apply hint is only honest once the mtime poll can't drop an edit generation). THE chat ruling (Q-110d): correlation = pre-computable item_id (`idempotency_key('chat', event_key)` == GateItem.item_id, verified in gate.py — no correlation id in the mouth-visible payload, CON-1); reply sink = NEW never-raise pass-through stage `chat_reply` between compose and speak at both build_drain_pathway sites (TK-164 flip precedent; SpeakSink byte-unaffected — reads last_output('compose') by name) resolving a ChatReplyBroker future registered before push; ItemKind.CHAT additive → EventClass GENERIC fallback (no rating-vocabulary change) → the EXISTING generic mouth; surface = stdlib-only asyncio loopback HTTP + per-launch token + handshake JSON at new config field `wombat_chat_handshake_file`; 30s honest `held` timeout (gate authority stands, never a hang); start/stop guarded — chat failure never touches the drain loop (CON-3/CON-7); zero new runtime deps (fastapi/uvicorn stay settings-app-extra-only). Also ruled: TK-227 cursor-defer semantics (advance only on success-or-vanish; non-dict reclassified malformed; warn once per failing mtime generation), TK-225 shape (theme.css `@theme` = single token source, tokens.ts names-only, lucide-react, Panel/Button/Field/Select/Indicator/Icon, vitest grep audits + DESIGN.md animation allowlist), TK-199 pins (WOMBAT_PYTHON→python spawn, 15s handshake timeout, `ipcMain 'wombat:settings-api-info'` + `contextBridge 'wombatSettings'.getInfo()` — TK-200 binds THIS name, kill-on-quit, showErrorBox degrade), TK-200 (Q-109(d) API shapes + DEC-37 hot-apply-vs-restart notice split + zero-analytics scan test). files_in_scope normalized on TK-222/TK-225/TK-200. DEFERRED: TK-223/TK-224/TK-201 (EP-32 close next batch), TK-213/TK-214 (EP-35 recorded-last; **flag**: behavior-event row has no axis/direction columns — TK-213 needs a persona_feedback row-encoding ruling at ITS batch), TK-131..136/TK-153 (browser arc opens after EP-32 closes), OAuth bring-up (operator-owned). |
| 2026-07-10 | **TK-227 CLOSED (COMMIT, contract v2.11 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this discharges CR5-3 per the Q-110 ruled cursor-defer semantics).** Board 128/144, Q-110 batch 1/5 (EP-33, P3 — the fix-first head; EP-33's post-closure fix ticket, the epic reads fully done again). `LivePersona.poll_settings_file` now advances the `_last_mtime` cursor ONLY after a successful generation — one transient read/parse failure on a settled file can no longer permanently drop that edit generation. Restructure exactly per the ruling: unchanged-mtime early return stands; `mtime is None` advances the cursor and returns (a vanished file is a legitimate observation, no warning); read/parse/apply wrapped in try with the non-dict top level RECLASSIFIED as malformed (raises inside the try, caught uniformly); the except arm leaves the cursor STANDING so the next Sweeper beat retries, warning via the new `_last_warned_mtime` guard once per failing mtime generation (a different mtime re-warns); the else arm advances the cursor on success including a valid dict carrying none of the five persona keys. No signature change; set()/_persist and the DEC-37(g) last-write-wins posture untouched; still never raises (CON-3). Independently verified PASS — AC1 fail-then-succeed recovery re-derived (monkeypatched one-shot read failure leaves the cursor standing, retry applies humor=DRY; verifier's own probe confirmed the malformed-then-valid variant); AC2 warn-once-per-generation + different-mtime re-warn; AC3 pre-existing suite green unchanged (12 untouched + 3 new; targeted 15 passed). Scope exactly the two files_in_scope. Quality bar (verifier own runs; architect re-ran pytest green pre-commit): pytest 1373 passed / 68 documented DSN-gated skips / 0 failed; ruff clean; mypy Success 288 files. validate PASS (208 nodes/177 gov); gate PASS (analyze). NEXT per the v2.10 batch order: TK-225 (design system). | architect | TK-227, TK-225, EP-33, EP-32, Q-110, DEC-37, CON-3, contract v2.11 |
| 2026-07-10 | **TK-225 CLOSED (COMMIT, contract v2.12 — within authority: ticket closeout on independent verifier PASS after one repair round; no locked DEC touched — this lands the DEC-39(4) binding design requirements per the Q-110 ruled shape).** Board 129/144, Q-110 batch 2/5 (EP-32, P2). The design system lands: `app/src/theme.css` = the SINGLE token source (one `@theme` block, every color/radius/font custom property; BRIGHT triadic OKLCH palette — brand amber ~75°, accent teal ~195° exclusively the focus ring, the ~315° magenta triadic point documented-but-undeclared until consumed; danger/positive single-purpose off-triad; WARM near-neutral surfaces/inks — the deliberate not-ai-slop differentiator; lightness-step hover/active ladders with per-fill `-ink` companions). `tokens.ts` exports typed NAMES only — complete literal class strings, never values, never interpolation-built classes (the Tailwind static-scanner honesty rule). Components Panel/Button/Field/Select/Indicator/Icon + cn; lucide-react the ONE icon set through the Icon wrapper; one shared focusRing everywhere; TK-198's placeholder App restyled (styles.css deleted). HONESTY AUDITS as vitest greps over ALL of app/src (test-support/audit-fs.ts walker): color/gradient/MUI/animation(pinned two-entry allowlist transition-colors+transition-shadow, doc-and-test lockstep)/icon audits + design-doc presence test. DESIGN.md records the triadic rationale, theme completeness, the allowlist, and FIVE extension rules (incl. the no-placebo bar carried into design); dark/light switcher honestly flagged not-free. REPAIR ROUND 1: Button secondary built hover/active classes by template interpolation — invisible to Tailwind's scanner, utilities NEVER compiled; fixed via NEW `interactive.neutral` literal token group; verifier confirmed the selectors now present in built CSS (previously absent) and swept app/src — zero remaining interpolation anti-pattern instances. Independently verified PASS — repair verified + full AC set re-derived (one-token re-theme + zero hard-coded colors; Tailwind-not-MUI/zero-gradients/allowlisted-animation/one-icon-set; completeness + written rationale + extension rules). Quality bar (verifier own runs; architect re-ran ALL green pre-commit): npm test 55 passed (8 files incl. all six audits), tsc clean, build green (css 12.14kB, hash-identical dist); Python bar untouched — pytest 1373 passed / 68 documented skips / 0 failed. files_in_scope normalized (+= design-doc.test.ts, test-support/; −= index.html untouched). validate PASS (208 nodes/177 gov); gate PASS (analyze). NEXT per the v2.10 batch order: TK-199 (Electron main → settings-API lifecycle bridge). | architect | TK-225, TK-198, TK-199, TK-200, EP-32, Q-110, DEC-39, DEC-29, contract v2.12 |
| 2026-07-10 | **TK-199 CLOSED (COMMIT, contract v2.13 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this lands the DEC-31 clause that the Electron main SPAWNS AND OWNS the loopback settings API, port+token reaching the renderer via contextBridge ONLY, per the Q-110 ruled pins).** Board 130/144, Q-110 batch 3/5 (EP-32, P2). NEW app/electron/api-process.ts — pure-testable spawn/parse/timeout/teardown: resolvePythonCommand (WOMBAT_PYTHON env var → default python), fixed `-m wombat.settings_app` args (the TK-197 handshake contract), STRICT parseHandshake (one JSON object, integer port + nonempty string token; everything else rejected), startApiProcess classifying EVERY failure distinctly (spawn-error/premature-exit/parse-error/timeout; HANDSHAKE_TIMEOUT_MS=15000 per the ruling; child killed on timeout/parse failure), IDEMPOTENT teardown() (child.kill() exactly once), describeFailure() naming which failure. main.ts awaits the handshake in app.whenReady BEFORE any window — failure → dialog.showErrorBox + app.exit(1), a window NEVER opens (loud degrade, no silent blank window); success → ipcMain.handle('wombat:settings-api-info') + teardown wired into BOTH window-all-closed and before-quit (no orphan python). preload.ts exposes window.wombatSettings.getInfo() via contextBridge EXCLUSIVELY — never a URL parameter; TK-200 binds these exact pinned names. Independently verified PASS — AC1 parse accept/reject matrix (7 tests) + scripted node stand-in handshake resolve (real python spawn = TK-201 smoke); AC2 double-teardown → kill exactly once (spy); AC3 real elapsed timeout (314ms, not faked) + the error-surface path. Targeted suite 22/22, no skips. Scope exactly the 4 files_in_scope, zero python touched (unrelated in-flight python mods excluded from the commit); no contract touch by the builder. Quality bar (verifier own runs; architect re-ran green pre-commit): npm test 77 passed / 9 files, tsc clean, build green; pytest green (ticket changed no python); ruff clean. validate PASS (208 nodes/177 gov); gate PASS (analyze). NEXT per the v2.10 batch order: TK-222 (chat surface — the Q-110(d) reply-sink/correlation ruling is already on record). | architect | TK-199, TK-197, TK-198, TK-200, TK-201, TK-222, EP-32, Q-110, DEC-31, DEC-30, contract v2.13 |
| 2026-07-10 | **TK-222 CLOSED (COMMIT, contract v2.14 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this lands the DEC-39(1) chat seam per the Q-110(d) ruled reply-sink/correlation mechanics).** Board 131/144, Q-110 batch 4/5 (EP-32, P2). The runtime chat seam lands: NEW `sources/chat_source.py` — ChatSource is a bare PushSource (id `chat`, 1.0s poll cadence) registered like every source (TK-161 pattern); chat is a REAL INPUT SOURCE, never a model side-channel (CON-1). NEW `chat/surface.py` — ChatReplyBroker (`dict[str, asyncio.Future[str]]`; register-BEFORE-push; unknown-id resolve = no-op) + ChatSurface, STDLIB-ONLY `asyncio.start_server` on 127.0.0.1 with a per-launch random token (X-Wombat-Chat-Token → 401 on tokenless/wrong; OPTIONS preflight token-free with CORS headers) and ONE handshake JSON (port+token) in the runtime data dir (TK-197 posture; CON-7). POST /chat mints `event_key=uuid4().hex`, PRE-COMPUTES `item_id=idempotency_key(source.id, event_key)` BEFORE pushing, awaits the future up to 30.0s → replied-with-text or an HONEST held on timeout. `ItemKind.CHAT` (additive) → EventClass.GENERIC (no new EventClass); bootstrap routes CHAT to the EXISTING compose mouth — enqueued, gate-rated, NEVER gate-bypassed (the ASR precedent). NEW `stages/chat_reply.py` — pass-through stage; compose terminal flips speak → chat_reply (the stage-name-addressed composed-output wire); broker=None = chat-disabled pure pass-through, drain/brief never depend on chat (CON-3). Independently verified PASS — AC1 real-surface round trip (tokened POST → one SourceEvent → registry-derived idempotency_key round-trips through gate_item_from_queue_item to the pre-computed item_id → ItemKind.CHAT → composed text back on the SAME connection) + structural import-scan/spy-egress: NO chat-to-model path skips queue+gate (chat modules import stdlib + domain/sources only; Enqueuer the sole egress; no correlation id in any payload). AC2 401s + loopback-only bind + exactly one parseable handshake per launch. AC3 surface failure/death → one loud WARNING, everything else unaffected. Quality bar (verifier own runs; architect re-ran pytest green pre-commit): pytest 1397 passed / 68 documented DSN-gated skips / 0 failed; ruff clean; mypy Success 296 files. files_in_scope normalized (+= the five mechanical speak→chat_reply terminal-assertion test files); `scripts/demo_drain.py` confirmed already-stale PRE-TK-222 (compose is its terminal, no speak_stage — no new breakage; outside the bar). Non-goals held — no streaming, no chat-history store, no renderer UI (TK-223). validate PASS (208 nodes/177 gov); gate PASS (analyze). NEXT per the v2.10 batch order: TK-200 (settings forms over the TK-199 bridge). | architect | TK-222, TK-161, TK-197, TK-199, TK-223, TK-200, EP-32, Q-110, DEC-39, CON-1, CON-3, CON-7, contract v2.14 |
| 2026-07-10 | **TK-200 CLOSED (COMMIT, contract v2.15 — within authority: ticket closeout on independent verifier PASS; no locked DEC touched — this lands the settings forms per the Q-110(e) ruled shape, completing the Q-110 batch 5/5).** Board 132/144 (EP-32, P2). The renderer's ONE settings form lands: NEW `app/src/api.ts` — tokened fetch wrapper; `window.wombatSettings.getInfo()` (TK-199 preload bridge, the ONLY renderer surface) resolved once per session, `X-Wombat-Token` on every request, fetch origin derived ENTIRELY from the bridge port (loopback the only literal URL); `KEY_PROVIDERS` + the `SettingsUpdate` Literal vocabularies mirrored verbatim from `settings_app/api.py`; GET /settings, PUT /settings (touched fields only), PUT /keys/{provider} (write-only, no secret ever echoed). NEW `app/src/App.tsx` — form on the TK-225 Panel/Button/Field/Select/Indicator components over tokens.ts (plain useState, no framework/router): name, STT/TTS provider selects, voice id, the five DEC-33 persona axis selects (verbatim levels, DEFAULT_MATRIX fallback), three write-only MASKED key fields with Indicators driven SOLELY by the GET keys booleans (inputs never carry a fetched value, cleared after save). Save = ONE PUT /settings (touched non-secrets only) + one key PUT per non-empty input + refresh GET; **DEC-37 notice split** — restart notice iff name/provider/voice-id/key touched, persona hot-apply hint iff persona axes touched (both/either/neither per THAT save). DEC-29 made STRUCTURAL — NEW `analytics-scan.test.ts` audits every renderer source for analytics keywords + proves api.ts the only network call site, loopback-only. main.tsx swaps the TK-225 placeholder for App; devDeps += @testing-library/react + jsdom (app/-only, never wombat-core). Independently verified PASS — all 4 ACs re-derived + executed (load indicators/masking, single-PUT save + write-only key, zero-analytics review, persona level-names + hint-vs-notice split). Quality bar app-side (zero .py touched): npm test 101 passed / 11 files; tsc lint clean; build clean; architect re-ran `uv run pytest` green pre-commit. files_in_scope normalized (+= App.test.tsx, analytics-scan.test.ts, vite.config.ts; -= components/ — consumed, not added to). Non-goals held — no voice-catalog browsing, no status/monitoring pane. validate PASS (208 nodes/177 gov); gate PASS (analyze). NEXT: post-batch review pass, then the EP-32 remainder (TK-223/TK-224/TK-201). | architect | TK-200, TK-199, TK-225, TK-209, EP-32, Q-110, DEC-29, DEC-32, DEC-33, DEC-37, contract v2.15 |
| 2026-07-10 | **EP-32 CLOSEOUT BATCH SELECTED + Q-111 PRE-BUILD RULINGS (COMMIT — within authority: sequencing + ticket-level how inside DEC-29/31/32/39 + CON-3/CON-7/NG-3; contract v2.16; board 132/144).** Batch = **TK-223 → TK-224 → TK-201** — CLOSES EP-32. Deliberately **3-not-5** (the Q-108 precedent): the **CR6 app-arc review pass sits immediately after the epic closes** (the whole first JS surface — TK-196..201 + TK-222..225 — reviews once, settled); the EP-35 pair (TK-213/TK-214) stays next-batch per the recorded EP-35-last sequencing AND TK-213 still needs its flagged persona_feedback row-encoding ruling (Q-110 note stands, ruled at its own batch); browser arc (TK-131..136, P3) opens after CR6. Every seam re-verified in SOURCE this session (zero cog-worx seams in this batch — all wombat-internal + app/): chat/surface.py wire shapes (replied/held/401/CORS), runtime handshake file (operator .env tier), CSP already loopback-open, APP_EDITABLE_FIELDS + lock-step mirror tests, CWD-relative WOMBAT_SETTINGS_FILE, api-process spawn (NO cwd today), __main__ keyring ctor, select.py injectable key_store, ASRSource .wav suffix set, main/preload pinned IPC surfaces, Electron's grant-all default permission handler. KEY RULINGS: (a) TK-223 — new **env-config.ts** operator-setting resolver (env > .env at the backend root; WOMBAT_BACKEND_CWD override; repo root = appPath/..) + **chat-info.ts** handshake reader; `wombat:chat-info` IPC **re-reads per call** (late-starting runtime picked up); renderer chat.ts = fresh info per send, closed **replied/held/unavailable** vocabulary, token only in the header; held renders HONESTLY (gate authority stands); live UI round trip = operator bring-up (RTL/fake-fetch is the runnable bar, the TK-200 split). (b) TK-224 — **wombat_voice_enabled JOINS APP_EDITABLE_FIELDS** (the one Python delta; mirror + lock-step tests; DEC-32 restart notice), **wombat_asr_drop_dir STAYS .env-tier** resolved main-side; pure **encodeWav** (16-bit PCM RIFF, unit-tested) + main-owned drop-dir write (renderer never passes a path); mute provably writes nothing; **media-only permission handler** (shipping mic capture on Electron's grant-everything default is the wrong posture — TK-198 discipline, not creep); unresolved drop-dir = disabled-AND-explained (satisfies no-placebo; complexity_budget corrected from 'No Python changes' to 'no new Python INGEST code'). (c) TK-201 — **backend-cwd spawn pin** (REAL round-trip break found: npm start writes app/wombat.settings.json while the runtime reads repo-root; StartApiProcessOptions gains cwd, main passes resolveBackendRoot) + **WOMBAT_KEYRING_SERVICE env override** in __main__; Playwright-for-Electron on a throwaway temp cwd + throwaway keyring service (npm run e2e, kept out of vitest); python -c proof that load_config()+build_tts_adapter resolves the fish primary via the injected test-service store; estimate S→M. Deps/files normalized (TK-224 += TK-223; TK-201 += TK-223/TK-224; files concretized). validate PASS (208 nodes/178 gov); gate PASS (analyze). NEXT after batch: CR6 review pass → EP-35 pair → browser arc; operator lane unchanged (OAuth bring-up, DEF-7, live chat/voice loop). | architect | TK-223, TK-224, TK-201, EP-32, Q-111, Q-110, DEC-29, DEC-31, DEC-32, DEC-39, contract v2.16 |
| 2026-07-10 | **TK-223 CLOSED (COMMIT — closeout on independent verifier PASS; contract v2.17; board 133/144).** Chat UI over the runtime chat handshake landed exactly on the Q-111(a) rulings: env-config.ts operator-setting resolver (env > .env at the backend root, WOMBAT_BACKEND_CWD override, relative-against-backend-root) + chat-info.ts reader with the `wombat:chat-info` bridge re-reading FRESH per invoke; renderer chat.ts maps surface.py's verbatim replied/held/unavailable vocabulary and held renders honestly (gate authority stands, CON-3 through the UI); token only ever in the X-Wombat-Chat-Token header, wombatChat solely via contextBridge (TK-199 posture re-pinned by security.test); bridge-null/dead-port = visible 'wombat is not running' banner with the settings surfaces proven alive alongside; DEC-29 structural — chat.ts joins api.ts as the only fetch sites under the analytics scan; TK-225 token discipline held (ink tokens, zero literals/animations). Verified independently: npm test 151/15 files + tsc clean, scope confined to app/; architect re-ran uv run pytest green (1397 passed) on the untouched Python tree pre-commit. files_in_scope normalized (+= security.test.ts, App.test.tsx, analytics-scan.test.ts, components/index.ts). Non-goals held — no streaming/typing indicators, no chat-history persistence. validate PASS (208 nodes/178 gov); gate PASS (analyze). NEXT: TK-224 (mic + audio, the one wombat_voice_enabled Python delta) → TK-201 closes EP-32 → CR6 app-arc review pass. | architect | TK-223, EP-32, Q-111, DEC-29, DEC-39, TK-222, TK-199, TK-225, CON-3, contract v2.17 |
| 2026-07-10 | **TK-224 CLOSED (COMMIT — closeout on independent verifier PASS; contract v2.18; board 134/144).** Mic + audio controls landed exactly on the Q-111(b) rulings: renderer capture (pure encodeWav 16-bit PCM mono RIFF + getUserMedia device enumeration/record/mute in audio.ts) hands to the Electron MAIN which owns the drop-dir write (save-capture.ts — renderer never passes a path; wombat_asr_drop_dir stays operator .env-tier resolved main-side via the TK-223 env-config resolver; closed ok/drop-dir-not-configured/write-failed vocabulary); the EXISTING ASRSource path transcribes byte-untouched — STRUCTURAL zero new Python ingest (git status clean under src/wombat/sources + integrations). The ONE Python delta: wombat_voice_enabled joins APP_EDITABLE_FIELDS with the lock-step mirror drift tests. permissions.ts replaces Electron's grant-everything default with the media-only handler (TK-198 discipline). HONESTY BAR held — no output-volume control anywhere (no winsound gain seam; flagged follow-up stands), unresolved drop-dir = disabled-AND-explained; the voice toggle PUTs exactly {wombat_voice_enabled:true} with the DEC-32 restart notice. Verified independently: encodeWav round-tripped by an independent reader, real .wav into a temp WOMBAT_ASR_DROP_DIR, mute provably writes nothing, device change flows to the next capture; quality bar all green (ruff clean, mypy strict 296 files, pytest settings_app+config 47, npm test 188, tsc clean; the 5 TK-224 test files 37 passed); architect re-ran uv run pytest green pre-commit. files_in_scope normalized (+= permissions.ts/.test.ts, save-capture.ts/.test.ts, security.test.ts, components/index.ts). Non-goals held — no VAD/wake-word/streaming ASR. validate PASS (208 nodes/178 gov); gate PASS (analyze). NEXT: TK-201 closes EP-32 → CR6 app-arc review pass → EP-35 pair. | architect | TK-224, EP-32, Q-111, DEC-29, DEC-32, DEC-39, TK-223, TK-225, TK-198, CON-3, contract v2.18 |
| 2026-07-10 | **TK-201 CLOSED (COMMIT — closeout on independent verifier PASS; contract v2.19; board 135/144) — EP-32 IS CLOSED (TK-201 was the epic's proof ticket; epic status field stays as-recorded per the EP-15/EP-16 precedent).** The launch + e2e smoke landed exactly on the Q-111(d) rulings: documented npm-start dev launch in README + ONE Playwright-for-Electron happy path (app/e2e/settings.spec.ts, `npm run e2e`, excluded from the vitest glob) on a throwaway temp cwd + throwaway WOMBAT_KEYRING_SERVICE — UI edit lands assistant name / tts=fish / voice id in the temp-cwd wombat.settings.json, fake key in the throwaway keyring service, and a spawned python load_config() + build_tts_adapter resolves FishAudioTTSAdapter: the DEC-28/DEC-32 config-to-selection round trip proven from the UI down. Same spec carries the security smoke — tokenless = 401, renderer require/process undefined (TK-198/TK-199 posture), token header-only never in a URL (TK-223 discipline). The two production seams landed: main.ts pins the spawned backend cwd to resolveBackendRoot (the settings.json round-trip break found in source is CLOSED) and settings_app `__main__` honors WOMBAT_KEYRING_SERVICE, unset default byte-identical ('wombat'). Verified independently: e2e 1 passed on real Electron + real spawned python; npm test 189, tsc clean, pytest settings_app 23, full pytest 1402 passed / 68 gated-skip, ruff clean, mypy strict 296 files; cleanup proven (no lingering Electron, zero vault pollution). files_in_scope normalized (+= app/vite.config.ts `base:"./"` — loadFile file:// 404s on root-absolute assets; += app/package-lock.json, .gitignore). Non-goals held — one happy path, no live cloud call (DEF-7 stands). **EP-32 (FEAT-13) fully done: TK-196..TK-201 + TK-222..TK-225 — wombat has its companion-app surface: settings, chat over the gate, mic capture, all under the pinned security + zero-analytics posture.** validate PASS (208 nodes/178 gov); gate PASS (analyze). NEXT: the CR6 app-arc review pass → EP-35 pair → browser arc; operator lane unchanged (OAuth bring-up, DEF-7, live chat/voice loop). | architect | TK-201, EP-32, FEAT-13, Q-111, DEC-28, DEC-29, DEC-31, DEC-32, DEC-39, TK-198, TK-199, TK-223, contract v2.19 |
| 2026-07-10 | **EP-35 CLOSEOUT BATCH SELECTED + PRE-BUILD RULED (COMMIT, Q-112, contract v2.20 - within authority: sequencing + ticket-level how inside DEC-36/DEC-37(h)/CON-3/CON-4/CON-6/NG-3; no locked decision touched; orchestrator-authorized under Jim's delegation).** BATCH (2, build order): TK-213 -> TK-214 - closes EP-35 and every non-browser ticket. Key rulings: (a) persona_feedback rides wombat_behavior_events ZERO-schema-change (event_type 'persona_feedback', source_id 'asr', outcome_label = the matched CLOSED-lexicon phrase VERBATIM under a total phrase->(axis,direction) mapping, idempotency_key('persona_feedback', <ASR sha256 event_key>), duration NULL; FEEDBACK_LEXICON_VERSION=1); (b) lexicon = observational per-axis directional phrases only, normalized-DISJOINT from TK-211's GRAMMAR, whole-utterance exact match via commands._normalize; 'loved that' STRUCK v1 (no (axis,direction) mapping = unusable = placebo record); (c) ASRSource gains optional feedback_hook(transcript, event_key) invoked AFTER command_hook and only when not consumed - never consumes; make_persona_feedback_hook guards the recorder (ONE loud WARNING on failure, CON-3); recorder closure built in assemble_runtime over the EXISTING RuntimeBundle.behavior_event_log, threaded via build_source_registry(persona_feedback_recorder=...) - event-log import-surface allowlist UNCHANGED; (d) writer-owns-honesty: WriteWindowSummariesStage excludes persona_feedback rows; event_log.py + 006 .sql single-writer/OUTCOME-only comments RESCOPED (TK-218 precedent); TK-213 estimate S->M, files normalized; (e) splice dream_tune -> dream_persona -> dream_behavior_log, DreamPersonaStage in dream_pathway.py (DreamTuneStage sibling), pure persona/tuner decision fn (PERSONA_FEEDBACK_WINDOW_HOURS=24.0, PERSONA_STEP_THRESHOLD=2, step iff >=2 one-direction AND zero opposing), steps applied via the EXISTING commands.apply saturating clamp, ONE live_persona.set(explicit=False), INFO journal line per stepped axis (axis/direction/counts - CON-4, motive-free), catch-loud-still-transition sibling parity; (f) DEC-37(h) pins: wombat_persona_pins {axis: aware-UTC ISO} in settings.json (unknown-key-safe against the boot source, settings-app PUT, and LivePersona._persist - all source-verified), LivePersona.set grows keyword-only explicit=True stamping pins for CHANGED axes (TK-212 call site byte-untouched), poll_settings_file pins app-edited axes, dream nudges never self-pin and never self-redetect (cursor-advance-after-own-write), pinned_axes(now) feeds the pure tuner, PERSONA_PIN_DAYS=7. Deliberately 2-not-5: the CR6 app-arc review pass is STILL DUE (orchestrator-owned) before the browser arc; EP-35 has zero overlap with the app/ surface CR6 reviews. DEFERRED: TK-131..136 + TK-153 (browser arc, own ruling session after CR6), OAuth + DEF-7 (operator). |
| 2026-07-10 | **TK-213 CLOSED (COMMIT, contract v2.21 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: no per-ticket independent verify; one batch-level Opus review audits the whole batch after TK-214 lands; no locked DEC touched — this lands the DEC-36 explicit-feedback tokens per the Q-112(a-d) rulings).** Board 136/144, Q-112 batch 1/2 (EP-35, P3). Closed-lexicon persona feedback lands motive-free: NEW persona/feedback.py (FEEDBACK_LEXICON_VERSION=1; frozen FeedbackToken axis/direction/phrase, NO motive field — CON-6; 6 observational phrases, 'loved that' struck as ruled; pure whole-utterance exact match via commands._normalize, normalized-disjoint from TK-211's GRAMMAR; token_for_phrase = TK-214's reader over the verbatim outcome_label). ASRSource optional feedback_hook(transcript, event_key) after the command_hook check, only-when-not-consumed, NEVER consumes — emitted SourceEvent byte-unchanged. make_persona_feedback_hook mirrors the TK-212 factory (recorder raise → one loud WARNING, never propagates — CON-3); recorder closure in assemble_runtime over the EXISTING behavior_event_log — the ruled zero-schema-change ride on wombat_behavior_events (event_type persona_feedback, source_id asr, idempotency_key('persona_feedback', ASR event_key)). Writer-owns-honesty riders: WriteWindowSummariesStage excludes persona_feedback rows; event_log.py + 006 sql single-writer comments rescoped. Quality bar = builder SELF-RUN (ruff clean, mypy strict 298 files, pg suite 1500 passed / 3 flagged pre-existing failures — boot_replay_pg x2 + outbound google-less-boot, due at the batch verify); architect re-ran uv run pytest -q pre-commit: 1414 passed / 69 gated skips / 0 failed. Scope exact (the 9 files_in_scope). validate PASS (208 nodes/179 gov); gate PASS (analyze). NEXT: TK-214 closes EP-35 → batch-level Opus review → CR6 app-arc pass. | architect | TK-213, TK-214, EP-35, Q-112, DEC-36, DEC-37, CON-3, CON-6, DEF-8, contract v2.21 |
| 2026-07-10 | **TK-214 CLOSED (COMMIT, contract v2.22 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: the batch-level Opus review of TK-213+TK-214 is now DUE, both batch tickets landed; no locked DEC touched — this lands the DEC-36 nightly aggregation + the DEC-37(h) pin per the Q-112(e-f) rulings) — and EP-35 IS CLOSED: every non-browser ticket on the board is done.** Board 137/144, Q-112 batch 2/2 (EP-35, P3). dream_persona lands: NEW pure persona/tuner.py (decide_persona_steps; PERSONA_FEEDBACK_WINDOW_HOURS=24.0, PERSONA_STEP_THRESHOLD=2 as recorded constants; AxisStepDecision; step iff >=2 one-direction AND zero opposing — property-tested never >1 step/axis/run). DreamPersonaStage spliced dream_tune -> dream_persona -> dream_behavior_log with sibling parity (catch-loud-still-transition); steps via the existing commands.apply saturating clamp through ONE LivePersona.set(explicit=False) + INFO journal line per stepped axis (axis/direction/counts — CON-4). DEC-37(h) pins in live.py: PERSONA_PIN_DAYS=7, wombat_persona_pins persisted alongside the five persona keys in the SAME read-modify-write; set(explicit=True default) stamps CHANGED axes only (nudges never self-pin); poll_settings_file pins app edits + advances the cursor past its own write (no self-redetect; TK-227 defer preserved); pinned_axes(now) feeds the tuner. Quality bar = builder SELF-RUN (ruff clean; mypy strict 301 files; pytest no-PG 1445/70/0; targeted pg suites green on throwaway docker); architect re-ran uv run pytest -q pre-commit green. Scope exact; files_in_scope normalized (+= 5 splice-reshaped pathway-shape test files, each flagged in-file). validate PASS (208 nodes/179 gov); gate PASS (analyze). NEXT: batch-level Opus review of TK-213+TK-214 (incl. the 3 flagged pre-existing pg failures) -> CR6 app-arc pass -> browser arc ruling session. | architect | TK-214, TK-213, EP-35, Q-112, DEC-36, DEC-37, CON-4, DEF-8, TK-227, contract v2.22 |
| 2026-07-10 | **ISS-8 LIVE TIMEZONE DEFECT RULED + FIX TICKETS MINTED (COMMIT, contract v2.23 — within authority: DEC-40 REALIZES the locked DEC-21, does not change it; ticket minting + how rulings; no scope change).** Source-verified this session: `bootstrap.py:263` `_UTC_ZONE = ZoneInfo("UTC")` defaults tz on assemble_runtime (612) + build_compose/brief_compose/brief_deliver (359/396/430) + `dream_substrate.py:176`; `runtime.py:233` never passes tz; NO timezone field in config.py — the brief fired 2026-07-10T07:00:08Z = 03:00 EDT (brief.md delivered_at). NEW **ISS-8** (the un-droppable defect record) → NEW **DEC-40**: (a) `wombat_timezone` IANA str, operator .env tier, default None → `tzlocal.get_localzone()` (tzlocal>=5 new hard dep — the ONE honest DST-correct ZoneInfo route on Windows; astimezone() fixed-offset rejected as placebo; required-explicit-env rejected as operator-parked), unknown key/resolution failure = ConfigurationError naming WOMBAT_TIMEZONE, NEVER silent UTC; (b) one-time civil-day ledger boundary shift ACCEPTED, no migration (day-keyed counters; the existing 2026-07-10 brief mark blocks a same-day double fire); (c) serve() resolves ONCE, `_UTC_ZONE` defaults deleted, tz REQUIRED at every composition seam — Q-15 made STRUCTURAL. NEW **TK-228** (P1, EP-1: config+resolver+threading; AC1 = runnable next_fire_at==07:00 LOCAL proof + serve-composition spy; AC3 = signature-inspection + src literal-scan) and **TK-229** (P2, EP-1, test-only: boot-replay `_spy_gate` gains/forwards `threshold_fn` matching Gate.__init__; outbound-wiring AC3 `_config(with_google=False)` made structurally google-less against the real repo .env — both failures re-verified in source, the TK-213/TK-214 batch-flag discharged into a ticket). Board 137/146. NEXT: build TK-228 (Jim is hit every morning) + TK-229, then the still-due TK-213/TK-214 batch verify + CR6 app-arc review pass, then the browser arc. | architect | ISS-8, DEC-40, TK-228, TK-229, DEC-21, Q-15, TK-152, TK-215, TK-202 |
| 2026-07-10 | **TK-228 CLOSED (COMMIT, contract v2.24 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: the batch-level Opus review is due once the batch lands; no locked DEC touched — this REALIZES DEC-21 per DEC-40, exactly as ruled at v2.23).** Board 138/146, ISS-8 fix (P1, EP-1). All four ACs builder-run green, architect re-ran the suite (1454 passed / 70 skipped) before commit; diff audited against the ticket: (a) `wombat_timezone` + `resolve_wombat_zone` in config.py — explicit→ZoneInfo, unknown key LOUD naming WOMBAT_TIMEZONE; unset→tzlocal.get_localzone() (tzlocal>=5 hard dep); tzlocal failure LOUD instructing the operator; zero silent-UTC paths. (b) serve() resolves ONCE, threads tz into assemble_runtime. (c) `_UTC_ZONE` singletons deleted; tz REQUIRED (keyword-only, no default) on all five composition seams incl. build_dream_substrate — bootstrap now threads the SAME zone into the dream-night boundary. (d) NEW tests/unit/test_tz_threading.py makes Q-15 structural (signature inspection + src literal scan). Sanctioned mechanical tz=UTC kwarg additions in existing factory-calling tests (incl. the two TK-229-scoped modules — kwargs only, TK-229's fixes untouched). NO ledger migration per DEC-40(b); the boundary-shift acceptance is now changelog-documented. NEXT: TK-229, then the due batch-verify + CR6 app-arc review pass, then the browser arc. | architect | TK-228, DEC-40, ISS-8, DEC-21, Q-15, TK-152, EP-1 |
| 2026-07-10 | **TK-229 CLOSED (COMMIT, contract v2.25 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: the batch-level Opus review of TK-228+TK-229 is now DUE, both ISS-8-arc tickets landed; no locked DEC touched — this discharges the two batch-flagged pre-existing failures exactly as ruled at v2.23).** Board 139/146, P2 test-only, EP-1. Diff audited against the ticket — precisely the two files_in_scope, zero src changes, no assertion weakened: (1) test_boot_replay_pg.py `_spy_gate` gains optional keyword-only `threshold_fn: Callable[[], float] | None = None` matching Gate.__init__ and forwards it verbatim (TK-215 drift closed; pending_set capture byte-unchanged). (2) test_outbound_wiring_e2e.py NEW opt-in `_no_env_file` fixture (monkeypatch.chdir(tmp_path); TK-202/Q-103 pattern by name) makes the google-less AC3 config structurally google-less against the live operator .env; sanctioned ADDITIVE `_BASELINE_DRAIN_STAGES` un-stale (+= chat_reply, reflection_compose — unconditional in both bootstrap drain branches, flagged in-file, set-equality preserved). Bar: builder self-run on throwaway postgres:16@5437 — both modules' 8 tests pass, full suite w/ DSN 1541 passed / 11 skipped; architect re-ran uv run pytest -q no-DSN pre-commit: 1454 passed / 70 skipped / 0 failed, both loud collection skips intact (AC3). validate PASS (210 nodes/181 gov); gate PASS (analyze). Every non-browser ticket is now done. NEXT: batch-verify (TK-228+TK-229) + the still-due TK-213/TK-214 batch verify -> CR6 app-arc review pass -> browser arc ruling session. | architect | TK-229, TK-228, ISS-8, DEC-40, TK-215, TK-202, Q-103, EP-1, contract v2.25 |
| 2026-07-10 | **BROWSER ARC OPENED — EP-25 BATCH SELECTED + PRE-BUILD RULED (COMMIT, Q-113, contract v2.26 — within authority: sequencing + ticket-level how inside DEC-19/CST-5/CON-4/NG-5 + the Q-32/Q-64 seam record; no locked DEC touched; orchestrator-authorized under Jim's delegation).** BATCH (5, build order): TK-131 → TK-132 → TK-133 → TK-134 → TK-153 — opens AND closes EP-25 (5 of the last 7 board tickets; board 139/146; none needs Jim's OAuth bring-up). Every framework seam re-verified in installed cog-worx source this session: Capability protocol is name/tier/input_schema + async invoke ONLY (no teardown hook — Q-32 stands); TaintState latches on external∧¬trusted-output OR untrusted-source tag; dispatch_one latches BEFORE invoke so the first external dispatch taints AND executes, subsequent ones TierViolation — hence the ruled one-dispatch shape (navigate returns the a11y snapshot in the same invoke). Key rulings (Q-113 a–h): playwright = NEW optional extra `browser` + dev-group entry, lazy import, chromium install a documented dev step, browser tests skip-gated (importorskip + launch-probe, no new marker); PlaywrightCapability hand-written external-tier class (name `browser`, action-enum schema); BrowserSession wombat-owned lazy-open/idempotent-async-close — runtime.py UNTOUCHED this arc (wiring lands with whichever future ticket first registers a browser pathway, TK-184 precedent); TK-133 rides the IngestEmailBody pattern (Done `wombat.web_page_read` / Degraded on failure, no model call, no auto-screenshot) with hermetic real-gate/fake-capability tests; TK-134 real-chromium proof over a stdlib fixture server — pytest-playwright NOT adopted (recorded budget deviation); TK-153 mirrors TK-148 (`read_web_page`, read-tier + untrusted-source tag, constant in ingest_web_page.py preserving taint.py's single-constant non-goal). files_in_scope normalized on TK-131/132/133. DEFERRED: TK-135/TK-136 (EP-26, the final batch) — they need the approved-dispatch-vs-taint_drops_external policy ruling, made with EP-25 as-built in hand. STANDING OBLIGATIONS PRE-EMPT DISPATCH: the TK-213/214 + TK-228/229 batch verifies and the CR6 app-arc review pass run BEFORE this batch builds; CR6 findings pre-empt build order. |
| 2026-07-10 | **TK-131 CLOSED (COMMIT, contract v2.27 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole EP-25 batch after all five tickets land; no locked DEC touched — built exactly per the v2.26 Q-113 pre-build rulings).** Board 140/146, EP-25 batch 1/5 — the browser seam exists. Diff audited against the ticket: `playwright_capability.py` = hand-authored BROWSER_INPUT_SCHEMA (action enum, url required only for navigate, additionalProperties false), BrowserSession (wombat-owned lifecycle per Q-32 — lazy open inside ensure_open with the ONLY playwright import, idempotent async close), PlaywrightCapability (name `browser`, tier external, async invoke only, isinstance-verified against cogworx's structural Capability protocol, registered WITHOUT trusted-output so gated dispatch latches TaintState; navigate returns the a11y snapshot in the SAME invoke per Q-113(c)). ONE in-frame builder deviation recorded: installed playwright 1.61.0 removed legacy `page.accessibility.snapshot()` — snapshot rides `page.aria_snapshot()` parsed via existing pyyaml into a JSON-native structure (no new dep, no screenshot, AC1 intact). pyproject: `browser` extra + dev-group playwright (voice/fastapi precedent) + jsonschema mypy override; chromium install stays a documented dev step, tests importorskip. Bar green builder-run and architect re-run (ruff, mypy strict 306 files, 1458 passed / 70 skipped). files_in_scope normalized (+2 package __init__.py, uv.lock). NEXT: TK-132 (interaction actions + screenshot fallback), then TK-133 → TK-134 → TK-153. |
| 2026-07-10 | **TK-132 CLOSED (COMMIT, contract v2.28 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole EP-25 batch after all five tickets land; no locked DEC touched — built exactly per the v2.26 Q-113(e) pre-build rulings).** Board 141/146, EP-25 batch 2/5 — the browser can now ACT, not just read. Diff audited against the ticket: click/type/select all resolve EXCLUSIVELY via page.get_by_role(role, name=...) through one shared `_act_role` helper (never CSS/XPath — AC1/AC2); success returns {ok:true, snapshot:<post-action a11y snapshot>} mirroring navigate; a locator unresolved within the bounded ELEMENT_TIMEOUT_MS=3000 has Playwright TimeoutError caught and converted to the STRUCTURED {ok:false, error:element_not_found, role, name} RETURN VALUE — never raised, because dispatch_one relays invoke exceptions raw, so AC4 caller-retry/fallback has to be a return; screenshot returns raw PNG bytes + logs a screenshot-fallback-used WARNING (AC3, CST-5 auditability — the capability NEVER decides to fall back on its own, non-goal held). BROWSER_INPUT_SCHEMA extended in place: action enum += click/type/select/screenshot, role/name/value props, per-action allOf if/then requirement blocks, additionalProperties stays false. NON-GOALS held — no taint latch (TK-133), no auth, no form-submit approval (TK-135). Bar: builder self-ran (6 action tests + 10 capability-suite + full suite + ruff green); architect re-ran uv run pytest -q pre-commit — 1464 passed / 70 documented pg-gated skips / 0 failed. SCOPE exact — git status precisely the two files_in_scope. validate PASS (210 nodes / 182 gov); gate PASS (analyze). NEXT per the v2.26 build order: TK-133 (BrowseAndRead Stage, taint latch on ingested content), then TK-134 → TK-153. | architect | TK-132, EP-25, Q-113, CST-5, TK-133 |
| 2026-07-10 | **TK-133 CLOSED (COMMIT, contract v2.29 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole EP-25 batch after all five tickets land; no locked DEC touched — built exactly per the v2.26 Q-113(c)+(f) pre-build rulings).** Board 142/146, EP-25 batch 3/5 — the safe web-READ pathway exists. Diff audited against the ticket: `browse_and_read.py` makes EXACTLY ONE `ctx.dispatch("browser", {action:navigate, url})` — the latch is STRUCTURAL, not bespoke: TK-131's external-tier no-trusted-output registration means the gate-routed dispatch latches TaintState BEFORE invoke runs, in the same call that fetches the page (Q-113c one-dispatch shape; navigate returns the a11y snapshot in that invoke, so no follow-up dispatch exists to be dropped). Success → Done, Artifact `wombat.web_page_read` {url, readable_text (recursive string-leaf flattener over the nested snapshot — never inspects/interprets content, DEC-19 latch-not-filter), tainted:True — a structural constant, not a content judgment}. Dispatch exception / structured {ok:false} / empty snapshot → Degraded, Artifact `wombat.web_page_read_error` {url, error} — never raises to the caller, NEVER auto-falls-back to screenshot (non-goal held; caller decides). Ctor `upstream_stage_name` + `last_output` pull = the ingest_email_body.py pattern; runtime pathway registration deliberately NOT here (TK-153's boundary). Tests (5) mirror test_taint_latch_adversarial.py: fake StageContext over REAL Registry+ToolGate+dispatch_one — AC1 post-read TierViolation on send_email + spec gone from exposed_specs, AC2 payload shape, AC3 adversarial ignore-prior-instructions text returns as inert data with ZERO send_email calls, AC4 ×3 with dispatch_count pinned at 1, + a benign-page twin proving content-independence. Bar: builder self-ran full bar (ruff, strict mypy 309 files); architect re-ran `uv run pytest -q` pre-commit — 1469 passed / 70 documented pg-gated skips / 0 failed. SCOPE exact — git status precisely the two files_in_scope, both new. validate PASS (210 nodes / 182 gov); gate PASS (analyze). NEXT per the v2.26 build order: TK-134 (BrowseAndRead local-fixture contract test), then TK-153. | architect | TK-133, EP-25, Q-113, DEC-19, TK-131, TK-153 |
| 2026-07-10 | **TK-134 CLOSED (COMMIT, contract v2.30 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole EP-25 batch after all five tickets land; no locked DEC touched — built exactly per the v2.26 Q-113 pre-build rulings, incl. the recorded pytest-playwright-not-adopted budget deviation).** Board 143/146, EP-25 batch 4/5 — the safe web-read pathway is now contract-proven over REAL chromium against a LOCAL-ONLY stdlib fixture server (port 0, 127.0.0.1; never the live internet). Diff audited against the ticket: exactly the one files_in_scope test module (new); zero src changes. AC1/AC2/AC3 all green NOT skipped (chromium present); AC2 proves the taint-latch thesis end to end on the real browser (injected instruction verbatim-inert, zero send_email calls); AC3 discharged through TK-133's existing empty-snapshot Degraded branch — a genuine 404-with-empty-body empirically yields an empty aria_snapshot (recorded as the honest 404 shape). Bar: module 3 passed, suite 1472/70/0 architect-re-run pre-commit, ruff + strict mypy clean (builder self-review removed one unused import + three unused type-ignores in the new file). NEXT per the v2.26 build order: TK-153 closes EP-25, then the batch-level Opus review of the whole browser arc. | architect (closeout on builder self-run bar; batch-verify pending) | TK-134, EP-25, Q-113, contract v2.30 |
| 2026-07-10 | **TK-153 CLOSED — EP-25 BROWSER ARC COMPLETE (COMMIT, contract v2.31 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole EP-25 batch now that all five have landed; this records the builder-self-run bar, not an independent verify).** The web-page ingest taint call-site lands on the SHARED TK-148 machinery exactly per Q-113(h): new `src/wombat/stages/ingest_web_page.py` defines READ_WEB_PAGE_CAPABILITY + the PageProvider seam LOCALLY (taint.py untouched — single-constant non-goal preserved), imports only UNTRUSTED_SOURCE_TAG, register_read_web_page tags at registration time, IngestWebPage dispatches via ctx.dispatch → structural TaintState latch → Done `wombat.web_page_ingested`; new `tests/safety/test_taint_latch_web.py` (9 tests) proves AC1 (adversarial latch, form_submit TierViolation, untagged-read MUST-fail twin, real-stage e2e), AC2 (shared-seam import identity, no competing tag/constant), AC3 (BrowseAndRead↔IngestWebPage both orders, one shared gate, idempotent latch). Bar green builder-run (suite 1479/70/0; ruff; mypy strict 312 files; manual load-bearing check — tag stripped → 4 tests fail) + architect pytest re-run pre-commit. Scope exactly the two files_in_scope. Board **144/146**; EP-25 done. NEXT: batch-level Opus verify of the EP-25 arc, then EP-26 (TK-135/TK-136) in the final batch. |
| 2026-07-10 | **FINAL BATCH — EP-26 SELECTED + PRE-BUILD RULED (COMMIT, Q-114, contract v2.32 — within authority: sequencing + ticket-level how inside DEC-19/DEC-22/DEC-26/NG-5/CON-4/CON-5; no locked DEC touched — DEC-26 is CONFIRMED, not amended; orchestrator-authorized under Jim's delegation).** BATCH (2, build order): TK-135 → TK-136 — closes EP-26 AND the board (146/146). Every seam re-verified in installed cog-worx source this session. THE HEADLINE RULING (deferred at v2.26): approval does NOT re-admit the external tier on a tainted drive — dispatch_one runs the tier check BEFORE check_approval and dispatch_approved bypasses ONLY ApprovalRequired, so DEC-26's taint_drops_external=True is absolute (TierViolation even with approved=True); corollary: check_approval is structurally unreachable for external-tier capabilities → the browser capability carries NO consequential/irreversible tags; approval for external side effects is carried ENTIRELY by the TK-149 journal→AwaitHuman→dispatch_approved pattern on an UNTAINTED drive (the propose side dispatches nothing pre-park; the one approved submit_form dispatch is itself the harmless terminal taint-latcher — Q-92 parallel). Also ruled: atomic submit_form action on PlaywrightCapability (one gated dispatch = goto + role/name fills + submit click); FormSubmitStage = ProposeDispatchStage subclass + the EXISTING generic DispatchApprovedStage (zero new dispatch-side code); TK-136 standalone login_handoff/login_confirm pair over an injected UNGATED {url,snapshot} PageStateProvider (a gated re-check snapshot would TierViolation on the tainted resumed drive); closed-lexicon textbox-name detection (recorded aria-snapshot limitation); UNCONDITIONAL deny-always password-fill guard inside the capability covering BOTH fill call sites — no human-confirm token exists anywhere (ruled stricter than the AC text; no-autofill made structural). files_in_scope normalized on both. STANDING OBLIGATION: the EP-25 batch-level Opus verify remains due — it runs before/alongside this batch; findings pre-empt build order. NEXT: TK-135, then TK-136, then the closeout review arc. | architect | Q-114, EP-26, TK-135, TK-136, TK-149, DEC-26, DEC-22, DEC-19, Q-92, Q-113, Q-32, contract v2.32 |
| 2026-07-10 | **TK-135 CLOSED (COMMIT, contract v2.33 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole EP-26 batch after both tickets land; this records the builder-self-run bar, not an independent verify; no locked DEC touched — built exactly per the v2.32 Q-114 pre-build rulings).** Board 145/146, EP-26 batch 1/2 — the approved-form-submit pathway exists. Diff audited against the ticket and the rulings: (1) PlaywrightCapability's ATOMIC `submit_form` — schema extended in place, ONE invoke branch does goto + role/name fill-all + submit click as a single gated dispatch (Q-113(c) one-dispatch shape); first unresolved locator → structured `{ok:false, error:element_not_found}` return, never a raise. (2) NEW FormSubmitStage = ProposeDispatchStage subclass, `build_proposal` ONLY (journal-before-park non-overridable): loud ValueError on malformed url/fields/submit (the human approves exactly what was read), `human_summary` enumerates URL + every field verbatim + the submit control, parks AwaitHuman; dispatch side = the EXISTING generic DispatchApprovedStage, ZERO new dispatch-side code, identity args mapping; graph wiring a caller concern (no v1 pathway registers form submit — consistent with the TK-184 precedent). Q-114 precedence PROVEN runnable: tainted drive + human approve → uncaught TierViolation via ctx.dispatch_approved. Bar green builder-run (5 + 3 real-chromium-local-fixture + 16 capability suite; full 1487/70/0) + architect pytest re-run pre-commit. SCOPE exact — git status precisely the four files_in_scope. validate PASS (210 nodes / 183 gov); gate PASS (analyze). NEXT per the v2.32 build order: TK-136 (the board closer), then the EP-26 batch-level Opus verify (EP-25's remains due too). | architect (closeout on builder self-run bar; batch-verify pending) | TK-135, EP-26, Q-114, Q-113, TK-149, contract v2.33 |
| 2026-07-10 | **TK-136 CLOSED (COMMIT, contract v2.34 — within authority: ticket closeout; v2 CADENCE — BATCH-VERIFY PENDING: one batch-level Opus review audits the whole EP-26 batch now that both tickets have landed; this records the builder-self-run bar, not an independent verify; no locked DEC touched — built exactly per the v2.32 Q-114 pre-build rulings f–j).** **THE BOARD CLOSES: 146/146, EP-26 batch 2/2** — the login handoff pathway exists and no code path can ever fill a password field. Diff audited against the ticket and the rulings: (1) PlaywrightCapability's shared `_checked_fill` — the ONE guard BOTH fill call sites route through (`type` via the new `_type_action` wrapper + `submit_form`'s field loop): reads the LIVE resolved element's `type` attribute BEFORE any fill; password → structured `{ok:false, error:password_field_blocked}` + logger.warning, fill never happens; UNCONDITIONAL deny-always per ruling (h), stricter than the AC's token-gated text — no human-confirm token exists anywhere in module or schema (additionalProperties:false rejects invented bypass args); a password field among submit_form fields blocks the WHOLE submit, no submit click. (2) NEW login_handoff.py — standalone login_handoff/login_confirm pair (Q-92 draft_composer precedent: detection-negative must pass through Done; ProposeDispatchStage.run always parks) over the ruled injected UNGATED PageStateProvider (a gated re-check would TierViolation on the tainted resumed drive, ruling f); pure closed-lexicon `page_has_login_textbox` aria walk (password/passcode/passphrase — recorded limitation: aria carries no type attribute, the detector is the courtesy handoff, the guard the strong guarantee); positive → record_proposal(ActionType.LOGIN_HANDOFF, action_id run_id:login_handoff) + AwaitHuman naming the domain; confirm side locates the decision via a reverse journal walk WIDENED across BOTH stage names (a self-re-park lands its next answer at its own step), `login-complete` → FRESH page recheck (textbox gone → mark_dispatched + Done wombat.login_confirmed; still present → re-park to itself); anything else → record_refusal + raise MissingApprovalAnswer (the dispatch_approved refuse-loud shape exactly). ONE documented structural deviation, coherent with the spine: LoginConfirmTerminalStage, a declared-but-never-taken raise-if-entered stub edge satisfying cog-worx's reachable-terminal StageGraph invariant (proven by a real StageGraphError; the exact BriefTimerTerminalStage/TK-53 precedent). Bar green builder-run — test_login_handoff.py 5 passed (AC1/AC2+re-park twin/AC4 unattended halt/negative), test_password_guard.py 4 passed (AC3 i–iii, REAL playwright, not skipped), full 1496 passed / 70 documented skips, ruff+mypy clean; architect re-ran `uv run pytest -q` pre-commit (1496/70/0). SCOPE exact — git status precisely the four files_in_scope. validate PASS (210 nodes / 183 gov); gate PASS (analyze). NEXT: the EP-26 batch-level Opus verify (EP-25's remains due), then the browser-arc review pass. | architect (closeout on builder self-run bar; batch-verify pending) | TK-136, EP-26, Q-114, Q-92, Q-32, DEC-19, NG-5, CON-5, TK-53, contract v2.34 |
| 2026-07-10 | **EP-25 BATCH VERIFY PASS recorded (COMMIT, contract v2.35 — within authority; recording the independent batch-verify verdict; discharges the v2.27–v2.31 BATCH-VERIFY PENDING).** Single Opus reviewer over 68a288c..0912fa9 re-ran the full bar itself (ruff clean, mypy strict 0 errors/312 files, pytest 1479 passed / 70 documented pg-DSN-gated skips / 0 failed, batch subset 25, REAL chromium 7 passed NOT skipped per TK-134's pass-not-skip AC) and observed every AC of all five tickets passing; integration confirmed the single-dispatch taint latch end-to-end on fake AND real PlaywrightCapability, latch idempotence in both stage orders, no navigate-success misclassification, empty-snapshot→Degraded intact, UNTRUSTED_SOURCE_TAG imported by identity. Two minors: goto-timeout (ruled v2.37) and uncommitted-TK-228-in-tree during verification (since committed at 8b18b6c; tree clean; resolved). | architect | EP-25, TK-131..TK-134, TK-153, Q-113, contract v2.35 |
| 2026-07-10 | **EP-26 BATCH VERIFY PASS recorded (COMMIT, contract v2.36 — within authority; discharges the v2.33–v2.34 BATCH-VERIFY PENDING and, with v2.35, BOTH due-verify claims in the v2.34 NEXT line).** Single Opus reviewer over de2516a..c83d014 (HEAD at board-close) re-ran the bar: ruff clean, mypy strict clean (318 files incl. tests), full suite 1496/70/0. TK-135: test_form_submit.py 5 passed incl. AC4ii — tainted drive + approve still raises uncaught TierViolation, zero invocations, mark_dispatched never called (the Q-114(a) precedence ruling proven runnable) — plus 3 real-chromium submit_form tests, not skipped. TK-136: login_handoff 5 + password_guard 4 passed on real chromium (password never lands, no import bypass of _checked_fill, schema rejects any confirm token). Integration: exactly ONE .fill() call site (inside _checked_fill) with both actions routed through it; action_id aligned across the pair; neither stage runtime-wired (correctly out of v1 scope). One minor sits WITHIN the recorded courtesy-handoff limitation (hand-crafted aria snapshots only; the deny-always guard is the real-chromium-tested strong guarantee). All 146/146 tickets now carry independent verification. | architect | EP-26, TK-135, TK-136, Q-114, contract v2.36 |
| 2026-07-10 | **Goto-timeout finding RULED resolved-as-mischaracterized — NO code change (COMMIT, contract v2.37 — within authority; disposition of a review finding against source-verified behavior).** page.goto(url) has no explicit timeout at either call site (navigate ~332, submit_form ~356) but Playwright's 30000ms default navigation timeout is live (nothing zeroes it), so goto raises TimeoutError at 30s and the raise routes into the already-tested raise→Degraded path (TK-133 AC) — bounded-and-handled, not hangs-indefinitely; meets the acceptance bar as-built. Hardening note recorded (not a ticket; board closed, no v1 runtime pathway dispatches a live browser): an explicit pinned navigation timeout belongs to whichever future ticket first wires a live browser pathway, riding with the Q-113(d) BrowserSession.close() runtime-wiring obligation. | architect | EP-25, TK-133, Q-113(d), contract v2.37 |
| 2026-07-10 | **CR6 standing obligation SUPERSEDED (COMMIT, contract v2.38 — recording JIM'S explicit v2-cadence ruling: no reason to verify twice, one Opus reviewer reviewing the whole code; supersede-not-edit per append-only).** The v2.26-era standing obligation (CR6 app-arc review pass + pending batch verifies before the browser arc dispatches) is cancelled as a separate arc pass, with full accounting so nothing drops silently: TK-213/TK-214 batch-verified in wf_a13d4b29 (Q-112 era, recorded); TK-228/TK-229 independently Opus-verified in the dedicated timezone-fix workflow (07:00-local year-round proven + full pg suite 1541/0; v2.23–v2.25); EP-25/EP-26 verifies discharged at v2.35/v2.36; the whole-repo closeout review now running at board-empty is the final independent pass and subsumes any residual arc-level obligation. Nothing remains owed under the CR6 line. | Jim (ruling), architect (record) | CR6, Q-112, ISS-8, contract v2.38 |
| 2026-07-10 | **FINAL WHOLE-REPO CLOSEOUT REVIEW COMPLETE (COMMIT, contract v2.39 — the board-empty pass v2.38 records as subsuming ALL residual review obligations; register at `planning/code-review-2026-07-10-closeout.md`).** Whole repo at HEAD bd32890, 146/146 done, 8 dimensions, every claim adversarially verified with runnable repros against real seams. **Outcome: 10 CONFIRMED (1 critical, 4 major, 5 minor), 0 downgraded, 2 refuted.** Clean bills: persona, tests (all 70 pg skips proven live; 1496/70), the full safety spine (taint latch, CON-5, Q-114 precedence, DEC-28 egress, CON-7 residency), app security posture, config/graph integration. Confirmed set: **CRF-2 CRITICAL** — standing drain run COMPLETES after one item (terminal Done cancels timers; Sweeper can never re-drive it; queue permanently deaf per process lifetime; the AC5 test asserted exactly the buggy shape); **CRF-3 MAJOR** — eternal self-park runs hit cogworx max_steps=1000 and FAIL silently (~83 min for the idle 5s drain heartbeat); **CRF-1 MAJOR (latent)** — `_checked_fill` password deny-always guard defeated by page-controlled type-attribute casing (case-sensitive compare; capability not yet wired in bootstrap); **CRF-4 MAJOR** — SourceRegistry `_poll_loop` leaves `enqueue()` unguarded (QueueFullError/psycopg error kills a source for good and re-raises through `stop()`, breaking shutdown teardown); **CRF-6 MAJOR** — `_build_local_transcriber` catches ImportError only, so a non-ImportError whisper load failure crashes boot (CON-3 violation); CRF-5/7/8/9/10 minors (pending_journal growth, non-atomic settings.json write, macOS reopen gap, .env.example omissions + stale comment, `wombat` console-script stub). **NEXT: route-and-fix — CRF-1..4 + CRF-6 enter the normal ticket loop before daily-driver reliance; the two runtime defects (CRF-2/3) kill the standing process within ~1.5 h of boot and are the top of the queue. Nothing reopens a locked DEC or v1 scope.** |
| 2026-07-10 | **CLOSEOUT REGISTER ROUTED AND RULED (COMMIT — Q-115 + DEC-41 + 8 fix tickets + 2 deferrals; contract v2.40; within authority — review routing + pipeline-level how; DEC-8/DEC-12/ASMP-2 and v1 scope explicitly HELD, nothing escalated).** Every CRF seam re-verified against installed cog-worx + wombat source before ruling: Sweeper wakes only due-timer runs (sweeper.py:67-69), fire_timer no-ops unless WAITING/RETRYING (engine.py:577), Done → COMPLETED + cancel_timers_for_run (855-857), max_steps=1000 default FAILs at seq≥1000 with RUN_FAILED into a None sink (112, 747-751), and the framework's own framing (engine.py:18) — the step ceiling exists to FAIL forever-loops, i.e. **cog-worx runs are bounded episodes BY DESIGN; no perpetual-run pattern exists** — so CRF-2 (critical) + CRF-3 stand exactly as registered and the eternal-drain-run shape was fighting the framework. **DEC-41 (the crux):** bounded-episode drain — DrainQueueStage's empty branch returns Done (never Wait-self-park); a wombat-owned pump in `_drive_and_serve`, gathered with the Sweeper, peeks a NEW read-only `WombatQueue.pending_count()` each beat and fires fresh `wombat-drain-<uuid>` runs chained until empty (idle beats = zero runs, zero journal records; an AwaitHuman-parked draft run no longer deafens the queue); timer runs keep Sweeper custody with `build_engine` pinning max_steps=100_000 + a logging event_sink (cogworx.coordination.events) so no run ever dies silent. Rejected: loop-back perpetual run (fights the framework, O(n) replay per re-drive), fire-wrapper respawn + idle-recycle (three mechanisms for one aesthetic), raise-max_steps-alone (a snooze, not a fix), per-item runs from the enqueue path (erodes ASMP-2). **TICKETS (board 146/154):** priority TK-230 (CRF-2, EP-4, M — pump + Done-on-empty + the corrected AC5 proving a SECOND enqueued item drains) → TK-231 (CRF-3, EP-1, S) → TK-232 (CRF-4, EP-3, S — the pattern_detector.py:184-201 two-arm precedent + stop() suppress-and-log so shutdown teardown always completes) → TK-233 (CRF-6, EP-31, S — except-Exception matching the `_build_local_tts` sibling; CON-3 boot-survival proven in ACs) → TK-234 (CRF-1, EP-26, S — `(field_type or '').lower()` normalization at the ONE shared `_checked_fill`, real-chromium PASSWORD/Password repros inverted); cleanup TK-235 (CRF-7, EP-32 — temp+os.replace, trail/renderer.py:136-148 precedent) → TK-236 (CRF-9, EP-1 — .env.example completeness + stale persona comment) → TK-237 (CRF-10, EP-1 — console script boots serve()). **DEFERRALS:** DEF-9 (CRF-5 pending_journal compaction — 4000-row replay measured 5ms; revisit at ~1s replay/~1M rows/ASMP-2 relaxation), DEF-10 (CRF-8 macOS reopen respawn — operator on Windows; revisit at any macOS deployment). validate PASS (218 nodes/187 gov); gate PASS. NEXT: dispatch the priority batch TK-230→TK-234 — wombat is not daily-reliable until it lands. | architect | Q-115, DEC-41, TK-230..TK-237, DEF-9, DEF-10, DEC-8, DEC-12, TK-53, contract v2.40 |
| 2026-07-10 | **PRIORITY BATCH DISPATCHED (COMMIT, contract v2.41 — batch selection + three briefing-level pins inside DEC-41; nothing new decided, nothing escalated).** TK-230 → TK-231 → TK-232 → TK-233 → TK-234 per the Q-115/DEC-41 build order; cleanup TK-235..TK-237 deliberately held for the next batch (daily-driver reliability first; batch cap 5). All deps done (TK-53/TK-177/TK-3/TK-193/TK-217/TK-136); every ticket atomic as minted, no splits. Every framework seam re-verified against installed cog-worx this session: `runtime/engine.py` (max_steps=1000 default at 112; seq≥max_steps → FAILED + cancel + RUN_FAILED emit at 747-751; Done → COMPLETED + cancel_timers_for_run at 855-857; `Engine.run` keyword-only run_id/session_id/pathway_id/initial; `event_sink: Callable[[Event], None] | None`), `coordination/events.py` (frozen `Event` with run_id; `EventType.RUN_FAILED`), `runtime/sweeper.py` run_forever, `testing/doubles.py` InMemoryJournal. Briefing pins: (a) TK-230 Done-on-empty reuses the DRAIN_HEARTBEAT kind and keeps DrainQueueStage's constructor signature (artifacts.py/bootstrap.py out of TK-230 scope); (b) `pending_count()` mirrors `drain()` eligibility exactly (`leased_by IS DISTINCT FROM` this epoch) so a parked leased item never re-fires the pump; (c) pump beat = `bootstrap._DRAIN_POLL_INTERVAL_SECONDS` imported by runtime.py. validate PASS; gate PASS. NEXT: build TK-230..TK-234, then the cleanup batch TK-235..TK-237. | architect | Q-115, DEC-41, TK-230..TK-234, contract v2.41 |
| 2026-07-10 | **TK-230 recorded DONE (COMMIT — board 147/154, contract v2.42; build record, within authority; batch-verify pending per the v2 cadence).** The DEC-41 crux lands as ruled: drain_queue.py empty branch returns Done with the terminal DRAIN_HEARTBEAT artifact (never Wait; transitions = the single gate edge, poll_interval_seconds kept for signature compat and documented unused); queue.py pending_count() is one read-only SELECT mirroring drain() eligibility exactly (v2.41 pin (b)); runtime.py _run_drain_pump — peek each beat, chain sequential fresh wombat-drain-<uuid> engine.run drives until empty, sleep one beat — gathered with the Sweeper in _drive_and_serve, replacing the old single boot drive (pin (c): beat = bootstrap._DRAIN_POLL_INTERVAL_SECONDS); ASMP-2 held by sequential awaits; brief/dream pathways and finally-teardown byte-identical. All four ACs proven runnable: the CRF-2 repro INVERTED (corrected serve-boot AC5 drains a SECOND post-cycle item, no restart), zero runs on idle beats, K-burst chains within one beat, an AWAITING_HUMAN-parked draft run stays parked while a fresh run drains new work; the test_runtime sweeper-wakes test honestly rewritten onto a purpose-built waiting stage since the drain no longer self-parks. Bar green builder-run (ruff / mypy-strict 0 errors 318 files / 1498 passed 70 skipped / throwaway-pg 17 passed) + architect pytest re-run pre-commit; scope exactly the seven files_in_scope. validate PASS (218 nodes/187 gov); gate PASS. NEXT: TK-231 (CRF-3 max_steps + event_sink). | architect | DEC-41, TK-230, Q-115, ASMP-2, contract v2.42 |
| 2026-07-10 | **TK-231 recorded DONE (COMMIT — board 148/154, contract v2.43; build record, within authority; batch-verify pending per the v2 cadence).** CRF-3 lands exactly per DEC-41(e), bootstrap.py only within the complexity budget: `_ENGINE_MAX_STEPS = 100_000` (module constant with the explaining comment — brief_timer/dream_timer eternal Wait-self-parks accrue one committed step per Sweeper re-drive, so cog-worx's 1000 default meant silent death at ~1000 days; 100_000 outlives any plausible process lifetime while a genuine hot loop still fails fast) + `_log_engine_event` sink over `cogworx.coordination.events` (RUN_FAILED → ERROR naming `event.run_id`; all other lifecycle events DEBUG — a safety net against silent death, not a general event log), both wired into the ONE `Engine(...)` construction in `build_engine`. No Engine subclass, no cog-worx edits (DEC-12 held), no per-run overrides, no new config fields; builder verified engine.py ceiling logic + Event/EventType against installed cog-worx source before writing. All three ACs proven runnable (3 new tests in test_bootstrap.py): AC1 built engine carries `_max_steps==100_000` and `_event_sink` IS the wombat sink, never None; AC2 the CRF-3 repro INVERTED — a real BriefTimerStage Wait-to-self run on a build_engine-built engine stays WAITING across 2000 `fire_timer` wakes (double the old ceiling), never FAILED; AC3 a tiny-max_steps harness Engine with the SAME sink trips into RunStatus.FAILED with exactly one caplog ERROR naming the run_id. Bar green builder-run (test_bootstrap 18 passed; ruff clean; mypy strict 0 errors/318 files; full suite 1501 passed/70 documented skips, none new) + architect pytest re-run pre-commit; scope exactly two of the three files_in_scope (test_runtime.py needed no change — the runtime seam was untouched). validate PASS (218 nodes/187 gov); gate PASS (analyze). NEXT per the v2.41 build order: TK-232 (CRF-4 registry enqueue guard + stop() suppress-and-log). | architect | DEC-41, TK-231, Q-115, DEC-12, TK-97, TK-52, contract v2.43 |
| 2026-07-10 | **TK-232 recorded DONE (COMMIT — board 149/154, contract v2.44; build record, within authority; batch-verify pending per the v2 cadence).** CRF-4 lands exactly per the Q-115 routing, sources/registry.py only within the complexity budget: the _poll_loop success-branch enqueue() calls are individually guarded on the verified pattern_detector.py:184-201 (TK-204/CR3-2) two-arm precedent — a kept-specific QueueFullError arm (ERROR naming source id + dropped event_key, exc_info) plus a broad Exception arm (logger.exception, same naming), each marking degraded and CONTINUING the loop (pull sources redeliver; a push-buffered event is honestly dropped-and-logged); degraded.discard() fires only on an all-enqueues-succeeded iteration (iteration_ok — the TK-171 most-recent-outcome semantics tightened, never loosened). stop() replaces the bare contextlib.suppress(CancelledError) with a per-task try/except (pass CancelledError, log-and-suppress any other Exception naming the source) so every task is awaited and every source stop() runs — a dead task stored exception can no longer abort the remaining stop() iterations or runtime._drive_and_serve finally-teardown (the shutdown-leak half closed). Non-goals held: no retry/backoff, no dead-letter store, no queue changes, ASMP-2 enqueue-only untouched. All 3 ACs proven runnable (3 new tests): AC1 the CRF-4 repro INVERTED (QueueFullError every interval → task.done() stays False, loud logs, degraded); AC2 generic-Exception arm + recovery clears degraded (two-arm parity); AC3 dead-task teardown — stop() suppresses-and-logs, all tasks awaited, every source stopped, a runtime-shaped finally completes. Bar green builder-run (test_source_registry 8 passed/1 pg-gated skip; ruff clean; mypy strict 0 errors/318 files; full suite 1504 passed/70 documented skips, none new) + architect pytest re-run pre-commit; scope exactly the two files_in_scope. validate PASS (218 nodes/187 gov); gate PASS (analyze). NEXT per the v2.41 build order: TK-233 (CRF-6 transcriber except broadened to the TTS-sibling shape, CON-3). | architect | DEC-41, TK-232, Q-115, TK-204, TK-171, ASMP-2, contract v2.44 |
| 2026-07-10 | **TK-233 recorded DONE (COMMIT — board 150/154, contract v2.45; build record, within authority; batch-verify pending per the v2 cadence).** CRF-6 lands exactly per the Q-115 routing, voice/select.py only within the complexity budget: `_build_local_transcriber` broadens `except ImportError` → `except Exception` around FasterWhisperTranscriber construction, matching the `_build_local_tts` sibling shape byte-for-byte — FasterWhisperTranscriber loads the whisper model AT construction and raises non-ImportError on realistic conditions (uncached model + offline, bad WOMBAT_ASR_MODEL, corrupted HF cache), which previously propagated unguarded through _maybe_register_asr → build_source_registry → assemble_runtime and CRASHED BOOT, violating CON-3 (voice is additive, never blocks boot). Docstring now records ANY-construction-failure degrade; both TK-217 role-discriminated warning messages byte-preserved; non-goals held — no retry, no lazy model loading, no provider changes, DEC-28 cloud-to-local fallback direction untouched. All 3 ACs proven runnable (3 new tests under a CRF-6 section in tests/voice/test_select.py): AC1 the register repro INVERTED (RuntimeError at construction → build_transcriber returns None with the byte-preserved primary warning; build_source_registry completes without raising — boot survives); AC2 healthy cloud STT primary whose local fallback-slot constructor raises → FallbackTranscriber wraps the live cloud primary with fallback None + the TK-217 fallback-role message; AC3 the ImportError path byte-preserved for both roles. Bar green builder-run (test_select 22 passed; ruff clean; mypy strict 0 errors/318 files; full suite 1507 passed/70 documented pg-gated skips, none new) + architect pytest re-run pre-commit; scope exactly the two files_in_scope. validate PASS (218 nodes/187 gov); gate PASS (tickets + analyze). NEXT per the v2.41 build order: TK-234 (CRF-1 password-guard casing normalization — the priority-batch closer, then the batch-level Opus verify). | architect | DEC-41, TK-233, Q-115, CON-3, TK-217, DEC-28, contract v2.45 |
| 2026-07-10 | **TK-234 recorded DONE (COMMIT — board 151/154, contract v2.46; build record, within authority; batch-verify pending per the v2 cadence — THE PRIORITY BATCH TK-230..TK-234 HAS FULLY LANDED).** CRF-1 lands exactly per the Q-115 routing, capabilities/playwright_capability.py only within the complexity budget: `_checked_fill` lowercases the LIVE `type` attribute before the deny-always compare (`(field_type or "").lower() == "password"`) so page-controlled casing (`type=PASSWORD`/`Password`) can never defeat the TK-136/Q-114 ruling-h UNCONDITIONAL guarantee at EITHER fill call site (the `type` action via `_type_action` and the `submit_form` field loop both route through the ONE shared helper — the single-`.fill()`-site invariant from the v2.36 verify untouched); the empty-string default keeps the None/absent-attribute fill path byte-identical. Non-goals held: no detection-heuristic change, no schema change, no new actions, no runtime wiring; the aria-snapshot courtesy-detector limitation stands recorded. All 3 ACs proven runnable — AC1 mixed-case fixture pages on REAL chromium (executed, not skipped): both call sites return password_field_blocked, field stays empty, a mixed-case password field among submit_form fields blocks the whole submit with NO submit click (the CRF-1 repro inverted at both sites); AC2 byte-unchanged (lowercase still blocks, non-password fills, no-type-attribute fills); AC3 hermetic unit twin over a fake async Locator proves case-insensitivity without a browser. Bar green builder-run (test_password_guard 14 passed on real chromium; ruff clean; mypy strict clean; full suite 1517 passed/70 documented skips, none new) + architect pytest re-run pre-commit; scope exactly the two files_in_scope. validate PASS (218 nodes/187 gov); gate PASS (analyze). NEXT: the batch-level Opus verify of the whole TK-230..TK-234 priority batch (now owed — all five landed), then the held cleanup batch TK-235..TK-237. | architect | DEC-41, TK-234, Q-115, TK-136, Q-114, CRF-1, contract v2.46 |
| 2026-07-10 | **Cleanup batch TK-235→TK-236→TK-237 selected + briefing-pinned (COMMIT — contract v2.47, within authority; the v2.40 cleanup order held).** All deps done, no pg anywhere, buildable without the operator OAuth bring-up. Pins: (a) TK-235 atomic write = the verified renderer.py `_save_sidecar` shape (mkstemp same-dir + os.replace + unlink-on-BaseException); (b) TK-236 no-drift AC scoped to WombatConfig-backed core vars — the test/memory-pipeline/graph blocks stay; (c) TK-237 `def main()` lives in `__main__.py`, script entry → `wombat.__main__:main`, stub deleted (grep-proven unreferenced). The TK-230..234 priority-batch Opus verify stays PENDING, owed alongside — not blocked by — this batch. |
| 2026-07-11 | **DEC-42 accepted + TK-238/TK-239 minted (COMMIT — contract v2.48; operator-directed scope reopen, Jim verbatim on the record).** Jim asked directly for (1) a visible console that spins up and STAYS while the runtime runs — closing it kills the process entirely — and (2) a Restart-server button in the UI that works regardless of runtime state. Rulings within his frame: file logs KEPT tee-style (the exit-5 diagnosis relied on the runtime-<stamp> logs; console-only loses post-mortems the moment the window closes); the launch/kill/restart logic homes IN-REPO under scripts/ (versioned + testable; the desktop launcher at C:UsersJimwombat-launcher becomes a thin caller — its re-point is a recorded OPERATOR step, never repo work); restart = kill-all '-m wombat' pythons via the Stop-Wombat.ps1 CIM commandline match, PROVE gone within a bounded wait, start exactly ONE fresh visible console (ASMP-2 at-most-one-drainer structural; running/dead/never-started all work); Electron main invokes the restart script hidden + detached/unref so Electron NEVER parents the runtime (the detached-launch incident precedent — closing the UI still never kills the runtime; only the console close or the restart kill may), with a wombat:* ipcMain.handle channel + preload contextBridge per the existing settings-api-info/chat-info/save-capture pattern, main-side single-flight + renderer disabled-while-pending (double-click guard), closed result vocabulary restarted/failed-with-detail/busy — loud, never silent; button lives in the App settings region under the DEC-39 design system (Button + lucide, tokens, no gradient); Windows-only mvp per DEC-6/DEF-10 — no cross-platform gold-plating on an operator convenience surface. Coherence checks: DEC-39 already widened the app past config-surface-only, so an operator lifecycle control is consistent; the chat pane re-reads its handshake per invocation, so a restarted runtime is picked up with zero app work; DEC-29 zero-analytics binds the new surface. Board reopens 151/156; TK-235..TK-237 + the two owed batch verifies unchanged. validate PASS (220 nodes/188 gov); gate PASS (analyze). NEXT: build TK-238 → TK-239. | architect | DEC-42, TK-238, TK-239, ASMP-2, DEC-39, DEC-29, DEF-10, contract v2.48 |
| 2026-07-11 | **Batch selected + pre-build ruled Q-116 (COMMIT — contract v2.49, within authority; DEC-42 frame held).** All five open tickets, order TK-238 → TK-239 → TK-235 → TK-236 → TK-237 — the Jim-directed P1 lifecycle pair leads (TK-239 depends on TK-238), then the v2.47-pinned cleanup trio; none touch pg, none need the OAuth bring-up. TK-238 pins: ONE shared CIM commandline-match helper (the verified Stop-Wombat.ps1:3-4 shape) for guard/kill-verify/post-start-assert; visible console = detached Start-Process powershell.exe hosting `venv-python -m wombat 2>&1 | Tee-Object logs/runtime-<stamp>.log` from repo-root cwd (window hosts the pipeline so closing it kills the runtime; Start-Process detaches from the caller — exit-5 precedent); single-instance refuse-loud exit 1; restart = kill-all → bounded-wait CIM prove-gone (~10s, fail loud) → start exactly one; exit codes 0/nonzero ARE the TK-239 contract; logs/ gitignored; mvp verification = builder-run live CIM smoke ending on restart (machine left with exactly one runtime), no invented PS test framework. TK-239 pins: `wombat:restart-runtime` ipcMain.handle + `wombatRuntime` preload namespace (fourth channel, existing contextBridge pattern); runtime-control.ts resolves scripts/restart-wombat.ps1 under the EXISTING resolveBackendRoot seam; spawn powershell hidden+detached+stdio-ignore+unref while awaiting exit events (unref doesn't suppress them); closed vocabulary restarted/failed-with-detail/busy, module-level single-flight latch; RuntimeControls = Button+Icon (lucide), disabled-while-pending, LOUD failure; handler tests mock child_process, RTL mocks preload — no live spawn in CI. All Electron/launcher/design seams verified live this session (main.ts:74/81/86, env-config, Button/Icon, Stop-Wombat.ps1). Atomicity: all five pass. validate PASS (220 nodes/189 gov); gate PASS (analyze). NEXT: build TK-238 → TK-239 → TK-235 → TK-236 → TK-237, then the two owed batch verifies (TK-230..234 priority batch + this batch) close the board 156/156. | architect | Q-116, DEC-42, TK-238, TK-239, TK-235, TK-236, TK-237, ASMP-2, contract v2.49 |
| 2026-07-11 | **TK-239 recorded DONE (COMMIT — board 152/156, contract v2.50; build record, within authority; batch-verify pending per the v2 cadence).** The DEC-42 Restart-server button lands exactly per the Q-116 pins (EP-32) — runtime-control.ts resolves scripts/restart-wombat.ps1 under resolveBackendRoot, spawns powershell hidden/detached/stdio-ignore + unref (Electron never parents the runtime console), awaits exit/error into the closed vocabulary restarted / failed-with-detail / busy (module-level single-flight, ASMP-2 no double-start); ipcMain.handle wombat restart-runtime + preload wombatRuntime.restart() (fourth wombat channel); RuntimeControls (Button + lucide RefreshCw, DEC-39 held) in the App settings region — disabled-while-pending, visible success, LOUD role=alert failure with detail. Builder self-ran the full bar — tsc clean, lint clean, vitest 21 files / 204 tests green incl. runtime-control.test.ts (6) + RuntimeControls.test.tsx (4) covering all three ACs; pytest green at commit. ORDERING NOTE — TK-238 is built on disk but its closeout commit is pending, so TK-239 lands first in git history (safe: tests mock the spawn seam, script path resolves at runtime). NEXT: close TK-238, then TK-235 → TK-236 → TK-237, then the two owed batch verifies. | architect | TK-239, DEC-42, Q-116, ASMP-2, EP-32, contract v2.50 |
| 2026-07-11 | **TK-235 recorded DONE (COMMIT — board 153/156, contract v2.51; build record, within authority; batch-verify pending per the v2 cadence).** CRF-7 closed — settings.json writes are atomic per the DEC-41 pin and the v2.47 briefing pin (EP-32): new _write_settings in settings_app/api.py rides the trail/renderer.py _save_sidecar precedent (mkstemp same-dir → os.fdopen write → os.replace; unlink temp + re-raise on BaseException), put_settings swaps its bare write_text for it. Torn-write mid-PUT now leaves the OLD intact file — the silent-reset chain (truncated JSON → _read_settings degrade-to-empty → next PUT drops all saved settings) is structurally broken. _read_settings untouched, no new endpoints/schema, TK-197 zero-runtime-imports held; two new tests pin both ACs (torn-write bytes-unchanged + structural temp-then-replace-same-dir, no residue). Builder self-ran the full bar (targeted 25 passed; full suite 1518 passed / 70 pg-gated skips / 1 pre-existing env-dependent persona-name failure confirmed identical on main via git stash); architect re-ran pytest green pre-commit. NEXT per the v2.49 order: TK-236 (.env.example completeness), then TK-237. | architect | TK-235, EP-32, DEC-41, TK-197, CRF-7 |
| 2026-07-11 | **TK-236 recorded DONE (COMMIT — board 154/156, contract v2.52; build record, within authority; batch-verify pending per the v2 cadence).** CRF-9 closed — .env.example is a complete operator template per the v2.47 no-drift pin (EP-1): a new "Optional operator channels" block adds commented examples with accurate one-liners (README/config.py wording) for all 8 omitted WombatConfig-backed vars — GOOGLE_OAUTH_CLIENT_ID/GOOGLE_OAUTH_CLIENT_SECRET, WOMBAT_BRIEF_PATH, WOMBAT_VOICE_ENABLED, WOMBAT_FEEDBACK_FILE, WOMBAT_ASR_DROP_DIR, WOMBAT_ASR_MODEL (default base), WOMBAT_CHAT_HANDSHAKE_FILE — and the stale "nothing reads these yet" persona-axes comment is corrected to the truth (axes read at boot via matrix_from_config into LivePersona, hot-applied per TK-209, gate actuates effective_urgency_threshold). Per the binding v2.47 ruling, WOMBAT_TEST_PG_DSN / WOMBAT_MEMORY_* / NEO4J_* stay untouched (separate documented consumers). Docs-only — .env.example the ONLY file changed; both ACs checked var-by-var (every name greppable to a real WombatConfig field, no drift); full suite 1518 passed / 70 pg-gated skips / 1 pre-existing unrelated persona-name env failure (identical with the change stashed — a local wombat.settings.json contaminating the test env, tracked outside this ticket); architect re-ran pytest green pre-commit. NEXT per the v2.49 order: TK-237 (real console entry point), plus the TK-238 closeout commit still owed. | architect | TK-236, EP-1, CRF-9, TK-209, contract v2.52 |
| 2026-07-11 | **TK-238 recorded DONE (COMMIT — board 155/156, contract v2.53; closeout on independent verifier PASS, within authority; batch-verify pending per the v2 cadence).** The DEC-42 console lifecycle scripts land per the Q-116 pins AMENDED by the root-match ruling (EP-1): on this host one logical launch stably yields TWO python.exe processes with byte-identical '-m wombat' command lines (venv-launcher parent + real child), so process IDENTITY for the single-instance guard and the post-start assert counts ROOT matches only (Get-WombatRootProcesses — a match whose ParentProcessId is not itself in the match set); the kill path still kills ALL matches and proves ALL gone. wombat-console.ps1: PSScriptRoot-resolved repo root + venv python, refuse-loud exit 1 on an existing root (ASMP-2), detached Start-Process powershell.exe hosting the Tee-Object pipeline (close-the-window kills; caller detached — exit-5 precedent), exit 0 only after bounded-wait CIM proves exactly one root. restart-wombat.ps1: kill-all + prove-gone (0 matches) then delegates start to the console script, LASTEXITCODE returned unchanged — the TK-239 exit contract intact; works from running/dead/never-started. .gitignore += logs/. Verifier re-derived everything against the REAL script: 4-raw-match tree → guard saw exactly 1 root, exited 1, started nothing (AC3a); kill drove matches to 0 in the bounded wait (AC3b/AC2); AC1 by artifact (tee'd runtime-<stamp>.log, runtime persisted after caller exit 0). Scope exactly the two scripts + the .gitignore line (pyproject/__init__/__main__ diffs = TK-237 work, left unstaged). ruff clean, mypy 318 files, pytest 1518/70/1-known-env; machine left clean. validate PASS (220 nodes/189 gov); gate PASS (analyze). NEXT: TK-237 closes the board 156/156, then the two owed batch verifies (TK-230..234 + this batch). | architect | TK-238, DEC-42, Q-116, ASMP-2, EP-1, TK-239, contract v2.53 |
| 2026-07-11 | **TK-237 recorded DONE (COMMIT — board 156/156, BOARD CLOSED AGAIN, contract v2.54; closeout on independent verifier PASS, within authority; batch-verify pending per the v2 cadence).** CRF-10 closed per the DEC-41 closeout register and the v2.47 pin (EP-1): the installed wombat console script boots the REAL runtime — pyproject [project.scripts] repointed to wombat.__main__:main; __main__.py gains def main() wrapping asyncio.run(serve()) with the module guard calling it (python -m byte-identical); __init__.py docstring-only, Hello-World stub deleted (grep-proven absent under src/tests). Verifier evidence: from a scratchpad cwd outside the repo, bare env fails loud ConfigurationError naming DEEPSEEK_API_KEY exit 1; dummy creds + pg unset names WOMBAT_PG_DSN exactly (AC literal), raised in serve before assemble_runtime so no drainer stands up; python -m wombat byte-identical. Bar: ruff clean, mypy strict clean (318 files), pytest 1518/70-skip/1 pre-existing env failure (local wombat.settings.json persona contamination, stash-and-rerun proven, same one v2.53 tracks). The v2.49 batch TK-238→TK-239→TK-235→TK-236→TK-237 has FULLY LANDED. NEXT: the two owed batch verifies (v2.42..v2.46 and v2.50..v2.54). | architect | TK-237, DEC-41, Q-116, EP-1 |
| 2026-07-11 | **TK-240 CLOSED (COMMIT, contract v2.59 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-240..243).** Board 157/160, P1 production, EP-32 — the DEC-43/DEC-44 settings substrate. Diff audited against the ticket — the six files_in_scope + the packaged migration: settings_store.py (table + SettingsStore + SecretFieldRefused + opt-in import_legacy_settings_file), schema_preflight sixth entry (docstring records the never-imports rule), runtime.serve() import call after assemble_runtime / before _drive_and_serve with the accepted one-boot non-persona lag documented in the docstring (v2.58 ruling (b) verbatim), plus the three test files. DEC-44 held in code AND test: bare ensure_all_schemas leaves the file byte-untouched (the new hazard AC), every import-adjacent test runs under chdir(tmp_path), and Jim's real settings file survived the full suite — verified present in the repo root at closeout. Builder bar: throwaway docker pg, 1613 passed / 6 failed all stash-proven pre-existing; architect pytest re-run pre-commit 1523/76/1 (the v2.51-tracked env failure, none new). validate + gate PASS. NEXT: TK-241 → TK-242 → TK-243, then the batch verifies. | architect | TK-240, DEC-43, DEC-44, NG-3, CON-3, Q-46, EP-32, contract v2.59 |
| 2026-07-11 | **TK-242 CLOSED (COMMIT, contract v2.60 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-240..243).** Board 158/160, P1 production, EP-32 — the app-write half of DEC-43. Built ahead of TK-241: dependency-legal (depends_on TK-240 only; v2.56 already flagged it parallel-eligible after TK-240) — recorded, not escalated. Diff audited against the ticket — the three files_in_scope plus ONE in-scope normalization: tests/unit/test_settings_store.py's sole-call-site structural test updated to the exactly-two-call-sites shape its own docstring promised at TK-242 (`serve()` + settings_app `__main__` — DEC-44's two-and-only-two production call sites now both live and structurally pinned). Degrade posture held exactly per the DEC-43 ruling: GET never 500s (200 all-null + `storage_unavailable: true`), PUT 503s with a fixed module-constant detail never derived from the exception (no secret/DSN echo); `__main__` resolves the DSN env-then-cwd-.env without `load_config` (TK-197 no-bootstrap/no-runtime import discipline re-proven structurally); TK-235's atomic-write machinery retired WITH the file path it protected. v2.58(a) chdir(tmp_path) hygiene held in the new subprocess legacy-import tests — Jim's real settings file untouched. Builder bar: ruff/mypy-strict clean (320 files), 42 passed over throwaway docker pg 5437 incl. the pg-gated AC1 round-trip + AC3 live subprocess import (.migrated rename verified); architect pytest re-run pre-commit 1528/78/1 — the same v2.51-tracked pre-existing env-contamination failure ('John' persona), none new. validate + gate PASS. NEXT: TK-241 (WombatConfig DB source swap), then TK-243, then the batch verifies. | architect | TK-242, DEC-43, DEC-44, TK-197, TK-235, CON-3, EP-32, contract v2.60 |
| 2026-07-11 | **Three orchestrator rulings recorded (COMMIT — contract v2.61, within authority; DEC-43/DEC-44 frame held; unblocks TK-241 + TK-243, batch wf_4ba3f46d-64c).** RULING 1 (TK-241): the wombat_settings table source activates ONLY inside load_config() — bare WombatConfig() construction performs ZERO database I/O (builder proved live that construction-time DSN self-resolution dialed Jim's real WOMBAT_PG_DSN on 5436 from the repo-root .env during ordinary test runs and broke 5 unrelated integration tests on latency); new runnable AC pins zero-IO-at-construction against an unreachable fake-host DSN; precedence env > .env > table > defaults unchanged; consistent with the lazy-no-IO-at-construction invariant (Q-83 precedent) and DEC-44's production-entry-points-only spirit. RULING 2 (TK-243): LivePersona FULLY LAZY — connection-free at construction even when given a store (bad-host connect measured 2.7s untimeouted would break assemble_runtime's ONE-eager-Postgres-touch invariant, bootstrap.py 658-667); pins/axes hydrate on the FIRST poll_settings() Sweeper beat; AC1 restart-survival amended to the first-beat boundary; boot gains no second eager Postgres read (v2.58 ruling b already accepted first-beat persona healing). RULING 3 (TK-240 batch-review minor): rename race hardening authorized as a one-line rider under the TK-241 batch — import_legacy_settings_file treats FileNotFoundError from the rename as already-migrated-by-the-other-process (log, continue); settings_store.py + its test file explicitly join TK-241 files_in_scope so the diff check never flags creep. Governance-only; no src/tests touched. |
| 2026-07-11 | **TK-243 CLOSED (COMMIT, contract v2.63 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-240..243).** Board 159/160, P1 production, EP-32 — the hot-apply half of DEC-43. Built ahead of TK-241, dependency-legal in substance: it consumes only the TK-240 SettingsStore, and the v2.62 ruling-A suite-red ordering hazard is DISSOLVED — TK-243 deletes live.py file machinery outright incl. the WOMBAT_SETTINGS_FILE import, so the TK-241 live.py one-line rider is MOOT (TK-241 scope shrinks by that line). As-built: optional store (None ⇒ fully in-memory, one loud construction warning, never a crash), FULLY LAZY construction per v2.61 ruling 2 (zero store I/O, structurally asserted), set() key-level upserts of exactly the five wombat_persona_* keys + wombat_persona_pins (persist failure ⇒ ONE WARNING, never raises into the drain — CON-3 held), poll_settings() = one get_all() per Sweeper beat with value-diff detection, unconditional FIRST-beat hydration (restart survival proven at the first-beat boundary, v2.58(b)), app-edit pin stamping ported, own-write cursor advance so a dream nudge (explicit=False) never reads as an app edit, read failures warn once per streak and retry (TK-227 retry collapses naturally). Ten test files beyond files_in_scope carry mechanical ctor-signature ripples ONLY (settings_path kwarg deleted) — accepted normalization. Architect pytest pre-commit: 1526 passed / 79 skipped / 1 failed — the SAME v2.51-tracked persona-name env-contamination failure, stash-proven identical pre-TK-243 (it now dies at the TK-241 config file source), none new. NEXT: TK-241 (WombatConfig DB source swap) closes the board 160/160, then the owed batch verifies (TK-240..243, v2.42..46, v2.50..54). | architect | TK-243, EP-32, DEC-43, DEC-44, TK-240, TK-241, TK-227, CON-3, contract v2.63 |
| 2026-07-11 | **DEC-45/DEC-46 batch selected + six pre-build rulings (COMMIT — contract v2.68, within authority; batch = TK-244 -> TK-245 -> TK-246 -> TK-247, the entire 160/164 remainder).** All framework/product seams verified live (schema_preflight at SIX entries, migrations top out at 007, RuntimeBundle Optional-default precedent, registry CRF-4 two-arm poll loop, create_app/_resolve_pg_dsn, CalendarEvent/GmailMessageItem payloads — the raw gmail poll payload CARRIES body_text, the load-bearing hazard behind rulings r1/r2). Rulings: r1 gmail projection persists ONLY message events (reply-prefixed intent events skipped — not external-API information); r2 gmail stored payload = exactly five whitelisted keys built by explicit key-pick, band recomputed via pure triage_message, body-key guard _SANCTIONED_PATHS byte-unchanged; r3 item_key = poller event_key verbatim + both prune/purge horizons live as module constants in their own store modules (30d external_store / 14d scratchpad); r4 TK-246 routes /external/calendar + /external/gmail STAND — the unapproved iteration-2 design brief (28d765b) cannot bind, normalization is the ticket's recorded follow-up amendment; envelope pinned (items + storage_unavailable, defaults window_hours=168 / limit=50); r5 assemble_runtime always constructs both stores (dsn required), bundle fields external_item_store/scratchpad_store Optional-default-None, serve() guards on non-None, "DSN-less" binds at the SourceRegistry sink seam; r6 sink = optional ctor callable (source_id, events) in its own try/except, one WARNING on failure, enqueue arm untouched, composition owns projections + fetched_at. Deliberately deferred: nothing — no other open ticket; owed batch verifies (TK-240..243, v2.42..46, v2.50..54) remain loop process debt. | architect | TK-244..TK-247, DEC-45, DEC-46, EP-36, EP-32, CRF-4, DEC-26, contract v2.68 |
| 2026-07-11 | **TK-244 CLOSED (COMMIT, contract v2.69 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-244..247).** Board 161/164, P1 production, EP-36 — the DEC-45 storage substrate, wiring-free. Diff audited against the ticket: exactly the five files_in_scope (external_store.py + migrations/008_external_items.sql NEW; schema_preflight.py seventh entry + docstring counts; the two test files), no creep — unrelated tree churn (design-iteration files, roadmap, .migrated artifact) left unstaged. As-built exactly per the pinned shape + the v2.68 rulings: composite (source, item_key) PK with JSONB payload, NULLable occurs_at, fetched_at re-bumped, first_seen_at write-once (ON CONFLICT DO UPDATE omits it — proven live, AC1); one idempotent CREATE-IF-NOT-EXISTS (NG-3); ExternalItemStore dsn-injected/lazy on the Q-46 SettingsStore mirror; per-source occurs_at-ordered get_window/get_recent + prune_older_than by fetched_at with count (AC2); r3 held — EXTERNAL_ITEMS_PRUNE_DAYS=30 ships in external_store.py for TK-245's serve() line; store never projects, body-key guard allowlist byte-unchanged (DEC-26 held); AC3 structural — no bootstrap/runtime import, exactly SEVEN ensure_schema calls. Builder bar green (ruff, mypy strict 0/322, unarmed 1521/87/0, pg-gated 21 passed on throwaway pg 5437); architect pytest pre-commit 1521/87/0 exit 0. validate PASS (229 nodes/193 gov); gate tickets PASS. NEXT per the v2.68 order: TK-245 (source-poll sync + whitelist projections), then TK-246 ∥ TK-247. | architect | TK-244, EP-36, DEC-45, NG-3, DEC-26, contract v2.69 |
| 2026-07-11 | **TK-245 CLOSED (COMMIT, contract v2.70 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-244..247).** Board 162/164, P1 production, EP-36 — the DEC-45(c)/(d) sync wiring. As-built per the v2.68 r6 ruling exactly: registry.py SourceRegistry ctor gains OPTIONAL `sink: Sink | None` (Callable[[str, list[SourceEvent]], None]) called once per successful poll in its OWN try/except — sink failure = ONE WARNING naming the source, enqueue arm byte-untouched, poll task alive at the next interval (AC2 CRF-4/TK-232 two-arm parity; ASMP-2 held, the store write is a sibling side-write, never a drain). sources/bootstrap.py splits gmail construction into `_build_gmail_source` (source + once-loaded TriageRules) so the SAME rules feed GmailWithReplyIntents AND the new `build_external_item_sink` whitelist projector — gcal payload = CalendarEvent.to_payload verbatim (round-trip proven, occurs_at = start); gmail projects EXACTLY message_id/subject/sender/received_at/priority_band (occurs_at = received_at), reply-prefixed events skipped, body key NEVER referenced — AC1 adversarial SECRET-MARKER proof on a real DSN-gated store (no body key/value anywhere in stored payloads); the body-key guard allowlist BYTE-UNCHANGED (DEC-26 untouched). build_source_registry threads the sink only when a store is supplied — DSN-less = sink absent, polls byte-unchanged (AC3). RuntimeBundle.external_item_store on the DSN posture; serve() prune_older_than EXACTLY ONCE at boot (DEC-45(f), 30-day constant). Builder bar green: ruff clean, mypy strict 0 (322 files), 1628/11 on throwaway pg 5437 (5 pre-existing tests/integration failures stash-proven unrelated — standing batch-verify debt); diff = the eight files_in_scope exactly. NEXT: TK-246 (EP-32 read routes) then TK-247 (scratchpad), then the owed EP-36 batch verify. | architect | TK-245, EP-36, DEC-45, DEC-26, ASMP-2, contract v2.70 |
| 2026-07-11 | **TK-246 CLOSED (COMMIT, contract v2.71 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-244..247).** Board 163/164, P1 production, EP-32 — the DEC-45(e) read path. As-built per the v2.68 r4 ruling exactly: create_app gains OPTIONAL `external_store` (ExternalItemStore | None, default None) + TWO token-guarded READ-ONLY routes — GET /external/calendar (window_hours default 168, get_window over gcal now→now+window) and GET /external/gmail (limit default 50, get_recent) — envelope pinned as items (stored payloads verbatim) + storage_unavailable bool. Degrade = the DEC-43 posture verbatim (None store or raising read → 200, empty items, storage_unavailable true; never a bare 500, never exception-derived detail); token discipline identical to every existing route; structural AC proves NO non-GET method routable under /external/ (the TK-245 sync stays the only writer). __main__ builds the store from the SAME _resolve_pg_dsn resolution as SettingsStore; absent DSN boots degraded; deliberately NO schema-ensure for wombat_external_items here — the runtime preflight owns it, a missing table degrades like any storage failure. No bootstrap/runtime import (TK-197 discipline). Diff audited: exactly the three files_in_scope. Builder bar green (ruff, mypy strict 0/322, 42 passed on throwaway pg 5438 incl. the pg-gated AC1 window/order/token proof); architect pre-commit pytest green. The r4 follow-up amendment stands — route/field normalization when the settings-screen design lands. NEXT per the v2.68 order: TK-247 (scratchpad) closes the board, then the owed batch verifies. | architect | TK-246, EP-32, DEC-45, DEC-43, TK-245, TK-197, contract v2.71 |
| 2026-07-11 | **TK-247 CLOSED (COMMIT, contract v2.72 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-244..247). THE BOARD CLOSES 164/164.** P1 production, EP-36 — the DEC-46 scratch substrate, the last open ticket. As-built exactly per the pinned shape + the v2.68 r3/r5 rulings: NEW scratchpad.py (ScratchpadStore, dsn-injected ONE lazy psycopg connection, Q-46 convention) + migrations/009_wombat_scratchpad.sql (scope_key/entry_key/value JSONB, PK scope_key+entry_key, created_at/updated_at); EIGHTH ensure_all_schemas entry; RuntimeBundle.scratchpad_store Optional-default-None with assemble_runtime always constructing it (dsn required); serve() purge_stale(SCRATCHPAD_PURGE_DAYS=14) exactly once at boot, non-None-guarded. Structural AC held — no bootstrap/runtime import. One accepted normalization beyond files_in_scope: tests/unit/test_external_store.py mechanical 7→8 preflight-count ripple only. Bar — builder ruff/mypy-strict-0-of-324/1540-92-0 unarmed + 85 pg-gated ACs on throwaway pg 5439 (armed-suite 5 fails = the standing v2.70 pre-existing pg-armed debt); architect pre-commit pytest 1540/92/0 exit 0. NEXT: the owed batch verifies (TK-244..247 + v2.42..46 / v2.50..54 / TK-240..243), then the settings-screen redesign on Jim explicit go. | architect | TK-247, EP-36, DEC-46, DEC-45, contract v2.72 |
| 2026-07-11 | **DEC-47 RECORDED (contract v2.73 — Jim ruling, catch-up sync row).** Settings-screen iteration-4 design APPROVED (brief §11 + mock @ c6b9bb3); UI/Phase-2 went OPERATOR-OWNED (Jim builds app/ himself); TK-225 theme-token rule binding; TK-246 route/field normalization converted to an operator-demand deferral. | architect | DEC-47, TK-225, TK-246, contract v2.73 |
| 2026-07-11 | **DEC-48 RECORDED (contract v2.74 — Jim direction, supersedes-in-part DEC-47(b), catch-up sync row).** ONE agent Phase-2 pass brings app/ to the approved iteration-4 design via the STANDARD build-batch workflow, THEN operator ownership resumes. Four atomic EP-32 tickets minted, strict serial (shared app/src files): TK-248 (token vocabulary, placeholder colors) → TK-249 (approved shell + chat pane + Today landing, settings re-housed, behavior unchanged) → TK-250 (/external/calendar client + event cards) → TK-251 (/external/gmail client + inbox cards + open-in-Gmail bridge + notepad honest-empty). Honest-render rule: nothing invented beyond the TK-246 envelopes; DEC-47(c)/(d) deferrals stand. Board 164/168. | architect | DEC-48, DEC-47, TK-248, TK-249, TK-250, TK-251, EP-32, contract v2.74 |
| 2026-07-11 | **BATCH SELECTED + PRE-BUILD RULINGS (COMMIT, contract v2.75 — within authority: pipeline-level how inside DEC-48).** Next batch = TK-248→249→250→251, strict serial build_order 1..4 (the only open tickets; all atomic, DEC-48 already supplies the frame). Three rulings pinned after SOURCE verification: r1 (TK-248) the mock :root set (15 tokens) is the naming contract and a LOWER BOUND — existing derived tokens (brand-active, danger-hover/-active, non-color tokens) remain, revalued into the placeholder family; placeholder values = the mock's oklch values verbatim (the iteration-2 midnight-burrow set, brief §11.2). r2 (TK-250) stored gcal payload is EXACTLY {event_id,title,start,end,all_day} (calendar/models.py to_payload verified) — no location/attendee fields exist, so the event-card meta line renders only what exists (source label; the mock's pin/attendee icons omitted, DEC-48(d) honest-render, snippet-retraction precedent); all_day→pill; bucketing from start client-side local. r3 (TK-251) open-in-Gmail = ONE contextBridge method on the existing four-channel preload pattern; renderer passes ONLY message_id, MAIN process validates the id as a plain URL-safe token, constructs https://mail.google.com/mail/#all/{message_id}, shell.openExternal — never a renderer-supplied URL (security posture + standing tests intact). Envelope facts for briefings verified live: /external routes return payload dicts verbatim behind X-Wombat-Token; gmail payload five fields with priority_band ∈ {high, normal} (closed StrEnum). NOT deferred: nothing — board has exactly these four open. Standing debt unchanged: the owed batch verifies (TK-244..247 et al.) remain process debt for the workflow's verify leg. | architect | TK-248, TK-249, TK-250, TK-251, DEC-48, DEC-47, TK-225, TK-246, contract v2.75 |
| 2026-07-11 | **TK-248 CLOSED (COMMIT, contract v2.76 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-248..251).** Board 165/168, P1 mvp, EP-32 — first leaf of the DEC-48 iteration-4 pass. As-built per the v2.75 r1 ruling exactly: theme.css aligns the 15 mock color tokens to the mock :root oklch values VERBATIM (mock @ c6b9bb3, brief §11.2); the mock set = naming contract + LOWER BOUND — pre-existing derived tokens (brand-active, danger-hover/-active) remain, revalued on the same −6% lightness step; radius/font/transition untouched. tokens.ts adds one typed accent (text/bg) name (previously only reachable via focusRing); zero component renames needed. DESIGN.md pins the placeholder token contract and flags the color-theory rationale as placeholder-era. Jim re-themes by editing theme.css alone. Bar — lint (tsc) clean; 21 files / 204 tests green incl. all standing color/gradient/MUI/animation/icon audits (TK-225 discipline); manual AC1 diff proof (all 15 names, byte-identical oklch, strict superset). Scope = the three files_in_scope exactly. NEXT per the v2.75 order: TK-249 (approved shell + chat pane + Today landing). | architect | TK-248, EP-32, DEC-48, TK-225, contract v2.76 |
| 2026-07-11 | **TK-249 CLOSED (COMMIT, contract v2.77 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-248..251).** Board 166/168, P1 mvp, EP-32 — second leaf of the DEC-48 iteration-4 pass: the APPROVED APP SHELL. New components — Header (mark + wordmark + honest running/offline status off the EXISTING window.wombatChat bridge, no new fetch), NavRail (Today first, then Persona / Voice & Audio / API Keys / System), ChatDock (persistent collapsible 320px dock wrapping ChatPane UNMODIFIED — wombat-down state preserved on every view), Today (honest placeholder sections only; data arrives TK-250/251 per DEC-48(d)). App.tsx composes shell + view switch, Today as default landing; the settings form re-housed across the four category views with ONE shared form/touched/keyInputs state — load/save/write-only-keys/restart-strip/persona-hint behavior UNCHANGED. Two mechanical ripples accepted as-built: components/index.ts exports; tokens.ts gains one typed brand.border name (the TK-225 tokens-only discipline is why the typed name exists). Bar — npm test 224/224 across 21 files incl. the rewritten TK-200 suites, the new shell AC1 suite, and all standing audits; architect pre-commit pytest green. NEXT per the v2.75 order: TK-250 (/external/calendar client + event cards). | architect | TK-249, EP-32, DEC-48, TK-225, contract v2.77 |
| 2026-07-11 | **TK-250 CLOSED (COMMIT, contract v2.78 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-248..251).** Board 167/168, P1 mvp, EP-32 — third leaf of the DEC-48 iteration-4 pass: Today's UPCOMING section. api.ts gains getCalendarEvents() riding tokenedFetch over GET /external/calendar (backend 168h default, no override), typed to the exact five-field gcal payload {event_id,title,start,end,all_day} per the v2.75 r2 ruling, consuming the TK-246 envelope verbatim. New Upcoming.tsx renders the iteration-4 event cards — 3px accent edge (neutral / brand-for-next / dashed-all-day), fixed-width time block (semibold start, muted end; weekday atop start in the Later bucket), 600-weight title, client-side LOCAL-TIME bucketing (Today/Tomorrow/Later this week), today's earliest event flagged NEXT (brand edge + chip), all-day → All-day pill. NO meta line — no location/attendee field exists and the gcal-only route makes a static source label pure chrome; accepted as a tightening of r2 under DEC-48(d) honest-render. States per AC2: plain card-shaped skeletons / degraded-inside-card-shells / designed empty line; thrown fetches degrade to the same posture — never a blank pane, never a crash. Today.tsx mounts Upcoming over its TK-249 placeholder; index.ts exports it. One mechanical ripple accepted as-built: tokens.ts gains one typed brand.text name for the outline-style next chip (TK-225 discipline, TK-248/249 precedent). Bar — npm test 233/233 incl. the new Upcoming.test.tsx AC suites and all standing audits; lint (tsc) clean; ruff clean; mypy strict 0/324; architect pre-commit pytest 1540/92/0. NEXT per the v2.75 order: TK-251 (inbox cards + open-in-Gmail bridge + notepad honest-empty) closes the DEC-48 pass, then DEC-47(b) operator ownership resumes. | architect | TK-250, EP-32, DEC-48, TK-225, TK-246, contract v2.78 |
| 2026-07-11 | **TK-251 CLOSED (COMMIT, contract v2.79 — within authority: ticket closeout on the builder self-run bar; v2 cadence — BATCH-VERIFY PENDING for TK-248..251).** BOARD CLOSES 168/168 — fourth and final leaf of the DEC-48 iteration-4 pass: Today's INBOX HIGHLIGHTS + Steward's notepad. api.ts gains getGmailMessages() over the token-guarded GET /external/gmail (limit=50 backend default), typed to the exact DEC-45 five-field projection {message_id, subject, sender, received_at, priority_band}, TK-246 envelope verbatim — no snippet, no body (DEC-45(d)), nothing invented (DEC-48(d)). InboxHighlights.tsx: iteration-4 inbox cards — 28px brand-tinted sender-initial block, subject primary line, sender + received-at meta, priority chip (high filled / normal outline, literal band text; needs-reply/fyi = standing DEC-47(c) deferral). Row click = the ONLY interaction → window.wombatGmail.openMessage(message_id); per the v2.75 r3 ruling verbatim: gmail-open.ts validates the id MAIN-side (URL-safe token regex), constructs https://mail.google.com/mail/#all/{id} itself, injected openExternal — no renderer-supplied URL ever reaches the shell; preload/main follow the existing four-channel pattern; security.test.ts extended. NotepadCard.tsx: designed HONEST-EMPTY timeline only — no scratch read route exists, none invented. States: skeletons / degraded-in-shell / honest-empty, thrown fetches degrade. App.tsx untouched (narrower than scoped). Bar — builder npm test green incl. new AC suites + all standing audits; architect pre-commit pytest 1540/92/0. **CONSEQUENCE NOW LIVE: per DEC-48/DEC-47(b), UI ownership RETURNS TO JIM — operator-owned, no agent UI work without new direction.** Remaining process debt: the owed batch verifies (TK-244..247, TK-248..251). | architect | TK-251, EP-32, DEC-48, DEC-47, TK-225, TK-246, contract v2.79 |
| 2026-07-17 | **BATCH-VERIFY DEBT DISCHARGED (RECORD, contract v2.80 — within authority: process-debt closeout; NO code touched, governance + changelog only).** Independent read-and-run reviewer ran one bar at HEAD 140ad87 plus five batch reviews with adversarial adjudication of every serious finding. Verdicts: **TK-230..234 FAIL** — one confirmed MAJOR → **ISS-9**: TK-230 orphaned `tests/integration/test_drain_pathway_e2e.py::test_idle_second_drive_on_empty_queue_parks_wait` (still asserts the removed Wait-on-empty self-park; reproduced FAILED at HEAD, COMPLETED vs WAITING; escaped every green bar because pg-gated + name misses the `-k pg` selector); product behavior CORRECT post-DEC-41, fix is test-only; every other AC in the batch re-executed PASS. **TK-235..239 PASS_WITH_MINORS** (live scrubbed-env boot proof for TK-237; TK-235 superseded-not-failed by TK-242/DEC-43 with a stronger single-transaction guarantee; TK-237 AC prose names the wrong env var — text only). **TK-240..243 PASS** (103/103 on throwaway pg zero skips; DEC-44 no-silent-import hazard proven; collection hermeticity independently re-proven via a psycopg.connect-guard over the full 1624-test collection, zero connects). **TK-244..247 PASS** (crown-jewel no-body-storage proven by the reviewer's own adversarial raw-SQL scan — SECRET marker never landed, gmail row = exactly the 5 whitelist keys). **TK-248..251 PASS_WITH_MINORS** (gmail-open security core independently traced and HOLDS — message_id-only renderer, MAIN-side regex, no renderer URL reaches the shell; ChatPane-unmodified diffstat-proven; minors: degraded states render a muted paragraph not card shells, Upcoming triple-computes buckets — noted-for-operator). Bar: ruff/mypy/tsc/npm ALL green; pytest NOT green — 21 unarmed + 4 pg-gated failures, ALL one root cause → **ISS-10**: assemble_runtime eagerly refreshes the REAL keyring Google credential during "hermetic" tests and it is expired/revoked (google.auth RefreshError invalid_grant); the unarmed-bar-hermeticity claim is FALSE at this HEAD. Both issues OPENED not fixed (verify-only). **gate_check analyze now FAILS on ISS-9/ISS-10 by design — routing owed: mint a test-only fix ticket (ISS-9) + a hermeticity fix ticket (ISS-10 fixture half); the ISS-10 live half (re-consent passes + publish OAuth app to production) goes on Jim's bring-up list.** Standing debt = 5 documented pg-ARMED e2e failures + ISS-9 + ISS-10. | architect | ISS-9, ISS-10, TK-230, DEC-41, DEC-43, DEC-45, DEC-48, contract v2.80 |
| 2026-07-17 | **ISS-9 + ISS-10 ROUTED (COMMIT, contract v2.81 — within authority: issue routing + ticket minting inside the locked frame; +DEC-49, a boot-degrade how-ruling under CON-3/the TK-16 loud-skip contract; no locked DEC or v1 scope touched).** Three atomic build-ready tickets, board 168/171, BUILD ORDER TK-254 → TK-253 → TK-252. **TK-254** (ISS-10(a), production, deps none): NEW root `tests/conftest.py` (verified: none exists) — autouse GOOGLE_OAUTH env-strip so `_has_google_client_credentials` is False by default (both composition sites take their EXISTING Google-less loud-skip branches) + autouse keyring tripwire (AssertionError on any `keyring.get/set/delete_password` reach) + canary test; google-path tests opt in via setenv + the existing injected-fake-token-store seams (`assemble_runtime(gmail_token_store=…)`, `build_source_registry(token_store=…)`). **TK-253** (ISS-10(b), production, deps TK-254) under **DEC-49**: expired/revoked stored credential degrades EXACTLY like absent at ALL THREE session-factory sites — `bootstrap.py` gmail outbound ~875, `sources/bootstrap.py` `_build_gcal_poller` ~186 / `_build_gmail_poller` ~225 (gcal crash class VERIFIED in source: token-present check passes on an expired token, then `get_credentials` → `creds.refresh`); guard ruled BROAD `except Exception` around ONLY the `make_*_session` call (CRF-6 precedent), WARNING + exc_info + exact re-consent command; capability+DRAFT-route all-or-nothing pair holds; builders return None so brief fetches share the decision (TK-96 extraction); interactive consent flow unwrapped. **TK-252** (ISS-9, mvp, test-only, deps TK-254): rewrite `test_idle_second_drive_on_empty_queue_parks_wait` to the as-built Done-on-empty contract (COMPLETED, steps `('drain_queue',)`, DRAIN_HEARTBEAT artifact — verified in `stages/drain_queue.py`, transitions `('gate',)`), keep the raising-FakeModel mouth-guard, fix stale comments, RENAME with a pg-bearing name so `-k 'pg or postgres'` collects it (escape vector closed). **OPERATOR HALF stays with Jim, NOT a ticket** — Google re-consent passes + verify the GCP OAuth app Testing-vs-Production publishing status. gate_check analyze back to PASS. Tickets go into the next standard build-batch. | architect | TK-252, TK-253, TK-254, DEC-49, ISS-9, ISS-10, contract v2.81 |
| 2026-07-17 | **TK-253 DONE (COMMIT, contract v2.82 — within authority: ticket closeout on builder self-run bar; batch-verify pending).** DEC-49 boot robustness LANDS — an expired/revoked stored Google credential degrades EXACTLY like an absent one at ALL THREE session-factory boot sites: `bootstrap.py` gmail outbound and `sources/bootstrap.py` `_build_gcal_poller`/`_build_gmail_poller` each wrap ONLY their `make_*_session` call in a broad `except Exception` (CRF-6 precedent per DEC-49), WARNING with `exc_info` + the exact per-source re-consent command, then the existing no-stored-credential skip path (gmail: capability+DRAFT-route all-or-nothing pair holds; sources: return None so registry AND brief fetches share the skip via the TK-96 extraction). Interactive consent flow untouched. **SEQUENCE NOTE (within authority):** builder landed TK-253 AHEAD of declared dep TK-254 — accepted; the dep was bar-ordering preference, and this fix removes the exact RefreshError crash class behind the 21 ISS-10 unarmed failures, so the full suite is green now (1544/92/0, ruff+mypy-strict clean). **TK-254 stays OWED as the hermeticity GUARANTEE** (green-by-degrade ≠ hermetic-by-construction; env-strip + keyring tripwire still required), then TK-252. Jim's operator half (re-consent passes + OAuth publishing status) unchanged. | architect | TK-253, DEC-49, ISS-10, TK-254, TK-96, contract v2.82 |
| 2026-07-17 | **TK-252 DONE (COMMIT, contract v2.83 — within authority: ticket closeout on builder self-run bar; batch-verify pending).** The ISS-9 stale test is RETIRED (test-only, no production code): `tests/integration/test_drain_pathway_e2e.py::test_idle_second_drive_on_empty_queue_parks_wait` rewritten + RENAMED `test_idle_second_drive_on_empty_queue_completes_done_pg` — assertions now pin the as-built TK-230/DEC-41 Done-on-empty contract (final.status COMPLETED, steps `('drain_queue',)`, NEW drain_queue-output-artifact-kind-is-DRAIN_HEARTBEAT assertion; raising-FakeModel mouth-never-called guard UNCHANGED), and the pg-bearing name closes the exact `-k 'pg or postgres'` selector escape vector that let the stale assertion falsify recorded green bars. Stale WAITING/self-park prose corrected everywhere in the file, incl. the module-level Q-53 IDLE SCENARIO docblock (would have failed AC2's file-wide grep). AC proofs on a builder throwaway postgres:16 (port 5511): AC1 6/6 pass, AC2 grep clean, AC3 renamed test collected under the pg selector; diff = the single files_in_scope file (12+/16-). **SEQUENCE NOTE (within authority, v2.82 precedent):** landed ahead of declared dep TK-254 — bar-ordering preference, not a code dependency; AC1 ran on a real armed pg regardless. Architect re-ran `uv run pytest -q` pre-commit at HEAD: 1544/92/0. **Board 170/171 — ONLY TK-254 remains** (root conftest env-strip + keyring tripwire, the ISS-10 hermeticity guarantee), then the batch verify for TK-253/252/254. Jim's operator half (re-consent + OAuth publishing status) unchanged. | architect | TK-252, ISS-9, DEC-41, TK-230, TK-254, contract v2.83 |
| 2026-07-17 | **BATCH FINDINGS ROUTED + DEC-50 IN-APP OAUTH (COMMIT, contract v2.84 — within authority except where Jim directed; four rulings in one pass).** **(1) TK-254 UNBLOCKED by scope amendment** on builder-PROVEN findings: env strip becomes `monkeypatch.setenv(key, "")` (delenv does NOT strip — the repo `.env` re-supplies via dotenv_settings; empty env outranks `.env` and consumers `.strip()`-check), and ONE narrow production change SANCTIONED — `assemble_runtime` gains `gcal_token_store` (default None) mirroring the gmail seam, threaded into its `build_source_registry` call AND (with gmail) into `build_brief_fetches` (verified: line ~1115 passes NO token stores today; both factories already accept both kwargs). files_in_scope += `src/wombat/bootstrap.py`, `tests/unit/test_runtime.py`. **(2) TK-255 MINTED** (EP-1, P1, S, test-only) — ISS-9 class sweep: dream e2e ~179/~366 (mechanical TK-252 shape + pg-bearing renames) + outbound tk179 ac1/ac2, with a tests-tree-wide grep AC closing the CLASS. **(3) tk179 PREMISE RULED after source verification** — the above-baseline position-independence route is unreachable post-DEC-41 BOTH ways (empty-queue runs COMPLETE immediately + terminal runs can't be `fire_timer`'d; drain graph strictly linear/acyclic, multi-item preload drains as ONE batch in ONE step → draft_composer parks at index 4 in ANY reachable run): COLLAPSE to `step_index == 4` with a DEC-41-citing comment; LOST COVERAGE recorded (no e2e proves the stage-identity lookup above baseline; unit coverage stands); approve/reject stage-identity halves stay load-bearing — never a placebo, never a skip. **(4) DEC-50 ACCEPTED (JIM DIRECTION, verbatim in the decision)** — in-app Google OAuth: consumer-facing consent lives in the app, CLI auth modules remain plumbing; supersedes DEC-47(b) IN PART for exactly this feature; CON-5/keyring custody/DEC-47(d) unchanged. **TK-256** (EP-32, backend): `settings_app/google_connect.py` — honest 4-state status (`not_configured/not_connected/expired/connected`; expired = refresh-probe in a broad try, DEC-49 posture; probe safe iff `load()` not None — verified `get_credentials` only prompts on stored-None) + background-thread consent trigger reusing GmailAuth/CalendarAuth VERBATIM; `GET /google/status` + `POST /google/{service}/connect` (202/409); `__main__` wiring resolves the two Google vars the `_resolve_pg_dsn` way, never `load_config()`; all tests on fakes. **TK-257** (EP-32, UI): GoogleConnections rows in the API Keys view, consent-in-progress polling, restart hint pointing at RuntimeControls, TK-225 tokens-only. Operator half stays with Jim: the consent click + GCP Testing-vs-Production check. **BUILD ORDER TK-254 → TK-255 → TK-256 → TK-257. Board 170/174.** validate PASS, gate_check analyze PASS. | architect | TK-254, TK-255, TK-256, TK-257, DEC-50, DEC-47, DEC-49, CON-5, EP-32, contract v2.84 |
| 2026-07-17 | **BATCH SELECTED + SEAMS RE-VERIFIED (COMMIT, contract v2.85 — within authority: batch sequencing inside the v2.84 ruled order; NO new rulings needed).** Next batch = **TK-254 → TK-255 → TK-256 → TK-257** (all four open tickets, board 170/174; 254/255 first because they restore the hermetic bar TK-256's AC3 depends on, then the DEC-50 backend before its UI). Every cited seam re-verified against live source with zero drift: no root `tests/conftest.py`; `assemble_runtime` (bootstrap.py 648) takes only `gmail_token_store` (655), `build_brief_fetches` call at 1115 passes no stores, `build_source_registry` call at 1154 threads gmail only — the sanctioned gcal passthrough is still owed exactly as ruled; both sources/bootstrap factories already accept both store kwargs (584/585, 716/717); cog-worx `RunStatus` verified in installed source `cogworx/loop/state.py:32` (COMPLETED/AWAITING_HUMAN/WAITING). TK-255 line targets confirmed (dream 179/366 + comment 170; outbound 452/455, docblock 428-435) **plus a pinned note: the collapse also rewrites the `step_index > 4` asserts at 495/551 to `== 4`** (same ruling, in scope). TK-256 seams: `create_app(store, key_store, token, external_store=None)` + X-Wombat-Token 401 guard; `_resolve_pg_dsn` dotenv pattern; `GmailAuth.get_credentials` stored-None→`run_local_server(port=0)` / stored-expired→non-interactive refresh+save (132–170), CalendarAuth mirrors; keyring accounts `gmail-oauth-token`/`gcal-oauth-token` under service `wombat`; WombatConfig needs a sentinel `deepseek_api_key`. TK-257 seams: `tokenedFetch` (api.ts 80), AudioPanel `restartNotice` pattern, App.tsx `view === "keys"` section (420). Operator OAuth bring-up deliberately excluded — nothing in this batch needs a live Google credential (all ACs on fakes/mocks). Nothing deferred; batch-verify for TK-252/253 rides this batch's verify leg. | architect | TK-254, TK-255, TK-256, TK-257, DEC-49, DEC-50, contract v2.85 |
| 2026-07-17 | **TK-254 CLOSED OUT (COMMIT, contract v2.86 — within authority; batch-verify pending). Board 171/174.** The ISS-10(a) hermeticity guard lands exactly as ruled in v2.84: new root `tests/conftest.py` with the autouse empty-string Google-env strip (`monkeypatch.setenv` — delenv proven insufficient, `.env` re-supplies) + the autouse keyring tripwire (AssertionError on any `keyring.get/set/delete_password` reach); `tests/unit/test_hermeticity_guard.py` canary proves an unpatched `KeyringTokenStore.load()` trips it. The ONE sanctioned production change: `assemble_runtime` gains `gcal_token_store` mirroring the gmail seam, threaded into both `build_source_registry` and `build_brief_fetches` calls (defaults None = real stores, byte-identical behavior). Two named tripwire casualties fixed in `tests/unit/test_runtime.py`; a THIRD unnamed casualty (`tests/unit/test_bootstrap.py::test_assemble_runtime_expired_gmail_token_degrades`) fixed the same way and the file added to `files_in_scope` — accepted within authority (same class, same fix shape). Bar: builder pytest 1545/92/0 + ruff + mypy strict (326 files) green; architect pre-commit pytest re-run 1545/92/0. ISS-10 code side now FULLY discharged (TK-253 degrade + TK-254 guarantee); operator half stays with Jim. validate PASS, gate_check analyze PASS. NEXT — TK-255. | architect | TK-254, TK-253, ISS-10, DEC-49, EP-1, contract v2.86 |
| 2026-07-17 | **TK-255 DONE (COMMIT, contract v2.87 — within authority: ticket closeout on builder self-run bar; batch-verify pending). Board 172/174.** The **ISS-9 CLASS is CLOSED** (test-only, no production code): the four remaining Wait-on-empty-drain assertions that TK-230/DEC-41 retired AND that escaped the `-k pg` selector are swept. **(A)** `tests/integration/test_dream_pathway_e2e.py` — both dream-pathway tests renamed with a pg-bearing suffix (selector now collects them), drain_final assertions pin `RunStatus.COMPLETED` + a NEW `DRAIN_HEARTBEAT` artifact-kind check on the drain_queue step (TK-252 shape), stale self-parks-WAITING comment corrected to the DEC-41 completes-with-heartbeat contract. **(B)** `tests/integration/test_outbound_wiring_e2e.py` — tk179 pair COLLAPSED to baseline per v2.84: `_idle_then_surface_draft` rewritten to enqueue-then-run (fresh run parks AWAITING_HUMAN directly), both tk179 tests pg-renamed, the two `step_index > 4` asserts (495/551) → `== 4` with a DEC-41/TK-255 comment citing the structurally-pinned linear 5-stage drain graph, stale docblock rewritten; approve-dispatches-once / reject-cancels stage-identity halves stay load-bearing and untouched. LOST COVERAGE recorded per v2.84 (no e2e proves DraftDispatchStage stage-identity lookup above the fresh-run baseline; unit coverage stands). **Incidental in-scope fix accepted within authority:** the file's three `with_google=True assemble_runtime()` calls were an UNNAMED fourth TK-254 keyring-tripwire casualty (TK-254 fixed test_runtime.py/test_bootstrap.py but missed this file) — fixed via the sanctioned gcal/gmail fake-token-store seams, same class/shape. AC proofs (builder throwaway postgres:16 port 5437): AC1+AC2 both files pass in full (2/2 dream, 6/6 outbound), `-k 'pg or postgres'` collects exactly the 4 renamed tests all passing; AC3 whole-tree grep for the retired contract = ZERO matches asserting it true (remaining hits are brief_timer/dream_timer/generic-runtime self-park contracts, correctly untouched). Architect re-ran `uv run pytest -q` pre-commit: 1545/92/0 (unarmed; the renamed pg-gated e2e collect only on an armed pg). validate PASS, gate_check analyze PASS. **Board 172/174 — NEXT TK-256** (DEC-50 in-app Google OAuth backend), then TK-257, then the batch verify. | architect | TK-255, ISS-9, DEC-41, TK-230, TK-254, EP-1, contract v2.87 |
| 2026-07-17 | **TK-256 DONE (COMMIT, contract v2.88 — within authority: ticket closeout on builder self-run bar; batch-verify pending). Board 173/174.** The **in-app Google OAuth BACKEND lands** (EP-32, DEC-50, third leaf of the v2.85 batch). NEW `src/wombat/settings_app/google_connect.py` — `GoogleServiceConnection` (per-service status probe + background consent trigger) + `GoogleConnectionManager`, both taking injectable `token_store`/`auth_factory` so every test runs on fakes (zero live Google credential). `status()` is honest + non-crashing per the DEC-49 posture: creds unresolved → `not_configured`; `token_store.load()` is None → `not_connected` and `get_credentials()` is NEVER called (a status read can never pop a browser — the VERIFIED seam contract); else a broad try around `get_credentials()` yields `connected` (verified-by-refresh) or `expired` (any Exception, same operational class, never crashes). `connect()` spawns a daemon thread running the SAME `get_credentials()` (`run_local_server(port=0)` blocks while the system browser pops — CON-5, the human does consent, the app never handles Google passwords), tracks idle/in_progress/error consent state in process memory only, raises `ConsentInProgressError` → 409 on concurrent connect; the token lands in the shared OS keyring via the auth module's OWN `token_store.save` (custody unchanged) and the runtime reads it at next boot (restart hint = the UI half, TK-257). `api.py` `create_app` gained optional `google_connections=None` mirroring the `store=None` degrade; token-guarded `GET /google/status` + `POST /google/{service}/connect` (202/409/503). `__main__.py` added `_resolve_google_oauth_credentials` (env-presence, not truthiness, outranks dotenv — the hermetic-strip precedent) and constructs `GmailAuth`/`CalendarAuth` over a directly-built `WombatConfig` with explicit kwargs when both creds present; CLI auth modules REUSED verbatim, never forked (DEC-50 standing rule). Bar: builder full-repo pytest 1565/92/0, settings_app 59/3, ruff clean, mypy strict clean (328 files), every AC manually exercised (four states, 202+in_progress+flip-to-connected, 409 concurrent, raising-runner surfaces error, process stays up); architect re-ran `uv run pytest -q` pre-commit 1565/92/0. validate PASS, gate_check analyze PASS. **Board 173/174 — NEXT TK-257** (GoogleConnections rows in the API Keys view, consent-in-progress polling, restart hint, tokens-only) closes the batch and the board, then the batch verify (TK-254..257 + the TK-252/253 debt). Operator half (consent click + GCP Testing-vs-Production check) stays with Jim. | architect | TK-256, DEC-50, DEC-49, CON-5, EP-32, contract v2.88 |
| 2026-07-17 | **TK-257 DONE — BOARD CLOSES 174/174 (COMMIT, contract v2.89 — within authority: ticket closeout on builder self-run bar; batch-verify pending).** The **in-app Google OAuth UI lands** (EP-32, DEC-50, fourth and final leaf of the v2.85 batch). `app/src/api.ts` gained the five Google types + `getGoogleStatus()` / `connectGoogleService(service)` over the existing `tokenedFetch` (NO new Electron bridge — plain token-guarded HTTP), verified against the TK-256 landed shapes (`GET /google/status`, `POST /google/{service}/connect` → 202/409/503). NEW `app/src/components/GoogleConnections.tsx` — one row per service (Google Calendar, Gmail): four-state status chip, Connect/Reconnect off status (no button for `not_configured`), consent-`in_progress` disables the button + shows the waiting-for-browser line + POLLS `/google/status` ONLY while in_progress (stops on any terminal state), consent error via `role=alert` (button re-enables automatically since disabled tracks `consent==="in_progress"`), restart notice ("Restart Wombat… Settings > System", the AudioPanel `restartNotice` pattern) shown ONLY when a service THIS component triggered lands on connected; API-down → honest degraded state. Housed in the existing API Keys view (App.tsx); RuntimeControls/ChatPane untouched — restart stays in Settings > System (notice only points at it). Bar: builder npm test 267/267 (incl. new GoogleConnections.test.tsx 7/7 — AC1 four statuses + no-button + degrade, AC2 consent lifecycle), tsc lint clean, all standing TK-225 tokens-only audits green; python untouched. Architect re-ran `uv run pytest -q` pre-commit: 1565/92/0. **DEC-50 FULLY discharged on the code side (TK-256 backend + TK-257 UI); operator half (consent click + GCP Testing-vs-Production check) stays with Jim.** validate PASS, gate_check analyze PASS. **BATCH-VERIFY PENDING per the v2 cadence — one Opus review now audits TK-254..257 + the TK-252/253 debt. Board 174/174 CLOSES; UI ownership returns to Jim per DEC-47(b) except where DEC-50 supersedes.** | architect | TK-257, DEC-50, DEC-47, CON-5, EP-32, contract v2.89 |
| 2026-07-17 | **DEC-51 ACCEPTED + TK-258 MINTED (COMMIT — live-defect routing, within authority; contract v2.90, board 174/175).** Operator-hit defect verified in source: TK-256's Reconnect could NEVER succeed with a stored-but-REVOKED token — get_credentials goes interactive only when load() is None (gmail/auth.py 132-161, gcal mirrors); a stored revoked token always hits creds.refresh → RefreshError('invalid_grant') → consent error with the raw tuple in the UI. Jim hit exactly this; the CLI shares the flaw, so the DEC-49 re-consent hint was operationally false. RULING (the DEC-50 verbatim pin HOLDS via the token-store seam — clear() verified on the TokenStore Protocol / KeyringTokenStore, gcal/token_store.py 78-86): (a) connect() = start-fresh-consent — _run_consent clears the stored token via the injected store's clear() before get_credentials(), which then provably goes interactive; GoogleTokenStoreLike widens to declare clear(); (b) the status probe stays strictly non-destructive; (c) recorded consequence — abandoning the browser post-clear honestly demotes to not_connected (the cleared token was worthless server-side anyway); (d) error humanization lands BACKEND-side (invalid_grant → 'expired or was revoked — reconnect', raw to logger exc_info) — no UI ticket, GoogleConnections.tsx already renders the payload message verbatim; (e) CLI parity — both auth main() entry points adopt clear-then-consent with corrected docstrings (entry points are DEC-50-classified plumbing; get_credentials bodies untouched; pollers' never-prompt-at-boot byte-identical); TK-256's no-token-deletion budget line superseded IN PART for exactly these paths. TK-258 (EP-32, P1, M, production, deps TK-256) carries the fix + the previously-missing stored-but-revoked runnable AC. BUILD ORDER: TK-258 alone. NEXT: build TK-258, then the standing post-batch Opus verify (TK-252..257 debt rides along). | architect | DEC-51, DEC-50, DEC-49, TK-258, TK-256, EP-32, CON-5 |
| 2026-07-17 | **BATCH SELECTED — TK-258 alone (COMMIT, contract v2.91 — within authority: sequencing under the v2.90 DEC-51 ruled order).** Board 174/175; TK-258 is the only open ticket, dep TK-256 done. All briefing seams RE-VERIFIED live pre-build, zero drift: GoogleTokenStoreLike load-only (google_connect.py 59-63, widens to clear()); _run_consent 139-150 / logger.warning 143 (upgrade exc_info=True); status probe 96-121 clear-free; TokenStore Protocol load/save/clear with idempotent KeyringTokenStore.clear() (gcal/token_store.py 78-86, gmail re-exports); gmail get_credentials 132-161 (interactive only on load() None; stored token → creds.refresh 158 → RefreshError when revoked); gmail main() 174-182 + gcal main() 160 both store-less with false docstring claims; __main__.py 110-125 proves the same-store custody invariant per service (clear via the seam empties the store the auth reads). No new ruling needed; ticket passes atomicity (one module + two entry points + tests, all ACs on fakes under the TK-254 keyring tripwire; operator OAuth bring-up stays outside the loop). NEXT: build TK-258, then the post-batch Opus verify (TK-252..257 debt rides along). | architect | TK-258, DEC-51, DEC-50, TK-256, TK-254, EP-32 |
| 2026-07-17 | **TK-258 DONE — BOARD CLOSES 175/175 (COMMIT, contract v2.92 — within authority: ticket closeout on builder self-run bar; batch-verify pending).** DEC-51 Reconnect-starts-fresh-consent LANDS (EP-32). `google_connect.py`: `GoogleTokenStoreLike` widened to declare `clear()`; `_run_consent` clears the stored token ON THE CONSENT THREAD before `get_credentials()` — a stored-but-revoked token now provably takes the interactive `InstalledAppFlow` branch instead of the doomed `creds.refresh` RefreshError loop (the operator-hit defect); status probe stays strictly non-destructive (spying-fake proven, DEC-51(b)). Error humanization per DEC-51(d): `CONSENT_EXPIRED_ERROR` ("Google connection expired or was revoked - reconnect to continue.") on RefreshError, concise generic message otherwise, raw text ONLY to `logger.warning(..., exc_info=True)` — never the payload (caplog-proven); no app/ change, GoogleConnections.tsx renders it verbatim. CLI parity per DEC-51(e): gmail + gcal `main()` construct `KeyringTokenStore`, `clear()`, pass `token_store=` in, then consent; false docstrings corrected. **`get_credentials()` bodies UNTOUCHED** — architect verified the gmail diff is main()-only and the gcal diff a single main() hunk; builder diff-confirmed byte-identical bodies (DEC-50 verbatim pin holds; pollers' never-prompt-at-boot byte-identical). Diff = exactly the seven files_in_scope (242+/24-). Bar: builder pytest 1571/92/0, ruff clean, mypy strict clean (328 files), test_google_connect 13/13; architect re-ran `uv run pytest -q` pre-commit. validate PASS, gate_check analyze PASS. **Board 175/175 CLOSES. NEXT — the post-batch Opus verify (this batch + the TK-252..257 debt). Operator half stays with Jim: Reconnect click per service → approve in browser → restart runtime, plus the GCP Testing-vs-Production publishing check.** | architect | TK-258, DEC-51, DEC-50, DEC-49, TK-254, EP-32, contract v2.92 |
| 2026-07-18 | **DEC-52 recorded + ISS-11/12/13/14 raised-and-routed + TK-259/260/261 minted (COMMIT, contract v2.93, board reopens 175/178 — within authority: operational how, no spine/scope change).** THE 2026-07-18 OUTAGE, on the record: the runtime died silently ~07:00–07:26 (Jim saw stuck email + offline chat AFTER a valid Google reauth; restart restored both instantly). **CAUSE CONFIRMED live** — a runtime booted inside a disposable background-command wrapper dies on wrapper teardown (reproduced in-session, exit-5 class; the traceless 07:26 ghost handshake = same pattern, ISS-13 closed structurally, no forensic ticket). Also live-observed: **THREE concurrent runtimes** (no runtime-side singleton — ISS-14) and **no logs/runtime-*.log from any bare boot** (VERIFIED: the project's ONLY file logging is wombat-console.ps1's Tee-Object — ISS-12). **DEC-52:** (a) log custody moves INTO `python -m wombat` main() — per-boot logs/runtime-<ts>.log + boot banner + last-gasp CRITICAL + honest shutdown line; console script drops its Tee (TK-259); (b) console host becomes a relaunch-with-backoff watchdog (5s→300s cap, 10min-healthy reset); restart-wombat.ps1 kills the marker-identified watchdog host FIRST (TK-260); (c) notification pinned — Offline chip is the v1 bar, death-toast = recorded deferral; (e) runtime-owned singleton = exclusive loopback bind in main(), new config `wombat_singleton_port` default 63218, OS-released so no stale-lock class (TK-261); (f) **STANDING LAUNCH RULE — never boot the runtime inside a disposable wrapper**; endorsed paths = the scripts / detached Start-Process. Noted, not a defect: gcal newest item 2026-07-11 with clean token refresh = empty poll window. **BUILD ORDER TK-259 → TK-261 → TK-260, AFTER the pending Opus batch-verify of TK-252..258.** validate PASS (243 nodes, 205 gov); gate_check analyze PASS. Current live state: exactly one runtime (detached console, 07:51:37), chat answering, gmail wired. | architect | DEC-52, ISS-11, ISS-12, ISS-13, ISS-14, TK-259, TK-260, TK-261, TK-238, TK-239, EP-1, CON-3, contract v2.93 |
| 2026-07-18 | **TK-259 intent amended (COMMIT, contract v2.94 — governance only): second live ISS-12 data point makes the entry-point-logging rationale BINDING.** Even the ENDORSED restart-wombat.ps1 launch produced NO logs/runtime-20260718-*.log minutes into a healthy boot (runtime pair up 07:59:25, handshake 07:59:32, chat answering) — Python stderr is block-buffered when piped and Tee-Object creates the file only on the first object, so nothing ever flushed. The Tee path is unreliable EVERYWHERE, not merely absent on bare boots. TK-259 now pins: the fix must NOT rely on console piping in any way — the FileHandler writes directly from inside the process, and the boot banner must appear in the per-boot file within seconds of boot on every launch path. ACs unchanged (they already assert runtime-written files). validate PASS; gate_check analyze PASS. Endorsed-path runtime remains the current live instance. | architect | TK-259, ISS-12, DEC-52, TK-238, EP-1, contract v2.94 |
| 2026-07-19 | **Batch selected (no contract change — v2.95 stands): TK-262 → TK-259 → TK-261 → TK-260 → TK-263, the full remaining board (175/180 done).** A prior batch attempt (wf_7125f8c3-8f8) never landed any code — repo at 1b7a773 with a clean tracked tree — so the v2.95 mint is re-briefed as-is. Every seam re-verified live pre-brief, zero drift: playback.py:38-40 raw PlaySound(SND_MEMORY); sinks/speak.py broad-except→terminal Degraded(spoken=False, degraded=True) with CancelledError re-raised (cogworx.loop.result.Degraded confirmed at ../cog_worx/cog-worx src/cogworx/loop/result.py:54); __main__.py main() is a bare asyncio.run(serve()) (no logging/lock — both tickets green-field there); wombat-console.ps1:74 carries the Tee-Object to drop; restart-wombat.ps1 kills only `-m wombat` python matches (no watchdog-host awareness yet — TK-260's ordering hardening is real); Header.tsx:17-25 once-at-mount getInfo()!==null (the ISS-16 lie), chat.ts getInfo bridge confirmed. No new ruling needed — DEC-52(a/b/e) + DEC-53(a/b) already bind every ticket. Order: TK-262 first (restores the crashed runtime), TK-259 (dep of both), TK-261, TK-260, TK-263 independent last (app-side, isolated). All ACs run on fakes/spies — operator OAuth bring-up and live launches stay outside the loop. | architect | TK-259, TK-260, TK-261, TK-262, TK-263, DEC-52, DEC-53, ISS-11..ISS-16, EP-1, EP-31, EP-32, contract v2.95 |
| 2026-07-19 | **TK-259 DONE (board 177/180, contract v2.97): DEC-52(a) runtime-owned log custody + DEC-53(b) faulthandler-into-the-per-boot-log LANDED — the traceless-death class (ISS-12/ISS-13, corroborated live twice) is closed structurally.** `src/wombat/__main__.py` main() now calls `_configure_logging()` exactly once: stderr handler preserved + one FileHandler at `logs/runtime-<yyyyMMdd-HHmmss>.log` under cwd (flush-per-emit, zero reliance on console piping per the v2.94 binding pin), boot banner first record, then `faulthandler.enable` pinned to that SAME open FileHandler stream (kept open for process lifetime) so a native AV dumps thread stacks into the runtime-owned file. Last-gasp wrapper around `asyncio.run(serve())`: clean return / KeyboardInterrupt / CancelledError → honest shutdown line, no traceback; any other exception → CRITICAL with full traceback + shutdown line + `sys.exit(1)`. SIGKILL signature = bannered file with no shutdown line (accepted honesty bar). Handler custody is entry-point-only — importing wombat / settings_app adds zero handlers. `wombat-console.ps1` drops its Tee (custody in exactly one place); guard, root-assert, close-window kill affordance untouched. New `tests/unit/test_main_entry.py` 6/6 on fakes covers all 5 ACs. Builder self-ran full bar green (pytest 1581 passed/92 skipped, ruff, mypy strict 329 files); architect re-ran pytest pre-commit. **Batch-verify pending** — one Opus review audits the whole batch after all five tickets land. Next: TK-261. |
| 2026-07-19 | **TK-261 DONE (closeout — within authority; contract v2.98, board 178/180): DEC-52(e) runtime-owned single-instance lock LANDS, closing the ISS-14 three-concurrent-runtimes class structurally.** `src/wombat/config.py` gains `wombat_singleton_port` (default 63218, operator .env tier — NOT in APP_EDITABLE_FIELDS, NOT in REQUIRED_ENV). `src/wombat/__main__.py` main() calls load_config() then `_acquire_singleton_lock(port)` BEFORE `asyncio.run(serve())` and AFTER TK-259 logging, so a refusal lands in that boot own per-boot log; the lock is an exclusive 127.0.0.1 TCP bind held in a module-level global for process lifetime — never accepted, never listened, never explicitly closed anywhere in the diff, so the OS provably releases it on ANY death including hard kills (no stale-lock file, no cleanup branch — the DEC-52 pidfile rejection holds by construction). Bind failure raises local `_SingletonLockError`, caught in main() ahead of the generic last-gasp handler: exactly ONE loud ERROR naming the port and the config field, then `sys.exit(1)`. serve() acquires no lock (library-clean, existing serve()-driving tests untouched); wombat-console.ps1 untouched. Tests cover all three ACs on ephemeral override ports with serve monkeypatched (second-instance fast refusal, hard-kill OS release, config tier discipline). Builder self-ran the bar green — full suite 1586 passed / 92 pg-skipped, ruff clean, mypy strict clean (329 files), diff exactly the four files_in_scope (227+/0-). Architect re-ran pytest pre-commit. BATCH-VERIFY PENDING per the v2 cadence. NEXT: TK-260. | architect | TK-261, EP-1, DEC-52, ISS-14, TK-259, contract v2.98 |
| 2026-07-19 | **TK-260 DONE (closeout — within authority; contract v2.99, board 179/180): DEC-52(b) console-host watchdog LANDS, closing the ISS-11 availability half.** `scripts/wombat-console.ps1` hosted command is now a relaunch-with-backoff loop around `python -m wombat` — loud marker line (timestamp + exit code) on every exit, 5s backoff doubling to a 300s cap, reset to 5s after >=10min healthy uptime, respawn forever; hosted command line carries the literal token `wombat-watchdog-host`; backoff math is a pure dot-source-testable `Get-WombatNextBackoffDelaySeconds`. `scripts/restart-wombat.ps1` ordering hardened: kill watchdog-host matches (by marker) FIRST, then all `-m wombat` python matches, prove zero matches of BOTH kinds within the bounded wait, then launch — the intentional-restart-vs-respawn race is closed by construction. TK-239 exit-code contract + DEC-42 close-window kill affordance preserved; no script-side file logging (TK-259 custody). NEW `tests/unit/test_wombat_console_watchdog.py` (12 tests, all three ACs on the real function). Builder full bar green (1598/92/0, ruff, mypy 330 files); architect re-ran pytest pre-commit. Batch-verify pending; remaining leaf TK-263. |
| 2026-07-19 | **TK-263 DONE (closeout — within authority; contract v2.100, board 180/180): ISS-16 header-liveness truthfulness LANDS — the batch is fully landed.** `app/src/chat.ts` gains `probeChat()`: re-reads `window.wombatChat.getInfo()` FRESH, then GETs the bridge-supplied loopback port — ANY HTTP response (incl. 404/405) proves the process alive; a thrown fetch or null info = dead; provably no POST /chat (no enqueue side effect) and the token never rides a URL. `Header.tsx` polls probeChat() at mount + every 15s (setInterval cleared on unmount), replacing the once-at-mount handshake-presence check that stayed green through three live runtime deaths on 2026-07-19; the three rendered states (Checking... / Running / Offline) and all styling are byte-identical; RuntimeControls/ChatPane/preload/Python untouched. 3 new App.test.tsx tests cover all ACs (dead-port Offline on the exact ISS-15 live state; any-status Running with no-POST proof; fake-timer Running→Offline flip + no leaked timers). DEC-52(c) death-notification deferral stands — the chip is now merely truthful. Builder bar green (App.test.tsx 10/10; app suite 26 files 270/270; lint tsc --noEmit; build); architect re-ran uv run pytest -q pre-commit (Python untouched). BATCH-VERIFY PENDING — all five leaves of the v2.95 batch (TK-262→TK-259→TK-261→TK-260→TK-263) are landed; the single Opus batch review is the next cadence step. | architect | TK-263, EP-32, ISS-16, ISS-15, DEC-52, contract v2.100 |
| 2026-07-19 | **Batch selected (contract v2.101 stands, no contract edit): TK-264 alone — the sole open ticket on the 180/181 board.** The ISS-17 ruling (v2.101) already binds everything the build needs: sentinel-normalize IN playback.py BEFORE the DEC-53a validation; ONLY the explicit sentinel class {0xFFFFFFFF, 0xFFFFFF00} is ever patched (module constant); non-sentinel overruns NEVER patched (TK-262 rejection contract verbatim); tts.py stays transport-dumb. Seams re-verified live pre-brief, zero drift: play() calls _validate_wav_bytes at playback.py:56 before PlaySound:57; validator 60-83 (wave.open succeeds on sentinel WAVs — the overrun check at 77-83 is what fires, so normalization MUST patch raw RIFF/data size bytes before wave.open sees them); existing rejection fixtures in tests/voice/test_playback.py:126 (empty/non-RIFF/truncated) must stay green untouched. Live crash shape (declared 4294967040 over 278572 actual, runtime-20260719-121721.log) pinned as a required fixture. Ticket is atomic (S, one helper + one call-order change + tests), no new ruling needed. Deliberately NOT picked: nothing else exists — board otherwise closed; operator OAuth bring-up and the live Fish restart smoke remain operator steps outside the loop. | architect | TK-264, ISS-17, DEC-53, TK-262, EP-31, contract v2.101 |
| 2026-07-19 | **Batch selected (contract v2.102 stands, no contract edit): TK-265 then TK-266 — the only two open tickets on the 181/183 board; the v2.102 DEC-54/ISS-18/ISS-19 rulings already bind both builds.** Order: TK-265 first (P1 production, the last known boot-killer class — SAPI/comtypes native abort on the drain-pump thread kills the runtime with zero record) then TK-266 (P2 app-side honesty). Seams re-verified live pre-brief, zero drift: Pyttsx3Adapter tts_adapter.py:35-48 constructs ONE engine in __init__ (line 43, lazy `import pyttsx3`) and speak() 45-48 runs say()/runAndWait() in-process — exactly the DEC-54 target; SpeakSink containment speak.py:81-99 (CancelledError re-raise at 81, broad except at 84 → terminal Degraded spoken=False/degraded=True) is the existing exception path the reshaped adapter's RuntimeError must flow into, consumers untouched. chat.ts sendChat 61-95 and probeChat 47-59 both fetch with NO timeout (ISS-19 confirmed at HEAD); ChatPane.tsx sending-state seam at 31/55/114-129 ("Sending..." at 129); the >30s invariant anchor is CHAT_REPLY_TIMEOUT_SECONDS = 30.0 at src/wombat/chat/surface.py:64 (surface.py untouched by the build). Tickets are disjoint (Python sinks vs app TS), both atomic, no new ruling needed. Deliberately NOT picked: nothing else exists — board otherwise closed; operator OAuth bring-up, the live Fish restart smoke, and the post-batch full chat round-trip remain operator/verification steps outside the build loop. | architect | TK-265, TK-266, DEC-54, ISS-18, ISS-19, EP-31, EP-32, contract v2.102 |
| 2026-07-19 | **TK-265 DONE (closeout — within authority; contract v2.104, board 182/183): ISS-18 subprocess isolation of local TTS lands per DEC-54 — the last known boot-killer class is contained.** NEW `sinks/_local_speak_child.py` (`python -m`): stdin UTF-8 text (never argv), fresh pyttsx3 engine per utterance, exit 0/1, no traceback spew — the exit code IS the wire. `Pyttsx3Adapter.__init__` now only probes `find_spec("pyttsx3")` (same ImportError shape; bootstrap voice-off byte-compatible; no in-process engine ever); `speak()` = `subprocess.run([sys.executable, -m, helper], input=text, timeout=_SPEAK_TIMEOUT_SECONDS=30.0)` raising RuntimeError on nonzero exit / TimeoutExpired (child killed, no orphan) / spawn OSError — a SAPI/comtypes native abort now flows the existing SpeakSink adapter-exception path into terminal Degraded. Protocol + all consumers untouched; cloud winsound path zero-diff (DEC-54b). Builder bar green (sinks 13, full 1612/92-skip, ruff, mypy strict 332 files, diff = 3 files_in_scope); architect re-ran pytest pre-commit. BATCH-VERIFY PENDING (one Opus review after TK-266). NEXT: TK-266, the last open ticket. | architect | TK-265, DEC-54, ISS-18, EP-31, contract v2.104 |
| 2026-07-19 | **TK-266 DONE — BOARD CLOSES 183/183 (closeout — within authority; contract v2.105): ISS-19 chat-send timeout honesty LANDS — the last open ticket.** `app/src/chat.ts` — `sendChat()` races its POST /chat fetch against an AbortController at new named export `SEND_TIMEOUT_MS=45_000` (comment + test both pin the strictly-greater-than-surface.py's-30s-held-window invariant, so a legitimate `held` answer can never falsely time out); an AbortError maps to the ONE new closed `ChatResult` variant `timed_out` (DOMException-name check keeps it distinct from `unavailable`); replied/held/unavailable settled paths byte-identical. `probeChat()` gets its own abort at `PROBE_TIMEOUT_MS=5_000` — a hung liveness probe now reports Offline instead of hanging. `ChatPane.tsx` renders `timed_out` as the honest line "Wombat stopped responding - the message may be lost." with controls re-enabled via the pre-existing `finally setSending(false)` — the live 2026-07-19 stuck-"Sending..." state (ISS-18 kill mid-hold) is structurally closed. No retry/queue; runtime/surface.py untouched. NORMALIZATION within authority: `files_in_scope` swaps App.test.tsx (untouched) for chat.test.ts (builder's honest deviation — the module's own test file). Builder bar green (npm test 26 files 274/274, tsc lint, build; diff = the four normalized files); architect re-ran `uv run pytest -q` pre-commit. validate PASS (248 nodes), gate_check analyze PASS. **BATCH-VERIFY PENDING — the single Opus review of the TK-265+TK-266 batch is the next cadence step, then the operator closeout sequence (restart onto final code, Fish plays live, full driven chat round-trip).** | architect | TK-266, ISS-19, ISS-18, TK-265, TK-263, EP-32, contract v2.105 |
| 2026-07-19 | **BATCH SELECTED + PRE-BUILD RULINGS (COMMIT, contract v2.107 — within authority: pipeline-level hows inside DEC-55).** Next batch = TK-267 alone (sole open ticket, board 183/184; operator OAuth bring-up + live closeout excluded). Three rulings amended into the ticket after live seam verification: (a) SLOT — the drain graph is compose → chat_reply → speak (TK-222), so speech_shape slots AFTER chat_reply (ChatReplyStage retargets, ComposeStage zero functional diff); chat-pane text delivery stays upstream, making DEC-55's text-channel-byte-untouched rule structural; stages/chat_reply.py added to files_in_scope. (b) GUARD SUFFIX — no fifth mouth (DEC-55e stands); fixed prompt appends guard_suffix(Mouth.COMPOSE) verbatim, read-only import, zero persona diff. (c) BUDGET — mirror ComposeStage's optional TK-9 layer-2 ledger wiring exactly, SAME ledger instance from bootstrap. All cited seams re-verified live with zero further drift (compose.py L89, chat_reply.py L62/L68, speak.py L63-103, expression.py L89/L106, bootstrap L982-1000 BOTH graph legs + make_speak_callable L500 off-graph, cog-worx graph.py L66/L74 guards, StageContext.model/last_output stage.py L52/L78). NOTE: a prior TK-267 build (wf_2aa6e0d8-106) may be in flight per session memory — the contract's todo status is the record; if that build lands, batch-verify it against THESE amended rulings (the slot point is the load-bearing check). | architect | TK-267, TK-222, EP-30, contract v2.107 |
| 2026-07-19 | **TK-267 DONE — BOARD CLOSES 184/184 (closeout — within authority; contract v2.108): DEC-55 speech-is-a-SUMMARY LANDS.** NEW `stages/speech_shape.py` slotted exactly per the v2.107 ruling — compose → chat_reply → **speech_shape** → speak (chat_reply retargeted; ComposeStage docstring-only; text channel byte-untouched by construction, chat delivery upstream). One `ctx.model.complete` call when voice-on + adapter-present (fixed module-constant summary prompt + `guard_suffix(Mouth.COMPOSE)` verbatim — no fifth mouth), ComposeStage's TK-9 layer-2 ledger pattern with the SAME bootstrap ledger instance; voice-off/no-adapter = zero model calls (spy-proven). No-placebo validator `_shape_speech_text` REJECTS-never-rewrites the closed token classes (bold/italic, headings, backticks/fences, md links, raw URLs, list markers, overlong); any rejection/blank/model-exception → degraded=True, text=None — NEVER verbatim fallback (DEC-55c). New `wombat.speech_output` artifact kind; SpeakSink speaks THAT text only, degrade → existing terminal Degraded; ack path off-graph zero-diff (DEC-55d). Bootstrap wires BOTH drain-graph legs. NORMALIZATION within authority: files_in_scope drops untouched runtime.py, adds the four ripple test files (test_chat_reply, unit/test_speak_sink, voice/test_egress_lesion, voice/test_playback — audited ripple-only). Builder bar green (26+34 targeted; full 1643 passed / 92 pg-gated skips); architect re-ran `uv run pytest -q` pre-commit; validate PASS (249 nodes), gate analyze PASS. **BATCH-VERIFY PENDING — the single Opus review is the next cadence step, then the operator closeout folds in the DEC-55 spoken-summary live check.** | architect | TK-267, DEC-55, TK-222, EP-30, contract v2.108 |
| 2026-07-19 | **BATCH SELECTED (contract v2.110 stands, no contract edit): TK-268 alone — the sole open ticket (board 184/185).** ISS-20's v2.110 ruling already binds (live-answering port → refuse overwrite with ONE loud WARNING and boot on; dead/absent/unparsable → write byte-identical to today; NO env flag — the guard derives from observable state). All seams re-verified live pre-build, zero drift: `_write_chat_handshake` runtime.py L123-131 writes unconditionally (`surface.handshake_path`/`surface.port`/`surface.token`); caller `_start_chat_surface` L134-150 owns the CON-3 one-loud-WARNING guard and stays the raise-catcher (the refusal branch must NOT raise — it is a logged no-write, not a failure); tests/unit/test_runtime.py is the existing test home with no current handshake-probe tests to collide with. Probe ruling restated for the builder: ONE bounded loopback TCP connect (~1s), connect-then-close, no HTTP, no token use. Deliberately excluded: operator OAuth bring-up + the DEC-55 spoken-summary ear-check (operator steps outside the build loop); nothing else exists to pick. | architect | TK-268, ISS-20, TK-260, TK-261, EP-1, CON-3 |
| 2026-07-19 | **TK-268 DONE — BOARD CLOSES 185/185 (closeout — within authority; contract v2.111): ISS-20 live-handshake stomp guard LANDS.** `runtime.py` gains `_existing_handshake_port()` + `_handshake_port_is_live()` (ONE bounded loopback TCP connect-then-close, module constant timeout, NO env flag/config field per the ISS-20 flag-discipline ruling) wired into `_write_chat_handshake()`: existing+parseable+port-ANSWERS → ONE loud WARNING, file byte-unchanged, boot continues; absent/unparsable/dead → write byte-identical to today (crash/watchdog/restart flows untouched). 5 new tests cover all three ACs. Normalization within authority: `tests/chat/test_chat_surface.py` added to files_in_scope — the TK-222 overwrite test now stops the first surface before relaunch (mirrors a genuine restart; the old test depended on the pre-guard stomp behavior). Builder bar green (1648 passed / 92 pg-gated skips, ruff clean, mypy strict 158 + full-config 334 files); architect re-ran pytest pre-commit. BATCH-VERIFY PENDING (one Opus review, v2 cadence). Remaining outside the loop: Jim's DEC-55 ear-check + OAuth bring-up; no restart needed for the guard (protects the NEXT boot's write). | architect | TK-268, ISS-20, EP-1, TK-222, contract v2.111 |
| 2026-07-19 | **BATCH SELECTED (contract v2.113): TK-269 → TK-270 — the whole open board (185 done + these 2), DEC-56(d) order.** Pre-build rulings from live seam verification: (1) **TK-269 wake placement corrected** — `ChatSource.push()` only buffers (base.py:89-91); the queue write commits in `SourceRegistry._poll_loop` (registry.py:144-151, synchronous, no await between `poll()` returning and the enqueue commits). A push-time wake races the 1s chat poll tick (pump wakes → empty peek → re-idles → item lands unwoken). RULED: wake fires from a `ChatSource.poll()` override when it drains ≥1 event; single-loop atomicity proves the pump observes the set only post-commit. (2) **TK-269 wiring**: RuntimeBundle lacks a chat_source field (only chat_surface, bootstrap.py:671) — bundle gains a default-None `chat_source` pass-through; files_in_scope += bootstrap.py + tests/unit/test_bootstrap.py. (3) **Q-117 raised — beat-attribution correction**: the shipped pump beat is **5.0s** (bootstrap.py:262, unchanged since TK-53; sweeper 5.0s; pollers 300s; NO ~600s constant exists), so ISS-21's 10min7s cadence cannot be the idle beat; leading suspect is gate-HOLD of low-urgency chat + flush_min_age 300s + ≤300s poller-driven redrive. DEC-56's rulings stand (wake + late delivery are correct/needed either way); only the attribution is corrected. Q-117 disposition: judge on the post-batch computer-control driven round-trip — if replies still lag minutes, a NEW DEC rules chat gate treatment. Operator OAuth bring-up excluded (outside the loop). | architect | TK-269, TK-270, DEC-56, ISS-21, Q-117, EP-32, EP-1, contract v2.113 |
| 2026-07-19 | **TK-269 DONE (closeout — within authority; contract v2.114, board 186/187): DEC-56(a) drain wake on interactive enqueue LANDS.** `runtime.py` gains `DrainWake` (asyncio.Event wrapper — set()/wait(timeout), TimeoutError swallowed, cleared on every return so coalesced sets ⇒ at most one extra early wake); `_run_drain_pump` takes optional `wake` (idle = `sleep(beat)` byte-identical when None, else `wake.wait(beat)`); `_drive_and_serve` builds the wake on the running loop and wires `wake.set` into `bundle.chat_source.wake` only when present. `ChatSource` gains `wake` + a `poll()` override firing once post-drain iff ≥1 event (the v2.113 placement ruling — single-event-loop atomicity guarantees the pump sees the set only after the enqueue commit). `RuntimeBundle` gains the `chat_source` pass-through. Pollers/Sweeper unwoken by construction — the beat stays the safety net. Builder bar green (1660 passed / 92 pg-gated skips, ruff, mypy 334 files); architect re-ran pytest pre-commit. **Batch-verify pending** (one Opus review after TK-270). Q-117 stands. TK-270 sole open ticket. |

## 2026-07-19 — Batch selection v2.119: TK-272 -> TK-273 (the DEC-57 chat-always-answers arc)

Board 188/190; the two DEC-57 tickets are the only open work. Four pre-build rulings recorded in the v2.119 changelog entry: R1 chat score custody at the pipeline.py:192 add-site (skip pending add for CHAT, carry the scored item on HOLD so ReviewOrSpeak wires the REAL score); R2 held_chat is an artifact-level optional field with additive accessors (Q-50 forbids payload; router:84 + compose:227 call sites join TK-272 scope, chat_reply zero-diff since text delivery is upstream of shaping); R3 held-chat voice suppression reuses the silent voice-off no-op branches (speech_shape:145, speak:75), NEVER the Degraded speech-text-None branch (speak:102) — quiet-by-design is not degradation; R4 TK-273's event_class comes from a new pure export of user_model's TOTAL fallback map (+payload override), user_model.py joins TK-273 scope. All seams re-verified live against wombat HEAD and installed cog-worx (result.py:23/31/54, stage.py:64/78) — zero drift. Operator OAuth bring-up remains outside the loop; nothing in this batch touches it.

## 2026-07-19 — TK-272 DONE (closeout — within authority; contract v2.120, board 189/190): DEC-57 chat-always-answers LANDS

Built exactly on the four v2.119 rulings, zero deviations. R1: `gate/pipeline.py` skips the pending-set add for a HOLD ItemKind.CHAT and returns `GateDecision(HOLD, items=(scored,))` — the real score is carried, never discarded; non-chat holds keep custody-durable add semantics byte-identically. R2: additive optional `held_chat: bool = False` on the surfaced_item/compose_request/composed_output artifact wires in `stages/artifacts.py` plus matching `*_held_chat_from_artifact_data` accessors (plain-JSON-native; absent defaults False so older artifacts round-trip unaffected), threaded through `review_or_speak.py` (a HOLD entry whose item is CHAT forwards as a surfaced Transition with held_chat=True instead of the terminal hold_report ack), `compose_dispatch_router.py`, and `compose.py`. R3: `speech_shape.py` + `sinks/speak.py` fold held_chat into their existing voice-off pass-through branches — held chat is quiet-by-design text-only (zero speech_shape model calls, no TTS, silent Done spoken=False degraded=False, never the Degraded warning branch); a surfaced chat item is byte-identical to today. `chat_reply.py` needed zero diff, as ruled — text delivery rides compose → chat_reply → broker, upstream of shaping. Non-chat holds and all surfaced items unchanged; gate scoring / rating / broker / surface / app zero-diff (DEC-57d/e); absorbed backlog intentionally not migrated (DEC-57f). Builder self-ran the full bar green: pytest 1678 passed / 92 pg-gated skips; on a throwaway pg the FULL suite 1791 passed / 11 live-gated skips, including all 8 e2e drain-pathway tests plus 2 NEW TK-272 e2e tests (AC1 held-chat, AC2 surfaced-chat) on a real Gate + real WombatQueue + FakeModel spy + recording adapter. Architect re-ran `uv run pytest -q` pre-commit. **BATCH-VERIFY PENDING** per the v2 cadence — one Opus review audits the whole TK-272/TK-273 batch after both land, then the operator re-drives the live chat round-trip. NEXT: TK-273 (gate-decision observability), the sole open ticket.

## 2026-07-19 — TK-273 DONE (closeout — within authority; contract v2.121, board 190/190): ISS-23 gate-decision observability LANDS

Built exactly on the v2.119 R4 ruling, zero deviations. `gate_stage.py`: ONE INFO line per decided (decision, queue_item) entry after the entries pairing — item_id, item_kind, event_class, action, urgency, load; urgency/load from `decision.items[0]` when carried (the TK-272 chat carry) else None, mirroring `_hold_record` honesty. `compose_dispatch_router.py`: ONE INFO line per dispatched item after composer_name resolution (fallback case included). `user_model.py`: NEW pure export `resolve_event_class_for_item(item)` wrapping the Q-41 ruling-1 logic (payload event_class override + TOTAL `_ITEM_KIND_FALLBACK`); `UserModel.resolve_event_class` now delegates — zero behavior change. Existing module loggers only (TK-259 per-boot custody, no new handlers/config); ids and enum values only, never message text (caplog tests). Diff confined to the five files_in_scope. Builder self-ran the full bar green (pytest 1680 passed / 92 pg-gated skips; ruff; mypy 334 files); architect re-ran `uv run pytest -q` pre-commit. **Batch-verify pending** per the v2 cadence — one Opus review audits the whole TK-272+TK-273 batch, then the operator re-drives the live chat round-trip. With this the next boot's recurring compose+speech_shape+Fish cluster (leading suspect: the flush path over the absorbed pending-set backlog, now holding Jim's absorbed turns) self-identifies in the runtime log with zero further forensics.

## 2026-07-19 — BATCH SELECTED (within authority; contract v2.122 stands, no contract edit): TK-274 alone — the ISS-24 anchor fix, sole open ticket (board 190/191)

TK-274 (console-guard regex anchor) is the only non-done ticket; it depends on nothing and blocks nothing. No new ruling needed — the ISS-24 v2.122 record binds the fix shape (end-or-whitespace anchor, e.g. `-m wombat(\s|$)`, honoring the CIM trailing-space quirk by anchor, not by fight). Seams re-verified live pre-build, one refinement over the ticket wording: `wombat-console.ps1` has ONE literal `-m wombat` match site (:69) — the shared CIM filter that feeds BOTH the :145 already-running guard and the :166 post-start root-match assert through `Get-WombatRootProcesses` (:79-80) — so the console fix is one pattern, not two. `restart-wombat.ps1` has its one site at :52; the watchdog-host marker match at :45 keys on the literal `wombat-watchdog-host` token and is untouched, exactly as the ticket's non-goal states. Builder is BARRED from touching live processes (the running app is the condition the fix must tolerate); ACs verified by pattern tests / grep-style asserts on script text, never by killing anything. Operator OAuth bring-up remains outside the loop. When TK-274 lands the board is fully closed at 191/191.

## 2026-07-19 — TK-274 DONE — BOARD FULLY CLOSED 191/191 (closeout — within authority; contract v2.123): ISS-24 console-guard regex anchor LANDS

Built exactly per the ISS-24 v2.122 ruling, zero deviations. Both literal CommandLine match sites — `scripts/wombat-console.ps1` `Get-WombatProcesses` (:69 shared filter feeding the :145 already-running guard AND the :166 post-start root-match assert via `Get-WombatRootProcesses`) and `scripts/restart-wombat.ps1` `Get-WombatProcesses` (:52, feeding the runtime kill + zero-match kill-verify) — changed from the unanchored `-m wombat` to the anchored `-m wombat(\s|$)`, each carrying an ISS-24/TK-238 trailing-space comment. Builder AC evidence: pattern matrix via pwsh `-match` — `python -m wombat` TRUE, `python -m wombat ` (the CIM trailing-space quirk TK-238 minded — honored by the anchor, not fought) TRUE, `python -m wombat.settings_app` FALSE; Select-String confirms both sites anchored (2/2) and restart-wombat.ps1:45 `wombat-watchdog-host` marker byte-identical — TK-239 exit codes, TK-260 kill ordering, TK-261 port-lock backstop all untouched. No Python/app files; no live process touched (barred per the batch ruling — the fix only affects future console launches, no restart needed). Architect re-ran `uv run pytest -q` pre-commit. **BATCH-VERIFY PENDING** per the v2 cadence (single Opus batch-level review). With this every issue from the 2026-07-19 E2E day is resolved; remaining outside the loop: operator OAuth bring-up + the operator live closeout checks.

## 2026-07-19 — DEC-58 ACCEPTED, TK-275/TK-276 MINTED (Jim directive — new scope recorded; contract v2.124; board reopens 191/193): CONFIGURABLE PUSH-TO-TALK

Jim's verbatim flow, honored exactly: a "Set push-to-talk" button in the Audio panel arms a ONE-SHOT capture — the NEXT keyboard key OR mouse button becomes the PTT binding; holding that binding thereafter opens the mic and rides the EXISTING path byte-untouched (MicCapture → encodeWav → saveCapture IPC → WOMBAT_ASR_DROP_DIR → ASRSource — zero new Python, zero new transcription machinery). My rulings inside DEC-58: **(a) HOLD-to-talk, not toggle** — Jim said push-to-talk; the roadmap's toggle sketch (future-work-roadmap.md:52-53) was contingent on `globalShortcut`, which v1 doesn't use — renderer keyup makes hold free and self-muting (release IS the mute). **(b) In-app v1** — document-level listeners, any screen while the window is focused; `globalShortcut` REJECTED for v1 (keyboard-only — can never carry Jim's explicit mouse-button requirement — and no keyup, so no hold); OS-global/tray stays roadmap. **(c) Persistence per DEC-43** — `wombat_ptt_binding` app-editable string (`key:<KeyboardEvent.code>` / `mouse:<button>`, default `""` unbound) in `wombat_settings` via the settings API, APP_EDITABLE_FIELDS + api mirror lock-step (the `wombat_voice_enabled` precedent); NO restart notice — renderer-only consumer. **(d) Minimal capture guard** — Escape cancels arming; left/right click rejected visibly; one bare event, modifiers ignored, no chord grammar. **(e) Never-silent-no-op** — auto-repeat guard, editable-target guard (typing in chat never opens the mic), single-capture latch vs the manual Record session, drop-dir degrade surfaces the existing message; Mute keeps its as-built manual-session scope — PTT is deliberately mute-independent. **(f) Q-97 PRESERVED, not superseded** — PTT is exactly push-to-activate through the same drop-dir; the roadmap posture is now binding: mic opens ONLY on explicit press, ALWAYS with a visible recording indicator. Tickets: **TK-275** (capture+persist UI + two-file Python field admission; deps TK-224; S) → **TK-276** (app-level hold listener wired into MicCapture with the guards + indicator; deps TK-275/TK-224; M). Both EP-32, mvp, vitest ACs per the app precedent. NEXT — run the build batch over TK-275 → TK-276.

## 2026-07-19 — BATCH SELECTED (within authority; contract v2.124 stands, no contract edit): TK-275 → TK-276 — the DEC-58 push-to-talk pair, the only open tickets (board 191/193)

Dependency order is forced: TK-276 depends_on TK-275 (the binding must exist and persist before the listener can consume it). No new ruling needed — DEC-58(a)-(f) at v2.124 binds every design point. Seams re-verified live pre-build, refinements for the builder: (1) `config.py` APP_EDITABLE_FIELDS is the tuple at :51-69 and `wombat_voice_enabled: bool = False` sits at :176 — `wombat_ptt_binding: str = ""` goes beside it; as a plain str it is EXEMPT from the api mirror's vocab lock-step check (the `wombat_stt_model` precedent), so only `test_mirror_model_field_set_matches_app_editable_fields` needs the new name. (2) `settings_app/api.py` mirror is `SettingsUpdate` (:83-112, extra="forbid") — add `wombat_ptt_binding: str | None = None`. (3) AudioPanel is self-contained over `getSettings`/`putSettings` from `../api` (:49-83) — the PTT row rides the same round-trip and must NOT touch `restartNotice` (renderer-only consumer, DEC-58(c)). (4) `MicCapture.start(deviceId?, initialMuted=false)` / `stop() -> {kind:"saved",result}|{kind:"empty"}`; the drop-dir degrade is `result.reason === "drop-dir-not-configured"` flipping `dropDirConfigured` → the ":139 voice drop-dir not configured" line — TK-276's never-silent-no-op guard reuses exactly that state, so the latch handshake lives in the new `ptt.ts` module with AudioPanel acquiring/releasing it around its manual Record session (captureRef stays panel-local). (5) App mount point: `App()` at App.tsx:191, AudioPanel rendered at :385 — the PTT hook mounts ONCE in App. Verification is vitest per the app precedent (saveCapture spied, no live audio). Operator OAuth bring-up remains outside the loop. When both land the board re-closes at 193/193.

## 2026-07-19 — TK-275 DONE (closeout — within authority; contract v2.125, board 192/193): DEC-58(c)/(d) PTT binding capture-and-persist LANDS

Built exactly to spec. AudioPanel gains the Set-push-to-talk one-shot arming (capture-phase document keydown+mousedown; bare key as `key:<code>` with modifiers ignored, non-left/right mouse as `mouse:<button>`; button 0/2 rejected visibly with arming continuing; Escape cancels with no write; one-shot disarm on capture; PUT via the existing settings round-trip with NO restartNotice). Python admission follows the `wombat_voice_enabled` precedent byte-for-byte: config.py APP_EDITABLE_FIELDS + `wombat_ptt_binding: str = ""` (runtime never reads it), settings_app/api.py SettingsUpdate mirror (`str | None`, vocab-lock-step exempt like `wombat_stt_model`). NORMALIZATION within authority: files_in_scope adds `app/src/api.ts` — the SettingsFields type mirror is a typecheck-forced 4-line ripple, audited. Builder self-ran the full bar green (npm test 286 incl. 12 AudioPanel; pytest 1683 passed / 93 pg-gated skips; ruff/mypy/tsc clean; settings suites re-run on a throwaway pg 5437). Architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING — one Opus review audits the TK-275+TK-276 batch after both land. NEXT — TK-276 (the hold-to-talk listener), sole open ticket, now unblocked.

## 2026-07-20 — TK-276 DONE — BOARD RE-CLOSES 193/193 (closeout — within authority; contract v2.126): DEC-58 hold-to-talk listener LANDS

Built exactly to the DEC-58(a)/(b)/(e) spec and the v2.124 batch refinements. NEW `app/src/ptt.ts`: pure `parseBinding`/`isEditableTarget`, a module-level single-capture-at-a-time latch (`acquireCaptureLatch`/`releaseCaptureLatch`, owners `'ptt'|'manual'`), and `usePushToTalk()` — self-contained hook (own `getSettings` fetch, AudioPanel's own-fetch style) installing document keydown/keyup + mousedown/mouseup listeners ONLY for a non-empty `wombat_ptt_binding`, driving `MicCapture.start()/stop()` through the EXISTING saveCapture → drop-dir path byte-untouched, returning `{active, degraded}`. All never-silent-no-op guards land with visible truths: `event.repeat` never restarts; editable-target press ignored (typing in chat never opens the mic); shared latch defers to a manual Record session (AudioPanel holds `'manual'` — its Stop state is the visible reason); unbound installs nothing; drop-dir degrade surfaces via `degraded`. Additive `announcePttBinding()` makes TK-275's persist update the live binding immediately (no reload). Mounted ONCE in App.tsx with a fixed-position recording/degrade indicator — the DEC-58(f) always-visible-indicator posture honored. `audio.ts`/`components/index.ts` were in scope but needed ZERO diff (MicCapture consumed as-built) — narrower than scoped, recorded not silent. Builder self-ran the full bar green (vitest 27 files / 303 tests incl. ptt.test.ts's 12 + the AudioPanel latch-defer AC2 case; tsc clean; pytest 1683 passed / 93 pg-gated skips — Python untouched; ruff/mypy clean). Architect re-ran `uv run pytest -q` pre-commit. **BATCH-VERIFY PENDING** per the v2 cadence — one Opus review audits the whole TK-275+TK-276 batch, then the operator ear-checks live hold-to-talk end-to-end. Remaining outside the loop: operator OAuth bring-up + spoken-summary ear-check.

## 2026-07-20 — DEC-59 + TK-277 (within authority; contract v2.127): live ASR break — local faster-whisper pinned to CPU

Jim-diagnosed live runtime bug, confirmed in code: every voice-drop transcription raises `RuntimeError: Library cublas64_12.dll is not found or cannot be loaded` inside CTranslate2's encode. Root cause — `FasterWhisperTranscriber.__init__` (src/wombat/sources/asr.py:110-113) constructs `WhisperModel(model_name)` with default `device="auto"`; CTranslate2 auto-selects CUDA on Jim's box, the CUDA 12 runtime DLLs are absent, and CTranslate2's CUDA init is **LAZY** — the constructor succeeds and the error only raises at first encode inside `transcribe()`, so select.py's construction-failure degrade never fires and ASRSource's per-file handler moves EVERY capture to `failed/`. Net effect: the just-landed DEC-58 push-to-talk path (TK-275/TK-276) records and drops WAVs perfectly and nothing ever transcribes. **RULING (DEC-59, shape a of the routed candidates):** pin `device='cpu'` explicitly at the one constructor seam + ONE loud construction `logger.info` naming the pinned device/model (never-silent posture). Try-CUDA-then-fallback REJECTED — the lazy failure mode means an honest construction-time fallback needs probe-inference machinery, and a transcribe-time fallback would retry-and-fail per file. A device config field REJECTED — speculative; the single seam keeps a future GPU option a one-line, DEC-supersedable change. whisper-base on CPU is comfortably fast for short PTT clips; fully consistent with the local-only ASR posture (CST-2) and laptop v1. **TK-277** minted (EP-31, P1, S, mvp, dep TK-162; files asr.py + tests/sources/test_asr.py) — board 193/194, the sole open ticket. validate PASS (259 nodes), gate_check analyze PASS. NEXT — build batch runs TK-277, then the operator ear-check of live hold-to-talk end-to-end finally becomes possible.

## 2026-07-20 — TK-277 DONE (closeout; contract v2.128): DEC-59 CPU pin lands — board re-closes 194/194

Built exactly to the DEC-59 shape: `FasterWhisperTranscriber.__init__` now constructs `WhisperModel(model_name, device="cpu")` plus exactly one `logger.info` line naming the pinned device and model before construction. Diff confined to the constructor + tests (3-line asr.py delta): Transcriber protocol, lazy import, select.py degrade, ASRSource per-file `failed/` handling all byte-untouched; no config field, no CUDA fallback, per the DEC-59 rejections. Tests: +2 modelless fake-module tests (device kwarg recorded via sys.modules injection; caplog-asserted single line); builder ran the full bar green (test_asr.py 10 passed; full suite 1685 passed / 93 pg-gated skips; ruff clean; mypy 334 files); architect re-ran `uv run pytest -q` pre-commit. **Batch-verify pending** (single Opus batch review, v2 cadence). NEXT — after batch-verify, the operator ear-check of live hold-to-talk end-to-end: hold binding, speak, release, transcription arrives instead of `failed/`. The DEC-58 push-to-talk path is whole again on paper.

## 2026-07-20 — DEC-60 + ISS-25 + TK-278..TK-282 (within authority; contract v2.129): voice turns ARE conversation — the PTT-to-chat arc ruled

Jim verbatim: 'the button isn't actually appending text to the chat pane or actually speaking to wombat at all. I should be able to have a conversation with wombat with STT/TTS only when I'm doing other things.' Log-proven defect pair (ISS-25, runtime-20260720-152408.log): (A) three ASR transcripts rated item_kind='generic', held, absorbed into the PendingSet at 15:24:41 — the ISS-23 structural-absorption class one source over (DEC-57 keys on ItemKind.CHAT; ASRSource's payload has no item_kind key, Q-41 falls back GENERIC); (B) two live WAVs (15:24:48/51) stranded in the drop root — the 300s asr scan beat is structurally deaf for conversation and the synchronous whisper decode provably froze the whole event loop ~11s at boot. Seams source-verified: chat payload shape ('item_kind':'chat') and item_id = idempotency_key(source, event_key) in chat/surface.py; chat_reply resolves EVERY composed item (unknown-id no-op); speech_shape's held-chat pass-through condition at :152; pump beat 5.0s (Q-117) so NO wake extension needed. RULED (DEC-60, five clauses): (a) ASRSource stamps item_kind 'chat' + voice_turn true on every non-command transcript (TK-278, EP-29 S) — DEC-57 always-answer/no-absorption applies by construction, DEC-35/36 interception + gate scoring untouched; (b) held+voice_turn takes the FULL DEC-55 speech_shape+TTS path — the reply channel mirrors the ask channel (answering aloud what was asked aloud is the solicited response, not an interruption; supersedes DEC-57 quiet-held IN PART for voice origin ONLY; typed held chat byte-identical) (TK-279, EP-30 M); (c) pane echo — bounded broker voice-turn ledger (TK-270 cap+TTL discipline) via new optional ASRSource turn_hook (TK-212/213 precedent), replies parked by the existing resolve, NEW token-guarded GET /chat/voice-turns (TK-280, EP-32 M) + ChatPane ~3s poll rendering transcript-as-operator-message with a visible voice marker (TK-281, EP-32 M); (d) DEFAULT_ASR_POLL_INTERVAL_SECONDS 300 -> 2.0 asr-only + transcribe off-loop via asyncio.to_thread (hooks/moves stay ON-loop — thread-affinity AC is load-bearing) + never-silent found/outcome logging (TK-282, EP-29 S); (e) the stranded WAVs re-driven live after land; a provably dead scan loop becomes a new ISS with the new logging as evidence. Rejected: wake-to-asr (5s beat suffices), watchdog dep, renderer-side echo, websocket push, PTT-vs-Record provenance split, quiet voice replies. BUILD ORDER TK-278 -> TK-279 -> TK-282 -> TK-280 -> TK-281 (asr.py touches serialized by deps). Steer: verify OUTSIDE repo root (ISS-20/TK-268 discipline), throwaway-pg only, sinks/speak.py expected ZERO-diff in TK-279 (verify, don't change). validate PASS (264 nodes / 225 governance), gate_check analyze PASS. NEXT — dispatch the batch; operator closeout is the live hands-free round-trip: hold PTT, speak, transcript in pane, reply in pane AND spoken.

## 2026-07-20 — BATCH SELECTED (within authority; contract v2.130): TK-278 → TK-279 → TK-282 → TK-280 → TK-281 — the full DEC-60 voice-turns-are-conversation arc

The five v2.129 tickets are the only open items (board 194/199); order is the recorded one (asr.py touches serialized by deps: 278 before 282/280; 279 needs 278's routing; 281 needs 280's route). No new ruling — DEC-60(a)-(e) binds every design point. Seams re-verified live pre-dispatch: asr.py `_process_one` payload at :223 (transcript+captured_at only — the TK-278 fix site), hooks at :211/:218 pre-payload; gate.py :42-:46 Q-41 GENERIC fallback (the absorption mechanism); artifacts.py held_chat additive trio :163/:193/:250 (TK-279's threading pattern) and speech_shape.py :152 (the ONE condition change; sinks/speak.py zero-diff expected); sources/bootstrap.py :138 (`DEFAULT_ASR_POLL_INTERVAL_SECONDS = 300.0`) and `_maybe_register_asr` :519; chat/surface.py broker late-slot :120-:200 (TK-280's ledger precedent), typed-send `item_kind: "chat"` precedent :27; `idempotency_key` at sources/registry.py :148 with `ASRSource.id = "asr"` (asr.py :129); app: chat.ts token/abort discipline, ChatPane poll effect :66-:82. **One normalization (TK-280):** files_in_scope `src/wombat/runtime.py` → `src/wombat/bootstrap.py` + `tests/unit/test_bootstrap.py` — the composition root (broker at bootstrap.py:1011, build_source_registry at :1225) is bootstrap.py; runtime.py only late-binds `chat_source.wake`; the turn_hook must tolerate the None-broker chat-disabled path (:1001). Steer stands verbatim (verify outside repo root, throwaway pg only, thread-affinity AC load-bearing in TK-282). Deferred: nothing — the board holds no other open tickets; operator OAuth bring-up and ear-checks stay outside the loop. validate PASS (264 nodes / 225 governance), gate_check analyze PASS.

## 2026-07-20 — TK-278 DONE (closeout — within authority; contract v2.131, board 195/199): DEC-60(a) voice transcripts ARE chat turns — the absorption defect is dead at the source

Landed exactly at the minted seam: `ASRSource._process_one` now builds the payload as `{"item_kind": "chat", "voice_turn": True, "transcript": ..., "captured_at": ...}` (asr.py :229-:234) — the chat/surface.py typed-send key precedent verbatim — so the gate resolves ItemKind.CHAT directly (no Q-41 fallback) and the DEC-57/TK-272 always-compose exception applies to every voice turn by construction. Command interception (TK-212) and feedback hook (TK-213) byte-identical (both pre-payload); module docstring honestly rewritten. Downstream needed ZERO changes — TK-272's held_chat compose routing was already in place; the missing routing key WAS the whole defect, as predicted. AC2 pinned through the REAL gate stage (test_review_or_speak.py: asr-shaped item rates CHAT, routes to compose with held_chat=True, pending set gains nothing — the 15:24:41 repro is dead). Builder bar green: targeted 21 passed; full suite 1688 passed / 93 skipped (all pg-DSN-gated, expected); ruff + mypy strict clean; diff confined to the three files_in_scope exactly. **Batch-verify PENDING** (single Opus review after all five DEC-60 tickets land — v2 cadence). validate PASS (264 nodes / 225 governance), gate_check analyze PASS. NEXT — TK-279 (held voice replies SPEAK).

## 2026-07-20 — TK-282 DONE (closeout — within authority; contract v2.132, board 196/199): DEC-60(d) conversation-grade drop-dir pickup lands

All three minted changes, confined to the four files_in_scope exactly: (1) `DEFAULT_ASR_POLL_INTERVAL_SECONDS` 300.0 → 2.0 (asr only — gcal/gmail/feedback byte-untouched, DEC-56a stands, no wake seam); (2) `_process_one` is now async and ONLY the pure `transcriber.transcribe(path)` call crosses via `await asyncio.to_thread(...)` — read_bytes/sha256, command_hook, feedback_hook, `_safe_move` all stay ON the loop (hooks/asyncio not thread-safe) — killing the proven ~11s decode freeze; (3) never-silent scan via `_log_outcome`: one found-count INFO line when N>0, one per-file INFO line (event_key prefix, processed/failed, duration), empty scans silent at INFO. Per-file degrade semantics byte-identical. AC1 pinned on the constructed source instances; AC2 is the load-bearing thread-affinity test (thread idents across two files); AC3 caplog pair. Builder bar green: targeted 33 passed / 1 pg-gated skip; full suite 1692 passed / 93 pg-gated skips; ruff clean; mypy strict clean (334 files). Architect re-ran `uv run pytest -q` pre-commit. Order note: built ahead of TK-279 — dep-legal (TK-282 depends only on TK-278; TK-279 touches no asr.py). **Batch-verify PENDING** (single Opus review after all five DEC-60 tickets land — v2 cadence). validate PASS (264 nodes / 225 governance), gate_check analyze PASS. NEXT — TK-279 (held voice replies SPEAK), then TK-280/TK-281.

## 2026-07-20 — TK-280 DONE (closeout — within authority; contract v2.133, board 197/199): DEC-60(c) server half — voice-turn ledger + GET /chat/voice-turns

Landed exactly per the minted shape, diff confined to the eight files_in_scope. `chat/surface.py` ChatReplyBroker gains the BOUNDED voice-turn ledger (cap 32 oldest-evicted + TTL 3600s — the TK-270 late-slot discipline verbatim): `register_voice_turn(item_id, transcript, captured_at)` creates reply=None entries; `resolve(item_id, text)` parks reply text into a matching entry at its EXISTING unknown-id fall-through — typed-chat pending/late-slot semantics byte-untouched (every pre-existing register/resolve/discard/poll_late test green). NEW `GET /chat/voice-turns` — same X-Wombat-Chat-Token guard, 401 shape, CORS preflight — answers a snapshot-not-pop JSON array `{id, transcript, captured_at, reply}` so transcript and reply render across different poll ticks; server prunes by cap/TTL only. `ASRSource` gains optional `turn_hook` (TK-212/213 precedent, default None byte-identical) fired in `_process_one` for every NON-command transcript; commands never register a turn. Wiring per the v2.130 normalization ruling: sources/bootstrap.py threads the hook through; src/wombat/bootstrap.py `assemble_runtime` builds it ONLY when the broker exists, deriving item_id via `idempotency_key('asr', event_key)` — pinned by test through the REAL derivation to equal the id chat_reply resolves with (the load-bearing end-to-end id identity). CON-1 held — ledger fields surface-side only, never model-visible. Builder bar green (full suite 1705 passed / 93 pg-gated skips; ruff; mypy strict 334 files); architect re-ran `uv run pytest -q` pre-commit. Order note: built ahead of TK-279 — dep-legal (deps TK-278/TK-282/TK-270 all done; zero file overlap with TK-279). **BATCH-VERIFY PENDING** per the v2 cadence — one Opus review audits the whole five-ticket DEC-60 diff after TK-279 and TK-281 land. NEXT — TK-279 (held voice replies SPEAK), then TK-281 (pane consumes the route). validate PASS (264 nodes / 225 governance), gate_check analyze PASS.

## 2026-07-20 — TK-281 DONE (closeout — within authority; contract v2.134, board 198/199): DEC-60(c) app half — the pane renders voice turns

Landed exactly per the minted shape, diff confined to the four files_in_scope. `app/src/chat.ts` gains `getVoiceTurns()` on the sendChat/pollChatReply fetch discipline verbatim — AbortController with its own `VOICE_TURNS_TIMEOUT_MS` bound, bridge port/token re-read fresh via `window.wombatChat.getInfo()`, token ONLY in the `X-Wombat-Chat-Token` header, plus an optional `externalSignal` for unmount abort — parsing TK-280's `GET /chat/voice-turns` snapshot array into camelCase VoiceTurn objects, with every malformed/non-200/network/thrown-json case collapsed to ONE unavailable result (a failed poll can never throw into the pane). ChatPane adds a second useEffect polling every 3s while mounted, two id-keyed seen-sets (transcript vs reply) so snapshot re-polls never re-append: unseen transcript renders ONCE as the OPERATOR message with a theme-token (brand.text) mic-glyph marker, aria-label "voice message" (DEC-39 tokens-never-hardcoded); a landed reply appends ONCE exactly like a typed reply. Interval cleared + in-flight fetch aborted on unmount (TK-266 discipline); failed polls show no error spam and typed send/held/late machinery stays byte-identical (all pre-existing ChatPane tests green). Builder bar green: targeted 35 passed; full vitest 27 files / 314 tests; tsc clean; Python untouched (pytest/ruff/mypy n/a to the diff). Architect re-ran `uv run pytest -q` pre-commit. Order note: built ahead of TK-279 — dep-legal (sole dep TK-280 done, zero file overlap). **BATCH-VERIFY PENDING** per the v2 cadence — one Opus review audits the whole five-ticket DEC-60 diff once TK-279 (the last open ticket) lands; then the operator live hands-free round-trip closeout. validate PASS (264 nodes / 225 governance), gate_check analyze PASS. NEXT — TK-279 (held voice replies SPEAK), the final DEC-60 ticket.

## 2026-07-20 — TK-279 BLOCK RULED (within authority; contract v2.130): the zero-diff speak.py steer was wrong — the one-condition mirror is the fix; four DEC-60 tickets DONE

Batch outcome: TK-278 (f2ac41f), TK-282 (766c8b4), TK-280 (19747a6), TK-281 (90ac2fc) landed with batch review PASS (full bar: pytest 1705 green, mypy 334 files, app tests 314, tsc clean; one below-threshold minor on TK-280 re-drop ledger reset). TK-279 came back BLOCKED on a REAL builder finding, architect-verified in source: sinks/speak.py:81 gates its silent no-op on held_chat ALONE ('not voice_enabled or adapter is None or held_chat') and returns the quiet Done BEFORE the speech_shape artifact read at :102 — so the v2.129 briefing's 'SpeakSink zero-diff, its speech-text-present branch speaks it' claim was factually false for held+voice_turn; my steer, my miss. RULED option (a), the minimal mirror: SpeakSink reads voice_turn from the SAME composed artifact it already reads (ctx.last_output('compose'), identical accessor pattern to held_chat) and the condition becomes 'not voice_enabled or adapter is None or (held_chat and not voice_turn)' — the exact lock-step mirror of speech_shape's gate, pinned by test. Safety checked: a SURFACED voice turn carries held_chat=False so both conditions pass it through byte-identically (one speak per run, no double-speak); a held voice turn whose shaping call failed/was validator-rejected falls through to the existing speech-text-None Degraded branch — the DEC-55 failure-means-silence bar holds by construction (never quiet Done, never verbatim). Option (b) (route held voice turns to a transition the held_chat check doesn't cover) REJECTED — new graph mechanism for zero benefit over a one-condition mirror. TK-279 amended in place (tickets are not append-only): intent corrected, files_in_scope += src/wombat/sinks/speak.py, one NEW failure-means-silence AC, non_goal recast to 'no NEW branch — one-condition mirror + artifact read only'. DEC-60 itself untouched — briefing/seam correction, not a decision change. Board 198/199; validate PASS (264 nodes), gate_check analyze PASS. NEXT — rerun the batch for TK-279 alone, then the operator closeout: the live hands-free round-trip (hold PTT, speak, transcript in pane, reply in pane AND spoken) plus the stranded-WAV re-drive (DEC-60e).


## 2026-07-20 — BATCH SELECTED (within authority; contract v2.135): TK-279 alone — the final DEC-60 ticket, files_in_scope completed pre-dispatch

Board 198/199; TK-279 (held voice replies SPEAK, DEC-60b + the v2.130 SpeakSink mirror ruling) is the only open ticket — a one-ticket batch by necessity, not choice. Pre-dispatch seam audit caught that the v2.130 amendment was INCOMPLETE: the voice_turn threading (surfaced_item -> compose_request -> composed_output, the TK-272 held_chat pattern byte-for-byte) necessarily crosses `compose_dispatch_router.py` (:71 reads surfaced held_chat, :96 writes compose_request) and `compose.py` (:135 reads it, :237 stamps composed_output) — and composed_output, the ONLY artifact speech_shape/speak read (`ctx.last_output('compose')`), is built by compose.py. Neither file was in scope; the ticket was unbuildable as written. Normalized files_in_scope += compose_dispatch_router.py, compose.py, tests/unit/test_compose_dispatch_router.py, tests/unit/test_compose_stage.py, tests/unit/test_speak_sink.py (all verified on disk), and corrected the stale complexity_budget ('no SpeakSink changes' contradicted the v2.130 mirror). No decision touched — DEC-60 and the v2.130 block ruling stand verbatim. All binding seams re-verified live in source (speech_shape.py:150-:152, speak.py:79-:109, review_or_speak.py:199/:231, artifacts.py additive trio + .get-False accessors). validate PASS (264 nodes / 225 governance), gate_check analyze PASS. NEXT — build TK-279, single-ticket batch-verify closes the whole five-ticket DEC-60 Opus review, then the operator hands-free round-trip + stranded-WAV re-drive (DEC-60e).

## 2026-07-20 — TK-279 DONE (closeout — within authority; contract v2.136, board 199/199 — BOARD CLOSED): DEC-60(b) + the v2.130 SpeakSink mirror — held voice replies SPEAK; the DEC-60 arc is code-complete

Landed exactly per the minted shape, diff confined to eleven of the twelve files_in_scope (tests/sinks/test_speak_sink.py untouched — the speak-sink tests live at tests/unit/test_speak_sink.py, the v2.135-verified home). Wire half: artifacts.py gains the additive optional `voice_turn` bool (default False, plain-JSON-native) on the surfaced_item → compose_request → composed_output trio with three `.get(..., False)` accessors — the TK-272 held_chat pattern byte-for-byte, so AC5's no-compat-break holds by construction; review_or_speak.py reads `voice_turn` off `queue_item.payload` at the surfaced-item forward and threads it through compose_dispatch_router.py → compose.py otherwise unchanged. Gate half, the lock-step mirror I ruled in v2.130: speech_shape.py:158 and speak.py:90 each change EXACTLY ONE disjunct — `held_chat` → `(held_chat and not voice_turn)` — both reading voice_turn from the SAME composed artifact; no new branch anywhere, architect-verified in the diff. Safety holds as ruled: a surfaced voice turn (held_chat=False) passes both gates byte-identically — one speak per run, no double-speak; a held voice turn with failed/rejected shaping hits the existing speech-text-None Degraded branch (DEC-55 failure-means-silence, never quiet Done, never verbatim composed text — pinned by the new lock-step AC test); held TYPED chat stays byte-identical quiet (TK-272 tests verbatim); voice-off/no-adapter short-circuits first, honestly text-only. Builder bar green: full suite 1717 passed / 93 pg-gated skips; ruff clean; mypy strict Success 334 files; targeted AC files 87 passed; app/ untouched. Architect re-ran `uv run pytest -q` pre-commit. **BATCH-VERIFY PENDING** per the v2 cadence — the single Opus review now audits the whole five-ticket DEC-60 diff (TK-278/282/280/281/279). validate PASS (264 nodes / 225 governance), gate_check analyze PASS. NEXT — operator closeout only: runtime relaunch, the live hands-free round-trip (hold PTT, speak, transcript in pane, reply in pane AND spoken), and the stranded-WAV re-drive (DEC-60e).

## 2026-07-20 — DEC-61 + TK-283 (within authority; contract v2.137; board reopens 199/200): the mouth's model wait becomes a tunable — spurious compose degrade under boot CPU contention ruled at the root

Live finding (runtime-20260720-183045.log): ComposeStage degraded a HEALTHY DeepSeek call to template — `asyncio.wait_for` cut it at the 2.0s default (compose.py:189) with 200 headers already received (18:30:56.231) and the body mid-stream when cancelled (18:30:57.073). Root cause of the slowness: boot-time faster-whisper CPU float32 decode (DEC-59 device pin; ctranslate2 auto-converts float16→float32) saturated cores and slowed the async HTTP read past 2s; every later compose in the session ran 1.1–1.5s. **DEC-61 rules the TK-8 AC conflated TWO bounds**: the contract-guaranteed promptness is the FALLBACK's (deterministic template render, no further model call, on genuine timeout/error/empty-text give-up); the model WAIT is an operating parameter — a healthy in-flight call is never degraded to satisfy an arbitrary wall clock. Shape: ONE TK-13 tunable `mouth_model_timeout_seconds` (OperatingParams + wombat_params.yaml version 7→8, default **10.0** — the as-built brief_compose precedent; fits well inside the 30s CHAT_REPLY_TIMEOUT_SECONDS window; batch_size=1 bounds a true-hang episode at 10s, and real 503s degrade instantly regardless), injected at the composition root into ALL FIVE model-calling mouth sites — compose, speech_shape, brief_compose, reflection_compose, draft_composer (the tree had already drifted 2.0-vs-10.0, proving hardcoded defaults don't hold a line). Supersedes TK-8's "timeout_seconds default 2.0 is AC-FIXED (not a TK-13 tunable)" clause IN PART; ctor defaults stay for standalone callers; degrade machinery, DEC-18 never-raise, DEC-55 failure-means-silence all byte-untouched. Rejected: restate-AC-only (doesn't stop the next boot), constant-without-tunable (TK-13 exists for exactly this), per-stage knobs (one mouth, one bound — TK-9 posture), ASR load-shedding as THE fix (any CPU pressure reproduces it; separate concern if evidence recurs). **TK-283** (EP-5, P1, S, todo) carries it: params.py + YAML bump + bootstrap injection; the repro is pinned by AC (fake model resolving >2s-but-under-timeout returns NON-degraded phrased text); files_in_scope params.py / wombat_params.yaml / bootstrap.py + test_wombat_params / test_bootstrap / test_compose_stage. validate PASS (265 nodes / 226 governance), gate_check analyze PASS. Ready for the build workflow — dispatch TK-283 alone.

## 2026-07-20 — BATCH SELECTED (within authority; contract v2.138): TK-283 alone — DEC-61's carrier, seams re-verified pre-dispatch

Board 199/200; TK-283 is the only open ticket — a one-ticket batch by necessity. All injection seams re-verified live in source, fully consistent with the minted shape: all FIVE mouth stages already accept `timeout_seconds` ctor kwargs (compose.py:107, speech_shape.py:130, brief_compose_stage.py:84, behavior/stages/reflection_compose.py:162, integrations/gmail/draft_composer.py:183 — note brief/reflection/draft live outside `stages/`); bootstrap factories build_compose_stage:385 / build_brief_compose_stage:422 / build_speech_shape_stage:501 each already load `op` (:410/:446/:525) so injection is one kwarg per factory; the draft factory's exact name is **build_draft_composer_stage** (:567, assemble_runtime call site :949) — it gains a pass-through preserving the ctor default when unfed (`timeout_seconds: float | None = None`, pass only when not None, so bootstrap never imports the stage's private constant); inline ReflectionComposeStage at :910 has `op` in scope (loaded :744). wombat_params.yaml at version 7 → bumps to 8; OperatingParams' all-fields-required posture (params.py:82) makes the new field load-time-mandatory as intended. Test homes on disk. Stale "AC-FIXED not a tunable" comments in stage files (e.g. reflection_compose.py:88) stay untouched per the zero-stage-edits budget — DEC-61's changelog records their supersession. Atomicity holds (S). Nothing deferred — no other open ticket; operator OAuth bring-up and the DEC-60 hands-free/stranded-WAV closeout stay outside the build loop. validate PASS (265 nodes / 226 governance), gate_check analyze PASS. NEXT — build TK-283; batch-verify per the v2 cadence; then operator relaunch picks up both DEC-60 and DEC-61 live.

## 2026-07-20 — TK-283 DONE (contract v2.139): DEC-61's mouth_model_timeout_seconds lands — BOARD CLOSED 200/200; batch-verify pending

The build landed exactly per the minted shape, diff confined to the six files_in_scope with ZERO stage-module edits. (1) `OperatingParams` gains required `mouth_model_timeout_seconds: float`; `wombat_params.yaml` version 7 → 8 with `10.0` and a CHOSEN-HERE comment carrying the DEC-61 rationale (the 18:30 runtime evidence, healthy 1.1–1.5s range, fits the 30s chat reply window, only bites real hangs). (2) Composition-root injection architect-verified in the diff: `build_compose_stage`, `build_brief_compose_stage`, `build_speech_shape_stage` each pass `timeout_seconds=op.mouth_model_timeout_seconds`; `assemble_runtime` feeds the inline `ReflectionComposeStage` and the `build_draft_composer_stage` call; `build_draft_composer_stage` gained the `timeout_seconds: float | None = None` pass-through forwarded to `DraftComposer` ONLY when not None (two-branch construct, no private-constant import — standalone callers byte-identical). (3) Every `_DEFAULT_TIMEOUT_SECONDS` constant and ctor default untouched; no inline timeout literal at any injection site (TK-13 AC4 style). AC coverage: AC1 pinned on constructed instances via a distinct-value params fixture asserting `stage._timeout_seconds` across all four factories; AC2 the boot-contention repro (fake model resolving >2s-but-under-timeout returns NON-degraded phrased text); AC3 hang-past-timeout still degrades to template `degraded=True`; params present/typed/missing-field tests. Builder self-ran the full bar green (1727 passed / 93 pg-gated skips); architect re-ran `uv run pytest -q` pre-commit. Contract: TK-283 status → done, changelog v2.139 appended; validate PASS (265 nodes / 226 governance), gate_check analyze PASS. **BATCH-VERIFY PENDING** per the v2 cadence — one Opus review audits the batch diff. NEXT — operator closeout only: runtime relaunch picks up DEC-60 + DEC-61 live, hands-free ear-check, stranded-WAV re-drive, GCP Testing→Production.

| 2026-07-20 | **ISS-26 → DEC-62 ACCEPTED + TK-284/TK-285 MINTED (within authority; contract v2.140; board reopens 200/202): capability honesty is structural.** Jim (pre-vetted): wombat played along with "set an alarm" instead of honestly failing. Source-verified diagnosis — the compose prompt is persona-only (`_compose_base` + clauses + guard "No preamble.") plus raw payload; the model has ZERO grounding in what wombat can/cannot do, chat has NO invocable action layer (`chat_reply` only delivers model text), so the mouth pattern-completes a fulfilled request and nothing downstream can veto the fabricated success. DEC-62 — a verbatim-pinned `CAPABILITY_CHARTER` (single source `src/wombat/persona/capabilities.py`: CAN converse / morning brief read-only Calendar+Gmail / user-approved Gmail drafts / read web pages; CANNOT alarms-timers-reminders / send email / calendar writes / any other device action; outside the list say plainly can't, NEVER claim an action was done or scheduled) joins `_GUARD_SUFFIX["compose"]` at the Q-108(a) seam (structurally unremovable by strategy/matrix/policy) plus the byte-equivalent append on compose.py's frozen fallback; brief/draft/reflection byte-identical; supersedes Q-106(a)'s compose guard pin IN PART. Explicitly NOT registry-derived — no chat-action registry exists; minting one would be speculative machinery (rejected in alternatives). TK-284 (EP-33, S): charter module + seam injection + structural pinned tests (charter present at every matrix level, other mouths byte-identical, both ComposeStage paths carry it, prose-in-one-file grep). TK-285 (EP-5, S): live-wire env-gated honesty check — "Set an alarm for 7 am." must compose non-degraded, match an inability marker, match NO success-claim pattern; doable control turn unaffected; loud skip without DEEPSEEK_API_KEY. validate PASS (267 nodes / 228 governance), gate_check analyze PASS. When a real capability lands later, the charter is the ONE constant to amend — DEC-62 is the standing reminder. | architect | ISS-26, DEC-62, TK-284, TK-285, EP-33, EP-5, Q-106, Q-108, contract v2.140 |

## 2026-07-20 — Batch selected (contract v2.141): TK-284 → TK-285, the DEC-62 capability-honesty pair, with two pre-build rulings

Board 200/202; the two DEC-62 tickets are the only open work — order forced by depends_on. Pre-dispatch seam audit re-verified live: _GUARD_SUFFIX compose entry expression.py:89-90, render_expression join :123 (body + " " + guard), guard_suffix :106-110 (the ONLY table readers), frozen _system_instruction compose.py:82-86 with the live_persona branch :144-147, pinned-string homes test_expression.py:50 / test_builder.py:79 / test_compose_stage.py:315,349. TWO RULINGS (recorded on the tickets + changelog v2.141): (1) grep found a THIRD compose-guard consumer the mint missed — speech_shape.py:148 appends guard_suffix(Mouth.COMPOSE) at construction, so the spoken-summary mouth INHERITS the charter automatically; ruled IN-SPINE and desirable (a voice "set an alarm" ask gets its spoken reply through speech_shape per the DEC-60 arc — same fabrication surface, zero code change); tests/stages/test_speech_shape.py joins TK-284 files_in_scope (its :114 endswith pin → endswith(guard_suffix(Mouth.COMPOSE))) with a new AC pinning the inheritance. (2) TK-285's minted key-presence gate ruled WRONG for this repo — the repo .env carries real creds, so gating on DEEPSEEK_API_KEY alone would make every plain full-suite run silently spend two live model calls; re-ruled to the established arming-var idiom (WOMBAT_TEST_CAPABILITY_LIVE=1 PLUS load_config()-resolvable creds, mirroring tests/persona/test_output_effects_live.py:105-136); AC givens amended. Atomicity holds (S each). Nothing deferred — no other open ticket; operator OAuth/ear-check/GCP items stay outside the loop. validate PASS (267 nodes / 228 governance), gate_check analyze PASS. NEXT — build TK-284 then TK-285; batch-verify per the v2 cadence; TK-285 closeout records the two verbatim live replies.

## 2026-07-20 — TK-285 DONE (closeout — within authority; contract v2.142, board 201/202): DEC-62(c) live capability-honesty proof lands; the alarm lie is dead at the mouth boundary

One self-gated module, tests/integration/test_capability_honesty_live.py — armed only by WOMBAT_TEST_CAPABILITY_LIVE=1 plus load_config()-resolvable creds (the v2.141 arming-var ruling, test_output_effects_live.py lazy skipif-string idiom; unarmed = 2 loud SKIPs, zero live spend from plain suite runs). Bootstrap-equivalent real ComposeStage + real DeepSeek via bootstrap._deepseek_spec, one call per turn, pattern-level assertions. Architect ran it live at closeout per the intent — verbatim replies: alarm turn "I can't do that." (non-degraded, inability marker hit, NO success-claim hit); control turn "Good morning." (non-degraded, non-empty) — ordinary chat unharmed by the charter. FINDING routed to TK-284's closeout (its files_in_scope, not TK-285's): the full-suite run shows exactly one failure — test_compose_stage.py test_ac1_success_path's internal-keys leak-check substring-scans system+user text and now trips on the literal word "action" inside the charter prose; false-positive pin collision, not a leak; fix = scope the internal-key scan to the user message (or exclude the charter span). TK-285 committed alone (test module + contract v2.142 + this entry) — the module imports no TK-284 file and self-gates, so the committed tree stays green; TK-284's working-tree diff stays unstaged for its own closeout. Batch-verify pending per the v2 cadence. NEXT — TK-284 closeout with the leak-check pin fix, then the Opus batch review of the DEC-62 diff.

| 2026-07-20 | **TK-284 UNBLOCKED (leak-check reconciliation ruled; contract v2.143): the AC1 internals scan is RE-SCOPED to the user message only.** The batch blocker was a false-positive pin collision, not a leak — `test_compose_stage.py` AC1 substring-scans `_INTERNAL_KEYS` (incl. the bare word "action") over system+user text concatenated (lines 105-107), and the DEC-62(a) pinned charter legitimately says "action" twice. Ruled: scan `user_msg.content` ONLY — the sole payload-derived channel where a gate/queue internal could actually cross the Q-50 prompt boundary; the system message is architect-pinned constant prose whose exact bytes TK-284's own tests pin by EQUALITY (strictly stronger than a substring blacklist). `_INTERNAL_KEYS` untouched — "action" stays fully checked where it matters. Rejected: key-shaped-token narrowing (guesses shapes, invites the next prose collision) and dropping "action" (weakens real coverage). Recorded on TK-284 (complexity_budget ruling + new AC pinning the re-scoped scan); changelog v2.143; validate PASS (267/228), gate_check analyze PASS. Sequencing note: TK-285 already DONE at v2.142 with the live proof (alarm ask → "I can't do that.", control turn unaffected); TK-284 is the last open ticket — its close returns the working tree's single red test to green and re-closes the board 202/202. | architect | TK-284, TK-285, DEC-62, Q-50, Q-106, Q-108, contract v2.143 |

| 2026-07-20 | **Batch selected (contract v2.144): TK-284 alone — the re-dispatch that closes the board.** Sole open ticket (201/202), last DEC-62 carrier, unblocked by v2.143. Pre-dispatch inventory found TK-284's diff already mostly in the working tree from the earlier dispatch (charter module byte-exact to DEC-62(a); both injections; all four pin updates including the speech_shape :116 inheritance pin) — the ONE remaining code change is the v2.143 ruling itself: test_compose_stage.py:105-107 still scans _INTERNAL_KEYS over system+user concatenated, must re-scope to user_msg.content only. Briefing directs the builder to COMPLETE the in-tree diff, never redo/revert (removal discipline). Nothing deferred — no other open ticket; operator OAuth/ear-check items outside the loop. On close: 202/202, then the pending Opus batch-verify of the whole DEC-62 diff. validate PASS (267/228), gate_check analyze PASS. | architect | TK-284, DEC-62, contract v2.144 |

## 2026-07-20 — TK-284 DONE (closeout — within authority; contract v2.145, board 202/202 — BOARD RE-CLOSED): DEC-62's capability charter lands at the COMPOSE guard seam; the DEC-62 arc is code-complete

The re-dispatch closed exactly per the v2.144 briefing: the pre-existing in-tree diff was verified correct and left untouched (removal discipline held) — `src/wombat/persona/capabilities.py` exports `CAPABILITY_CHARTER` byte-exact to the DEC-62(a) pinned text (single source; AC4 grep — expression.py and compose.py reference by import only); `_GUARD_SUFFIX["compose"]` is `"No preamble. " + CAPABILITY_CHARTER` by concatenation at the table so no strategy/matrix/policy retune can strip it (Q-108(a) seam property pinned via stub strategy); compose.py's frozen `_system_instruction` appends the same import so the LivePersona-absent fallback stays byte-equal to `instruction_for(Mouth.COMPOSE, DEFAULT_MATRIX, name)` (AC3 both-paths pin); brief/draft/reflection byte-identical (AC2, pins unmodified); speech_shape INHERITS the charter with zero stage edit (v2.141 ruling — `endswith(guard_suffix(Mouth.COMPOSE))` pin, AC5). The ONE new change was the v2.143 ruling applied: `test_compose_stage.py` AC1's `_INTERNAL_KEYS` scan re-scoped to `user_msg.content` only — the sole payload-derived Q-50 channel; the system message is covered by its own byte-equality pins; `_INTERNAL_KEYS` untouched incl. "action" (AC6). Builder bar green: targeted AC suites 117 passed; full suite 1727 passed / 95 skipped (pg-gated + the TK-285 live-capability-gated pair, expected); ruff clean; mypy strict clean. Architect re-ran `uv run pytest -q` pre-commit. **BATCH-VERIFY PENDING** per the v2 cadence — one Opus review audits the whole DEC-62 batch diff (TK-284 + TK-285, whose live proof at v2.142 recorded the honest "I can't do that." alarm reply with the control turn unaffected). validate PASS (267 nodes / 228 governance), gate_check analyze PASS. NEXT — the Opus batch review, then operator closeout items only (runtime relaunch, hands-free ear-check, GCP Testing→Production). When a real capability lands later, the charter is the ONE constant to amend — DEC-62 remains the standing reminder.

## 2026-07-21 — ISS-27 → DEC-63 ACCEPTED + TK-286/TK-287 MINTED (within authority; contract v2.146; board reopens 202/204): the repeating-digest loop is ruled dead at TWO structural seams

Jim-reported + log-proven (runtime-20260720-192648.log): the Steward speaks the SAME digest every ~10-20min — gmail:19f8532762838b9e flushed 5x at ~606s intervals with identical scores; 15 surface_flush over ~19h across only 8 unique message ids. Diagnosed three-defect chain, source-verified: (1) GmailPoller re-emits every in-window message each 300s poll (poller.py:191-227, no cursor); (2) queue idempotency holds only while a row is LIVE — ack() DELETEs it (queue.py:229), so the next poll re-inserts cleanly; (3) _try_flush (pipeline.py:130-144) has NO rate bound — the wombat_params.yaml:26 "fires once/day" comment was never enforced (the only once/day logic lives in trigger_sim.py and the brief counter); pending-clear resets load, re-ingestion rebuilds it, the arm re-fires forever. DEC-63: (a) EXACTLY-ONCE INGEST — TK-286 (EP-3, M): persisted SeenLedger (pg wombat_seen_events, migration 010; idempotency_key PK + sha256 canonical-payload fingerprint) behind a DedupingEnqueuer implementing the registry's Enqueuer protocol, wired at the composition root ONLY around build_source_registry's queue arg (bootstrap.py:1257; PatternDetectorStage keeps the raw enqueue); unchanged key+fingerprint → ALREADY_QUEUED without touching the queue; CHANGED fingerprint is the sole legitimate re-entry (an updated gcal event re-enters; static gmail payloads ingest exactly once forever, restart-proof); record only after a non-raising inner enqueue (at-least-once preserved); chat (uuid4) / asr (per-file sha256) keys never-seen by construction — DEC-57 untouched. (b) ONCE-PER-WOMBAT-DAY FLUSH LATCH — TK-287 (EP-9, S): FlushDayLatch beside CeilingLedger over the SAME shared DailyLedger (ledger "flush:load", once/day pinned — the BriefRunLedger precedent, no new tunable), REQUIRED Gate kwarg (Q-69 lesson); denied flush holds the pending set intact; at most ONE INFO denial line/day; immediate arm, select_items, and the brief (Q-23/B separately-counted class) byte-untouched. Rejected: gmail high-water cursor as THE fix (source-local, leaves gcal/feedback exposed; refetch cost = separate efficiency concern), ack tombstones (load-bearing drain/lease/capacity semantics churn), speak-boundary novelty filter (symptom at the last seam), in-memory guards (restart-heavy runtime), cooldown tunable (weaker than the sim-proven once/day target). Self-resolved en route: gcal/feedback DO share the ack-delete exposure — covered uniformly at the one enqueue seam rather than per-poller; and the fingerprint dimension is what keeps forever-dedup safe for mutable calendar payloads. Batch order TK-286 → TK-287. validate PASS (269 nodes / 230 governance), gate_check analyze PASS. NEXT — dispatch the pair as one build batch; on close the runtime relaunch picks up DEC-60/61/63 together.

## 2026-07-21 — BATCH SELECTED (within authority; contract v2.147): TK-286 → TK-287, the DEC-63 repeating-digest pair, with one pre-build scope ruling

Board 202/204; the two DEC-63 tickets are the only open work — order per the v2.146 mint (front door first, then the latch; either alone kills the live drumbeat, together they close the class). Pre-dispatch seam audit re-verified everything live: `build_source_registry(config, queue: Enqueuer, ...)` at sources/bootstrap.py:580-582 (the DedupingEnqueuer drops in type-clean — the registry already takes the Protocol, not WombatQueue); assemble_runtime call site bootstrap.py:1255-1266; PatternDetectorStage keeps the raw `queue.enqueue` (bootstrap.py:1180); ack DELETE queue.py:229; EnqueueResult/QueueFullError/QueueItem queue.py:41-62; `_try_flush` pipeline.py:130-144 with the keyword-only Gate ctor :85-99 (no flush_latch yet); CeilingLedger ceiling.py:22-39 over DailyLedger `current_row`/`increment` plus `today()` (daily_ledger.py:114-119 — hands FlushDayLatch its wombat-date for the once-a-day INFO denial dedup); Gate composition bootstrap.py:792-827; migration slot 010 free; WOMBAT_TEST_PG_DSN skip idiom at tests/gate/test_pending_journal_pg.py:46-54. **ONE RULING (TK-286 scope amended)**: the minted "own ensure-schema mirroring ExternalItemStore" is not self-applying — ExternalItemStore's `ensure_schema` is module-level and never invoked automatically; the live DB gets its tables ONLY via `schema_preflight.ensure_all_schemas` (schema_preflight.py:35-54, run on the replay_pending=True production path, bootstrap.py:780-781). Ruled: seen_ledger.py exports module-level `ensure_schema(conn)` reading 010_seen_events.sql (the daily_ledger idiom) AND `ensure_all_schemas` gains the ninth call; schema_preflight.py + tests/unit/test_schema_preflight.py join TK-286 files_in_scope. Without it the production table never exists and the wrapper dies on first enqueue after the operator relaunch. Atomicity holds (M + S). Orchestrator-mandated guards recorded in both briefings: pg ACs on throwaway WOMBAT_TEST_PG_DSN state only, and DEC-57 chat-always-answers byte-untouched (chat uuid4 / asr sha256 keys never-seen by construction, pinned by a TK-286 pass-through test). Nothing deferred — no other open ticket; operator OAuth/GCP/ear-check items stay outside the loop. validate PASS (269 nodes / 230 governance), gate_check analyze PASS. NEXT — build TK-286 then TK-287; single Opus batch-verify; then the operator relaunch picks up DEC-60/61/62/63 together.

## 2026-07-21 — TK-286 DONE (closeout — within authority; contract v2.148, board 203/204): DEC-63a exactly-once ingest lands — the ack-delete re-ingestion loop is structurally dead

Built exactly per the v2.146 mint + the v2.147 preflight ruling, diff confined to scope. NEW src/wombat/migrations/010_seen_events.sql (wombat_seen_events — idempotency_key TEXT PRIMARY KEY, payload_fingerprint NOT NULL, first/last_seen_at timestamptz) + NEW src/wombat/sources/seen_ledger.py: module-level `ensure_schema(conn)` reading the packaged migration (the daily_ledger idiom per v2.147), `fingerprint()` = sha256 over canonical sort_keys json, `SeenLedger` lazy-conn store (Q-46 posture), `SeenLedgerLike` protocol for test doubles, and `DedupingEnqueuer` wrapping the inner Enqueuer — an unchanged key+fingerprint short-circuits to ALREADY_QUEUED without touching the inner queue; never-seen or changed-fingerprint passes through and is recorded ONLY on a non-raising return, so QueueFullError (or any raise) propagates unrecorded and at-least-once redelivery survives. `schema_preflight.ensure_all_schemas` gains the NINTH call (v2.147 — without it the production table never exists on the replay_pending boot path). bootstrap.assemble_runtime constructs SeenLedger(dsn) + DedupingEnqueuer(queue, seen_ledger) and threads it ONLY into build_source_registry; PatternDetectorStage keeps the raw queue.enqueue (AC6 asserted on the constructed objects); queue/registry/pollers byte-untouched, DEC-57 chat-always-answers untouched (chat uuid4 / asr sha256 keys never-seen by construction, pinned). files_in_scope normalized += tests/unit/test_external_store.py + tests/unit/test_scratchpad.py — the eight→nine preflight-count pin tests the ninth call forces (consequential, not creep). Builder self-ran the full bar green: targeted suites 39 passed / 6 pg-skips without WOMBAT_TEST_PG_DSN and 45 passed on a THROWAWAY docker postgres:16 (port 5437 — the mandated guard held, live wombat DB never touched) covering all pg-gated ACs incl. the ack-delete live-defect repro and restart-survival; full repo suite on that DSN 1849 passed / 13 live-gated skips / 0 failed; ruff clean; mypy clean. Architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING per the v2 cadence — one Opus review audits the whole DEC-63 batch diff after TK-287 lands. NEXT — TK-287 (the once-per-wombat-day FlushDayLatch), then the operator runtime relaunch.

## 2026-07-21 — TK-287 DONE (closeout — within authority; contract v2.149, board 204/204 — BOARD CLOSED): DEC-63b's FlushDayLatch lands; the repeating-digest class is structurally dead at both seams

Built exactly per the v2.146 mint, architect-verified in the diff. `gate/ceiling.py` gains `FlushDayLatch` beside `CeilingLedger` — the identical `allow()`/`record()` shape over the SAME shared `DailyLedger`, fixed ledger_name `"flush:load"` (once per wombat day PINNED, no new tunable — the BriefRunLedger precedent), plus `note_denied()` deduping the denial INFO line to at most one per wombat day via an in-memory last-denied wombat-date against `daily_ledger.today()` (a restart resets only the log dedup; the durable count stays in Postgres). `pipeline.py` gains `FlushLatchProtocol` and the REQUIRED keyword-only `flush_latch` Gate kwarg (Q-69 lesson — omission is a TypeError); `_try_flush` checks `allow()` FIRST, a denial returns None with the pending set UNTOUCHED (items keep aging toward decay or tomorrow), and `record()` fires exactly on the SURFACE_FLUSH return. Composed in `bootstrap.assemble_runtime` AND `scripts/demo_drain.py` over the same `DailyLedger` as CeilingLedger/DayRollover. `select_items`/BriefForceFlushStage byte-untouched (Q-23/B upheld) and the immediate-voice arm is unbound. NEW pg-gated `tests/gate/test_flush_latch_pg.py` proves restart durability (fresh latch, same DSN, still denied — AC4), cleaning only `flush:load` rows. files_in_scope normalized: +`scripts/demo_drain.py` + the 11 Gate-construction test files that gained the always-allow fake (the required-kwarg ripple — consequential, not creep). No yaml/params change — the wombat_params.yaml:26 "fires once/day" comment simply became true. Builder self-ran the full bar green: full suite on THROWAWAY docker postgres 5437 (guard held) 1858 passed / 13 live-gated skips / 0 failed; targeted 46 passed incl. 3 pg-gated; ruff clean; mypy clean. Architect re-ran `uv run pytest -q` pre-commit. **BATCH-VERIFY PENDING** per the v2 cadence — one Opus review now audits the whole DEC-63 batch diff (TK-286 + TK-287). NEXT — the Opus batch review, then operator closeout: runtime relaunch picks up DEC-60/61/62/63 live (the digest drumbeat dies on relaunch), hands-free ear-check, GCP Testing→Production.

## 2026-07-21 — JIM RULING RECORDED (contract v2.150, board reopens 204/208): the Steward converses walkie-talkie style — ISS-28 → DEC-64 + DEF-11 + TK-288..TK-291

Jim ruled the conversational-Steward proposal: no VAD approach — keep the push-to-talk button, replies are walkie-talkie style, leave room for VAD if the button gets annoying. Recorded exactly as ruled: **PTT stays the ONLY mic trigger and the reply channel** (source-confirmed zero app changes — `app/src/ptt.ts` capture → drop dir → `ASRSource` already IS the reply loop); **Q-97 stands untouched** (no supersession); **auto-listen/VAD = DEF-11** (revisit trigger: Jim reports the button annoying) with the new `on_spoken` seam + `LastSpokenRegister` pinned as the future attachment point — a listen-window consumer subscribes to the same hook and feeds the SAME drop-dir pipeline, zero rework.

The complaint (ISS-28, Jim verbatim: "just blurts out what he says and that's that") decomposes into two mouth-side gaps, both closed on the EXISTING payload wire so Q-50's payload-verbatim rule is upheld rather than routed around (the context IS payload — spoken text the user just heard + DEC-45 whitelisted projections; CON-1 clean):

- **Gap A (reply context)** — TK-288 (EP-30): `src/wombat/voice/reply_context.py` `LastSpokenRegister`, single-slot, in-memory, `LAST_SPOKEN_TTL_SECONDS = 120.0` + 600-char cap as PINNED constants (DEC-63 no-knob precedent); optional `on_spoken` hooks fire ONLY after a successful speak — `SpeakSink` post-`adapter.speak` with the DEC-55 shaped summary (what the user actually HEARD), `BriefDeliverStage` on `voice_spoken=True`; a hook raise never degrades speech. TK-289 (EP-29): `ASRSource` gains the FOURTH optional ctor hook `context_hook` (command/feedback/turn lineage), merging `replying_to` into the payload strictly after command consumption, reserved keys unoverridable; compose untouched — `format_payload_fields` already renders every payload field.
- **Gap B (grounding)** — TK-290 (EP-29): `build_voice_context` over `ExternalItemStore.get_window('gcal', today's civil-local bounds — DEC-21)` + `get_recent('gmail', 5)`, deterministic rendering under hard caps (10/800, 5/400); store absent/raising = no keys + one WARNING (CON-3 additive); voice-only by construction (the hook lives on ASRSource); DEC-26 held structurally — the store's gmail rows carry no body key at all. TK-291 (EP-29): the e2e FakeModel-spy proof + byte-untouched pins (DEC-62 charter, DEC-57 held-chat, DEC-60 voice path, brief/draft/reflection).

Rejected and recorded on DEC-64: VAD (Jim's ruling), T1 free LLM tool loop (mouth-not-brain; taint tier forbids web-read-then-dispatch in one drive), T2 closed-vocab intent legs (unscheduled follow-up, zero v1 tickets), compose-side injection (would re-open the TK-284 leak-check pins and the Q-50 rule), multi-turn history window, gate/persona re-pathing (the gate keeps whether/when; this arc is purely what-the-mouth-knows). Typed-chat grounding is a named follow-up, not scope. Accepted costs: bounded payload growth in queue/journal, one ms-fast sync pg read on the capture path (DEF-5 class), restart forgets the register.

## 2026-07-21 — BATCH SELECTED (within authority; contract v2.151): TK-288 → TK-289 → TK-290 → TK-291 — the full DEC-64 walkie-talkie arc, with three pre-build rulings

Board 204/208; the four DEC-64 tickets are the only open work — order forced by the depends_on chain (288 → 289 → 290 → 291). Every seam re-verified live pre-dispatch: speak.py `adapter.speak` :136, silent branch :90-106, both Degraded branches :118-133/:140-157; brief_deliver_stage.py `voice_spoken=True` flip :126, `speak=` ctor kwarg :59-70; asr.py ctor :149-166 (command/feedback/turn hook lineage), command early-return :250-256, payload build :270-277; sources/bootstrap.py `_maybe_register_asr` :519-527 / pass-through :575 / `build_source_registry` :580-596/:663; compose.py payload wire :158 + ceiling :168-188; templates.py `format_payload_fields` :26-35 (sorted, "; "-joined); external_store.py `get_window` :132-150 (inclusive SQL window) + `get_recent` :152-170 (DESC LIMIT, ascending re-sort); cogworx `Done`/`Degraded` confirmed in the installed framework. **Three rulings (v2.151, briefing-binding):** (1) TK-288 — two SpeakSink construction sites exist; the `on_spoken` closure wires at assemble_runtime's DIRECT construction (bootstrap.py:1010 — the runtime instance), `build_speak_sink` helper (:490-501, zero src callers) untouched; the BriefDeliverStage hook threads through `build_brief_deliver_stage` via a new optional `on_spoken` kwarg mirroring `speak=` (call site :1246). (2) TK-289 — AC6's prompt check asserts the `format_payload_fields` rendered substring (`replying_to: <text>`) in `user_msg.content`, never a line form. (3) TK-290 — renderer fields pinned to the verified projections: gcal `{event_id,title,start,end,all_day}`, gmail `{message_id,subject,sender,received_at,priority_band}` (sink at sources/bootstrap.py:353-377 — NO body key, DEC-26 by construction confirmed); email lines read `sender`/`received_at`; today's bounds = tz-local 00:00:00–23:59:59.999999 aware datetimes into `get_window`. Orchestrator guards restated in every briefing: ZERO app/ changes, NO register persistence, throwaway WOMBAT_TEST_PG_DSN only. Atomicity holds (S/S/M/S; TK-291 proof-only, zero-src budget — any arc-unprovable finding routes back to me, never patched in place). Nothing deferred — no other open ticket; operator OAuth/GCP/ear-check items stay outside the loop. validate PASS (273 nodes / 233 governance), gate_check analyze PASS. NEXT — build the four in order; single Opus batch-verify; then operator relaunch + the live walkie-talkie ear-check.

Self-resolved within authority (no locked DEC/CST/CON/NG or v1-scope change): the TTL value (120s pinned, no tunable), the payload-wire delivery over compose-side injection, voice-only scope for the prefetch, on_spoken-fires-only-on-successful-speak, and the register being in-memory. validate PASS (273 nodes), gate_check analyze PASS. **BUILD ORDER: TK-288 → TK-289 → TK-290 → TK-291.** Still owed: the DEC-63 pair's Opus batch-verify; operator relaunch picks up DEC-60/61/62/63.

## 2026-07-21 — TK-288 DONE (closeout — within authority; contract v2.152, board 205/208): DEC-64 gap A half 1 — the LastSpokenRegister + on_spoken seam lands

Built exactly per the mint plus the v2.151 wiring ruling, diff confined to the eight files_in_scope. New `src/wombat/voice/reply_context.py`: `LastSpokenRegister(clock=epoch-seconds)` — single-slot newest-wins, `_MAX_SPOKEN_CHARS = 600` truncation, `LAST_SPOKEN_TTL_SECONDS = 120.0`, both pinned constants (DEC-63 no-knob precedent), NO persistence, NO locking (every touch point on the event loop). Seam 1: `SpeakSink` optional ctor `on_spoken` fired with `(item_id, speech_text)` strictly AFTER `adapter.speak()` returns without raising — never on the silent Done branch, never on either Degraded branch; a raising hook = exactly one WARNING, `Done(spoken=True)` byte-identical. Seam 2: `BriefDeliverStage` same kwarg, fired with `("brief:" + ctx.run_id, text)` exactly when `voice_spoken` flips True, same guard. Wiring per v2.151 verbatim: `assemble_runtime` constructs ONE shared register and threads `note_spoken` into the direct SpeakSink construction AND `build_brief_deliver_stage`'s new optional `on_spoken` pass-through; `build_speak_sink` byte-untouched. One files_in_scope normalization (within authority): `tests/sinks/test_speak_sink.py` → `tests/unit/test_speak_sink.py`, the v2.135-verified AC-suite home (same slip v2.136 recorded on TK-279). Builder bar green (targeted 82 passed; full suite 1762 passed / 100 pg-gated skips; ruff clean; mypy strict 341 files; app/ untouched); architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING — one Opus review audits the whole DEC-64 arc after TK-289/290/291 land. DEF-11's future auto-listen attachment point now exists in code. NEXT — TK-289 (`context_hook` stamps `replying_to`).

## 2026-07-21 — TK-289 DONE (closeout — within authority; contract v2.153, board 206/208): DEC-64 gap A half 2 — `replying_to` rides the payload wire; one AC-homing ruling

Built exactly per the mint; diff confined to six of the eight files_in_scope (`tests/sources/test_asr.py` needed no edit — its suite ran green as the no-regression pin). `ASRSource` gains the FOURTH optional ctor hook `context_hook: () -> Mapping[str, str]`, evaluated in `_process_one` strictly AFTER the `command_hook` early-return, at payload-build time; the hook's mapping merges UNDER the four built-ins (spread first, built-ins after — `item_kind`/`voice_turn`/`transcript`/`captured_at` structurally unoverridable). UNLIKE the command/feedback/turn lineage, THIS call IS guarded: a raise = ONE WARNING (`exc_info`) and the turn proceeds unstamped, file still to `processed/`. `sources/bootstrap.py` threads it through `_maybe_register_asr` + `build_source_registry` in the TK-280 pass-through shape (None default = byte-identical construction). `assemble_runtime` builds the unconditional `asr_context_hook` closure over the SAME shared TK-288 register: fresh `current()` stamps `replying_to` with the exact spoken text; stale/None = key ABSENT, never empty string. **ONE CLOSEOUT RULING (within authority — AC-check homing, no decision/scope change):** AC6's FakeModel-spy prompt clause is CARRIED BY TK-291 AC1/AC2 — the identical assertion in its stronger end-to-end form (real Gate + real ComposeStage + spy), later in this SAME dispatched batch; TK-289's diff proves the structural half now (compose.py clean diff; verbatim wire re-verified — artifacts.py:207 → stages/compose.py:158 → templates.py:26-35 renders every sorted key unfiltered; the generic pin at tests/unit/test_compose_stage.py:96-102 already asserts arbitrary payload values reach `user_msg.content`). REOPEN CONDITION: if TK-291 does not land in this batch, TK-289 reopens for its own AC6 spy check. Builder bar green (targeted 91 passed / 1 pg-skip; full suite 1771 passed / 100 pg-gated skips; ruff clean; mypy strict 341 files; app/ untouched); architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING — one Opus review audits the whole DEC-64 arc after TK-290/TK-291 land. NEXT — TK-290 (`build_voice_context` grounding prefetch).

## 2026-07-21 — TK-290 DONE (closeout — within authority; contract v2.154, board 207/208): DEC-64 gap B — the deterministic grounding bundle rides voice turns

Built exactly per the mint plus the v2.151 field-pin ruling; diff confined to the four files_in_scope. New `src/wombat/voice/context_prefetch.py`: `build_voice_context(store, tz, clock)` returns AT MOST `{context_calendar_today, context_recent_email}` — gcal via `get_window('gcal', ...)` over the tz-local day 00:00:00–23:59:59.999999 as aware datetimes (DEC-21 `wombat_today`, computed at call time; the store's inclusive SQL filter does the exclusion, NO renderer-side re-filtering), gmail via `get_recent('gmail', 5)`; deterministic one-per-line rendering under pinned module-constant caps 10/800 and 5/400 (DEC-63 no-knob). Zero rows = NO key; `store=None` = `{}` silent; ANY raise from either call (ONE guarded block) = `{}` + exactly ONE WARNING, turn proceeds with transcript/`replying_to` unaffected (CON-3 additive). DEC-26 held by construction AND pinned: renderer reads ONLY the v2.151-verified projections (gcal `event_id/title/start/end/all_day`, gmail `message_id/subject/sender/received_at/priority_band` — DEC-45 whitelist; email lines read `sender`/`received_at`); no body/body_text token anywhere in the module (grep + unit pin). Wiring: merged into the SAME TK-289 `asr_context_hook` closure (voice-only by construction); `ExternalItemStore` construction moved earlier in `assemble_runtime` so the closure captures it (still always-constructed/fully-lazy per v2.68 r5); `replying_to` spread first, bundle merged after, ASRSource built-ins unoverridable above both; compose byte-untouched — the daily token ceiling stands as the spend guard. Builder bar green (targeted 37 passed/1 skip; 50 passed on throwaway postgres:16 @5437; full suite with DSN 1905 passed / 13 skipped / 0 failed; ruff clean; app/ untouched); architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING — one Opus review audits the whole DEC-64 arc after TK-291 lands. NEXT — TK-291 (the e2e walkie-talkie proof — also carries TK-289's homed AC6 spy clause; last open ticket of the arc).

## 2026-07-21 — TK-291 DONE (closeout — within authority; contract v2.155, board 208/208 — BOARD CLOSED): the DEC-64 walkie-talkie arc proven end-to-end; the whole four-ticket arc is landed

Built exactly per the mint with ZERO src changes (the ticket's complexity budget) — the only code diff is the ONE new pg-gated integration module `tests/integration/test_voice_conversation_context_e2e.py`, so AC3's byte-untouched-neighbors clause holds by construction AND by the full suite passing unmodified. The module drives the REAL seams, fakes only at the edges: real `ASRSource` (fake keyed Transcriber, tmp drop dir) via the real `SourceRegistry` onto a real `WombatQueue` (module-level skip without `WOMBAT_TEST_PG_DSN` — never the live wombat DB); the real production Gate (`make_gate_evaluator` over `Gate` with CeilingLedger/FlushDayLatch/PendingSet/UserModel, mirroring `test_drain_pathway_e2e.py`'s `_build_real_gate_stack`); real ComposeStage/ChatReplyStage/SpeechShapeStage/SpeakSink with a FakeModel spy; the TK-288 register on its OWN steppable clock plus the TK-289/TK-290 combined `asr_context_hook` closure wired verbatim as `assemble_runtime` wires them (`replying_to` spread first, `build_voice_context` merged on top). AC1: seeded register + today-gcal/recent-gmail store rows + a file dropped inside the TTL — the captured compose user message carries the `replying_to` key with the EXACT spoken text AND both `context_calendar_today`/`context_recent_email` substrings (asserted as substrings per the v2.151 sorted-one-line ruling), and the reply flows the full DEC-60 voice path (voice_turn on the composed artifact, real non-degraded speech_shape call, adapter actually spoken, broker future resolved — pane echo). AC2: register clock advanced past `LAST_SPOKEN_TTL_SECONDS` — the second turn's prompt has NO `replying_to` while both context keys persist. The v2.153 REOPEN CONDITION on TK-289 is DISCHARGED — TK-291 landed in the same batch carrying the stronger end-to-end spy assertion. Builder bar green (ruff clean; mypy Success 344 files; module 1 passed on throwaway postgres:16 @5437, stopped after; full suite with DSN 1906 passed / 13 skipped / 0 failed); architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING per the v2 cadence — ONE Opus review now audits the whole four-ticket DEC-64 diff (TK-288..TK-291). NEXT — the Opus batch review; then the operator runtime relaunch + the live walkie-talkie ear-check picks up DEC-60/61/62/63/64.

## 2026-07-30 — BATCH SELECTED (contract v2.159, within authority): TK-292 -> TK-293 -> TK-294 -> TK-295 -> TK-296 — the first five of the DEC-65 companion arc

The v2.156 recorded order stands; the 5-ticket cap is the ONLY reason TK-297 (dream_facts) and TK-298 (charter sentence + e2e proof) wait for the next batch — TK-297 needs TK-294+TK-295 landed, TK-298 needs TK-296+TK-297, so nothing in this cut can strand them. TK-299/EP-37 stays undispatched per DEF-13. Every cited seam re-verified live pre-dispatch (builder/expression/policy/live, compose selection point :148-151, preflight ninth-entry shape, external-item sink :339/:651, asr_context_hook :1088 + wiring :1319, surface payload build :475-488). THREE pre-build rulings recorded in v2.159: (r1) TK-295's boot purge fires in runtime.py beside the scratchpad purge_stale (:441) — the ticket's "from assemble_runtime" wording had drifted; chat_turn_store joins the runtime bundle; files_in_scope amended (+runtime.py, +tests/unit/test_runtime.py). (r2) SourceRegistry takes ONE sink — build_source_registry gains optional chat_turn_store and composes the chat-turn tap WITH the external-item sink into one callable, chat tap one-WARNING-never-blocks, external behavior byte-identical. (r3) TK-296's typed-chat hook homes as a PUBLIC ChatSource ctor-kwarg attribute (the wake precedent :53), fired by surface._accept_message at payload-build, merged UNDER item_kind/text/received_at, TK-289 raise-posture verbatim. files_in_scope normalization: TK-296's tests/chat/test_surface.py -> the real tests/chat/test_chat_surface.py. validate PASS 282 nodes / 238 governance (v2.159), gate_check analyze PASS. NEXT — dispatch the five briefings; on landing, batch TK-297 -> TK-298 closes the arc, then the Opus batch review + Jim's live chat feel-check, then DEF-13's trigger window opens for TK-299.

## 2026-07-30 — TK-292 DONE (contract v2.160): the FIFTH mouth — Mouth.CHAT companion register + wombat_user_name (DEC-65a/c); board 209/216, batch-verify pending

The companion arc's first ticket lands exactly per the mint, diff confined to files_in_scope (tests/persona/test_live.py untouched — its suite ran green as the no-regression pin, the TK-289 precedent). `Mouth.CHAT` joins the closed enum with the pinned `_chat_base(assistant_name, user_display)` companion string verbatim; `ClauseAlgebraStrategy` gains construction-held `user_name` (None/blank → "the user"), special-cased in `render` because CHAT is the only two-name-slot mouth (does not fit the one-arg `_BASE_ROLE_BUILDERS` dict); `instruction_for`/`LivePersona` thread it through with every existing call site byte-unaffected. `_GUARD_SUFFIX["chat"]` reuses the compose charter verbatim — the DEC-62 posture extends to CHAT at the same strip-proof seam. Policy extends to the five-mouth closed set, `mouth_axes.chat` = brevity/warmth/directness/humor with NO new clause strings; `PERSONA_POLICY_VERSION` + yaml `version` bumped 1→2 in lock-step (within-authority acceptance — the mint didn't name the bump, the module's own documented version rule requires it when a mouth is added). `wombat_user_name` ('' default) joins WombatConfig + APP_EDITABLE_FIELDS + the SettingsUpdate vocabulary (enabling seam only, no UI); `assemble_runtime` passes `config.wombat_user_name or None` into LivePersona. The four original mouths' DEFAULT byte-identity oracles re-asserted with user_name set and unset — the companion register is base-role text, personable at DEFAULT_MATRIX per Jim's directive, axes modulate on top. Builder bar green (full suite 1936 passed / 14 live-gated skips, run twice incl. a throwaway postgres:16 @5437 so every pg-gated case executed); architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING — one Opus review audits the whole DEC-65 batch after TK-293..TK-296 land. NEXT — TK-293 (ComposeStage selects Mouth.CHAT by item_kind).

## 2026-07-30 — TK-293 DONE (contract v2.161): ComposeStage selects Mouth.CHAT by item_kind (DEC-65b); board 210/216, batch-verify pending

The companion arc's second ticket lands exactly per the mint, diff confined to the two files_in_scope (`stages/compose.py` + `tests/unit/test_compose_stage.py`; app/ untouched). `run()` selects the mouth at the SAME TK-209 live/frozen branch point: `item_kind is ItemKind.CHAT` (typed AND voice both carry it, DEC-57/60) → `live_persona.instruction(Mouth.CHAT)` read fresh every turn (LivePersona threads `user_name` from TK-292); `live_persona` None → the NEW frozen `_chat_system_instruction(assistant_name, user_name)` built ONCE in `__init__` from `config.wombat_assistant_name`/`wombat_user_name` (blank → "the user", matching `ClauseAlgebraStrategy.render`'s CHAT special case) and test-pinned byte-equivalent to `instruction_for(Mouth.CHAT, DEFAULT_MATRIX, ...)` — the `_system_instruction`/TK-284 equivalence discipline, sound because every chat clause renders empty at DEFAULT_MATRIX. Every non-CHAT `item_kind` takes the untouched `Mouth.COMPOSE`/`self._system_instruction` path byte-identically; NO new ctor params, NO payload/wire/template/router/gate changes; the user message stays verbatim (Q-50 untouched, DEC-64's compose-side-injection rejection honored); degrade stays TemplateComposer's deterministic line for chat too (DEC-37e honest asymmetry). Builder bar green (ruff clean; mypy Success 344 files; compose suite 27 passed; full suite 1818 passed / 105 pg-/live-gated skips / 0 failed); architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING — one Opus review audits the whole DEC-65 batch after TK-294..TK-296 land. NEXT — TK-294 (`wombat_user_facts` migration 011 + UserFactsStore).

## 2026-07-30 — TK-294 DONE (contract v2.162): `wombat_user_facts` migration 011 + UserFactsStore, the durable user-model substrate (DEC-65d); board 211/216, batch-verify pending

The companion arc's third ticket lands exactly per the mint, wiring-free. `migrations/011_user_facts.sql` — `wombat_user_facts(fact_key TEXT PRIMARY KEY, fact, source, first_seen_at write-once, updated_at)`, one idempotent CREATE IF NOT EXISTS (NG-3). `src/wombat/user_facts.py` — lazy dsn-injected `UserFactsStore` mirroring `scratchpad.py`/`seen_ledger.py` exactly (Q-46 zero-IO-at-construction): `upsert_fact` (ON CONFLICT DO UPDATE deliberately omits `first_seen_at` — write-once pinned), `list_facts` updated_at DESC, `delete_fact`, `count`; pinned `_MAX_FACTS = 200` (DEC-63 no-knob) with evict-oldest-updated-row(s) + ONE WARNING naming evicted keys ONLY when a NEW fact_key arrives at cap — re-upserts can never evict. DURABLE FOREVER — no purge/TTL anywhere; the module docstring records why DEC-46's scratchpad (14-day purge) was NOT reused, the CON-6 custody note (rows hold what the user SAID/did, never inferred motive; TK-297 owns enforcement at the only organic write path), and the DEC-66 provenance vocabulary for `source` (dream|derived|behavior|told, never defaulted here). `schema_preflight` gains the TENTH `ensure_all_schemas` entry; imports nothing from bootstrap/runtime (test-pinned). Necessary follow-on ACCEPTED beyond files_in_scope: the three sibling suites that hard-assert the entry count/table list (`test_scratchpad.py`, `test_external_store.py`, `test_schema_preflight.py`) mechanically bumped 9→10 — the same shape TK-247/TK-286 recorded when THEY added an entry; zero other content touched. Builder bar green (ruff clean; mypy strict Success 346 files; full suite 1947 passed / 14 live-gated skips / 0 failed on a throwaway postgres:16 @5437, `test_user_facts.py` also run directly against it); architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING — one Opus review audits the whole DEC-65 batch after TK-295/TK-296 land. NEXT — TK-295 (`wombat_chat_turns` 7-day ledger).

## 2026-07-30 — TK-295 DONE (contract v2.163): `wombat_chat_turns` 7-day ledger + registry-sink tap + boot purge (DEC-65e); board 212/216, batch-verify pending

The companion arc's fourth ticket lands exactly per the mint plus the v2.159 rulings r1/r2. `migrations/012_chat_turns.sql` — `wombat_chat_turns(id BIGSERIAL PK, text, voice BOOLEAN, captured_at TIMESTAMPTZ)` + captured_at index, idempotent (NG-3). `src/wombat/chat_turns.py` — lazy dsn-injected `ChatTurnStore` (`record_turn`/`turns_since` ascending/`purge_older_than`) with `_RETENTION_DAYS = 7` a PRIVATE pinned constant (DEC-63 no-knob; runtime passes literal 7 per the ticket text, not a shared import). `schema_preflight` gains the ELEVENTH `ensure_all_schemas` entry. Per r2, `sources/bootstrap.py` gains `build_chat_turn_sink` — the `item_kind == 'chat'` tap covering BOTH typed-chat text and voice-ASR transcript payload shapes, per-event guarded so a store raise is ONE WARNING and the event still enqueues — plus `_compose_sinks`, composing it with the TK-245 external-item sink into the ONE callable SourceRegistry accepts (either half absent degrades to the other byte-identically; store None = today's sink exactly). Root bootstrap constructs `ChatTurnStore(dsn)` beside ScratchpadStore, threads it into `build_source_registry`, exposes it on RuntimeBundle; per r1, `runtime.py` fires `bundle.chat_turn_store.purge_older_than(7)` once at boot beside the scratchpad `purge_stale` site (verified at :448-449). NOT-list HELD — never rendered into any prompt, never a history window, never a gate input; dream-extraction (TK-297) is the ONLY reader; user utterances only, no reply logging (CON-1/CON-6 clean; DEC-64's multi-turn-history rejection restated, not reopened). Necessary follow-on ACCEPTED beyond files_in_scope — the sibling count-pinning suites (test_external_store/test_scratchpad/test_user_facts/test_schema_preflight) mechanically bumped 10→11, the same TK-294/TK-286/TK-247 shape. Builder bar green (ruff clean; mypy strict Success 348 files; full suite 1960 passed / 14 live-gated skips / 0 failed on a throwaway postgres:16 @5437, never the live 5436 db); architect re-ran `uv run pytest -q` pre-commit. BATCH-VERIFY PENDING — one Opus review audits the whole DEC-65 batch after TK-296 lands. NEXT — TK-296 (typed-chat context parity + the `known_user_context` stamp, the batch's last ticket).

## 2026-07-30 — TK-296 DONE (contract v2.164): typed-chat context parity + the `known_user_context` stamp (DEC-65f); board 213/216, BATCH COMPLETE, batch-verify pending

The companion arc's fifth ticket lands exactly per the mint plus the v2.159 r3 hook-homing ruling — DEC-64's recorded typed-chat grounding follow-up CLOSES. `voice/context_prefetch.py` gains `build_user_facts_context(store)` → at most `{"known_user_context": one-fact-per-line}` via the SAME `_render` helper under new pinned caps `_MAX_FACTS_LINES = 15` / `_MAX_FACTS_CHARS = 900` (DEC-63 no-knob), plus a `UserFactsContextStore` Protocol mirroring `VoiceContextStore`; degrade shape mirrors `build_voice_context` EXACTLY (store None = `{}` no call no warning; zero rows = NO key; any raise = `{}` + exactly ONE WARNING, CON-3). Per r3: `ChatSource` gains optional ctor kwarg `context_hook` held PUBLIC (the `wake` precedent), default None = byte-identical; `chat/surface.py._accept_message` fires it ONCE at payload-build time, mapping merged UNDER the unoverridable built-ins (`item_kind`/`text`/`received_at` spread after the extras), raise = ONE WARNING + unstamped turn that still proceeds — the TK-289 posture verbatim. `assemble_runtime` constructs `UserFactsStore(dsn)` beside `scratchpad_store` (always-constructed/fully-lazy, Q-46) and extends the EXISTING `asr_context_hook` closure IN PLACE to also merge the facts block, then wires the SAME closure onto `chat_source.context_hook` post-construction (late-binding closures make the source-order forward reference safe, documented at both sites, mypy-clean): typed turns gain replying_to + calendar/gmail grounding + facts, voice turns gain facts, in one stroke; gmail/gcal/brief payloads carry NONE of these keys (pinned). Files_in_scope note: `src/wombat/sources/bootstrap.py` needed NO edit — r3's construct-then-wire homing makes a pass-through unnecessary (the TK-289 no-edit-in-scope shape). Compose byte-untouched by construction — `format_payload_fields` delivers everything on the existing Q-50 wire. Builder bar green (ruff clean; mypy clean 348 files; full suite 1972 passed / 14 live-gated skips / 0 failed on a throwaway postgres:16 @5437, scoped suites also run individually); architect re-ran `uv run pytest -q` pre-commit. BATCH COMPLETE — all five dispatched DEC-65 tickets (TK-292..TK-296) landed; ONE Opus batch review now audits the whole diff. NEXT — batch selection for TK-297 (`dream_facts`) → TK-298 (charter sentence + e2e proof); TK-299 stays undispatched per DEF-13.
