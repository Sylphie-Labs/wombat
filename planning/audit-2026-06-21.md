# Frontier constructibility audit — 2026-06-21

Read-only audit of 73 unplanned tickets (all except the deep-planned TK-14/TK-1/TK-2). Each agent verified named cog-worx seams against ../cog_worx/cog-worx/src/cogworx and flagged Q-29-class constructibility gaps. Nothing was written to the contract.

Counts: {"criticalHigh":74,"medium":82,"seamMisses":17,"splits":3,"weak":22,"notReady":45}

## CRITICAL gaps (6)

### [TK-47] Q-29 repeat: neither cog-worx sweeper can be constructed from what TK-47 + its declared dep (TK-46, a bare timer scaffold) provide. CoherenceReconciler.__init__ requires entity_kg/store(CoherenceStore)/oracle(ConsistencyOracle); ClaimExtractor.__init__ requires journal/entity_kg/model/source_registry. No ticket in TK-47's dependency chain provisions a CoherenceStore or a ConsistencyOracle at all, and the model/source_registry handoff into the dream pathway is undeclared. This is exactly the Engine.__init__ four-unprovisioned-seams failure the audit exists to catch.
**Why:** A build pass would write DreamPathway wiring, then hit un-constructible collaborators with no factory to call — forcing a stop-and-redesign mid-build (the cost the gate is meant to prevent).

### [TK-47] Frame conflict with NG-4 / gate-deterministic. ClaimExtractor makes a mandatory per-session LLM call (self._model + hard-coded _EXTRACTION_SYSTEM prompt), and ConsistencyOracle is the model-backed adjudicator the reconciler calls. The ticket markets these as deterministic 'reuse cog-worx stages' with no mention that the dream pathway now performs LLM inference, no model-provisioning, and no cost/budget consideration. NG-4 forbids an LLM deciding when/whether to act; consolidation-time extraction/adjudication is arguably allowed (off-path, S1), but the ticket must declare it and wire a Model/oracle — it does neither.
**Why:** Either the ticket silently imports an LLM dependency into a pathway the frame treats as model-free, or (if a no-LLM path is intended) it must be respecified — either way the current AC set cannot be built as written without resolving the contradiction.

### [TK-99] The 'consolidated-flush arm' seam TK-99 adapts does not exist in the form required. EP-9/TK-27 ships only gate.pipeline(), where threshold-checking, 'emit all pending', and clearing-the-pending-set are fused into one path. TK-99 needs a threshold-free selection callable that does NOT clear/mutate the pending set (its own non_goal: 'Does not modify gate ... pending-set'). TK-27 provides no such bypass seam. This is a Q-29-class missing-provisioning: the ticket relies on a capability no upstream ticket builds.
**Why:** Without a threshold-bypass selection entry point on the gate, BriefForceFlushStage cannot be built as a 'thin adapter' — it would have to reach into gate internals and either re-implement selection (violating 'selection logic lives in EP-9/gate') or call pipeline() and corrupt the live pending set (violating its own non_goal and CON-1 determinism for the normal drain pathway).

### [TK-99] AC #1 and #3 require the BriefPayload's own items (calendar events from TK-98, conflict candidates from EP-16) to flow through 'the gate's deterministic selection', but no ticket accumulates BriefPayload items into the gate's pending set, and EP-9's flush selects 'all pending', not 'items that pass selection'. The per-item selection predicate the ACs name is unprovisioned.
**Why:** AC #3 ('empty pending + 1 event -> artifact still contains that one event') proves the brief's events must be selectable independently of the gate's pending accumulation — yet the only path into selection (TK-27) is the in-memory pending set populated by the live drain pathway. The input wiring for brief-specific items into selection is missing, so the acceptance test cannot be satisfied by any provisioned capability.

### [TK-118] No channel for pattern_id to reach the contributor. AC1 requires the contributor to 'resolve the pattern_id' and call extract_phrasing_hints(pattern_id), but the cog-worx contributor seam (contribute(request, allocation)) only receives a frozen ContextRequest (task/instructions/query/policy) with no pattern_id or metadata field. Nothing in TK-118 or its deps (TK-117, EP-5) provisions a way for the gate-cleared item's pattern_id to flow into the contributor.
**Why:** The contributor cannot do its one job -- look up hints by pattern_id -- because the per-item key it needs is not delivered by the named seam. This is a Q-29-class missing-seam: the AC references an input no listed mechanism produces. Build cannot proceed without first deciding the side-channel (per-turn contributor construction, a wombat-side request wrapper, or passing hints in from the Stage rather than via a contributor).

### [TK-148] AC5 asserts cog-worx raises 'ToolGateViolation', but no such exception exists in cog-worx. The actual class is TierViolation (out-of-tier dispatch once external is dropped) or ApprovalRequired (tainted+consequential+irreversible). An adversarial test written against ToolGateViolation will not import / will never match, so AC5 is uncheckable as written.
**Why:** The acceptance criterion names a non-existent seam symbol; the build will either fail to import or silently never catch the intended exception, defeating the safety assertion. Same misnamed symbol also appears in TK-146's non_goals cross-ref — propagated naming error.

## HIGH gaps (68)

### [TK-152] TK-152 asserts persistence behavior (AC2 'the persisted row is reloaded ... survives restart per the load-on-start contract'; AC3 'exactly one new wombat_date row is in effect') over a Postgres table (src/wombat/migrations/003_daily_ledger.sql), but nothing in its declared dependency chain provisions a Postgres connection/session or runs the wombat-domain migration. Its only depends_on is TK-1, which is a pure assembly/wiring composition root that — per its own non_goals and Q-29 — explicitly does NOT construct or connect any store and opens no DB connection. The cog-worx SubstrateBundle (built by TK-14) holds cog-worx's journal/graph_store/latent stores, NOT a generic wombat-domain table, so it does not supply this either. This is the exact Q-29 pattern: a required seam (a pg connection/session handle + a migration-runner that applies 003_daily_ledger.sql) that no declared dependency builds.
**Why:** The build cannot satisfy the 'load-on-start / survives restart' ACs without a live Postgres session and the migrated table existing. Whoever builds TK-152 will discover there is no provided DB handle and either (a) fork a connection inline (violating the local-first/config-swap frame where infra is injected, TK-14/Q-29) or (b) stall. The undeclared dependency (a wombat-domain DB session/migration provider) must be named and pointed at, or the persistence ACs must be expressible against an injected store.

### [TK-6] Undeclared hard dependency on TK-21. TK-6's acceptance_criteria and complexity_budget require importing the canonical GateAction enum (surface_immediate, hold) and the GateDecision type, which are DEFINED ONLY by TK-21 (src/wombat/gate/models.py, per ISS-4's resolution which explicitly names 'TK-6 stub gate emits surface_immediate' by importing TK-21's module). Yet TK-6 declares depends_on: [TK-5, TK-4] — TK-21 is absent. Every other TK-21 consumer (TK-23, TK-24, TK-27) declares depends_on: [TK-21]. TK-6 cannot compile its ACs without TK-21's enum/types. This is a Q-29-class undeclared, build-blocking seam.
**Why:** A builder pulling the lean briefing for TK-6 would not see TK-21 as a prerequisite, would have nothing to import GateAction from, and would be tempted to invent a private 'surface'/'hold' vocabulary — which is exactly the ISS-4 CRITICAL the contract just resolved. The fix is a one-line dep add (add TK-21 to depends_on); no scope change.

### [TK-7] Undeclared dependency on TK-21 (the canonical-vocabulary ticket). TK-7's complexity_budget and AC #1 import and branch on GateAction.surface_immediate/surface_flush/hold and the GateDecision/item_kind artifact, which ISS-4's resolution defines as a NEW AC on TK-21. But TK-21 is NOT in TK-7's depends_on [TK-6, TK-10], and is NOT transitively reachable: TK-6 (the GateDecision producer) depends_on [TK-5, TK-4] and also omits TK-21 despite its own budget saying it 'Uses the canonical GateAction enum from TK-21'. So the module that DEFINES the enum TK-7 imports is an undeclared dep across the whole drain chain.
**Why:** This is the exact ISS-4/Q-29 class (a required seam no ticket's dependency edge provisions). The artifact is fully specified and TK-21 does produce it, so it is provisionable — hence HIGH not CRITICAL — but with no declared edge a build pass could start TK-7 (or TK-6) before GateAction exists, forcing rework. Fix: add TK-21 to depends_on of TK-7 (and TK-6).

### [TK-10] Undeclared dependency on TK-21 for the item_kind enum. TK-10's complexity_budget states it 'reads GateDecision.item_kind (the canonical closed Enum from TK-21/ISS-4)' and all four ACs dispatch on item_kind, yet depends_on is only [TK-6, TK-8]. TK-6 produces GateAction (hold/surface_*), not item_kind; the {brief,reflection,draft,generic} enum is produced by TK-21 (AC1 of TK-21). A build pass cannot resolve the item_kind import / enum vocabulary from the declared deps.
**Why:** Q-29-class: an artifact every AC relies on (the item_kind closed Enum) has no producer in this ticket's declared dependency chain. The dep is provisionable (TK-21 exists as a ticket), but until TK-21 is added to depends_on the build resolves item_kind only by accident/transitively, and the ticket's 'unknown kind -> generic' AC4 cannot be meaningfully tested against a closed enum that isn't formally available. Fix: add TK-21 to depends_on (and ideally have TK-6 declare it too).

### [TK-8] AC3 requires a ConfigurationError at ComposeStage CONSTRUCTION time when DEEPSEEK_API_KEY is missing, but neither cog-worx nor the declared wiring produces that there. TK-1 already owns env validation (TK-1 AC2 raises ConfigurationError in WombatConfig/bootstrap), and the Engine assembles the model per-drive from the registered ModelSpec — the model is not constructed when the ComposeStage is instantiated. OpenAICompatModel.__init__ tolerates api_key=None and only ever raises ValueError (on missing capabilities). So for AC3 to pass, the ComposeStage must itself read the env/config at __init__ and raise wombat's ConfigurationError.
**Why:** Without an explicit decision that the ComposeStage re-checks the key at construction, a builder will either (a) assume TK-1's bootstrap check covers it (it fires earlier, at composition, not at Stage construction) or (b) get a ValueError from the adapter at first async call, not a ConfigurationError at construction. Either way AC3's specific 'at construction, not first call' contract is unmet and forces rework. Constructible in wombat-domain (read env in __init__) but the responsibility/seam is undeclared and mildly contradicts TK-1 owning config validation.

### [TK-9] Internal contradiction on WHERE the daily-spend table lives. TK-9 complexity_budget + files_in_scope define a self-owned DailySpendLedger with its own migration (src/wombat/migrations/002_daily_spend_ledger.sql) and schema 'one row per date, columns date/tokens_spent/cost_usd, loaded on start'. But AC5 and depends_on TK-152 place the day boundary AND row lifecycle in the SHARED DailyLedger primitive (TK-152: one row per (ledger_name,wombat_date), migration 003_daily_ledger.sql, current_row/increment). TK-152 non_goals say it does NOT own counter semantics (spend amount) — those live in TK-9. So the intended split is: TK-152 owns the row+boundary, TK-9 owns the spend value; yet TK-9 still ships its own dated table+migration as if TK-152 did not exist.
**Why:** A builder cannot satisfy both. Either two competing daily tables (002 spend-only + 003 shared) reintroduce the exact Q-15/DEC-21 split-definition bug TK-152 was created to close, or TK-9 must drop its own table/migration and store the spend amount in (or keyed to) TK-152's shared row. Must be edited to pick one before build, or the build produces a second independent date mechanism violating DEC-21.

### [TK-27] AC-3 requires the flush arm to 'clear the pending set' after a surface_flush, but no ticket provisions a clear/drain-all operation on the durable journal-backed pending set. TK-25 (the pending set's owner) specifies add, capacity-eviction, rebuild_from_journal, list, and cumulative_load — but no bulk clear/remove-on-surface. TK-24 proves exactly-once for single PendingSetAdd/PendingSetRemove journal claims only. A bulk clear after flush must itself be journaled to stay exactly-once across a kill, yet that capability is unowned.
**Why:** Q-29-class: TK-27 leans on a capability (durable, journaled clear of the pending set) that its declared dependency does not build. A build pass would have to either invent an un-journaled in-memory clear (breaking the RISK-5 exactly-once guarantee the whole gate rests on) or back-port a clear() into TK-25. Either way it forces rework / silently violates the kill-consistency invariant.

### [TK-27] AC-3 gates the flush on 'at least one item is older than flush_min_age_seconds', but flush_min_age_seconds is NOT among the parameters TK-13 (the param store, in depends_on) enumerates (it lists urgency_threshold, load_flush_threshold, decay_ttl_seconds, MAX_PENDING, per_class_daily_ceiling, daily_token_ceiling, morning_brief_time). No ticket persists flush_min_age_seconds.
**Why:** An AC references an operating constant that no config-store ticket produces. Per the locked frame, production operating parameters must live in the param store (TK-13/Q-19), not as a scattered magic number. A builder would have to add the field to TK-13 (rework) or hard-code it (frame violation). The spike TK-26 also never sweeps a min-age parameter, so even the recommended value is unsourced.

### [TK-28] Undeclared dependency on TK-13 (production param store). AC-1 and AC-4 both consume decay_ttl_seconds, which TK-13 explicitly owns (TK-13 AC enumerates decay_ttl_seconds; TK-13 intent: 'the gate (TK-27/28) ... read their constants from one audited place'). TK-28's depends_on is [TK-27, TK-152, TK-24] and omits TK-13 — even though its split-sibling TK-27 DOES declare TK-13.
**Why:** Without TK-13 declared, the builder will likely inline a decay_ttl_seconds literal in decay.py, violating the single-source rule TK-13 enforces (its AC asserts 'no competing hard-coded threshold/ceiling constant exists in the gate'). That is exactly the kind of magic-number scatter TK-13 was created to close, and would force rework once TK-13 lands.

### [TK-13] File-ownership AND type-name collision with declared dependency TK-1. TK-13 files_in_scope claims src/wombat/config.py and its complexity_budget names the type 'WombatConfig'. But TK-1 ALSO owns src/wombat/config.py (TK-1 files_in_scope) and TASK-6 already defines 'WombatConfig' there as a pydantic-settings BaseSettings env loader for DEEPSEEK_API_KEY/DEEPSEEK_BASE_URL — a fundamentally different concept (env-sourced process credentials) from TK-13's YAML-sourced, versioned, file-edited operating-parameter store. Two tickets, one file, one type name, two incompatible designs (different source: env vs YAML; different shape; different concern; different change-control: env-vars vs human-edited versioned file).
**Why:** On build, TK-13 will either overwrite/redefine TK-1's WombatConfig (breaking the composition root's credential loader) or be forced into an undeclared rename/relocation that no AC specifies. A clean build needs a distinct module (e.g. src/wombat/params.py) and a distinct type (e.g. WombatParams / OperatingParams) for TK-13, or an explicit decision merging the two concerns. As written the two tickets contradict each other on the same artifact.

### [TK-42] complexity_budget names the wrong cog-worx read seam: 'reads from cog-worx user_model scope via the RecallStack/S5 seam.' RecallStack is a ranked RRF recall/fusion engine (recall/stack.py), not a deterministic point-read of a parameter node. AC-1 ('returns the stored personalized values' for a specific CALENDAR_CONFLICT node) is a precise key-lookup RecallStack cannot do and that a fuzzy ranked recall would violate (gate-deterministic, NG-4).
**Why:** A builder following the ticket literally wires UserModel to RecallStack.recall(query) and gets ranked FusedResults, not a deterministic parameter row — forcing rework. The real seam is ScopedKG (knowledge.scoped_kg.user_model) or EntityKG.claims_about(..., scope='user:<id>').

### [TK-42] Required read-seam inputs are unprovisioned (Q-29 pattern). The actual read needs an injected EntityKG (or ScopedKG view) plus a scope id derived as 'user:<user_id>'. ratings_for(item) takes only an item — nothing supplies which user_id/scope to read, and the ticket does not declare the EntityKG/ScopeRegistry injection. If routed through scoped_kg.user_model() it additionally requires an owner and mints an S7 write-token, conflicting with the 'pure read / no mutation' non_goal.
**Why:** No single-user assumption is stated anywhere in the ticket or deps, so the constructor's required seams (graph-store handle + user_id source) are undefined — the Q-29 failure mode of a method whose required substrate seams no ticket provisions. Cannot build cleanly until the EntityKG injection and user_id/scope-id source are named.

### [TK-44] Q-29-class missing provisioning: the user_model write seam requires an EntityKG + ScopeRegistry + a declared user Scope + ScopeWriteToken (single-writer S7) + a SourceDeclaration/epistemic_type per call. TK-44 names NONE of these and depends only on TK-43 (claim vocab) and TK-41 (rating vocab). No referenced ticket constructs the user-scope ScopedKG. Without it ObservationWriter cannot be instantiated or wired.
**Why:** Exactly the Q-29 failure mode: required constructor/seam inputs that no ticket provisions. A build pass hits an un-obtainable ScopedKG (needs kg+registry+owner) and stalls or invents ad-hoc wiring that violates the single-writer contract.

### [TK-44] AC1 ('a Neo4j node ... is created') and AC3 ('Neo4j is unavailable ... error logged and re-raised') hard-code Neo4j, contradicting the locked cold-boot frame (TK-14/Q-29: default is cog-worx in-memory doubles, real timescale/neo4j/pg as a config swap). The default EntityKG is InMemoryEntityKG, so these ACs are not runnable as written.
**Why:** An AC must be a checkable 'then' under the default build configuration. AC1's check is impossible on the zero-infra default and AC3 can't simulate 'Neo4j unavailable' against an in-memory double. Forces a contradictory infra dependency or a rewrite at build time.

### [TK-45] AC6 ('the user scope is queried for outcome claims over a 7-day window ... a Neo4j query runs ... all labeled items appear with their outcomes and event_ids') references a READ/QUERY capability over outcome claims that NO ticket in scope produces. TK-45's complexity_budget is 'One OutcomeLabeler class writing... via ObservationWriter' and files_in_scope is only outcome_labeler.py. TK-44's ObservationWriter is write-only (record). The consumer (RatingTuner, TK-49) is the natural owner of this query, and it is not a dependency here.
**Why:** Q-29 class: an acceptance criterion that gates 'done' on a capability nothing in the ticket (or its deps) constructs. A build pass would either fail AC6 or silently bloat scope by inventing an outcome-query path inside the labeler, contradicting the 'labeling only' non_goal.

### [TK-45] AC2 requires the terminal OUTCOME_<signal> claim to be 'written... SUPERSEDING the PENDING marker.' The supersede/Defeat operation exists in cog-worx (substrate.coherence Defeat -> SUPERSEDES) but is a coherence-reconciler defeat, NOT the create-a-node write that TK-44's ObservationWriter.record exposes (TK-44 AC1: 'a Neo4j node is created'). Neither TK-45's complexity_budget nor TK-44 declares a supersede/defeat write surface.
**Why:** Will force rework: the builder will discover that 'supersede PENDING' is not expressible through the declared ObservationWriter seam and must either widen TK-44 or wire a coherence-defeat call, neither of which is provisioned. Undeclared capability requirement against the deps as written.

### [TK-50] Feedback-signal consumption seam is unprovisioned: TK-50 reads 'queued TK-51 feedback' OFF-PATH NIGHTLY (AC3, AC4, intent, complexity_budget), but TK-51 enqueues FeedbackSignal via the SAME SourceRegistry.enqueue path as every other input (TK-51 AC2, explicitly 'no special-cased feedback path'). The SourceRegistry queue is the HOT-PATH drain queue — the drain pathway consumes and removes items as it runs. No ticket provisions a durable feedback-signal sink that survives from hot-path capture to the nightly TK-50 read. As written, feedback items are either drained on the hot path (so TK-50 has nothing 'queued' to consume nightly) or require a separate durable feedback store that no ticket builds.
**Why:** AC3 and AC4 (the override/fold behavior and the non-degenerate gradient that proves the ISS-6/RISK-7 mitigation) cannot pass without a place where captured feedback persists until the nightly pass. This is a Q-29-class seam: a required handoff between TK-51's capture and TK-50's nightly consumption with no owning artifact. Forces rework on whichever of TK-50/TK-51 lands second, and risks the whole RISK-7 mitigation being structurally unreachable.

### [TK-50] The inference-only REGRETTED rule needs Gmail-draft-deletion observation that no ticket produces. TK-50's named REGRETTED inference is 'a drafted reply whose Gmail draft the user later deleted unsent -> REGRETTED' (complexity_budget). Drafts are CREATED (TK-78) but no ticket observes/snapshots draft existence over time to detect a later deletion. There is no Gmail-draft-state poller/observer; the only Gmail reads in scope are ingest-side message reads. AC4 requires the fixture distribution to contain >=1 REGRETTED and explicitly allows 'inference-only' items, so the inference path must be able to produce REGRETTED, not only the source=feedback path.
**Why:** Without a draft-deletion observation input, the only producible REGRETTED is via explicit TK-51 feedback (not_useful). If AC4's fixture relies on an inference-only REGRETTED, the engine has no observable signal to compute it from and the AC is unsatisfiable as drawn. At minimum this forces either a new draft-observer ticket or an AC/complexity_budget narrowing to 'REGRETTED is feedback-only in v1'.

### [TK-51] Undeclared dependency on TK-3. AC-2 requires SourceRegistry.enqueue, SourceRegistry.list_sources(), and source registration. SourceRegistry is produced by TK-3 (src/wombat/sources/registry.py), but TK-51 depends_on lists only TK-44, TK-12, TK-147, TK-161. TK-161 produces only the InputSource Protocol (sources/base.py), not the registry.
**Why:** Q-29 class: an AC leans on a capability (the registry's enqueue/list_sources path) that no DECLARED dependency provisions. A build pass that pulls only the lean briefing for TK-51's declared deps will not have the registry seam, forcing rework. Fix: add TK-3 to depends_on.

### [TK-46] No recurring-nightly-trigger mechanism is named, and cog-worx provides none. The ticket's framing ('registered on a cog-worx nightly Wait timer', AC3 'the next nightly timer is scheduled regardless') assumes cog-worx offers recurring/cron scheduling. It does not: Wait is one-shot and explicitly 'never re-arms' (cogworx.loop.result.Wait / engine.py C6). Firing 'once per night, every night' requires either a self-rescheduling pathway (each run, on completion, starts/arms the next night) or an external nightly scheduler — and which approach, plus where the re-arm lives, is unspecified. The sibling brief epic made this its own ticket (TK-97 'once-daily trigger'); EP-13 has no equivalent, so TK-46 must invent the recurrence mechanism with zero design guidance. Will force rework once the builder discovers Wait can't recur on its own.
**Why:** Build will stall or guess: a naive 'register a Wait timer' produces a single fire, not a nightly recurrence, silently failing AC3 and the FEAT-4 'nightly consolidation' goal.

### [TK-46] AC2's 'no duplicate run is started for that night' has no defined idempotency key. cog-worx Engine.start_run is idempotent on run_id (and the journal won't object to a repeat), so the correct construction is to key the run_id on the night (e.g. dream-<date>). But neither the AC, complexity_budget, nor files_in_scope specify this key or where it is computed/derived (note: project item-identity is idempotency_key=f(source_id,natural_id) per TK-12/Q-18, but that is for QUEUE ITEMS, not nightly runs — there is no analogous run-key convention). As written the AC is not checkable: a builder could implement a fresh-UUID-per-fire run and still claim 'looks done,' yet a crash-during-fire could double-start.
**Why:** Without a stated per-night run-key, the crash-safety AC (AC2) cannot be implemented or tested deterministically; this is the exact Q-29-class 'capability assumed, not provisioned' trap for the once-per-night fence.

### [TK-47] Sweeper-vs-stage shape mismatch. Both ACs assume a stage that 'completes' / 'returns no changes (clean night)'. Real semantics: reconciler.tick() processes batch_limit subjects and returns ReconcilerStats; extractor.tick() returns an int and stalls (not 'completes') on failure. A correct dream pass must loop both ticks to drain, with its own termination + idempotency-per-night logic — unscoped work that the 'thin wiring' budget and the ACs do not account for.
**Why:** Will force rework: the 'completes/zero-change' acceptance checks have no direct seam to assert against, so a builder must invent run-to-drain orchestration the ticket never authorized.

### [TK-47] Undeclared dependencies. The reconciler/extractor inputs come from the memory/recall write+read layer (EP-10/EP-11, TK-42/TK-43 entity-KG/user-scope) and the Engine/model wiring (TK-1, plus TK-14 substrate, and a ModelRegistry). TK-47 declares only TK-46. depends_on is materially incomplete.
**Why:** Scheduling/ordering will be wrong and the ticket can be picked up before its real prerequisites exist, guaranteeing a failed build.

### [TK-49] The clamp source ('clamps to floor/ceiling from TK-48') is unprovisioned. TK-48 is engineering_level=prototype, a standalone SPIKE whose explicit non-goals are 'No Neo4j or Postgres I/O' and 'No production tuner logic — prototype only', producing only 'a numeric stability report'. It RECOMMENDS floor/ceiling values but persists no queryable/typed bound store. TK-41 (RatingParams vocabulary) does not define floor/ceiling fields, and TK-13 (param store) explicitly disclaims the adaptive RatingParams (line 986). No ticket produces the runtime artifact TK-49 reads its clamp bounds from.
**Why:** Q-29-class: a required input (the floor/ceiling bounds the tuner clamps against, the very thing that makes the AC2/AC3 'equals the ceiling/floor' tests meaningful) has no owning ticket. A builder must invent where bounds live (hardcode? extend TK-41? new field on RatingParams?), forcing rework once the answer is decided.

### [TK-49] Undeclared direct dependency on TK-44 (ObservationWriter). TK-49's complexity_budget states it 'writes updated RatingParams via ObservationWriter provenance path' and AC1 asserts a provenance-stamped write, yet TK-44 is absent from depends_on. It is only transitively present via TK-45->TK-44 and TK-50->TK-44.
**Why:** depends_on is dishonest about the write seam TK-49 directly invokes. If dependency ordering is driven off depends_on, TK-49 could be scheduled/briefed without the write seam guaranteed, and the omission hides a real build coupling.

### [TK-72] AC#3 dedup-by-event_id contradicts the canonical identity frame and depends on an undeclared ticket (TK-12). TK-72 AC#3: 'WombatQueue deduplicates by event_id'. The locked frame (item identity = idempotency_key = f(source_id, natural_id), TK-12/Q-18) and TK-12's own AC#4 (contract.yaml lines 361-363) require that TK-72's dedup path resolve through the canonical idempotency_key derivation, with source_id as part of the key so a calendar event and a Gmail message sharing a raw id string never collide. Building dedup on raw event_id directly violates this and re-introduces the exact three-independent-keying-schemes problem Q-18 resolved. TK-12 is not in depends_on (lists only TK-71/EP-2/EP-3).
**Why:** A build pass taking AC#3 literally would key the queue on bare event_id, contradicting TK-12 and forcing rework once TK-12 lands; it also silently breaks outcome-label binding (TK-45/TK-50) which key on the canonical idempotency_key. Either TK-72 must depend on TK-12 and AC#3 must be reworded to 'dedup via canonical idempotency_key = f(source_id='gcal', event_id)', or TK-12 must be sequenced first.

### [TK-75] AC2 (taint latch ACTIVE during the read phase) and AC3 (external Capability not reachable from the SAME tainted ingest drive) assert drive-scoped ToolGate/TaintState properties, but TK-75's scope (auth.py/poller.py/models.py) describes a plain Gmail-API poll loop that maps messages to GmailMessageItem and enqueues to WombatQueue. The ticket never names where the poll path becomes a cog-worx drive Stage, nor which Stage binds the StageToolPolicy/asserts taint. That drive-Stage seam is provisioned by TK-148 (src/wombat/stages/ingest_email_body.py), a declared dep — but TK-75 gives the taint/capability ACs no home in its own files. Q-29 class: an AC referencing a capability (a tainted drive with a bound ToolGate) whose provisioning seam is unnamed in this ticket.
**Why:** A builder taking TK-75 alone will produce a poller with no cog-worx drive, so AC2/AC3 cannot be written as runnable checks (no TaintState/ToolGate to inspect) — forcing rework to either pull the IngestEmailBody Stage into TK-75 or relocate the ACs to TK-148. The drive-boundary split (ISS-3) is correctly decided, but the ingest-side Stage ownership between TK-75 and TK-148 is ambiguous.

### [TK-77] TK-77 emits 'typed TaskItem QueueItems' for the gate to surface, but there is no 'task' member in the canonical item_kind enum. TK-21 defines item_kind as a CLOSED Enum {brief, reflection, draft, generic} and asserts no module may define a competing kind vocabulary; TK-10 (ComposeDispatchRouter) routes only those four kinds (unknown -> generic ComposeStage with a warning). TK-77 does not depend on TK-21 and never states which item_kind a TaskItem carries.
**Why:** Without a declared item_kind, a TaskItem either silently falls through to the 'generic' composer (likely wrong for an action-item-with-deadline) or the builder invents a 'task' kind that violates TK-21's closed-enum invariant. The ticket's own intent ('so the gate can surface tasks the user must act on') is unprovisioned: no AC verifies the TaskItem is routable/surfaceable. Build will either pick the wrong route or break the canonical-vocabulary test.

### [TK-77] Undeclared dependency on TK-12 (Canonical item identity). TK-12's AC explicitly names 'a TaskItem extracted from a Gmail message (TK-77)' and requires its ItemRef(source_id, source_natural_id) to link the extracted-task identity back to the parent message_id recoverably. So TaskItem MUST carry an ItemRef / canonical idempotency_key. TK-77 does not list TK-12 in depends_on, never mentions ItemRef or idempotency_key, and its files_in_scope (task_extractor.py) contains no identity wiring.
**Why:** The two tickets reference each other but TK-77's dependency edge and the identity field on TaskItem are missing. A build of TK-77 in isolation would emit TaskItems with no canonical key/ItemRef, breaking dedup (TK-2), gate pending-set identity (TK-21), and outcome-label binding (TK-45/TK-50) — exactly the cross-source id-reconciliation gap Q-18 closes. This forces rework once TK-12 lands.

### [TK-96] Undeclared dependencies / inverted decomposition: TK-96's ACs require the Gather, DetectConflicts, ForceFlush, Compose, and Deliver stages, but the tickets that build them (TK-98 Gather, EP-16 DetectConflicts, TK-99 ForceFlush, TK-100 Compose, TK-101 Deliver) are absent from TK-96.depends_on (only epics listed). The sibling chain runs downstream (TK-99->TK-98, TK-100->TK-99, TK-101->TK-100), so the concrete stages the skeleton needs are produced AFTER it in the graph.
**Why:** A build pass on TK-96 cannot satisfy AC-1/AC-2 without those stage implementations; either TK-96 must declare them as deps (and shrink to pure wiring) or it re-implements them, colliding with TK-98/99/100/101. Effort is wasted until the dependency direction is fixed.

### [TK-96] File-ownership collision: TK-96.files_in_scope lists src/wombat/stages/brief_gather_stage.py (owned by TK-98), brief_compose_stage.py (owned by TK-100), and brief_deliver_stage.py (owned by TK-101). Three modules are each claimed by two tickets with no statement of who is authoritative.
**Why:** Two tickets writing the same module is a merge/rework hazard and an atomicity violation — the contract never says whether TK-96 stubs these files or TK-98/100/101 own them, so a builder cannot know what to create vs. import.

### [TK-96] Q-29-class artifact gap in AC-2: the AC requires 'the decision artifact assembled by ForceFlush' (the sealed BriefDecisionArtifact), but TK-96 has NO ForceFlush file in scope (no brief_force_flush_stage.py, no brief_decision_artifact.py) and does not depend on TK-99, the only ticket producing both ForceFlushStage and the BriefDecisionArtifact type.
**Why:** The acceptance criterion references a capability (forced gate flush) and a typed artifact (BriefDecisionArtifact) that nothing in TK-96's scope or declared deps constructs — the exact unprovisioned-seam pattern Q-29 was created to catch. The complexity_budget's 'stubs acceptable' note covers conflict/mouth but NOT ForceFlush, which AC-2 treats as real.

### [TK-97] Undeclared runtime dependency: a RUNNING Sweeper loop is required for AC1/AC2 to pass, but no ticket in TK-97's depends_on (TK-96, TK-152, TK-13, EP-9) owns instantiating cogworx.runtime.sweeper.Sweeper and running its poll loop (run_forever). The composition-root (TK-1) explicitly excludes 'loop code' and only builds the Engine + empty PathwayRegistry; TK-14 builds only the journal/substrate stores. A cog-worx Wait that is armed but never swept never wakes — so 'fires exactly once when morning arrives' (AC1) and 'Sweeper runs on wake' (AC2) are unsatisfiable until some component runs the Sweeper.
**Why:** This is the Q-29 pattern: a required cog-worx seam that exists but is unprovisioned by any declared owner. TK-97's complexity_budget scopes only timer+ledger+pathway, so either TK-97 must take ownership of Sweeper startup (widening its scope) or depend on a runtime/process-boot ticket that runs the Sweeper — and no such dependency is declared. The same gap also affects the dream timer (TK-100) which consumes the identical Sweeper, so a shared runtime-boot ticket is the natural home.

### [TK-98] AC-3 ('the payload is written to the cog-worx journal BEFORE the stage returns, so crash-resume does not re-fetch') is mis-specified against the real seam. A Stage does not journal its own output; it returns a StageResult and the ENGINE calls Journal.commit_step after run() returns. Exactly-once gather comes from the engine committing the returned Artifact, not from the stage writing the journal in-band.
**Why:** If a builder takes the AC literally they will inject ctx.journal.commit_step() inside BriefGatherStage.run() — duplicating/racing the engine's own commit and breaking the positional (run_id, step_index) exactly-once contract. This forces rework and risks corrupting crash-resume. The AC must be restated as 'returns its BriefPayload as the stage's Artifact output so the engine journals it' to be buildable. (Q-29 class: an AC referencing a capability the named seam does not provide at that layer.)

### [TK-99] depends_on lists EP-9 but should declare the specific producer of the selection seam (TK-27) and the BriefPayload-source tickets it actually consumes are only reachable via TK-98 — yet the 'detected conflicts' the intent feeds to ForceFlush come from EP-16, which TK-99 does NOT depend on (only TK-98 depends on EP-15/EP-17; conflict detection is EP-16, declared as a TK-99 dep but TK-98 explicitly defers conflicts to EP-16).
**Why:** The intent says ForceFlush receives 'a BriefPayload and detected conflicts'. TK-98's BriefPayload carries 'raw conflict candidates' but EP-16 (declared dep) is what turns candidates into 'detected conflicts'. The handoff order (gather -> conflict-detect -> force-flush) is not pinned by a ticket that runs EP-16 between TK-98 and TK-99, leaving the conflict items' provenance into the artifact undefined.

### [TK-101] AC4 dedup mechanism is incoherent / under-provisioned. AC4 prevents duplicate delivery 'because the daily-run ledger (TK-97) ensures no second full brief is triggered' for a crash 'after writing the file but before journaling completion'. But the TK-97 daily-run ledger gates whether the brief PATHWAY fires once per wombat-day; it does not prevent THIS stage from re-appending the output file when cog-worx replays the stage mid-run on Engine.resume. Per TK-97 AC4 the ledger is marked run only AFTER Deliver completes, so during the exact crash window AC4 describes the ledger is not yet set and the stage replays -> the file is appended twice. The real guard for intra-run replay is journal-backed exactly-once stage replay (the cog-worx journal), not the daily-run ledger. As written, AC4 is not provable and the file-append is not idempotent.
**Why:** Q-29-class: an acceptance criterion references a guarantee that no provisioned mechanism actually delivers for the stated failure window. Build would either over-trust TK-97 or silently double-append the brief file on crash-resume.

### [TK-101] Undeclared dependency on TK-97. AC4 explicitly relies on the TK-97 daily-run ledger, yet depends_on = [EP-30, TK-100] and does not list TK-97. Even setting aside the coherence problem above, the ledger artifact this AC names is produced by a ticket not in the dependency set.
**Why:** An AC cannot be verified against a capability from a ticket outside depends_on. Either TK-97 must be added to depends_on, or AC4 must be rewritten to rely on the cog-worx journal (which IS reachable via StageContext) for stage-level idempotency.

### [TK-111] Identity key inconsistency: AC1 makes the upsert idempotent on 'item_id' and stores a bare 'source_id' column, but the locked frame and TK-12 define canonical identity as idempotency_key = f(source_id, natural_id) / ItemRef(source_id, source_natural_id). TK-111 invents an undefined 'item_id' key and omits natural_id; TK-12 is NOT in depends_on.
**Why:** The log must bind to the same canonical identity that downstream consumers (TK-112, EP-14/RatingTuner, and the OUTCOME_* claims from TK-45/TK-50) key on. An ad-hoc item_id/source_id key cannot cleanly join the outcome corpus and will force rework. Undeclared dependency on TK-12.

### [TK-111] Motive-free enforcement duplicates and contradicts the EP-11/TK-43 schema wall with a different mechanism. AC2 mandates RUNTIME 'schema validation at insert time' raising SchemaViolation; TK-43 (parent EP-11) already defines the motive-free vocabulary as a TYPE-LEVEL wall (Enum + mypy, raising TypeError) and explicitly states it is 'structural, not a runtime string check'. TK-111 neither depends on EP-11/TK-43 nor reuses it.
**Why:** Two competing motive-free enforcement mechanisms with no decision reconciling them is a contradiction against an existing constraint owner (CON-6/NG-1). Either TK-111 should reuse the TK-43 predicate wall (declare the dep) or a decision must justify a second runtime gate. Undeclared dependency + mechanism contradiction = rework risk.

### [TK-112] AC #2 is incoherent: 'when UserModel.ratings_for() is called the next morning, then the gate can read the most recent WindowSummary'. UserModel.ratings_for(item) (TK-41/TK-42) returns RatingParams for an event class — it has no contract, signature, or return type that yields a WindowSummary. The acceptance criterion cannot be made to pass against the seam it names.
**Why:** Builder will either (a) implement against ratings_for() and produce a wrong/nonsensical coupling, or (b) be blocked. This is a Q-29-class mismatch between an AC and the actual capability of the seam it invokes.

### [TK-112] No ticket provisions a WindowSummary READ-BACK path. TK-112 builds the write (write_window_summaries.py) but AC #2 asserts a reader ('the gate can read the most recent WindowSummary without a full log scan'). EP-10/TK-42 reads RatingParams, EP-11/TK-44 writes Claims; neither reads WindowSummary. The capability AC #2 depends on is built by nothing.
**Why:** An AC referencing a read capability that no ticket produces is exactly the unprovisioned-seam class. Either the read path must become its own ticket/dependency, or AC #2 must be rewritten to a check this ticket can satisfy (e.g. row persisted + queryable).

### [TK-113] Queue-item kind naming mismatch breaks the end-to-end reflection path. TK-113 AC1/AC4 enqueue a QueueItem of kind='pattern_reflection', but the canonical closed enum (TK-21/ISS-4) and the dispatch router (TK-10 AC2) route on item_kind=reflection to reach ReflectionComposeStage (TK-114).
**Why:** If the item is stamped kind='pattern_reflection' (wrong field name and wrong value) instead of the canonical item_kind=reflection, TK-10's router hits its 'unknown kind -> generic ComposeStage + warning' fallback and TK-114 is never invoked — the FEAT-8 single-reflection path silently dies. Field name (kind vs item_kind) AND value (pattern_reflection vs reflection) must both conform to the TK-21 canonical enum.

### [TK-114] Hint-delivery mechanism contradicts the real cog-worx context-contributor contract. TK-114 AC1 says the Stage calls the mouth 'with ... the resolved KB phrasing hints' (i.e. the Stage reads hints as data and passes them), while TK-118 wires them via a cog-worx ContextContributor and claims it injects them 'into the Stage context under key kb_phrasing_hints'. But the real ContextContributor Protocol (cogworx.context.contributor) returns SlotContent text CHUNKS into a named slot consumed by the ContextAssembler under a token budget — it does not expose an arbitrary keyed 'kb_phrasing_hints' value for the Stage to read. So it is unspecified and contradictory whether TK-114 obtains hints as data (coupling directly to TK-117, which TK-118 explicitly tries to avoid) or whether they arrive as assembled prompt slot text the Stage never sees as data. A builder cannot satisfy both AC1's 'pass hints to the mouth' and TK-118's contributor model without picking one and reworking the other.
**Why:** Constructibility: the two adjacent tickets describe incompatible data flows for the same hints, and neither matches the actual contributor seam they cite. Building TK-114 to AC1 then wiring TK-118 will force a rework of the mouth-call signature.

### [TK-118] 'Scoped to ReflectionCompose only' (AC3) has no per-Stage registration mechanism. ContextAssembler.register binds one contributor per slot name globally on an assembler instance; cog-worx offers per-stage policy binding (bind_context_policy) but not per-stage contributor selection. The ticket asserts stage-scoped invocation without naming how (a dedicated reflection-path assembler/slot) or declaring the dependency that builds it.
**Why:** Will force rework: a naive register() makes the contributor fire for ALL Compose stages sharing the assembler, violating AC3 and risking KB-coupling leaking into morning-brief/other Compose paths the non_goals forbid. The provisioning decision (separate assembler vs custom slot vs gating inside contribute on request content) must be settled before build.

### [TK-118] 'inject the hints into the Stage context under key kb_phrasing_hints' (AC1) maps to no real cog-worx mechanism. Contributors return SlotContent (slot-bound text chunks) assembled into banded ChatMessages -- there is no key-addressable context dict. TK-114's AC1 mirrors this by expecting to receive 'resolved KB phrasing hints' as a discrete input.
**Why:** The AC describes an artifact (a context entry under key 'kb_phrasing_hints') that the seam does not produce, so the 'then' is unverifiable as written. The wombat team must re-express the contract as 'hints rendered as chunks into a designated slot whose text the mouth's system prompt consumes' -- and TK-114 must agree on that shape. Cross-ticket interface drift; rework on both TK-118 and TK-114 if not reconciled first.

### [TK-131] complexity_budget mandates a 'sync open/close/navigate/snapshot API' but the cog-worx Capability seam dispatches only via `async def invoke(self, args)`. A synchronous capability cannot conform to the async Protocol, so the build target as written is unbuildable against the real seam.
**Why:** cogworx.capability.base.Capability.invoke is async and is the sole dispatch method Registry/Engine use. The ticket's own open POC ('does the lifecycle API need a wrapper?') admits this is unresolved, yet the budget hard-codes 'sync', forcing a contract violation or rework.

### [TK-131] AC-3 ('engine torn down or close() called -> session closes cleanly, no zombie processes') and AC-2's 'closed on teardown, managed by the Capability' rely on an Engine->Capability teardown seam that does not exist in cog-worx.
**Why:** The Capability Protocol has no close/__aexit__; Engine.aclose() only drains the in-flight task registry and never invokes any capability close hook. The teardown guaranteeing no zombie Chromium is unprovisioned by EP-1 or any cited dep — a Q-29-class required-but-unprovisioned seam. wombat must own teardown wiring this ticket does not scope.

### [TK-133] AC-1 model mismatch with the actual taint seam. AC-1 requires that 'when the Stage begins execution, the taint latch is activated and external tool calls are dropped for the rest of this Stage, and THEN the Stage proceeds to navigate and read.' cog-worx provides no on-entry latch primitive: TaintState only latches as a side effect of dispatching an external-tier or 'untrusted-source'-tagged capability (policy.py:128-146, router.dispatch_one). The buildable realization is to register/tag the PlaywrightCapability read as 'untrusted-source' so taint latches when the read is dispatched — i.e. during the read, not strictly before navigation.
**Why:** A builder taking AC-1 literally will look for a 'latch.activate()' call that does not exist and either invent bespoke taint machinery (violating DEC-19/S10 'reuse cog-worx structural latch' and forbidden by NG on TK-148/TK-153 'no new taint machinery') or mis-sequence the latch. The AC must be reworded to the tag-the-read-capability mechanism before build.

### [TK-133] Undeclared dependency on TK-148's shared taint convention. The sibling ticket TK-153 (also EP-25) explicitly states the web ingest call-site MUST reuse TK-148's taint.py and the 'untrusted-source' both-direction tag convention (depends_on TK-148, TK-133) and forbids re-implementing taint machinery. TK-133 introduces the same 'taint latch on entry + taint flag' behavior but depends_on only TK-131 + EP-28, NOT TK-148. If TK-133 builds first/independently it will create the very duplicate tagging machinery TK-153 was split out to prevent (Q-26 atomicity).
**Why:** Two tickets in the same epic provisioning the same untrusted-source tagging by different routes is a coherence break that forces rework. TK-133 should either depend_on TK-148 (the shared taint seam) or have its taint-tagging scope explicitly deferred to TK-153, with the tag convention named, before build.

### [TK-134] Undeclared pre-req for the test to pass green: for BrowseAndRead to dispatch the external-tier PlaywrightCapability at all (which is what latches taint), a Stage policy admitting the 'external' tier must be bound — otherwise cog-worx ToolGate raises TierViolation at dispatch (cog-worx default allowed_tiers={read,write}, external is opt-in). Q-10 resolved this via DEC-22 + per-stage admission policy TK-151 (P1, EP-28) plus a build-time enablement flag. Neither TK-134 nor its dep TK-133 declares TK-151 or any external-admitting policy setup, yet the fixture test cannot exercise BrowseAndRead -> Playwright nav (and therefore cannot observe taint=True or 'no external tool called') without it. The test fixture/setup must bind an external-admitting (or test-double) policy.
**Why:** AC1 asserts taint flag True AND no external tool called; both depend on an external-tier capability actually dispatching under an admitting policy. Without that wiring the Stage either refuses with TierViolation (test red for the wrong reason) or the taint trigger never engages — forcing rework once the runner hits the gate. This is exactly the Q-29-class 'required seam no ticket provisioned' pattern, here a required policy-binding the test silently assumes.

### [TK-135] AC4's bypass-closed guarantee is under-provisioned relative to the cog-worx gate's actual semantics. AC4 wants 'submit without an approved journal entry -> rejected', but cog-worx ToolGate.check_approval only fires when the drive is TAINTED and the capability is tagged consequential+irreversible. A form submission on an UNtainted drive (no untrusted content ingested) would pass check_approval freely, so cog-worx machinery alone does not close the path.
**Why:** If the builder assumes the cog-worx ApprovalRequired gate enforces AC4, the asserted-closed test will either fail or give false assurance. The guarantee must be built in the FormSubmitStage contract (dispatch_approved refuses to act without a human-approved journal entry/token) or via a wombat capability guard. files_in_scope lists only src/wombat/stages/form_submit.py — no capability-guard file — so the AC's 'or a Capability guard' branch has no provisioned artifact. The Stage-contract branch IS buildable in the named file, so this is rework-risk, not a hard block.

### [TK-146] Undeclared dependency + dependency inversion on the dispatch Stages. depends_on lists only EP-4, but all four ACs assert the execution of ComposeGmailDraft (TK-78), SubmitBrowserForm (TK-135), Speak (EP-4 ReviewOrSpeak), and dispatch_approved (TK-149). The complexity_budget commits to 'one call site per side-effectful Stage'. TK-149 — the ticket that actually creates the two-step dispatch pattern and these Stages — itself depends_on TK-146. So TK-146 must add call sites into Stage classes that do not exist when TK-146 is built, and its ACs cannot run.
**Why:** Q-29-class: an AC references a capability (the dispatch Stages and dispatch_approved) that NO ticket in TK-146's dependency closure produces; the build cannot satisfy the ACs in isolation, forcing rework or a hidden re-ordering.

### [TK-146] AC2 ('kill the process after the insert but before approval and assert the trail row is durable / survives restart') requires a real durable Postgres for the wombat-domain action_trail_projection table, but TK-146 declares no dependency on the real-adapter/config path (no TK-13 param store, no TK-14 real-substrate selection, no SubstrateSettings DSN). The locked frame's default is cog-worx in-memory doubles (zero infra), which do not survive restart. This is the same cold-boot/durability coherence problem already noted for the queue (TK-2).
**Why:** As written the durability AC is not constructible under the default cold-boot config and has no declared real-Postgres harness; the test would either be skipped or silently pass against a non-durable store, defeating the assertion.

### [TK-147] AC3 requires the renderer to append '  [CANCELLED <timestamp>]', but no ticket provisions a cancellation timestamp column. TK-146's schema (the only dependency) defines {action_id, action_type, human_summary, target, proposed_at, status} plus dispatched_at on dispatch — there is no cancelled_at. TK-149/TK-79 only set status='cancelled' with no timestamp. The renderer has no source for the CANCELLED <timestamp>.
**Why:** Q-29-class missing-seam: an AC references a value (cancellation timestamp) that the upstream projection schema never writes, so the build cannot satisfy AC3 as written. Either TK-146's schema must add cancelled_at (a contract change to a dependency), or AC3 must drop the timestamp. Forces rework across the ticket boundary.

### [TK-148] The ticket's mechanism model is wrong: it says 'tag each untrusted-content read Phase/Stage with TAINT_UNTRUSTED' and 'when the Stage completes the taint register is set'. In cog-worx, taint latches structurally inside ToolGate when a registered CAPABILITY whose tags include 'untrusted-source' is INVOKED through the gate (TaintState.update). There is no Stage/Phase-level taint setter. The wombat email-body read must therefore be a registered, gate-dispatched read-tier Capability tagged 'untrusted-source' — not a plain Python read of an email string.
**Why:** If ingest_email_body.py just reads a string and returns, nothing ever calls the gate and the latch never fires — AC1/AC4 silently pass-by-omission (cog-worx does NOT error on an untagged read; CF-3.2-B confirms untagged read-tier ingestion does not taint). This forces rework of how the ingest Stage is structured and where the tag is attached, and demands a wombat-side enforcement that an untrusted read MUST go through the tagged capability.

### [TK-148] AC5 requires a trail entry with action_type='blocked_by_taint' and labels it '(TK-146 integration)', but TK-146 (the sole declared dependency) does not produce that artifact. TK-146's schema/writer only emits rows with status in {pending,dispatched,cancelled} and action_types tied to dispatch stages (draft/form/speak); there is no 'blocked_by_taint' action_type or a write path for a gate-refusal event in TK-146's files_in_scope.
**Why:** AC5 references a capability no ticket builds. Either TK-146's schema must be extended to admit a 'blocked_by_taint' action_type plus a refusal-write call site, or TK-148 must own that writer — currently neither does, so AC5 cannot pass cleanly. Q-29-class: a required artifact with no provisioning ticket.

### [TK-149] AC-5 asserts cog-worx ToolGate raises ApprovalRequired when a pathway bypasses AwaitHuman and calls a side-effectful external Capability directly. But the real ToolGate.check_approval (policy.py:268) raises ApprovalRequired ONLY when the drive is tainted AND the capability is tagged consequential+irreversible AND not approved. The dispatch case is a fresh UNTAINTED drive (per TK-151 AC-1), so check_approval returns silently and ApprovalRequired never fires. The mechanism that actually refuses a direct/unadmitted external call is TierViolation from check_dispatch (tier gate), or — if the stage IS tier-admitted — nothing structural stops the direct call at all. As written this AC is not a runnable check and encodes a wrong belief about the cog-worx safety mechanism.
**Why:** Q-29-class seam-misuse: the named seam exists but its firing condition is misstated, so the safety guarantee the ticket claims (no direct side effect without approval) is not actually enforced by the cited mechanism on the relevant code path. A build pass would write a test that cannot pass or wire the wrong guard.

### [TK-149] Undeclared dependency on the external-tier admission binding (TK-151 / DEC-20 / DEC-22). AC-2 requires dispatch_approved to make exactly one Gmail API draft-create call (an external-tier capability). Per cog-worx the engine default is allowed_tiers={read,write}; without the TK-151 Stage-scoped {read,write,external} policy bound to the dispatch stage (and the cog-worx external-tier flag enabled), check_dispatch raises TierViolation and the approved dispatch is refused. depends_on lists only TK-146 and EP-4.
**Why:** An AC relies on a capability (external-tier admission) that no declared dependency provisions; building TK-149 against current deps would fail the approved-dispatch AC with TierViolation. Declaring the TK-151 dependency (or sequencing) is required for a clean build.

### [TK-150] Undeclared dependency on the DeepSeek-egress capability. AC2 asserts 'a Compose Stage calls the DeepSeek API ... exactly one outbound call to the DeepSeek base_url and zero writes to any other remote host.' That ComposeStage and the configured DEEPSEEK_BASE_URL are produced by EP-5 (the line-611 'Mouth wiring — DeepSeek via S4 + ComposeStage' ticket) and config.py (TK-2). Neither EP-5 nor TK-2 is in depends_on (EP-1, EP-15, EP-17).
**Why:** Q-29-class: an AC references a capability (DeepSeek phrasing egress via ComposeStage) that no DECLARED dependency provisions. Forces rework or a hand-stubbed test that does not prove the real egress path / known base_url.

### [TK-150] Cold-boot vs real-adapter config ambiguity makes AC1 vacuous in the default config. The locked frame and TK-14 establish the v1 default as cog-worx in-memory doubles with ZERO DB writes ('a test constructs an Engine from the bundle with no database running'). AC1 asserts 'every sqlalchemy/psycopg/neo4j write resolves to a SAME-HOST peer.' In the cold-boot default there are no such writes to intercept, so AC1 trivially passes without exercising the guard; the ticket never states it must run against the real-adapter substrate path (TK-14's real factory) where the guard is meaningful.
**Why:** Cold-boot/queue coherence class: the residency assertion is only load-bearing on the real-adapter path, but the ticket does not pin the config it runs against, so the build could ship a green-but-meaningless AC1.

### [TK-151] Binding location contradicts the cog-worx seam. TK-151 says the admission is 'one policy-binding in the composition root (TK-1)' with files_in_scope=bootstrap.py, but cog-worx binds tier policy PER-STAGE: the engine reads getattr(stage,'tool_policy',None) (engine.py:775) and there is no composition-root stage→policy registry. To admit external on DraftComposer/browser/form-submit you must set those Stages' tool_policy attribute, which lives in TK-78/131/133/135 — explicitly non-goals here. As scoped, bootstrap.py cannot reach the seam that actually grants the tier.
**Why:** A build pass against bootstrap.py will find no cog-worx hook to attach a per-stage policy; the implementer must instead touch the dispatch Stage classes (out of scope / undeclared deps) or invent a non-existent wombat indirection layer. Q-29-class: a required seam is provisioned in the wrong place.

### [TK-151] The 'build-time external-tier flag' (AC-3, intent, complexity_budget, DEC-22) is not a real cog-worx artifact. cog-worx has no runtime/deploy config flag for the external tier — opt-in is purely StageToolPolicy.allowed_tiers containing 'external'. No ticket produces such a flag.
**Why:** AC-3 ('flag NOT enabled → call refused, proving no silent self-grant') references a capability/artifact nothing builds. The intended fail-closed property is real but is achieved structurally (a stage simply not carrying external in allowed_tiers), so the AC as written tests a phantom. Either delete the flag fiction or redefine the AC against the real default-policy mechanism.

### [TK-151] Undeclared dependencies. AC-1/AC-2 require a runnable dispatch Stage that invokes gmail.drafts.create (external) and a non-dispatch Stage, plus a REGISTERED external Capability in the cog-worx Registry. depends_on lists only TK-1/TK-148/TK-149 — not TK-78 (the DraftComposer/gmail.drafts.create capability) nor whatever registers the external Gmail capability.
**Why:** The positive admission AC and the Q-10 negative AC cannot pass without TK-78's stage + a registered external capability. A build pass will block on missing artifacts that the contract does not declare as deps (or it will stub them, making the 'runnable check' vacuous).

### [TK-161] Duplicate/conflicting definition of the InputSource contract in the same file as TK-5. TK-5 (parent EP-3, 'SourceRegistry + InputSource registration contract') has files_in_scope src/wombat/sources/base.py + src/wombat/sources/registry.py and AC: 'a stub InputSource ... is registered ... translated to a QueueItem and enqueued into WombatQueue'. TK-161 also defines a Protocol/dataclass InputSource in src/wombat/sources/base.py. Two tickets author the same artifact in the same file with potentially different signatures (TK-5 emphasizes id/poll/lifecycle; TK-161 emphasizes id/push/poll). Building both as written causes a redefinition collision and a contract-shape contradiction.
**Why:** A build pass on TK-161 would either overwrite or conflict with TK-5's base.py, or two divergent InputSource shapes would exist. Either way rework is forced and the 'one uniform interface' goal (DEC-5/DEC-14) is undermined.

### [TK-162] SourceRegistry.enqueue() is an unprovisioned public seam. TK-162 AC1 (and the feedback ticket at line 1194) assert items reach WombatQueue 'via SourceRegistry.enqueue()' as a public call site, but the canonical SourceRegistry ticket TK-5 (parent EP-3) defines only start()/stop() and enqueues internally from poll(). No ticket declares a public enqueue() method.
**Why:** Q-29-class: an AC's checkable 'then' references a method nothing in depends_on builds. The builder will either invent the method ad hoc (drift from the canonical registry) or discover the seam is missing mid-build and rework. The registry's public push/enqueue surface must be declared on a SourceRegistry-owning ticket first.

### [TK-162] Push-driven registration is unprovisioned and contradicts the canonical registry. TK-162's ASR is inherently push-based ('listens on a push channel'); TK-161 adds a push callback to the base InputSource contract. But TK-5 — the ticket that actually implements SourceRegistry — explicitly non-goals push ('Does not implement push/webhook sources (poll suffices for v1)'). TK-162 depends only on EP-3 generically, with no ticket wiring push events into the registry/queue.
**Why:** Direct contradiction between this ticket's mechanism and the registry its dependency builds. AC1 ('a spoken utterance ... appears on WombatQueue via SourceRegistry.enqueue()') cannot pass against a poll-only registry. Needs a declared ticket that adds push support to SourceRegistry, then TK-162 must depend on it.

### [TK-163] Push-enqueue path is named but not provisioned by the dependency chain. TK-163 AC1 fires 'the adapter's push callback' and asserts a QueueItem lands on WombatQueue via 'the same enqueue() call site as other sources'. TK-162 AC1 makes the same push->SourceRegistry.enqueue() claim. But the SourceRegistry definer, TK-3 (EP-3), implements a poll-loop only and explicitly lists 'Does not implement push/webhook sources (poll suffices for v1)' as a non_goal. TK-161 adds a 'push' callback to the InputSource protocol but no ticket wires push -> enqueue in the registry. So the capability AC1 tests (fire push callback -> item enqueued) is produced by NO ticket in the chain and is disclaimed by the one ticket that owns the registry.
**Why:** Q-29-class: an AC references a capability (push-driven enqueue) that the named provisioning ticket explicitly excludes. A build pass would either fail the AC or be forced to add an unscoped push path to SourceRegistry, contradicting TK-3's poll-only design. Resolve by either (a) adding a push-enqueue seam to TK-3/TK-161's scope and declaring it, or (b) rewording TK-162/TK-163 to drive the source via the poll path that TK-3 actually builds.

### [TK-164] Undeclared dependency on TK-21. AC-1 and AC-2 consume 'action=surface_immediate or surface_flush, canonical enum' and 'action=hold', and ISS-4's resolution explicitly states 'TK-164/TK-165 all import GateAction/item_kind from this module' (src/wombat/gate/models.py, owned by TK-21). But TK-164.depends_on is only [EP-9, EP-5] — TK-21 is absent. The sink cannot import the canonical vocabulary it keys off without TK-21, yet the dependency is not declared (Q-29 class: a required seam the ticket relies on with no provisioning edge).
**Why:** A build pass picking TK-164 off the ready frontier could start before the canonical enum exists, or wire against an ad-hoc string set — exactly the drift ISS-4 was raised to kill. Fixed by adding TK-21 to depends_on.

## Seam misses (named cog-worx API absent / misnamed / wrong seam) (17)

### [TK-5] WombatQueue.drain(batch_size) — bounded dequeue handed to the gate
**Note:** Not a cog-worx seam — wombat's own Postgres-backed queue. Provisioned by TK-2 (drain/lease/ack, TASK-12/13/14). Listed only to show the at-least-once mechanism AC3 relies on lives in TK-2, which TK-5 declares as a dependency.

### [TK-24] cog-worx journal as write-ahead authority for PendingSetAdd/PendingSetRemove claims (intent)
**Note:** The real cog-worx Journal is a STEP/RUN write-ahead log keyed POSITIONALLY by (run_id, step_index), committing StageResult records via commit_step(). Its docstring states it is 'not a generic store that flattens engines.' It exposes NO append/replay of arbitrary application records and NO concept of PendingSetAdd/PendingSetRemove set-claims. So the real Journal cannot host these claims as-is. For THIS spike it does not have to: non_goals scope the journal to 'an in-process journal stub emulating write-ahead,' so the spike builds its own stub and does not bind to the cog-worx seam. The intent wording 'using the cog-worx journal as write-ahead authority' therefore overstates the real seam.

### [TK-24] pending_set.rebuild_from_journal(), PendingSetAdd, PendingSetRemove, PendingSet (AC3, intent)
**Note:** Pure wombat-domain. Correctly NOT a cog-worx seam — these are the spike's own artifacts. No constructibility issue: the spike produces them.

### [TK-42] RecallStack / S5 seam as the user_model scope READ path for fetching a rating-parameter node by event class
**Note:** RecallStack EXISTS but is the WRONG seam: it is a fan-out-over-channels + RRF-fusion + rerank recall engine returning ranked FusedResult/RecallOutcome (recall(query)->RecallOutcome). It has no method to point-read a specific user-scope parameter node by event class. Using it for a deterministic parameter fetch contradicts the gate-deterministic frame (NG-4).

### [TK-75] SourceRegistry-conformant class (complexity_budget: 'One SourceRegistry-conformant class using the Gmail API')
**Note:** Namesake collision. TK-75's 'SourceRegistry-conformant' means wombat's OWN EP-3 input-source registration contract (id/poll/push/map + lifecycle), a wombat-domain artifact, NOT the cog-worx SourceRegistry. Constructible (EP-3 is a declared dep) but the term is a documentation hazard: a builder could wire the wrong class.

### [TK-99] EP-9 / gate 'consolidated-flush arm' as a reusable selection callable invokable with the threshold bypassed (the load-bearing seam this ticket adapts)
**Note:** This is a wombat seam, not a cog-worx seam, but it is the ticket's central dependency and it does NOT exist as described. TK-27's flush requires load>threshold AND age>flush_min_age, emits 'all pending sorted by urgency', and CLEARS the pending set. There is no exposed callable that (a) skips the guard, (b) runs per-item selection, and (c) leaves the pending set untouched (TK-99 non_goal forbids mutating it).

### [TK-101] EP-30 speak-event — brief is spoken tersely via EP-30 (AC2)
**Note:** Wombat-domain dependency, not a cog-worx API. EP-30 is an unbuilt epic (normal), but no speak-event signature/contract is pinned in any referenced node; AC3 text-only degrade keeps this from hard-blocking the build.

### [TK-111] SchemaViolation (raised at insert-time on motive predicate, AC2)
**Note:** No SchemaViolation type in cog-worx. It is a wombat-domain error the ticket must define. Note the existing schema wall (TK-43/EP-11) raises TypeError/mypy at the TYPE level, not a runtime SchemaViolation — see gaps.

### [TK-112] UserModel.ratings_for() (AC #2) used to read back the most recent WindowSummary
**Note:** Not a cog-worx seam miss (it's wombat-owned), but the ticket uses it for the wrong purpose: ratings_for(item) resolves per-event-class Urgency/Load RatingParams, it has no contract to return a WindowSummary. AC #2 misuses this seam.

### [TK-118] registration of the contributor 'for the ReflectionCompose Stage path' such that it is scoped to ReflectionCompose only (AC3)
**Note:** register() binds ONE contributor per SLOT NAME on a given ContextAssembler instance, globally for that assembler -- there is no per-Stage or per-pathway contributor scoping. The engine (cogworx.runtime.engine) builds one assembler and registers contributors on it (e.g. assembler.register('rules', RulesContributor(...))). Per-stage customization exists only as bind_context_policy (policy, not contributors). 'Scoped to ReflectionCompose only' is achievable but requires provisioning a dedicated assembler/slot for the reflection path -- a mechanism the ticket neither names nor declares a dependency for.

### [TK-118] the contributor 'resolves the pattern_id' from the Stage assembly (AC1) -- i.e. pattern_id is available to contribute()
**Note:** contribute() receives only ContextRequest + SlotAllocation. ContextRequest is frozen with exactly task/instructions/query/policy and no extensible metadata field -- there is no pattern_id channel. There is no documented seam by which the gate-cleared item's pattern_id reaches the contributor. The build must invent a side-channel (e.g. constructing a per-item contributor instance and re-registering per turn), which contradicts 'registered ... the contributor is invoked automatically'.

### [TK-118] inject hints 'into the Stage context under key kb_phrasing_hints' (AC1)
**Note:** The assembler produces an AssembledCallContext of banded ChatMessages + tools; a contributor emits SlotContent chunks into its assigned slot. There is no key-addressable context dict, so 'inject under key kb_phrasing_hints' has no corresponding mechanism. Hints would instead become text chunks in a head/instructions-style slot. TK-114 AC1 also expects to receive 'resolved KB phrasing hints' as a discrete input -- the two tickets share this mismatched mental model.

### [TK-131] Capability-managed lifecycle closed on Engine teardown
**Note:** No seam propagates teardown from Engine to a Capability. Engine.aclose() does not iterate the capability Registry; the Capability Protocol has no close(). AC-3's clean-close-on-teardown is unprovisioned in cog-worx. Only the unrelated MCPCapability wrapper (capability/mcp.py) has its own __aexit__.

### [TK-136] Capability guard that blocks typing into a password field without a human-confirm token (AC-3)
**Note:** cog-worx ships ToolGate/capability.policy (admission/tier gating) but NOT a field-level password guard. The 'guard cannot be bypassed' AC is wombat-authored logic wrapping the type action. The type/click action itself is introduced in TK-132, which is NOT in depends_on — see gaps.

### [TK-148] TAINT_UNTRUSTED tag applied to an untrusted-content read Phase (complexity_budget + intent)
**Note:** There is no symbol/constant named TAINT_UNTRUSTED. The taint tag is the literal string 'untrusted-source', and it is a tag on a registered CAPABILITY (read at invocation), NOT a marker set on a Stage/Phase. The ticket's 'tag the read Phase with TAINT_UNTRUSTED' framing does not map to a real cog-worx API.

### [TK-148] cog-worx raises ToolGateViolation when a tainted drive invokes an external Capability (AC5; also cross-referenced in TK-146)
**Note:** When the drive is tainted, 'external' is removed from effective tiers and ToolGate.check_dispatch raises TierViolation (not ToolGateViolation). ApprovalRequired is raised only for tainted AND consequential AND irreversible AND not-approved. An adversarial test importing/asserting ToolGateViolation will fail to resolve the symbol.

### [TK-151] a cog-worx external-tier FLAG in runtime/deploy config that wombat enables at build-time; binding stays inert/refused until that flag is set
**Note:** TK-151's central 'build-time external-tier flag enablement' has no cog-worx counterpart. Fail-closed is already automatic (a stage without external in its allowed_tiers is refused). AC-3 tests refusal-when-flag-unset against an artifact that does not exist as a separate seam.

## Split candidates (3)

### [TK-46]
- Single-responsibility violated: the ticket bundles THREE distinct concerns into one 'scaffold' — (a) a DreamPathway Stage scaffold, (b) a durable RECURRING nightly trigger that fires every night, and (c) a once-per-night idempotency fence ('no duplicate run is started for that night'). The sibling brief epic (EP-19) deliberately splits exactly these: TK-96 = timer-fired pathway skeleton (no timer, no idempotency fence), TK-97 = 'once-daily trigger' + idempotency fence. EP-13 has no TK-97 analogue, so TK-46 silently absorbs that whole second ticket's worth of work.
- AC2 ('no duplicate run is started for that night') is only testable if the run's idempotency/start key is defined — but neither the AC, complexity_budget, nor files_in_scope name the per-night key (e.g. run_id keyed on the date). cog-worx Engine.start_run IS idempotent on run_id, so this is achievable, but the 'then' is not checkable as written because the keying mechanism is unspecified.
- AC3 ('the next nightly timer is scheduled regardless') requires a re-arm / reschedule mechanism. cog-worx's Wait is explicitly a ONE-SHOT park within an already-started run that 'never re-arms' (loop/result.py + engine.py C6); there is no recurring/cron primitive in cog-worx. So 'the next nightly timer is scheduled' depends on a recurring-scheduler capability the ticket assumes cog-worx provides but it does not — making the AC reference an unbuilt/unspecified capability.
- Vertical slice and off-path isolation (AC1: drain heartbeat continues) ARE genuinely testable and well-scoped — that part is atomic. The non-atomicity is concentrated in the trigger/idempotency half, which should be split out (mirroring TK-97) so the scaffold and the durable recurring fence are separate build passes.

### [TK-112]
- AC #1, #3, #4 form a clean vertical slice around the pure detector function detect_productivity_windows(events) -> list[WindowSummary] plus a no-UI lint guard — single responsibility, testable, bounded by the complexity_budget (one pure function + one nightly Stage).
- AC #2 breaks atomicity: it introduces a SECOND responsibility — a read-back path ('the gate can read the most recent WindowSummary') — that is neither the pure function nor the write Stage, and asserts it via UserModel.ratings_for(), a seam that returns RatingParams (TK-41/TK-42), not WindowSummary. The 'then' is not a runnable check against anything this ticket builds.
- depends_on is dishonest for AC #2: it invokes UserModel.ratings_for() (owned by EP-10 / TK-42) but EP-10 is NOT in depends_on (only TK-111, EP-11, EP-13 are listed).
- Recommend split/rewrite: keep the detector + write-Stage slice (AC 1/3/4) as TK-112; move the WindowSummary read-back capability to its own ticket under EP-10/EP-11, or delete AC #2 and replace its 'then' with a check that the summary row is persisted and queryable by date.

### [TK-146]
- Single core responsibility on paper (one table + one writer function project_action_to_trail), but the acceptance_criteria do not test that responsibility in isolation — every AC asserts the runtime behavior of dispatch Stages (ComposeGmailDraft, SubmitBrowserForm, Speak, dispatch_approved) that are owned/created by OTHER tickets (TK-78, TK-135, TK-149), not by TK-146.
- Not an independently-testable vertical slice at its own layer: AC1-AC4 require the side-effectful Stage classes and the two-step dispatch pattern to already exist to run, yet those are built by TK-149 which itself depends_on TK-146 — a dependency inversion. The writer's own contract (insert-on-call, idempotent status update) could be tested directly without those Stages, but the ACs do not.
- depends_on:[EP-4] is not honest: the ACs lean on capabilities from TK-78/TK-135/TK-149 (the dispatch Stages and dispatch_approved), none declared. The complexity_budget itself commits to 'one call site per side-effectful Stage', i.e. editing Stage code those tickets own.
- AC2's 'kill the process... assert the trail row survives restart' tests durable-Postgres infrastructure, not the writer — and conflicts with the cold-boot in-memory-doubles default frame; it is a different concern bolted onto a schema/writer ticket.
- Recommend: keep TK-146 as schema + project_action_to_trail writer with ACs that test the writer directly (insert, idempotent status transition) against its own table; move the 'Stage calls the writer before AwaitHuman / dispatch_approved updates status' integration assertions into TK-149 (which owns the Stages and already depends_on TK-146).

## Weak atomicity (22)

### [TK-4]
- Single responsibility is clean: one concrete PresenceSource on the TK-3 InputSource contract emitting one PresenceSnapshot type, plus conservative degrade. Vertical slice (source -> QueueItem) is real and bounded by an explicit complexity_budget (single OS idle signal, no fusion).
- AC1 and AC2 are testable against TK-4's own files_in_scope (presence.py + test): poll() with an idle-time mock asserting state/staleness, and the import/syscall-failure degrade path returning state=unknown/confidence=0.0 without raising. Good.
- AC3 is the weak point: 'when the gate checks presence ... the gate's presence check returns conservative' tests GATE behavior, not the PresenceSource. The gate lives in wombat.gate (TK-6, files src/wombat/gate/*), which is NOT in TK-4's files_in_scope and NOT in its depends_on. There is no runnable check TK-4 can satisfy for AC3 — nothing in TK-4's slice produces a gate presence-check. The dependency runs the OTHER way (TK-6 depends_on TK-4).
- Net: two ACs are atomic and buildable; the third belongs to TK-6/TK-11 (the gate-side presence_hold contract). Recommend rewording AC3 to assert the SNAPSHOT contract TK-4 actually emits (e.g. 'a snapshot older than 5min carries staleness>5min and state is preserved so a downstream conservative consumer can hold'), or moving the gate-behavior assertion to TK-6. This is a wording/scope fix, not a split — hence weak, not split.

### [TK-10]
- Single responsibility is clean: the Stage only selects a composer by item_kind; it explicitly does not phrase (composer) or decide surface/hold (gate). Non_goals reinforce this boundary.
- Vertical slice with four testable ACs, each with a checkable 'then' verified via composer stubs (generic->ComposeStage once, reflection->ReflectionComposeStage, draft->DraftComposer, unknown->generic+warning, no raise/no drop).
- depends_on is DISHONEST: it lists only [TK-6, TK-8] but the complexity_budget and every AC key off GateDecision.item_kind, the closed Enum {brief,reflection,draft,generic} produced by TK-21 (ISS-4). Neither TK-6 nor TK-8 produces item_kind (TK-6 produces GateAction). TK-21 is not declared, so the enum the router imports has no declared producer in this ticket's dep chain — the exact Q-29 'unprovisioned seam' shape, here a wombat-domain enum rather than a cog-worx one.
- Composer targets TK-114/TK-79/TK-100 are referenced by AC2/AC3/non_goals but are NOT hard build deps: verification is by stubs and non_goals exclude implementing any composer, so a kind->composer map populated with test stubs satisfies the ACs. These are correctly out of depends_on.

### [TK-9]
- Single coherent responsibility (cap mouth spend on two layers) and a vertical slice; ACs are mostly testable with checkable thens (mock-HTTP-not-invoked at 100/100, ledger row incremented on success, reload-after-restart enforces persisted total, BudgetGuard refusal degrades to template, wombat-day boundary read via shared ledger).
- depends_on is honest: TK-8 supplies ComposeStage, TK-1 supplies the Engine/BudgetPolicy wiring point, TK-152 supplies the shared DailyLedger/day boundary, TK-13 supplies the daily_token_ceiling value — every capability the ACs lean on is declared.
- Knocked from pass to weak by an internal contradiction: the ticket's own complexity_budget/files_in_scope define a SEPARATE DailySpendLedger over its own migration 002_daily_spend_ledger.sql (one row per date, columns date/tokens_spent/cost_usd), while AC5 + the TK-152 dependency place the day boundary AND row lifecycle in the shared DailyLedger (one row per (ledger_name,wombat_date), migration 003). A builder cannot tell whether to create a second dated table or layer spend onto TK-152's shared row.
- This is one design decision to resolve, not multiple responsibilities to separate — so weak, not split.

### [TK-42]
- Single responsibility is clean: one class, one public read method, no writes (non_goals honored).
- Vertical slice is plausible (item in -> RatingParams out), and all three acceptance_criteria have checkable 'then' clauses (returns stored params; returns documented defaults; returns defaults + logs warning on Neo4j-down).
- depends_on TK-41 (RatingParams type) is honest; EP-6/EP-10 are epic parents.
- WEAK (not split): complexity_budget names 'the RecallStack/S5 seam' as the read path, but RecallStack is a ranked RRF recall-fusion engine returning FusedResult — it cannot do the deterministic point-read of 'the rating-parameter node for CALENDAR_CONFLICT' that AC-1 requires, and a fuzzy ranked recall conflicts with the gate-deterministic frame (NG-4). The correct seam (cogworx.knowledge.scoped_kg.user_model -> ScopedKG, or EntityKG.claims_about(..., scope='user:<id>')) is never named.
- The work is still one ticket's worth — the fix is tightening the seam and provisioning inputs, not splitting.

### [TK-44]
- Single responsibility is clean: one ObservationWriter class writing one behavior/outcome claim into the user scope; vertical slice is reasonable (valid claim -> persisted user-scope claim).
- depends_on is dishonest about the write substrate: calling the cog-worx user_model write seam needs an EntityKG + ScopeRegistry + a declared user Scope + ScopeWriteToken (single-writer S7), none of which TK-43 (claim vocab) or TK-41 (rating vocab) provide, and no other ticket is referenced for it. TK-41 is unrelated to this write path; the sibling read ticket TK-42 correctly declares EP-6, TK-44 declares no equivalent.
- AC1 and AC3 assert 'a Neo4j node is created' / 'Neo4j is unavailable', which is not a checkable 'then' under the cold-boot in-memory-doubles default — the seam writes through EntityKG (InMemoryEntityKG by default), so the AC is unrunnable as written and contradicts the locked frame.
- The ticket never names the actual seam (assert_fact / SourceDeclaration / epistemic_type), so 'ObservationWriter.record(claim)' hides a non-trivial decomposition from a wombat Claim into the assert_fact keyword surface.

### [TK-45]
- Single responsibility is mostly clean: write an OUTCOME_* claim for a queue item. But the ticket smuggles in a second capability via AC6 (a 7-day Neo4j read/query over outcome claims) that is neither a labeler responsibility nor in files_in_scope (only outcome_labeler.py). Labeling and querying are two responsibilities.
- Most ACs are testable with a checkable 'then' (claim written keyed on TK-12 identity, TypeError before I/O, supersede PENDING). AC6 is testable but tests a capability this ticket does not build.
- depends_on (TK-44, TK-50, TK-12) is honest for the WRITE path: TK-44 supplies ObservationWriter, TK-50 supplies the OutcomeSignal, TK-12 supplies canonical identity. But two ACs rely on capabilities NOT provisioned by any listed dep: AC2's supersede semantics and AC6's query path.
- Scope is bounded (S, mvp, one class) EXCEPT for the undeclared read/query surface in AC6, which inflates it beyond 'labeling only' (its own non_goal).

### [TK-51]
- Single responsibility is mostly honored: emit a feedback affordance token on the action trail + register a FeedbackInputSource that enqueues a FeedbackSignal. Two thin pieces, no LLM, no new egress — bounded.
- depends_on is DISHONEST about the registry seam: ACs 2 require SourceRegistry.enqueue / list_sources / registration, but depends_on lists only TK-44/TK-12/TK-147/TK-161. SourceRegistry is produced by TK-3, which is NOT a declared dependency. TK-161 only supplies the InputSource Protocol, not the registry.
- AC-3 is not testable WITHIN this ticket's build pass: it asserts an end-to-end outcome (useful->LOAD_BEARING, not_useful->REGRETTED) that requires TK-50's inference engine to consume the signal — but TK-50 depends_on TK-51, so this AC cannot run green during TK-51's own pass without inverting the dependency. It belongs in TK-50 or must be reframed as a contract/stub assertion (signal is durably enqueued with the right shape).
- ACs 1, 2, 4 are individually checkable (affordance carries item_ref; source appears in list_sources with no bespoke branch; absence-of-feedback leaves capability unaffected). AC-3 is the weak one.
- Files_in_scope (one source + one test) match the scope, but the trail-affordance half (AC-1) writes into TK-147's renderer territory, not the single file_in_scope — a second touch-point the file list omits.

### [TK-47]
- Single responsibility holds at the headline level (wire cog-worx consolidation into DreamPathway), but the ticket conflates TWO distinct cog-worx sweepers (CoherenceReconciler and ClaimExtractor) that have different constructors, different stores, and different lifecycles into one undifferentiated 'reconciler/extractor stages' step.
- AC1's 'then' references a 'reconciliation artifact is journaled' and 'duplicate/conflicting user-scope nodes merged per cog-worx reconciler semantics' — checkable in principle, but only if the reconciler's four required collaborators (entity_kg, store, oracle) are actually constructed; nothing in the ticket or its single dep (TK-46, a timer scaffold) provisions them, so the AC is not runnable as scoped.
- AC2's 'extractor stage runs ... logs a zero-change result' treats the extractor as a one-shot stage that 'completes'; the real ClaimExtractor is sweeper-shaped (tick() drains up to batch_limit per call against a journal watermark) and has no 'completes/zero-change' terminal semantic — the AC's testable 'then' does not map onto the seam.
- depends_on is dishonest: it lists only TK-46. The reconciler/extractor need a constructed EntityKG / CoherenceStore / ConsistencyOracle / Journal / Model / SourceRegistry, which come from the memory/recall layer (EP-10/EP-11, TK-42/TK-43) and the Engine/model wiring (TK-1/TK-14) — none are declared deps.
- complexity_budget says 'thin wiring ... compose existing reconciler + extractor sub-stages' and 'no bespoke extraction logic', but composing two sweepers into a bounded run-to-drain pathway, provisioning their constructors, and journaling an artifact is more than thin wiring as written.

### [TK-72]
- Single responsibility holds: one SourceRegistry-conformant class that polls Google Calendar (read-only) and maps event JSON to typed CalendarEventItem QueueItems. Scope is bounded by an explicit complexity_budget and clean non_goals (no writes, no conflict logic in EP-16, single primary calendar).
- Vertical slice is real: registration -> poll against mocked Google API -> typed items on WombatQueue, end-to-end testable with mocks. Files_in_scope (poller.py, models.py, test_poller.py) match.
- AC#1 and AC#2 are testable with checkable 'then' clauses (exactly 3 items + zero write calls via mock call-list; auth error -> logs, no items, no crash).
- WEAK on AC#3 honesty: AC#3 asserts 'WombatQueue deduplicates by event_id', but the canonical-identity rule (TK-12, which resolved Q-18) explicitly mandates that TK-72's dedup MUST resolve through idempotency_key = f(source_id, event_id), NOT raw event_id. TK-12 is not in depends_on. The AC as written both (a) contradicts the locked frame's idempotency rule and (b) relies on a capability owned by an undeclared dependency. This is a Q-18-class binding gap, not a nit — it makes AC#3 un-buildable as literally stated.
- depends_on lists TK-71 (auth/token), EP-2 (queue), EP-3 (registry) but omits TK-12 (canonical identity) even though AC#3's dedup semantics are owned by TK-12.

### [TK-75]
- Single responsibility is mostly clean: one SourceRegistry-conformant GmailPoller class plus auth + models, enqueuing typed GmailMessageItems. Five ACs, each with a checkable 'then' (item count/fields, taint active, capability not reachable, dedup, auth-error no-crash).
- depends_on is honest about the taint machinery: AC2/AC3 lean on the cog-worx taint latch + external-tier dropping, and TK-148 (which owns that wiring) IS listed in depends_on alongside EP-2 (WombatQueue dedup) and EP-3 (source-registration contract).
- WEAK because AC2 ('taint latch ACTIVE during read phase') and AC3 ('external Capability not reachable from the SAME tainted drive') assert drive-scoped ToolGate/TaintState properties that only exist inside a cog-worx drive Stage with a bound StageToolPolicy — yet TK-75's own scope (poller.py/auth.py/models.py) describes a plain Gmail-API poll loop that maps to a typed item and enqueues. The ticket never names WHERE the poll path becomes a cog-worx drive Stage or which Stage binds the tool policy; that drive-Stage boundary is built in TK-148 (src/wombat/stages/ingest_email_body.py), not here. The body-read taint assertions and the poller-enqueue responsibility are arguably two slices.
- Recommend either (a) explicitly scoping the drive-Stage boundary (the IngestEmailBody Stage and its StageToolPolicy binding) so AC2/AC3 have a named home in this ticket, or (b) moving AC2/AC3 onto TK-148 and leaving TK-75 as the pure poll+enqueue slice with TK-148 owning the taint assertions.

### [TK-77]
- Single responsibility is clean: deterministic extract_tasks(item) over a GmailMessageItem body, regex/keyword heuristics, emits zero-or-more TaskItem records. No LLM in path (matches NG-4 / CON-1).
- All four acceptance_criteria are vertical and have a checkable 'then' (deadline signal returned; empty list; latch ACTIVE asserted; off-path trace asserted with MUST-fail conditions).
- Bounded scope with explicit non_goals (triage scoring -> TK-76, LLM inference, calendar write-back).
- WEAK, not pass: depends_on is not honest in both directions. It omits TK-12 (canonical item identity), whose AC explicitly requires TK-77's TaskItem to carry a back-linkable ItemRef, and it omits TK-21 (the canonical item_kind enum the emitted TaskItem must route through). Conversely it lists TK-76 though no AC consumes TriageResult.
- The TaskItem artifact's surfacing/routing contract (which item_kind it stamps) is unspecified, so 'so the gate can surface tasks' is an outcome no AC actually verifies end-to-end.

### [TK-96]
- Single-responsibility is muddled: framed as a thin end-to-end WIRING skeleton (one pathway, one brief out), yet its files_in_scope claims the concrete stage modules (brief_gather_stage.py, brief_compose_stage.py, brief_deliver_stage.py) that sibling tickets TK-98/TK-100/TK-101 each own as their sole deliverable. The ticket is simultaneously 'wire the stages' and 'build the stages' — two responsibilities overlapping three other tickets.
- ACs are individually testable with checkable thens (one brief written to sink; artifact contents == ForceFlush output; degrade-to-template when mouth down), so testability itself is fine.
- Vertical slice is honest (event+email in -> delivered brief out) and scope is bounded (no timer, no idempotency, no voice; stub conflicts/mouth allowed).
- depends_on is NOT honest: AC-2 requires the ForceFlush stage to assemble a sealed decision artifact and AC-1 requires the Gather/Compose/Deliver stages, but the deliverable tickets for those (TK-98 Gather, TK-99 ForceFlush+BriefDecisionArtifact, TK-100 Compose, TK-101 Deliver) appear in NONE of TK-96's depends_on (only epics EP-9/15/16/17/5/30). The decomposition even runs the other way (TK-99->TK-98, TK-100->TK-99, TK-101->TK-100), so the real stages sit downstream of the skeleton that needs them.

### [TK-99]
- Single-responsibility is clean: one Stage that forces a flush and seals a BriefDecisionArtifact, no model call. Scope is bounded and the two files_in_scope match the intent.
- All three acceptance_criteria have checkable 'then' clauses (artifact contents, sealed/immutable, structural pass-through to TK-100), so testability is fine in isolation.
- depends_on is NOT honest: the ticket leans on a reusable, threshold-free 'consolidated-flush selection' callable that EP-9 (TK-27) does not expose. TK-27 only ships gate.pipeline(), which couples the load+min-age threshold guard inseparably to selection and CLEARS the pending set as a side effect.
- An undeclared input dependency: AC #1/#3 require the BriefPayload's calendar events + conflict candidates to be selectable, but no ticket (TK-98 gather, EP-16 conflicts) accumulates those items into the gate's pending set, and EP-9's flush selects only 'all pending', not per-item 'items that pass selection'. The 'deterministic selection' capability the ACs name is not produced by any ticket as a callable seam.

### [TK-101]
- Single responsibility and a clean vertical slice: one Stage that writes BriefText to a text sink (append-only file + stdout) and optionally speaks it via EP-30. Scope is bounded and no model call is involved.
- All four acceptance_criteria have checkable 'then' clauses (file appended with TZ header + journal write; spoken-once exact-match; degrade-with-warning no-raise; no duplicate on replay).
- Dependency honesty is the weak point: AC4 leans on the TK-97 daily-run ledger for dedup, but TK-97 is NOT in depends_on (only EP-30 and TK-100 are). This is an undeclared cross-ticket reliance.
- AC4's causal claim is incoherent: the TK-97 daily-run ledger guards whether the brief *pathway fires per wombat-day*, not whether *this stage re-appends the file* on cog-worx journal replay within an already-started run. The stated mechanism does not actually cover the AC4 crash window, so that AC is not currently provable as written.

### [TK-117]
- Core responsibility is genuinely atomic: one pure function extract_phrasing_hints(pattern_id, kb) -> list[str], a dict lookup, zero model calls, S/mvp. ACs #1 (hit) and #2 (miss -> empty list) are testable in isolation and deterministic.
- AC #3 is NOT checkable within this ticket's surface: it asserts no hint string appears verbatim in TK-114's 'final rendered observation', which requires the Compose Stage (TK-114) and the mouth (EP-5) to exist and run. TK-114 depends on TK-117, not the reverse, so the verbatim-injection guarantee is properly TK-114's/TK-118's AC, not TK-117's. This is the same no-verbatim assertion already present in TK-114 (AC #4) and TK-118 (AC #4) — duplicated/misplaced here.
- AC #4 (CI linter forbidding 'you must'/'you should'/'diagnos'/'disorder'/motive attribution in 'any hint string in any KB entry') is a property of the TK-115 KB data, not of TK-117's extraction function. TK-115 already owns a clinical-language linter AC (no 'diagnos'/'disorder'/etc.). This AC is misplaced scope; TK-117 builds nothing that this linter exercises.
- Net: the function itself is atomic, but 2 of 4 ACs leak responsibility into TK-114/TK-118 (output rendering) and TK-115 (KB data linting). Trim ACs #3 and #4 (or relocate them to their owning tickets) and TK-117 is a clean pass.

### [TK-118]
- Single responsibility is clean in spirit (one contributor class implementing the cog-worx contributor contract, registered for the reflection Compose path, calling TK-117, no model calls) and scope is bounded (one file, estimate S).
- However the acceptance_criteria are written against a mental model of the cog-worx context seam that does not match the real API, so several ACs are not checkable as written against the actual seam: AC1 ('inject the hints into the Stage context under key kb_phrasing_hints') and AC3 ('NOT invoked for non-reflection Compose Stages -- scoped to ReflectionCompose only') assume capabilities the named seam does not provide (per-stage contributor scoping; arbitrary-key context dict; pattern_id reaching the contributor). That makes the 'then' clauses unrunnable without first inventing a wiring mechanism the ticket does not name.
- depends_on lists TK-117 and EP-5 -- both correct and honest. But the ticket leans on a Stage->context handoff (how pattern_id and the per-stage scoping reach the contributor) that no listed dependency provisions; the mechanism is assumed, not declared. This is the atomicity weakness rather than a missing dependency edge.
- AC4 (hints never appear verbatim in TK-114's output) is a cross-ticket assertion about TK-114's rendered string; it is testable but couples this ticket's pass/fail to TK-114 behavior -- acceptable as a guard, noted.

### [TK-131]
- Single responsibility is clean: one Capability class, navigate+snapshot over the a11y tree, plus registration.
- Vertical slice is real (Engine constructed with capability registered -> invoke navigate -> structured a11y snapshot).
- But the complexity_budget pins a SYNC open/close/navigate/snapshot API while the actual cog-worx Capability Protocol's only dispatch method is `async def invoke(args)`; a sync API cannot conform, and the ticket's own POC concedes this is unresolved, so the build target is self-contradictory.
- AC-3 ('engine torn down -> session closes cleanly, no zombies') asserts a teardown contract the Capability Protocol does not expose and Engine.aclose() does not exercise, so its 'then' is not satisfiable through the named seam.
- depends_on EP-1 honestly provides the Engine/Registry, but the Engine->Capability teardown propagation that AC-2/AC-3 rely on is produced by neither EP-1 nor any cited ticket.

### [TK-149]
- Single responsibility is clean: a shared two-step dispatch mixin/base Stage (journal-proposed -> AwaitHuman -> dispatch_approved) wired to the three side-effect stages. Vertical slice and bounded scope are good; non_goals correctly exclude UI/retry/auth.
- ACs 1-4 are testable with checkable thens (returns AwaitHuman, zero pre-approval API calls, exactly-one post-approval call, reject->cancelled+halt, Playwright not called pre-approval).
- AC-5 is the weak point: it asserts cog-worx raises ApprovalRequired on a direct side-effectful external call, but the real ToolGate.check_approval only raises ApprovalRequired under tainted AND consequential AND irreversible AND not-approved — not on an arbitrary direct call, and the dispatch case is explicitly a fresh UNTAINTED drive (TK-151 AC-1), where check_approval returns without raising. The correct refusal in that case is TierViolation from check_dispatch. So AC-5 is not a runnable check as written.
- depends_on (TK-146, EP-4) is honest for journaling/trail and the Stage pipeline, but the ticket relies on the external-tier admission policy being bound (TK-151/DEC-20/DEC-22) for the dispatch_approved external call to actually be admitted — that dependency is undeclared.
- Verdict weak rather than split: scope is atomic; the defect is one incorrect AC plus one missing dep, both fixable in place.

### [TK-150]
- Single responsibility is clean: one safety harness (LocalResidencyGuard + a startup check + interceptor fixtures) asserting the storage-residency boundary; tight, well-bounded complexity_budget (no new persistence code, one fixture + table-driven test).
- All four ACs have checkable 'then' clauses (AssertionError raised / RemoteStorageConfigError raised / exactly-one-outbound-call / zero token egress), and Q-25 sharpened the residency definition so the guard is testable and unambiguous.
- Dishonest depends_on at epic granularity: AC2 ('a Compose Stage calls the DeepSeek API ... exactly one outbound call to the DeepSeek base_url') relies on the ComposeStage + configured DEEPSEEK_BASE_URL produced by EP-5/the line-611 mouth-wiring ticket, which is NOT in depends_on (EP-1, EP-15, EP-17).
- AC1's interceptor only has DB writes to resolve under the REAL-adapter substrate path owned by TK-14; under the v1 default cold-boot config (cog-worx in-memory doubles, zero infra, no sqlalchemy/psycopg/neo4j writes) AC1 has nothing to assert, and the ticket never pins which config the harness runs against.
- TK-14 is the actual consumer of TK-150's residency_check seam (per the Q-29 resolution), a coupling TK-150's depends_on does not reflect.

### [TK-151]
- Single responsibility is clear (bind external-tier admission to the named dispatch stages) and estimate S is plausible — the core act is small.
- But the acceptance criteria are not testable by THIS ticket in isolation: AC-1 and AC-2 require a real dispatch Stage that invokes an external Capability (gmail.drafts.create) and a real non-dispatch Stage, plus a registered external Capability. None of those exist within TK-151's scope — the dispatch stages (TK-78/131/133/135) are explicitly non-goals and are NOT in depends_on. So the 'then' clauses cannot be checked with a runnable check until those stages exist.
- depends_on is dishonest for the ACs as written: it lists TK-1/TK-148/TK-149 but every positive/negative admission AC leans on a dispatch Stage + a registered external capability produced by TK-78 (and the capability registry). The taint AC (AC-4) does honestly map to TK-148.
- AC-3 tests fail-closed behavior of a 'deploy-config external-tier flag' that is not a real cog-worx artifact (see seams/gaps) — an AC pinned to a non-existent capability is not testable as specified.
- Vertical-slice is muddy: the ticket conflates (a) binding a StageToolPolicy with (b) a build-time operational flag enablement that has no cog-worx counterpart, mixing planning and a phantom ops step into one ticket.

### [TK-161]
- Single-file, single-responsibility on its face (define one InputSource Protocol/dataclass) and estimate S/mvp is appropriate.
- AC1 is testable (importable Protocol with id/push/poll in src/wombat/sources/base.py, no ASR/TTS deps). AC2 ('satisfying InputSource is sufficient, no core file needs editing to add a source') is a property-style claim that is hard to pin to a single runnable check inside THIS one-file ticket with no SourceRegistry present — it really exercises TK-5's registry, which TK-161 does not build.
- DEPENDENCY DISHONESTY / DUPLICATION: depends_on is EP-3 + EP-2 (epics), but the concrete InputSource contract in src/wombat/sources/base.py is ALREADY produced by TK-5 (parent EP-3), whose files_in_scope explicitly lists src/wombat/sources/base.py and whose complexity_budget is 'One Protocol (InputSource) + one SourceRegistry class'. TK-161 redefines the same InputSource artifact in the same file. The two tickets collide on one artifact; TK-161 should either depend directly on TK-5 and EXTEND/specialize the contract (e.g. only add a SourceRegistration dataclass), or one of them should own InputSource exclusively.
- The title promises a 'SourceRegistration dataclass' but the acceptance_criteria never mention SourceRegistration at all — they only describe the InputSource Protocol. Title and ACs are out of sync, so the ticket's actual deliverable is ambiguous.

### [TK-162]
- Single responsibility and vertical slice are good: one ASR InputSource file + composition-root wiring, ending in a QueueItem on WombatQueue. Scope is genuinely bounded (push-to-activate harness, local-only, no TTS).
- All three acceptance_criteria have checkable 'then' clauses (source_id='asr' on WombatQueue; non-voice features unaffected; 'asr' in list_sources()).
- WEAK because the ACs assert a SourceRegistry public surface that the canonical registry ticket (TK-5, parent EP-3) never provisions: AC1 uses SourceRegistry.enqueue() and AC3 uses SourceRegistry.list_sources(), neither of which appears in TK-5's contract (TK-5 exposes start()/stop() and enqueues internally from poll()).
- depends_on is partly dishonest about the push seam: TK-162 is push-driven ('listens on a push channel') and TK-161 adds a push callback to the base contract, but TK-162 depends only on EP-3 generically. TK-5 — the ticket that actually builds SourceRegistry — explicitly non-goals push ('Does not implement push/webhook sources, poll suffices for v1'). No ticket in depends_on provisions push-driven registration/enqueue into the registry.
- The title calls the component a 'Stage' (a cog-worx loop abstraction) but every AC treats it as an InputSource/SourceRegistry registration — a category conflation that should be resolved so the builder knows it is building a wombat InputSource, not a cog-worx pathway Stage.

## MEDIUM gaps (82)

### [TK-152] The locked frame says cold-boot default uses cog-worx in-memory doubles for ZERO infra, with real timescale/neo4j/pg only as a config swap (TK-14/Q-29). TK-152's intent and complexity_budget hard-bind DailyLedger to 'a Postgres table' with no in-memory double, so the persistence ACs cannot run in the zero-infra cold-boot path that the rest of wombat boots in. There is no declared abstraction (e.g. a LedgerStore protocol with an in-memory implementation) letting the primitive — and the three counters that consume it — run cold without Postgres.
**Why:** Either the cold-boot zero-infra invariant is violated for the daily-counter subsystem (it now requires Postgres to boot/test), or the ticket silently needs a store-abstraction + in-memory double that no AC or non_goal mentions. This will force rework once TK-9/27/28/97 try to run on the cold-boot in-memory path. Worth a one-line decision: does DailyLedger sit behind the same in-memory-double/real-adapter swap as the cog-worx substrate, or is it Postgres-only (and thus excluded from cold boot)?

### [TK-12] AC3 is phrased as 'an OUTCOME_* claim written by TK-45 ... when the claim's subject is inspected, then it carries the item's canonical idempotency_key / ItemRef.' TK-45 is NOT (and must not be) in TK-12's depends_on — TK-45 depends_on TK-12, so requiring TK-12's test to construct/import a real TK-45 claim would be circular. The claim shape does not exist yet.
**Why:** Taken literally the AC references an artifact no in-scope/declared-dependency ticket produces, which could mislead the builder into thinking TK-45 is a prerequisite. The buildable reading — TK-12 provides an ItemRef/key carried on a claim-subject-shaped object, and a test asserts a Gmail-derived vs calendar-derived subject are distinguishable and correctly attributed — is fully testable inside TK-12. The AC should be reworded to assert TK-12's own output (ItemRef on a subject), not TK-45's write.

### [TK-12] AC4's 'all three [TK-2 idempotency_key, TK-72 event_id, TK-75 message_id] resolve through the canonical derivation (no three independent ad-hoc keying schemes)' is a cross-ticket integration/adoption assertion that TK-12 cannot enforce or verify from within its own test suite. TK-72/TK-75 do not exist yet and the obligation to ADOPT TK-12 lives in those consumer tickets, not in TK-12.
**Why:** The only part of AC4 TK-12 can actually prove is the collision test (source_id makes a calendar event and a Gmail message with the same raw natural-id not collide). The 'all three resolve through the canonical derivation' clause is an outcome owned by TK-2/72/75 adopting TK-12; if it is intended as TK-12's acceptance gate it is not checkable here, and if it is intended as a constraint on the consumers it should live on (or be cross-referenced from) those tickets. Clarify which, so the builder does not try to assert downstream adoption from item_identity's own tests.

### [TK-3] AC1 requires the stub event be 'translated to a QueueItem' with an enqueue-able identity, but the canonical idempotency_key/ItemRef derivation is owned by TK-12 (canonical item identity), which is NOT in TK-3's depends_on (and vice-versa). TK-3 must mint SOME idempotency_key for its stub to enqueue, while TK-12 later canonicalizes how every source derives that key.
**Why:** For a fixed-event stub this is constructible (a hardcoded/uuid key suffices for the e2e enqueue), so it is not a blocker — but the 'event->QueueItem mapping' contract TK-3 establishes is the exact surface TK-12 later refactors. Builder should mint a placeholder key now and expect TK-12 to route it through the canonical function, to avoid a competing ad-hoc keying scheme (the very split-key bug Q-18/TK-12 closes).

### [TK-4] AC3 ('the gate's presence check returns conservative do-not-interrupt') asserts behavior of the wombat gate, which is built by TK-6 (files src/wombat/gate/*). TK-4's files_in_scope are only src/wombat/sources/presence.py + its test, and TK-4 depends_on only TK-3. The gate is neither produced by TK-4 nor declared as a dependency — and the real edge is reversed (TK-6 depends_on TK-4). So AC3 has no runnable check inside TK-4's slice.
**Why:** A build pass cannot make AC3 pass without reaching into TK-6's gate code, which TK-4 does not own and which is correctly downstream. The criterion is misplaced: it tests a consumer of the snapshot, not the snapshot/source TK-4 builds. Reword AC3 to assert the PresenceSnapshot staleness/confidence contract TK-4 emits (the thing a conservative gate later reads), and leave the conservative-hold assertion to TK-6/TK-11 where presence_hold actually lives.

### [TK-11] AC 4 ('a reviewer traces which code TK-27's hold-on-stale-presence relies on -> it resolves to this hardened predicate, NOT TK-4's prototype spike') is a documentation/review assertion, not a runnable check. There is no executable 'then' — unlike ACs 1-3 it cannot be proven by a test run.
**Why:** Per the project rules a ticket is done only when every AC passes a runnable check. This AC is satisfiable only by manual inspection, so 'done' for AC 4 is judgment-based. Non-blocking because the substantive guarantee (conservative-on-stale) is fully covered by the runnable ACs 1-3; reframe AC 4 as e.g. 'TK-27/TK-6 import presence_hold from gate.presence_hold (production module), not from sources/presence.py prototype' to make it checkable.

### [TK-5] AC2 calls the empty-queue return a 'cog-worx Wait result (idle heartbeat)', but cogworx.loop.result.Wait is a durable-timer park requiring three fields the AC never mentions: to (next stage), wake_at (an ABSOLUTE datetime from ctx.clock — not wall-clock at replay), and output (an Artifact). The builder must source wake_at from ctx.clock and set to so the drain stage re-parks on itself.
**Why:** Underspecification, not a blocker. The seam exists and is usable, but a builder reading AC2 literally could emit an invalid Wait (missing wake_at/output) or use wall-clock, breaking deterministic replay. Specify the three fields in the task breakdown.

### [TK-5] AC3 (process killed before acknowledgment -> same items re-appear next drain) tests at-least-once durability, but that guarantee is delivered by TK-2's leased-drain + separate ack() (TK-2 AC3 / TASK-14), not by anything TK-5's own DrainQueueStage builds. TK-5's complexity_budget only mentions calling WombatQueue.drain(batch_size) and says items 'remain in the queue (not yet acknowledged)' — it never names who calls ack or when.
**Why:** The capability IS provisioned (TK-2 is a declared dep), so this is not an undeclared-dep gap. But AC1 says drained items 'remain in the queue (not yet acknowledged)' while AC3 asserts they redrive on restart — leaving ambiguous whether DrainQueueStage leases (TK-2's job) and where ack happens. ISS/ticket TK-7 actually owns ack on the hold/surface branch; TK-5 should clarify it only leases, never acks, to avoid double-implementing TK-2/TK-7 boundaries.

### [TK-6] files_in_scope collision risk with TK-21. TK-6 lists src/wombat/gate/gate.py and src/wombat/gate/decision.py. TK-21 owns src/wombat/gate/models.py (which defines GateDecision/GateAction) and src/wombat/gate/pipeline.py. A 'decision.py' in TK-6 invites re-declaring GateDecision/GateAction in a second module, which TK-21's AC explicitly forbids ('a test asserts no module under src/wombat defines a competing surface/hold/flush string set').
**Why:** Ambiguous module ownership between TK-6's decision.py and TK-21's models.py could either (a) duplicate the enum and trip TK-21's no-competing-vocabulary test, or (b) leave it unclear that decision.py must import from models.py rather than define. Briefing should state decision.py re-exports/consumes TK-21's models.py, not redefines it.

### [TK-7] AC #2 'the hold reason is written to the cog-worx journal' does not specify the mechanism, and the real Journal Protocol (cogworx.substrate.journal.Journal) has no free-form note/reason write — only commit_step(StepRecord), set_run_status, emit-via-StageContext. The canonical cog-worx way for a Stage to record output is returning a StageResult the engine journals, or ctx.emit(event).
**Why:** Without naming the path, a builder may try to call a non-existent journal.write(reason) and stall, or invent an out-of-band write that violates 'journal stays cog-worx run authority' (TK-2 non_goal). Ambiguity, not a blocker: pin the AC to 'returns a StageResult carrying the hold reason (journaled by the engine)' or 'ctx.emit a hold event'.

### [TK-8] The ticket's complexity_budget says 'ComposeStage retrieves it via S4' and AC1 says 'the model was called once', but the actual cog-worx seam is StageContext.model (the Engine assembles the model per-drive and injects it); a Stage does not resolve/build from the ModelRegistry itself.
**Why:** A builder following the literal wording could try to call ModelRegistry.resolve_spec/assemble inside the Stage (wrong layer — assemble needs the per-run BudgetGuard the Engine owns). The correct path is ctx.model.complete(...). Ambiguity only; the capability exists, so it does not block, but it should be pinned in the briefing.

### [TK-8] AC2 demands the terse template within 2 seconds on 503/timeout, but OpenAICompatModel.complete only does bounded transport retries internally and exposes no wall-clock deadline; the 2s cap is the ComposeStage's responsibility and no timeout primitive is named.
**Why:** The 2s bound is testable only if the Stage wraps the async complete() call in its own timeout (e.g. asyncio.wait_for) and treats both the 503 exception and the timeout as the degrade trigger. Implementable in wombat, but unstated — a builder may rely on adapter-level timeouts that do not guarantee a 2s ceiling, risking a flaky AC2.

### [TK-9] Unit mismatch between the two layers. The wombat daily cap (AC1/AC3) is a TOKEN ceiling (daily_token_ceiling, '100 tokens'), but the cog-worx per-drive BudgetGuard/BudgetPolicy ceiling (AC4) is priced in USD and call-count (max_usd_per_drive / max_calls_per_drive), not tokens. The ticket presents them as inner/outer layers of the same budget, but they meter different units.
**Why:** Not a build-blocker, but the ledger has no token role on the inner layer and AC4's 'exceed the per-segment ceiling' test must be written against max_usd_per_drive/max_calls_per_drive, while AC1/AC3 assert on tokens. The builder should be told the two layers are unit-distinct so the AC4 test targets the right axis.

### [TK-21] AC5 ('pipeline runs end-to-end with no thresholds crossed' -> returns action=hold) presumes a threshold-comparison step exists in the skeleton, but the complexity_budget says 'no scoring logic' and non_goals say 'No trigger arms (TK-27)'. The ticket never specifies where the thresholds come from (a passed-in config? hard-coded constants? a no-op stub that always holds?).
**Why:** Ambiguity, not a blocker. The trivially-correct build is a skeleton whose pipeline always returns hold when no trigger arm is wired (arms are TK-27), so AC5 is satisfiable as 'default = hold'. But a builder could over-interpret AC5 as requiring real threshold evaluation and pull TK-27 scope forward. The simplest reading (always-hold default in the skeleton) should be the chosen one; worth a one-line clarification.

### [TK-23] The RatingParams vocabulary/schema (which keys exist, which are required) is unspecified in TK-23, yet AC-1 asserts the return is 'a RatingParams dict' and AC-3 tests behavior on 'a missing key'. AC-3's missing-key fallback is only buildable/testable once the required key set is defined.
**Why:** DEC-15 explicitly notes EP-10 'needs a versioned rating-parameter vocabulary up front'. The ambiguity is owned by the declared dependency EP-10, not undeclared, so it is not a build-blocker for TK-23 itself — but TK-23 cannot be implemented before EP-10 pins the vocabulary. Ensure EP-10's decomposition fixes the key set before TK-23 reaches the build frontier.

### [TK-24] Intent vs non_goals tension on the journal: intent says the proof uses 'the cog-worx journal as write-ahead authority,' but non_goals say 'use an in-process journal stub emulating write-ahead.' The real cogworx.substrate.journal.Journal is a (run_id, step_index)-keyed StageResult log and cannot natively store PendingSetAdd/Remove set-claims, so the stub is the only buildable path. The wording could mislead a builder into trying to bind the real Journal/InMemoryJournal double (whose commit_step takes a StageResult, not an arbitrary claim).
**Why:** For the spike this is only an ambiguity (the stub path is clearly scoped in non_goals and is self-contained), so it does not block the TK-24 build. But it signals that the persistence design this spike is meant to validate will not map onto the real cog-worx Journal seam without a wombat-owned WAL — a HIGH-value warning to carry into TK-25 (production, 'journal-backed', engineering_level: production), where the same claim 'cog-worx journal as write-ahead authority' would become a CRITICAL seam miss if taken literally.

### [TK-25] TK-25 is engineering_level=production and uses production operating parameters — flush_threshold=1.0 and MAX_PENDING (capacity cap) — but does not declare TK-13 (param store) in depends_on. The locked frame states 'production operating parameters live in a param store (TK-13/Q-19).' AC #1 bakes flush_threshold=1.0 and AC #2 references MAX_PENDING as if a constant.
**Why:** If production thresholds are meant to come from the param store, building TK-25 with hardcoded constants creates rework when TK-27 (which DOES depend on TK-13) wires the real param-sourced flush_threshold/ceiling. Either TK-13 belongs in depends_on, or the ticket should explicitly state thresholds are injected/owned downstream and TK-25 only computes the raw sum (cumulative_load) leaving threshold comparison to TK-27. Ambiguity about where the threshold lives.

### [TK-27] AC-1 requires item_kind to be 'carried through from the source item', which presumes the input GateItem already bears item_kind. TK-21 (per ISS-4) defines item_kind as a closed enum carried on the SURFACED ARTIFACT for consumer routing; it does not clearly state that the input GateItem carries item_kind, and TK-12 (item identity) keys on idempotency_key/ItemRef, not kind. Who stamps item_kind onto the GateItem upstream of the gate is unspecified.
**Why:** If GateItem does not already carry item_kind, the 'carried through from the source item' AC is unbuildable as written and the gate would have to derive/default a kind (the very private-vocabulary anti-pattern ISS-4 forbids). This is an undeclared upstream dependency on whoever populates item_kind on queue/gate items.

### [TK-27] TK-27's complexity_budget puts the per-class daily ceiling ledger on Postgres and builds it on the shared DailyLedger primitive (TK-152), whose own complexity_budget is 'one DailyLedger class over a Postgres table' with no in-memory variant. The locked frame's cold-boot default is zero-infra (cog-worx in-memory doubles), with real PG as a config swap (TK-14/Q-29).
**Why:** If TK-152 ships PG-only, the ceiling ledger TK-27 wires in cannot run on the zero-infra cold-boot path, conflicting with the cold-boot frame. This is primarily TK-152's gap, but TK-27 inherits and surfaces it: its ledger ACs (increment, CeilingHit-at-limit) presuppose a ledger backend that the cold-boot frame says must also work in-memory. Worth flagging so the in-memory/PG swap is provisioned at the DailyLedger layer before TK-27 builds on it.

### [TK-28] The decay pass operates over 'the pending set' and AC-3's kill proof relies on pending_set.rebuild_from_journal(); the PRODUCTION durable journal-backed pending set is TK-25, but TK-28 declares only TK-24 (the spike, in-process journal stub, 'No production Postgres schema'). TK-25 is not in depends_on.
**Why:** TK-24 explicitly is a spike with a stubbed journal and 'No surfacing-ledger integration'; the real artifact decay.py must call (durable pending_set.py + rebuild_from_journal over the production journal) is TK-25. TK-25 is reachable transitively (TK-27 -> TK-25), so this is not unbuildable, but the direct artifact dependency is undeclared, creating ambiguity about whether the kill proof runs against the spike stub or the production set.

### [TK-13] ACs reference modules/values the ticket asserts about but does not (and should not) build, with no declared dependency on the producers. AC3 asserts 'no competing hard-coded threshold/ceiling constant exists in the gate or ledger modules' (gate = TK-27/28, ledger = TK-9) and AC4 asserts 'the tuner does not write into this store' (tuner = TK-49). TK-13 depends_on only TK-1; it does not depend on TK-9/TK-27/TK-49. The intent acknowledges TK-27/TK-97/TK-9 are wired to depend on TK-13 (per Q-19 resolution), not the reverse, so a strict 'no competing constant' assertion is unverifiable until those modules exist.
**Why:** These assertions are correct as design intent but are not independently runnable at TK-13 build time — the gate/ledger/tuner modules may not exist yet (NORMAL: deps unbuilt). The test for AC3/AC4 must be written to tolerate absent modules (assert-if-present), or the assertion belongs in the consuming tickets. As stated, the AC could be read as requiring those modules to exist, which would be an undeclared dependency. Clarify the test is a guard that passes vacuously when the gate/ledger/tuner are absent.

### [TK-42] AC-1 references an artifact — 'a rating-parameter node for CALENDAR_CONFLICT' in user scope — that no ticket in scope produces. TK-41 defines the RatingParams vocabulary (in-memory, no I/O); the writer that persists per-event-class rating-parameter nodes (the RatingTuner) is future work and is not in depends_on. The decode from stored claims to a RatingParams object is also unspecified.
**Why:** The happy-path AC presupposes stored data whose producer isn't this ticket nor a declared dependency. Still testable via doubles by pre-seeding InMemoryEntityKG, so it is an ambiguity rather than a hard blocker — but the encode/decode contract between the future writer and this reader must be pinned or the two diverge.

### [TK-43] AC-2 accepts 'a TypeError OR a mypy error' for a motive predicate, while the complexity_budget states the wall is 'structural (type-checked), not a runtime string check.' A pure type-level wall (Enum/Literal membership) is enforced by mypy and would NOT raise a runtime TypeError; raising TypeError requires a runtime validator the budget appears to exclude.
**Why:** The AC and the budget pull the builder two different ways (runtime guard vs. type-only guard). It is still constructible because the OR lets either satisfy AC-2, but the contradiction is a real ambiguity that should be resolved so the build picks one mechanism deliberately.

### [TK-44] depends_on includes TK-41 (rating-parameter vocabulary), unrelated to writing behavior/outcome claims, and OMITS the substrate-provisioning dependency (sibling read ticket TK-42 declares EP-6 for cog-worx-scope access; TK-44 declares no equivalent). The dependency set is both over- and under-specified.
**Why:** Misleads the builder about what must exist first and obscures the genuine missing dependency (whatever constructs the user-scope ScopedKG + write token).

### [TK-44] 'ObservationWriter.record(claim)' implies a Claim-object API, but the seam is assert_fact(*, subject, predicate, obj, epistemic_type, source, created_by, ...) — a decomposed keyword surface that mints its own cog-worx Claim. The mapping from the wombat TK-43 Claim's fields (predicate/value/event_id/source/timestamp) onto assert_fact's args (subject? obj? object_is_entity? epistemic_type? SourceDeclaration?) is unspecified.
**Why:** The field mapping is non-trivial and load-bearing (where event_id and value land, what subject identity is used — TK-12 item identity is referenced by sibling TK-45 but not here). Left implicit, it invites inconsistent provenance and breaks the RatingTuner read contract.

### [TK-45] Label-set coherence: AC5 demands a TypeError for any label 'not in the closed Enum,' and AC1 writes OUTCOME_PENDING, so PENDING must be an Enum member. But TK-50's OutcomeSignal (the off-path producer) emits only LOAD_BEARING/REGRETTED/IGNORED — PENDING is never a valid TK-50 signal. The label Enum spans two disjoint roles (hot-path PENDING vs terminal signals) and the ticket does not state the split, risking an over-broad Enum that lets a caller pass PENDING off-path.
**Why:** Ambiguity that affects the closed-Enum test design and the TK-50<->TK-45 contract; resolvable with one sentence but currently unstated.

### [TK-50] The 'already-observable local state' the inference diffs against is under-specified for the calendar-conflict-disappeared rule (AC1). The calendar snapshot in scope (TK at contract line 1368) is a FRESH/live snapshot; nothing persists a history of prior snapshots. The 'what was originally surfaced' side is recoverable from the OUTCOME_PENDING claim (TK-45, keyed on canonical identity TK-12), but the engine must know WHICH conflicting event the item flagged in order to assert it 'no longer appears in the latest snapshot'. Whether that conflicting-event detail rides the PENDING claim / ItemRef is not stated.
**Why:** AC1 is checkable in a fixture, but the production data path (does the PENDING claim carry enough to diff against a fresh live snapshot?) is ambiguous. Likely resolvable by reading the claim subject, so MEDIUM rather than HIGH, but it should be pinned before build to avoid a mid-build discovery that the inference inputs aren't queryable.

### [TK-51] TK-3 and TK-161 both define the InputSource contract and both list src/wombat/sources/base.py in files_in_scope. TK-51 depends on TK-161 for InputSource while needing TK-3 for the registry.
**Why:** Two tickets owning the same file/contract risks two divergent InputSource definitions; which is authoritative is unstated. TK-51 building against TK-161's InputSource while registering on TK-3's SourceRegistry could mismatch if the two definitions drift.

### [TK-51] Circular integration assertion between TK-51 and TK-50. TK-51 AC-3 asserts an end-to-end result (useful->LOAD_BEARING, not_useful->REGRETTED) realized by TK-50's engine consuming the queued signal, yet TK-50 depends_on TK-51.
**Why:** TK-51's own acceptance pass cannot run this end-to-end check without TK-50, which by dependency order is built after TK-51. The AC is therefore not a runnable check within TK-51's pass. It should move to TK-50, or be reframed as a contract test asserting the FeedbackSignal(item_ref, useful|not_useful) is durably enqueued in the shape TK-50 expects.

### [TK-46] DreamPathway re-registration on cold resume is an unstated precondition. Engine.resume / fire_timer rehydrate the graph from PathwayRegistry using the run's stored pathway_id, and resume raises PathwayError if the pathway is not registered at startup. AC2 (kill + restart -> resume) therefore silently REQUIRES bootstrap.py to register the DreamPathway in the registry on every cold boot. files_in_scope includes bootstrap.py, so it is implied, but no AC asserts 'after restart the DreamPathway is re-registered before the sweeper fires' — the resume AC could pass in-process and fail cross-process if registration is missed.
**Why:** A cross-process resume that omits re-registration fails with PathwayError; making this an explicit precondition/AC avoids a rework loop and matches cog-worx's documented honest requirement.

### [TK-47] Naming/semantics drift: the ticket says the extractor 'extract[s] patterns from overnight logs'. ClaimExtractor extracts factual subject/predicate/object claims from journal session turns into the entity KG — it is not a 'pattern/insight' extractor. 'merge/deduplicate memory' maps to the reconciler's contradiction-adjudication/defeat, not literal node dedup.
**Why:** Ambiguity will mislead the builder about what the stage actually produces and what AC1's 'duplicate/conflicting user-scope nodes merged' should assert.

### [TK-49] Undeclared direct dependency on TK-41 (RatingParams vocabulary). TK-49 directly constructs and writes RatingParams and references the field 'urgency_base', but TK-41 is not in depends_on (only transitively via TK-42->TK-41).
**Why:** The type TK-49 reads and writes is owned by TK-41; depends_on should name it. Low build risk (TK-41 is a leaf with no deps, likely built early) but the dependency graph is inaccurate.

### [TK-49] AC1 names a specific field 'urgency_base' on RatingParams, but TK-41's acceptance_criteria do not enumerate RatingParams fields (they only assert 'all required fields' with defaults). Whether 'urgency_base' exists is assumed, not contracted.
**Why:** If TK-41 lands with differently-named fields, TK-49's AC1 ('updated urgency_base ... is written') fails on a name mismatch — a coherence gap between the two tickets' contracts.

### [TK-71] AC-3 requires credentials 'stored encrypted (not plaintext)' but neither TK-71, its deps (EP-2/EP-3), nor the named frame elements specify the encryption key provenance. The frame names a param store (TK-13/Q-19) for production operating parameters and 'local-first storage', but TK-71 declares no dependency on TK-13 and names no key source (OS keyring vs. derived local key vs. param-store secret).
**Why:** engineering_level is 'production'. 'Encrypted at rest' with no declared key-management source is an ambiguity a builder must resolve by guessing. It is buildable with a self-contained scheme (e.g. an OS keyring or a generated local key file), so it does not block a build pass, but it risks rework if the intended key source is the param store and that linkage was meant to be declared.

### [TK-72] event_to_queue_item / SourceRegistry conformance is named but the exact InputSource contract method surface (id/poll vs push, event_to_queue_item signature) is defined in TK-3 (EP-3), which is correctly a dependency, but TK-3 is still todo and the mapping-function name in the budget ('event_to_queue_item') must match TK-3's protocol exactly (TK-3 budget says 'event->QueueItem mapping'). Minor naming-drift risk between this ticket's complexity_budget and TK-3's protocol.
**Why:** If TK-3's protocol method is named differently (e.g. map_event), the poller won't conform without an adapter. Not a blocker because TK-3 is a declared dep, but the builder should pull TK-3's exact signature, not invent it.

### [TK-75] 'SourceRegistry-conformant' in the complexity_budget collides with the real cog-worx class cogworx.knowledge.source_registry.SourceRegistry, which has entirely different semantics (provenance source-identity, not an input-source poll/push/map contract). The intended contract is wombat's EP-3 one.
**Why:** A builder may wire or subclass the wrong (cog-worx) SourceRegistry. Constructible once the term is disambiguated to the EP-3 wombat input-source contract; recommend renaming the reference (e.g. 'EP-3 InputSource contract') to avoid the namesake trap.

### [TK-76] AC-4 ('no triage invocation appears on the hot-path trace; the test MUST fail if triage is called inline with queue drain') asserts an absence, but the ticket never specifies the off-path invocation seam — who calls triage_message(), or where the off-path scheduler/harness lives. The off-path host is EP-13 (an epic, declared in depends_on), but no concrete invocation contract is named, so 'is triage on the hot path?' has no defined surface to trace against beyond 'it is not wired into the EP-4 drain Stages.'
**Why:** The negative test is constructible (assert triage_message is not imported/called from the drain Stages, or assert it executes only under the EP-13 timer pathway), but the AC's phrasing ('invoked during a hot-path drain cycle ... traced') implies an instrumentation/trace mechanism that no artifact in scope provides. A builder must improvise the trace strategy; this is ambiguity, not a hard blocker.

### [TK-77] AC-3 requires the taint latch to be ACTIVE for the duration of extract_tasks(), but the ticket provides no seam to assert it in its own drive. The latch is per-drive (cog-worx seeds TaintState per RunContext; TK-148 wires it on the untrusted-body READ phase in the INGEST drive). TK-77 runs OFF-PATH in the nightly consolidation pass (EP-13), a separate drive from TK-75's ingest drive. A latch set in TK-75's drive does not carry into TK-77's extraction drive, and TK-77's files_in_scope contains no taint.py call-site. It is unspecified whether TK-77 re-reads the raw body (re-tainting its own drive) or operates on an already-tainted carried item.
**Why:** Q-29-class seam-provisioning ambiguity: the AC asserts a structural safety property the ticket gives no mechanism to satisfy. A builder cannot know whether to add a TAINT_UNTRUSTED tag-site in the extractor's drive (which would mean TK-77 must depend on TK-148's tagging convention beyond just the latch existing) or to inherit taint some other way. Ambiguity will surface as a failing-but-unguided test.

### [TK-80] Undeclared dependency on the trusted-output tagging convention owner. AC1 and AC3 require the emitted ReplyIntent to 'carry the trusted-output tag' so TK-78's outbound drive stays untainted, but that BOTH-DIRECTIONS tag convention is owned/established by TK-148 (Q-11 resolution), which TK-80 does not list in depends_on. The path is transitive (TK-75 depends_on TK-148), so the machinery exists at build time, but the tagging-convention contract TK-80 must conform to is not a declared dependency — a builder of TK-80 in isolation could emit the artifact without the trusted-output tag and silently break the untainted-outbound guarantee TK-78's positive AC rests on.
**Why:** If the convention is not surfaced to the TK-80 builder, the ReplyIntent could be emitted untagged; cog-worx then either taints the outbound drive (broken feature) or, worse, the gap goes unnoticed until TK-78 integration. Declaring depends_on TK-148 (or referencing its tag convention explicitly) closes it.

### [TK-78] ISS-3 negative AC (AC2) assumes a ReplyIntent carrying raw untrusted body text will STRUCTURALLY taint the outbound drive and force a gmail.drafts.create refusal. But cog-worx taint (TaintState, capability/policy.py) fires only on external-tier-without-trusted-output OR a 'untrusted-source'-tagged capability dispatch — NOT on a drive merely holding/receiving a string that contains raw body content. Per CF-3.2-B, untagged read-tier ingestion does NOT taint, and tagging untrusted sources is the integrator's obligation. So 'the raw body leaked into the ReplyIntent' will not auto-taint unless the leak path actually re-dispatches a capability tagged untrusted-source. The negative test as worded ('the outbound drive attempts dispatch -> drive is tainted') is only satisfiable if the build wires an explicit sanitization/tag check that taints (or refuses) on detecting an unsanitized artifact. That mechanism is implied but not provisioned by TK-78 or a named dep.
**Why:** If the builder assumes taint fires automatically from the artifact content, AC2 will be unimplementable as written and the negative safety test cannot pass without adding an unbudgeted sanitization-boundary enforcement (a check that re-tags/refuses on raw-body presence). The fix is small (assert the sanitization boundary in wombat, e.g. the upstream ReplyIntent producer tags or the composer validates) but it must be named — this is precisely a Q-29-class 'capability the AC relies on that nothing explicitly builds' seam.

### [TK-79] TK-79 requires the action trail to be set to status 'approved-for-send' (on approve) and 'cancelled' (on reject), but the producing ticket TK-146 (action_trail_projection schema + writer) only enumerates status values 'pending' and 'dispatched' in its ACs (contract lines ~2392, 2401). The 'approved-for-send' and 'cancelled' statuses TK-79 depends on are not defined in the schema ticket that builds the writer.
**Why:** A clean build of TK-79 needs writer.py / project_action_to_trail to accept these status transitions and the schema's status column/enum to permit them. If TK-146 ships with only {pending, dispatched}, TK-79 will force rework on TK-146's schema or invent an out-of-band status. This is the Q-29 class of gap: a capability an AC needs (set trail status='approved-for-send'/'cancelled') is not provisioned by the ticket that owns the artifact.

### [TK-79] depends_on lists EP-27 (the epic container) rather than its concrete child TK-146, which actually builds the trail writer and schema (src/wombat/trail/writer.py) that TK-79's 'updates the action trail to approved-for-send (or cancelled)' requires.
**Why:** Depending on the epic rather than the producing ticket leaves the build-order edge imprecise — the capability is provisioned (TK-146 is EP-27's sole trail-writer child, so not unprovisionable), but the dependency is not honest at ticket granularity. Tightening to depends_on TK-146 (alongside or in place of EP-27) makes the seam explicit and surfaces the status-value gap above.

### [TK-96] DetectConflicts stage has no home: the intent's chain is Gather->DetectConflicts->ForceFlush->Compose->Deliver and AC-1 expects a 'conflict note (if conflicts exist)', but files_in_scope has no detect-conflicts module; the non_goal only waives real conflict-alternatives computation (stub returning empty). A stub still needs a file/seam to live in.
**Why:** Ambiguity about where the conflict-detection stub stage is wired (inline in brief_pathway.py vs. a missing stage file) forces a mid-build decision; minor relative to the dependency issues but still unspecified.

### [TK-97] AC2 (miss-catch after sleep across midnight) couples two independent mechanisms whose interaction is asserted but not localized to a single owned unit: the Sweeper fires a DUE timer (wake_at in the past), while 'fire exactly once, not twice' across a midnight sleep depends on TK-152's access-time wombat_today() plus this ticket's ledger fence (AC3). The cog-worx layer guarantees exactly-once ADVANCE past a given Wait, NOT that a re-armed next-day Wait won't also fire — only the DailyLedger 'today' check prevents the second brief. The ticket states this correctly in prose, but the AC's 'fires once, not zero or twice' is only testable end-to-end once both TK-152 and the ledger-skip (AC3) are wired, which is a slightly heavier integration test than the AC text implies.
**Why:** Not a contradiction — the design is sound — but a builder could implement AC1/AC2 against the Sweeper alone and believe it passes, while the double-fire protection actually lives in the AC3 ledger fence. Flagging so the build pass treats AC2 and AC3 as one integrated test (timer + ledger), not two.

### [TK-98] AC wording 'BriefGatherStage.run() is called ... emits a BriefPayload' contradicts the Stage seam signature run(ctx: StageContext) -> StageResult. The BriefPayload is not a return value; it is carried in the returned result's Artifact.output (Artifact.data dict).
**Why:** Ambiguous about the call shape and return type a builder/tester must implement. A direct unit test of run() must pass a StageContext (or a double) and assert on the returned StageResult.output, not on a bare BriefPayload — the AC as written invites a test that cannot compile against the Protocol.

### [TK-98] The ticket never names how the BriefPayload is wrapped for journaling — Artifact (kind/produced_by/provenance/data) is the only journalable output type, and BriefPayload must serialize into Artifact.data:dict[str,Any]. files_in_scope lists only brief_gather_stage.py + brief_payload.py.
**Why:** The provenance-bearing Artifact wrapping and the kind/item_kind stamping are unstated build steps; without them the payload cannot be committed or pulled by downstream stages via ctx.last_output. A builder may model BriefPayload as a free-standing object and discover at integration time it must be Artifact-packed.

### [TK-99] Surfacing-class ambiguity: a load-triggered surface_flush (TK-27) and the morning brief's forced flush (TK-99) may both fire, and it is undefined whether they count as the same surfacing class against the daily ceiling. This is logged in governance (contract line ~2948) but unresolved and touches TK-99's correctness.
**Why:** If the forced brief flush double-counts or collides with TK-27's load flush, the 'exactly one consolidated brief per morning' goal can be violated, and TK-99's interaction with the per-class ceiling ledger is left to build-time guesswork.

### [TK-100] The acceptance criteria say the stage calls the mouth 'via EP-5/S4 behind the BudgetGuard', but TK-100 does not declare HOW the BudgetGuarded mouth instance reaches this Stage. In cog-worx, BudgetGuard is stateful and one-shot per drive segment (BudgetPolicy.new_guard per CANON S11), and BudgetGuardedModel must be assembled with that fresh guard. Whether the guard-wrapped Model is injected via StageContext, resolved from the ModelRegistry inside run(), or assembled by a caller is unspecified.
**Why:** This is a wiring seam between EP-5 (registration) and TK-100 (consumption). It is undeclared rather than non-existent — all the cog-worx primitives exist (ModelRegistry.assemble(profile, guard=...), BudgetGuardedModel, BudgetPolicy.new_guard). It will not block the build but may force a small rework of the run() signature / ctx contract if the per-drive guard plumbing is decided differently than the builder assumes. Recommend pinning the injection contract (likely: guard-wrapped Model obtained from ctx/registry per drive) when decomposing.

### [TK-101] EP-30 speak-event interface is unspecified. AC2 requires 'spoken once via EP-30 with no words added — spoken content matches BriefText exactly', but no speak-event signature/contract is defined in EP-30 or any referenced node. The builder cannot write the AC2 call or its prompt-content assertion without inventing the interface.
**Why:** Forces the builder to guess the EP-30 boundary. Mitigated (not blocking) by AC3's text-only degrade path, which lets text delivery ship before EP-30 exists.

### [TK-111] Parallel/duplicate behavioral record. EP-11/EP-12 (TK-44/TK-45/TK-50) already persist behavior+outcome as provenance-typed claims in the cog-worx Neo4j user scope; TK-111 stands up a SECOND standalone Postgres table (wombat_behavior_events) holding the same event_type/outcome_label data. No decision/open-question reconciles the two stores or names which is authoritative.
**Why:** Same data in two stores keyed differently risks divergence and double-write coherence bugs. The duplication is implied by complexity_budget ('one Postgres table') but is undeclared and unjustified relative to the existing claim corpus.

### [TK-111] Row-provenance / input contract underspecified. AC1 source is 'queue items that have outcome labels (EP-12)', but EP-12's terminal labels are produced off-path nightly by TK-45 (OutcomeLabeler) / TK-50 (OutcomeSignal) keyed on TK-12 identity. TK-111 depends on the EP-12 epic but not on the concrete producer tickets or the identity ticket that define the shape it consumes.
**Why:** The writer's input record (what an 'item with an outcome label' looks like, and how to key it) is the load-bearing contract for this ticket and is left to inference. This is the Q-29 failure pattern: a required upstream shape not provisioned by a declared dependency.

### [TK-112] Undeclared dependency: AC #2 calls UserModel.ratings_for() (owned by EP-10 / TK-42) but depends_on lists only TK-111, EP-11, EP-13 — EP-10/TK-42 is absent.
**Why:** Even setting aside the misuse, invoking a capability from a ticket not in depends_on hides a real ordering/seam dependency from the build frontier.

### [TK-112] Persistence-shape ambiguity: TK-112 says the nightly Stage writes WindowSummary(start_utc, end_utc, event_count, switch_rate, outcome_mix) 'into the user scope (EP-11)', but EP-11's only write seam (TK-44 ObservationWriter.record) accepts a Claim constrained to the motive-free predicate Enum (TK-43). There is no defined predicate/encoding for a structured window summary, and outcome_mix is a derived aggregate that may not fit the BEHAVIOR_OBSERVED/OUTCOME_LOAD_BEARING vocabulary.
**Why:** The builder has no defined way to serialize a WindowSummary through the EP-11 claim wall. Ambiguity will force an ad-hoc encoding decision or a new predicate the schema wall (TK-43) does not allow — risking either rework or a CON-6/NG-1 violation.

### [TK-113] Undeclared dependency on TK-21 / ISS-4 (the canonical item_kind enum). The enqueued payload must carry the canonical item_kind so TK-10/TK-114 can route it, yet depends_on lists EP-9 (the gate) but not TK-21 or the enum source.
**Why:** Without conforming to the closed enum that TK-21/ISS-4 owns, the item is unroutable downstream. The dependency is real but undeclared, so a build pass could implement an inconsistent kind value (see HIGH gap) without the contract flagging it.

### [TK-113] 'Exactly one per night / never two or more' is asserted but the dedup/selection mechanism is unprovisioned. AC4's payload is pattern_id/window_ref/date with no idempotency_key specified; WombatQueue dedup (TK-1) keys on idempotency_key=f(source_id,natural_id) per TK-12/Q-18.
**Why:** The 'one per night' guarantee leans on an idempotency derivation (what are source_id/natural_id for a reflection item?) that no AC pins down, and if multiple nudge-worthy patterns exist there is no specified tie-break/selection rule to choose the single one. The Stage cannot be built to a runnable 'exactly one' check without this being specified.

### [TK-114] AC4 ('KB phrasing hints are absent / contributor not wired -> safe default prompt') overlaps and partly contradicts TK-118 AC2 (empty hint list -> safe default) and is possibly unreachable. If hints arrive only via the assembler slot (TK-118), TK-114 cannot itself detect 'contributor not wired' — there is no in-Stage signal for absence. The 'absent hints' branch needs a defined detection mechanism (empty slot? empty list param? missing key?).
**Why:** Ambiguity that determines whether AC4 is testable at all; resolution depends on closing the HIGH gap above.

### [TK-116] gate_condition evaluation semantics are underspecified. TK-115 defines gate_condition as a structured object {metric, operator, threshold}, but TK-116 AC-1 writes the example as an expression string ('switch_rate > 0.6'). The function must resolve 'metric' against the pattern_metrics dict and map 'operator' to a comparison. The set of supported operators and the behavior when a metric key is absent from pattern_metrics are not stated.
**Why:** Constructible but the builder must invent the operator-dispatch table and the missing-metric rule. The safe reading (missing metric => entry does not match => contributes toward False) aligns with CON-3 silence-default, so this is ambiguity, not a blocker.

### [TK-116] Multi-entry matching semantics unstated. KB is a list[KBEntry] but the function returns a single bool; the ACs never say whether it is 'True if ANY entry's gate_condition matches' versus tied to one specific entry.
**Why:** AC-2 ('satisfying no entry's gate_condition -> False') and AC-4 (fixture with mixed results) imply an any-match disjunction over the KB list, which is buildable by inference, but stating it removes a guess and pins the deterministic fixture test.

### [TK-117] AC #3 references an artifact TK-117 does not build and does not depend on: TK-114's 'final rendered observation'. Verifying 'no hint string appears verbatim in the final rendered observation' requires the ReflectionCompose Stage (TK-114) and the mouth (EP-5). TK-117 only returns list[str]; it has no rendered output to assert against. The guarantee is real and important but belongs to TK-114 (which already has the equivalent AC #4) and TK-118 (also AC #4).
**Why:** A builder of TK-117 cannot write a runnable check for this AC from this ticket alone — it would force pulling in TK-114+EP-5, contradicting the S/mvp 'one pure function' complexity_budget. Leaving it here makes the ticket falsely look unable to be 'done' until downstream tickets land.

### [TK-117] AC #4 (linter forbidding clinical/directive/motive language across 'any hint string in any KB entry') tests TK-115's KB data, not TK-117's extractor. TK-115 already owns the human-authored, clinical-language-free KB and its own linter AC. The phrasing_hints content originates and is validated in TK-115; TK-117 only reads it.
**Why:** This AC adds scope TK-117 does not produce (it builds no KB entries and no KB linter beyond what TK-115 defines). A builder could waste effort standing up a second linter, or the AC sits unsatisfiable because the data under test is owned elsewhere. Relocate to TK-115.

### [TK-118] Contributor contract is async (async def contribute) but TK-117's extract_phrasing_hints is a pure sync dict lookup. Minor, but the ticket's prose ('the contributor is invoked') does not flag the async boundary or where pattern_id resolution happens relative to the async call.
**Why:** Low rework risk but an ambiguity the briefing should pin so the implementer wraps the sync TK-117 call correctly inside the async contribute and does not raise (the Protocol says contributors SHOULD NOT raise; return SlotContent(status='degraded') instead) -- relevant to AC2's empty-hint-no-error path.

### [TK-131] AC-2 'any Stage resolves it by capability id' implies a resolve-by-id API; the actual Registry API is get(name) keyed on capability.name, with no separate id and no resolve method.
**Why:** Not a blocker (get(name) covers it) but the AC should be pinned to Registry.get(name) so the runnable check targets the real method and doesn't invent a 'capability id' the registry lacks.

### [TK-132] Sync-vs-async dispatch shape is unresolved upstream and TK-132's ACs presume it is settled. cog-worx Capability.invoke is async (base.py:34), but TK-131's complexity_budget specifies a 'sync open/close/navigate/snapshot API' and its poc ('Does the external-tier Capability contract accommodate a stateful long-lived session cleanly, or does the lifecycle API need a wrapper?') is status:open with result:null.
**Why:** TK-132 AC1/AC2 ('a type action is dispatched ... the subsequent a11y snapshot reflects the new value') assume a concrete dispatch+snapshot surface. If TK-131's POC resolves toward async invoke or a session wrapper, TK-132's action signatures and the in-scope file (playwright_capability.py) change shape, forcing rework. Lives inside the declared dependency, so it is a normal just-in-time resolution item, not an undeclared/unprovisionable dep — but worth pinning before building TK-132.

### [TK-132] The action-dispatch contract is undefined: whether click/type/select are separate methods, separate registered capabilities, or one invoke({'action': 'type', ...}) discriminated payload. TK-131 establishes a single Capability with navigate/snapshot but does not pin how multiple actions are addressed, and TK-132 adds three more plus screenshot without specifying the dispatch convention.
**Why:** Affects input_schema design and how a Stage selects an action; ambiguity could yield an inconsistent API across TK-131 and TK-132. Resolvable at build time within files_in_scope; not a blocker but should be decided once, not twice.

### [TK-133] AC-2's 'taint flag (True)' has no public StageContext accessor. StageContext exposes the gate only privately (self._gate); there is no public property returning gate.taint.tainted. Downstream stages read taint structurally via the shared gate, not via a value in this Stage's record.
**Why:** The Stage can emit taint=True as a constant (acceptable for mvp), but the AC conflates an in-record bookkeeping flag with the engine's drive-level taint register. Worth clarifying so the builder does not try to read a non-existent ctx.taint property.

### [TK-134] AC3 (HTTP 404 -> structured error, no exception propagates) may assert a capability TK-133 does not clearly produce. TK-133 AC4 only covers 'URL unreachable OR a11y snapshot empty' -> structured error. A 404 commonly returns a valid, NON-empty a11y tree (a server error page), so it falls into neither of TK-133's named error conditions and could be treated as successful content rather than a structured error.
**Why:** A contract test must assert behavior the unit-under-test actually guarantees. If TK-133 does not classify HTTP-status-failure (vs. transport-unreachable / empty-snapshot) as a structured error, AC3 tests a capability nothing builds, and the test would fail until TK-133 is amended to recognize non-2xx status as an error path. Tighten by adding an explicit non-2xx-status error branch to TK-133's AC or rewording TK-134 AC3 to use an unreachable URL.

### [TK-135] The 'approval token' / 'approved journal entry' that the dispatch_approved Stage checks is not defined by any named artifact. The two-stage pattern relies on the dispatch stage correlating the human-input payload back to the specific journaled action record, but neither TK-135 nor TK-146 (action-trail schema) specifies the correlation key (e.g. action_id) that proves THIS approval authorizes THIS submission.
**Why:** Without a defined approval->action correlation, a builder could wire approval generically (any human-input resumes the run) and silently weaken the per-action guarantee. TK-146's action_trail row has action_id; aligning the AwaitHuman payload to carry/echo that id should be stated. Ambiguity, resolvable in the briefing.

### [TK-136] Undeclared dependency on TK-132. AC-3 requires that 'any code path attempts to type into a password-type input' be blocked by a Capability guard. The Playwright type/click/select interaction actions (the thing being guarded) are introduced in TK-132, not in TK-131. TK-136 declares only TK-131. Its sibling TK-135 (same EP-26, same pattern) correctly declares TK-132.
**Why:** If TK-136 is built against TK-131 alone there is no type action to guard, so AC-3's 'guard the password-field type action + bypass-proof test' cannot be exercised. This will force rework or a hidden cross-ticket reach. Add TK-132 to depends_on (or move the guard to TK-132).

### [TK-146] action_id provenance is unspecified. The row carries action_id and TK-147 + AC4 both make it load-bearing (idempotent status update keyed to the action; dedup by action_id). Nothing in TK-146 or EP-4 defines who mints action_id or whether it reuses the frame's idempotency_key=f(source_id,natural_id) (TK-12/Q-18) or the cog-worx journal step id.
**Why:** Without a defined, stable action_id, AC4's 'no new row is created (idempotent)' and TK-147's dedup are not checkable; ambiguity will surface as rework when TK-149 wires the call sites.

### [TK-146] human_summary authorship is undefined. Intent says 'wombat-authored structured record / plain description'; AC3 requires verbatim speak text, but for draft/form actions nothing specifies whether human_summary is produced by deterministic templating (consistent with NG-4 gate-determinism) or by the phrasing mouth. The writer's input contract (what fields the caller passes vs what the writer composes) is unstated.
**Why:** The writer's signature and the call-site contract are under-specified; if human_summary requires phrasing it crosses NG-4 / the ASMP-1 egress boundary, which would be a frame conflict — needs to be pinned to deterministic composition.

### [TK-147] The renderer's trigger/lifecycle is unspecified: AC2/AC3 say 'the renderer picks up the status update' and AC4 covers restart, but nothing states whether the renderer polls the projection, is invoked inline by the dispatch Stage, or tails the table. The dedup/append guarantees depend on this model.
**Why:** Ambiguity that affects how AC2/AC3/AC4 are implemented and tested (e.g. how dedup state is persisted across restart). Buildable but invites divergent implementations; should be pinned to keep the 'runnable check' deterministic.

### [TK-148] AC5's full path (a tainted drive 'attempts to use an external Capability ... cog-worx raises ...') presumes the external tier is admitted for that stage. Per DEC-20/TK-151, external tier is opt-in per-stage and inert until Jim's cog-worx sign-off; the engine default is {read,write}. A stage that never admitted external would raise TierViolation for a baseline reason (external not in default tiers) rather than specifically because of taint, making the test assert the right exception for the wrong reason unless the test first admits external then taints.
**Why:** Without pinning the policy (allowed_tiers includes external, taint_drops_external True) the AC5 test does not actually prove the TAINT dropped the tool vs. the tool never being admitted. Ambiguity that will produce a test that looks green but does not isolate the taint behavior. TK-151/DEC-20 is an undeclared coherence dependency for AC5's semantics.

### [TK-149] AC-3 (reject path) requires setting the trail row status to 'cancelled', but TK-146 (the trail-schema dependency) defines the status lifecycle as pending -> dispatched and its idempotency AC only covers the dispatched transition. 'cancelled' is not an enumerated status in the depended-on schema.
**Why:** An AC writes a status value the schema ticket does not declare; either TK-146's status enum must include 'cancelled' or TK-149 silently extends the schema — an undeclared cross-ticket contract gap that forces rework or a migration.

### [TK-150] Bidirectional coupling with TK-14 not reflected in depends_on. The Q-29 resolution and TK-14's complexity_budget state TK-14's real-adapter factory 'does not BYPASS TK-150's residency check (it passes its config through it)' via build_substrate(settings, residency_check=...). TK-14 thus imports/calls TK-150's LocalResidencyGuard, but TK-150 depends only on the coarse epic EP-1 (TK-14's parent), not TK-14 directly, and the check()/residency_check signature contract is pinned by no ticket.
**Why:** Undeclared fine-grained dependency on a shared seam; if TK-150 and TK-14 disagree on the residency_check signature, integration rework follows.

### [TK-151] DEC-22 reframes the prior 'Jim sign-off' (DEC-20, a real cog-worx docstring note at policy.py:80/103) as a 'build-time deploy-config enablement'. Since no such config knob exists, the bring-up step TK-151 'performs' is actually 'edit a StageToolPolicy / the DEFAULT_TOOL_POLICY constant' — which is exactly the per-stage binding, not a separate ops step. The ticket presents two distinct gates (bind + enable-flag) where the seam has one.
**Why:** Implementer ambiguity: the ticket will read as needing two artifacts when only one mechanism exists, inviting either a no-op 'flag' shim or a global DEFAULT_TOOL_POLICY edit (which would violate NG-5/CON-5 'never global').

### [TK-161] Title/AC mismatch: ticket title and intent promise a typed 'SourceRegistration dataclass', but neither acceptance criterion mentions SourceRegistration. ACs only assert the InputSource Protocol exists and is dependency-free. The actual deliverable artifact (Protocol? dataclass? both?) is underspecified.
**Why:** A builder cannot tell whether SourceRegistration is a required separate type or just prose; the 'done' check is ambiguous, risking an under- or over-built result.

### [TK-161] AC2 references the SourceRegistry registration capability ('registers a new source', 'no wombat core file needs editing to add a source') but TK-161 builds no registry and does not depend_on TK-5 directly (only on the EP-3 epic). The criterion's checkable behavior lives in TK-5's SourceRegistry, not in this one-file ticket.
**Why:** The acceptance criterion cannot be proven by the artifact TK-161 produces in isolation; it leans on an undeclared concrete dependency (TK-5), making the 'then' unverifiable within this ticket's scope.

### [TK-162] SourceRegistry.list_sources() is referenced by AC3 but not defined by any ticket. TK-5's ACs never introduce list_sources(); it is asserted as existing here and in the feedback ticket.
**Why:** Ambiguity/missing artifact: the introspection method the test calls has no owning ticket. Cheap to add to the registry contract, but undeclared today, so the AC is not currently checkable.

### [TK-163] SourceRegistry.enqueue() is an undeclared method. TK-163 (AC1 + complexity_budget), TK-162 (AC1), and TK-50 (line 1194) all reference 'SourceRegistry.enqueue()' as a shared public call site. TK-3, which defines SourceRegistry, declares only start()/stop()/poll() and performs enqueue internally within the poll loop; no public enqueue() method is specified anywhere in the contract.
**Why:** The named seam does not exist as declared. Buildable (add the method), but until TK-3's API surface is reconciled to include a public enqueue() the call site three tickets assert is not guaranteed. Pin the SourceRegistry public API (does enqueue() exist and is it the uniform call site?) so these tests target a real method.

### [TK-164] Source of the voice_enabled flag is unprovisioned. AC-1/AC-2 gate behavior on 'voice_enabled=true/false', and ISS-4 says speak-vs-text is 'a SINK concern keyed off a surface_* action + a voice_enabled flag', but no ticket is named as the producer/owner of voice_enabled. It is unclear whether the flag is read from the param store (TK-13/Q-19), from config, or carried on the artifact — and which ticket defines it.
**Why:** Builder must invent where voice_enabled comes from, risking a different read-path than the rest of the system expects (e.g. hardcoded vs param store). Name the source (param store or delivery config) and, if external, declare the dependency.

### [TK-164] Internal vocabulary inconsistency: AC-3's 'given' clause still says 'called with a speak-event artifact' even though the complexity_budget and ISS-4 both state there is NO 'speak-event' gate action (the canonical enum is hold|surface_immediate|surface_flush). Same stale wording leaks into TK-165 AC text via the surface_* rewrite.
**Why:** Leftover pre-ISS-4 language. A literal builder could try to branch on a non-existent 'speak-event' action. Low build risk but a real contradiction inside the ticket; should read 'a surface_* artifact with voice_enabled=true'.

### [TK-165] The canonical GateAction/item_kind enum that TK-165's tests must construct (action=surface_immediate, surface_flush, hold per ISS-4) is defined on TK-21, but neither TK-165 nor its sole dep TK-164 declares TK-21 in depends_on. TK-164 depends only on EP-9/EP-5.
**Why:** To build artifacts keyed on the canonical closed enum (ISS-4 mandates a single shared GateAction and asserts no module defines a competing surface/hold string set), the test fixtures should import that enum rather than re-declare string literals — otherwise the test risks reintroducing exactly the private-enum drift ISS-4 was resolved to prevent. The missing TK-21 edge is properly TK-164's to declare (it consumes the enum), so for TK-165 in isolation this is a transitive note, not a self-contained blocker — hence MEDIUM, not HIGH.

## Not-ready tickets (45)

TK-152, TK-4, TK-6, TK-7, TK-10, TK-8, TK-9, TK-27, TK-28, TK-13, TK-42, TK-44, TK-45, TK-50, TK-51, TK-46, TK-47, TK-49, TK-72, TK-75, TK-77, TK-96, TK-97, TK-98, TK-99, TK-101, TK-111, TK-112, TK-113, TK-114, TK-118, TK-131, TK-133, TK-134, TK-135, TK-146, TK-147, TK-148, TK-149, TK-150, TK-151, TK-161, TK-162, TK-163, TK-164