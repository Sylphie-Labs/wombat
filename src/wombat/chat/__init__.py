"""wombat.chat — the runtime chat surface (TK-222, EP-32, Q-110(d)).

``surface.py`` owns the loopback-only stdlib HTTP transport (``ChatSurface``) and the reply
correlation seam (``ChatReplyBroker``) — see that module's docstring for the full design.
"""
