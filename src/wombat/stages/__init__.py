"""wombat.stages — cog-worx Stage implementations for the drain pathway (EP-4).

``artifacts.py`` defines the spine-wide inter-stage Artifact convention (kind constants + the
QueueItem<->Artifact.data serialization helpers) that every stage in this package shares.
"""
