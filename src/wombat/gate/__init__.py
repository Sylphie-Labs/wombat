"""wombat.gate — the deterministic interruption gate (no model calls, ever; DEC-13, S9).

This package owns the ONE canonical decision vocabulary (``GateAction`` / ``ItemKind``)
and the accumulate -> score -> trigger -> decay pipeline shape (TK-21).
"""
